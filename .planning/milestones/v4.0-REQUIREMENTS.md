# Requirements Archive: v4.0 Canvas API Bridge

**Archived:** 2026-04-09
**Milestone:** v4.0 Canvas API Bridge (Phases 18–19)

---

## v4.0 Requirements (All Validated)

- [x] Canvas iframe postMessage JSON-RPC ブリッジ — iframe 内 JS から POST /api/iframe-rpc 経由で DB クエリ（SELECT）と AI（Copilot ワンショット）を呼び出せる仕組み — **v4.0 ✓**
- [x] Canvas アプリ独立ホスティング — GET /apps/{app_id} で Canvas アプリをスタンドアロン URL で公開、parent-bridge.js 共通リレー — **v4.0 ✓**

## Traceability

| Requirement | Phase | Plan | Status |
|-------------|-------|------|--------|
| Canvas iframe postMessage JSON-RPC ブリッジ | 18 | 18-01, 18-02, 18-03 | Complete |
| Canvas アプリ独立ホスティング | 19 | 19-01, 19-02 | Complete |

## Requirement Outcomes

### Validated as Designed

- **Canvas iframe JSON-RPC ブリッジ**: 設計通り実装完了。SELECT-only SQL ガード・ChatCopilot ワンショット・JWT Cookie 認証すべて機能確認済み。
- **Canvas アプリ独立ホスティング**: `GET /apps/{app_id}` 動的シェル実装。parent-bridge.js 共通化により CanvasPane と Shell 両方でリレーロジックを共有。

### Notable Adjustments During Development

- **D-07 (JWT auth)**: 当初 `auth_manager.load_token()` でサーバー共有トークンに変更 → Plan 02 動作確認で問題判明 → JWT Cookie 認証に復活（`parent-bridge.js` が `credentials: 'include'` で送信するため自動付与）
- **SSE URL**: `parent-bridge.js` が `/api/job/{id}/stream`（存在しない）を呼んでいたバグを `/api/chat/{id}/stream` に修正

---

*See .planning/milestones/v4.0-ROADMAP.md for full phase details.*
*For current requirements, see .planning/PROJECT.md — Validated (v4.0) section.*
