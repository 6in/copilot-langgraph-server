---
phase: 26-adr-patterns-md-gsd
plan: 03
type: execute
wave: 2
depends_on: ["26-01", "26-02"]
files_modified:
  - CLAUDE.md
  - .claude/commands/create-adr.md
  - .planning/ROADMAP.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "CLAUDE.md に /gsd-discuss-phase 実行時の canonical_refs 必須追加ルールが記載されている"
    - "CLAUDE.md に ADR 追加時の patterns.md 手動更新義務が記載されている"
    - "CLAUDE.md に scripts/install-hooks.sh の初回実行手順が記載されている"
    - ".claude/commands/create-adr.md に patterns.md 更新リマインダが追加されている"
    - ".planning/ROADMAP.md の Phase 26 Goal が『[To be planned]』から実ゴールに置換されている"
  artifacts:
    - path: "CLAUDE.md"
      provides: "GSD 統合ルール + ADR 運用ルール"
      contains: "patterns.md"
    - path: ".claude/commands/create-adr.md"
      provides: "ADR 作成時の patterns.md 更新手順"
      contains: "patterns.md"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 26 の最終 Goal"
  key_links:
    - from: "CLAUDE.md"
      to: ".planning/patterns.md"
      via: "relative path reference (not @import)"
      pattern: "\\.planning/patterns\\.md"
    - from: "CLAUDE.md"
      to: "docs/adr/INDEX.md"
      via: "relative path reference"
      pattern: "docs/adr/INDEX\\.md"
---

<objective>
Phase 26 成果物 (3) GSD 統合を実現する。CLAUDE.md に「フェーズ開始時に patterns.md と INDEX.md を canonical_refs に必ず追加せよ」という運用ルールを明記し、`/create-adr` スキルに patterns.md 更新リマインダを追加する。さらに ROADMAP.md の Phase 26 Goal プレースホルダを埋める。

Purpose: D-11（canonical_refs 再帰的追加）・D-12（@import しない）・D-13（GSD 本体不変更）・D-15（patterns.md 手動更新運用）を本リポジトリ内で完結させる。

Output: CLAUDE.md 追記、create-adr.md 追記、ROADMAP.md Goal 更新。Plan 01/02 とファイル重複なしのため並列実行可能。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-adr-patterns-md-gsd/26-CONTEXT.md
@.planning/phases/26-adr-patterns-md-gsd/26-RESEARCH.md
@CLAUDE.md
@.claude/commands/create-adr.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLAUDE.md に GSD 統合ルールと ADR 運用ルールを追記する</name>
  <read_first>
    - CLAUDE.md 全体（追記箇所の特定 — 調査結果 6 参照）
    - .planning/phases/26-adr-patterns-md-gsd/26-RESEARCH.md（調査結果 5: GSD discuss-phase ワークフロー 252-284 行目、調査結果 6: CLAUDE.md 追記箇所 288-311 行目、Pitfall 5: canonical_refs 追加忘れ 471-474 行目）
    - .planning/phases/26-adr-patterns-md-gsd/26-CONTEXT.md（D-11/D-12/D-13/D-15）
  </read_first>
  <files>CLAUDE.md</files>
  <action>
    CLAUDE.md の 2 箇所に追記する。既存の日本語規約と整合させ、箇条書き/見出し形式を揃える。

    **追記箇所 1: `## Conventions` セクションの末尾**（既存の「Merge Workflow」サブセクションの後ろに新サブセクションを追加）

    追加内容:

    ```markdown
    ### ADR Pattern Reference (GSD Integration)

    `/gsd-discuss-phase` を実行する際は、CONTEXT.md の `canonical_refs` セクションに以下の 2 ファイルを必ず追加すること:

    - `.planning/patterns.md` — ADR 由来のパターンカタログ（設計判断の前に参照）
    - `docs/adr/INDEX.md` — ADR カテゴリ別索引（関連 ADR 特定に使用）

    これにより `/gsd-research-phase` と `/gsd-plan-phase` が過去の意思決定パターンを自動的に参照できる。
    `@import` 形式での常時ロードはしない（コンテキスト肥大回避 — D-12）。必要なフェーズでのみ canonical_refs 経由で読み込む。

    **新規 ADR 追加時の義務:**

    - `/create-adr` で新規 ADR を作成した直後、パターンとして記録すべき設計判断があれば `.planning/patterns.md` に**手動で追記**する（D-15）
    - patterns.md は自動生成しない — 要約の粒度は人間判断が必要
    - ADR にないパターンは patterns.md に載せない（ADR が唯一の真実源 — D-08）
    - 1 エントリは 5-10 行（パターン名 + 要約 + 関連 ADR リンク）
    - カテゴリは 7 種: `Auth` / `LangGraph・Graph` / `MCP・Tools` / `Worker・Jobs` / `Frontend・UI` / `Infra・Deploy` / `Data・Persistence`
    ```

    **追記箇所 2: `## GSD Workflow Enforcement` セクションの末尾**

    追加内容:

    ```markdown
    ### ADR INDEX 自動生成 hook のインストール

    `docs/adr/INDEX.md` は `scripts/generate_adr_index.py` により自動生成される。新規クローン直後は以下を 1 回実行して pre-commit hook を有効化すること:

    ```bash
    bash scripts/install-hooks.sh
    ```

    これにより `docs/adr/NNNN-*.md` がコミットに含まれる際、hook が自動で `INDEX.md` を再生成・ステージングする。
    カテゴリマッピングは `.planning/adr-categories.yaml` で管理する。新規 ADR を追加したら YAML にも番号とカテゴリを追記すること。
    ```

    編集方針:
    - Edit ツールで既存テキスト末尾（`## Chrome DevTools MCP` の直前 = `## Conventions` 末尾、および `## Developer Profile` の直前 = `## GSD Workflow Enforcement` 末尾）に新サブセクションを挿入
    - 既存の日本語トーン・箇条書きスタイルを踏襲
    - 既存の「応答言語」「Merge Workflow」と同じレベル（h3 = `###`）で新サブセクションを作る
  </action>
  <verify>
    <automated>grep -q "ADR Pattern Reference" CLAUDE.md && grep -q "\.planning/patterns\.md" CLAUDE.md && grep -q "docs/adr/INDEX\.md" CLAUDE.md && grep -q "install-hooks\.sh" CLAUDE.md && grep -q "adr-categories\.yaml" CLAUDE.md && ! grep -q "^@\.planning/patterns\.md" CLAUDE.md && echo "CLAUDE OK"</automated>
  </verify>
  <done>
    - CLAUDE.md に "ADR Pattern Reference" サブセクションが追加
    - `.planning/patterns.md` と `docs/adr/INDEX.md` 両方への参照がある
    - `scripts/install-hooks.sh` 手順がある
    - `@.planning/patterns.md` 形式の `@import` 行は存在しない（D-12 準拠）
  </done>
</task>

<task type="auto">
  <name>Task 2: .claude/commands/create-adr.md に patterns.md 更新リマインダを追加する</name>
  <read_first>
    - .claude/commands/create-adr.md 全体（既存の手順構造を確認）
    - .planning/phases/26-adr-patterns-md-gsd/26-RESEARCH.md（調査結果 7: /create-adr スキルへのリマインダー追加方法 316-343 行目）
    - .planning/phases/26-adr-patterns-md-gsd/26-CONTEXT.md（D-15: patterns.md 手動更新）
  </read_first>
  <files>.claude/commands/create-adr.md</files>
  <action>
    `.claude/commands/create-adr.md` の手順の末尾（既存のコミット手順の後ろ）に patterns.md 更新リマインダステップを追加する。

    追加内容（Markdown セクションとして末尾に挿入）:

    ```markdown

    ## 6. patterns.md 更新リマインダ

    ADR 作成・コミット後、パターンとして記録すべき設計判断が含まれていれば `.planning/patterns.md` にも手動で追記すること（D-15 / CLAUDE.md 参照）。

    **追記ルール:**

    - patterns.md は手動更新（自動生成しない）
    - 1 パターンあたり 5-10 行: パターン名見出し + 要約 2-4 行 + 関連 ADR 相対リンク
    - カテゴリは 7 種: `Auth` / `LangGraph・Graph` / `MCP・Tools` / `Worker・Jobs` / `Frontend・UI` / `Infra・Deploy` / `Data・Persistence`
    - ADR にないパターンは追加しない（ADR が唯一の真実源 — D-08）
    - ADR 番号を `.planning/adr-categories.yaml` にも追記する（primary + secondary カテゴリ）

    **記載例:**

    ```markdown
    ### パターン名
    設計判断の要約 2-4 行。具体的な挙動・採用理由・回避したい代替案を簡潔に記す。
    関連 ADR: [NNNN](../docs/adr/NNNN-slug.md)
    ```

    pre-commit hook が有効な環境では、次回コミット時に `docs/adr/INDEX.md` が自動で再生成される（`scripts/install-hooks.sh` でインストール済みの場合）。
    ```

    注意:
    - 既存の手順番号を崩さない（Step 6 として追加。既存が 1-5 なら 6 として新規追加）
    - もし既存ファイルに別の番号体系があれば、新規末尾セクションとして `## patterns.md 更新リマインダ` 見出しのみで追加してよい
    - 既存の日本語トーンを維持
  </action>
  <verify>
    <automated>grep -q "patterns\.md" .claude/commands/create-adr.md && grep -q "adr-categories\.yaml" .claude/commands/create-adr.md && grep -q "D-15\|手動更新\|手動で追記" .claude/commands/create-adr.md && echo "CREATE-ADR OK"</automated>
  </verify>
  <done>
    - .claude/commands/create-adr.md に patterns.md 追記リマインダセクションが存在
    - 7 カテゴリ列挙あり
    - adr-categories.yaml への追記指示あり
  </done>
</task>

<task type="auto">
  <name>Task 3: ROADMAP.md の Phase 26 Goal プレースホルダを実値に更新する</name>
  <read_first>
    - .planning/ROADMAP.md（現 Phase 26 セクション、179-188 行目付近）
    - .planning/phases/26-adr-patterns-md-gsd/26-CONTEXT.md（domain ブロック、フェーズゴール）
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
    `.planning/ROADMAP.md` の Phase 26 セクションを更新する:

    **Before:**
    ```
    ### Phase 26: ADR 整理 + patterns.md 作成 + GSD プランニング統合

    **Goal:** [To be planned]
    **Requirements**: TBD
    **Depends on:** Phase 25
    **Plans:** 0 plans

    Plans:
    - [ ] TBD (run /gsd-plan-phase 26 to break down)
    ```

    **After:**
    ```
    ### Phase 26: ADR 整理 + patterns.md 作成 + GSD プランニング統合

    **Goal:** 30 件の ADR から再利用可能パターンを `.planning/patterns.md` として抽出・カタログ化し、`docs/adr/INDEX.md`（pre-commit hook 自動生成）と合わせて CLAUDE.md 経由で GSD の discuss/plan フェーズが canonical_refs 経由で自動参照できる状態を作る。ADR 本文の変更・Status 付与・欠番補完は対象外（D-03/D-04/D-05）。
    **Requirements**: none (整備フェーズのため REQ-ID なし)
    **Depends on:** Phase 25
    **Plans:** 3 plans

    Plans:
    - [ ] 26-01-PLAN.md — adr-categories.yaml + generate_adr_index.py + pre-commit hook + pytest
    - [ ] 26-02-PLAN.md — docs/adr/INDEX.md 生成 + .planning/patterns.md 新規作成
    - [ ] 26-03-PLAN.md — CLAUDE.md 運用ルール追記 + /create-adr リマインダ + ROADMAP 更新
    ```

    編集方法: Edit ツールで上記 before/after をそのまま置換する。既存の `### Phase 26:` 見出しと「Plans:」リストは保持する。
  </action>
  <verify>
    <automated>grep -q "Phase 26: ADR 整理" .planning/ROADMAP.md && ! grep -q "\[To be planned\]" .planning/ROADMAP.md && grep -q "26-01-PLAN\.md" .planning/ROADMAP.md && grep -q "26-02-PLAN\.md" .planning/ROADMAP.md && grep -q "26-03-PLAN\.md" .planning/ROADMAP.md && grep -q "\*\*Plans:\*\* 3 plans" .planning/ROADMAP.md && echo "ROADMAP OK"</automated>
  </verify>
  <done>
    - ROADMAP.md の Phase 26 Goal が実値に置換（`[To be planned]` が消滅）
    - Plans カウントが 3
    - 3 件のプラン参照が列挙されている
  </done>
</task>

</tasks>

<verification>
- CLAUDE.md / create-adr.md / ROADMAP.md の 3 ファイルが変更されている
- `@import` 形式の行が CLAUDE.md に存在しない（D-12 準拠）
- ROADMAP.md に `[To be planned]` が残っていない
- 既存の日本語トーン・マークダウン構造を壊していない
</verification>

<success_criteria>
- CLAUDE.md に canonical_refs 必須追加ルール + hook インストール手順
- create-adr.md に patterns.md 更新リマインダ
- ROADMAP.md Phase 26 Goal が実値
</success_criteria>

<output>
完了後、`.planning/phases/26-adr-patterns-md-gsd/26-03-SUMMARY.md` を作成する。
</output>
