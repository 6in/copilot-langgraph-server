---
phase: 26-adr-patterns-md-gsd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/adr-categories.yaml
  - scripts/generate_adr_index.py
  - scripts/install-hooks.sh
  - tests/test_generate_adr_index.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "adr-categories.yaml が全 30 件の ADR（欠番 0015-0017 を除く）を 7 カテゴリに振り分けている"
    - "scripts/generate_adr_index.py を実行すると docs/adr/INDEX.md が生成される"
    - "スクリプトは ADR 0020 の異なるフォーマット（ADR 0020:、**Date**:）も正しくパースできる"
    - "欠番 0015-0017 が INDEX.md に明示的に記録される"
    - "pytest tests/test_generate_adr_index.py が pass する"
    - "scripts/install-hooks.sh により .git/hooks/pre-commit が導入される"
  artifacts:
    - path: ".planning/adr-categories.yaml"
      provides: "ADR 番号 → カテゴリのマッピング"
      contains: "adr_categories"
    - path: "scripts/generate_adr_index.py"
      provides: "INDEX.md 自動生成スクリプト"
    - path: "scripts/install-hooks.sh"
      provides: "pre-commit hook 導入スクリプト"
    - path: "tests/test_generate_adr_index.py"
      provides: "生成スクリプトのユニットテスト"
  key_links:
    - from: "scripts/generate_adr_index.py"
      to: ".planning/adr-categories.yaml"
      via: "yaml.safe_load()"
      pattern: "adr-categories\\.yaml"
    - from: "scripts/generate_adr_index.py"
      to: "docs/adr/*.md"
      via: "pathlib.Path.glob"
      pattern: "docs/adr"
---

<objective>
Phase 26 の基盤ツールチェーンを構築する。`adr-categories.yaml`（カテゴリマッピング）、`scripts/generate_adr_index.py`（INDEX.md 自動生成スクリプト）、pre-commit hook（install スクリプト）、および pytest テストを整備する。

Purpose: 後続プラン（Plan 02）が INDEX.md を生成するための実行可能スクリプトと、将来の新規 ADR 追加時に自動で INDEX.md が更新される仕組みを作る。D-14（pre-commit hook で自動生成）・D-03（ADR 本文を変更しない）に準拠。

Output: カテゴリマッピング設定ファイル、Python 生成スクリプト、hook インストールスクリプト、pytest テスト。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/26-adr-patterns-md-gsd/26-CONTEXT.md
@.planning/phases/26-adr-patterns-md-gsd/26-RESEARCH.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: .planning/adr-categories.yaml を作成する</name>
  <read_first>
    - .planning/phases/26-adr-patterns-md-gsd/26-RESEARCH.md（調査結果 1: 全 30 件 ADR 一覧と初期マッピング・119 行目付近〜127 行目のカテゴリ別件数サマリ）
    - .planning/phases/26-adr-patterns-md-gsd/26-CONTEXT.md（D-09: 7 カテゴリ、D-10: 主/副カテゴリ、D-03: ADR 本文不変更、D-04: 欠番の扱い）
    - docs/adr/ ディレクトリのファイル一覧（`ls docs/adr/` で 0001-0033 + 欠番の存在を確認）
  </read_first>
  <files>.planning/adr-categories.yaml</files>
  <action>
    RESEARCH.md の「調査結果 1」表（80-113 行目）の振り分けをそのまま YAML 化する。構造は以下:

    ```yaml
    # ADR 番号 → カテゴリ マッピング
    # D-09 の 7 カテゴリ: Auth / LangGraph・Graph / MCP・Tools / Worker・Jobs / Frontend・UI / Infra・Deploy / Data・Persistence
    # primary = 主カテゴリ、secondary = 副カテゴリ（任意）
    # 欠番 0015-0017 は missing リストに記録する

    categories:
      - Auth
      - LangGraph・Graph
      - MCP・Tools
      - Worker・Jobs
      - Frontend・UI
      - Infra・Deploy
      - Data・Persistence

    adr_categories:
      "0001": { primary: "Infra・Deploy", secondary: "Frontend・UI" }
      "0002": { primary: "Frontend・UI", secondary: "Infra・Deploy" }
      "0003": { primary: "Worker・Jobs", secondary: "LangGraph・Graph" }
      "0004": { primary: "LangGraph・Graph" }
      "0005": { primary: "LangGraph・Graph", secondary: "Worker・Jobs" }
      "0006": { primary: "Frontend・UI", secondary: "LangGraph・Graph" }
      "0007": { primary: "LangGraph・Graph", secondary: "Infra・Deploy" }
      "0008": { primary: "Frontend・UI" }
      "0009": { primary: "Frontend・UI", secondary: "Data・Persistence" }
      "0010": { primary: "Data・Persistence", secondary: "Frontend・UI" }
      "0011": { primary: "LangGraph・Graph", secondary: "Frontend・UI" }
      "0012": { primary: "Frontend・UI" }
      "0013": { primary: "Frontend・UI" }
      "0014": { primary: "Auth", secondary: "Worker・Jobs" }
      "0018": { primary: "Frontend・UI", secondary: "Worker・Jobs" }
      "0019": { primary: "Frontend・UI", secondary: "Infra・Deploy" }
      "0020": { primary: "MCP・Tools", secondary: "Infra・Deploy" }
      "0021": { primary: "LangGraph・Graph", secondary: "MCP・Tools" }
      "0022": { primary: "MCP・Tools", secondary: "LangGraph・Graph" }
      "0023": { primary: "MCP・Tools", secondary: "Data・Persistence" }
      "0024": { primary: "MCP・Tools", secondary: "Worker・Jobs" }
      "0025": { primary: "LangGraph・Graph", secondary: "Auth" }
      "0026": { primary: "Data・Persistence" }
      "0027": { primary: "Frontend・UI", secondary: "Infra・Deploy" }
      "0028": { primary: "Frontend・UI", secondary: "Infra・Deploy" }
      "0029": { primary: "Frontend・UI" }
      "0030": { primary: "MCP・Tools", secondary: "Data・Persistence" }
      "0031": { primary: "LangGraph・Graph", secondary: "Worker・Jobs" }
      "0032": { primary: "Data・Persistence", secondary: "Infra・Deploy" }
      "0033": { primary: "Frontend・UI", secondary: "MCP・Tools" }

    missing:
      - "0015"
      - "0016"
      - "0017"
    ```

    注意:
    - `docs/adr/` に実在する ADR 番号（30 件）と完全一致させる。事前に `ls docs/adr/*.md | grep -oE '[0-9]{4}' | sort -u` で確認する。
    - 欠番 3 件を `missing:` に記録する（D-04）。
    - カテゴリ名は D-09 の日本語中点入り表記（`・`）をそのまま使う。
  </action>
  <verify>
    <automated>test -f .planning/adr-categories.yaml && python3 -c "import yaml; d=yaml.safe_load(open('.planning/adr-categories.yaml')); assert len(d['adr_categories'])==30; assert d['missing']==['0015','0016','0017']; assert len(d['categories'])==7; print('OK')"</automated>
  </verify>
  <done>
    - .planning/adr-categories.yaml が存在
    - adr_categories に 30 エントリ（0001-0033 から 0015-0017 を除く）
    - categories リストが 7 件
    - missing リストが 0015, 0016, 0017
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scripts/generate_adr_index.py 実装 + pytest テスト</name>
  <read_first>
    - .planning/phases/26-adr-patterns-md-gsd/26-RESEARCH.md（調査結果 1 の ADR 一覧、Pitfall 1（ADR 0020 フォーマット差異）、Architecture Patterns の正規表現パターン 403-413 行目、INDEX.md フォーマット設計 346-383 行目）
    - .planning/adr-categories.yaml（Task 1 で作成）
    - docs/adr/0001-nginx-prefix-strip-for-url-routing.md（標準フォーマット例）
    - docs/adr/0020-*.md（異種フォーマット例）
    - pyproject.toml（pytest 設定、pyyaml 依存確認）
    - tests/conftest.py（既存テスト構造確認）
  </read_first>
  <behavior>
    - Test 1: 標準フォーマットの ADR (例 0001) から (番号, タイトル, 日付) が抽出できる
    - Test 2: ADR 0020 の異種フォーマット（# ADR 0020: / **Date**:）が正しくパースされる
    - Test 3: 欠番 0015-0017 が結果に「欠番」として含まれる
    - Test 4: 生成された INDEX.md に 7 カテゴリ全ての見出し（## Auth 等）が含まれる
    - Test 5: 各 ADR が adr_categories の primary カテゴリの下に配置される
    - Test 6: `## 欠番` セクションに 0015/0016/0017 が含まれる
  </behavior>
  <files>scripts/generate_adr_index.py, tests/test_generate_adr_index.py</files>
  <action>
    **scripts/generate_adr_index.py** を作成。以下の構成:

    ```python
    #!/usr/bin/env python3
    """docs/adr/INDEX.md を .planning/adr-categories.yaml に基づいて自動生成する。

    D-03 準拠: ADR 本文は一切変更しない。
    D-04 準拠: 欠番 0015-0017 を「欠番」として明示記録する。
    """
    from __future__ import annotations
    import re
    import sys
    from pathlib import Path
    import yaml

    REPO_ROOT = Path(__file__).resolve().parents[1]
    ADR_DIR = REPO_ROOT / "docs" / "adr"
    CATEGORIES_YAML = REPO_ROOT / ".planning" / "adr-categories.yaml"
    INDEX_MD = ADR_DIR / "INDEX.md"

    # ADR 0020 は "# ADR 0020:" 形式、他は "# 0001. タイトル" 形式
    TITLE_RE = re.compile(r'^# (?:ADR )?(\d+)[.:]\s+(.+?)\s*$', re.MULTILINE)
    # "**Date:** YYYY-MM-DD" と "**Date**: YYYY-MM-DD" の両方に対応
    DATE_RE = re.compile(r'^\*\*Date\*?\*?:?\s*(\d{4}-\d{2}-\d{2})', re.MULTILINE)

    def parse_adr(path: Path) -> tuple[str, str, str] | None:
        """(番号, タイトル, 日付) を返す。パース失敗時は None。"""
        text = path.read_text(encoding="utf-8")
        t = TITLE_RE.search(text)
        d = DATE_RE.search(text)
        if not t:
            return None
        number = t.group(1).zfill(4)
        title = t.group(2).strip()
        date = d.group(1) if d else "-"
        return number, title, date

    def load_categories() -> dict:
        return yaml.safe_load(CATEGORIES_YAML.read_text(encoding="utf-8"))

    def build_index(adr_dir: Path, categories_data: dict) -> str:
        """INDEX.md の markdown 文字列を生成する。"""
        parsed: dict[str, tuple[str, str, str, str]] = {}  # number -> (number, title, date, filename)
        for md in sorted(adr_dir.glob("[0-9][0-9][0-9][0-9]-*.md")):
            r = parse_adr(md)
            if r:
                parsed[r[0]] = (*r, md.name)

        cats_list = categories_data["categories"]
        adr_cats = categories_data["adr_categories"]
        missing = categories_data.get("missing", [])

        # カテゴリ別に ADR を集める（primary のみ）
        by_cat: dict[str, list] = {c: [] for c in cats_list}
        for num, meta in adr_cats.items():
            if num in parsed:
                by_cat[meta["primary"]].append(parsed[num])

        lines: list[str] = []
        lines.append("# ADR Index")
        lines.append("")
        lines.append(f"**Total:** {len(parsed)} 件（欠番 {len(missing)} 件: {', '.join(missing)}）")
        lines.append("")
        lines.append("> このファイルは `scripts/generate_adr_index.py` により自動生成されます。手動編集しないこと。")
        lines.append("")

        for cat in cats_list:
            lines.append(f"## {cat}")
            lines.append("")
            entries = sorted(by_cat[cat], key=lambda x: x[0])
            if not entries:
                lines.append("_（該当 ADR なし）_")
                lines.append("")
                continue
            lines.append("| No. | タイトル | Date |")
            lines.append("|-----|---------|------|")
            for num, title, date, fname in entries:
                lines.append(f"| [{num}]({fname}) | {title} | {date} |")
            lines.append("")

        lines.append("## 欠番")
        lines.append("")
        lines.append("| No. | 備考 |")
        lines.append("|-----|------|")
        for m in missing:
            lines.append(f"| {m} | — 欠番 — |")
        lines.append("")

        return "\n".join(lines)

    def main() -> int:
        data = load_categories()
        content = build_index(ADR_DIR, data)
        INDEX_MD.write_text(content, encoding="utf-8")
        print(f"Generated {INDEX_MD.relative_to(REPO_ROOT)} ({len(content)} bytes)")
        return 0

    if __name__ == "__main__":
        sys.exit(main())
    ```

    **tests/test_generate_adr_index.py** を作成（既存の実 ADR ファイルと adr-categories.yaml を使う統合寄りのユニットテスト）:

    ```python
    """generate_adr_index.py のユニットテスト。"""
    import sys
    from pathlib import Path
    import pytest

    REPO_ROOT = Path(__file__).resolve().parents[1]
    sys.path.insert(0, str(REPO_ROOT / "scripts"))

    import generate_adr_index as gai  # noqa: E402


    def test_parse_standard_adr():
        """標準フォーマット (# 0001. ...) がパースできる"""
        path = REPO_ROOT / "docs" / "adr" / "0001-nginx-prefix-strip-for-url-routing.md"
        result = gai.parse_adr(path)
        assert result is not None
        number, title, date = result
        assert number == "0001"
        assert "nginx" in title.lower() or "prefix" in title.lower()
        assert date.startswith("2026-")


    def test_parse_adr_0020_alternate_format():
        """ADR 0020 の '# ADR 0020:' / '**Date**:' 形式がパースできる"""
        path = REPO_ROOT / "docs" / "adr" / "0020-fastmcp-docker-service-infrastructure.md"
        result = gai.parse_adr(path)
        assert result is not None
        number, title, date = result
        assert number == "0020"
        assert date.startswith("2026-")


    def test_load_categories_structure():
        data = gai.load_categories()
        assert len(data["categories"]) == 7
        assert "Auth" in data["categories"]
        assert "LangGraph・Graph" in data["categories"]
        assert len(data["adr_categories"]) == 30
        assert data["missing"] == ["0015", "0016", "0017"]


    def test_build_index_has_all_categories():
        data = gai.load_categories()
        content = gai.build_index(gai.ADR_DIR, data)
        for cat in data["categories"]:
            assert f"## {cat}" in content


    def test_build_index_records_missing():
        data = gai.load_categories()
        content = gai.build_index(gai.ADR_DIR, data)
        assert "## 欠番" in content
        assert "0015" in content and "0016" in content and "0017" in content


    def test_build_index_includes_adr_0020():
        """ADR 0020 がインデックスに含まれていること（Pitfall 1 回避検証）"""
        data = gai.load_categories()
        content = gai.build_index(gai.ADR_DIR, data)
        assert "0020" in content
        # 0020 は MCP・Tools カテゴリにあるべき
        mcp_section = content.split("## MCP・Tools", 1)[1].split("## ", 1)[0]
        assert "0020" in mcp_section


    def test_build_index_total_count():
        data = gai.load_categories()
        content = gai.build_index(gai.ADR_DIR, data)
        assert "**Total:** 30 件" in content
    ```

    注意事項:
    - `yaml.safe_load()` のみ使用（Security Domain 参照）
    - RESEARCH.md Pitfall 1 対応: `TITLE_RE` と `DATE_RE` の両方に ADR 0020 互換正規表現を使用
    - スクリプトの shebang は `#!/usr/bin/env python3`
    - 生成 INDEX.md の相対リンクは `{fname}`（同ディレクトリ内相対パス）
  </action>
  <verify>
    <automated>python3 scripts/generate_adr_index.py && test -f docs/adr/INDEX.md && cd /home/parallels/workspaces/copilot-langgraph && python3 -m pytest tests/test_generate_adr_index.py -xvs 2>&1 | tail -30</automated>
  </verify>
  <done>
    - scripts/generate_adr_index.py が存在し `python3 scripts/generate_adr_index.py` で成功終了
    - tests/test_generate_adr_index.py の 7 テスト全て pass
    - docs/adr/INDEX.md が生成されている（Plan 02 で内容精査）
  </done>
</task>

<task type="auto">
  <name>Task 3: scripts/install-hooks.sh 作成 + 実行で hook を有効化</name>
  <read_first>
    - .planning/phases/26-adr-patterns-md-gsd/26-RESEARCH.md（調査結果 4: pre-commit hook 方式 228-248 行目、Pitfall 2・3: hook がリポジトリに含まれない問題 456-464 行目）
    - .git/hooks/ の既存状態（`ls -la .git/hooks/` で .sample のみと確認）
  </read_first>
  <files>scripts/install-hooks.sh</files>
  <action>
    `scripts/install-hooks.sh` を作成し、実行可能にする（`chmod +x`）。内容:

    ```bash
    #!/usr/bin/env bash
    # scripts/install-hooks.sh — リポジトリローカル git hook を .git/hooks/ にインストールする
    #
    # 用途: 新規クローン後に 1 回実行する。CLAUDE.md の運用ルールでも呼ばれる。
    # 何をするか: docs/adr/*.md の変更を検知したら scripts/generate_adr_index.py を
    # 実行し、生成された INDEX.md をステージングに追加する pre-commit hook を作成。

    set -euo pipefail

    REPO_ROOT="$(git rev-parse --show-toplevel)"
    HOOK_PATH="$REPO_ROOT/.git/hooks/pre-commit"

    cat > "$HOOK_PATH" <<'HOOK_EOF'
    #!/usr/bin/env bash
    # Auto-generated by scripts/install-hooks.sh — do not edit manually.
    set -e

    REPO_ROOT="$(git rev-parse --show-toplevel)"

    # docs/adr/ 配下の ADR が変更された場合のみ INDEX.md を再生成する
    if git diff --cached --name-only | grep -qE '^docs/adr/[0-9]{4}-.*\.md$'; then
      echo "[pre-commit] docs/adr/ 変更を検知 → INDEX.md を再生成"
      python3 "$REPO_ROOT/scripts/generate_adr_index.py"
      git add "$REPO_ROOT/docs/adr/INDEX.md"
    fi
    HOOK_EOF

    chmod +x "$HOOK_PATH"
    echo "Installed pre-commit hook at: $HOOK_PATH"
    ```

    スクリプト自身も `chmod +x scripts/install-hooks.sh` で実行可能にし、その後 `./scripts/install-hooks.sh` を実行して hook を有効化する。

    注意:
    - Pitfall 3 対策: hook 内で `git add docs/adr/INDEX.md` を明示する
    - regex `^docs/adr/[0-9]{4}-.*\.md$` で INDEX.md 自身の変更ではトリガーされないようにする（INDEX.md は番号プレフィックスなし）
    - hook は bash、`set -e` で失敗時中断
  </action>
  <verify>
    <automated>test -x scripts/install-hooks.sh && bash scripts/install-hooks.sh && test -x .git/hooks/pre-commit && grep -q "generate_adr_index.py" .git/hooks/pre-commit && echo "HOOK OK"</automated>
  </verify>
  <done>
    - scripts/install-hooks.sh が存在し実行可能
    - 実行すると .git/hooks/pre-commit が作成され実行可能
    - hook 本体に generate_adr_index.py 呼び出しと `git add docs/adr/INDEX.md` が含まれる
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adr-categories.yaml → Python | YAML パース（safe_load のみ使用） |
| ADR 本文 → 正規表現 | ファイル内容を正規表現でパース（任意実行なし） |
| git hook → shell | pre-commit で local スクリプトを実行 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | adr-categories.yaml | mitigate | `yaml.safe_load()` のみ使用（任意 Python オブジェクト構築を防ぐ） |
| T-26-02 | Elevation | pre-commit hook | accept | ローカル開発環境限定、外部入力なし、信頼された開発者のみ実行 |
| T-26-03 | DoS | generate_adr_index.py | accept | 処理対象は docs/adr/*.md の静的ファイルのみ、最大 O(100) 件想定 |
</threat_model>

<verification>
- `python3 scripts/generate_adr_index.py` が成功し docs/adr/INDEX.md を出力
- `python3 -m pytest tests/test_generate_adr_index.py -xvs` で全テスト pass
- `bash scripts/install-hooks.sh` 実行後 .git/hooks/pre-commit が存在し実行可能
- `python3 -c "import yaml; print(len(yaml.safe_load(open('.planning/adr-categories.yaml'))['adr_categories']))"` が 30 を出力
</verification>

<success_criteria>
- adr-categories.yaml に 30 エントリ（primary カテゴリ必須、secondary 任意）
- generate_adr_index.py が ADR 0020 を含む全 30 件を正しく処理
- pytest 全テスト pass（7 テスト）
- install-hooks.sh で .git/hooks/pre-commit が有効化
</success_criteria>

<output>
完了後、`.planning/phases/26-adr-patterns-md-gsd/26-01-SUMMARY.md` を作成する。
</output>
