---
phase: 19-canvas-apps-app-id-iframe-phase-18-rpc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/js/parent-bridge.js
  - app/api/routes/hosted_apps.py
  - app/api/routes/iframe_rpc.py
  - app/api/main.py
  - frontend/src/components/CanvasPane.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13

must_haves:
  truths:
    - "GET /apps/{app_id} がブラウザで開けて、iframe に Canvas アプリの HTML が表示される"
    - "DB に HTML が存在しないアプリへのアクセスは 404 を返す"
    - "iframe sandbox は allow-scripts allow-forms のみ（allow-same-origin なし）"
    - "POST /api/iframe-rpc が JWT 認証なしで呼び出せる"
    - "shell HTML から /js/parent-bridge.js が読み込まれる"
    - "CanvasPane.tsx の postMessage リレーが e.source を使うように変更されている"
  artifacts:
    - path: "static/js/parent-bridge.js"
      provides: "親フレーム postMessage リレー（Shell・CanvasPane 共通ロジック）"
      contains: "handleIframeMessage.*e.source"
    - path: "app/api/routes/hosted_apps.py"
      provides: "GET /apps/{app_id} 動的ルート"
      exports: ["router"]
    - path: "app/api/routes/iframe_rpc.py"
      provides: "JWT 依存なし POST /api/iframe-rpc"
      contains: "Depends(get_jwt_payload) が存在しない"
    - path: "app/api/main.py"
      provides: "hosted_apps.router を /apps StaticFiles より前に登録"
      contains: "hosted_apps"
    - path: "frontend/src/components/CanvasPane.tsx"
      provides: "iframeRef 除去 + parent-bridge.js ロード"
      contains: "parent-bridge"
  key_links:
    - from: "app/api/main.py"
      to: "app/api/routes/hosted_apps.py"
      via: "include_router(hosted_apps.router)"
      pattern: "include_router.*hosted_apps"
    - from: "app/api/routes/hosted_apps.py"
      to: "canvas_apps DB テーブル"
      via: "psycopg.AsyncConnection SELECT html FROM canvas_apps WHERE app_id = %s::uuid"
      pattern: "SELECT.*html.*canvas_apps"
    - from: "hosted_apps shell HTML"
      to: "static/js/parent-bridge.js"
      via: "<script src=/js/parent-bridge.js>"
      pattern: "parent-bridge"
    - from: "frontend/src/components/CanvasPane.tsx"
      to: "static/js/parent-bridge.js"
      via: "script injection useEffect"
      pattern: "parent-bridge"
---

<objective>
Canvas アプリのデプロイ＆ホスティング機能を実装する。

Purpose: デプロイ済み Canvas アプリを `/apps/{app_id}` URL で独立してホスティングし、Phase 18 の iframe postMessage JSON-RPC ブリッジが動作するホスティングシェルを配信する。

**設計変更（プランナー再設計）:**
- `parent-bridge.js` を新規作成し、親フレーム側のリレーロジックを共通化する。
- Shell HTML は `iframe-rpc.js`（iframe 内クライアント）ではなく `parent-bridge.js`（親フレームリレー）を読み込む。
- CanvasPane.tsx も `parent-bridge.js` に委譲し、`iframeRef` を除去する。

Output:
- `static/js/parent-bridge.js` — 親フレームリレー（Shell・CanvasPane 共通）
- `app/api/routes/hosted_apps.py` — 動的ルート（DB から HTML 取得 → srcdoc 埋め込み shell 返却）
- `app/api/routes/iframe_rpc.py` — JWT 認証削除（D-07）
- `app/api/main.py` — hosted_apps.router を StaticFiles より前に登録（D-02）
- `frontend/src/components/CanvasPane.tsx` — iframeRef 除去、parent-bridge.js 委譲
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-canvas-apps-app-id-iframe-phase-18-rpc/19-CONTEXT.md

<interfaces>
<!-- 実装に必要なインターフェース抜粋。探索不要。 -->

From app/api/routes/canvas.py (DB アクセスパターン — JWT あり版):
```python
import psycopg
from psycopg.rows import dict_row
from fastapi import APIRouter, Depends, HTTPException, Request

@router.get("/apps/{app_id}", response_model=CanvasAppInfo)
async def get_app(app_id: str, request: Request, payload: dict = Depends(get_jwt_payload)):
    db_uri = request.app.state.db_uri
    async with await psycopg.AsyncConnection.connect(db_uri, row_factory=dict_row) as conn:
        async with conn.cursor() as cur:
            await cur.execute(
                "SELECT app_id, thread_id, name, html, source, deployed, deployed_at, created_at "
                "FROM canvas_apps WHERE app_id = %s::uuid AND github_login = %s",
                (app_id, github_login),
            )
            row = await cur.fetchone()
    if row is None:
        raise HTTPException(status_code=404, detail="Canvas app not found")
```

From app/api/routes/iframe_rpc.py (変更前):
```python
from app.api.routes.chat import get_github_token, get_jwt_payload

@router.post("/iframe-rpc", response_model=IframeRpcResponse)
async def iframe_rpc(
    request: Request,
    body: IframeRpcRequest,
    github_token: str = Depends(get_github_token),   # ← 削除 (D-07)
    payload: dict = Depends(get_jwt_payload),          # ← 削除 (D-07)
):
    ...
    github_login = payload.get("github_login", "unknown")  # ← "anonymous" に変更
```

From app/api/main.py (関連部分):
```python
# L338-347: include_router calls (この後に hosted_apps を追加)
app.include_router(iframe_rpc.router)

# L356-357: StaticFiles マウント (ここより前に hosted_apps.router が必要)
app.mount("/apps", StaticFiles(directory="./static/apps", html=True), name="canvas_apps")
```

From frontend/src/components/CanvasPane.tsx (変更対象):
```typescript
// L49: iframeRef — parent-bridge.js 委譲後は不要になる
const iframeRef = useRef<HTMLIFrameElement>(null);

// L57-126: handleIframeMessage + useEffect — parent-bridge.js に委譲
const handleIframeMessage = useCallback(async (e: MessageEvent) => { ... }, []);
useEffect(() => {
  window.addEventListener('message', handleIframeMessage);
  return () => window.removeEventListener('message', handleIframeMessage);
}, [handleIframeMessage]);

// L399-406: iframe に ref={iframeRef} がある — 削除
<iframe ref={iframeRef} srcDoc={...} sandbox="allow-scripts allow-forms" ... />
```

From static/js/iframe-rpc.js (iframe 内クライアント — 変更なし):
```javascript
// これは iframe 内で動くクライアントライブラリ。window.parent.postMessage を送る側。
// Shell HTML は このファイルを直接読み込まない（iframe 内の Canvas app HTML が読み込む）。
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: parent-bridge.js — 親フレームリレー共通スクリプト作成</name>
  <files>static/js/parent-bridge.js</files>
  <action>
新規ファイル `static/js/parent-bridge.js` を作成する。

**役割:** 親フレーム（Shell HTML または CanvasPane の親ウィンドウ）として動作し、
iframe 内の `iframe-rpc.js` からの postMessage を受け取り、
`/api/iframe-rpc` に転送して結果を iframe に返す。

**Shell HTML と CanvasPane.tsx の両方が利用する共通ロジック。**

```javascript
/**
 * parent-bridge.js — Parent frame relay for Canvas iframe JSON-RPC.
 *
 * Loaded by the hosting shell (GET /apps/{app_id}) and CanvasPane.tsx.
 * Listens for JSON-RPC 2.0 postMessages from child iframes, forwards to
 * POST /api/iframe-rpc, awaits completion via SSE, replies to the source iframe.
 *
 * Protocol (matches iframe-rpc.js on the iframe side):
 *   Request (iframe → parent):  { jsonrpc: '2.0', id, method, params }
 *   Response (parent → iframe): { jsonrpc: '2.0', id, result: true|false, ...payload }
 *
 * Phase 19 — shared between hosted shell and CanvasPane.tsx.
 */

/**
 * Wait for an arq job to complete via SSE, then fetch the result.
 * @param {string} job_id
 * @returns {Promise<string>} raw result JSON string
 */
async function _waitForJob(job_id) {
  return new Promise((resolve, reject) => {
    const es = new EventSource('/api/job/' + job_id + '/stream');

    const timer = setTimeout(function () {
      es.close();
      reject(new Error('RPC timeout'));
    }, 60000);

    es.onmessage = async function (ev) {
      try {
        var event = JSON.parse(ev.data);
        if (event.status === 'done') {
          clearTimeout(timer);
          es.close();
          var jobResp = await fetch('/api/job/' + job_id, { credentials: 'include' });
          var job = await jobResp.json();
          resolve(job.result != null ? job.result : '{"result":false,"error":"No result"}');
        }
        // status === 'thinking' — keep waiting
      } catch (_) {
        // Malformed SSE frame — ignore
      }
    };

    es.onerror = function () {
      clearTimeout(timer);
      es.close();
      reject(new Error('SSE connection failed'));
    };
  });
}

/**
 * Handle a postMessage from a child iframe.
 * Validates origin, parses JSON-RPC, relays to /api/iframe-rpc, replies to source.
 * @param {MessageEvent} e
 */
async function handleIframeMessage(e) {
  // Origin validation: same origin or 'null' (srcdoc iframe has null origin)
  if (e.origin !== window.location.origin && e.origin !== 'null') return;

  var msg = e.data;
  if (!msg || typeof msg !== 'object' || msg.jsonrpc !== '2.0') return;

  var id = msg.id;
  var method = msg.method;
  var params = msg.params;
  if (!id || !method) return;

  var source = e.source;
  if (!source) return;

  try {
    var resp = await fetch('/api/iframe-rpc', {
      method: 'POST',
      credentials: 'include',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ id: id, method: method, params: params != null ? params : {} }),
    });

    if (!resp.ok) {
      throw new Error('iframe-rpc HTTP ' + resp.status);
    }

    var data = await resp.json();
    var job_id = data.job_id;
    var resultStr = await _waitForJob(job_id);

    var parsed;
    try {
      parsed = JSON.parse(resultStr);
    } catch (_) {
      parsed = { result: false, error: 'Invalid response format' };
    }

    var reply = { jsonrpc: '2.0', id: id };
    if (parsed && typeof parsed === 'object') {
      Object.assign(reply, parsed);
    } else {
      reply.result = parsed;
    }

    source.postMessage(reply, '*');
  } catch (err) {
    source.postMessage(
      { jsonrpc: '2.0', id: id, result: false, error: err instanceof Error ? err.message : 'Unknown error' },
      '*'
    );
  }
}

// Idempotency guard: prevent duplicate listeners if script is loaded multiple times
if (!window.__parentBridgeInstalled) {
  window.__parentBridgeInstalled = true;
  window.addEventListener('message', handleIframeMessage);
}
```

**注意事項:**
- `e.source` を使って返信先を特定する（iframeRef 不要）
- `window.__parentBridgeInstalled` フラグでスクリプトの二重登録を防ぐ
- ES modules ではなく通常の JS — `<script src="/js/parent-bridge.js">` で読み込み可能
- `/api/iframe-rpc` への fetch は `credentials: 'include'` 付き（D-09 準拠）
- SSE 待機は 60 秒タイムアウト（CanvasPane.tsx の既存実装に合わせる）
  </action>
  <verify>
    <automated>test -f /home/parallels/workspaces/copilot-langgraph/static/js/parent-bridge.js && grep -q "__parentBridgeInstalled" /home/parallels/workspaces/copilot-langgraph/static/js/parent-bridge.js && grep -q "e.source" /home/parallels/workspaces/copilot-langgraph/static/js/parent-bridge.js && echo "parent-bridge.js OK"</automated>
  </verify>
  <done>
    - `static/js/parent-bridge.js` が存在する
    - `window.__parentBridgeInstalled` による二重登録防止が実装されている
    - `e.source.postMessage(...)` で返信している
    - `/api/iframe-rpc` への fetch が `credentials: 'include'` 付き
    - SSE 待機 (`EventSource`) が実装されている
  </done>
</task>

<task type="auto">
  <name>Task 2: hosted_apps.py 実装 + Shell テンプレートに parent-bridge.js 適用 (D-01, D-03〜D-13)</name>
  <files>app/api/routes/hosted_apps.py</files>
  <action>
新規ファイル `app/api/routes/hosted_apps.py` を作成する。

**実装内容:**

```python
"""Hosted Canvas Apps — dynamic hosting shell (Phase 19).

GET /apps/{app_id} — DB から HTML を取得し、iframe srcdoc に埋め込んだ
フルスクリーンシェル HTML を返す。認証不要（D-08）。

Shell は parent-bridge.js を読み込むことで、iframe 内の Canvas アプリが
/api/iframe-rpc 経由で DB クエリ・AI 呼び出しを行える（Phase 18 ブリッジ）。

DB に HTML が存在しない場合は 404 を返す（D-12）。
"""
import psycopg
from fastapi import APIRouter, HTTPException, Request
from fastapi.responses import HTMLResponse
from psycopg.rows import dict_row

router = APIRouter(tags=["hosted-apps"])

_SHELL_TEMPLATE = """<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>{app_name}</title>
<style>
  * {{ margin: 0; padding: 0; box-sizing: border-box; }}
  html, body {{ height: 100%; overflow: hidden; }}
  iframe {{ width: 100%; height: 100%; border: none; display: block; }}
</style>
</head>
<body>
<iframe
  sandbox="allow-scripts allow-forms"
  srcdoc="{srcdoc_escaped}"
></iframe>
<script src="/js/parent-bridge.js"></script>
</body>
</html>"""


@router.get("/apps/{app_id}", response_class=HTMLResponse)
async def serve_hosted_app(app_id: str, request: Request) -> HTMLResponse:
    """Serve a deployed Canvas app as a standalone page (D-01, D-08).

    Fetches HTML from DB (no auth required), embeds in srcdoc iframe shell.
    Loads parent-bridge.js so the iframe can call /api/iframe-rpc (Phase 18 RPC bridge).
    Returns 404 if app not found or has no HTML (D-12).
    """
    db_uri = request.app.state.db_uri

    async with await psycopg.AsyncConnection.connect(db_uri, row_factory=dict_row) as conn:
        async with conn.cursor() as cur:
            await cur.execute(
                "SELECT name, html FROM canvas_apps WHERE app_id = %s::uuid",
                (app_id,),
            )
            row = await cur.fetchone()

    if row is None or not row["html"]:
        raise HTTPException(status_code=404, detail="Canvas app not found")

    # srcdoc 属性に埋め込むため HTML 内の " と & をエスケープ (T-19-02)
    srcdoc_escaped = row["html"].replace("&", "&amp;").replace('"', "&quot;")
    app_name = row["name"] or "Canvas App"

    html = _SHELL_TEMPLATE.format(
        app_name=app_name,
        srcdoc_escaped=srcdoc_escaped,
    )
    return HTMLResponse(content=html)
```

**注意事項:**
- `github_login` フィルターなし — app_id のみで検索（D-05 調整版、D-08）
- `sandbox="allow-scripts allow-forms"` のみ。`allow-same-origin` は含めない（D-06）
- `<script src="/js/parent-bridge.js">` を読み込む（D-13 更新版 — `iframe-rpc.js` ではなく `parent-bridge.js`）
- DB に HTML がない場合（html が None または空文字）は 404（D-12）
- srcdoc エスケープ: `"` → `&quot;`、`&` → `&amp;`（T-19-02）
- `{` と `}` を Python format 文字列テンプレートで使うため CSS の `{{ }}` に注意
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && python -c "from app.api.routes.hosted_apps import router, _SHELL_TEMPLATE; print('import OK'); routes = [r.path for r in router.routes]; assert '/apps/{app_id}' in routes, f'route not found: {routes}'; print('route OK'); assert 'parent-bridge.js' in _SHELL_TEMPLATE, 'parent-bridge.js not in template'; assert 'allow-same-origin' not in _SHELL_TEMPLATE, 'allow-same-origin must NOT be in template'; print('template OK')"</automated>
  </verify>
  <done>
    - `app/api/routes/hosted_apps.py` が存在し import 可能
    - `GET /apps/{app_id}` ルートが登録されている
    - DB から html を取得して srcdoc に埋め込む実装が完成
    - sandbox 属性が `allow-scripts allow-forms` のみ（`allow-same-origin` なし）
    - `<script src="/js/parent-bridge.js">` がテンプレートに含まれる
    - `iframe-rpc.js` はテンプレートに含まれない
  </done>
</task>

<task type="auto">
  <name>Task 3: CanvasPane.tsx — iframeRef 除去 + parent-bridge.js 委譲</name>
  <files>frontend/src/components/CanvasPane.tsx</files>
  <action>
`frontend/src/components/CanvasPane.tsx` を修正する。

**変更内容:**

1. **iframeRef を除去:**
   - `const iframeRef = useRef<HTMLIFrameElement>(null);` を削除
   - `<iframe ref={iframeRef} ...>` の `ref={iframeRef}` 属性を削除
   - `useRef` import を削除（他で使っていない場合）

2. **handleIframeMessage + 関連 useEffect を除去:**
   - `const handleIframeMessage = useCallback(async (e: MessageEvent) => { ... }, []);` 全体を削除
   - `useEffect(() => { window.addEventListener('message', handleIframeMessage); ... }, [handleIframeMessage]);` を削除
   - `useCallback` import を削除（他で使っていない場合）

3. **parent-bridge.js をスクリプト注入で読み込む useEffect を追加:**
   ```typescript
   // parent-bridge.js を一度だけ読み込む（Shell HTML との共通リレーロジック）
   useEffect(() => {
     const src = `${appBase}/js/parent-bridge.js`;
     if (document.querySelector(`script[src="${src}"]`)) return;
     const script = document.createElement('script');
     script.src = src;
     document.head.appendChild(script);
   }, []); // eslint-disable-line react-hooks/exhaustive-deps
   ```
   この useEffect は `appBase` の初期値（マウント時）で一度だけ実行する。
   `appBase` は `const appBase = (import.meta.env.VITE_APP_BASE as string ?? '').replace(/\/$/, '');` で既に定義済み。

4. **不要になった import を削除:**
   - `postIframeRpc, streamJob, getJob` が CanvasPane.tsx 内でのみ handleIframeMessage に使われていた場合、そのインポートを削除する。
   - 他のコード（onSave など）で `getJob` 等を使っている場合は残す。実際のファイルを確認して判断すること。

**実装時の注意:**
- `appBase` 変数は CanvasPane.tsx 内（L46）に既に定義されている。
- `srcDoc={htmlContent.replace(/\$URL_PREFIX/g, appBase)}` はそのまま維持する（Preview タブの srcdoc）。
- useRef と useCallback の削除は、他の箇所で使われていないことを確認してから行う。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && grep -n "iframeRef" frontend/src/components/CanvasPane.tsx | head -5 || echo "iframeRef removed OK"; grep -n "parent-bridge" frontend/src/components/CanvasPane.tsx && echo "parent-bridge OK"</automated>
  </verify>
  <done>
    - `iframeRef` が CanvasPane.tsx から削除されている
    - `handleIframeMessage` の inline 実装が削除されている
    - `parent-bridge.js` をスクリプト注入する useEffect が追加されている
    - `postIframeRpc, streamJob, getJob` の import が正しく整理されている（使用箇所がなければ削除）
  </done>
</task>

<task type="auto">
  <name>Task 4: iframe_rpc.py JWT 認証削除 + main.py ルート登録 (D-02, D-07, D-10)</name>
  <files>app/api/routes/iframe_rpc.py, app/api/main.py</files>
  <action>
**app/api/routes/iframe_rpc.py の修正（D-07）:**

`get_github_token` と `get_jwt_payload` の Depends を削除する。

変更前:
```python
from app.api.routes.chat import get_github_token, get_jwt_payload

@router.post("/iframe-rpc", response_model=IframeRpcResponse)
async def iframe_rpc(
    request: Request,
    body: IframeRpcRequest,
    github_token: str = Depends(get_github_token),
    payload: dict = Depends(get_jwt_payload),
):
    ...
    github_login = payload.get("github_login", "unknown")
```

変更後:
```python
# import から get_github_token, get_jwt_payload を削除
# (どちらも未使用になるため)

@router.post("/iframe-rpc", response_model=IframeRpcResponse)
async def iframe_rpc(
    request: Request,
    body: IframeRpcRequest,
):
    ...
    github_login = "anonymous"  # D-07: JWT 認証不要化、ログ用固定値
```

arq enqueue_job の `github_token` 引数も対応が必要:
- `github_token` は arq ワーカーが Copilot AI 呼び出しに使う。
- JWT 認証を外したため request からは取れない。
- `app.state` 経由で auth_manager から現在のトークンを取得する:
  ```python
  auth_manager = request.app.state.auth_manager
  github_token = auth_manager.load_token() or ""
  ```
  これにより既存の Copilot 呼び出しフローが維持される。

ファイルヘッダーのドキュメント文字列も D-07 の変更を反映して更新する。

**app/api/main.py の修正（D-02, D-03）:**

1. import 行（L25）に `hosted_apps` を追加:
   ```python
   from app.api.routes import agents, apps, auth, canvas, chat, gems, health, hosted_apps, iframe_rpc, jobs, me
   ```

2. include_router 呼び出し群（L338-347）に追加。`iframe_rpc.router` の直後かつ `app.mount("/apps", ...)` より前に:
   ```python
   app.include_router(hosted_apps.router)
   ```
   既存の `/apps` StaticFiles マウント（L357）はそのまま残す（D-10）。

**静的ファイルへの影響（D-10）:**
- `static/apps/{app_id}/index.html` は変更しない。
- FastAPI のルート優先順位: `GET /apps/{app_id}` 動的ルートが先に登録されるため StaticFiles より優先される。
- StaticFiles は動的ルートにマッチしないパス（例: `/apps/{app_id}/assets/...`）のフォールバックとして機能する。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && python -c "
from app.api.routes import iframe_rpc
import inspect
src = inspect.getsource(iframe_rpc.iframe_rpc)
assert 'get_jwt_payload' not in src, 'get_jwt_payload still present'
assert 'get_github_token' not in src, 'get_github_token still present'
print('JWT Depends removed OK')
" && python -c "
from app.api import main as m
import inspect
src = inspect.getsource(m)
assert 'hosted_apps' in src, 'hosted_apps not imported in main.py'
print('hosted_apps import OK')
"</automated>
  </verify>
  <done>
    - `iframe_rpc.py` から `Depends(get_jwt_payload)` と `Depends(get_github_token)` が削除されている
    - `github_login` が `"anonymous"` に変更されている
    - `github_token` が `auth_manager.load_token()` で取得されている
    - `main.py` に `hosted_apps` が import され `include_router(hosted_apps.router)` が `/apps` StaticFiles より前に登録されている
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 外部→GET /apps/{app_id} | 認証なし。app_id を知る任意ユーザーがアクセス可能 |
| iframe srcdoc→ブラウザ | Canvas アプリ HTML が sandboxed iframe で実行される |
| iframe→POST /api/iframe-rpc | D-07 により JWT 不要。arq へ enqueue される |
| parent-bridge.js→parent window | `e.source` で返信。targetOrigin '*' は srcdoc null origin への許容済みリスク |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Information Disclosure | GET /apps/{app_id} | accept | D-08 決定: URL を知る者は誰でもアクセス可能。社内ツール・200人規模なので許容。将来フェーズで JWT チェック追加予定 |
| T-19-02 | Tampering | srcdoc 注入（HTML エスケープ） | mitigate | DB の HTML を srcdoc 属性値に埋め込む前に `"` → `&quot;`、`&` → `&amp;` でエスケープ |
| T-19-03 | Elevation of Privilege | iframe sandbox の破壊 | mitigate | `sandbox="allow-scripts allow-forms"` のみ。`allow-same-origin` を除外することで iframe 内 JS が parent DOM にアクセス不可 |
| T-19-04 | Spoofing | POST /api/iframe-rpc JWT 削除後の不正利用 | accept | D-07 の暫定決定。SELECT + AI ワンショットのみ。破壊操作は Phase 18 D-09 で拒否済み |
| T-19-05 | Information Disclosure | DB app_id 列挙 | mitigate | UUIDv4 の app_id は予測不可能（128bit） |
| T-19-06 | Denial of Service | 大量 GET /apps リクエスト | accept | 社内 200 名規模。レートリミットは現フェーズ対象外 |
| T-19-07 | Spoofing | parent-bridge.js の postMessage 受信元偽装 | mitigate | origin 検証（`window.location.origin` または `'null'`）を実施。不一致は即時 return |
</threat_model>

<verification>
Phase 19 全体の完了確認:

1. `GET /apps/{valid_app_id}` がブラウザで shell HTML を返すこと（srcdoc に HTML が埋め込まれている）
2. `GET /apps/{invalid_app_id}` が 404 を返すこと
3. shell HTML の iframe に `sandbox="allow-scripts allow-forms"` が設定されていること（`allow-same-origin` なし）
4. shell HTML に `<script src="/js/parent-bridge.js">` が含まれること
5. `POST /api/iframe-rpc` を JWT Cookie なしで呼び出せること（200 + job_id が返る）
6. `static/js/parent-bridge.js` が存在し `__parentBridgeInstalled` ガードを持つこと
7. CanvasPane.tsx に `iframeRef` が残っていないこと
8. FastAPI 起動時エラーなし
</verification>

<success_criteria>
- `static/js/parent-bridge.js` が存在し親フレームリレーロジックを持つ
- `app/api/routes/hosted_apps.py` が存在し `GET /apps/{app_id}` を提供している
- shell テンプレートが `parent-bridge.js` を読み込む（`iframe-rpc.js` は読み込まない）
- `app/api/routes/iframe_rpc.py` から JWT 依存が削除されている
- `app/api/main.py` が `hosted_apps.router` を `/apps` StaticFiles より前に登録している
- `frontend/src/components/CanvasPane.tsx` から `iframeRef` と inline handleIframeMessage が除去されている
- 全 13 決定事項（D-01〜D-13）が実装に反映されている
- FastAPI アプリケーションが import エラーなしで起動できる
</success_criteria>

<output>
完了後、`.planning/phases/19-canvas-apps-app-id-iframe-phase-18-rpc/19-01-SUMMARY.md` を作成すること。
</output>
