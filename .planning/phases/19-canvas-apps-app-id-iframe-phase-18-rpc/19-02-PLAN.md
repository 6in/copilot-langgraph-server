---
phase: 19-canvas-apps-app-id-iframe-phase-18-rpc
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01-PLAN.md
files_modified: []
autonomous: false
requirements:
  - D-01
  - D-04
  - D-06
  - D-07
  - D-11
  - D-12
  - D-13

must_haves:
  truths:
    - "デプロイ済み Canvas アプリが /apps/{app_id}/ でブラウザ上に表示される"
    - "iframe 内の JS から RPC ブリッジが動作する"
    - "未登録 app_id へのアクセスは 404 になる"
  artifacts:
    - path: "app/api/routes/hosted_apps.py"
      provides: "動作確認済み GET /apps/{app_id}"
    - path: "app/api/routes/iframe_rpc.py"
      provides: "JWT なし POST /api/iframe-rpc"
  key_links:
    - from: "browser"
      to: "GET /apps/{app_id}"
      via: "HTTP GET"
      pattern: "200 HTMLResponse with srcdoc"
---

<objective>
Phase 19 実装の動作確認チェックポイント。

Purpose: Plan 01 で実装した hosted_apps ルートと iframe_rpc 認証削除が正常に機能することを人間の目で確認する。
Output: 承認されれば Phase 19 完了。問題があれば修正タスクに戻る。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-canvas-apps-app-id-iframe-phase-18-rpc/19-CONTEXT.md
@.planning/phases/19-canvas-apps-app-id-iframe-phase-18-rpc/19-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 自動検証 — import + ルート構造チェック</name>
  <files></files>
  <action>
以下のコマンドで実装の整合性を自動検証する。

**1. モジュール import チェック:**
```bash
cd /home/parallels/workspaces/copilot-langgraph
python -c "
from app.api.routes.hosted_apps import router as hr
from app.api.routes import iframe_rpc

# hosted_apps: /apps/{app_id} ルートが存在するか
paths = [r.path for r in hr.routes]
assert '/apps/{app_id}' in paths, f'/apps/{{app_id}} not found in {paths}'
print('hosted_apps: /apps/{app_id} OK')

# iframe_rpc: JWT Depends が消えているか
import inspect
src = inspect.getsource(iframe_rpc.iframe_rpc)
assert 'get_jwt_payload' not in src, 'get_jwt_payload still present'
assert 'get_github_token' not in src, 'get_github_token still present'
print('iframe_rpc: JWT Depends removed OK')
"
```

**2. main.py ルート登録順チェック:**
```bash
python -c "
import ast, pathlib
src = pathlib.Path('app/api/main.py').read_text()
hosted_pos = src.index('hosted_apps')
mount_pos = src.index('mount(\"/apps\"')
assert hosted_pos < mount_pos, f'hosted_apps ({hosted_pos}) must come before /apps mount ({mount_pos})'
print('Route order OK: hosted_apps before /apps StaticFiles')
"
```

**3. shell HTML 構造チェック（テンプレート内容）:**
```bash
python -c "
from app.api.routes.hosted_apps import _SHELL_TEMPLATE
assert 'sandbox=\"allow-scripts allow-forms\"' in _SHELL_TEMPLATE, 'sandbox attr missing'
assert 'allow-same-origin' not in _SHELL_TEMPLATE, 'allow-same-origin must NOT be present'
assert '/js/iframe-rpc.js' in _SHELL_TEMPLATE, 'iframe-rpc.js script missing'
assert 'srcdoc=' in _SHELL_TEMPLATE, 'srcdoc attr missing'
print('Shell HTML template OK')
"
```
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && python -c "from app.api.routes.hosted_apps import router, _SHELL_TEMPLATE; from app.api.routes import iframe_rpc; import inspect; src = inspect.getsource(iframe_rpc.iframe_rpc); assert 'get_jwt_payload' not in src; assert '/apps/{app_id}' in [r.path for r in router.routes]; assert 'allow-scripts allow-forms' in _SHELL_TEMPLATE; assert 'allow-same-origin' not in _SHELL_TEMPLATE; assert '/js/iframe-rpc.js' in _SHELL_TEMPLATE; print('ALL CHECKS PASSED')"</automated>
  </verify>
  <done>
    - 全自動チェックが PASSED
    - ルート登録順が正しい（hosted_apps < /apps StaticFiles）
    - shell テンプレートの属性値が正確
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Plan 01 で実装:
- `GET /apps/{app_id}` — DB から Canvas アプリ HTML を取得し srcdoc iframe に埋め込んだ shell HTML を返す（認証不要）
- `POST /api/iframe-rpc` — JWT 認証削除済み（ホスティングシェルから直接呼び出し可能）
- `main.py` — hosted_apps.router を /apps StaticFiles より前に登録済み
  </what-built>
  <how-to-verify>
**前提: `docker compose up` でサービスが起動していること**

**1. 既存のデプロイ済み Canvas アプリの app_id を確認:**
```bash
docker compose exec postgres psql -U app -d app -c \
  "SELECT app_id, name, LEFT(html, 50) as html_preview FROM canvas_apps WHERE html != '' LIMIT 3;"
```
app_id をメモする（UUID 形式）。

**2. ホスティング URL をブラウザで開く:**
```
http://localhost:8000/apps/{取得した app_id}
```
または nginx 経由の場合:
```
http://localhost:5173/apps/{app_id}
```
（Vite dev server は /apps をプロキシしないため、直接 :8000 にアクセスするほうが確実）

**3. 確認ポイント:**
- [ ] ページが表示される（白画面ではなく Canvas アプリの UI が iframe 内に表示）
- [ ] ブラウザの DevTools で iframe の `sandbox` 属性が `allow-scripts allow-forms` のみであること（`allow-same-origin` がないこと）
- [ ] DevTools の Network タブで `/js/iframe-rpc.js` が 200 で読み込まれていること

**4. 404 確認:**
```
http://localhost:8000/apps/00000000-0000-0000-0000-000000000000
```
→ `{"detail": "Canvas app not found"}` が返ること

**5. iframe-rpc 認証なし呼び出し確認:**
```bash
curl -s -X POST http://localhost:8000/api/iframe-rpc \
  -H "Content-Type: application/json" \
  -d '{"id": "test-1", "method": "QUERY", "params": {"pool_name": "main", "sql": "SELECT 1", "user": "test"}}' \
  | python3 -m json.tool
```
→ `{"job_id": "..."}` が返ること（JWT Cookie なしで 200）
  </how-to-verify>
  <resume-signal>動作確認 OK なら「approved」、問題があれば内容を記述してください。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 検証者→アプリ | 動作確認のみ。本番データへの影響なし |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | - | Plan 02 (verification only) | accept | このプランは検証のみ。新たな脅威面なし |
</threat_model>

<verification>
Task 1 の自動チェック全項目が PASSED、かつ Task 2 のチェックポイントで「approved」が得られること。
</verification>

<success_criteria>
- 自動検証 ALL CHECKS PASSED
- ブラウザで Canvas アプリが /apps/{app_id} に表示される
- sandbox 属性に allow-same-origin が含まれない
- /js/iframe-rpc.js が正常読み込み
- JWT なしで /api/iframe-rpc が 200 を返す
- 存在しない app_id で 404 が返る
</success_criteria>

<output>
完了後、`.planning/phases/19-canvas-apps-app-id-iframe-phase-18-rpc/19-02-SUMMARY.md` を作成すること。
</output>
