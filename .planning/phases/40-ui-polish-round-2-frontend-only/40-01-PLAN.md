---
phase: 40
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/GemsScreen.tsx
  - frontend/src/components/CanvasScreen.tsx
autonomous: true
requirements:
  - UI-BACKBUTTON
user_setup: []

must_haves:
  truths:
    - Gems 画面の戻るボタンが画面遷移してもヘッダー左端 (Chat/SuperChat と同 viewport 座標) から動かない
    - Canvas 画面の戻るボタンが画面遷移してもヘッダー左端 (Chat/SuperChat と同 viewport 座標) から動かない
    - Gems / Canvas 画面のヘッダーに appName ("Gems" / "Canvas") が Header.tsx の "Orochi Chat &middot; {appName}" 形式で表示される
    - GemsScreen.tsx / CanvasScreen.tsx の内部に独自 "← Back" ボタンが残らない (画面内の重複戻るボタンが消える)
  artifacts:
    - path: frontend/src/App.tsx
      provides: GemsScreenRoute と CanvasScreenRoute が Header に onBackToMenu と appName を渡す
      contains: 'appName="Gems"'
    - path: frontend/src/App.tsx
      provides: GemsScreenRoute と CanvasScreenRoute が Header に onBackToMenu と appName を渡す
      contains: 'appName="Canvas"'
    - path: frontend/src/components/GemsScreen.tsx
      provides: 画面内戻るボタンが削除され、見出し "Gems" のみが残る
    - path: frontend/src/components/CanvasScreen.tsx
      provides: 画面内戻るボタンが削除され、見出し "Canvas Apps" のみが残る
  key_links:
    - from: frontend/src/App.tsx GemsScreenRoute
      to: frontend/src/components/Header.tsx
      via: Header props onBackToMenu={() => navigate('/')} と appName="Gems"
      pattern: 'onBackToMenu=.*navigate.*appName="Gems"'
    - from: frontend/src/App.tsx CanvasScreenRoute
      to: frontend/src/components/Header.tsx
      via: Header props onBackToMenu={() => navigate('/')} と appName="Canvas"
      pattern: 'onBackToMenu=.*navigate.*appName="Canvas"'
---

<objective>
GemsScreen と CanvasScreen の戻るボタンを、ChatApp / SuperChatApp / DebateChatApp と同じ共有 Header コンポーネント (frontend/src/components/Header.tsx L84-100 の `‹ メニュー` ボタン) に統一する。

Purpose: 画面遷移ごとに戻るボタンが viewport 上で「飛ぶ」現象を解消し、メニュー画面間移動の視覚一貫性を Phase 35 design system のヘッダー領域に揃える。

Output: App.tsx 内の GemsScreenRoute / CanvasScreenRoute が Header に onBackToMenu と appName を渡し、GemsScreen.tsx / CanvasScreen.tsx は画面内の独自 "← Back" ボタンを削除した版になる。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/patterns.md
@docs/adr/INDEX.md
@.planning/todos/pending/2026-05-13-align-back-button-position-in-gems-and-canvas-screens-with-c.md

<interfaces>
<!-- Header.tsx の最終インタフェース (frontend/src/components/Header.tsx L13-20 から抽出) -->
<!-- 既存。本 plan で変更しない。GemsScreenRoute / CanvasScreenRoute は以下の prop シグネチャに合わせて Header を組み込む。 -->

interface HeaderProps {
  selectedModel: string;
  onModelChange: (model: string) => void;
  theme: 'light' | 'dark';
  onToggleTheme: () => void;
  onBackToMenu?: () => void;   // 渡すと "‹ メニュー" ボタンが Header 左端に表示される (L84-100)
  appName?: string;             // 渡すと "Orochi Chat · {appName}" として L111-124 に表示される
}

<!-- DebateRoute がすでに同パターンを採用 (frontend/src/App.tsx L231)。本 plan はこの形を踏襲する。 -->
<!-- 既存例: <Header ... onBackToMenu={() => navigate('/')} appName="討論チャット" /> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App.tsx の GemsScreenRoute / CanvasScreenRoute に Header の onBackToMenu / appName 配線を追加する</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx (現状の GemsScreenRoute L169-182, CanvasScreenRoute L184-197, 参照する Header の使い方は ChatRoute L200-211 / DebateRoute L225-235)
    - frontend/src/components/Header.tsx (HeaderProps L13-20 と L84-100 / L111-124 の onBackToMenu・appName 描画箇所)
    - .planning/todos/pending/2026-05-13-align-back-button-position-in-gems-and-canvas-screens-with-c.md (案 1: 共通 Header 導入)
  </read_first>
  <action>
    frontend/src/App.tsx の GemsScreenRoute (現状 L169-182) と CanvasScreenRoute (現状 L184-197) を編集する。

    GemsScreenRoute の Header タグを `<Header selectedModel={selectedModel} onModelChange={onModelChange} theme={theme} onToggleTheme={onToggleTheme} onBackToMenu={() => navigate('/')} appName="Gems" />` に変更する。GemsScreen は引き続き onBack={() => navigate('/')} を受け取り、GemsScreen 側で onBack が呼ばれた場合でも navigate('/') 同等の挙動になるよう一旦そのまま維持する (後続タスクで GemsScreen 内戻るボタンを削除する)。

    CanvasScreenRoute の Header タグを `<Header selectedModel={selectedModel} onModelChange={onModelChange} theme={theme} onToggleTheme={onToggleTheme} onBackToMenu={() => navigate('/')} appName="Canvas" />` に変更する。CanvasScreen は引き続き onBack={() => navigate('/')} を受け取る。

    DebateRoute (L225-235) の既存パターン `<Header ... onBackToMenu={() => navigate('/')} appName="討論チャット" />` を参考にする。MenuScreenRoute (L149-167) は意図的に変更しない (メニュー自身が "Back" 不要)。
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n 'appName="Gems"' frontend/src/App.tsx` が 1 件以上を返す
    - Source assertion: `grep -n 'appName="Canvas"' frontend/src/App.tsx` が 1 件以上を返す
    - Source assertion: `grep -c 'onBackToMenu={() => navigate(.\.).}' frontend/src/App.tsx` が 4 以上 (ChatRoute / DebateRoute / GemsScreenRoute / CanvasScreenRoute)
    - Test command: `cd frontend && bun run typecheck` exit 0
    - Source negation: `frontend/src/App.tsx` の MenuScreenRoute (L149-167 相当) には `onBackToMenu` 配線を**追加しない** — メニュー画面では Back ボタンを出さない
  </acceptance_criteria>
  <done>App.tsx の 2 つの Screen ルートが Header の onBackToMenu / appName を受けて Chat / SuperChat / Debate と同じヘッダー位置に Back ボタンを表示する状態になる。bun run typecheck は通る。</done>
</task>

<task type="auto">
  <name>Task 2: GemsScreen.tsx / CanvasScreen.tsx から独自 "← Back" ボタンを削除し、見出しのみ残す</name>
  <files>frontend/src/components/GemsScreen.tsx, frontend/src/components/CanvasScreen.tsx</files>
  <read_first>
    - frontend/src/components/GemsScreen.tsx (現状の戻るボタン L288-301、ヘッダー行 L279-312、props 受け取り L44 — onBack は signature 上残す)
    - frontend/src/components/CanvasScreen.tsx (現状の戻るボタン L158-172、ヘッダー行 L156-176、props L11-14 — onBack は signature 上残す)
    - frontend/src/App.tsx の Task 1 で配線済 Header (Back は Header 側で担保される)
  </read_first>
  <action>
    GemsScreen.tsx の L280-312 のヘッダー行 (Back ボタン + h1 "Gems") を編集する。L288-301 の `<button onClick={onBack}>← Back</button>` 要素を**完全に削除**し、h1 "Gems" のみが残るレイアウトにする。gap: '16px' は h1 単独でも問題ないが marginBottom: '16px' は維持する。props 型 GemsScreenProps の onBack: () => void シグネチャは変更しない (呼び出し元の App.tsx GemsScreenRoute が引き続き onBack を渡すため)。useGems / 検索フォーム / Create / Edit / Delete などの他 UI 要素は触らない。

    CanvasScreen.tsx の L156-176 のヘッダー行を同様に編集する。L158-172 の `<button onClick={onBack} aria-label="Back to menu">← Back</button>` 要素を**完全に削除**し、h1 "Canvas Apps" のみが残るレイアウトにする。props 型 CanvasScreenProps の onBack: () => void シグネチャは変更しない (App.tsx CanvasScreenRoute が引き続き onBack を渡すため)。CanvasAppCard / 検索 / Loading / Error の他要素は触らない。

    両ファイルで内部の textColor / focus-visible スタイルは Back ボタン以外でも使われているので削除しない。
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; bun run typecheck &amp;&amp; bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - Source negation: `grep -n '← Back' frontend/src/components/GemsScreen.tsx` が 0 件
    - Source negation: `grep -n '← Back' frontend/src/components/CanvasScreen.tsx` が 0 件
    - Source assertion: `grep -n '>Gems<' frontend/src/components/GemsScreen.tsx` で h1 が残っていることを確認 (1 件以上)
    - Source assertion: `grep -n 'Canvas Apps' frontend/src/components/CanvasScreen.tsx` で h1 が残っていることを確認 (1 件以上)
    - Source assertion: `grep -n 'onBack:.*=>.*void' frontend/src/components/GemsScreen.tsx` で onBack prop 型シグネチャが残っている (1 件以上)
    - Source assertion: `grep -n 'onBack:.*=>.*void' frontend/src/components/CanvasScreen.tsx` で onBack prop 型シグネチャが残っている (1 件以上)
    - Test command: `cd frontend && bun run typecheck` exit 0
    - Test command: `cd frontend && bun run lint` exit 0
  </acceptance_criteria>
  <done>GemsScreen.tsx / CanvasScreen.tsx から独自 Back ボタンが完全に消え、各画面のヘッダー領域は共有 Header (App.tsx 側) のみで表現される状態。typecheck / lint 共に通る。</done>
</task>

</tasks>

<verification>
- bun run typecheck (frontend/) で 0 件
- bun run lint (frontend/) で 0 件
- 手動: docker compose up 後、http://localhost:5173/orochi/gems を開くと Header に "‹ メニュー" ボタン + "Orochi Chat · Gems" 表示。クリックで http://localhost:5173/orochi/ に戻る
- 手動: http://localhost:5173/orochi/canvas でも同様に "‹ メニュー" + "Orochi Chat · Canvas" 表示
- 手動: 画面内に "← Back" の独自ボタンが GemsScreen / CanvasScreen から消えている
</verification>

<success_criteria>
- ROADMAP Phase 40 Success Criteria 1 (Gems / Canvas Screen の戻るボタンが Chat/SuperChat の Header と同位置・同スタイルに揃っている) が観察可能
- GemsScreenRoute / CanvasScreenRoute / ChatRoute / DebateRoute の 4 画面で Back ボタンが viewport 上同座標 (Header 左端) に表示される
- TS / lint regression なし
</success_criteria>

<output>
After completion, create `.planning/phases/40-ui-polish-round-2-frontend-only/40-01-SUMMARY.md`
</output>
