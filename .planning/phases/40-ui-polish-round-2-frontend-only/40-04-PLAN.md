---
phase: 40
plan: 04
type: execute
wave: 3
depends_on:
  - 03
files_modified:
  - frontend/src/components/SuperChatApp.tsx
  - frontend/src/components/GemChatApp.tsx
  - frontend/src/components/CanvasChatApp.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - UI-ATTACHBTN
user_setup: []

must_haves:
  truths:
    - SuperChat (/orochi/superchat[/:tid]) で 📎 ボタンが InputBar toolbar slot に表示され、クリック / drop / Ctrl+V paste でファイルを staging できる
    - Gem Chat (/orochi/gemchat/:gemId[/:tid]) で 📎 ボタンが InputBar toolbar slot に表示され、同様に staging できる
    - Canvas Chat (/orochi/canvaschat[/:tid]) で 📎 ボタンが InputBar toolbar slot に表示され、同様に staging できる
    - Debate Chat (/orochi/debate[/:tid]) には 📎 ボタンを**追加しない** (debate_handler が attachments 非対応、Out of Scope)
    - 3 アプリで attachments が ChatRequest body に乗り、Phase 36 既存の langgraph_handler.py / orchestrator_handler.py の attachments パイプラインで処理される (新規 backend 変更ゼロ)
    - vision 非対応モデルでの画像 staging 時、ChatApp と同じ VisionWarningBanner が SuperChat / Gem / Canvas でも表示される
    - 送信成功時 (D-06 ケース C) に staging がクリアされ、技術失敗 (ケース B) / ユーザーキャンセル (ケース A) では staging が残る (ChatApp と同等)
  artifacts:
    - path: frontend/src/components/SuperChatApp.tsx
      provides: useAttachments / AttachmentButton / AttachmentChips / VisionWarningBanner 配線、drop / paste handler、useChat への getReadyAttachments / onAttachmentsSent props
      contains: useAttachments
    - path: frontend/src/components/GemChatApp.tsx
      provides: 同上 (Gem 用)
      contains: useAttachments
    - path: frontend/src/components/CanvasChatApp.tsx
      provides: 同上 (Canvas 用 — CanvasPane との競合に注意)
      contains: useAttachments
  key_links:
    - from: SuperChatApp.tsx の useAttachments
      to: MessageArea.tsx inputToolbarSlot / inputPreviewSlot / inputWarningSlot
      via: '<AttachmentButton onFilesSelected={(files) => attachments.upload(files)} disabled={...} />'
      pattern: 'AttachmentButton'
    - from: SuperChatApp.tsx / GemChatApp.tsx / CanvasChatApp.tsx の useChat 呼び出し
      to: useChat options (getReadyAttachments, onAttachmentsSent)
      via: 'getReadyAttachments={attachments.getReadyItems}, onAttachmentsSent={attachments.clearAll}'
      pattern: 'getReadyAttachments'
---

<objective>
Phase 36 で ChatApp.tsx にのみ配線された AttachmentButton + useAttachments パイプラインを、SuperChat / Gem Chat / Canvas Chat の 3 アプリにも展開する。Debate Chat は backend が attachments 非対応のため明示的に除外 (Out of Scope)。

Purpose: ユーザー報告 todo #13 — 「ファイル添付クリップが Chat 以外で発見不可能」を解消する。Phase 36 で確立した HumanMessage.additional_kwargs サイドカー envelope (patterns.md "HumanMessage.additional_kwargs サイドカー envelope" / ADR-0050) は backend で既に動作しており、frontend 側で 3 アプリに同じ wiring を加えるだけで vision multimodal / text/code 抽出ともに動作する。

Output: SuperChatApp.tsx / GemChatApp.tsx / CanvasChatApp.tsx の 3 ファイルに ChatApp.tsx と同等の useAttachments hook + AttachmentButton InputBar slot 配線 + drop/paste handler + VisionWarningBanner + useChat への getReadyAttachments / onAttachmentsSent props 追加。Debate には変更を加えない。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/patterns.md
@docs/adr/INDEX.md
@docs/adr/0050-copilot-sdk-multimodal-attachments.md
@.planning/todos/pending/2026-05-13-propagate-attachmentbutton-to-superchat-gem-canvas-debate-ch.md

<interfaces>
<!-- ChatApp.tsx (frontend/src/components/ChatApp.tsx) の参照実装 — Phase 36 で確立済: -->

<!-- L13-15 imports: -->
<!--   import { AttachmentButton } from './AttachmentButton'; -->
<!--   import { AttachmentChips } from './AttachmentChips'; -->
<!--   import { VisionWarningBanner } from './VisionWarningBanner'; -->
<!-- L19-20 hooks: -->
<!--   import { useAttachments } from '../hooks/useAttachments'; -->
<!--   import { useModels } from '../hooks/useModels'; -->

<!-- L85-115 currentModelInfo / vision 判定: -->
<!--   const { modelById, suggestedVisionModel } = useModels(); -->
<!--   const currentModelInfo = modelById(selectedModel); -->
<!--   const attachments = useAttachments(activeThreadId, currentModelInfo ?? null); -->
<!--   const [warningDismissed, setWarningDismissed] = useState(false); -->
<!--   useEffect(() => { setWarningDismissed(false); }, [selectedModel]); -->
<!--   const hasStagedImages = attachments.items.some(it => ['png','jpg','jpeg','webp'].includes(it.ext.toLowerCase())); -->
<!--   const showVisionWarning = !!(hasStagedImages && currentModelInfo && currentModelInfo.vision === false && suggestedVisionModel && !warningDismissed); -->
<!--   const handleSwitchModel = useCallback(() => { if (suggestedVisionModel) onModelChange(suggestedVisionModel); }, [suggestedVisionModel, onModelChange]); -->

<!-- L117-159 drag/drop/paste handlers: -->
<!--   const [dragOver, setDragOver] = useState(false); -->
<!--   const rootRef = useRef<HTMLDivElement>(null); -->
<!--   onDragOver / onDragLeave / onDrop コールバックで attachments.upload(files) -->
<!--   useEffect で document 全体に paste listener を追加し、image/* clipboard item を upload -->

<!-- L176-186 useChat options: -->
<!--   const { ... } = useChat({ -->
<!--     activeThreadId, selectedModel, setMessages, refreshThreads, -->
<!--     onCanvasResponse: (app) => setCanvasApp(app), -->
<!--     getReadyAttachments: attachments.getReadyItems,    -->
<!--     onAttachmentsSent: attachments.clearAll,           -->
<!--   }); -->

<!-- L233-238 root div: onDragOver / onDragLeave / onDrop / ref={rootRef} / position: relative + overflow: hidden -->
<!-- L240-269 drop overlay (dragOver 中) -->
<!-- L271-297 validation error banner (attachments.validationError) -->

<!-- L337-372 MessageArea props: -->
<!--   inputToolbarSlot={ <AttachmentButton onFilesSelected={(files) => attachments.upload(files)} disabled={isThinking || !activeThreadId} disabledReason={!activeThreadId ? 'no-thread' : 'thinking'} /> } -->
<!--   inputPreviewSlot={ <AttachmentChips items={attachments.items} onRemove={attachments.removeItem} /> } -->
<!--   inputWarningSlot={ showVisionWarning && suggestedVisionModel ? <VisionWarningBanner ... /> : undefined } -->
<!--   activeThreadId={activeThreadId} -->

<!-- ChatApp interface (L35-39): -->
<!--   interface ChatAppProps { selectedModel: string; onModelChange: (model: string) => void; } -->
<!--   ←  onModelChange は VisionWarningBanner の "別モデルへ切替" CTA で必要 -->

<!-- SuperChatApp.tsx 既存シグネチャ (L28-33): -->
<!--   interface SuperChatAppProps { selectedModel: string; appId: string; appName: string; appAgents?: string[]; } -->
<!--   → onModelChange が無いため、本 plan で onModelChange?: (m: string) => void を optional 追加し App.tsx SuperChatWrapper から渡す -->

<!-- GemChatApp.tsx 既存シグネチャ (L22-26): -->
<!--   interface GemChatAppProps { gem: GemInfo; selectedModel: string; onBack: () => void; } -->
<!--   → 同様に onModelChange?: optional 追加 -->

<!-- CanvasChatApp.tsx 既存シグネチャ (L27-30): -->
<!--   interface CanvasChatAppProps { selectedModel: string; onBack: () => void; } -->
<!--   → 同様に onModelChange?: optional 追加 -->

<!-- Debate (frontend/src/components/DebateChatApp.tsx L780-792): -->
<!--   MessageArea を使うが inputToolbarSlot は **渡さない** — 本 plan で変更しない -->
<!--   backend (app/jobs/handlers/debate_handler.py) が ChatRequest.attachments を読まないため、UI に出すと送信時に黙って捨てられる -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SuperChatApp.tsx に useAttachments + AttachmentButton + drop/paste + VisionWarningBanner を ChatApp.tsx 同形で配線する</name>
  <files>frontend/src/components/SuperChatApp.tsx, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/components/ChatApp.tsx (参照実装、特に L13-20 imports, L85-159 hooks + drop/paste, L176-186 useChat options, L226-269 root div + drop overlay, L271-297 validation error banner, L337-372 MessageArea props)
    - frontend/src/components/SuperChatApp.tsx (現状 L1-322 全体 — useChat 呼び出し L184-194, return L242-322)
    - frontend/src/components/AttachmentButton.tsx (props interface L7-12)
    - frontend/src/hooks/useAttachments.ts (hook signature 確認、特に第 2 引数 modelInfo の意味)
    - frontend/src/hooks/useChat.ts L24-47 (UseChatOptions の getReadyAttachments / onAttachmentsSent)
    - frontend/src/App.tsx SuperChatWrapper (L35-92 — onModelChange 経由 props を SuperChatApp に渡す配線追加が必要)
    - frontend/src/components/VisionWarningBanner.tsx (props 確認)
  </read_first>
  <action>
    SuperChatApp.tsx を ChatApp.tsx と同形に拡張する。

    (a) imports に AttachmentButton / AttachmentChips / VisionWarningBanner / useAttachments / useModels を追加する (パスはそれぞれ './AttachmentButton', './AttachmentChips', './VisionWarningBanner', '../hooks/useAttachments', '../hooks/useModels')。

    (b) SuperChatAppProps interface (L28-33) に `onModelChange?: (model: string) => void` を**省略可能 prop として**追加する (Debate / Gem / Canvas でも同様に省略可能とする — Task 2/3 の方針と統一)。

    (c) component 本体 (L121-) に ChatApp.tsx L85-159 と同じ hook 群を追加する: const { modelById, suggestedVisionModel } = useModels(); const currentModelInfo = modelById(selectedModel); const attachments = useAttachments(activeThreadId, currentModelInfo ?? null); の 3 行を useThreads 直後に挿入する。warningDismissed state + selectedModel 変更時のリセット useEffect も追加する。

    (d) useChat 呼び出し (L184-194) に getReadyAttachments: attachments.getReadyItems と onAttachmentsSent: attachments.clearAll を追加する。

    (e) drop / paste handler (ChatApp.tsx L117-159) を **そのまま** SuperChatApp に追加する。dragOver state + rootRef + onDragOver / onDragLeave / onDrop コールバック + paste useEffect。

    (f) showVisionWarning / handleSwitchModel ロジック (ChatApp.tsx L100-115) を追加する。onModelChange は SuperChatAppProps から受け取ったものを使う (Task 1 (b))。

    (g) return の最外 div (L243) を `<div ref={rootRef} style={{ flex: 1, minHeight: 0, overflow: 'hidden', position: 'relative' }} onDragOver={onDragOver} onDragLeave={onDragLeave} onDrop={onDrop}>` に変更する。直下に drop overlay (ChatApp.tsx L240-269) と validation error banner (L271-297) を挿入する。

    (h) MessageArea (現状 L293-304) の props に以下を追加する:
        - inputToolbarSlot: ChatApp.tsx L348-354 と同形 (`<AttachmentButton onFilesSelected={(files) => attachments.upload(files)} disabled={isThinking || !activeThreadId} disabledReason={!activeThreadId ? 'no-thread' : 'thinking'} />`)
        - inputPreviewSlot: ChatApp.tsx L355-360 と同形 (`<AttachmentChips items={attachments.items} onRemove={attachments.removeItem} />`)
        - inputWarningSlot: ChatApp.tsx L362-371 と同形 (showVisionWarning && suggestedVisionModel ? <VisionWarningBanner currentModel={currentModelInfo!.name} suggestedModel={modelById(suggestedVisionModel)?.name ?? suggestedVisionModel} onSwitchModel={handleSwitchModel} onDismiss={() => setWarningDismissed(true)} /> : undefined)
        - activeThreadId={activeThreadId}

    (i) App.tsx SuperChatWrapper (L74-91) の `<SuperChatApp selectedModel={selectedModel} appId={app.slug} appName={app.name} appAgents={app.agents} />` に `onModelChange={onModelChange}` を追加する。これで App.tsx の common props (`onModelChange: setSelectedModel`) が SuperChatApp → VisionWarningBanner CTA に届く。SuperChatWrapper の関数 signature は既に common props を受けているので、本 plan ではその onModelChange を SuperChatApp 呼び出しに渡すだけで良い。
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'import { AttachmentButton }' frontend/src/components/SuperChatApp.tsx` が 1 件
    - Source assertion: `grep -c 'useAttachments(' frontend/src/components/SuperChatApp.tsx` が 1 件
    - Source assertion: `grep -c 'getReadyAttachments:' frontend/src/components/SuperChatApp.tsx` が 1 件
    - Source assertion: `grep -c 'onAttachmentsSent:' frontend/src/components/SuperChatApp.tsx` が 1 件
    - Source assertion: `grep -c 'inputToolbarSlot=' frontend/src/components/SuperChatApp.tsx` が 1 件
    - Source assertion: `grep -c 'onDragOver={onDragOver}' frontend/src/components/SuperChatApp.tsx` が 1 件
    - Source assertion: `grep -cE "addEventListener\\(['\"]paste" frontend/src/components/SuperChatApp.tsx` が 1 件以上
    - Source assertion: `grep -c 'onModelChange' frontend/src/components/SuperChatApp.tsx` が 1 件以上 (props interface + 利用箇所)
    - Source assertion: `grep -c 'onModelChange={onModelChange}' frontend/src/App.tsx` が 4 件以上 (ChatRoute / CanvasChatRoute / DebateRoute / SuperChatWrapper / GemChatWrapper のうち 4 以上が onModelChange={onModelChange} を持つ)
    - Test command: `cd frontend && bun run typecheck` exit 0
  </acceptance_criteria>
  <done>SuperChatApp.tsx が ChatApp.tsx と同じ AttachmentButton + drop/paste + VisionWarningBanner 配線を持つ。App.tsx SuperChatWrapper が onModelChange を SuperChatApp に渡している。typecheck 通過。</done>
</task>

<task type="auto">
  <name>Task 2: GemChatApp.tsx に useAttachments + AttachmentButton + drop/paste + VisionWarningBanner を ChatApp.tsx 同形で配線し、App.tsx GemChatWrapper から onModelChange を渡す</name>
  <files>frontend/src/components/GemChatApp.tsx, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/components/ChatApp.tsx (参照実装、L13-20 imports, L85-159 hooks + drop/paste, L176-186 useChat options, L226-269 root div + drop overlay, L271-297 validation error banner, L337-372 MessageArea props)
    - frontend/src/components/GemChatApp.tsx (現状 L1-223 — useChat 呼び出し位置を把握、return 構造の Gem ヘッダーバー L124-167 と MainContainer L170-)
    - frontend/src/components/SuperChatApp.tsx (Task 1 完了後の参照実装)
    - frontend/src/App.tsx GemChatWrapper (L94-146 — onModelChange 配線が現状 GemChatApp に渡っていないことを確認)
  </read_first>
  <action>
    GemChatApp.tsx を ChatApp.tsx と同形に拡張する。

    (a) imports に AttachmentButton / AttachmentChips / VisionWarningBanner / useAttachments / useModels を追加する。

    (b) GemChatAppProps interface (L22-26) に `onModelChange?: (model: string) => void` を追加する。

    (c) component 本体 (L28-) に useChat より前で attachments hook を初期化する: useModels / currentModelInfo / attachments / warningDismissed の 4 ブロック (ChatApp.tsx L85-115 と同形)。

    (d) GemChatApp の useChat 呼び出し箇所 (現存) に getReadyAttachments: attachments.getReadyItems と onAttachmentsSent: attachments.clearAll を追加する。

    (e) drop / paste handler (ChatApp.tsx L117-159) を追加する。dragOver state + rootRef。

    (f) return の最外 `<div>` (L123, `<div style={{ display: 'flex', flexDirection: 'column', flex: 1, minHeight: 0 }}>`) はそのまま、その下の Gem ヘッダーバー (L124-167) を維持。**Gem ヘッダーバーの直下にある MainContainer の wrapper div** (L170 `<div style={{ flex: 1, minHeight: 0, overflow: 'hidden' }}>`) を編集して `ref={rootRef}`, `position: 'relative'`, `onDragOver={onDragOver}`, `onDragLeave={onDragLeave}`, `onDrop={onDrop}` を追加する。

    (g) wrapper div 直下に drop overlay と validation error banner を追加する (ChatApp.tsx L240-297 と同形)。

    (h) MessageArea (現状 L207-216) の props に inputToolbarSlot / inputPreviewSlot / inputWarningSlot / activeThreadId を Task 1 (h) と同形で追加する。

    (i) App.tsx GemChatWrapper (L94-146) の `<GemChatApp gem={gem} selectedModel={selectedModel} onBack={() => navigate('/gems')} />` (L139-143) に `onModelChange={onModelChange}` を追加する。
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'import { AttachmentButton }' frontend/src/components/GemChatApp.tsx` が 1 件
    - Source assertion: `grep -c 'useAttachments(' frontend/src/components/GemChatApp.tsx` が 1 件
    - Source assertion: `grep -c 'inputToolbarSlot=' frontend/src/components/GemChatApp.tsx` が 1 件
    - Source assertion: `grep -c 'getReadyAttachments:' frontend/src/components/GemChatApp.tsx` が 1 件
    - Source assertion: `grep -c 'onAttachmentsSent:' frontend/src/components/GemChatApp.tsx` が 1 件
    - Source assertion: `grep -A3 'GemChatApp' frontend/src/App.tsx | grep -c 'onModelChange={onModelChange}'` が 1 件以上
    - Test command: `cd frontend && bun run typecheck` exit 0
  </acceptance_criteria>
  <done>GemChatApp.tsx が AttachmentButton + drop/paste + VisionWarningBanner 配線を持つ。App.tsx GemChatWrapper が onModelChange を渡している。typecheck 通過。</done>
</task>

<task type="auto">
  <name>Task 3: CanvasChatApp.tsx に useAttachments + AttachmentButton + drop/paste + VisionWarningBanner を配線し (CanvasPane との競合に注意)、App.tsx CanvasChatRoute から onModelChange を渡す</name>
  <files>frontend/src/components/CanvasChatApp.tsx, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/components/ChatApp.tsx (参照実装、特に L226-269 の drop overlay zIndex / pointer-events の取り扱い)
    - frontend/src/components/CanvasChatApp.tsx (現状 L1-345 程度 — useChat 呼び出し、CanvasPane の存在、L317-329 のドラッグハンドル領域、L331-345 の CanvasPane wrapper)
    - frontend/src/components/CanvasPane.tsx (iframe を含むため、drop 領域から iframe を除外する必要性を判断 — drop handler が iframe にバブルしないことを確認)
    - frontend/src/components/SuperChatApp.tsx Task 1 完了後の参照
    - frontend/src/App.tsx CanvasChatRoute (L213-223)
  </read_first>
  <action>
    CanvasChatApp.tsx を ChatApp.tsx と同形に拡張する。Canvas 特有の注意点: ファイルドロップが CanvasPane (iframe) で起きた場合に親 div ではなく iframe 内に dispatch されることがあるが、ChatApp の onDragOver は `e.dataTransfer.types.includes('Files')` で gate しており iframe 領域への drop は本ハンドラに来ない (iframe が postMessage 経由で別ライフサイクルになるため)。drop overlay の zIndex: 100 は CanvasPane 上に被さって構わない (drop 中のみ表示)。

    (a) imports に AttachmentButton / AttachmentChips / VisionWarningBanner / useAttachments / useModels を追加する。

    (b) CanvasChatAppProps interface (L27-30) に `onModelChange?: (model: string) => void` を追加する。

    (c) component 本体 (L32-) に useModels / currentModelInfo / attachments / warningDismissed の 4 ブロックを useChat より前で初期化する (ChatApp.tsx L85-115 同形)。

    (d) useChat 呼び出しに getReadyAttachments: attachments.getReadyItems と onAttachmentsSent: attachments.clearAll を追加する。

    (e) drop / paste handler (ChatApp.tsx L117-159) を追加する。

    (f) return の最外コンテナ div (現状 CanvasChatApp の outer wrapper) を編集して `ref={rootRef}`, `position: 'relative'`, `onDragOver={onDragOver}`, `onDragLeave={onDragLeave}`, `onDrop={onDrop}` を追加する。直下に drop overlay (zIndex: 100, pointer-events: 'none') と validation error banner を挿入する。

    (g) MessageArea (現状 L305-314) の props に inputToolbarSlot / inputPreviewSlot / inputWarningSlot / activeThreadId を Task 1 (h) と同形で追加する。

    (h) App.tsx CanvasChatRoute (L213-223) の `<CanvasChatApp selectedModel={selectedModel} onBack={() => navigate('/canvas')} />` (L220) に `onModelChange={onModelChange}` を追加する。
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'import { AttachmentButton }' frontend/src/components/CanvasChatApp.tsx` が 1 件
    - Source assertion: `grep -c 'useAttachments(' frontend/src/components/CanvasChatApp.tsx` が 1 件
    - Source assertion: `grep -c 'inputToolbarSlot=' frontend/src/components/CanvasChatApp.tsx` が 1 件
    - Source assertion: `grep -c 'getReadyAttachments:' frontend/src/components/CanvasChatApp.tsx` が 1 件
    - Source assertion: `grep -c 'onAttachmentsSent:' frontend/src/components/CanvasChatApp.tsx` が 1 件
    - Source assertion: `grep -A3 'CanvasChatApp ' frontend/src/App.tsx | grep -c 'onModelChange={onModelChange}'` が 1 件以上
    - Source negation: `grep -c 'inputToolbarSlot=' frontend/src/components/DebateChatApp.tsx` が 0 件 (Debate には**追加しない**ことを確認)
    - Test command: `cd frontend && bun run typecheck` exit 0
  </acceptance_criteria>
  <done>CanvasChatApp.tsx が AttachmentButton + drop/paste + VisionWarningBanner 配線を持つ。CanvasPane との視覚的競合は drop overlay の zIndex 設計で吸収済。App.tsx CanvasChatRoute が onModelChange を渡している。Debate は変更されていない (副作用確認)。typecheck 通過。</done>
</task>

</tasks>

<verification>
- bun run typecheck (frontend/) で 0 件
- bun run lint (frontend/) で 0 件
- 手動: docker compose up 後、SuperChat / Gem Chat / Canvas Chat それぞれで InputBar 左に 📎 ボタンが出現
- 手動: 各画面でテキストファイル (.md / .txt) を drop → AttachmentChips に staging される
- 手動: 画像ファイル (.png) を drop → AttachmentChips に staging される。vision 非対応モデル選択中なら VisionWarningBanner が出て CTA で別モデルへ切替できる
- 手動: 送信 → AI 応答に添付内容を反映 (text/code 抽出 or 画像認識)、staging クリア
- 手動: 送信中の cancel → staging 残る
- 手動: Debate Chat (/orochi/debate/...) は 📎 ボタンが**出ない** (Out of Scope の確認)
</verification>

<success_criteria>
- ROADMAP Phase 40 Success Criteria 4 (SuperChat / Gem / Canvas に AttachmentButton が追加され、Phase 36 の既存 attachments パイプラインで動作する。Debate は Phase 41 へ defer 明記) が観察可能
- backend 変更ゼロ (Phase 36 既存パイプライン経由)
- typecheck / lint regression なし
</success_criteria>

<output>
After completion, create `.planning/phases/40-ui-polish-round-2-frontend-only/40-04-SUMMARY.md`
</output>
