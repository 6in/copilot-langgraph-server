---
phase: 40
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/theme.css
autonomous: true
requirements:
  - UI-BALLOON
user_setup: []

must_haves:
  truths:
    - Debate Chat (/orochi/debate/:threadId) の各エージェントメッセージで chatscope デフォルト bubble (light mode 薄青 rgb(198, 227, 250)) が wrapper のエージェント別カラーから上下に「はみ出して」表示されない
    - 同 CSS override が Chat / SuperChat / Gem / Canvas の incoming AI バブルでも適用されるが、それら 4 アプリは MarkdownMessage 直下が senderName なしの構成のため視覚的に「外側 chatscope bubble の薄青背景が単独で残る」ことがなくなる (背景は MessageList 側に統一)
    - dark mode (data-theme="dark") では既存の `.cs-message--incoming .cs-message__content { background: var(--color-surface) !important; }` (theme.css L197-200) が優先され、暗色テーマで暗背景バブルが消失する regression は出ない
  artifacts:
    - path: frontend/src/theme.css
      provides: chatscope `cs-message__content` を全テーマで透明化する rule (cascade 上 light mode 既定の薄青を上書きする位置)
      contains: '.cs-message--incoming .cs-message__content'
  key_links:
    - from: frontend/src/theme.css の新規 rule (Phase 40 UIFIX)
      to: '@chatscope/chat-ui-kit-react の SECTION.cs-message--incoming > DIV.cs-message__content-wrapper > DIV.cs-message__content DOM 構造'
      via: CSS selector `.cs-message--incoming .cs-message__content { background: transparent !important; padding: 0 !important; }`
      pattern: 'background: transparent !important'
---

<objective>
Debate Chat で観察された "chatscope デフォルト薄青 bubble (rgb(198, 227, 250)) + Phase 35 エージェント別カラー wrapper" の 2 層重ね問題を、frontend/src/theme.css に CSS override を 1 か所追加することで解消する (todo の Option A)。

Purpose: ユーザー報告のスクリーンショット (todo の `/tmp/debate-actual.png` 参照) で確認された "外側 chatscope bubble の薄青が wrapper の上下から数 px はみ出して二重縁取りになる" 視覚バグを修正する。

Output: theme.css に追加された 1 つの CSS rule (background: transparent + padding: 0)。dark mode 既存 override (L197-200) は優先順位を保持。Chat/SuperChat/Gem/Canvas/Debate の 5 アプリで incoming bubble の見た目に副作用が出ないことを検証する。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/patterns.md
@docs/adr/INDEX.md
@.planning/todos/pending/2026-05-13-fix-overlapping-agent-message-balloons-in-debate-chat.md

<interfaces>
<!-- chatscope DOM (todo の "DOM 階層" セクションから抽出 — 実画面確認結果): -->
<!-- SECTION.cs-message.cs-message--incoming -->
<!--   DIV.cs-message__content-wrapper -->
<!--     DIV.cs-message__content   ← 修正対象: light mode 既定 background=rgb(198, 227, 250) -->
<!--       DIV.cs-message__custom-content -->
<!--         DIV (Phase 35 agentBgColor wrapper, MessageArea.tsx L486-491) -->
<!--           SPAN senderName badge / MarkdownMessage / AttachmentChipRow -->

<!-- 既存 theme.css の cs-message__content 関連 rule (L157-163, L197-200, L202-205): -->
<!-- L152-163: width / max-width / box-sizing のみ調整、background は触っていない -->
<!-- L197-200: [data-theme="dark"] .cs-message--incoming .cs-message__content { background: var(--color-surface) !important; ... } — これが dark mode override -->
<!-- L202-205: [data-theme="dark"] .cs-message--outgoing .cs-message__content { background: var(--color-surface-elevated) !important; ... } -->

<!-- Phase 35 wrapper の background は MessageArea.tsx L486-491 で msg.senderName がある時のみ agentBgColor(msg.senderName, theme) を適用 -->
<!-- => senderName が存在する場合 (SuperChat / Debate) のみ 2 層重ねが視覚的に発生 -->
<!-- => Chat / Gem / Canvas は通常 senderName なし → 外側 chatscope bubble の薄青が「単独で」現れる -->
<!--    今回の修正で外側を透明化することで、これら 3 アプリでは MessageList 側の background がそのまま見える形になる -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: theme.css に `.cs-message--incoming .cs-message__content { background: transparent !important; padding: 0 !important }` を追加し、dark mode override が後勝ちで残ることを確認する</name>
  <files>frontend/src/theme.css</files>
  <read_first>
    - frontend/src/theme.css (Phase 39 UIFIX-02 の既存 override L147-177、dark mode override L197-205、cascade 順序の現状把握。新規 rule は L177 の既存 Phase 39 ブロック直下に追加し、L197 の `[data-theme="dark"]` rule より前に置くことで dark mode 上書きが後勝ちになる)
    - frontend/src/components/MessageArea.tsx L470-510 (agentBgColor wrapper が `<Message.CustomContent>` の中で margin: -8px -12px / padding: 8px 12px を持っており、外側 chatscope の padding を打ち消す前提のレイアウトであることを確認 — 本 plan の `padding: 0` 設定と整合)
    - .planning/todos/pending/2026-05-13-fix-overlapping-agent-message-balloons-in-debate-chat.md (Option A の CSS 指定)
  </read_first>
  <action>
    frontend/src/theme.css L177 の既存 Phase 39 UIFIX-02 ブロック (L171-177) 直下に、Phase 40 UIFIX 用の新規 rule を追加する。

    新規 rule の selector は `.cs-message--incoming .cs-message__content` で、`background: transparent !important; padding: 0 !important;` を設定する。

    rule にはコメント `/* Phase 40 UIFIX: chatscope デフォルト薄青 bubble を透明化。Debate Chat (senderName 付き) の agentBgColor wrapper との 2 層重ねを解消。Chat/SuperChat/Gem/Canvas 側は MessageArea.tsx L486-491 の wrapper が senderName 無し時に発生しない (background style 自体が undefined) ため、外側透明化で MessageList の bg がそのまま見える形になる。dark mode override (L197-200) は本 rule の後に定義されているため、specificity 同等で後勝ちとなり data-theme="dark" 環境では --color-surface 背景が維持される。 */` を付ける。

    既存の light mode `.cs-message--incoming .cs-message__content { max-width: 100% ... }` (L157-163) は box-sizing / max-width を扱っていて本 rule と衝突しないため、両方残す。

    dark mode の outgoing rule (L202-205) は **触らない** (outgoing は今回の問題と無関係)。
  </action>
  <verify>
    <automated>grep -c 'background: transparent !important' frontend/src/theme.css | grep -v '^0$' &amp;&amp; cd frontend &amp;&amp; bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -A1 'Phase 40 UIFIX' frontend/src/theme.css | grep -c 'background: transparent !important'` が 1 件以上
    - Source assertion: `grep -B0 -A3 'Phase 40 UIFIX' frontend/src/theme.css | grep -c 'padding: 0 !important'` が 1 件以上
    - Source assertion: 新規 rule のセレクタが `.cs-message--incoming .cs-message__content` を含む (grep '.cs-message--incoming .cs-message__content' frontend/src/theme.css で 1 件以上)
    - Source ordering: 新規 rule の行番号 (grep -n 'Phase 40 UIFIX' で取得) が、`[data-theme="dark"] .cs-message--incoming .cs-message__content` の行番号 (grep -n '\[data-theme="dark"\] .cs-message--incoming .cs-message__content' で取得) より小さい → cascade 上 dark mode override が後勝ちになる
    - Test command: `cd frontend && bun run lint` exit 0
    - Source negation: `[data-theme="dark"] .cs-message--outgoing` を含む既存 rule (L202-205) は本 plan で変更しない (差分 0 行)
  </acceptance_criteria>
  <done>theme.css に "Phase 40 UIFIX" コメント付きで `.cs-message--incoming .cs-message__content { background: transparent !important; padding: 0 !important }` rule が追加され、dark mode override より前 (cascade 上で後勝ち) に配置されている。lint 通過。</done>
</task>

</tasks>

<verification>
- bun run lint (frontend/) で 0 件
- 手動: docker compose up 後、light mode で http://localhost:5173/orochi/debate/{既存 thread} を開き、エージェントバブルの上下から薄青がはみ出していないことを目視確認 (todo 添付スクリーンショット参照)
- 手動: dark mode (Header の 🌙 トグル後) で同 thread を開き、incoming バブル背景が暗色 (--color-surface) で表示され、透明 (= MessageList 背景剥き出し) にならないことを確認
- 手動: Chat (/orochi/chat) / SuperChat (/orochi/superchat — 後続 Plan 03 で routing 変更予定だが Plan 02 はそれと独立) / Gem (/orochi/gemchat/...) / Canvas (/orochi/canvaschat/...) で incoming AI バブルが「白背景に黒テキスト」になり読みづらくならないことを目視確認
- 手動 fallback: もし Chat/Gem/Canvas で背景透過によりコントラスト低下が顕著な場合、本 plan を revert して chunked override (senderName ありの message のみへ scope 絞る方針) を別途検討 — Plan 02 の必要時 rollback 手順は theme.css の新規 rule 行を削除するのみ
</verification>

<success_criteria>
- ROADMAP Phase 40 Success Criteria 3 (Debate Chat のエージェントメッセージで chatscope デフォルト bubble と Phase 35 エージェント別カラー wrapper の 2 層重ねが解消されている) が観察可能
- light / dark mode 両方で incoming バブルが破綻なく表示される
- lint regression なし
</success_criteria>

<output>
After completion, create `.planning/phases/40-ui-polish-round-2-frontend-only/40-02-SUMMARY.md`
</output>
