---
phase: 40
plan: 05
type: execute
wave: 4
depends_on:
  - 03
  - 04
files_modified:
  - frontend/src/components/ChatApp.tsx
  - frontend/src/components/SuperChatApp.tsx
autonomous: true
requirements:
  - UI-INIT-THREAD
user_setup: []

must_haves:
  truths:
    - http://localhost:5173/orochi/chat (threadId なし) を初回ロードすると、自動的に新規 thread が作成され URL が /orochi/chat/{uuid} に遷移し、ThreadSidebar の "+ 新しいチャット" を押した状態と同等になる
    - http://localhost:5173/orochi/superchat (threadId なし、Plan 03 で導入された default app 短縮 URL) を初回ロードすると、自動的に新規 thread が作成され URL が /orochi/superchat/{uuid} に遷移する (Plan 03 の buildSuperChatPath による default app 短縮形を維持)
    - 既存 thread が開いている状態 (URL に threadId あり) では新規 thread が自動作成されない
    - ページリロード (F5) で URL に threadId がある場合、その thread が表示され**新規作成されない**
    - ブラウザバックで /chat に戻ったとき、useRef の in-flight フラグにより**新規 thread が二重作成されない**
    - 既存 thread を sidebar から削除した後 activeThreadId が null + URL が /chat になる → そこで新規 thread が自動作成される (連続削除時の最終状態)
    - Gem Chat (/orochi/gemchat/:gemId) / Canvas Chat (/orochi/canvaschat) / Debate (/orochi/debate) は**変更しない** (todo scope: Chat / SuperChat のみ)
  artifacts:
    - path: frontend/src/components/ChatApp.tsx
      provides: 初回 thread 自動作成 useEffect — urlThreadId === undefined && activeThreadId === null && messages.length === 0 && !inFlightRef.current の AND 条件で createNewThread + navigate(replace) を実行
      contains: 'urlThreadId === undefined'
    - path: frontend/src/components/SuperChatApp.tsx
      provides: 同等の auto-create useEffect (buildSuperChatPath を使用)
      contains: 'urlThreadId === undefined'
  key_links:
    - from: frontend/src/components/ChatApp.tsx auto-create useEffect
      to: useThreads.createNewThread + react-router navigate
      via: 'navigate(`/chat/${tid}`, { replace: true })'
      pattern: 'navigate\(`/chat/\$\{'
    - from: frontend/src/components/SuperChatApp.tsx auto-create useEffect
      to: buildSuperChatPath (App.tsx export) + react-router navigate
      via: 'navigate(buildSuperChatPath(appId, tid), { replace: true })'
      pattern: 'navigate\(buildSuperChatPath'
    - from: useRef<boolean>(false) inFlight フラグ
      to: useEffect 内の二重作成防止
      via: 'if (inFlightRef.current) return; inFlightRef.current = true;'
      pattern: 'inFlightRef'
---

<objective>
ChatApp / SuperChatApp の初回表示 (URL に threadId なし + activeThreadId null + messages 空) で、自動的に新規 thread を作成する。ThreadSidebar の「+ 新しいチャット」ボタンを押したのと同等の UX に揃える (todo #10 案 1)。

Purpose: Chat / SuperChat を初めて開いたユーザーが「何かを送信するまで thread が存在しない中ぶらりん状態」になる UX 不整合を解消する。一方で、リロード / ブラウザバック / 既存 thread 切替時に二重作成しないよう発火条件を厳密に AND 結合する。

Output: ChatApp.tsx / SuperChatApp.tsx に同形の auto-create useEffect + inFlight ref。useChat / useThreads は変更しない (既存 createNewThread をそのまま使用)。Gem / Canvas / Debate は対象外。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/patterns.md
@docs/adr/INDEX.md
@docs/adr/0028-react-router-v7-url-based-routing-for-spa.md
@.planning/todos/pending/2026-05-13-auto-create-new-thread-on-chat-superchat-initial-render.md
@.planning/phases/40-ui-polish-round-2-frontend-only/40-03-PLAN.md

<interfaces>
<!-- ChatApp.tsx 現状 (frontend/src/components/ChatApp.tsx): -->

<!-- L42-43: -->
<!--   const { threadId: urlThreadId } = useParams<{ threadId?: string }>(); -->
<!--   const navigate = useNavigate(); -->

<!-- L45-66 useThreads + URL sync useEffect: -->
<!--   const { threads, activeThreadId, messages, isLoadingMessages, switchThread, createNewThread, ... } = useThreads('chat'); -->
<!--   useEffect(() => { -->
<!--     if (urlThreadId && urlThreadId !== activeThreadId) switchThread(urlThreadId); -->
<!--     // urlThreadId が undefined の場合は activeThreadId をクリアしない (新規スレッド作成時の /chat → /chat/:tid 遷移で消えないように) -->
<!--   }, [urlThreadId, activeThreadId, switchThread]); -->

<!-- L161-165 handleNewChat: -->
<!--   const handleNewChat = async () => { -->
<!--     const tid = await createNewThread(); -->
<!--     navigate(`/chat/${tid}`, { replace: true }); -->
<!--   }; -->

<!-- SuperChatApp.tsx 現状 (frontend/src/components/SuperChatApp.tsx) — Plan 03 適用後 -->
<!-- L124: const { slugOrThreadId, threadId: urlThreadId } = useParams<...>();  (Plan 03 で形が変わる) -->
<!-- L196-199 handleNewChat (Plan 03 完了後): -->
<!--   const handleNewChat = async () => { -->
<!--     const tid = await createNewThread(); -->
<!--     navigate(buildSuperChatPath(appId || 'superchat', tid), { replace: true }); -->
<!--   }; -->

<!-- useRef のパターン (リロード/バック時の二重作成防止): -->
<!--   const inFlightRef = useRef<boolean>(false); -->
<!--   useEffect(() => { -->
<!--     if (inFlightRef.current) return; -->
<!--     if (urlThreadId !== undefined) return;  ← URL に既に thread がある -->
<!--     if (activeThreadId !== null) return;     ← 既存 thread がアクティブ -->
<!--     if (messages.length !== 0) return;        ← 既存メッセージが残っている -->
<!--     inFlightRef.current = true; -->
<!--     (async () => { -->
<!--       try { -->
<!--         const tid = await createNewThread(); -->
<!--         navigate(`/chat/${tid}`, { replace: true }); -->
<!--       } finally { -->
<!--         inFlightRef.current = false; -->
<!--       } -->
<!--     })(); -->
<!--   }, [urlThreadId, activeThreadId, messages.length, createNewThread, navigate]); -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChatApp.tsx に初回 thread 自動作成 useEffect + inFlightRef を追加する</name>
  <files>frontend/src/components/ChatApp.tsx</files>
  <read_first>
    - frontend/src/components/ChatApp.tsx (現状 L42-66 useParams + URL sync useEffect、L45-56 useThreads return、L161-165 handleNewChat、L188-201 handleSend)
    - .planning/todos/pending/2026-05-13-auto-create-new-thread-on-chat-superchat-initial-render.md (案 1 採用、特に "考慮事項" の dependency / inFlight ref / replace: true 注意点)
    - frontend/src/hooks/useThreads.ts L60-66 createNewThread の挙動 (setActiveThreadId(thread_id) と setMessages([]) を行う — 自動作成後すぐ activeThreadId が更新されるので二重発火対策が必須)
  </read_first>
  <action>
    ChatApp.tsx を編集する。

    (a) 既存の useRef インポート (現状 L7) を確認し、必要に応じて inFlightRef 用に追加 import は不要 (useRef は既に import 済)。

    (b) ChatApp 関数本体 (L41-) の URL sync useEffect (L60-66) の**直後**に、以下の構造の useEffect を追加する:

        - `const initThreadInFlightRef = useRef<boolean>(false);` を component scope (URL sync useEffect の直前) に宣言する
        - useEffect 本体は dependency array に `[urlThreadId, activeThreadId, messages.length, createNewThread, navigate]` を取る
        - 中身: inFlightRef.current が true なら return / urlThreadId !== undefined なら return / activeThreadId !== null なら return / messages.length !== 0 なら return。すべて AND 条件で**初回マウント直後の中ぶらりん状態だけが通過**する。
        - 通過したら inFlightRef.current = true を立て、async IIFE で `const tid = await createNewThread();` → `navigate(\`/chat/\${tid}\`, { replace: true });` を実行。finally で inFlightRef.current = false に戻す。
        - useEffect の説明コメントとして「Phase 40 UI-INIT-THREAD (#10): URL に threadId なし / activeThreadId null / messages 空 の AND 条件でのみ新規 thread を自動作成。inFlightRef でリロード・ブラウザバック時の二重発火を防止。useThreads.createNewThread が activeThreadId を更新するため、自動作成後の再 render では activeThreadId !== null となり再発火しない。」を付ける。

    (c) 既存の handleNewChat (L161-165) と handleSend 内の createNewThread 呼出 (L188-201) は**変更しない** — 手動 + 自動の両方が同じ pathway を通る。

    (d) 既存の URL sync useEffect (L60-66) のコメントを 1 行追記し、「Phase 40: 初回 mount で urlThreadId === undefined && activeThreadId === null の場合は次の useEffect (auto-create) が起動する」と記す。
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; bun run typecheck &amp;&amp; bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'initThreadInFlightRef' frontend/src/components/ChatApp.tsx` が 2 件以上 (declaration + read + write の合計)
    - Source assertion: `grep -E 'urlThreadId !== undefined' frontend/src/components/ChatApp.tsx | wc -l` が 1 件以上
    - Source assertion: `grep -E 'activeThreadId !== null' frontend/src/components/ChatApp.tsx | wc -l` が 1 件以上
    - Source assertion: `grep -E 'messages.length !== 0' frontend/src/components/ChatApp.tsx | wc -l` が 1 件以上
    - Source assertion: `grep -c 'navigate(\`/chat/' frontend/src/components/ChatApp.tsx` が 2 件以上 (既存 handleNewChat + 新規 auto-create)
    - Source assertion: `grep -c 'replace: true' frontend/src/components/ChatApp.tsx` が 2 件以上
    - Source assertion: `grep -c 'Phase 40 UI-INIT-THREAD' frontend/src/components/ChatApp.tsx` が 1 件以上
    - Test command: `cd frontend && bun run typecheck` exit 0
    - Test command: `cd frontend && bun run lint` exit 0
  </acceptance_criteria>
  <done>ChatApp.tsx に auto-create useEffect + inFlightRef が追加され、AND 3 条件 + inFlight gate で初回マウント時に新規 thread が作成される。typecheck / lint 通過。</done>
</task>

<task type="auto">
  <name>Task 2: SuperChatApp.tsx に初回 thread 自動作成 useEffect + inFlightRef を追加する (Plan 03 の buildSuperChatPath 経由)</name>
  <files>frontend/src/components/SuperChatApp.tsx</files>
  <read_first>
    - frontend/src/components/SuperChatApp.tsx Plan 03 適用後の状態 (useParams が { slugOrThreadId, threadId } 形式 + UUID 判定で urlThreadId を導出)
    - frontend/src/components/ChatApp.tsx Task 1 完了後の参照実装
    - frontend/src/App.tsx Plan 03 で追加された buildSuperChatPath export
  </read_first>
  <action>
    SuperChatApp.tsx を編集する。

    (a) buildSuperChatPath は Plan 03 で App.tsx から既に import されている前提で、追加 import は不要。

    (b) SuperChatApp 関数本体 (L121-) の URL sync useEffect (L141-145) の直後に、以下の useEffect を追加する:

        - `const initThreadInFlightRef = useRef<boolean>(false);` を component scope に宣言する
        - useEffect の dependency array は `[urlThreadId, activeThreadId, messages.length, createNewThread, navigate, appId]`
        - 中身: inFlightRef.current が true → return / urlThreadId !== undefined → return / activeThreadId !== null → return / messages.length !== 0 → return
        - 通過したら inFlightRef.current = true → async IIFE で `const tid = await createNewThread();` → `navigate(buildSuperChatPath(appId || 'superchat', tid), { replace: true });` を呼ぶ。finally で inFlightRef.current = false に戻す。
        - 説明コメント「Phase 40 UI-INIT-THREAD (#10): Chat 側と同形。buildSuperChatPath で default app 短縮形 (/superchat/{uuid}) を維持する。」を付ける。

    (c) 既存の handleNewChat / handleSelectThread / handleSend の navigate (Plan 03 で buildSuperChatPath 経由に統一済) は変更しない。
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; bun run typecheck &amp;&amp; bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'initThreadInFlightRef' frontend/src/components/SuperChatApp.tsx` が 2 件以上
    - Source assertion: `grep -c 'urlThreadId !== undefined' frontend/src/components/SuperChatApp.tsx` が 1 件以上
    - Source assertion: `grep -c 'activeThreadId !== null' frontend/src/components/SuperChatApp.tsx` が 1 件以上
    - Source assertion: `grep -c 'messages.length !== 0' frontend/src/components/SuperChatApp.tsx` が 1 件以上
    - Source assertion: `grep -c 'buildSuperChatPath(' frontend/src/components/SuperChatApp.tsx` が 4 件以上 (Plan 03 の 3 件 + Plan 05 の auto-create 1 件)
    - Source assertion: `grep -c 'replace: true' frontend/src/components/SuperChatApp.tsx` が 4 件以上 (Plan 03 で 3 件 + 本 plan で 1 件)
    - Source assertion: `grep -c 'Phase 40 UI-INIT-THREAD' frontend/src/components/SuperChatApp.tsx` が 1 件以上
    - Source negation: `grep -c 'Phase 40 UI-INIT-THREAD' frontend/src/components/GemChatApp.tsx` が 0 件 (Gem 対象外確認)
    - Source negation: `grep -c 'Phase 40 UI-INIT-THREAD' frontend/src/components/CanvasChatApp.tsx` が 0 件 (Canvas 対象外確認)
    - Source negation: `grep -c 'Phase 40 UI-INIT-THREAD' frontend/src/components/DebateChatApp.tsx` が 0 件 (Debate 対象外確認)
    - Test command: `cd frontend && bun run typecheck` exit 0
    - Test command: `cd frontend && bun run lint` exit 0
  </acceptance_criteria>
  <done>SuperChatApp.tsx に auto-create useEffect + inFlightRef が追加され、buildSuperChatPath 経由で default app 短縮形を維持しつつ初回 thread が自動作成される。Gem / Canvas / Debate は変更されない (scope 守られている)。typecheck / lint 通過。</done>
</task>

</tasks>

<verification>
- bun run typecheck (frontend/) で 0 件
- bun run lint (frontend/) で 0 件
- 手動 Test 1 (Chat): docker compose up 後、http://localhost:5173/orochi/chat (slug なし) を開く → URL が即座に /orochi/chat/{uuid} に切り替わり、メッセージリストが空、入力欄が enabled、ThreadSidebar に新規 thread が表示される
- 手動 Test 2 (リロード安全): Test 1 の状態で F5 → 同じ thread URL のまま、追加の自動作成は起きない (ThreadSidebar に thread が 1 つだけ)
- 手動 Test 3 (ブラウザバック): Test 1 で発言 → 別の既存 thread に切替 → ブラウザバック → /orochi/chat/{元 uuid} に戻り、二重作成されない
- 手動 Test 4 (削除後の再発火): Test 1 で 1 thread しかない状態で sidebar から thread を削除 → activeThreadId が null + URL が /chat になる → 自動的に新規 thread が作成され URL が /chat/{new-uuid} に
- 手動 Test 5 (SuperChat): http://localhost:5173/orochi/superchat (Plan 03 default app 短縮形) を開く → 即座に /orochi/superchat/{uuid} に遷移、`/orochi/superchat/superchat/{uuid}` には**ならない** (buildSuperChatPath 経由)
- 手動 Test 6 (Gem/Canvas/Debate 非影響): /orochi/gemchat/{gemId} / /orochi/canvaschat / /orochi/debate を開くと従来通り threadId なしで何もしない (中ぶらりんのまま — 対象外 scope 守られている)
</verification>

<success_criteria>
- ROADMAP Phase 40 Success Criteria 2 (Chat / SuperChat 初回表示時に新しい thread が自動作成され、リロード/戻る/既存スレッド開閉時に新規 thread が二重作成されない) が観察可能
- inFlight ref + AND 3 条件で二重発火・無限ループの可能性が source 上で防がれている
- Gem / Canvas / Debate に変更が及んでいない (todo scope 厳守)
- typecheck / lint regression なし
</success_criteria>

<output>
After completion, create `.planning/phases/40-ui-polish-round-2-frontend-only/40-05-SUMMARY.md`
</output>
