---
phase: 03-web-chat-ui
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified: []
autonomous: false
requirements:
  - AUTH-03
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - CHAT-04

must_haves:
  truths:
    - "User can open the app in a browser and see the chat UI"
    - "Full auth -> chat -> new chat flow works end to end"
    - "Markdown and code blocks render formatted"
  artifacts: []
  key_links: []
---

<objective>
Visually verify the complete chat application works end-to-end in a browser.

Purpose: Automated tests cover API behavior with mocks. This checkpoint confirms the real UI renders correctly, interactions feel right, and the full flow (auth -> chat -> markdown -> new thread) works.

Output: User approval of the working application.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-web-chat-ui/03-CONTEXT.md
@.planning/phases/03-web-chat-ui/03-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify full chat application in browser</name>
  <files>static/index.html, static/style.css, static/app.js, app/api/main.py</files>
  <action>
Start the development server and verify the complete application visually. This is a human verification checkpoint — no code changes expected.

Run: `uv run uvicorn app.api.main:app --reload --host 0.0.0.0 --port 8000`
Open: http://localhost:8000
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && uv run pytest tests/ -x -q</automated>
  </verify>
  <done>User approves the visual appearance and functional behavior of the chat application</done>
  <what-built>
Complete chat application:
- FastAPI backend with auth, chat, and thread management APIs
- Vanilla JS frontend with dark theme, sidebar, message bubbles, markdown rendering
- Device Flow authentication with polling
- Multi-turn conversation with thread history
  </what-built>
  <how-to-verify>
**Start the server:**
```bash
cd /home/parallels/workspaces/copilot-langgraph
uv run uvicorn app.api.main:app --reload --host 0.0.0.0 --port 8000
```
Open http://localhost:8000 in a browser.

**Verify layout (30 seconds):**
1. Dark theme visible: dark background `#1e1e2e`, sidebar on left `240px`
2. Header shows "Copilot Chat" on left, auth status on right
3. Sidebar has "New Chat" button at top, model dropdown at bottom
4. Chat area shows empty state: "New conversation" heading

**Verify auth flow (AUTH-03):**
1. Header should show "Login with GitHub" (clickable)
2. Click it -- auth panel overlay appears with URL and device code
3. Copy button works (clipboard)
4. (If you want full auth: open URL, enter code, wait for poll to succeed)
5. After auth: header updates to green dot + "Authenticated"

**Verify chat (CHAT-01, CHAT-02):**
1. Type a message, press Enter or click Send
2. User message appears as right-aligned bubble
3. Typing indicator ("...") appears as left-aligned AI bubble
4. After response: AI reply appears as left-aligned bubble
5. Input was disabled during wait, re-enabled after

**Verify markdown (CHAT-03):**
1. Ask: "Show me a Python hello world with code block"
2. AI reply should have syntax-highlighted code block
3. Bold, headers, and inline code render formatted (not raw)

**Verify new chat (CHAT-04):**
1. Click "New Chat" in sidebar
2. Message list clears, empty state shows
3. New thread appears in sidebar list
4. Previous thread still clickable in sidebar

**Verify thread switching:**
1. Click a previous thread in sidebar
2. Messages from that thread load
3. Active thread highlighted with accent border

**Verify model selector (D-09, D-10):**
1. Change model dropdown to "gpt-4o"
2. Send a message -- should work (model applied on next send per D-11)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
User visual confirmation of all functional areas.
</verification>

<success_criteria>
- User sees dark-themed chat UI matching UI-SPEC layout
- Auth flow works (Login button -> auth panel -> codes -> polling)
- Chat flow works (send message -> typing indicator -> AI reply)
- Markdown renders formatted (headers, bold, code blocks with syntax highlighting)
- New Chat creates fresh thread, clears messages
- Thread sidebar lists conversations, switching loads history
- Model selector changes model for next message
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-chat-ui/03-04-SUMMARY.md`
</output>
