---
phase: 16-canvas-app
plan: 04
type: execute
wave: 3
depends_on:
  - "16-02"
  - "16-03"
files_modified: []
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-12
  - D-14
  - D-17
must_haves:
  truths:
    - "MenuScreen に Canvas カードが表示される"
    - "Canvas カード → CanvasScreen（デプロイ済み一覧）の遷移が機能する"
    - "「新しいチャットを開始」ボタン → CanvasChatApp の遷移が機能する"
    - "CanvasChatApp の右パネルに CanvasPane プレースホルダーが常時表示される"
    - "AI に HTML を依頼すると右パネルに生成結果が表示される"
    - "Deploy App ボタンでデプロイされたアプリが CanvasScreen の一覧に表示される"
    - "drag handle で左右パネルのリサイズができる"
  artifacts:
    - path: ".planning/phases/16-canvas-app/16-UAT.md"
      provides: "Phase 16 UAT チェックリスト（完了記録）"
  key_links:
    - from: "MenuScreen Canvas カード"
      to: "CanvasChatApp での HTML 生成・デプロイ"
      via: "CanvasScreen → CanvasChatApp の完全フロー"
      pattern: "E2E"
---

<objective>
Phase 16 の全機能をブラウザで End-to-End 検証する。
自動テストでは確認できないビジュアル・インタラクション・AI 連携動作を人間が確認する。

Purpose: D-01〜D-17 のすべての決定が正しく実装されていることを確認し、Phase 16 を完了状態にする。
Output: 完了チェックリスト付きの 16-UAT.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-canvas-app/16-CONTEXT.md
@.planning/phases/16-canvas-app/16-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 自動テスト最終確認 + docker compose ビルド検証</name>
  <files></files>
  <read_first>
    - .planning/phases/16-canvas-app/16-01-SUMMARY.md
    - .planning/phases/16-canvas-app/16-02-SUMMARY.md
    - .planning/phases/16-canvas-app/16-03-SUMMARY.md
  </read_first>
  <action>
    以下のコマンドを順番に実行し、すべてが成功することを確認する:

    ```bash
    # 1. バックエンドテスト全件
    docker compose run --rm api pytest tests/ -q

    # 2. TypeScript コンパイル確認
    docker compose run --rm frontend bun run tsc --noEmit

    # 3. フロントエンドビルド確認（本番ビルド）
    docker compose run --rm frontend bun run build

    # 4. Docker Compose 全サービス起動確認
    docker compose up -d
    docker compose ps
    ```

    いずれかが失敗した場合は、該当するプランの SUMMARY.md を読んでエラーを診断・修正してから次に進む。
  </action>
  <verify>
    <automated>docker compose run --rm api pytest tests/ -q 2>&1 | tail -5 && docker compose run --rm frontend bun run tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/ -q` が全テストパス（PASSED / no errors）
    - `bun run tsc --noEmit` が 0 で終了（エラーなし）
    - `bun run build` が成功（exit 0）
    - `docker compose ps` で api, frontend, db, redis が全て running
  </acceptance_criteria>
  <done>自動テストが全件パスし、Docker Compose スタック全体が正常起動している</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: ブラウザ E2E 検証</name>
  <what-built>
    Phase 16 の全機能:
    - MenuScreen に Canvas カードが追加された
    - CanvasScreen (Canvas App ハブ) が新規作成された
    - CanvasChatApp (左右分割チャット) が新規作成された
    - バックエンド: deployed フィルタ、Canvas 専用 Gem 自動登録、/api/canvas/gem エンドポイント
  </what-built>
  <how-to-verify>
    アプリを開いて以下を順番に確認する（http://localhost:5173 または http://localhost:8000/app）:

    **[1] MenuScreen Canvas カード確認（D-05, D-16）**
    - [ ] MenuScreen に「🎨 Canvas」カードが表示される
    - [ ] Canvas カードは Gems カードと同じ行（または隣接位置）に並列表示されている
    - [ ] Canvas カードの説明文は「AI チャットで HTML アプリを作成・プレビュー・デプロイ」

    **[2] CanvasScreen ナビゲーション確認（D-06, D-08, D-09, D-11）**
    - [ ] Canvas カードをクリックすると CanvasScreen（"Canvas Apps" ページ）が表示される
    - [ ] 「← Back」ボタンで MenuScreen に戻れる
    - [ ] デプロイ済みアプリがない場合「まだデプロイ済みアプリがありません」が表示される
    - [ ] 「+ 新しいチャットを開始」ボタンが表示される

    **[3] CanvasChatApp 基本レイアウト確認（D-01, D-02, D-04）**
    - [ ] 「+ 新しいチャットを開始」をクリックすると CanvasChatApp が表示される
    - [ ] 左側にスレッドサイドバー + チャットエリア、右側にパネルが表示される
    - [ ] 右パネルに「🎨 アプリがここに表示されます」のプレースホルダーが常時表示されている（AI 応答前）
    - [ ] ヘッダーに「🎨 Canvas」と表示されている

    **[4] drag handle リサイズ確認（D-03）**
    - [ ] サイドバーとチャットエリアの境界にドラッグ可能なハンドルがある（ホバーで青くなる）
    - [ ] チャットエリアと右パネルの境界にもドラッグ可能なハンドルがある（ホバーで青くなる）
    - [ ] 右側の drag handle をドラッグして右パネルのサイズが変更できる

    **[5] AI による HTML 生成確認（D-12, D-13, D-14）**
    - [ ] チャットに「シンプルなカウンターアプリを作ってください」と送信する
    - [ ] AI が HTML を生成し、右パネルに Canvas エディタが表示される（プレースホルダーが置き換わる）
    - [ ] CanvasPane の「Preview」タブで生成された HTML がプレビューされる
    - [ ] CanvasPane の「Editor」タブで HTML コードが表示される

    **[6] デプロイ確認（D-09 の Deployed Apps 一覧）**
    - [ ] CanvasPane の「Deploy App」ボタンをクリックしてデプロイする
    - [ ] 「← Back」→ CanvasScreen に戻ると、デプロイ済みアプリカードが表示される
    - [ ] カードの「Open App」リンクをクリックするとデプロイされたアプリにアクセスできる

    **[7] 既存アプリからの CanvasChatApp 起動確認（D-10）**
    - [ ] CanvasScreen のデプロイ済みアプリカードの「チャットを開く」をクリックする
    - [ ] CanvasChatApp が起動し、以前のチャット履歴が表示される

    **[8] リグレッション確認**
    - [ ] Gems カードが引き続き機能する（GemsScreen が開く）
    - [ ] 討論チャットカードが引き続き機能する（DebateChatApp が開く）
    - [ ] Chat アプリ（SuperChat）が引き続き機能する
  </how-to-verify>
  <resume-signal>すべての確認項目が OK の場合は「approved」と入力してください。問題がある場合は具体的な症状を説明してください（例：「[3] 右パネルが表示されない」）</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ブラウザ → iframe | Canvas アプリプレビューは sandbox で隔離 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-12 | Information Disclosure | デプロイ URL | accept | /apps/{uuid}/ の URL は予測困難な UUID パス — robots.txt なし、内部向けツールのため許容 |
| T-16-13 | XSS | CanvasPane iframe プレビュー | mitigate | sandbox="allow-scripts allow-forms" — allow-same-origin なし。Phase 15 から継承済みの制約。UAT 手順 [5] で iframe が正しくサンドボックスされていることを DevTools で確認する |
</threat_model>

<verification>
```bash
# 最終確認（UAT 前の自動チェック）
docker compose run --rm api pytest tests/ -q
docker compose run --rm frontend bun run tsc --noEmit
docker compose run --rm frontend bun run build
```
</verification>

<success_criteria>
- 自動テスト全件パス
- TypeScript コンパイルエラーなし
- ブラウザ E2E 検証の全 8 項目が「approved」
- Phase 16 の D-01〜D-17 すべてが実装・検証済み
</success_criteria>

<output>
完了後、以下を作成する:
1. `.planning/phases/16-canvas-app/16-UAT.md` — E2E 検証結果チェックリスト
2. `.planning/phases/16-canvas-app/16-04-SUMMARY.md`
</output>
