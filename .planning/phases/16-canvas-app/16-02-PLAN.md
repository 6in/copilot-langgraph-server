---
phase: 16-canvas-app
plan: 02
type: execute
wave: 2
depends_on:
  - "16-01"
files_modified:
  - frontend/src/components/CanvasChatApp.tsx
  - frontend/src/components/CanvasScreen.tsx
  - frontend/src/api/client.ts
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-08
  - D-09
  - D-10
  - D-11
  - D-14
  - D-15
  - D-17
must_haves:
  truths:
    - "CanvasChatApp が左右分割レイアウトでレンダリングされる（左: ThreadSidebar+MessageArea、右: CanvasPane 常時表示）"
    - "CanvasPane は AI 応答前（canvasApp=null）でもプレースホルダーとして右側に常時存在する"
    - "drag handle（4px 幅）でチャットエリアと CanvasPane のサイズを変更できる"
    - "CanvasScreen がデプロイ済みアプリ一覧と新規チャット起動 CTA を表示する"
    - "CanvasScreen のデプロイ済みアプリカードをクリックすると CanvasChatApp が起動する"
    - "TypeScript コンパイルエラーがない"
  artifacts:
    - path: "frontend/src/components/CanvasChatApp.tsx"
      provides: "左右分割チャット+Canvas 画面"
      contains: "handleCanvasDividerMouseDown"
    - path: "frontend/src/components/CanvasScreen.tsx"
      provides: "Canvas App ハブ画面"
      contains: "listCanvasApps"
    - path: "frontend/src/api/client.ts"
      provides: "listCanvasApps / getCanvasGemId 関数"
      contains: "listCanvasApps"
  key_links:
    - from: "CanvasChatApp"
      to: "useThreads(undefined, canvasGemId)"
      via: "gem_id によるスレッド分離（D-17）"
      pattern: "useThreads"
    - from: "CanvasChatApp"
      to: "useChat({ gemId: canvasGemId, onCanvasResponse })"
      via: "Canvas HTML 抽出ロジック発動（D-14）"
      pattern: "useChat"
    - from: "CanvasScreen"
      to: "GET /api/canvas/apps?deployed=true"
      via: "listCanvasApps(true)"
      pattern: "listCanvasApps"
---

<objective>
フロントエンドの 2 つの新規コンポーネントと API クライアント拡張を実装する:

1. **CanvasChatApp** — GemChatApp.tsx を参照実装として、左右分割レイアウト（ThreadSidebar + MessageArea + CanvasPane 常時表示）を実装
2. **CanvasScreen** — GemsScreen.tsx を参照実装として、デプロイ済みアプリ一覧 + 新規チャット起動 CTA のハブ画面を実装
3. **client.ts 拡張** — `listCanvasApps(deployed?: boolean)` と `getCanvasGemId()` を追加

Purpose: D-01〜D-17 のうちフロントエンドコンポーネントに関する決定（D-01〜D-04, D-08〜D-11, D-14, D-15, D-17）をすべて実装する。
Output: CanvasChatApp.tsx、CanvasScreen.tsx、更新済み client.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-canvas-app/16-CONTEXT.md
@.planning/phases/16-canvas-app/16-UI-SPEC.md
@.planning/phases/16-canvas-app/16-RESEARCH.md

<interfaces>
<!-- 参照実装の主要構造。エグゼキューターはこれを読んでから実装する -->

From frontend/src/components/GemChatApp.tsx (CanvasChatApp の直接ベース):
```typescript
// 定数
const SIDEBAR_MIN = 160;
const SIDEBAR_MAX = 480;
const SIDEBAR_DEFAULT = 240;

// Props
interface GemChatAppProps {
  gem: GemInfo;
  selectedModel: string;
  onBack: () => void;
}

// useThreads パターン（D-17 準拠）
const { threads, activeThreadId, messages, isLoadingMessages, switchThread,
        createNewThread, removeThread, setMessages, refreshThreads }
  = useThreads(undefined, gem.gem_id);  // CanvasChatApp では canvasGemId を渡す

// useChat パターン（D-14 準拠）
const { isThinking, sendMessage } = useChat({
  activeThreadId,
  selectedModel,
  gemId: gem.gem_id,  // CanvasChatApp では canvasGemId を渡す
  setMessages,
  refreshThreads,
});

// Drag handle（サイドバー用 — CanvasChatApp では符号が逆になる点に注意）
const handleDividerMouseDown = useCallback((e: React.MouseEvent) => {
  e.preventDefault();
  dragStartX.current = e.clientX;
  dragStartWidth.current = sidebarWidth;
  const onMouseMove = (ev: MouseEvent) => {
    if (dragStartX.current === null) return;
    const delta = ev.clientX - dragStartX.current;
    const newWidth = Math.min(SIDEBAR_MAX, Math.max(SIDEBAR_MIN, dragStartWidth.current + delta));
    setSidebarWidth(newWidth);
  };
  const onMouseUp = () => {
    dragStartX.current = null;
    window.removeEventListener('mousemove', onMouseMove);
    window.removeEventListener('mouseup', onMouseUp);
  };
  window.addEventListener('mousemove', onMouseMove);
  window.addEventListener('mouseup', onMouseUp);
}, [sidebarWidth]);

// ThreadSidebar の width（UI-SPEC: collapse 時 32px — GemChatApp は 40px のまま）
width={sidebarCollapsed ? 40 : sidebarWidth}  // CanvasChatApp では 40 → 32 に修正
```

From frontend/src/components/CanvasPane.tsx (再利用コンポーネント):
```typescript
interface CanvasPaneProps {
  canvasApp: CanvasAppInfo;
  isSaving: boolean;
  isDeploying: boolean;
  deployUrl: string | null;
  deployError: string | null;
  onSave: (html: string, name?: string) => Promise<void>;
  onDeploy: () => Promise<void>;
  onClose: () => void;  // Pitfall 4: required — CanvasChatApp では () => {} を渡す
}
```

From frontend/src/hooks/useCanvas.ts (再利用フック):
```typescript
// 戻り値
const { canvasApp, isSaving, isDeploying, deployUrl, deployError, saveCanvas, deployCanvas }
  = useCanvas();
// onCanvasResponse コールバックで canvasApp を更新する
// const { ..., setCanvasApp } = useCanvas();  // または onCanvasResponse をフックに渡す
// → 実際の API は useCanvas.ts を読んで確認すること
```

From frontend/src/components/GemsScreen.tsx (CanvasScreen の参照実装 — 骨格のみ):
```typescript
// レイアウトパターン
<div style={{
  flex: 1, display: 'flex', flexDirection: 'column',
  alignItems: 'center', padding: '48px 32px',
  background: screenBg, overflowY: 'auto',
}}>
  <div style={{ maxWidth: '640px', width: '100%' }}>
    {/* Back ボタン + h1 */}
    {/* SkeletonCard x3（ローディング中）*/}
    {/* カード一覧 */}
    {/* CTA ボタン */}
  </div>
</div>

// SkeletonCard（pulse アニメーション）
function SkeletonCard({ cardBg }: { cardBg: string }) {
  return (
    <div style={{
      background: cardBg, borderRadius: '12px', padding: '24px',
      height: '120px', animation: 'pulse 0.8s ease-in-out infinite',
    }}>
      <style>{`@keyframes pulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.4; } }`}</style>
    </div>
  );
}
```

From frontend/src/api/client.ts (追加先):
```typescript
// 既存パターン
const API_BASE = (import.meta.env.VITE_APP_BASE ?? '').replace(/\/$/, '');
async function apiFetch<T>(path: string, init?: RequestInit): Promise<T>
// 既存 Canvas 関数の末尾に追加する
```

From frontend/src/types.ts (既存型):
```typescript
export interface CanvasAppInfo {
  app_id: string;
  thread_id: string | null;
  name: string;
  html: string;
  source: string;
  deployed: boolean;
  deployed_at: string | null;
  created_at: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: client.ts 拡張 — listCanvasApps / getCanvasGemId 追加</name>
  <files>frontend/src/api/client.ts</files>
  <read_first>
    - frontend/src/api/client.ts（現状の Canvas API セクション末尾を確認）
    - frontend/src/types.ts（CanvasAppInfo 型を確認）
  </read_first>
  <action>
    frontend/src/api/client.ts の末尾（既存 Canvas API 関数の後）に以下を追加する:

    ```typescript
    export const listCanvasApps = (deployed?: boolean): Promise<CanvasAppInfo[]> => {
      const params = new URLSearchParams();
      if (deployed !== undefined) params.set('deployed', String(deployed));
      const qs = params.toString();
      return apiFetch<CanvasAppInfo[]>(`${API_BASE}/api/canvas/apps${qs ? `?${qs}` : ''}`);
    };

    export const getCanvasGemId = (): Promise<{ gem_id: string }> =>
      apiFetch<{ gem_id: string }>(`${API_BASE}/api/canvas/gem`);
    ```

    これ以外の既存コードは変更しない。
  </action>
  <verify>
    <automated>docker compose run --rm frontend bun run tsc --noEmit 2>&1 | grep -E "error|Error" | head -20; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - client.ts に `export const listCanvasApps` が存在する（grep で確認）
    - client.ts に `export const getCanvasGemId` が存在する（grep で確認）
    - TypeScript コンパイルエラーがない（tsc --noEmit が 0 で終了）
    - deployed パラメータなしで呼ぶと URL に `?deployed=` が付かない
  </acceptance_criteria>
  <done>listCanvasApps(true) が `/api/canvas/apps?deployed=true` を呼ぶ関数として export されている</done>
</task>

<task type="auto">
  <name>Task 2: CanvasChatApp 新規作成 — 左右分割チャット+Canvas（D-01〜D-04, D-14, D-15, D-17）</name>
  <files>frontend/src/components/CanvasChatApp.tsx</files>
  <read_first>
    - frontend/src/components/GemChatApp.tsx（参照実装 — 全文読む）
    - frontend/src/components/CanvasPane.tsx（再利用コンポーネントの全 props 確認）
    - frontend/src/hooks/useCanvas.ts（useCanvas フックの戻り値確認）
    - frontend/src/hooks/useChat.ts（onCanvasResponse の型確認）
    - frontend/src/api/client.ts（getCanvasGemId 確認）
  </read_first>
  <action>
    frontend/src/components/CanvasChatApp.tsx を新規作成する。
    GemChatApp.tsx の構造をベースに以下の差分を適用する:

    **Props:**
    ```typescript
    interface CanvasChatAppProps {
      canvasGemId: string;      // Canvas 専用 Gem の gem_id（App.tsx から渡す）
      selectedModel: string;
      onBack: () => void;
      initialThreadId?: string | null;  // D-10: 既存アプリから起動時にスレッドを復元
    }
    ```

    **定数（GemChatApp と同じ）:**
    ```typescript
    const SIDEBAR_MIN = 160;
    const SIDEBAR_MAX = 480;
    const SIDEBAR_DEFAULT = 240;
    const CANVAS_PANE_MIN = 320;  // UI-SPEC: CanvasPane 最小幅
    const CANVAS_PANE_DEFAULT = 400;  // 初期幅（ピクセル）
    ```

    **State（GemChatApp に追加）:**
    ```typescript
    const [canvasPaneWidth, setCanvasPaneWidth] = useState(CANVAS_PANE_DEFAULT);
    const canvasDragStartX = useRef<number | null>(null);
    const canvasDragStartWidth = useRef<number>(CANVAS_PANE_DEFAULT);
    ```

    **useCanvas フック:**
    ```typescript
    const { canvasApp, isSaving, isDeploying, deployUrl, deployError, saveCanvas, deployCanvas }
      = useCanvas();
    // useCanvas.ts の実際の API を読んで onCanvasResponse コールバックの渡し方を確認すること
    ```

    **useThreads（D-17: gem_id フィルタ）:**
    ```typescript
    const { threads, activeThreadId, ... } = useThreads(undefined, canvasGemId);
    ```

    **useChat（D-14: gemId 渡す）:**
    ```typescript
    const { isThinking, sendMessage } = useChat({
      activeThreadId,
      selectedModel,
      gemId: canvasGemId,
      setMessages,
      refreshThreads,
      onCanvasResponse: /* useCanvas.ts の setCanvasApp に相当する関数 */,
    });
    ```

    **Drag handle — CanvasPane 用（RESEARCH.md Pitfall 1: delta の符号が逆）:**
    ```typescript
    const handleCanvasDividerMouseDown = useCallback((e: React.MouseEvent) => {
      e.preventDefault();
      canvasDragStartX.current = e.clientX;
      canvasDragStartWidth.current = canvasPaneWidth;
      const onMouseMove = (ev: MouseEvent) => {
        if (canvasDragStartX.current === null) return;
        const delta = ev.clientX - canvasDragStartX.current;
        // 注意: 右から左への drag で CanvasPane が拡大 → delta を反転
        const newWidth = Math.max(CANVAS_PANE_MIN, canvasDragStartWidth.current - delta);
        setCanvasPaneWidth(newWidth);
      };
      const onMouseUp = () => {
        canvasDragStartX.current = null;
        window.removeEventListener('mousemove', onMouseMove);
        window.removeEventListener('mouseup', onMouseUp);
      };
      window.addEventListener('mousemove', onMouseMove);
      window.addEventListener('mouseup', onMouseUp);
    }, [canvasPaneWidth]);
    ```

    **ThreadSidebar の collapse width（UI-SPEC: 32px — GemChatApp の 40px を修正）:**
    ```typescript
    width={sidebarCollapsed ? 32 : sidebarWidth}
    ```

    **Drag handle の幅（UI-SPEC: 4px — GemChatApp の 5px を修正）:**
    ```tsx
    <div onMouseDown={handleDividerMouseDown}
      style={{ width: '4px', cursor: 'col-resize', background: 'transparent', flexShrink: 0, zIndex: 10 }}
      onMouseEnter={(e) => { (e.currentTarget as HTMLElement).style.background = '#b0c4d8'; }}
      onMouseLeave={(e) => { (e.currentTarget as HTMLElement).style.background = 'transparent'; }}
    />
    ```

    **ヘッダー（UI-SPEC: "🎨 Canvas"）:**
    ```tsx
    <div style={{
      display: 'flex', alignItems: 'center', gap: '12px',
      padding: '8px 16px', height: '48px',
      borderBottom: '1px solid #d1dbe3', background: cardBg, flexShrink: 0,
    }}>
      <style>{`button:focus-visible { outline: 2px solid #7c6ff7; outline-offset: 2px; }`}</style>
      <button onClick={onBack} aria-label="Back to Canvas screen"
        style={{ padding: '0.25rem 0.75rem', borderRadius: '4px', border: '1px solid #555',
                 background: 'transparent', color: textColor, cursor: 'pointer', fontSize: '0.875rem' }}>
        ← Back
      </button>
      <span style={{ fontSize: '1rem', fontWeight: 600, color: textColor }}>🎨 Canvas</span>
    </div>
    ```

    **CanvasPane プレースホルダー（D-02: canvasApp=null でも常時表示）:**
    ```tsx
    {canvasApp ? (
      <CanvasPane
        canvasApp={canvasApp}
        isSaving={isSaving}
        isDeploying={isDeploying}
        deployUrl={deployUrl}
        deployError={deployError}
        onSave={saveCanvas}
        onDeploy={deployCanvas}
        onClose={() => {}}  // Pitfall 4: 常時表示のためダミー
      />
    ) : (
      <div style={{
        minWidth: `${CANVAS_PANE_MIN}px`,
        width: `${canvasPaneWidth}px`,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        flexDirection: 'column',
        gap: '8px',
        background: '#ffffff',
        borderLeft: '1px solid #d1dbe3',
        color: '#666666',
        textAlign: 'center',
        padding: '24px',
      }}>
        <div style={{ fontSize: '2rem' }}>🎨</div>
        <div style={{ fontSize: '1rem', fontWeight: 600 }}>アプリがここに表示されます</div>
        <div style={{ fontSize: '0.875rem', color: '#666666', lineHeight: 1.5 }}>
          チャットで HTML アプリを生成してください
        </div>
      </div>
    )}
    ```

    **レイアウト構造（MainContainer 内）:**
    ```
    ThreadSidebar | sidebar drag handle (4px) | MessageArea | canvas drag handle (4px) | CanvasPane or Placeholder
    ```

    initialThreadId が渡された場合は useThreads の初期スレッドとして使う（useThreads の API を確認して適切に実装）。
  </action>
  <verify>
    <automated>docker compose run --rm frontend bun run tsc --noEmit 2>&1 | grep -E "error TS" | head -20; echo "tsc exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/CanvasChatApp.tsx が存在する
    - `handleCanvasDividerMouseDown` 関数が含まれる（grep "handleCanvasDividerMouseDown"）
    - drag handle 幅が 4px（grep で "width: '4px'" が存在する）
    - sidebarCollapsed 時の width が 32（grep で "? 32 :" が存在する）
    - `onClose={() => {}}` が CanvasPane に渡されている（grep "onClose"）
    - `useThreads(undefined, canvasGemId)` パターンが存在する
    - `gemId: canvasGemId` が useChat に渡されている
    - TypeScript コンパイルエラーがない（tsc --noEmit）
  </acceptance_criteria>
  <done>CanvasChatApp.tsx が TypeScript エラーなしでコンパイルされ、D-01〜D-04、D-14、D-15、D-17 の要件を実装している</done>
</task>

<task type="auto">
  <name>Task 3: CanvasScreen 新規作成 — Canvas App ハブ画面（D-08〜D-11）</name>
  <files>frontend/src/components/CanvasScreen.tsx</files>
  <read_first>
    - frontend/src/components/GemsScreen.tsx（参照実装 — 骨格パターン）
    - frontend/src/api/client.ts（listCanvasApps が追加されていることを確認）
    - frontend/src/types.ts（CanvasAppInfo 型）
    - frontend/src/contexts/ThemeContext.tsx（useCurrentTheme）
  </read_first>
  <action>
    frontend/src/components/CanvasScreen.tsx を新規作成する。
    GemsScreen.tsx の骨格を参照して以下の仕様で実装する:

    **Props:**
    ```typescript
    interface CanvasScreenProps {
      onBack: () => void;           // D-11: MenuScreen に戻る
      onStartChat: (initialThreadId?: string) => void;  // D-09: 新規チャット or 既存アプリ起動
    }
    ```

    **State:**
    ```typescript
    const [apps, setApps] = useState<CanvasAppInfo[]>([]);
    const [loading, setLoading] = useState(true);
    const [error, setError] = useState<string | null>(null);
    ```

    **データ取得（マウント時）:**
    ```typescript
    useEffect(() => {
      setLoading(true);
      setError(null);
      listCanvasApps(true)  // deployed=true のみ
        .then((data) => { setApps(data); setLoading(false); })
        .catch(() => {
          setError('Canvas アプリの読み込みに失敗しました。ページを更新してください。');
          setLoading(false);
        });
    }, []);
    ```

    **レイアウト（UI-SPEC 準拠）:**
    ```tsx
    <div style={{
      flex: 1, display: 'flex', flexDirection: 'column',
      alignItems: 'center', padding: '48px 32px',
      background: screenBg, overflowY: 'auto',
    }}>
      <style>{`button:focus-visible { outline: 2px solid #7c6ff7; outline-offset: 2px; }`}</style>
      <div style={{ maxWidth: '640px', width: '100%' }}>

        {/* ヘッダー行 */}
        <div style={{ display: 'flex', alignItems: 'center', gap: '16px', marginBottom: '32px' }}>
          <button onClick={onBack} aria-label="Back to menu"
            style={{ padding: '0.25rem 0.75rem', borderRadius: '4px', border: '1px solid #555',
                     background: 'transparent', color: textColor, cursor: 'pointer', fontSize: '0.875rem' }}>
            ← Back
          </button>
          <h1 style={{ fontSize: '2rem', fontWeight: 600, color: textColor, margin: 0 }}>
            Canvas Apps
          </h1>
        </div>

        {/* エラー表示 */}
        {error && <div role="alert" style={{ /* エラースタイル */ }}>{error}</div>}

        {/* Section 1: Deployed Apps */}
        <div style={{ marginBottom: '32px' }}>
          <div style={{
            fontSize: '0.875rem', fontWeight: 600, letterSpacing: '0.08em',
            textTransform: 'uppercase', color: textSecondary, marginBottom: '16px',
          }}>
            Deployed Apps
          </div>

          {loading ? (
            <div aria-busy="true" aria-label="Loading canvas apps">
              {[0, 1, 2].map((i) => <SkeletonCard key={i} cardBg={cardBg} />)}
            </div>
          ) : apps.length === 0 ? (
            /* 空状態 */
            <div style={{ textAlign: 'center', padding: '48px 0' }}>
              <div style={{ fontSize: '1rem', fontWeight: 600, color: mutedColor, marginBottom: '8px' }}>
                まだデプロイ済みアプリがありません
              </div>
              <div style={{ fontSize: '0.875rem', color: mutedColor, lineHeight: 1.5 }}>
                チャットで HTML アプリを生成して Deploy App ボタンを押すとここに表示されます
              </div>
            </div>
          ) : (
            /* アプリカード一覧 */
            apps.map((app) => (
              <CanvasAppCard
                key={app.app_id}
                app={app}
                cardBg={cardBg}
                cardBorder={cardBorder}
                textColor={textColor}
                onClick={() => onStartChat(app.thread_id ?? undefined)}  // D-10
              />
            ))
          )}
        </div>

        {/* Section 2: 新規チャット起動 CTA */}
        <button
          onClick={() => onStartChat()}
          style={{
            width: '100%', padding: '16px', borderRadius: '8px',
            border: 'none', background: '#7c6ff7', color: '#ffffff',
            fontSize: '1rem', fontWeight: 600, cursor: 'pointer',
            minHeight: '48px',
          }}
        >
          + 新しいチャットを開始
        </button>

      </div>
    </div>
    ```

    **CanvasAppCard コンポーネント（同ファイル内）:**
    ```typescript
    interface CanvasAppCardProps {
      app: CanvasAppInfo;
      cardBg: string;
      cardBorder: string;
      textColor: string;
      onClick: () => void;
    }

    function CanvasAppCard({ app, cardBg, cardBorder, textColor, onClick }: CanvasAppCardProps) {
      return (
        <div style={{
          background: cardBg, border: `1px solid ${cardBorder}`,
          borderRadius: '12px', padding: '24px', marginBottom: '16px',
          cursor: 'pointer', transition: 'box-shadow 0.2s, transform 0.1s',
        }}
          onMouseEnter={(e) => {
            (e.currentTarget as HTMLElement).style.boxShadow = '0 4px 16px rgba(0,0,0,0.18)';
            (e.currentTarget as HTMLElement).style.transform = 'translateY(-2px)';
          }}
          onMouseLeave={(e) => {
            (e.currentTarget as HTMLElement).style.boxShadow = 'none';
            (e.currentTarget as HTMLElement).style.transform = 'none';
          }}
          onClick={onClick}
        >
          <div style={{ fontSize: '1rem', fontWeight: 600, color: textColor, marginBottom: '8px' }}>
            {app.name}
          </div>
          {app.deployed && (
            <a href={`/apps/${app.app_id}/`} target="_blank" rel="noopener noreferrer"
              style={{ display: 'block', fontSize: '0.875rem', color: '#7c6ff7', marginBottom: '16px' }}
              onClick={(e) => e.stopPropagation()}>
              Open App
            </a>
          )}
          <button
            style={{
              padding: '8px 16px', borderRadius: '4px', border: 'none',
              background: '#7c6ff7', color: '#ffffff', cursor: 'pointer',
              fontSize: '0.875rem', fontWeight: 600, minHeight: '48px',
            }}
            onClick={(e) => { e.stopPropagation(); onClick(); }}
          >
            チャットを開く
          </button>
        </div>
      );
    }
    ```

    **SkeletonCard（GemsScreen パターン準拠）:**
    ```typescript
    function SkeletonCard({ cardBg }: { cardBg: string }) {
      return (
        <div style={{
          background: cardBg, borderRadius: '12px', padding: '24px',
          height: '120px', marginBottom: '16px',
          animation: 'pulse 0.8s ease-in-out infinite',
        }}>
          <style>{`@keyframes pulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.4; } }`}</style>
        </div>
      );
    }
    ```

    テーマカラーは `useCurrentTheme()` を使い、既存パターン（screenBg/cardBg/textColor/cardBorder/subtitleColor/mutedColor）に従う。
  </action>
  <verify>
    <automated>docker compose run --rm frontend bun run tsc --noEmit 2>&1 | grep -E "error TS" | head -20; echo "tsc exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/CanvasScreen.tsx が存在する
    - `listCanvasApps(true)` の呼び出しが含まれる（grep "listCanvasApps"）
    - "Canvas Apps" テキストが含まれる（grep "Canvas Apps"）
    - "まだデプロイ済みアプリがありません" テキストが含まれる（grep "まだデプロイ"）
    - "+ 新しいチャットを開始" CTA が含まれる（grep "新しいチャット"）
    - `onStartChat(app.thread_id` パターンが含まれる（D-10 実装確認）
    - `role="alert"` がエラー表示に付いている（grep 'role="alert"'）
    - `aria-busy="true"` がローディング中に付いている（grep 'aria-busy'）
    - TypeScript コンパイルエラーがない（tsc --noEmit）
  </acceptance_criteria>
  <done>CanvasScreen.tsx が TypeScript エラーなしでコンパイルされ、D-08〜D-11 の要件をすべて実装している</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CanvasPane iframe | sandbox="allow-scripts allow-forms" のみ — allow-same-origin なし（XSS 防止）|
| フロントエンド → バックエンド | JWT cookie 付きリクエスト — 認証は既存パターン継承 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Spoofing | CanvasChatApp の canvasGemId | mitigate | canvasGemId は App.tsx が getCanvasGemId() で取得しサーバーから受け取る UUID — ユーザーが任意の gem_id を渡せないよう props 経由でのみ渡す |
| T-16-06 | Information Disclosure | CanvasPane iframe | accept | sandbox="allow-scripts allow-forms" — allow-same-origin なし。既存の Phase 15 セキュリティ制約を継承 [VERIFIED: CanvasPane.tsx] |
| T-16-07 | Elevation of Privilege | CanvasScreen の D-10（initialThreadId） | mitigate | initialThreadId は canvas_apps.thread_id (UUID) — ユーザー入力ではなく DB から取得した値。バックエンドでも所有権チェック済み |
| T-16-08 | XSS | CanvasScreen のアプリ名表示 | mitigate | app.name は {app.name} の JSX テキストノードとして挿入 — innerHTML を使わない。React の自動エスケープに依存 |
</threat_model>

<verification>
```bash
# TypeScript コンパイル確認
docker compose run --rm frontend bun run tsc --noEmit

# 新規ファイルの存在確認
ls -la frontend/src/components/CanvasChatApp.tsx
ls -la frontend/src/components/CanvasScreen.tsx

# 主要実装の確認
grep -n "handleCanvasDividerMouseDown" frontend/src/components/CanvasChatApp.tsx
grep -n "listCanvasApps" frontend/src/components/CanvasScreen.tsx
grep -n "listCanvasApps\|getCanvasGemId" frontend/src/api/client.ts

# UI-SPEC 準拠確認
grep -n "width: '4px'" frontend/src/components/CanvasChatApp.tsx  # drag handle 4px
grep -n "? 32 :" frontend/src/components/CanvasChatApp.tsx          # collapse width 32px
```
</verification>

<success_criteria>
- frontend/src/components/CanvasChatApp.tsx が存在しコンパイルが通る
- frontend/src/components/CanvasScreen.tsx が存在しコンパイルが通る
- client.ts に listCanvasApps と getCanvasGemId が export されている
- drag handle が 4px 幅（UI-SPEC 準拠）
- ThreadSidebar の collapse width が 32px（UI-SPEC 準拠、GemChatApp の 40px から修正）
- CanvasPane が canvasApp=null のときプレースホルダーを表示する
- CanvasScreen が deployed=true のアプリのみ取得する
</success_criteria>

<output>
完了後、`.planning/phases/16-canvas-app/16-02-SUMMARY.md` を作成する。
</output>
