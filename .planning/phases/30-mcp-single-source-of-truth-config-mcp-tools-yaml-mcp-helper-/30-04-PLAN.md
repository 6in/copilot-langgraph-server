---
phase: 30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-
plan: 04
type: execute
wave: 2
depends_on: ["30-01", "30-02"]
files_modified:
  - static/js/tool-catalog-generated.js
  - static/js/iframe-rpc.js
  - scripts/sync-tool-list-to-js.py
  - tests/test_tool_catalog_js.py
autonomous: true
requirements: [TBD]
must_haves:
  truths:
    - "`static/js/tool-catalog-generated.js` が新設され、AVAILABLE_TOOLS 定数を export する ES module になっている"
    - "`static/js/iframe-rpc.js` から AVAILABLE_TOOLS の埋め込み（L20-40 のマーカー間）が削除され、`import { AVAILABLE_TOOLS } from './tool-catalog-generated.js'` で差し替わっている"
    - "生成された tool-catalog-generated.js の先頭に `// DO NOT EDIT` ヘッダーが付いている"
    - "旧スクリプト `scripts/sync-tool-list-to-js.py` が削除されている（責務が `generate_mcp_artifacts.py` に移ったため）"
    - "Canvas iframe から `iframe-rpc.js` を import しても AVAILABLE_TOOLS が従来どおりエクスポートされる（後方互換）"
    - "`scripts/generate_mcp_artifacts.py --check` で JS 部分が drift なし"
  artifacts:
    - path: static/js/tool-catalog-generated.js
      provides: "AVAILABLE_TOOLS を export する ES module（自動生成）"
      contains: "export const AVAILABLE_TOOLS"
    - path: static/js/iframe-rpc.js
      provides: "postMessage RPC 本体（AVAILABLE_TOOLS を import し re-export）"
      contains: "import { AVAILABLE_TOOLS }"
    - path: tests/test_tool_catalog_js.py
      provides: "JS カタログファイル形状の pytest 検証"
      min_lines: 60
  key_links:
    - from: static/js/iframe-rpc.js
      to: static/js/tool-catalog-generated.js
      via: "import { AVAILABLE_TOOLS } from './tool-catalog-generated.js'"
      pattern: "import.*AVAILABLE_TOOLS.*tool-catalog-generated"
    - from: scripts/generate_mcp_artifacts.py
      to: static/js/tool-catalog-generated.js
      via: "--target js で上書き生成"
      pattern: "JS_PATH"
---

<objective>
JS カタログを物理的に分離する。`static/js/iframe-rpc.js` に埋め込まれていた AVAILABLE_TOOLS を `static/js/tool-catalog-generated.js` という独立 ES module に切り出し、`iframe-rpc.js` は import して re-export するだけにする。既存 `scripts/sync-tool-list-to-js.py` は責務重複のため削除する。Canvas iframe 側の `import { AVAILABLE_TOOLS } from '.../js/iframe-rpc.js'` は **変えない**（後方互換維持のため iframe-rpc.js 側で re-export）。

Purpose: 手書き (RPC ロジック) と自動生成 (カタログ) の境界を明確にする。
Output: 新規 ES module + iframe-rpc.js 書き換え + 旧スクリプト削除 + pytest。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/patterns.md
@config/mcp_tools.yaml
@scripts/generate_mcp_artifacts.py
@static/js/iframe-rpc.js
@scripts/sync-tool-list-to-js.py

**現行 iframe-rpc.js の構造（L1-131）:**
- L1-19: module docstring（手書き、変更しない）
- L20-40: `// --- BEGIN/END TOOL_CATALOG ---` マーカー間に AVAILABLE_TOOLS 埋め込み（Plan 04 で削除）
- L41-131: 手書き RPC 実装（pending Map、message listener、ai / query / call 関数、変更しない）

**AVAILABLE_TOOLS のコンシューマ（import 互換性調査）:**
`static/js/iframe-rpc.js` から名前付き export されている AVAILABLE_TOOLS は現時点で iframe hosting shell や Canvas 内部から参照可能なパブリック API。再配置後も同じ `import { AVAILABLE_TOOLS } from 'path/to/iframe-rpc.js'` が動作するよう、iframe-rpc.js 側で `export { AVAILABLE_TOOLS } from './tool-catalog-generated.js'` する。

**既存旧スクリプト:**
`scripts/sync-tool-list-to-js.py` は AVAILABLE_TOOLS を iframe-rpc.js に埋め込む古い仕組み。Plan 04 完了後は `generate_mcp_artifacts.py --target js` が独立ファイルを書き出すため不要。削除する。
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tool-catalog-generated.js 生成 + iframe-rpc.js を import 化 + 旧スクリプト削除</name>
  <files>static/js/tool-catalog-generated.js, static/js/iframe-rpc.js, scripts/sync-tool-list-to-js.py</files>
  <read_first>
    - static/js/iframe-rpc.js（L1-131 全体 — L20-40 のマーカー間のみ削除）
    - scripts/generate_mcp_artifacts.py（Plan 02 の build_js 出力仕様）
    - scripts/sync-tool-list-to-js.py（削除対象）
  </read_first>
  <behavior>
    - `static/js/tool-catalog-generated.js` が新規作成され、`// DO NOT EDIT` ヘッダー + JSDoc テーブル + `export const AVAILABLE_TOOLS = [...]` を含む
    - `static/js/iframe-rpc.js` から L20-40 のマーカーブロック (`BEGIN/END TOOL_CATALOG`) が削除され、代わりに先頭近くに import + re-export が配置される
    - `scripts/sync-tool-list-to-js.py` が削除される（`git rm`）
    - iframe-rpc.js の元の手書き部分（L1-19 のドキュメント + L41-131 の RPC 実装）は一字一句変更しない
  </behavior>
  <action>
**Step 1 — `scripts/generate_mcp_artifacts.py --target js` を実行して tool-catalog-generated.js を生成:**

```bash
cd /home/parallels/workspaces/copilot-langgraph
mkdir -p static/js
python3 scripts/generate_mcp_artifacts.py --target js > static/js/tool-catalog-generated.js
```

生成結果の期待形状（Plan 02 の build_js 出力）:

```javascript
// DO NOT EDIT — auto-generated from config/mcp_tools.yaml by scripts/generate_mcp_artifacts.py
// To regenerate: python3 scripts/generate_mcp_artifacts.py --target js

/**
 * Available MCP tools (auto-generated from config/mcp_tools.yaml).
 * Use `call(toolName, params)` to invoke.
 *
 * | Tool                 | Description                                                            |
 * | -------------------- | ---------------------------------------------------------------------- |
 * | ping                 | MCP サーバーのヘルスチェック（疎通確認・タイムスタンプ取得）                                         |
 * | web_search           | Tavily 経由でリアルタイム Web 検索を実行（...）                                        |
 * | db_query             | PostgreSQL に対して SELECT クエリを実行（...）                                    |
 * | claude_code          | Claude Code CLI をサブプロセスとして実行（...） [privileged]                         |
 * | execute_python       | Python コードをサンドボックス内で実行し stdout/stderr/exit_code を返す（...） [privileged] |
 * | get_current_datetime | 現在の日時を JST で返す（...）                                                   |
 */
export const AVAILABLE_TOOLS = [
  { name: "ping", description: "..." },
  { name: "web_search", description: "..." },
  { name: "db_query", description: "..." },
  { name: "claude_code", description: "...", privileged: true },
  { name: "execute_python", description: "...", privileged: true },
  { name: "get_current_datetime", description: "..." },
];
```

**Step 2 — static/js/iframe-rpc.js を Edit ツールで書き換える（確定方針）:**

**採用する方式:** Edit ツールの単一 `old_string` / `new_string` ペアで L20-40 のマーカーブロックを import 文に置換する（Write ツールでの全文書き換えは採用しない — 手書き RPC 本体の保全性を高めるため）。

**手順:**

1. まず `static/js/iframe-rpc.js` を **Read ツールで全文読み込む**（Edit の前提条件）
2. 読み込んだ内容から L20-40 の該当ブロック（`// --- BEGIN TOOL_CATALOG ---` から `// --- END TOOL_CATALOG ---` まで、両マーカー行も含む）を **逐語でコピーして `old_string` に設定する**
3. Edit ツールで以下のペアを適用:

**`old_string`（L20-40 の現行ブロックを逐語コピー — Edit 前に Read した内容を正確に反映する）:**

現行ファイル L20 起点の該当ブロックは `scripts/sync-tool-list-to-js.py` が生成した内容で、概ね以下の形状:

```
// --- BEGIN TOOL_CATALOG ---
/**
 * Available MCP tools (auto-generated — do not edit manually).
 * Run `python3 scripts/sync-tool-list-to-js.py` to regenerate.
 *
 * | Tool | Description |
 * | ---- | ----------- |
 * | ping | ... |
 * ...(複数行)...
 */
export const AVAILABLE_TOOLS = [
  { name: "ping", description: "..." },
  ...(複数行)...
];
// --- END TOOL_CATALOG ---
```

Edit 実行時は、Read で取得した **実際のファイル内容の L20-40 を空白・改行含めて逐語一致**させて `old_string` に与える（空白 1 個でも異なると Edit は失敗する）。

**`new_string`（import + re-export に置換、計 3 行 + 先頭空行）:**

```
// AVAILABLE_TOOLS は config/mcp_tools.yaml から自動生成される別ファイルを参照する (Phase 30)。
// Canvas iframe などの既存コンシューマとの後方互換のため、ここで re-export する。
export { AVAILABLE_TOOLS } from './tool-catalog-generated.js';
```

**Edit ツール呼び出し例（擬似コード）:**

```
Edit(
  file_path="static/js/iframe-rpc.js",
  old_string=<L20-40 の逐語コピー（Read 済み内容から抽出）>,
  new_string="// AVAILABLE_TOOLS は config/mcp_tools.yaml から自動生成される別ファイルを参照する (Phase 30)。\n// Canvas iframe などの既存コンシューマとの後方互換のため、ここで re-export する。\nexport { AVAILABLE_TOOLS } from './tool-catalog-generated.js';",
)
```

**結果として iframe-rpc.js の全体構造は:**
- L1-19: module docstring（元のまま、一字一句変更しない）
- L20-22: import/re-export 3 行（新規 — コメント 2 行 + `export { AVAILABLE_TOOLS } from '...'` 1 行）
- L23 以降: 手書き RPC 実装（元のまま、一字一句変更しない）

total 約 113 行程度（-21 + 3 = -18 行）。

**注意:**
- L1-19 の module docstring は触らない
- L41 以降（`/** @type {Map<string, ...>} */ const pending = new Map();` から末尾 `export function call(...)` まで）は一切変更しない
- マーカー `// --- BEGIN TOOL_CATALOG ---` / `// --- END TOOL_CATALOG ---` の文字列がファイルに残らないこと

**Edit ツールの `old_string` が一致せず失敗した場合のフォールバック:** ブロックの構造自体は変わっていないはずだが、空白や末尾改行の差でマッチしない可能性がある。その場合は Edit を再試行する前にもう一度 Read して **現行の正確な文字列**を取得し直す（Write ツールでの全文書き換えには切り替えない — 手書き RPC 本体を誤って変更するリスクがあるため）。

**Step 3 — 旧スクリプトを削除:**

```bash
git rm scripts/sync-tool-list-to-js.py
```

`git rm` により実ファイルも削除される。CLAUDE.md や README.md に `sync-tool-list-to-js.py` への参照があるか grep し、ある場合は `generate_mcp_artifacts.py --target js` 呼び出しに置換する（存在しなければこのサブステップはスキップ）。

```bash
grep -rn "sync-tool-list-to-js" --include="*.md" --include="*.sh" --include="*.yaml" . 2>/dev/null
```

ヒットした参照全てを `scripts/generate_mcp_artifacts.py --target js`（または `--target all`）に置き換える。

**Step 4 — 生成 JS の構文検証:**

以下を検証:
- 先頭 2 行が `// DO NOT EDIT` で始まる
- `export const AVAILABLE_TOOLS = [` が 1 行存在する
- 6 行の `name: "..."` エントリが存在する
- `];` で閉じられている

Step 2 の iframe-rpc.js について:
- `// --- BEGIN TOOL_CATALOG ---` が残存しない
- `// --- END TOOL_CATALOG ---` が残存しない
- `export { AVAILABLE_TOOLS } from './tool-catalog-generated.js'` が存在する
- `const pending = new Map();` と `export function ai(` と `export function query(` と `export function call(` が残存している
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && head -2 static/js/tool-catalog-generated.js | grep -q 'DO NOT EDIT' && grep -c 'export const AVAILABLE_TOOLS' static/js/tool-catalog-generated.js && grep -c 'name: "' static/js/tool-catalog-generated.js && ! grep -q 'BEGIN TOOL_CATALOG' static/js/iframe-rpc.js && ! grep -q 'END TOOL_CATALOG' static/js/iframe-rpc.js && grep -q "from './tool-catalog-generated.js'" static/js/iframe-rpc.js && grep -q 'export function ai' static/js/iframe-rpc.js && grep -q 'export function query' static/js/iframe-rpc.js && grep -q 'export function call' static/js/iframe-rpc.js && [ ! -f scripts/sync-tool-list-to-js.py ] && echo "ALL OK"</automated>
  </verify>
  <acceptance_criteria>
    - `static/js/tool-catalog-generated.js` が存在する
    - `head -2 static/js/tool-catalog-generated.js` に `// DO NOT EDIT` が含まれる
    - `grep -c 'export const AVAILABLE_TOOLS = \[' static/js/tool-catalog-generated.js` が **1**
    - `grep -c '  { name: "' static/js/tool-catalog-generated.js` が **6** (6 ツール)
    - `grep 'privileged: true' static/js/tool-catalog-generated.js` が 2 行以上ヒット（claude_code + execute_python）
    - `static/js/iframe-rpc.js` に `// --- BEGIN TOOL_CATALOG ---` が **存在しない**
    - `static/js/iframe-rpc.js` に `// --- END TOOL_CATALOG ---` が **存在しない**
    - `static/js/iframe-rpc.js` に `from './tool-catalog-generated.js'` を含む行が存在する
    - `static/js/iframe-rpc.js` に `export function ai(`, `export function query(`, `export function call(` が残存する（手書き部の回帰なし）
    - `scripts/sync-tool-list-to-js.py` が **存在しない**（`[ ! -f scripts/sync-tool-list-to-js.py ]` が真）
    - `grep -rn "sync-tool-list-to-js" --include="*.md" --include="*.sh" --include="*.yaml" .` が何もヒットしない（全参照が更新済み）
    - `cd . && python3 scripts/generate_mcp_artifacts.py --check` の JS 部分で drift が報告されない（helper のみ Plan 03 済・docs は Plan 06 予定のため、JS 単体で突き合わせるには `python3 -c "import importlib.util; ...; spec.loader.exec_module(gen); expected = gen.build_js(gen.load_tools()); actual = open('static/js/tool-catalog-generated.js').read(); assert actual == expected"`）
  </acceptance_criteria>
  <done>
    JS カタログが独立 ES module として分離され、iframe-rpc.js は import + re-export のみ。旧同期スクリプトは削除済み。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/test_tool_catalog_js.py で JS カタログの形状検証</name>
  <files>tests/test_tool_catalog_js.py</files>
  <read_first>
    - static/js/tool-catalog-generated.js（Task 1 で生成済み）
    - static/js/iframe-rpc.js（Task 1 で書き換え済み）
    - config/mcp_tools.yaml（期待 tool name リスト）
    - scripts/generate_mcp_artifacts.py（build_js 関数を直接比較対象にする）
  </read_first>
  <behavior>
    - Python の正規表現ベースの軽量テスト（node を CI に要求しない）
    - 生成ファイル構造（ヘッダー + export + 6 エントリ + 閉じ `];`）の検証
    - iframe-rpc.js にマーカーが残っていないこと、re-export が存在すること
    - 旧スクリプトが削除されていること（ファイル存在チェック）
    - ジェネレータの build_js 出力とファイル内容が完全一致（drift なし）
  </behavior>
  <action>
`tests/test_tool_catalog_js.py` を新規作成:

```python
"""Phase 30 Plan 04: JS カタログ分離後の回帰テスト。

Node を CI に要求しないため Python の正規表現で軽量に検証する。
決定論的な文字列一致は build_js の出力と実ファイル内容を比較して担保する。
"""
from __future__ import annotations

import importlib.util
from pathlib import Path

import pytest

REPO_ROOT = Path(__file__).resolve().parent.parent
CATALOG_PATH = REPO_ROOT / "static" / "js" / "tool-catalog-generated.js"
IFRAME_RPC_PATH = REPO_ROOT / "static" / "js" / "iframe-rpc.js"


def _load_generator():
    spec = importlib.util.spec_from_file_location(
        "generate_mcp_artifacts",
        REPO_ROOT / "scripts" / "generate_mcp_artifacts.py",
    )
    mod = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(mod)
    return mod


def test_catalog_file_exists():
    assert CATALOG_PATH.exists(), f"missing: {CATALOG_PATH}"


def test_catalog_header_is_do_not_edit():
    text = CATALOG_PATH.read_text()
    assert text.startswith("// DO NOT EDIT"), f"先頭: {text[:100]}"


def test_catalog_exports_available_tools():
    text = CATALOG_PATH.read_text()
    assert text.count("export const AVAILABLE_TOOLS = [") == 1


def test_catalog_contains_six_tools():
    text = CATALOG_PATH.read_text()
    # 6 つの `name: "...` エントリ（JSON object リテラル内）
    import re
    count = len(re.findall(r'\{\s*name:\s*"', text))
    assert count == 6, f"expected 6 tool entries, got {count}"


def test_catalog_privileged_tools_flagged():
    text = CATALOG_PATH.read_text()
    # claude_code と execute_python の両方に privileged: true
    assert 'name: "claude_code"' in text
    assert 'name: "execute_python"' in text
    # privileged: true が 2 回出現
    assert text.count("privileged: true") == 2


def test_iframe_rpc_no_marker_block():
    text = IFRAME_RPC_PATH.read_text()
    assert "BEGIN TOOL_CATALOG" not in text
    assert "END TOOL_CATALOG" not in text


def test_iframe_rpc_reexports_catalog():
    text = IFRAME_RPC_PATH.read_text()
    assert "from './tool-catalog-generated.js'" in text
    assert "AVAILABLE_TOOLS" in text


def test_iframe_rpc_keeps_handwritten_rpc():
    text = IFRAME_RPC_PATH.read_text()
    # 手書き RPC 関数が回帰していないこと
    assert "export function ai(" in text
    assert "export function query(" in text
    assert "export function call(" in text
    # message listener と pending Map も残っている
    assert "const pending = new Map();" in text
    assert "window.addEventListener('message'" in text


def test_legacy_sync_script_removed():
    legacy = REPO_ROOT / "scripts" / "sync-tool-list-to-js.py"
    assert not legacy.exists(), "scripts/sync-tool-list-to-js.py must be deleted in Plan 04"


def test_catalog_matches_generator_output():
    """drift 検知: build_js(tools) の出力と実ファイル内容が完全一致する。"""
    gen = _load_generator()
    expected = gen.build_js(gen.load_tools())
    actual = CATALOG_PATH.read_text()
    assert actual == expected, "tool-catalog-generated.js is out of sync with generator output"
```
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && uv run pytest tests/test_tool_catalog_js.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_tool_catalog_js.py -v` が全テスト PASSED (10 テスト)
    - 出力に `test_catalog_header_is_do_not_edit PASSED` が含まれる
    - 出力に `test_iframe_rpc_no_marker_block PASSED` が含まれる
    - 出力に `test_iframe_rpc_keeps_handwritten_rpc PASSED` が含まれる
    - 出力に `test_legacy_sync_script_removed PASSED` が含まれる
    - 出力に `test_catalog_matches_generator_output PASSED` が含まれる（drift なし）
    - Plan 03 の `uv run pytest tests/test_mcp_helper_generated.py` も引き続き全 pass
    - Plan 01 の `uv run pytest tests/test_tool_registry.py` も引き続き全 pass
  </acceptance_criteria>
  <done>
    JS 分離の回帰テストが全 pass。build_js 出力とファイル内容が drift なし。旧同期スクリプトも削除確認済み。
  </done>
</task>

</tasks>

<verification>
- `static/js/tool-catalog-generated.js` は「生成」ファイル (先頭 DO NOT EDIT)
- `static/js/iframe-rpc.js` は「手書き」ファイル (RPC 本体) + re-export 行だけが新規追加
- `scripts/sync-tool-list-to-js.py` が削除されている
- generator の build_js 出力と実ファイルが完全一致 (drift なし)
- 既存 Canvas / iframe-rpc コンシューマが `import { AVAILABLE_TOOLS } from '.../iframe-rpc.js'` で引き続き動作（re-export 経由）
</verification>

<success_criteria>
- JS カタログが独立 ES module に分離
- iframe-rpc.js から TOOL_CATALOG マーカーが消滅
- 旧スクリプト削除
- pytest 回帰テスト全 pass
</success_criteria>

<output>
After completion, create `.planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-04-SUMMARY.md`
</output>
