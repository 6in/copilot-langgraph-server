---
phase: 30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-
plan: 06
type: execute
wave: 2
depends_on: ["30-02"]
files_modified:
  - docs/mcp-tools.md
  - docs/mcp-tool-add-manual.md
  - .claude/commands/add-mcp-tool.md
  - CLAUDE.md
  - .planning/patterns.md
autonomous: true
requirements: [TBD]
must_haves:
  truths:
    - "`docs/mcp-tools.md` が `scripts/generate_mcp_artifacts.py --target docs` で生成されており、先頭に DO NOT EDIT ヘッダーが付いている"
    - "`docs/mcp-tool-add-manual.md` が手書きで新規作成され、YAML スキーマ・手書き境界・privileged 基準・pre-commit 挙動を説明している"
    - "`.claude/commands/add-mcp-tool.md` スラッシュコマンドが新規作成され、新規 MCP ツール追加フローを 1 コマンドで自動化する（create-adr.md の既存パターン踏襲）"
    - "`CLAUDE.md` に MCP ツール追加時の運用ルール（/add-mcp-tool スラッシュコマンドと docs/mcp-tool-add-manual.md への参照）が追記されている"
    - "`.planning/patterns.md` の旧「iframe-rpc.js ツールカタログ埋め込み + 同期スクリプト」エントリが Phase 30 スキーマに更新され、新規「MCP ツール single-source-of-truth 化」パターンが追加されている"
    - "Plan 06 完了後に `python3 scripts/generate_mcp_artifacts.py --check` が全ターゲットで drift なし (exit 0)"
  artifacts:
    - path: docs/mcp-tools.md
      provides: "自動生成ツールカタログドキュメント（6 ツール詳細）"
      contains: "# MCP Tools Catalog"
    - path: docs/mcp-tool-add-manual.md
      provides: "新規 MCP ツール追加マニュアル（手書き）"
      min_lines: 120
    - path: .claude/commands/add-mcp-tool.md
      provides: "/add-mcp-tool スラッシュコマンド"
      min_lines: 80
    - path: .planning/patterns.md
      provides: "MCP・Tools セクションの Phase 30 対応更新"
      contains: "MCP ツール single-source-of-truth"
  key_links:
    - from: .claude/commands/add-mcp-tool.md
      to: docs/mcp-tool-add-manual.md
      via: "マニュアル参照"
      pattern: "mcp-tool-add-manual"
    - from: CLAUDE.md
      to: .claude/commands/add-mcp-tool.md
      via: "運用ルール記載"
      pattern: "add-mcp-tool"
---

<objective>
Phase 30 の運用面を完成させる。(1) `docs/mcp-tools.md` を Plan 02 ジェネレータで初回生成する。(2) `docs/mcp-tool-add-manual.md` を手書きで作成し YAML スキーマ・手書き/自動生成境界・privileged 基準・pre-commit 挙動を説明する。(3) `.claude/commands/add-mcp-tool.md` スラッシュコマンドを実装して新規ツール追加を自動化する。(4) `CLAUDE.md` に運用ルールを追記する。(5) `.planning/patterns.md` の MCP・Tools セクションを Phase 30 スキーマに合わせて更新する。

Purpose: 今後の新規ツール追加が 1 コマンドで完結する UX を完成させる + 既存パターンカタログを Phase 30 の到達点に合わせて維持する。
Output: 自動生成ドキュメント + 手書きマニュアル + スラッシュコマンド + CLAUDE.md 更新 + patterns.md 更新。

**wave 2 配置の理由（BLOCKER 4 案 A による調整）:** 本 Plan 06 は Plan 05 (pre-commit hook) より前に完了させる必要がある。Plan 05 の pre-commit hook は `docs/mcp-tools.md` を drift 検査対象に含むため、REPO_ROOT に同ファイルが存在しない状態で hook をインストールすると後続の全コミットが `[drift] missing: docs/mcp-tools.md` でブロックされる。したがって wave 2（Plan 06 が docs/mcp-tools.md を REPO_ROOT に生成・コミット）→ wave 3（Plan 05 が hook をインストール）の順で実行する。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/patterns.md
@config/mcp_tools.yaml
@scripts/generate_mcp_artifacts.py
@scripts/install-hooks.sh
@.claude/commands/create-adr.md
@CLAUDE.md
@mcp_server/tools/mcp_helper_utils.py
@mcp_server/tools/mcp_helper.py
@mcp_server/server.py
@.planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-02-SUMMARY.md
@.planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-03-SUMMARY.md
@.planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-04-SUMMARY.md

**.claude/commands/create-adr.md の既存パターン (Plan 06 が踏襲):**
- 冒頭 1-2 行で目的
- `## Steps` 見出しで番号付きステップ (1-6)
- 各ステップに bash コマンドや Claude への指示を混在
- ステップ 6 のようにフォローアップリマインダ可

**新規ツール追加フロー（/add-mcp-tool の挙動）:**
1. ユーザーから引数 (ツール名 or 説明) を受け取る
2. `config/mcp_tools.yaml` に新エントリを追加（Claude が対話的にフィールド収集）
3. `mcp_server/tools/<name>.py` を既存 `stubs.py` / `ping.py` 等から雛形生成
4. `mcp_server/server.py` の import と `register_<name>_tools(mcp)` 呼び出しを追加
5. `scripts/generate_mcp_artifacts.py --target all` を実行
6. `tests/test_<name>_tool.py` の雛形を作成
7. `docs/mcp-tool-add-manual.md` を読むようリマインド

**現行 .planning/patterns.md の MCP・Tools セクション（Phase 30 更新対象）:**
- L94-97: 「iframe-rpc.js ツールカタログ埋め込み + 同期スクリプト」エントリ → Phase 30 で `scripts/sync-tool-list-to-js.py` は削除され、新仕組み（`config/mcp_tools.yaml` single-source + 独立 JS カタログ + `generate_mcp_artifacts.py`）に置き換わった。本文を Phase 30 後のスキーマに書き換える。
- 末尾: Phase 30 新規パターン「MCP ツール single-source-of-truth 化」エントリを新設。関連 ADR リンクは Phase 完了後に `/create-adr` 経由で作成予定のため placeholder（番号未定）で OK。
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: docs/mcp-tools.md 生成 + docs/mcp-tool-add-manual.md 新規作成</name>
  <files>docs/mcp-tools.md, docs/mcp-tool-add-manual.md</files>
  <read_first>
    - scripts/generate_mcp_artifacts.py（build_docs 関数の出力仕様）
    - config/mcp_tools.yaml（6 ツール一覧）
    - mcp_server/tools/mcp_helper_utils.py（手書き境界の説明対象）
    - scripts/install-hooks.sh（pre-commit 挙動の説明対象）
    - docs/adr/0023-mcp-db-query-and-claude-code-tools.md（privileged 基準の出典）
    - docs/adr/0024-mcp-tool-catalog-validation.md（ToolRegistry 契約）
  </read_first>
  <behavior>
    - `docs/mcp-tools.md` が Plan 02 の build_docs の出力と **完全一致** する内容でディスクに作成される
    - `docs/mcp-tool-add-manual.md` が手書きで新規作成され、Phase 30 アーキテクチャの 6 トピック（YAML スキーマ / 手書き境界 / 生成コマンド / privileged 基準 / pre-commit / 新規ツール追加チェックリスト）を網羅する
  </behavior>
  <action>
**Step 1 — docs/mcp-tools.md を生成:**

```bash
cd /home/parallels/workspaces/copilot-langgraph
python3 scripts/generate_mcp_artifacts.py --target docs > docs/mcp-tools.md
```

これで Plan 02 の build_docs 出力（6 ツールの一覧表 + 各ツール詳細セクション）がディスクに書き出される。先頭は `<!-- DO NOT EDIT -->` コメント。

生成後の構造確認:
- `head -2 docs/mcp-tools.md` が DO NOT EDIT コメントで始まる
- `grep -c '^## \`' docs/mcp-tools.md` が 6（6 ツール詳細セクション）
- `grep -c '^| \`' docs/mcp-tools.md` が 6（一覧表 6 行）

**Step 2 — docs/mcp-tool-add-manual.md を手書きで新規作成:**

約 160-200 行の Markdown。以下の構造で **必ず日本語**で記述する:

```markdown
# MCP ツール追加マニュアル

このドキュメントは MCP ツールを新規追加する際の **手順書** です。
自動化されたスラッシュコマンド `/add-mcp-tool` が用意されていますが、その中身で何が
起きているかを理解するため、また個別の事情で手動調整が必要な場合のためにこのマニュアル
を参照してください。

Phase 30 で確立された single-source-of-truth 方針:
`config/mcp_tools.yaml` が MCP ツールカタログの **唯一の宣言**。以下 3 ファイルは全て
そこから自動生成される:

- `mcp_server/tools/mcp_helper.py` — sandbox から MCP を呼ぶ Python ラッパー
- `static/js/tool-catalog-generated.js` — iframe-rpc.js が import する JS カタログ
- `docs/mcp-tools.md` — 人間向けツールカタログ

## 手書き境界（Phase 30 D-02）

| ファイル | 区分 | 編集方法 |
|---------|-----|---------|
| `config/mcp_tools.yaml` | 手書き（SSoT） | 手動編集 |
| `mcp_server/tools/<name>.py` | 手書き（実装） | 手動編集 |
| `mcp_server/tools/mcp_helper_utils.py` | 手書き（基盤） | 手動編集 |
| `mcp_server/tools/mcp_helper.py` | **自動生成** | ジェネレータ再実行 |
| `static/js/tool-catalog-generated.js` | **自動生成** | ジェネレータ再実行 |
| `static/js/iframe-rpc.js` | 手書き（RPC 本体） | 手動編集（カタログは import 経由） |
| `docs/mcp-tools.md` | **自動生成** | ジェネレータ再実行 |
| `docs/mcp-tool-add-manual.md` | 手書き（本ファイル） | 手動編集 |

自動生成ファイルの先頭には `# DO NOT EDIT` ヘッダーが付く。手動編集すると pre-commit
hook (`scripts/install-hooks.sh` でインストール) が drift を検知し commit をブロック
する。

## YAML スキーマリファレンス

```yaml
tools:
  - name: <str>                    # 必須。MCP サーバーの @mcp.tool 関数名と完全一致
    description: <str>              # 必須。短文説明（JS カタログ・docs で表示）
    privileged: <bool>              # 任意。true で SubAgentRegistry が WARNING を出す
    sandbox_exposed: <bool>         # 任意。false で mcp_helper.py に wrapper を生成しない
    python_wrapper:                 # sandbox_exposed: true かつ sandbox から呼ぶ場合に必須
      function_name: <str>          # mcp_helper.py 内の関数名
      args:                         # Python 関数の引数リスト
        - name: <str>
          type: <str>               # Python 型注釈文字列（"str", "int", "list[dict]" 等）
          default: <str>            # 任意。引用符込みで書く（例: '"default"'）
          description: <str>
      return_type: <str>            # "list[dict]", "dict" 等
      docstring: |                  # 関数 docstring。Args/Returns/Example ブロックを含める
        ...
      mcp_args_mapping:             # Python 関数引数 → MCP tool 引数へのマッピング
        <py_arg_name>: <mcp_arg_name>
      result_transform:             # _call_tool の戻り値の変換方法
        mode: passthrough | extract_key | web_search_results
        key: <str>                  # extract_key の場合のみ必須
```

### result_transform.mode の 3 種

1. `passthrough` — `_call_tool` の戻り値をそのまま返す（ping / get_current_datetime）
2. `extract_key` — dict から `result.get("<key>", [result])` を取り出す（db_query の `rows`）
3. `web_search_results` — `results` キーを取り出し、各 item の `content` に `_clean_content()` を適用（web_search 専用）

新しい transform パターンが必要になった場合は `scripts/generate_mcp_artifacts.py` の
`_generate_wrapper_body()` に分岐を追加すること。

## 生成コマンド

```bash
# 3 ファイル全て再生成
python3 scripts/generate_mcp_artifacts.py --target all

# 個別ターゲット（stdout に出力）
python3 scripts/generate_mcp_artifacts.py --target helper
python3 scripts/generate_mcp_artifacts.py --target js
python3 scripts/generate_mcp_artifacts.py --target docs

# drift 検知（pre-commit hook が内部で使用）
python3 scripts/generate_mcp_artifacts.py --check
```

## privileged フラグの基準 (ADR 0023 / 0024)

`privileged: true` は以下のいずれかに該当するツールに付与する:

- worker / mcp-server コンテナのファイルシステム全域にアクセスできる (claude_code)
- 任意の Python コードを実行できる (execute_python)
- 外部 API に書き込み操作を行える
- 機密情報（秘密鍵、DSN 等）を読み出せる

`privileged: true` のツールを `agents/<name>/AGENT.md` の `tools:` フィールドに
宣言すると `SubAgentRegistry` が WARNING ログを出す。社内機密を扱うエージェントには
付与しないこと。

## pre-commit hook の挙動

`scripts/install-hooks.sh` を実行すると `.git/hooks/pre-commit` がインストールされる。
hook は以下 2 つの検査を行う:

1. **ADR INDEX 再生成** (Phase 26) — `docs/adr/*.md` が staged なら `generate_adr_index.py`
2. **MCP artifacts drift 検知** (Phase 30) — 以下のいずれかが staged なら `generate_mcp_artifacts.py --check`:
   - `config/mcp_tools.yaml`
   - `mcp_server/tools/mcp_helper.py`
   - `static/js/tool-catalog-generated.js`
   - `docs/mcp-tools.md`

drift が検知されると commit がブロックされ、以下のメッセージが表示される:

    python3 scripts/generate_mcp_artifacts.py --target all
    git add mcp_server/tools/mcp_helper.py static/js/tool-catalog-generated.js docs/mcp-tools.md
    git commit

## 新規ツール追加チェックリスト

### 推奨: スラッシュコマンドを使う

```
/add-mcp-tool <tool_name>
```

これで以下 6 ステップが対話的に実行される。

### 手動手順

1. `config/mcp_tools.yaml` に新エントリを追加（上記スキーマに従う）
2. `mcp_server/tools/<name>.py` を新規作成（既存 `ping.py` / `web_search.py` / `db_query.py` を参考）
3. `mcp_server/server.py` に `register_<name>_tools` import と呼び出しを追加
4. `python3 scripts/generate_mcp_artifacts.py --target all` を実行
5. `tests/test_<name>_tool.py` に最低 1 つの pytest テストを追加
6. `ToolRegistry` が MCP 実ツールリストとの一致を検証するため、`docker compose restart worker` で MCP サーバーとの同期を確認（YAML とコードの両方がコミットされた状態である必要がある）
7. `git add config/mcp_tools.yaml mcp_server/tools/<name>.py mcp_server/server.py mcp_server/tools/mcp_helper.py static/js/tool-catalog-generated.js docs/mcp-tools.md tests/test_<name>_tool.py`
8. `git commit -m "feat(mcp): add <name> tool"`

### 削除手順

1. `config/mcp_tools.yaml` から該当エントリを削除
2. `mcp_server/tools/<name>.py` を削除
3. `mcp_server/server.py` の import と register 呼び出しを削除
4. `python3 scripts/generate_mcp_artifacts.py --target all`
5. 関連テストファイルを削除
6. コミット

## ToolRegistry との関係 (ADR 0024)

`app/orchestrator/tool_registry.py` は `config/mcp_tools.yaml` の `tools[*].name` と
MCP サーバーが実際に公開するツール名の集合が **完全一致** することを worker 起動時に
検証する。不一致時は `RuntimeError` を raise して worker 起動を失敗させる。

Phase 30 で YAML スキーマが拡張された後も、ToolRegistry は `name` と `privileged` の
2 フィールドしか参照しないため、`python_wrapper` / `sandbox_exposed` 等の追加フィールド
は無視される（後方互換）。
```

上記を `docs/mcp-tool-add-manual.md` に書き出す（日本語で約 160 行）。code fence は triple-backtick でエスケープする。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && head -2 docs/mcp-tools.md | grep -q 'DO NOT EDIT' && [ "$(grep -c '^## \`' docs/mcp-tools.md)" = "6" ] && [ -f docs/mcp-tool-add-manual.md ] && grep -q 'single-source-of-truth' docs/mcp-tool-add-manual.md && grep -q 'result_transform' docs/mcp-tool-add-manual.md && grep -q 'privileged' docs/mcp-tool-add-manual.md && grep -q 'pre-commit' docs/mcp-tool-add-manual.md && python3 scripts/generate_mcp_artifacts.py --check && echo "DOCS_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/mcp-tools.md` が存在する
    - `head -2 docs/mcp-tools.md` に `DO NOT EDIT` が含まれる
    - `grep -c '^## \`' docs/mcp-tools.md` が **6**（6 ツール詳細セクション）
    - `grep -c '^| \`' docs/mcp-tools.md` が **6**（一覧表 6 行）
    - `docs/mcp-tool-add-manual.md` が存在し、**日本語**で書かれている（`手書き` または `追加` 等の日本語文字が含まれる）
    - `docs/mcp-tool-add-manual.md` に `YAML スキーマリファレンス` セクションが含まれる
    - `docs/mcp-tool-add-manual.md` に `result_transform` の 3 モードが言及されている（passthrough / extract_key / web_search_results）
    - `docs/mcp-tool-add-manual.md` に `privileged` フラグ基準の説明が含まれる
    - `docs/mcp-tool-add-manual.md` に `pre-commit` 挙動の説明が含まれる
    - `docs/mcp-tool-add-manual.md` に `/add-mcp-tool` スラッシュコマンドへの参照が含まれる
    - `docs/mcp-tool-add-manual.md` の先頭に `# DO NOT EDIT` が **含まれない**（手書きファイルであるため）
    - `python3 scripts/generate_mcp_artifacts.py --check` が exit 0 で終了（Plan 03/04 完了後 + 今回の docs/mcp-tools.md 生成で全て drift なし）
    - `wc -l docs/mcp-tool-add-manual.md` が 120 行以上
  </acceptance_criteria>
  <done>
    自動生成の docs/mcp-tools.md と手書きの docs/mcp-tool-add-manual.md が両方コミット可能な状態で揃っている。3 生成ファイル全て drift なし。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /add-mcp-tool スラッシュコマンド + CLAUDE.md 運用ルール追記 + patterns.md 更新</name>
  <files>.claude/commands/add-mcp-tool.md, CLAUDE.md, .planning/patterns.md</files>
  <read_first>
    - .claude/commands/create-adr.md（既存スラッシュコマンドのスタイル参考）
    - config/mcp_tools.yaml（現行スキーマ）
    - mcp_server/tools/web_search.py や db_query.py（ツール実装雛形の参考元）
    - mcp_server/server.py（register_*_tools 呼び出しパターン）
    - Task 1 で書き終えた docs/mcp-tool-add-manual.md
    - CLAUDE.md（既存「GSD Workflow Enforcement」「ADR Pattern Reference」セクションのスタイル）
    - .planning/patterns.md L72-110 (MCP・Tools セクション全体、特に L94-97 の「iframe-rpc.js ツールカタログ埋め込み + 同期スクリプト」エントリ)
  </read_first>
  <behavior>
    - `.claude/commands/add-mcp-tool.md` が新規作成され、Claude が「引数からツール名を取得 → YAML 追加 → ツール実装雛形作成 → server.py 登録 → ジェネレータ実行 → テスト雛形 → マニュアル参照」の 7 ステップを自動実行する指示書になっている
    - `CLAUDE.md` の既存「ADR Pattern Reference」セクション付近または新規「MCP Tool Catalog (Phase 30)」セクションに運用ルールが追記される
    - CLAUDE.md への追記は既存テキストを破壊しない（git diff で追加のみ）
    - `.planning/patterns.md` の MCP・Tools セクションが Phase 30 後のスキーマに更新される:
      - 旧「iframe-rpc.js ツールカタログ埋め込み + 同期スクリプト」エントリ（L94-97）の本文を新仕組みに書き換える
      - 新規「MCP ツール single-source-of-truth 化」エントリを MCP・Tools セクションに追加
  </behavior>
  <action>
**Step 1 — .claude/commands/add-mcp-tool.md を新規作成:**

`.claude/commands/create-adr.md` のスタイル (`## Steps` + 番号付きステップ) を踏襲する。

ファイル全文:

```markdown
新規 MCP ツールを Phase 30 の single-source-of-truth 方針に従って追加する。`$ARGUMENTS` にツール名が入る想定（例: `/add-mcp-tool fetch_url`）。空なら Claude が対話的にユーザーから目的を聞いて命名する。

## Steps

**1. ツール名と概要の確定**

`$ARGUMENTS` を読み取り:
- 非空ならツール名として採用（`snake_case` であることを確認）
- 空ならユーザーに「追加したい MCP ツールの目的は？」と 1 回だけ質問し、結果から `snake_case` のツール名を Claude が命名する

既存ツールとの衝突確認:

```bash
grep -E "^  - name: ${TOOL_NAME}" config/mcp_tools.yaml && echo "衝突: 既存" && exit 1
```

**2. config/mcp_tools.yaml に新エントリを追加**

`docs/mcp-tool-add-manual.md` の YAML スキーマリファレンスに従い、以下をユーザーと対話で決定してから YAML 末尾に追記する:

- `name`: Step 1 で確定
- `description`: 1 行説明（日本語）
- `privileged`: 基準は `docs/mcp-tool-add-manual.md` 参照 — 迷ったら false
- `sandbox_exposed`: sandbox (execute_python) から呼び出したいなら true、そうでなければ false
- `python_wrapper`（sandbox_exposed=true の場合のみ）:
  - `function_name` / `args` / `return_type` / `docstring` / `mcp_args_mapping` / `result_transform`

YAML 編集後、構文チェック:

```bash
python3 -c "import yaml; yaml.safe_load(open('config/mcp_tools.yaml'))"
```

**3. ツール実装ファイルの雛形を作成**

`mcp_server/tools/${TOOL_NAME}.py` を新規作成する。既存の `mcp_server/tools/web_search.py` (簡易 Tavily ラッパー) を雛形として最適。骨格:

```python
"""${TOOL_NAME} MCP ツール (Phase 30 で追加)."""
from __future__ import annotations

from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from fastmcp import FastMCP


def register_tools(mcp: "FastMCP") -> None:
    """${TOOL_NAME} を FastMCP インスタンスに登録する。"""

    @mcp.tool
    def ${TOOL_NAME}(<args>) -> dict:
        """<description from YAML>

        Args:
            ...

        Returns:
            {...} or {"error": "..."}
        """
        try:
            # 実装をここに書く
            return {"ok": True}
        except Exception as e:
            return {"error": f"${TOOL_NAME} failed: {e}"}
```

実際の引数・戻り値・例外処理はユーザーの要件に応じて Claude が提案する。

**4. mcp_server/server.py に登録を追加**

以下 2 箇所を編集する:

(a) import セクション (L17-22 近辺):
```python
from tools.${TOOL_NAME} import register_tools as register_${TOOL_NAME}_tools
```

(b) register 呼び出しセクション (L49-53 近辺、`register_execute_python_tools(mcp)` の直前か直後):
```python
register_${TOOL_NAME}_tools(mcp)
```

**5. 生成ファイルを再生成**

```bash
python3 scripts/generate_mcp_artifacts.py --target all
```

これで `mcp_server/tools/mcp_helper.py` / `static/js/tool-catalog-generated.js` / `docs/mcp-tools.md` の 3 ファイルが最新化される。

**6. テスト雛形を作成**

`tests/test_${TOOL_NAME}_tool.py` を新規作成する。最低限 1 つの正常系テストを書く。既存 `tests/test_mcp_helper_generated.py` 等のスタイルを踏襲。

**7. pre-commit 互換性確認 + コミット**

```bash
python3 scripts/generate_mcp_artifacts.py --check
uv run pytest tests/test_tool_registry.py tests/test_mcp_helper_generated.py tests/test_tool_catalog_js.py tests/test_${TOOL_NAME}_tool.py -v
git add config/mcp_tools.yaml mcp_server/tools/${TOOL_NAME}.py mcp_server/server.py mcp_server/tools/mcp_helper.py static/js/tool-catalog-generated.js docs/mcp-tools.md tests/test_${TOOL_NAME}_tool.py
git commit -m "feat(mcp): add ${TOOL_NAME} tool"
```

pre-commit hook が走り、drift なしなら commit 成功。

## 関連ドキュメント

- `docs/mcp-tool-add-manual.md` — 手動手順・YAML スキーマ詳細・privileged 基準・pre-commit 挙動
- `config/mcp_tools.yaml` — ツールカタログ (single source of truth)
- `scripts/generate_mcp_artifacts.py` — 3 ファイル生成ジェネレータ

## 完了後の報告

Claude は実行終了時に以下を報告する:

```
Added MCP tool: ${TOOL_NAME}
- YAML entry: config/mcp_tools.yaml
- Implementation: mcp_server/tools/${TOOL_NAME}.py
- Test: tests/test_${TOOL_NAME}_tool.py
- Regenerated: mcp_helper.py / tool-catalog-generated.js / mcp-tools.md

Next: docker compose restart worker mcp-server
```
```

**Step 2 — CLAUDE.md に運用ルールを追記:**

既存の `## ADR Pattern Reference (GSD Integration)` セクションの直後（または `## Architecture` の前）に以下のセクションを新規追加する。既存テキストは **一切削除・変更しない**:

```markdown
## MCP Tool Catalog (Phase 30)

MCP ツールカタログは `config/mcp_tools.yaml` を single source of truth とし、以下 3 ファイルが自動生成される:

- `mcp_server/tools/mcp_helper.py` — sandbox 用 Python ラッパー
- `static/js/tool-catalog-generated.js` — iframe-rpc.js が import する JS カタログ
- `docs/mcp-tools.md` — 人間向けカタログ

### 新規ツール追加

推奨: `/add-mcp-tool <name>` スラッシュコマンドを使う。
手動手順・YAML スキーマ詳細は `docs/mcp-tool-add-manual.md` を参照。

### 手書きと自動生成の境界

- **手書き**: `config/mcp_tools.yaml`、`mcp_server/tools/<name>.py`、`mcp_server/tools/mcp_helper_utils.py`、`static/js/iframe-rpc.js`（RPC 本体）、`docs/mcp-tool-add-manual.md`
- **自動生成**（DO NOT EDIT ヘッダー付）: `mcp_server/tools/mcp_helper.py`、`static/js/tool-catalog-generated.js`、`docs/mcp-tools.md`

自動生成ファイルを手動編集すると `scripts/install-hooks.sh` でインストールされる pre-commit hook が `python3 scripts/generate_mcp_artifacts.py --check` で drift を検知し commit をブロックする。修正は `python3 scripts/generate_mcp_artifacts.py --target all` を実行して再ステージすること。

### 新規クローン直後の 1 回限り実行

```bash
bash scripts/install-hooks.sh
```

このコマンドは Phase 26 の ADR INDEX 自動生成と Phase 30 の MCP drift 検知の両方を同じ pre-commit hook に組み込む。
```

既存 CLAUDE.md の末尾にある `## Developer Profile` セクションの **前** に挿入する（Developer Profile は管理外セクションとの指示があるため）。

挿入位置の目安: `## GSD Workflow Enforcement` の直後、または既存 `### ADR INDEX 自動生成 hook のインストール` の直後。後者がより自然（hook インストール系の説明が隣接する）。

**実装手順:** Claude は `CLAUDE.md` を読み、`### ADR INDEX 自動生成 hook のインストール` セクションの終了地点を特定し、その直後（次の `## ...` または `# ...` 見出しの直前）に上記 `## MCP Tool Catalog (Phase 30)` セクションを挿入する。

**Step 3 — .planning/patterns.md の MCP・Tools セクションを更新:**

現行 `.planning/patterns.md` の L94-97（MCP・Tools セクション内）の「iframe-rpc.js ツールカタログ埋め込み + 同期スクリプト」エントリは Phase 30 で仕組み全体が置き換わったため本文を更新する。具体的には Edit ツールで以下の `old_string` / `new_string` ペアを適用する:

**`old_string`（現行 L94-97 — Edit 前に Read した内容を正確に反映する）:**
```
### iframe-rpc.js ツールカタログ埋め込み + 同期スクリプト
`config/mcp_tools.yaml` のツール情報を `static/js/iframe-rpc.js` に `AVAILABLE_TOOLS` 定数として埋め込む。
マーカーコメント (`BEGIN/END TOOL_CATALOG`) で囲み、`scripts/sync-tool-list-to-js.py` で自動更新可能。
関連 ADR: [0040](../docs/adr/0040-ui-improvements-batch-mermaid-copy-thread-grouping-authflow.md)
```

**`new_string`（Phase 30 後のスキーマに書き換え、新規パターンエントリも続けて追加）:**
```
### iframe-rpc.js ツールカタログは独立 ES module 参照
`static/js/iframe-rpc.js` の `AVAILABLE_TOOLS` 埋め込みは Phase 30 で廃止。`static/js/tool-catalog-generated.js` に分離し、`iframe-rpc.js` は `export { AVAILABLE_TOOLS } from './tool-catalog-generated.js'` で re-export するのみ。
旧 `scripts/sync-tool-list-to-js.py` は削除され、`scripts/generate_mcp_artifacts.py --target js` に責務が統合された。
関連 ADR: [0040](../docs/adr/0040-ui-improvements-batch-mermaid-copy-thread-grouping-authflow.md), Phase 30 (ADR 番号未定 — `/create-adr` で追補予定)

### MCP ツール single-source-of-truth 化
`config/mcp_tools.yaml` を MCP ツールカタログの唯一の宣言源とし、以下 3 ファイルは全て `scripts/generate_mcp_artifacts.py` から決定論的に自動生成する:
- `mcp_server/tools/mcp_helper.py`（sandbox 用 Python ラッパー）
- `static/js/tool-catalog-generated.js`（iframe-rpc が import する JS カタログ）
- `docs/mcp-tools.md`（人間向けドキュメント）

手書き基盤 (`_call_tool` / `_clean_content`) は `mcp_server/tools/mcp_helper_utils.py` に分離。生成ファイルの手動編集は pre-commit hook (`scripts/install-hooks.sh`) の `--check` モードで drift 検知・commit ブロックされる。
関連 ADR: Phase 30 (ADR 番号未定 — `/create-adr` で追補予定)
```

Edit ツールの仕様上 `old_string` は現行ファイルの該当ブロックに**空白・改行含めて逐語一致**している必要があるため、Claude は Edit 実行前に必ず `Read` で当該区間を確認し、実際の文字列を `old_string` に与える。空白 1 個でも異なると Edit は失敗するので注意。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && [ -f .claude/commands/add-mcp-tool.md ] && grep -q 'config/mcp_tools.yaml' .claude/commands/add-mcp-tool.md && grep -q 'generate_mcp_artifacts.py --target all' .claude/commands/add-mcp-tool.md && grep -q 'docs/mcp-tool-add-manual.md' .claude/commands/add-mcp-tool.md && grep -q 'MCP Tool Catalog' CLAUDE.md && grep -q 'add-mcp-tool' CLAUDE.md && grep -q 'docs/mcp-tool-add-manual.md' CLAUDE.md && grep -q 'generate_mcp_artifacts.py' CLAUDE.md && grep -q 'MCP ツール single-source-of-truth' .planning/patterns.md && grep -q 'tool-catalog-generated.js' .planning/patterns.md && ! grep -q 'sync-tool-list-to-js.py で自動更新可能' .planning/patterns.md && python3 scripts/generate_mcp_artifacts.py --check && echo "CMD_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/commands/add-mcp-tool.md` が存在する
    - `.claude/commands/add-mcp-tool.md` に `## Steps` が含まれる（create-adr.md スタイル踏襲）
    - `.claude/commands/add-mcp-tool.md` に `config/mcp_tools.yaml` への言及
    - `.claude/commands/add-mcp-tool.md` に `generate_mcp_artifacts.py --target all` の実行指示
    - `.claude/commands/add-mcp-tool.md` に `docs/mcp-tool-add-manual.md` への参照
    - `.claude/commands/add-mcp-tool.md` に `privileged` の言及（ユーザーに対話的に確認する指示）
    - `.claude/commands/add-mcp-tool.md` が 7 ステップ以上を含む（`**1.` ～ `**7.` のパターンが 7 回以上出現）
    - `wc -l .claude/commands/add-mcp-tool.md` が 80 行以上
    - `CLAUDE.md` に `## MCP Tool Catalog` セクションが追加されている
    - `CLAUDE.md` に `/add-mcp-tool` への参照が含まれる
    - `CLAUDE.md` に `docs/mcp-tool-add-manual.md` への参照が含まれる
    - `CLAUDE.md` に `generate_mcp_artifacts.py` への言及
    - `CLAUDE.md` の既存セクション (`## Project` / `## Technology Stack` / `## Conventions` / `## Architecture` / `## Chrome DevTools MCP` / `## Merge Safety Rules` / `## GSD Workflow Enforcement`) が **全て残っている**（`grep -q '## GSD Workflow Enforcement' CLAUDE.md` 等が全て真）
    - `CLAUDE.md` の `## Developer Profile` セクションが変更されていない (`grep -q 'Profile not yet configured' CLAUDE.md`)
    - `.planning/patterns.md` から旧文言 `sync-tool-list-to-js.py で自動更新可能` が削除されている
    - `.planning/patterns.md` に新パターン `MCP ツール single-source-of-truth 化` のエントリが追加されている
    - `.planning/patterns.md` に `tool-catalog-generated.js` への言及が含まれる
    - `python3 scripts/generate_mcp_artifacts.py --check` が exit 0（drift なし）
  </acceptance_criteria>
  <done>
    /add-mcp-tool スラッシュコマンドが使える状態で、CLAUDE.md にも運用ルールが追記済み。patterns.md の MCP・Tools セクションも Phase 30 後のスキーマに更新済み。Plan 05 の pre-commit hook と統合運用できる。
  </done>
</task>

</tasks>

<verification>
- docs/mcp-tools.md は「生成」ファイル（`scripts/generate_mcp_artifacts.py --check` で drift なし）
- docs/mcp-tool-add-manual.md は「手書き」ファイル（先頭 DO NOT EDIT なし）
- .claude/commands/add-mcp-tool.md は既存 create-adr.md と同じパターン
- CLAUDE.md は既存セクション全維持 + 新規 `## MCP Tool Catalog` セクション追加のみ
- .planning/patterns.md は MCP・Tools セクションが Phase 30 後のスキーマに更新済み
- Phase 30 完了後、全 pytest スイート（test_tool_registry / test_generate_mcp_artifacts / test_mcp_helper_generated / test_tool_catalog_js / test_install_hooks）が pass
</verification>

<success_criteria>
- 自動生成 docs/mcp-tools.md が 6 ツール詳細を含む
- 手書き docs/mcp-tool-add-manual.md が 120 行以上で 6 トピックを網羅
- /add-mcp-tool スラッシュコマンドが 7 ステップの指示書として機能する
- CLAUDE.md に Phase 30 運用ルールが追記されている
- .planning/patterns.md の MCP・Tools セクションが Phase 30 後のスキーマに更新されている
- Phase 30 全体で `generate_mcp_artifacts.py --check` が drift なし
</success_criteria>

<output>
After completion, create `.planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-06-SUMMARY.md`
</output>

<!--
Phase 完了後の ADR 作成（本 phase スコープ外）:

CLAUDE.md の「ADR Pattern Reference (GSD Integration)」節に定められた運用ルールに従い、
Phase 30 マージ前に以下を別途実施する（Plan 06 の範囲外）:

1. `/create-adr` を実行して Phase 30 の設計判断（YAML single-source + 決定論ジェネレータ +
   pre-commit drift 検知）を新規 ADR として docs/adr/ 配下に作成する
2. ADR 作成後、.planning/patterns.md の「MCP ツール single-source-of-truth 化」エントリと
   「iframe-rpc.js ツールカタログは独立 ES module 参照」エントリの `ADR 番号未定` 部分を
   実際の ADR 番号（例: `[0042](../docs/adr/0042-xxx.md)`）に差し替える
3. patterns.md への反映は手動（D-15 — 自動生成しない）。カテゴリは「MCP・Tools」のまま

新規 ADR 作成は CLAUDE.md で定められたマージ直前の運用フローの一部であり、本 Plan 06 の
タスク内には含めない（phase 完了時の振り返りの一環として実施する）。
-->
