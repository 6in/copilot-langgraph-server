---
phase: 30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-
plan: 03
type: execute
wave: 2
depends_on: ["30-01", "30-02"]
files_modified:
  - mcp_server/tools/mcp_helper_utils.py
  - mcp_server/tools/mcp_helper.py
  - tests/test_mcp_helper_generated.py
autonomous: true
requirements: [TBD]
must_haves:
  truths:
    - "`mcp_server/tools/mcp_helper_utils.py` に手書き基盤 (_call_tool / _clean_content / _INTERNAL_URL / _TIMEOUT) が分離されている"
    - "`mcp_server/tools/mcp_helper.py` が `scripts/generate_mcp_artifacts.py --target helper` から自動生成された結果に完全一致する"
    - "生成後 `mcp_helper.py` の 4 public 関数 (search / query_db / get_datetime / ping) が既存の public API と同等の挙動を返す（回帰テスト）"
    - "execute_python サンドボックスから `from mcp_helper import search, query_db, get_datetime, ping` が引き続き成功する（CodeAct agent の後方互換）"
    - "`scripts/generate_mcp_artifacts.py --check` を実行しても drift なし (exit 0) の状態"
  artifacts:
    - path: mcp_server/tools/mcp_helper_utils.py
      provides: "_call_tool + _clean_content の手書きモジュール"
      min_lines: 50
    - path: mcp_server/tools/mcp_helper.py
      provides: "自動生成された sandbox 用 Python ラッパー（4 関数）"
      contains: "from mcp_helper_utils import _call_tool, _clean_content"
    - path: tests/test_mcp_helper_generated.py
      provides: "生成 mcp_helper.py の挙動回帰テスト"
      min_lines: 100
  key_links:
    - from: mcp_server/tools/mcp_helper.py
      to: mcp_server/tools/mcp_helper_utils.py
      via: "from mcp_helper_utils import _call_tool, _clean_content"
      pattern: "from mcp_helper_utils import"
    - from: scripts/generate_mcp_artifacts.py
      to: mcp_server/tools/mcp_helper.py
      via: "--target helper で上書き生成"
      pattern: "HELPER_PATH"
---

<objective>
手書きロジックと自動生成コードを物理的に分離する。基盤（`_call_tool` / `_clean_content` / `_INTERNAL_URL` / `_TIMEOUT`）を `mcp_server/tools/mcp_helper_utils.py` に手書きモジュールとして切り出し、`mcp_server/tools/mcp_helper.py` は Plan 02 のジェネレータから自動生成したコードで上書きする。生成結果が既存 4 wrapper の挙動と同等であることを回帰テストで担保する。

Purpose: Phase 30 の中核。手書きと自動生成の境界が明確になり、以降の YAML 変更は generate_mcp_artifacts.py の再実行のみで反映できるようになる。
Output: 手書きモジュール + 再生成された mcp_helper.py + 回帰テスト。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/patterns.md
@docs/adr/0041-codeact-direct-execution-over-react.md
@config/mcp_tools.yaml
@mcp_server/tools/mcp_helper.py
@scripts/generate_mcp_artifacts.py
@mcp_server/tools/execute_python.py
@app/orchestrator/codeact_agent.py
@config/sandbox_allowlist.yaml
@.planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-01-SUMMARY.md
@.planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-02-SUMMARY.md

**現行 mcp_helper.py の手書き箇所（切り出し対象）:**

L11-15:
```python
import json
import urllib.request

_INTERNAL_URL = "http://localhost:8001/internal/call_tool"
_TIMEOUT = 55  # execute_python の 60 秒タイムアウトより短く
```

L18-37 `_call_tool()`:
```python
def _call_tool(name: str, args: dict | None = None) -> dict:
    """MCP ツールを呼び出して結果を dict で返す。エラー時は {"error": "..."} を返す。"""
    payload = json.dumps({"tool": name, "args": args or {}}).encode("utf-8")
    req = urllib.request.Request(_INTERNAL_URL, data=payload, headers={"Content-Type": "application/json"})
    try:
        with urllib.request.urlopen(req, timeout=_TIMEOUT) as resp:
            body = json.loads(resp.read().decode("utf-8"))
            return body.get("result", body)
    except urllib.error.HTTPError as e:
        error_body = e.read().decode("utf-8", errors="replace")
        return {"error": f"HTTP {e.code}: {error_body}"}
    except Exception as e:
        return {"error": str(e)}
```

L40-64 `_clean_content()`:
web_search 専用テキスト整形ヘルパー（skip_patterns リスト + max_lines=15 で最大 15 行抽出）

**config/sandbox_allowlist.yaml L43-45 は変更不要:**
`mcp_helper` と `urllib` が既に allowlist に入っており、`mcp_helper_utils` は `mcp_helper` からの相対 import 扱い（同じトップレベル import 名 `mcp_helper_utils` を明示的に allowlist に追加する必要がある — Task 1 で対応）。

**patch 対象モジュールの確定（Phase 30 Plan 03 Task 2 の重要事項）:**

自動生成された `mcp_helper.py` は先頭で `from mcp_helper_utils import _call_tool, _clean_content` の形式で import する。Python のモジュールシステムでは、この import 文で `_call_tool` / `_clean_content` シンボルは **呼び出し側（mcp_helper）モジュールの名前空間にバインドされる**。

したがって `mcp_helper.search()` や `mcp_helper.query_db()` の内部から呼ばれる `_call_tool` は `mcp_helper` モジュールスコープのシンボルであり、`mcp_helper_utils` モジュールスコープのシンボルではない。テストで差し替えるには:

- ✅ 正解: `patch.object(mcp_helper, "_call_tool", ...)` → mcp_helper スコープのシンボルを差し替え、実際に `search()` / `query_db()` / `ping()` / `get_datetime()` から呼ばれる `_call_tool` を確実にモック化できる
- ❌ 不正解: `patch.object(mcp_helper_utils, "_call_tool", ...)` → `mcp_helper` 側の import 済みシンボルは差し替わらず、実際のネットワーク呼び出しが走ってテストが不安定になる

`_clean_content` についても同じ理屈で `patch.object(mcp_helper, "_clean_content", ...)` が正解（mcp_helper 側にバインドされているシンボルを差し替える）。

Task 2 の全テストはこの方針で `patch.object(mcp_helper, ...)` に統一する。
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mcp_helper_utils.py を手書きモジュールとして切り出し + allowlist 追記</name>
  <files>mcp_server/tools/mcp_helper_utils.py, config/sandbox_allowlist.yaml</files>
  <read_first>
    - mcp_server/tools/mcp_helper.py（L1-65 の手書き部分 — 完全な切り出し元）
    - config/sandbox_allowlist.yaml L43-45（mcp_helper と urllib は allowlist 内）
    - mcp_server/tools/execute_python.py L130-132（PYTHONPATH = tools ディレクトリ追加ロジック）
  </read_first>
  <behavior>
    - `mcp_server/tools/mcp_helper_utils.py` が `_INTERNAL_URL` / `_TIMEOUT` / `_call_tool` / `_clean_content` の 4 シンボルを export する
    - 外部 import は `json` / `urllib.request` のみ（既存と同じ）
    - `config/sandbox_allowlist.yaml` の `allowed_modules` に `mcp_helper_utils` を追加する（mcp_helper.py から import されるため、sandbox 内でも import 解決できる必要がある）
  </behavior>
  <action>
**Step 1 — mcp_server/tools/mcp_helper_utils.py を新規作成:**

ファイル先頭コメントで「手書き」であることを明記する（自動生成ヘッダーは付けない）:

```python
"""mcp_helper_utils — 手書き基盤モジュール（scripts/generate_mcp_artifacts.py は生成しない）。

Phase 30 (D-02): 自動生成される mcp_helper.py から分離された手書きロジック。
- _call_tool: MCP サーバーの /internal/call_tool HTTP エンドポイントへ POST
- _clean_content: web_search 専用のテキスト前処理（ナビ・フッター除去）
- _INTERNAL_URL / _TIMEOUT: 接続設定定数

自動生成された mcp_helper.py は以下の 1 行で本モジュールを取り込む:

    from mcp_helper_utils import _call_tool, _clean_content

このモジュールを編集する場合は手動で行う（ジェネレータは触らない）。
関連: docs/mcp-tool-add-manual.md (Plan 06)
"""
from __future__ import annotations

import json
import urllib.request

_INTERNAL_URL = "http://localhost:8001/internal/call_tool"
_TIMEOUT = 55  # execute_python の 60 秒タイムアウトより短く


def _call_tool(name: str, args: dict | None = None) -> dict:
    """MCP ツールを呼び出して結果を dict で返す。

    エラー時は {"error": "..."} を返す（例外は投げない）。
    """
    payload = json.dumps({"tool": name, "args": args or {}}).encode("utf-8")
    req = urllib.request.Request(
        _INTERNAL_URL,
        data=payload,
        headers={"Content-Type": "application/json"},
    )
    try:
        with urllib.request.urlopen(req, timeout=_TIMEOUT) as resp:
            body = json.loads(resp.read().decode("utf-8"))
            return body.get("result", body)
    except urllib.error.HTTPError as e:
        error_body = e.read().decode("utf-8", errors="replace")
        return {"error": f"HTTP {e.code}: {error_body}"}
    except Exception as e:
        return {"error": str(e)}


def _clean_content(text: str, max_lines: int = 15) -> str:
    """Web ページのテキストからナビ・フッター・空行を除去して要約する。"""
    if not text:
        return ""
    lines = text.split("\n")
    cleaned = []
    skip_patterns = [
        "cookie", "copyright", "©", "all rights reserved",
        "プライバシー", "利用規約", "個人情報", "広告",
        "twitter", "facebook", "instagram", "youtube", "line",
        "ログイン", "新規登録", "会員登録",
    ]
    for line in lines:
        stripped = line.strip()
        if not stripped:
            continue
        if len(stripped) < 3:
            continue
        lower = stripped.lower()
        if any(p in lower for p in skip_patterns):
            continue
        cleaned.append(stripped)
        if len(cleaned) >= max_lines:
            break
    return "\n".join(cleaned)
```

この内容は現行 `mcp_server/tools/mcp_helper.py` の L1-64 を **逐語コピー**し、module docstring のみ差し替えたもの。挙動に一切の変更を加えないこと。

**Step 2 — config/sandbox_allowlist.yaml を更新:**

ファイル末尾の「MCP ヘルパー」セクション (L43-45) を以下に変更:

```yaml
  # MCP ヘルパー — サンドボックスから MCP ツールを呼び出す
  - mcp_helper
  - mcp_helper_utils
  - urllib
```

`mcp_helper_utils` を `mcp_helper` の直後に追加するだけ。他のエントリは一切変更しない。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && python3 -c "import sys; sys.path.insert(0, 'mcp_server/tools'); import mcp_helper_utils as u; assert u._INTERNAL_URL == 'http://localhost:8001/internal/call_tool'; assert u._TIMEOUT == 55; assert callable(u._call_tool); assert callable(u._clean_content); assert u._clean_content('foo\\nbar\\ncookie notice\\nbaz') == 'foo\\nbar\\nbaz'; print('OK')" && python3 -c "import yaml; a=yaml.safe_load(open('config/sandbox_allowlist.yaml'))['allowed_modules']; assert 'mcp_helper_utils' in a, 'missing in allowlist'; assert 'mcp_helper' in a; print('allowlist OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `mcp_server/tools/mcp_helper_utils.py` が存在する
    - `python3 -c "import sys; sys.path.insert(0, 'mcp_server/tools'); import mcp_helper_utils"` がエラーなし
    - `mcp_helper_utils._INTERNAL_URL == "http://localhost:8001/internal/call_tool"`
    - `mcp_helper_utils._TIMEOUT == 55`
    - `mcp_helper_utils._call_tool` が callable
    - `mcp_helper_utils._clean_content` が callable で `_clean_content("foo\\nbar\\ncookie notice\\nbaz") == "foo\\nbar\\nbaz"` （現行と同一挙動）
    - `grep -c '^  - mcp_helper_utils$' config/sandbox_allowlist.yaml` が **1** を返す
    - `grep -c '^  - mcp_helper$' config/sandbox_allowlist.yaml` が **1** を返す（回帰しない）
    - `grep -c '^  - urllib$' config/sandbox_allowlist.yaml` が **1** を返す
  </acceptance_criteria>
  <done>
    手書きモジュールが切り出され、allowlist にも `mcp_helper_utils` が追加されている。sandbox 内でも import 可能な状態。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scripts/generate_mcp_artifacts.py で mcp_helper.py を再生成 + 挙動回帰テスト</name>
  <files>mcp_server/tools/mcp_helper.py, tests/test_mcp_helper_generated.py</files>
  <read_first>
    - Task 1 で書き終えた mcp_server/tools/mcp_helper_utils.py
    - scripts/generate_mcp_artifacts.py (Plan 02 で作成したジェネレータ)
    - mcp_server/tools/mcp_helper.py (現行の手書き版 — 上書き前に挙動を把握)
    - app/orchestrator/codeact_agent.py (mcp_helper を import するコード — 破壊されないこと確認用)
  </read_first>
  <behavior>
    - `python3 scripts/generate_mcp_artifacts.py --target helper` の出力で `mcp_server/tools/mcp_helper.py` を完全上書きする
    - 生成ファイルの先頭が `# DO NOT EDIT` で始まる
    - 生成ファイルが `from mcp_helper_utils import _call_tool, _clean_content` を含む
    - 生成ファイルが 4 関数 (search / query_db / get_datetime / ping) を含む
    - 挙動回帰テスト: `_call_tool` をモックして、既存 mcp_helper.py と同じ入力 → 同じ戻り値を返すことを確認する
    - `scripts/generate_mcp_artifacts.py --check` が drift なし (exit 0) になる
  </behavior>
  <action>
**Step 1 — mcp_helper.py を再生成:**

```bash
cd /home/parallels/workspaces/copilot-langgraph
python3 scripts/generate_mcp_artifacts.py --target helper > mcp_server/tools/mcp_helper.py
```

これにより既存の手書き mcp_helper.py は Plan 02 ジェネレータの出力で **完全に上書き**される。生成後のファイルは:
- 先頭: `# DO NOT EDIT` ヘッダー 2 行
- module docstring
- `from mcp_helper_utils import _call_tool, _clean_content  # noqa: F401`
- 4 関数 (search, query_db, get_datetime, ping) — YAML の python_wrapper から生成
- claude_code / execute_python の wrapper は **含まない**（sandbox_exposed: false + python_wrapper 未定義のため）

**Step 2 — Python 構文確認と import 確認:**

```bash
python3 -c "import ast; ast.parse(open('mcp_server/tools/mcp_helper.py').read())"
python3 -c "import sys; sys.path.insert(0, 'mcp_server/tools'); import mcp_helper; assert callable(mcp_helper.search); assert callable(mcp_helper.query_db); assert callable(mcp_helper.get_datetime); assert callable(mcp_helper.ping); print('OK')"
```

**Step 3 — tests/test_mcp_helper_generated.py を新規作成:**

ジェネレータ出力の `mcp_helper.py` が既存 4 wrapper と挙動同等であることを `_call_tool` モックで検証するテストを追加する。

**patch 対象モジュールの確定方針:**

自動生成された `mcp_helper.py` は `from mcp_helper_utils import _call_tool, _clean_content` 形式で import する。この形式では、`_call_tool` / `_clean_content` のシンボルは **呼び出し側（mcp_helper）モジュールの名前空間にバインドされる**。したがってテストで差し替える際は `patch.object(mcp_helper, "_call_tool", ...)` を使うのが正解（mcp_helper_utils スコープを差し替えても mcp_helper 側には反映されない）。

以下の全テストで `patch.object(mcp_helper, "_call_tool", ...)` を採用する（`mcp_helper_utils` への patch は使わない）。

```python
"""Phase 30: 自動生成された mcp_helper.py の挙動回帰テスト。

Plan 03 で mcp_helper.py は scripts/generate_mcp_artifacts.py --target helper によって
生成されたコードに置き換わった。本テストは「Phase 30 前の手書き版と同一の挙動」を保証する。

_call_tool をモック化し、各 public 関数 (search / query_db / get_datetime / ping) が
期待通りの引数で _call_tool を呼び、期待通りの戻り値を返すことを検証する。

patch 対象は `mcp_helper` モジュール（import 済みシンボルを差し替えるため）。
`mcp_helper_utils` 側を patch しても mcp_helper 側の bindings は変わらない。
"""
from __future__ import annotations

import sys
from pathlib import Path
from unittest.mock import patch

import pytest

# mcp_helper は mcp_server/tools ディレクトリに存在する（sandbox と同じ import 方式）
TOOLS_DIR = Path(__file__).resolve().parent.parent / "mcp_server" / "tools"
sys.path.insert(0, str(TOOLS_DIR))

import mcp_helper           # noqa: E402
import mcp_helper_utils     # noqa: E402  （import 可能性確認のみに使用）


def test_mcp_helper_has_do_not_edit_header():
    """生成されたファイルの先頭が DO NOT EDIT ヘッダーであることを確認する。"""
    text = Path(TOOLS_DIR / "mcp_helper.py").read_text()
    assert text.startswith("# DO NOT EDIT"), f"先頭: {text[:100]}"


def test_mcp_helper_imports_from_utils():
    """自動生成ファイルが mcp_helper_utils から _call_tool と _clean_content を import する。"""
    text = Path(TOOLS_DIR / "mcp_helper.py").read_text()
    assert "from mcp_helper_utils import _call_tool, _clean_content" in text


def test_mcp_helper_has_four_public_functions():
    """search / query_db / get_datetime / ping の 4 関数が存在する。claude_code / execute_python は存在しない。"""
    assert callable(mcp_helper.search)
    assert callable(mcp_helper.query_db)
    assert callable(mcp_helper.get_datetime)
    assert callable(mcp_helper.ping)
    assert not hasattr(mcp_helper, "claude_code")
    assert not hasattr(mcp_helper, "execute_python")


def test_ping_calls_call_tool_with_no_args():
    """ping() は _call_tool("ping") を呼ぶ（引数なし passthrough）。"""
    with patch.object(mcp_helper, "_call_tool", return_value={"status": "ok"}) as m:
        result = mcp_helper.ping()
    m.assert_called_once_with("ping")
    assert result == {"status": "ok"}


def test_get_datetime_calls_call_tool_with_no_args():
    """get_datetime() は _call_tool("get_current_datetime") を呼ぶ（引数なし passthrough）。"""
    with patch.object(mcp_helper, "_call_tool", return_value={"date": "2026-04-18"}) as m:
        result = mcp_helper.get_datetime()
    m.assert_called_once_with("get_current_datetime")
    assert result == {"date": "2026-04-18"}


def test_query_db_maps_pool_to_pool_name():
    """query_db(sql, pool) は _call_tool("db_query", {"sql":..., "pool_name":...}) を呼ぶ。"""
    fake_rows = [{"id": 1}, {"id": 2}]
    with patch.object(mcp_helper, "_call_tool", return_value={"rows": fake_rows}) as m:
        result = mcp_helper.query_db("SELECT 1", pool="analytics")
    m.assert_called_once_with("db_query", {"sql": "SELECT 1", "pool_name": "analytics"})
    assert result == fake_rows


def test_query_db_default_pool():
    """query_db の pool デフォルト値が 'default' であることを確認する。"""
    with patch.object(mcp_helper, "_call_tool", return_value={"rows": []}) as m:
        mcp_helper.query_db("SELECT 1")
    m.assert_called_once_with("db_query", {"sql": "SELECT 1", "pool_name": "default"})


def test_query_db_error_passthrough():
    """query_db は _call_tool が error を返した場合 [{error: ...}] を返す。"""
    with patch.object(mcp_helper, "_call_tool", return_value={"error": "boom"}):
        result = mcp_helper.query_db("SELECT 1")
    assert result == [{"error": "boom"}]


def test_search_calls_call_tool_and_cleans_content():
    """search(query) は web_search を呼び、結果の content に _clean_content を適用する。

    _clean_content は `from mcp_helper_utils import ...` 経由で mcp_helper モジュール側に
    バインドされているため、patch.object(mcp_helper, "_clean_content", ...) で差し替える。
    """
    fake_result = {
        "results": [
            {"title": "T", "url": "U", "content": "abc\ncookie notice\ndef"},
        ],
    }
    with patch.object(mcp_helper, "_call_tool", return_value=fake_result) as m:
        results = mcp_helper.search("python 3.12")
    m.assert_called_once_with("web_search", {"query": "python 3.12"})
    assert isinstance(results, list)
    assert results[0]["title"] == "T"
    # cookie notice は skip_patterns に含まれるため除去される（実 _clean_content を通過）
    assert "cookie" not in results[0]["content"].lower()


def test_search_error_passthrough():
    """search は error 時に [{error: ...}] を返す。"""
    with patch.object(mcp_helper, "_call_tool", return_value={"error": "network"}):
        results = mcp_helper.search("foo")
    assert results == [{"error": "network"}]


def test_generator_check_mode_has_no_drift():
    """Plan 03 完了後、scripts/generate_mcp_artifacts.py の build_helper 出力が
    mcp_helper.py とバイト完全一致する（drift なし）。

    Plan 03 は helper のみ上書き — js と docs はまだ差分がある可能性があるため、
    本テストでは helper 単体の一致だけを直接検証する。
    """
    import importlib.util
    repo_root = Path(__file__).resolve().parent.parent
    spec = importlib.util.spec_from_file_location(
        "generate_mcp_artifacts", repo_root / "scripts" / "generate_mcp_artifacts.py"
    )
    gen = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(gen)
    expected = gen.build_helper(gen.load_tools())
    actual = (TOOLS_DIR / "mcp_helper.py").read_text()
    assert actual == expected, "mcp_helper.py is out of sync with generator output"
```

**注意:**
- テストが `sys.path.insert(0, str(TOOLS_DIR))` で mcp_server/tools を path に入れる理由は、sandbox と同じ import 方式を再現するため。
- `test_generator_check_mode_has_no_drift` は helper のみ（js / docs の整合は Plan 04 / 06 で担保）。
- 全テストで `patch.object(mcp_helper, "_call_tool", ...)` および `patch.object(mcp_helper, "_clean_content", ...)` を使う（mcp_helper_utils への patch は使わない）。これは `from mcp_helper_utils import _call_tool, _clean_content` 形式の import が呼び出し側（mcp_helper）の名前空間にシンボルをバインドするため、差し替えたいのは mcp_helper スコープであるため。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && python3 -c "import ast; ast.parse(open('mcp_server/tools/mcp_helper.py').read())" && head -2 mcp_server/tools/mcp_helper.py | grep -q 'DO NOT EDIT' && grep -q 'from mcp_helper_utils import' mcp_server/tools/mcp_helper.py && uv run pytest tests/test_mcp_helper_generated.py -v && uv run pytest tests/test_tool_registry.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 mcp_server/tools/mcp_helper.py` が `# DO NOT EDIT` を含む
    - `grep -q 'from mcp_helper_utils import _call_tool, _clean_content' mcp_server/tools/mcp_helper.py`
    - `grep -c '^def ' mcp_server/tools/mcp_helper.py` が **4** を返す（search / query_db / get_datetime / ping）
    - `grep 'def claude_code' mcp_server/tools/mcp_helper.py` が **何もヒットしない** (claude_code は sandbox_exposed: false で wrapper 生成されない)
    - `grep 'def execute_python' mcp_server/tools/mcp_helper.py` が **何もヒットしない**
    - `python3 -c "import sys; sys.path.insert(0, 'mcp_server/tools'); import mcp_helper; assert callable(mcp_helper.search) and callable(mcp_helper.query_db) and callable(mcp_helper.get_datetime) and callable(mcp_helper.ping)"` が成功
    - `uv run pytest tests/test_tool_registry.py tests/test_mcp_helper_generated.py -v` が **exit code 0**（全テスト pass、回帰なし）
    - 出力に `test_mcp_helper_has_do_not_edit_header PASSED` / `test_query_db_maps_pool_to_pool_name PASSED` / `test_search_calls_call_tool_and_cleans_content PASSED` / `test_generator_check_mode_has_no_drift PASSED` が含まれる
  </acceptance_criteria>
  <done>
    mcp_helper.py が自動生成バージョンに置き換わり、既存 public API との挙動同等性が回帰テストで担保されている。ToolRegistry テストも引き続き全 pass。
  </done>
</task>

</tasks>

<verification>
- `mcp_server/tools/mcp_helper.py` は「生成」ファイル (先頭に DO NOT EDIT)
- `mcp_server/tools/mcp_helper_utils.py` は「手書き」ファイル (ヘッダーに手書き明記)
- 両ファイルの import 境界: mcp_helper は mcp_helper_utils から `_call_tool` と `_clean_content` を import するのみ
- config/sandbox_allowlist.yaml に `mcp_helper_utils` が追加されている
- pytest 回帰テストが全 pass
- app/orchestrator/codeact_agent.py は本 Plan では **変更しない**（mcp_helper の public API は互換のため）
</verification>

<success_criteria>
- 手書きと自動生成の物理分離が完了
- mcp_helper.py の search / query_db / get_datetime / ping が既存挙動と同等（モックテストで担保）
- sandbox から `from mcp_helper import search, ...` が引き続き成功（allowlist 更新済み）
- scripts/generate_mcp_artifacts.py --check で helper 部分が drift なし
</success_criteria>

<output>
After completion, create `.planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-03-SUMMARY.md`
</output>
