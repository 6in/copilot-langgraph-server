---
phase: 30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-
plan: 05
type: execute
wave: 3
depends_on: ["30-02", "30-03", "30-04", "30-06"]
files_modified:
  - scripts/install-hooks.sh
  - tests/test_install_hooks.py
autonomous: true
requirements: [TBD]
must_haves:
  truths:
    - "`scripts/install-hooks.sh` を再実行すると `.git/hooks/pre-commit` が拡張版に差し替わる（ADR INDEX ロジック + MCP artifacts drift 検知の両方）"
    - "`config/mcp_tools.yaml` が staged で変更されていると pre-commit が `python3 scripts/generate_mcp_artifacts.py --check` を実行し、drift ありなら commit がブロックされる"
    - "生成対象ファイル (`mcp_server/tools/mcp_helper.py` / `static/js/tool-catalog-generated.js` / `docs/mcp-tools.md`) が staged で変更されているだけでも drift 検知が走る（手動編集を防ぐ）"
    - "既存の ADR INDEX 再生成ロジック (26-01-PLAN で導入) は破壊されない"
    - "pre-commit フック自体のテストが pytest で実行可能（hook スクリプトを temp git repo にインストールして挙動確認）"
  artifacts:
    - path: scripts/install-hooks.sh
      provides: "拡張版 pre-commit hook インストーラ"
      contains: "generate_mcp_artifacts.py --check"
    - path: tests/test_install_hooks.py
      provides: "hook インストーラと生成された hook の pytest 検証"
      min_lines: 80
  key_links:
    - from: .git/hooks/pre-commit
      to: scripts/generate_mcp_artifacts.py
      via: "--check 起動"
      pattern: "generate_mcp_artifacts.py --check"
---

<objective>
既存の `scripts/install-hooks.sh` を拡張し、pre-commit hook に「`config/mcp_tools.yaml` または MCP artifacts (`mcp_helper.py` / `tool-catalog-generated.js` / `mcp-tools.md`) が staged なら `generate_mcp_artifacts.py --check` を実行して drift を検知し、不一致なら commit をブロックする」ロジックを追加する。既存の ADR INDEX 再生成ロジック (Phase 26 由来) は一切破壊しない。

Purpose: Phase 30 の価値を持続させる自動化の柱。手動編集・YAML と生成物の乖離を CI 相当のチェックで防ぐ。
Output: 拡張版 install-hooks.sh + hook 挙動の pytest 単体テスト。

**実行順序の注意（Phase 30 の wave 設計）:** 本 Plan 05 は Plan 06 (wave 2) の完了後に実行される。Plan 06 で `docs/mcp-tools.md` が REPO_ROOT に生成・コミットされた後でないと、pre-commit hook の drift 対象である `docs/mcp-tools.md` が存在せず `[drift] missing` で全ての後続コミットがブロックされてしまう。したがって wave 順は `30-01/30-02 (w1)` → `30-03/30-04/30-06 (w2)` → `30-05 (w3)` となる。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/patterns.md
@scripts/install-hooks.sh
@scripts/generate_mcp_artifacts.py
@.planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-02-SUMMARY.md
@.planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-03-SUMMARY.md
@.planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-04-SUMMARY.md
@.planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-06-SUMMARY.md

**現行 scripts/install-hooks.sh (30 行):**

```bash
#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"
HOOK_PATH="$REPO_ROOT/.git/hooks/pre-commit"

cat > "$HOOK_PATH" <<'HOOK_EOF'
#!/usr/bin/env bash
set -e

REPO_ROOT="$(git rev-parse --show-toplevel)"

if git diff --cached --name-only | grep -qE '^docs/adr/[0-9]{4}-.*\.md$'; then
  echo "[pre-commit] docs/adr/ 変更を検知 → INDEX.md を再生成"
  python3 "$REPO_ROOT/scripts/generate_adr_index.py"
  git add "$REPO_ROOT/docs/adr/INDEX.md"
fi
HOOK_EOF

chmod +x "$HOOK_PATH"
echo "Installed pre-commit hook at: $HOOK_PATH"
```

**拡張後の pre-commit フック要件:**
1. ADR INDEX ロジックは現状維持（既存チェックを外さない）
2. 以下のどれかが staged なら drift 検知を実行:
   - `config/mcp_tools.yaml`
   - `mcp_server/tools/mcp_helper.py`
   - `static/js/tool-catalog-generated.js`
   - `docs/mcp-tools.md`
3. `python3 scripts/generate_mcp_artifacts.py --check` を実行し、非 0 終了なら commit をブロック
4. drift あり時のエラーメッセージは「python3 scripts/generate_mcp_artifacts.py --target all を実行して git add してから再コミットしてください」
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scripts/install-hooks.sh を拡張して MCP artifacts drift 検知を追加</name>
  <files>scripts/install-hooks.sh</files>
  <read_first>
    - scripts/install-hooks.sh 全文（現行 30 行）
    - scripts/generate_mcp_artifacts.py の main / check_all (Plan 02)
    - scripts/generate_adr_index.py（hook 内で既存呼び出しされているスクリプト）
  </read_first>
  <behavior>
    - install-hooks.sh を再実行すると `.git/hooks/pre-commit` が拡張版になる
    - 既存の ADR INDEX ロジックは削除せず、その直下に MCP drift 検知ロジックを追加する
    - MCP drift 検知は 4 パスのいずれかが `git diff --cached --name-only` に現れた場合のみ発火
    - `python3 scripts/generate_mcp_artifacts.py --check` の exit code が非 0 なら `exit 1` で commit をブロック
    - ブロック時に「修正コマンド」メッセージを stderr に表示
  </behavior>
  <action>
`scripts/install-hooks.sh` の hook 本体部分（heredoc `HOOK_EOF` 内）を拡張する。以下のように書き換える:

```bash
#!/usr/bin/env bash
# scripts/install-hooks.sh — リポジトリローカル git hook を .git/hooks/ にインストールする
#
# 用途: 新規クローン後に 1 回実行する。CLAUDE.md の運用ルールでも呼ばれる。
# 何をするか:
#   1. docs/adr/*.md の変更を検知したら scripts/generate_adr_index.py を実行し
#      生成された INDEX.md をステージングに追加する（Phase 26）
#   2. config/mcp_tools.yaml または MCP 生成対象ファイルの変更を検知したら
#      scripts/generate_mcp_artifacts.py --check で drift を検知し、差分があれば
#      commit をブロックする（Phase 30）

set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"
HOOK_PATH="$REPO_ROOT/.git/hooks/pre-commit"

cat > "$HOOK_PATH" <<'HOOK_EOF'
#!/usr/bin/env bash
# Auto-generated by scripts/install-hooks.sh — do not edit manually.
set -e

REPO_ROOT="$(git rev-parse --show-toplevel)"
STAGED_FILES="$(git diff --cached --name-only)"

# ─────────────────────────────────────────────
# Phase 26: ADR INDEX 自動再生成
# ─────────────────────────────────────────────
if echo "$STAGED_FILES" | grep -qE '^docs/adr/[0-9]{4}-.*\.md$'; then
  echo "[pre-commit] docs/adr/ 変更を検知 → INDEX.md を再生成"
  python3 "$REPO_ROOT/scripts/generate_adr_index.py"
  git add "$REPO_ROOT/docs/adr/INDEX.md"
fi

# ─────────────────────────────────────────────
# Phase 30: MCP ツールカタログ drift 検知
# ─────────────────────────────────────────────
MCP_PATHS_REGEX='^(config/mcp_tools\.yaml|mcp_server/tools/mcp_helper\.py|static/js/tool-catalog-generated\.js|docs/mcp-tools\.md)$'
if echo "$STAGED_FILES" | grep -qE "$MCP_PATHS_REGEX"; then
  echo "[pre-commit] MCP ツールカタログ関連ファイルの変更を検知 → drift 検査"
  if ! python3 "$REPO_ROOT/scripts/generate_mcp_artifacts.py" --check; then
    cat >&2 <<ERR_EOF

[pre-commit] MCP artifacts drift detected.

config/mcp_tools.yaml と生成ファイル (mcp_helper.py / tool-catalog-generated.js /
docs/mcp-tools.md) が不整合です。以下を実行して再生成・再ステージしてから commit してください:

    python3 scripts/generate_mcp_artifacts.py --target all
    git add mcp_server/tools/mcp_helper.py static/js/tool-catalog-generated.js docs/mcp-tools.md
    git commit

詳細は docs/mcp-tool-add-manual.md を参照してください。
ERR_EOF
    exit 1
  fi
fi
HOOK_EOF

chmod +x "$HOOK_PATH"
echo "Installed pre-commit hook at: $HOOK_PATH"
```

**変更点まとめ:**
- 先頭コメントに Phase 26 / Phase 30 の説明を追記
- hook 本体内で `STAGED_FILES` 変数を 1 回だけ計算し、複数の正規表現に対して再利用
- 既存 ADR ロジックは 1 行も改変せず、条件部分だけ `STAGED_FILES` 変数に切り替え（挙動同一）
- 新規 MCP drift 検査ブロックを ADR ブロックの直下に追加
- drift 検知でエラー時は stderr にヒアドキュメントでメッセージを出して `exit 1`

**Step — install-hooks.sh を再実行して hook を実機更新:**

```bash
cd /home/parallels/workspaces/copilot-langgraph
bash scripts/install-hooks.sh
```

再実行後、`.git/hooks/pre-commit` の内容が上記拡張版に置き換わったことを以下で確認:

```bash
grep 'generate_mcp_artifacts.py --check' .git/hooks/pre-commit
grep 'generate_adr_index.py' .git/hooks/pre-commit
```
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && bash scripts/install-hooks.sh && grep -q 'generate_mcp_artifacts.py --check' .git/hooks/pre-commit && grep -q 'generate_adr_index.py' .git/hooks/pre-commit && grep -q 'MCP_PATHS_REGEX' .git/hooks/pre-commit && grep -q 'config/mcp_tools\\.yaml' .git/hooks/pre-commit && grep -q 'docs/mcp-tools\\.md' .git/hooks/pre-commit && [ -x .git/hooks/pre-commit ] && echo "HOOK_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/install-hooks.sh` が exit 0 で終了
    - `.git/hooks/pre-commit` が実行可能（`[ -x .git/hooks/pre-commit ]` が真）
    - `.git/hooks/pre-commit` に `generate_adr_index.py` が含まれる（既存ロジック維持）
    - `.git/hooks/pre-commit` に `generate_mcp_artifacts.py --check` が含まれる（新規ロジック）
    - `.git/hooks/pre-commit` に `config/mcp_tools\.yaml` / `mcp_server/tools/mcp_helper\.py` / `static/js/tool-catalog-generated\.js` / `docs/mcp-tools\.md` の 4 パスが regex で含まれる
    - `.git/hooks/pre-commit` に `MCP_PATHS_REGEX` 変数が含まれる
    - `.git/hooks/pre-commit` に `MCP artifacts drift detected` の文言が含まれる
    - `.git/hooks/pre-commit` に `python3 scripts/generate_mcp_artifacts.py --target all` の修正コマンド案内が含まれる
    - `scripts/install-hooks.sh` の `HOOK_EOF` ヒアドキュメントが構文的に正しい（`bash -n scripts/install-hooks.sh` が exit 0）
  </acceptance_criteria>
  <done>
    拡張版 hook がインストールされ、ADR + MCP の両方の drift 検知が動作可能な状態。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/test_install_hooks.py で hook 挙動を pytest 検証</name>
  <files>tests/test_install_hooks.py</files>
  <read_first>
    - scripts/install-hooks.sh（Task 1 で拡張した版）
    - scripts/generate_mcp_artifacts.py
    - 現行 .git/hooks/pre-commit（Task 1 実行後の実機）
  </read_first>
  <behavior>
    - 一時ディレクトリに git repo を作り、リポジトリのスクリプト群をコピーして hook インストール → staged ファイルを変えて commit を試す統合テストを行う
    - テストは次の 4 シナリオを検証:
      1. install-hooks.sh 実行後、.git/hooks/pre-commit が作成される
      2. `config/mcp_tools.yaml` を staged すると hook が drift 検査を走らせる
      3. drift なし (YAML と生成ファイルが一致) なら commit が通る
      4. drift あり (YAML を変えて生成ファイルを古いまま) なら commit が exit 1 でブロックされる
  </behavior>
  <action>
`tests/test_install_hooks.py` を新規作成する。fixture は以下の 5 ステップで一時 git repo を構築する確定版の手順に従う:

1. `git init -b main` で tmp 直下に空 repo を作る
2. 必要ファイルを REPO_ROOT からコピー（`scripts/install-hooks.sh`, `scripts/generate_mcp_artifacts.py`, `scripts/generate_adr_index.py`, `config/mcp_tools.yaml`, `mcp_server/tools/mcp_helper_utils.py`, `.planning/adr-categories.yaml` 等）
3. `bash scripts/install-hooks.sh` を実行して hook をインストール
4. **temp_repo 内で** `python3 scripts/generate_mcp_artifacts.py --target all` を実行し、3 つの生成ファイル（`mcp_server/tools/mcp_helper.py` / `static/js/tool-catalog-generated.js` / `docs/mcp-tools.md`）を最新化する（Plan 05 実行時点では REPO_ROOT にこれらが揃っている前提だが、テストの独立性を保つため temp_repo 内で明示的に再生成する）
5. `git add -A && git commit -m initial --no-verify` で初期コミットを作成（`--no-verify` で pre-commit hook をスキップし、純粋な初期状態を作る）

この 5 ステップにより drift 検査が pass する「クリーンな」temp_repo が用意される。

**テスト全文:**

```python
"""Phase 30 Plan 05: install-hooks.sh による pre-commit hook の挙動テスト。

一時 git repo を作って hook をインストールし、staged ファイルを変えて git commit を
実行し、drift 検知が期待通り発火するかを統合的に検証する。
"""
from __future__ import annotations

import os
import shutil
import subprocess
from pathlib import Path

import pytest

REPO_ROOT = Path(__file__).resolve().parent.parent


def _run(cmd: list[str], cwd: Path, env: dict | None = None, check: bool = True) -> subprocess.CompletedProcess:
    """subprocess wrapper — utf-8 capture, optional non-raising mode."""
    merged_env = os.environ.copy()
    # コミット時の identity を fake 値で設定
    merged_env.setdefault("GIT_AUTHOR_NAME", "test")
    merged_env.setdefault("GIT_AUTHOR_EMAIL", "test@example.com")
    merged_env.setdefault("GIT_COMMITTER_NAME", "test")
    merged_env.setdefault("GIT_COMMITTER_EMAIL", "test@example.com")
    if env:
        merged_env.update(env)
    return subprocess.run(
        cmd,
        cwd=str(cwd),
        capture_output=True,
        text=True,
        env=merged_env,
        check=check,
    )


@pytest.fixture
def temp_repo(tmp_path):
    """最小限のリポジトリ構造を tmp_path に用意する（確定版の 5 ステップ手順）。

    ステップ:
      1. git init -b main
      2. 必要ファイルを REPO_ROOT からコピー
      3. bash scripts/install-hooks.sh で hook をインストール
      4. temp_repo 内で python3 scripts/generate_mcp_artifacts.py --target all を実行して 3 生成ファイルを最新化
      5. git add -A && git commit -m initial --no-verify で初期コミット
    """
    repo = tmp_path / "repo"
    repo.mkdir()

    # Step 1: git init
    _run(["git", "init", "-q", "-b", "main"], cwd=repo)

    # Step 2: 必要なファイルを REPO_ROOT からコピー
    for rel in [
        "scripts/install-hooks.sh",
        "scripts/generate_mcp_artifacts.py",
        "scripts/generate_adr_index.py",
        "config/mcp_tools.yaml",
        "mcp_server/tools/mcp_helper_utils.py",
        ".planning/adr-categories.yaml",
    ]:
        src = REPO_ROOT / rel
        if not src.exists():
            continue
        dst = repo / rel
        dst.parent.mkdir(parents=True, exist_ok=True)
        shutil.copy2(src, dst)

    # 実行権限
    (repo / "scripts" / "install-hooks.sh").chmod(0o755)

    # Step 3: hook をインストール
    _run(["bash", "scripts/install-hooks.sh"], cwd=repo)

    # Step 4: temp_repo 内で 3 生成ファイルを最新化
    _run(["python3", "scripts/generate_mcp_artifacts.py", "--target", "all"], cwd=repo)

    # Step 5: 初期コミット（--no-verify で hook をスキップし純粋な初期状態を作る）
    _run(["git", "add", "-A"], cwd=repo)
    _run(["git", "commit", "-q", "-m", "initial", "--no-verify"], cwd=repo)

    return repo


def test_hook_installed(temp_repo):
    hook = temp_repo / ".git" / "hooks" / "pre-commit"
    assert hook.exists()
    assert os.access(hook, os.X_OK)
    text = hook.read_text()
    assert "generate_mcp_artifacts.py --check" in text
    assert "generate_adr_index.py" in text


def test_no_drift_commit_passes(temp_repo):
    """YAML と生成ファイルが同期していれば commit が成功する。"""
    (temp_repo / "README_TEST.md").write_text("touch\n")
    _run(["git", "add", "README_TEST.md"], cwd=temp_repo)
    res = _run(["git", "commit", "-q", "-m", "add unrelated file"], cwd=temp_repo, check=False)
    assert res.returncode == 0, f"commit failed: stdout={res.stdout} stderr={res.stderr}"


def test_drift_commit_blocked(temp_repo):
    """YAML を変更したのに生成ファイルを再生成していない場合、hook が commit をブロックする。"""
    yaml_path = temp_repo / "config" / "mcp_tools.yaml"
    original = yaml_path.read_text()
    # description を変更 → 生成ファイルと内容が乖離する
    modified = original.replace(
        "MCP サーバーのヘルスチェック",
        "MCP サーバーのヘルスチェック (modified)",
        1,
    )
    assert modified != original, "replacement did not change YAML content"
    yaml_path.write_text(modified)

    _run(["git", "add", "config/mcp_tools.yaml"], cwd=temp_repo)
    res = _run(["git", "commit", "-m", "modify yaml without regen"], cwd=temp_repo, check=False)
    assert res.returncode != 0, f"commit should have been blocked, got exit 0: stdout={res.stdout}"
    combined = (res.stdout or "") + (res.stderr or "")
    assert "drift" in combined.lower() or "out of sync" in combined.lower()


def test_drift_fixed_by_regen(temp_repo):
    """drift を起こした後、generate_mcp_artifacts.py --target all で再生成すれば commit が通る。"""
    yaml_path = temp_repo / "config" / "mcp_tools.yaml"
    original = yaml_path.read_text()
    yaml_path.write_text(original.replace("MCP サーバーのヘルスチェック", "MCP サーバーのヘルスチェック v2", 1))

    _run(["git", "add", "config/mcp_tools.yaml"], cwd=temp_repo)
    # 一度 block されることを確認
    res = _run(["git", "commit", "-m", "drift"], cwd=temp_repo, check=False)
    assert res.returncode != 0

    # 再生成
    _run(["python3", "scripts/generate_mcp_artifacts.py", "--target", "all"], cwd=temp_repo)
    _run(["git", "add", "-A"], cwd=temp_repo)

    # 今度は通る
    res = _run(["git", "commit", "-m", "fix drift"], cwd=temp_repo, check=False)
    assert res.returncode == 0, f"regen commit failed: stdout={res.stdout} stderr={res.stderr}"
```
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && uv run pytest tests/test_install_hooks.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_install_hooks.py -v` が全テスト PASSED (4 テスト)
    - 出力に `test_hook_installed PASSED` が含まれる
    - 出力に `test_no_drift_commit_passes PASSED` が含まれる
    - 出力に `test_drift_commit_blocked PASSED` が含まれる
    - 出力に `test_drift_fixed_by_regen PASSED` が含まれる
    - 既存 pytest スイート (tests/test_tool_registry.py / tests/test_mcp_helper_generated.py / tests/test_tool_catalog_js.py / tests/test_generate_mcp_artifacts.py / tests/test_generate_adr_index.py) が全 pass (回帰なし)
    - `bash -n scripts/install-hooks.sh` が exit 0
  </acceptance_criteria>
  <done>
    drift 検知が実動作する hook がインストールされ、pytest で 4 シナリオ (install / 通常 commit / drift block / regen 後 commit) 全てが検証されている。
  </done>
</task>

</tasks>

<verification>
- install-hooks.sh の再実行が冪等（何度走らせても同じ内容の hook ができる）
- hook が ADR INDEX 再生成と MCP drift 検知の両方を実行する
- drift 検知は 4 パス (YAML + 3 生成ファイル) のいずれかが staged のときのみ発火
- 4 統合テストが pass
</verification>

<success_criteria>
- pre-commit で手動編集や YAML 更新忘れを自動ブロック
- Phase 26 の ADR INDEX ロジックは壊れない
- pytest 4 シナリオ全 pass
</success_criteria>

<output>
After completion, create `.planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-05-SUMMARY.md`
</output>
