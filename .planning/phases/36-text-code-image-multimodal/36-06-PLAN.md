---
phase: 36-text-code-image-multimodal
plan: 06
type: execute
wave: 5
depends_on:
  - 04
  - 05
files_modified:
  - frontend/src/api/client.ts
  - frontend/src/hooks/useModels.ts
  - frontend/src/components/Header.tsx
  - frontend/src/components/VisionWarningBanner.tsx
  - frontend/src/components/InputBar.tsx
  - frontend/src/components/MessageArea.tsx
  - frontend/src/components/ChatApp.tsx
  - frontend/src/hooks/useChat.ts
autonomous: true
requirements:
  - FIN-01
  - FIN-02
must_haves:
  truths:
    - "GET /api/models の結果を useModels hook がキャッシュし、Header の model select に vision 絵文字 🖼 を付けて表示する"
    - "Header の MODEL_OPTIONS ハードコードは API 失敗時の fallback としてのみ動く"
    - "InputBar に warningSlot が追加され、ChatApp から VisionWarningBanner を差し込める"
    - "vision 非対応モデル選択中に画像を staging したら VisionWarningBanner が表示される"
    - "VisionWarningBanner の CTA ボタンで vision 対応モデルへワンクリック切替できる"
    - "POST /api/chat の payload に attachments が載り、worker が per-turn 添付を受け取れる"
    - "D-06 4 ケースの staging クリア契約 (A キャンセル / B 技術失敗 → staging 残す、C graceful 成功 → clearAll、D × 削除 → DELETE) が useChat + useAttachments + ChatApp に配線されている"
    - "メッセージバブル内に AttachmentChipRow が描画され、履歴の添付チップ (画像サムネ / text pill) が表示される (D-21)"
  artifacts:
    - path: "frontend/src/hooks/useModels.ts"
      provides: "GET /api/models + 1h TTL in-memory cache + vision suggestion"
      contains: "useModels"
    - path: "frontend/src/components/VisionWarningBanner.tsx"
      provides: "D-17 banner with ワンクリックモデル切替 CTA"
      contains: "VisionWarningBanner"
    - path: "frontend/src/components/Header.tsx"
      provides: "vision 絵文字付き model select + fallback MODEL_OPTIONS"
      contains: "🖼"
    - path: "frontend/src/components/InputBar.tsx"
      provides: "warningSlot prop 追加 (previewSlot の上に配置)"
      contains: "warningSlot"
    - path: "frontend/src/components/MessageArea.tsx"
      provides: "bubble 内 AttachmentChipRow (D-21) + InputBar warningSlot forwarding"
      contains: "AttachmentChipRow"
    - path: "frontend/src/hooks/useChat.ts"
      provides: "sendMessage payload に attachments を載せる"
      contains: "attachments"
    - path: "frontend/src/components/ChatApp.tsx"
      provides: "useModels + VisionWarningBanner wiring + useChat に attachments を渡す"
      contains: "VisionWarningBanner"
  key_links:
    - from: "Header.tsx onModelChange"
      to: "ChatApp selectedModel state"
      via: "App.tsx props drilling (既存)"
      pattern: "onModelChange"
    - from: "useAttachments.getReadyItems"
      to: "useChat.sendMessage payload.attachments"
      via: "ChatApp から useChat options に渡す"
      pattern: "attachments:.*getReadyItems"
    - from: "VisionWarningBanner CTA"
      to: "onModelChange(suggestedModel)"
      via: "props callback"
      pattern: "onSwitchModel"
---

<objective>
Phase 36 frontend 最終段階。Plan 05 が完成させた staging + upload 経路の上に、vision 判定 + 送信 payload + 履歴 UI を重ねる。具体的には (1) `useModels` hook で `/api/models` を fetch + 1h TTL キャッシュ + vision 対応モデル提案、(2) `<VisionWarningBanner>` で D-17 の画像非対応警告 + ワンクリック切替、(3) Header.tsx の MODEL_OPTIONS ハードコードを `useModels` 由来に切替 (fallback として保持)、(4) InputBar に `warningSlot` prop を追加、(5) `useChat.sendMessage` で attachments を payload に載せ D-06 のクリア契約を実装、(6) MessageArea bubble に `AttachmentChipRow` で履歴添付表示 (D-21)。Plan 04 で用意した worker attachments 経路に信号を流す最後のコネクタ。

Purpose: FIN-02 の graceful fallback (D-17 UI + D-18 worker の 2 段) を UI 側で完成させる。履歴チップ (D-21) で「スレッド再オープン時も添付情報が確認できる」(Success Criteria 4) を満たす。
Output: 2 新規ファイル + 6 修正ファイル。テストは Plan 07 の integration check (chromium MCP) に委ねる。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/36-text-code-image-multimodal/36-CONTEXT.md
@.planning/phases/36-text-code-image-multimodal/36-RESEARCH.md
@.planning/phases/36-text-code-image-multimodal/36-PATTERNS.md
@.planning/phases/36-text-code-image-multimodal/36-UI-SPEC.md
@.planning/phases/36-text-code-image-multimodal/36-VALIDATION.md
@.planning/phases/36-text-code-image-multimodal/36-04-SUMMARY.md
@.planning/phases/36-text-code-image-multimodal/36-05-SUMMARY.md
@.planning/patterns.md
@docs/adr/INDEX.md
@CLAUDE.md

<interfaces>
<!-- Plan 02 で提供済み backend -->
- GET /api/models → [{id, name, vision, vision_limits, billing_multiplier}, ...]

<!-- Plan 03 で提供済み backend -->
- POST /api/chat body.attachments → arq worker に流れる
- GET /api/threads/{tid}/messages → messages[].additional_kwargs.attachments を含む
- GET /api/threads/{tid}/attachments/{name} → raw bytes (画像 <img src> で使う)

<!-- Plan 05 で提供済み frontend -->
From frontend/src/hooks/useAttachments.ts:
```typescript
export function useAttachments(threadId, selectedModelInfo?): {
  items, upload, removeItem, clearAll, getReadyItems,
  validationError, dismissValidationError,
};
```

From frontend/src/api/client.ts:
```typescript
export const postAttachments, deleteAttachment;
// 本 plan で追加: getModels
```

From frontend/src/components/InputBar.tsx:
```typescript
export interface InputBarProps {
  // ... 既存 ...
  toolbarSlot?: ReactNode;
  previewSlot?: ReactNode;
  copyAllSlot?: ReactNode;
  // 本 plan で追加: warningSlot?: ReactNode
}
```

<!-- 本 plan が提供 -->
```typescript
// frontend/src/hooks/useModels.ts (新規)
export function useModels(): {
  models: ModelInfo[] | null;
  isLoading: boolean;
  error: Error | null;
  suggestedVisionModel: string | null;  // vision:true 先頭候補
  modelById: (id: string) => ModelInfo | undefined;
};

// frontend/src/components/VisionWarningBanner.tsx (新規)
export function VisionWarningBanner(props: {
  currentModel: string;
  suggestedModel: string;
  onSwitchModel: () => void;
  onDismiss?: () => void;
}): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useModels hook + client.getModels + Header.tsx の model select を /api/models 由来に切替（fallback 保持）</name>
  <files>frontend/src/api/client.ts, frontend/src/hooks/useModels.ts, frontend/src/components/Header.tsx</files>
  <read_first>
    - /home/parallels/workspaces/copilot-langgraph/frontend/src/api/client.ts (Plan 05 修正後状態、apiFetch wrapper L31-37)
    - /home/parallels/workspaces/copilot-langgraph/frontend/src/components/Header.tsx (L1-277 全体、特に L20-43 MODEL_OPTIONS, L119-142 select 描画)
    - /home/parallels/workspaces/copilot-langgraph/frontend/src/types.ts (Plan 05 で追加済 ModelInfo 型)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-PATTERNS.md (§useModels L1067-1113, §Header.tsx L929-961)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-UI-SPEC.md (§Header Model Selector L421-439)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-RESEARCH.md (§Pattern 4 TTL cache)
  </read_first>
  <action>
    **Step 1 — `frontend/src/api/client.ts` に `getModels` を追加**:
    既存 imports に `ModelInfo` を追加:
    ```typescript
    import type {
      // ... 既存 ...
      AttachmentMeta,
      ModelInfo,   // Phase 36
    } from '../types';
    ```
    既存 `postAttachments` / `deleteAttachment` (Plan 05 で追加) の直後に:
    ```typescript
    // --- Phase 36: Models (D-16) ---
    export const getModels = () =>
      apiFetch<ModelInfo[]>(`${API_BASE}/api/models`);
    ```

    **Step 2 — `frontend/src/hooks/useModels.ts` を新規作成**:
    PATTERNS.md L1067-1113 の実装を採用。加えて `suggestedVisionModel` と `modelById` ヘルパーを追加する:
    ```typescript
    // frontend/src/hooks/useModels.ts
    // Phase 36 D-16: /api/models fetch + 1h TTL in-memory キャッシュ。
    // 単一モジュール変数 _cache で SPA 全体を通じて共有する (ユーザーごとに TTL リセット不要)。

    import { useEffect, useState, useMemo, useCallback } from 'react';
    import { getModels } from '../api/client';
    import type { ModelInfo } from '../types';

    let _cache: { at: number; models: ModelInfo[] } | null = null;
    const TTL_MS = 60 * 60 * 1000;  // 1h (D-16)

    export function useModels() {
      const [models, setModels] = useState<ModelInfo[] | null>(
        _cache && Date.now() - _cache.at < TTL_MS ? _cache.models : null,
      );
      const [isLoading, setIsLoading] = useState(false);
      const [error, setError] = useState<Error | null>(null);

      useEffect(() => {
        const now = Date.now();
        if (_cache && now - _cache.at < TTL_MS) {
          setModels(_cache.models);
          return;
        }
        let cancelled = false;
        setIsLoading(true);
        setError(null);
        getModels()
          .then((list) => {
            if (cancelled) return;
            _cache = { at: now, models: list };
            setModels(list);
          })
          .catch((e: Error) => {
            if (cancelled) return;
            setError(e);
            setModels(null);
          })
          .finally(() => {
            if (!cancelled) setIsLoading(false);
          });
        return () => { cancelled = true; };
      }, []);

      const modelById = useCallback((id: string): ModelInfo | undefined => {
        return (models ?? _cache?.models ?? []).find((m) => m.id === id);
      }, [models]);

      const suggestedVisionModel = useMemo(() => {
        const list = models ?? _cache?.models ?? [];
        const pick = list.find((m) => m.vision);
        return pick?.id ?? null;
      }, [models]);

      return { models, isLoading, error, modelById, suggestedVisionModel };
    }
    ```

    **Step 3 — `frontend/src/components/Header.tsx` を /api/models 由来に切替**:
    既存 imports に追加:
    ```tsx
    import { useMemo } from 'react';
    import { useAuth } from '../hooks/useAuth';
    import { getMe } from '../api/client';
    import { useModels } from '../hooks/useModels';
    import { ConfirmModal } from './ConfirmModal';
    import type { UserInfoResponse } from '../types';
    ```
    L20-43 の `MODEL_OPTIONS` 定数はそのまま残す（fallback）。`Header` コンポーネント内に追加:
    ```tsx
    export function Header({ selectedModel, onModelChange, theme, onToggleTheme, onBackToMenu, appName }: HeaderProps) {
      const { authState, performLogout } = useAuth();
      const [user, setUser] = useState<UserInfoResponse | null>(null);
      const [showLogoutConfirm, setShowLogoutConfirm] = useState(false);

      // Phase 36 D-16: API 由来のモデル一覧 (未取得時は null → fallback 使う)
      const { models: apiModels } = useModels();

      // 本番表示: API 由来優先、未取得時はハードコード fallback
      const useApiModels = apiModels !== null && apiModels.length > 0;

      // ... 既存 state / useEffect ...

      return (
        <header /* 既存 style */>
          {/* ... 既存 back button / title / appName ... */}
          <div className="header-desktop-actions" /* ... 既存 style ... */>
            <label htmlFor="model-select" className="header-model-label" /* ... */>モデル:</label>
            <select
              id="model-select"
              value={selectedModel}
              aria-label={_buildModelAriaLabel(selectedModel, apiModels)}
              onChange={(e) => onModelChange(e.target.value)}
              style={{
                padding: '0.25rem 0.5rem',
                borderRadius: 'var(--radius-sm)',
                border: 'none',
                fontSize: '0.85rem',
              }}
            >
              {useApiModels ? (
                apiModels!.map((m) => (
                  <option key={m.id} value={m.id}>
                    {m.name}{m.vision ? ' 🖼' : ''}
                  </option>
                ))
              ) : (
                MODEL_OPTIONS.map((group) => (
                  <optgroup key={group.group} label={group.group}>
                    {group.models.map((m) => (
                      <option key={m.value} value={m.value}>{m.label}</option>
                    ))}
                  </optgroup>
                ))
              )}
            </select>
            {/* ... 既存 avatar / logout / theme toggle ... */}
          </div>
          {/* ... 既存 hamburger ... */}
        </header>
      );
    }

    function _buildModelAriaLabel(currentId: string, models: ModelInfo[] | null): string {
      if (!models) return 'モデル選択';
      const m = models.find((x) => x.id === currentId);
      if (!m) return 'モデル選択';
      const visionPart = m.vision ? '、画像対応' : '、画像非対応';
      return `モデル選択（現在: ${m.name}${visionPart}）`;
    }
    ```

    **Step 4 — `bun tsc --noEmit` で型チェック**:
    - `useModels()` 呼び出しは既存コードに影響なし（新 hook）
    - Header の render は useApiModels 分岐で切替、既存 MODEL_OPTIONS 構造はそのまま残す
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph/frontend && bun tsc --noEmit 2>&1 | head -50</automated>
    <manual>
      1. `grep -n "export const getModels" frontend/src/api/client.ts` が 1 行返る
      2. `ls frontend/src/hooks/useModels.ts` が存在
      3. `grep -n "useModels\|apiModels\|🖼" frontend/src/components/Header.tsx` が 3 箇所以上
      4. 既存 `MODEL_OPTIONS` 定数 (fallback) が Header.tsx に残っている (`grep -c "MODEL_OPTIONS" frontend/src/components/Header.tsx` >= 2)
      5. useModels の TTL 定数が `60 * 60 * 1000` (1h) であることを確認
    </manual>
    <integration_check>Header の 🖼 絵文字表示 + /api/models TTL cache runtime 動作は Plan 07 Task 1 シナリオ 2/3 (model select に Claude Sonnet 4.6 🖼 が見える) で E2E 検証</integration_check>
  </verify>
  <done>
    - `grep -n "export const getModels" frontend/src/api/client.ts` が 1 行
    - `ls frontend/src/hooks/useModels.ts` が存在
    - `grep -n "useModels\|apiModels\|🖼" frontend/src/components/Header.tsx` が 3 箇所以上
    - `bun tsc --noEmit` が exit 0
    - 既存の Header.tsx の `MODEL_OPTIONS` 定数 (fallback) が削除されていない (L20-43)
  </done>
</task>

<task type="auto">
  <name>Task 2: VisionWarningBanner 新規作成 + InputBar に warningSlot prop 追加 + MessageArea で warningSlot を forward + ChatApp で VisionWarningBanner を配線</name>
  <files>frontend/src/components/VisionWarningBanner.tsx, frontend/src/components/InputBar.tsx, frontend/src/components/MessageArea.tsx, frontend/src/components/ChatApp.tsx</files>
  <read_first>
    - /home/parallels/workspaces/copilot-langgraph/frontend/src/components/InputBar.tsx (Plan 05 完了後状態、L85-101 slot 描画, L9-28 props interface)
    - /home/parallels/workspaces/copilot-langgraph/frontend/src/components/MessageArea.tsx (Plan 05 完了後状態、MessageAreaProps + InputBar 差し込み箇所)
    - /home/parallels/workspaces/copilot-langgraph/frontend/src/components/ChatApp.tsx (Plan 05 完了後状態、useAttachments 配線)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-UI-SPEC.md (§VisionWarningBanner L344-392, §InputBar 内の配置順序 L377-388, §Copywriting L216-224)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-PATTERNS.md (§VisionWarningBanner L767-824, §InputBar.tsx L831-862)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-CONTEXT.md (D-17 ワンクリック切替, D-18)
  </read_first>
  <action>
    **Step 1 — `frontend/src/components/VisionWarningBanner.tsx` を新規作成**:
    UI-SPEC L344-392 + PATTERNS.md L767-824 に従い:
    ```tsx
    // frontend/src/components/VisionWarningBanner.tsx
    // Phase 36 D-17: vision 非対応モデル選択中に画像を staging したときの警告 + ワンクリック切替 CTA。
    // 色は accent 系のみ (destructive 不使用、graceful 方針)。

    interface VisionWarningBannerProps {
      currentModel: string;
      suggestedModel: string;
      onSwitchModel: () => void;
      onDismiss?: () => void;
    }

    export function VisionWarningBanner({
      currentModel,
      suggestedModel,
      onSwitchModel,
      onDismiss,
    }: VisionWarningBannerProps) {
      return (
        <div
          role="status"
          aria-live="polite"
          style={{
            display: 'flex',
            alignItems: 'flex-start',
            gap: 'var(--space-3)',
            padding: 'var(--space-3) var(--space-4)',
            borderLeft: '3px solid var(--color-accent)',
            background: 'var(--color-accent-subtle)',
          }}
        >
          <span aria-hidden="true" style={{ fontSize: 20, flexShrink: 0 }}>⚠</span>
          <div style={{ flex: 1, minWidth: 0 }}>
            <div style={{
              fontSize: 14,
              fontWeight: 600,
              color: 'var(--color-text)',
              marginBottom: 4,
            }}>画像非対応モデル</div>
            <div style={{ fontSize: 14, color: 'var(--color-text)', lineHeight: 1.4 }}>
              現在のモデル（{currentModel}）は画像を読めません。
              <br />
              画像対応モデル（例: {suggestedModel}）に切り替えると画像付きで送信できます。
            </div>
            <button
              onClick={onSwitchModel}
              aria-label={`モデルを ${suggestedModel} に切り替える`}
              style={{
                marginTop: 'var(--space-2)',
                padding: 'var(--space-2) var(--space-3)',
                borderRadius: 'var(--radius-md)',
                border: 'none',
                background: 'var(--color-accent)',
                color: 'var(--color-accent-contrast)',
                fontWeight: 'bold',
                fontSize: 14,
                cursor: 'pointer',
              }}
            >{suggestedModel} に切り替える</button>
          </div>
          {onDismiss && (
            <button
              onClick={onDismiss}
              aria-label="この案内を閉じる"
              style={{
                border: 'none',
                background: 'transparent',
                color: 'var(--color-text-muted)',
                cursor: 'pointer',
                fontSize: 18,
                lineHeight: 1,
                padding: 'var(--space-1)',
                flexShrink: 0,
              }}
            >×</button>
          )}
        </div>
      );
    }
    ```

    **Step 2 — `frontend/src/components/InputBar.tsx` に `warningSlot` prop を追加**:
    既存 L9-28 の InputBarProps interface を拡張:
    ```tsx
    export interface InputBarProps {
      // 送信系 (既存)
      value: string;
      onChange: (next: string) => void;
      onSend: (text: string, contextMessages?: ContextMessage[]) => void;
      onCancel?: () => void;
      onAskMe?: () => void;

      // 状態 (既存)
      disabled?: boolean;
      isThinking?: boolean;
      placeholder?: string;

      // スロット (既存 + Phase 36 追加)
      toolbarSlot?: ReactNode;
      previewSlot?: ReactNode;
      copyAllSlot?: ReactNode;
      warningSlot?: ReactNode;  // Phase 36 D-17: previewSlot のさらに上に VisionWarningBanner を描画する named slot
    }
    ```
    L77-85 の component destructure 部に `warningSlot` を追加:
    ```tsx
    export function InputBar({
      value, onChange, onSend, onCancel, onAskMe,
      disabled, isThinking, placeholder,
      toolbarSlot, previewSlot, copyAllSlot,
      warningSlot,   // Phase 36
    }: InputBarProps) {
    ```
    L85-101 の slot 描画部の先頭（copyAllSlot の上）に warningSlot を挿入:
    ```tsx
      return (
        <div className="chat-input-bar" style={/* 既存 */}>
          {/* warningSlot: 空なら帯を出さない (Phase 36 D-17 の VisionWarningBanner) */}
          {warningSlot && (
            <div style={{ borderBottom: '1px solid var(--color-border)' }}>
              {warningSlot}
            </div>
          )}

          {/* copyAllSlot: 既存 L86-91 */}
          {copyAllSlot && (
            <div style={/* 既存 */}>
              {copyAllSlot}
            </div>
          )}

          {/* previewSlot: 既存 L93-101 */}
          {previewSlot && (
            <div className="chat-input-preview" style={/* 既存 */}>
              {previewSlot}
            </div>
          )}

          {/* ... 既存 chat-input-row ... */}
        </div>
      );
    ```

    **Step 3 — `frontend/src/components/MessageArea.tsx` の MessageAreaProps に `inputWarningSlot` を追加し InputBar に forward**:
    Plan 05 で追加した `inputToolbarSlot` / `inputPreviewSlot` と同じ pattern で `inputWarningSlot` を追加:
    ```tsx
    interface MessageAreaProps {
      // ... 既存 + Plan 05 追加の inputToolbarSlot / inputPreviewSlot ...
      inputWarningSlot?: React.ReactNode;   // Phase 36: VisionWarningBanner
    }

    // 既存の destructure に追加:
    export function MessageArea({
      messages, isThinking, /* ... */,
      inputToolbarSlot,
      inputPreviewSlot,
      inputWarningSlot,   // 追加
    }: MessageAreaProps) {
      // ... 既存 ...
      // InputBar 呼出し (Plan 05 で toolbarSlot / previewSlot 追加済) を拡張:
      <InputBar
        value={inputValue}
        onChange={setInputValue}
        onSend={handleSendWrapped}
        onCancel={onCancel}
        onAskMe={handleAskMeWrapped}
        isThinking={isThinking}
        disabled={disabled}
        placeholder={placeholder}
        copyAllSlot={messages.length > 0 ? <CopyAllButton messages={messages} /> : undefined}
        toolbarSlot={inputToolbarSlot}
        previewSlot={inputPreviewSlot}
        warningSlot={inputWarningSlot}   // Phase 36
      />
    ```

    **Step 4 — `frontend/src/components/ChatApp.tsx` で useModels + VisionWarningBanner を配線**:
    既存 imports (Plan 05) に追加:
    ```tsx
    import { useModels } from '../hooks/useModels';
    import { VisionWarningBanner } from './VisionWarningBanner';
    ```
    `export function ChatApp` 内で:
    ```tsx
    export function ChatApp({ selectedModel }: ChatAppProps) {
      // ... 既存 Plan 05 の hooks ...

      // Phase 36 D-16: model info for vision pre-validate + VisionWarningBanner
      const { modelById, suggestedVisionModel } = useModels();
      const currentModelInfo = modelById(selectedModel);

      // Plan 05 で null 固定だった selectedModelInfo を currentModelInfo に差し替え
      const attachments = useAttachments(activeThreadId, currentModelInfo);

      // ... 既存 useEffect (drop/paste) ...

      // Phase 36 D-17: vision 警告バナー
      // 画像 staging アイテムがあるかつモデルが vision=false なら表示
      const [warningDismissed, setWarningDismissed] = useState(false);
      // モデル変更時に dismiss 状態をリセット (新しい選択で再評価)
      useEffect(() => { setWarningDismissed(false); }, [selectedModel]);

      const hasStagedImages = attachments.items.some(
        (it) => ['png', 'jpg', 'jpeg', 'webp'].includes(it.ext.toLowerCase())
      );
      const showVisionWarning =
        hasStagedImages
        && currentModelInfo
        && currentModelInfo.vision === false
        && !!suggestedVisionModel
        && !warningDismissed;

      // ChatApp からモデル切替は App.tsx の selectedModel state が source of truth なので、
      // onSwitchModel を ChatApp props にできるよう拡張が必要 → ChatApp から App.tsx の
      // onModelChange にアクセスする手段を props で受ける。
      // ChatAppProps に追加: onModelChange?: (id: string) => void
    ```

    **ここで props の見直しが必要**:
    ChatApp.tsx の既存 ChatAppProps は `selectedModel: string` のみ。モデル切替 (`onModelChange`) は Header.tsx から App.tsx の setState 経由で行われる。VisionWarningBanner の「切り替える」ボタンが動くには、ChatApp が `onModelChange` を受け取る必要がある。`App.tsx` → `ChatApp` への props drilling を追加する:
    ```tsx
    // frontend/src/components/ChatApp.tsx — Props 拡張
    interface ChatAppProps {
      selectedModel: string;
      onModelChange: (model: string) => void;   // Phase 36: VisionWarningBanner 切替用
    }

    export function ChatApp({ selectedModel, onModelChange }: ChatAppProps) {
      // ...
      const handleSwitchModel = useCallback(() => {
        if (suggestedVisionModel) {
          onModelChange(suggestedVisionModel);
        }
      }, [suggestedVisionModel, onModelChange]);

      return (
        // ... 既存 JSX ...
        <MessageArea
          // ... 既存 + Plan 05 追加の props ...
          inputToolbarSlot={/* 既存 Plan 05 */}
          inputPreviewSlot={/* 既存 Plan 05 */}
          inputWarningSlot={
            showVisionWarning && suggestedVisionModel ? (
              <VisionWarningBanner
                currentModel={currentModelInfo!.name}
                suggestedModel={modelById(suggestedVisionModel)?.name ?? suggestedVisionModel}
                onSwitchModel={handleSwitchModel}
                onDismiss={() => setWarningDismissed(true)}
              />
            ) : undefined
          }
        />
      );
    }
    ```

    **Step 5 — `frontend/src/App.tsx` から ChatApp への props drilling**:
    App.tsx で ChatApp を呼び出している箇所を grep で特定 (`grep -n "<ChatApp" frontend/src/App.tsx`) し、`onModelChange={setSelectedModel}` 相当を渡す。既存 setSelectedModel は Header にも渡している state setter なので、単純に同じ setter を ChatApp にも渡せば OK。CanvasChatApp / GemChatApp / SuperChatApp / DebateChatApp なども **同じ hook を使う場合は同様に onModelChange を渡す** 必要があるが、CONTEXT.md Discretion "ChatApp 中心" なので、本 plan では **ChatApp.tsx のみ** 変更する（他アプリでの Vision banner 対応は v6.1 検討とし SUMMARY に明記）。

    実装例:
    ```tsx
    // frontend/src/App.tsx (既存のうち ChatApp 呼び出し箇所を修正):
    <Route path="/chat/:threadId?" element={
      <ChatApp selectedModel={selectedModel} onModelChange={setSelectedModel} />
    } />
    ```
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph/frontend && bun tsc --noEmit 2>&1 | head -50</automated>
    <manual>
      1. `ls frontend/src/components/VisionWarningBanner.tsx` が存在
      2. `grep -n "warningSlot" frontend/src/components/InputBar.tsx` が 3 箇所以上（props interface, destructure, JSX 描画）
      3. `grep -n "inputWarningSlot" frontend/src/components/MessageArea.tsx frontend/src/components/ChatApp.tsx` が 4 箇所以上
      4. `grep "destructive" frontend/src/components/VisionWarningBanner.tsx` が 0 行（UI-SPEC Checker #9 graceful 方針遵守）
      5. App.tsx の ChatApp 呼び出し箇所で `onModelChange={setSelectedModel}` が渡されていることを確認
    </manual>
    <integration_check>VisionWarningBanner の runtime 挙動 (gpt-4.1 選択 + 画像 staging → banner 表示、CTA ワンクリック切替 → Header select が claude-sonnet-4.6 に変化、dismiss 動作) は Plan 07 Task 1 シナリオ 3 + シナリオ D-17 で E2E 検証</integration_check>
  </verify>
  <done>
    - `ls frontend/src/components/VisionWarningBanner.tsx` が存在
    - `grep -n "warningSlot" frontend/src/components/InputBar.tsx` が 3 箇所以上（props interface, destructure, JSX 描画）
    - `grep -n "inputWarningSlot" frontend/src/components/MessageArea.tsx frontend/src/components/ChatApp.tsx` が 4 箇所以上
    - `grep -n "VisionWarningBanner\|suggestedVisionModel\|handleSwitchModel" frontend/src/components/ChatApp.tsx` が 3 箇所以上
    - `grep -n "onModelChange" frontend/src/App.tsx frontend/src/components/ChatApp.tsx` が 3 箇所以上（App.tsx での prop 追加 + ChatApp での使用）
    - `bun tsc --noEmit` が exit 0
    - UI-SPEC Checker #9: `grep "destructive" frontend/src/components/VisionWarningBanner.tsx` が 0 行（graceful 方針）
  </done>
</task>

<task type="auto">
  <name>Task 3a: useChat.sendMessage の payload 拡張 (attachments 配送 + D-06 clearAll 契約)</name>
  <files>frontend/src/hooks/useChat.ts</files>
  <read_first>
    - /home/parallels/workspaces/copilot-langgraph/frontend/src/hooks/useChat.ts (L1-401 全体、特に L132-170 sendMessage の postChat 呼出し、L268-330 es.onmessage の done 処理、L331-380 es.onerror fallback 分岐、L380 付近 cancelJob)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-PATTERNS.md (§useChat.ts L1118-1146)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-CONTEXT.md (D-06 4 ケース: A 明示キャンセル / B 技術エラー / C graceful fallback / D 手動削除, D-14)
    - /home/parallels/workspaces/copilot-langgraph/frontend/src/types.ts (Plan 05 で追加済 AttachmentMeta + ChatRequest.attachments)
  </read_first>
  <action>
    **Step 1 — `UseChatOptions` interface に attachments 供給 callback を追加**:
    `frontend/src/hooks/useChat.ts` 冒頭 import に `import type { AttachmentMeta } from '../types';` を追加。既存 `UseChatOptions` interface (L20-40 付近) に以下を追加:
    ```typescript
    interface UseChatOptions {
      activeThreadId: string | null;
      selectedModel: string;
      // ... 既存 ...
      // Phase 36 (D-14): 送信時の添付ファイル取得コールバック (ChatApp から useAttachments.getReadyItems を渡す)
      getReadyAttachments?: () => AttachmentMeta[];
      // 送信成功時のクリアコールバック (ChatApp から useAttachments.clearAll を渡す)
      onAttachmentsSent?: () => void;
    }
    ```
    `useChat` 関数の destructure にも `getReadyAttachments, onAttachmentsSent` を追加。

    **Step 2 — `sendMessage` の postChat body に attachments を載せる**:
    L132-170 付近の postChat 呼び出し直前で `readyAttachments` を取得し、body に挿入:
    ```typescript
    const sendMessage = useCallback(async (text, threadId, contextMessages) => {
      if (!text.trim() || isThinking) return;
      const resolvedThreadId = threadId ?? activeThreadId;
      if (!resolvedThreadId) return;

      setMessages((prev) => [...prev, { role: 'user', content: text }]);
      setStreamPreview('');
      setIsThinking(true);

      // Phase 36 (D-14): 添付取得 (getReadyItems が done 状態のみを返す)
      const readyAttachments = getReadyAttachments?.() ?? [];

      try {
        const { job_id } = await postChat({
          message: text,
          thread_id: resolvedThreadId,
          model: selectedModel,
          // ... 既存 フィールド ...
          // Phase 36:
          ...(readyAttachments.length > 0 ? { attachments: readyAttachments } : {}),
        });
        // ... 既存 handleResult / SSE 等 ...
      }
    }, [/* ... 既存 deps ..., */ getReadyAttachments, onAttachmentsSent]);
    ```

    **Step 3 — D-06 クリア契約の実装 (4 ケース網羅)**:
    D-06 の 4 ケース:
    - **ケース A (ユーザー明示キャンセル)**: `cancelJob` 経路では staging を残す → `onAttachmentsSent?.()` を呼ばない
    - **ケース B (技術失敗)**: postChat 例外 / es.onerror fallback も failure → staging 残す (失敗で clear すると再送できない)。**ただし** 「サーバー側 folder にもアップロード済みだが送信失敗で不可視になる」という GC 観点では、B の扱いは staging は残す方針を採用 (useAttachments.removeItem でユーザーが手動で消すと DELETE が走るのでサーバー側 folder も整理される)
    - **ケース C (graceful fallback / vision 非対応で text 応答)**: 送信自体は成功扱い → staging clear する (`es.onmessage` の done 分岐で `onAttachmentsSent?.()` 呼び出し)
    - **ケース D (ユーザー手動 × 削除)**: useAttachments.removeItem 経由で clear — useChat 側は関与しない

    `es.onmessage` の `event.status === 'done'` ブロック (L267-324 付近) で成功判定後、`onAttachmentsSent?.()` を呼ぶ:
    ```typescript
    } else if (event.status === 'done') {
      setCurrentTool(null);
      es.close();
      const result = await getJob(job_id);
      if (result.result) {
        // ... 既存 handleResult ...
      }
      setIsThinking(false);
      await refreshThreads?.();
      // Phase 36 D-06 ケース C: 成功扱いで staging をクリア
      onAttachmentsSent?.();
    }
    ```
    `es.onerror` fallback polling の done 分岐でも同じ呼び出しを追加:
    ```typescript
    if (job.status === 'done' && job.result) {
      // ... 既存 ...
      setIsThinking(false);
      await refreshThreads?.();
      onAttachmentsSent?.();  // Phase 36 D-06 ケース C
    }
    ```
    **ケース A (cancelJob)**: L380 付近の `cancelJob` 実装では **絶対に** `onAttachmentsSent` を呼ばない。キャンセル時は staging を残すのが D-06 仕様。
    **ケース B (fallback polling も失敗)**: error 分岐では呼ばない。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph/frontend && bun tsc --noEmit 2>&1 | head -50</automated>
    <manual>
      1. `grep -n "getReadyAttachments\|onAttachmentsSent" frontend/src/hooks/useChat.ts` が 4 箇所以上（interface, destructure, 成功 2 箇所 call）
      2. `grep -n "attachments: readyAttachments\|attachments: getReadyAttachments" frontend/src/hooks/useChat.ts` が 1 箇所（postChat body に載る）
      3. `cancelJob` の実装内で `onAttachmentsSent` が呼ばれていない (D-06 ケース A) — `grep -B2 -A20 "cancelJob" frontend/src/hooks/useChat.ts | grep "onAttachmentsSent"` が 0 行
      4. `es.onerror` fallback polling の done 分岐でも `onAttachmentsSent?.()` が呼ばれている
      5. `readyAttachments.length > 0` のときのみ body に attachments を足す条件分岐が入っている (空なら付けない)
    </manual>
    <integration_check>useChat payload の runtime 検証 (POST /api/chat body.attachments = D-14 dict, worker が job.attachments 受領) は Plan 07 Task 1 シナリオ 1/2 + docker compose logs worker で確認</integration_check>
  </verify>
  <done>
    - `grep -n "getReadyAttachments\|onAttachmentsSent" frontend/src/hooks/useChat.ts` が 4 箇所以上
    - `grep -n "attachments: readyAttachments\|\.\.\.readyAttachments" frontend/src/hooks/useChat.ts` が 1 箇所以上
    - `bun tsc --noEmit` が exit 0
    - D-06 4 ケースすべての実装箇所 (or 呼ばない箇所) がコードに明示されている
  </done>
</task>

<task type="auto">
  <name>Task 3b: types.ts の ChatMessage 拡張（additional_kwargs）と MessageArea bubble 内 AttachmentChipRow 描画 (D-21)</name>
  <files>frontend/src/types.ts, frontend/src/components/MessageArea.tsx</files>
  <read_first>
    - /home/parallels/workspaces/copilot-langgraph/frontend/src/types.ts (Plan 05 修正後、既存 ChatMessage + AttachmentMeta)
    - /home/parallels/workspaces/copilot-langgraph/frontend/src/components/MessageArea.tsx (Plan 05 後、L223-325 user/ai bubble 描画、特に chatscope `<Message>` の type:'text' vs 'custom')
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-UI-SPEC.md (§Bubble 内 AttachmentChipRow L394-419)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-PATTERNS.md (§MessageArea.tsx L866-925)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-CONTEXT.md (D-21 bubble 内チップ表示, D-22/D-23)
  </read_first>
  <action>
    **Step 1 — `frontend/src/types.ts` 確認** (Plan 05 で ChatMessage.additional_kwargs はすでに追加済み):
    Plan 05 Task 1 で `ChatMessage.additional_kwargs?: { attachments?: AttachmentMeta[] }` が既に足されていることを確認。もし足されていなければこの Task で追加（Plan 05 Task 1 の Step 1 を参照）。

    **Step 2 — `frontend/src/components/MessageArea.tsx` に `AttachmentChipRow` component を追加 (D-21)**:
    MessageArea.tsx module レベル (既存 CopyButton / CopyAllButton と同じ場所) に以下を追加:
    ```tsx
    import type { AttachmentMeta, /* ... 既存 ... */ } from '../types';

    const API_BASE = (import.meta.env.VITE_APP_BASE ?? '').replace(/\/$/, '');
    const IMAGE_EXTS_SET = new Set(['png', 'jpg', 'jpeg', 'webp']);
    const HISTORY_THUMB = 48;

    function AttachmentChipRow({ attachments, threadId }: {
      attachments: AttachmentMeta[];
      threadId: string | null;
    }) {
      if (!attachments || attachments.length === 0) return null;
      return (
        <div
          role="group"
          aria-label={`添付ファイル ${attachments.length} 件`}
          style={{
            display: 'flex',
            flexWrap: 'wrap',
            gap: 'var(--space-2)',
            marginTop: 'var(--space-2)',
            paddingTop: 'var(--space-2)',
            borderTop: '1px solid var(--color-border)',
          }}
        >
          {attachments.map((a) => {
            const ext = (a.ext || '').toLowerCase();
            const isImage = IMAGE_EXTS_SET.has(ext);
            if (isImage && threadId) {
              return (
                <img
                  key={a.storage_name}
                  src={`${API_BASE}/api/threads/${encodeURIComponent(threadId)}/attachments/${encodeURIComponent(a.storage_name)}`}
                  alt={a.name}
                  width={HISTORY_THUMB}
                  height={HISTORY_THUMB}
                  title={`${a.name}（${_formatBytes(a.size)}）`}
                  style={{
                    borderRadius: 'var(--radius-md)',
                    border: '1px solid var(--color-border)',
                    objectFit: 'cover',
                  }}
                />
              );
            }
            return (
              <span
                key={a.storage_name}
                title={`${a.name}（${_formatBytes(a.size)}）`}
                style={{
                  display: 'inline-flex',
                  alignItems: 'center',
                  gap: 4,
                  padding: '2px 8px',
                  borderRadius: 'var(--radius-full)',
                  border: '1px solid var(--color-border)',
                  background: 'var(--color-surface-elevated)',
                  fontSize: 12,
                  color: 'var(--color-text-muted)',
                }}
              >
                📄 {a.name}
              </span>
            );
          })}
        </div>
      );
    }

    function _formatBytes(bytes: number): string {
      if (bytes < 1024) return `${bytes} B`;
      if (bytes < 1024 * 1024) return `${(bytes / 1024).toFixed(1)} KB`;
      return `${(bytes / 1024 / 1024).toFixed(1)} MB`;
    }
    ```

    **Step 3 — chatscope `<Message>` type を 'text' → 'custom' に変更して AttachmentChipRow を差し込む**:
    user bubble (L223-260 付近) を以下のように書き換え (`type: 'custom'` + `<Message.CustomContent>` 内で content + chip row を並列描画):
    ```tsx
    if (msg.role === 'user') {
      return (
        <Message
          key={index}
          model={{
            direction: 'outgoing',
            position: 'single',
            type: 'custom',
          }}
          style={/* 既存 enableResend ? ... : undefined */}
        >
          <Message.CustomContent>
            <div style={{ whiteSpace: 'pre-wrap' }}>{msg.content}</div>
            {msg.additional_kwargs?.attachments && msg.additional_kwargs.attachments.length > 0 && (
              <AttachmentChipRow
                attachments={msg.additional_kwargs.attachments}
                threadId={activeThreadId ?? null}
              />
            )}
          </Message.CustomContent>
          <Message.Footer style={/* 既存 */}>
            {/* 既存 */}
          </Message.Footer>
        </Message>
      );
    }
    ```
    AI bubble (L263-325 付近) も同様に MarkdownMessage の後に AttachmentChipRow を挿入:
    ```tsx
    <Message.CustomContent>
      <div style={/* 既存 senderName 装飾 */}>
        {/* 既存 senderName badge */}
        <MarkdownMessage content={msg.content} />
        {msg.additional_kwargs?.attachments && msg.additional_kwargs.attachments.length > 0 && (
          <AttachmentChipRow
            attachments={msg.additional_kwargs.attachments}
            threadId={activeThreadId ?? null}
          />
        )}
      </div>
    </Message.CustomContent>
    ```

    **Step 4 — MessageAreaProps に `activeThreadId` 追加**:
    MessageArea の props interface (L24-37 付近) に `activeThreadId?: string | null` を追加し、function destructure でも受け取る。AttachmentChipRow に `threadId={activeThreadId ?? null}` として渡す。

    **Step 5 — 型 / outgoing 装飾の保持確認**:
    `type: 'text'` → `type: 'custom'` に変更すると chatscope のデフォルト装飾 (outgoing の青背景など) が外れる可能性がある。CSS class `cs-message--outgoing` の装飾を `Message.CustomContent` 内の div でも再現するか、または chatscope の生成する outer wrapper の class で装飾が維持されるかを確認。崩れる場合は user bubble は `type: 'text'` に戻し chip row を Message の外 (別途 Message として) に置く代替案を採る (SUMMARY に記録)。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph/frontend && bun tsc --noEmit 2>&1 | head -50</automated>
    <manual>
      1. `grep -n "function AttachmentChipRow\|<AttachmentChipRow" frontend/src/components/MessageArea.tsx` が 3 箇所以上（定義 + user bubble + ai bubble）
      2. `grep -n "activeThreadId" frontend/src/components/MessageArea.tsx` が 2 箇所以上（props interface + 使用箇所）
      3. `grep -n "msg.additional_kwargs?.attachments" frontend/src/components/MessageArea.tsx` が 2 箇所以上（user + ai bubble）
      4. `grep -n "type: 'custom'" frontend/src/components/MessageArea.tsx` が user bubble 分岐で 1 箇所以上
      5. `grep -n "additional_kwargs" frontend/src/types.ts` が 1 行（ChatMessage 内）
    </manual>
    <integration_check>bubble 内 AttachmentChipRow の実際の見た目 (48x48 画像サムネ / 📄 pill / F5 リロード後の復元表示) は Plan 07 Task 1 シナリオ 4 (D-20 履歴復元) で E2E 検証</integration_check>
  </verify>
  <done>
    - `grep -n "function AttachmentChipRow\|<AttachmentChipRow" frontend/src/components/MessageArea.tsx` が 3 箇所以上
    - `grep -n "activeThreadId" frontend/src/components/MessageArea.tsx` が 2 箇所以上
    - `bun tsc --noEmit` が exit 0
    - user / ai 両 bubble で `msg.additional_kwargs?.attachments` 条件分岐が入っている
  </done>
</task>

<task type="auto">
  <name>Task 3c: ChatApp.tsx の staging clear タイミング統合 (D-06 4 ケース) + useChat / banner / chips / activeThreadId 配線</name>
  <files>frontend/src/components/ChatApp.tsx</files>
  <read_first>
    - /home/parallels/workspaces/copilot-langgraph/frontend/src/components/ChatApp.tsx (Plan 05 + Task 2 後状態、useAttachments + useModels + handleSwitchModel + MessageArea 呼び出し)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-CONTEXT.md (D-06 4 ケース)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-UI-SPEC.md (§Interaction Contract)
  </read_first>
  <action>
    **Step 1 — ChatApp から useChat に attachments callbacks を渡す**:
    既存の useChat 呼び出し (Plan 05 Task 3 で書いた) に Task 3a の 2 つの option を追加:
    ```tsx
    const { isThinking, streamPreview, sendMessage, cancelJob, pendingQuestion, handleQuestionSubmit } = useChat({
      activeThreadId,
      selectedModel,
      setMessages,
      refreshThreads,
      onCanvasResponse: (app) => setCanvasApp(app),
      // Phase 36 Task 3a:
      getReadyAttachments: attachments.getReadyItems,
      onAttachmentsSent: attachments.clearAll,
    });
    ```

    **Step 2 — `handleSend` から Plan 05 で書いた `attachments.clearAll()` を削除**:
    Plan 05 Task 3 で handleSend に直接書いた `attachments.clearAll()` は useChat の `onAttachmentsSent` 経由で呼ばれるため重複。削除する:
    ```tsx
    const handleSend = async (text: string) => {
      let threadId = activeThreadId;
      if (!threadId) {
        threadId = await createNewThread();
        navigate(`/chat/${threadId}`, { replace: true });
      }
      await sendMessage(text, threadId);
      // Plan 05 で書いた `attachments.clearAll()` はここから削除。
      // useChat の onAttachmentsSent (D-06 ケース C success) で clear される。
      await refreshThreads();
    };
    ```

    **Step 3 — D-06 4 ケース実装マップをコメントで明示**:
    ChatApp.tsx の冒頭または handleSend 近辺に以下の doc コメントを残す:
    ```tsx
    // Phase 36 D-06 staging クリア契約 (4 ケース):
    // A. ユーザー明示キャンセル (cancelJob 経路) -> staging 残す (useChat.cancelJob は onAttachmentsSent を呼ばない)
    // B. 技術失敗 (postChat 例外 / SSE error fallback) -> staging 残す (ユーザーが再送できる)
    // C. graceful fallback (vision 非対応での text 応答含む、送信は成功扱い) -> useChat.onAttachmentsSent -> attachments.clearAll()
    // D. ユーザー手動 × 削除 -> useAttachments.removeItem -> DELETE /api/threads/{tid}/attachments/{name}
    ```

    **Step 4 — MessageArea へ `activeThreadId` を渡す (Task 3b で追加した prop)**:
    MessageArea 呼び出し箇所を更新:
    ```tsx
    <MessageArea
      messages={messages}
      isThinking={isThinking}
      streamPreview={streamPreview}
      onSend={handleSend}
      onCancel={cancelJob}
      pendingQuestion={pendingQuestion}
      onQuestionSubmit={handleQuestionSubmit}
      activeThreadId={activeThreadId}  // Phase 36 Task 3b
      inputToolbarSlot={/* Plan 05 */}
      inputPreviewSlot={/* Plan 05 */}
      inputWarningSlot={/* Task 2 VisionWarningBanner */}
    />
    ```

    **Step 5 — Task 2 で追加した VisionWarningBanner の inputWarningSlot も引き続き配線**:
    Task 2 で追加した VisionWarningBanner は showVisionWarning のタイミングで表示される。Task 3c では Task 2 の `inputWarningSlot` 配線をそのまま残す (regression なし確認のみ)。

    **Step 6 — banner dismiss state のモデル変更時リセット**:
    Task 2 で入れた `useEffect(() => setWarningDismissed(false), [selectedModel])` が正しく動いていることを確認 (handleSwitchModel でモデル切替→banner 消える→再度画像 staging で再表示の flow)。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph/frontend && bun tsc --noEmit 2>&1 | head -50</automated>
    <manual>
      1. `grep -n "getReadyAttachments:\|onAttachmentsSent:" frontend/src/components/ChatApp.tsx` が 2 箇所以上（useChat options 内）
      2. `grep -n "attachments.clearAll" frontend/src/components/ChatApp.tsx` が **1 箇所のみ**（useChat options 経由、handleSend 内から消えている）
      3. `grep -n "activeThreadId={activeThreadId}" frontend/src/components/ChatApp.tsx` が 1 箇所（MessageArea 呼び出し）
      4. D-06 4 ケースの doc コメント (A/B/C/D) が ChatApp.tsx に存在する
      5. `grep -n "inputWarningSlot\|inputToolbarSlot\|inputPreviewSlot" frontend/src/components/ChatApp.tsx` が 3 箇所以上（Plan 05 + Task 2 + Task 3b の 3 スロット全て MessageArea に渡されている）
    </manual>
    <integration_check>D-06 4 ケース実機動作 (A キャンセル後 staging 残る / B 技術エラー後 staging 残る / C 成功後 staging 消える / D × 削除で DELETE 204) は Plan 07 Task 1 シナリオ 1 + シナリオ 9 (D-06 ケース D DELETE) で E2E 検証。banner 動作は Plan 07 シナリオ 3/D-17</integration_check>
  </verify>
  <done>
    - `grep -n "getReadyAttachments:\|onAttachmentsSent:" frontend/src/components/ChatApp.tsx` が 2 箇所以上
    - `grep -n "attachments.clearAll" frontend/src/components/ChatApp.tsx` が 1 箇所のみ (重複削除済)
    - `grep -n "activeThreadId={activeThreadId}" frontend/src/components/ChatApp.tsx` が 1 箇所
    - D-06 4 ケース doc コメント存在
    - `bun tsc --noEmit` が exit 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ChatApp → useChat via getReadyAttachments callback | useAttachments の done 状態アイテムのみ送信 |
| useChat → POST /api/chat attachments | 既存 JWT cookie 経由、Plan 03 でサーバー側バリデーション済 |
| MessageArea bubble → GET /api/threads/{tid}/attachments/{name} | 画像サムネ URL はサーバー側で所有者 filter 済 |
| Header model select → onModelChange | 既存 state setter 経由、URL / DB には保存しない (mode switch と同じ) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-06-01 | Tampering (UI bypass) | vision 非対応モデル選択中に画像送信 | medium | mitigate | VisionWarningBanner で事前通知 (D-17) + worker 側で画像 drop + SystemMessage 警告 (D-18 defense-in-depth, Plan 04)。成功扱いで止まらない |
| T-36-06-02 | Information Disclosure | 他ユーザー thread の画像サムネ URL を知っていても cookie が違えば 404 | low | mitigate | Plan 03 の `_resolve_thread_folder(github_login, thread_id)` が JWT github_login 下しか解決しない。既存防御で十分 |
| T-36-06-03 | Denial of Service | AttachmentChipRow が 100 件添付で DOM 膨張 | low | accept | CONTEXT.md D-02 で「1 メッセージあたり画像 5 枚まで」、text も実質数十件が上限 (Copilot prompt size）。社内 200 名で問題にならない |
| T-36-06-04 | Spoofing | suggestedVisionModel が API 未取得で undefined | low | mitigate | useModels の suggestedVisionModel が null の場合、showVisionWarning が false になり banner を出さない。バナー出した状態で切替 CTA クリック → handleSwitchModel 内で null check |
| T-36-06-05 | Information Disclosure | ChatMessage.additional_kwargs に機密フィールド混入 | low | mitigate | `MessageArea.AttachmentChipRow` は `attachments` のみ参照し、他 additional_kwargs フィールドは未使用。API 側 (Plan 03) で `public_kw["attachments"]` だけを公開 |
| T-36-06-06 | Tampering | Header.tsx の onModelChange が常に state 更新 → URL には反映されず state 不整合 | low | accept | 既存 design と同じ（model は URL param 化されていない、useState で管理）。Phase 36 で変更しない |
</threat_model>

<verification>
- `bun tsc --noEmit` が exit 0
- docker compose up 後の手動確認（Plan 07 で自動化）:
  1. Header の model select に 🖼 絵文字が付く（vision:true モデルのみ）
  2. gpt-4.1 選択中に画像を drop → VisionWarningBanner が出る → "claude-sonnet-4.6 に切り替える" → banner が消える
  3. 画像 + gpt-4.1 選択で送信 → エラーにならず、チャット内 AI 応答に「画像非対応」案内が含まれる（D-18）
  4. スレッド切替後にメッセージバブル内に 48×48 画像サムネ + 📄 pill が表示される（D-21）
  5. ブラウザ F5 リロード後も同じ bubble チップが表示される（Success Criteria 4）
</verification>

<success_criteria>
- useModels hook が /api/models + 1h TTL cache + suggestedVisionModel を提供
- Header.tsx が API 由来モデル一覧 + 🖼 絵文字表示、未取得時は fallback MODEL_OPTIONS
- VisionWarningBanner が D-17 仕様通りに表示・CTA でワンクリック切替・dismiss 可能
- InputBar に warningSlot が追加され、既存 3 スロット (warning/copyAll/preview/toolbar) で描画順序が維持
- useChat.sendMessage の payload に attachments が載り、D-06 の success/error クリア契約が動く
- MessageArea bubble に AttachmentChipRow が表示され画像 48×48 + text pill で履歴表示
- 既存チャット操作（送信・スレッド切替・再送信・QuestionPanel）が regression しない
</success_criteria>

<output>
完了後、`.planning/phases/36-text-code-image-multimodal/36-06-SUMMARY.md` を作成する。SUMMARY には以下を含める:
- user bubble を type:'custom' に変更した場合の視覚的影響と、代替案 (text+outer row) を取らざるを得なかった場合のメモ
- Gem/SuperChat/Canvas/Debate でも同じ attachments 対応が必要になった場合の拡張点（他アプリに本 plan で配線したのは ChatApp のみ）
- staging クリア契約 (D-06) の 4 ケースそれぞれの実装箇所
</output>
