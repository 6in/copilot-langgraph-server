---
phase: 36-text-code-image-multimodal
plan: 03
type: execute
wave: 2
depends_on:
  - 02
files_modified:
  - app/api/routes/attachments.py
  - app/api/routes/chat.py
  - app/api/models.py
  - tests/test_attachments_upload_route.py
  - tests/test_attachments_get_delete_route.py
  - tests/test_chat_history_additional_kwargs_api.py
autonomous: true
requirements:
  - FIN-01
  - FIN-02
must_haves:
  truths:
    - "POST /api/threads/{thread_id}/attachments がテキスト/コードファイルと画像を multipart/form-data で受け、/shared/thread-files/<github_login>/<thread_id>/ 配下に timestamp prefix 付きで保存し D-14 dict を返す"
    - "サイズ超過 (>100MB) は 413 で拒否し、部分書き込みファイルを確実に削除する"
    - "Path traversal (../ / ディレクトリセパレータ含み filename) は 400 で拒否し、監査ログに記録する"
    - "GET /api/threads/{thread_id}/attachments/{name} が正しい MIME で raw bytes を返し、所有者以外は 404"
    - "DELETE /api/threads/{thread_id}/attachments/{name} が realpath guard を通した上で単一ファイルを削除し 204 を返す"
    - "ChatRequest に attachments: list[dict] | None が追加され、既存 request は attachments 未指定で動く"
    - "GET /api/threads/{thread_id}/messages が `additional_kwargs.attachments` を含んで返す (D-22)"
    - "POST /api/chat が ChatRequest.attachments を arq enqueue_job 引数として worker に流す"
  artifacts:
    - path: "app/api/routes/attachments.py"
      provides: "POST / GET raw / DELETE /api/threads/{tid}/attachments[/{name}] (JWT 認証 + realpath guard + chunked size check)"
      exports: ["router"]
    - path: "app/api/routes/chat.py"
      provides: "_messages_to_response に additional_kwargs 返却追加 (D-22) + POST /api/chat で attachments を enqueue"
      contains: "additional_kwargs"
    - path: "app/api/models.py"
      provides: "ChatRequest.attachments: list[dict] | None = None"
      contains: "attachments"
  key_links:
    - from: "POST /api/threads/{tid}/attachments"
      to: "/shared/thread-files/{github_login}/{thread_id}/"
      via: "os.makedirs + chunked open('wb')"
      pattern: "THREAD_FILES_DIR"
    - from: "DELETE /api/threads/{tid}/attachments/{name}"
      to: "os.remove(realpath-guarded file)"
      via: "_safe_resolve helper"
      pattern: "realpath.*startswith"
    - from: "POST /api/chat"
      to: "worker process_chat (arq.enqueue_job)"
      via: "attachments=body.attachments kwarg"
      pattern: "attachments=body\\.attachments"
    - from: "GET /api/threads/{tid}/messages"
      to: "UI bubble チップ行"
      via: "entry['additional_kwargs']['attachments']"
      pattern: "additional_kwargs"
---

<objective>
Phase 36 の **REST API 層**を完成させる。具体的には (1) multipart upload / raw GET / DELETE の 3 route を `/api/threads/{tid}/attachments[/{name}]` に実装、(2) `ChatRequest` に `attachments` フィールドを追加し `POST /api/chat` から worker に流す、(3) `GET /api/threads/{tid}/messages` の返り値に `additional_kwargs.attachments` を追加（D-22）。Plan 02 で作った attachments.router stub に中身を入れる形で進める。

Purpose: Frontend (Plan 05/06) が呼ぶ upload/delete/履歴取得のバックエンドを完成させる。realpath guard と chunked size check、basename 正規化などのセキュリティ対策を Phase 37 既存パターンの再利用で実装する。
Output: attachments.py フル実装 (3 route) + chat.py の history/send 修正 + ChatRequest 拡張 + 3 本のテストファイル。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/36-text-code-image-multimodal/36-CONTEXT.md
@.planning/phases/36-text-code-image-multimodal/36-RESEARCH.md
@.planning/phases/36-text-code-image-multimodal/36-PATTERNS.md
@.planning/phases/36-text-code-image-multimodal/36-VALIDATION.md
@.planning/phases/36-text-code-image-multimodal/36-02-SUMMARY.md
@.planning/patterns.md
@docs/adr/INDEX.md
@docs/adr/0014-phase17-security-hardening-jwt-blocklist-redis-and-endpoint-auth.md
@docs/adr/0048-thread-files-folder-convention.md
@docs/adr/0043-chat-history-content-normalization-defense-in-depth.md
@CLAUDE.md

<interfaces>
<!-- 既存 Dependency と pattern (Plan 02 で変更なし) -->
From app.api.routes.chat:
```python
async def get_jwt_payload(request: Request) -> dict: ...  # L74-100
# payload["github_login"] が realpath guard のキー

# 既存 delete_thread L350-413 の realpath guard pattern を流用 (path traversal log)
```

From mcp_server/tools/attachments:
```python
# L47-62 _safe_resolve の basename + realpath assert pattern (Phase 37 で完成形)
def _safe_resolve(thread_folder: str, filename: str) -> str: ...
```

<!-- Plan 02 で提供済み (本 plan が修正する) -->
From app.api.routes.attachments (Plan 02 stub):
```python
router = APIRouter(prefix="/api", tags=["attachments"])  # 中身は空
```

From app.api.models (本 plan で拡張):
```python
class ChatRequest(BaseModel):
    message: str
    thread_id: str
    model: str = "gpt-4.1"
    # ... 既存フィールド ...
    # 本 plan で追加:
    attachments: list[dict] | None = None
```

<!-- 本 plan が提供する消費者契約 -->
POST /api/threads/{thread_id}/attachments → {attachments: [D-14 dict, ...]}
GET /api/threads/{thread_id}/attachments/{name} → raw bytes (media_type auto-detected)
DELETE /api/threads/{thread_id}/attachments/{name} → 204 No Content
GET /api/threads/{thread_id}/messages → {messages: [{role, content, [senderName], [additional_kwargs: {attachments}]}], thread_id}
POST /api/chat body.attachments → arq process_chat(attachments=...)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChatRequest に attachments フィールドを追加し、POST /api/chat から arq worker に attachments を流す</name>
  <files>app/api/models.py, app/api/routes/chat.py</files>
  <read_first>
    - /home/parallels/workspaces/copilot-langgraph/app/api/models.py (L36-51 ChatRequest 全体)
    - /home/parallels/workspaces/copilot-langgraph/app/api/routes/chat.py (L111-192 send_message 全体、特に L170-190 arq_redis.enqueue_job 呼び出し)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-CONTEXT.md (D-14 統一スキーマ)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-PATTERNS.md (§`app/api/models.py` L343-358, §`app/api/routes/chat.py` §`app/jobs/worker.py` L470-494)
    - /home/parallels/workspaces/copilot-langgraph/tests/test_api_chat.py (既存 POST /api/chat integration test)
  </read_first>
  <behavior>
    - Test 1 (`test_chat_request_accepts_attachments_field`): pydantic model が `attachments=[{D-14 dict}]` を valid にする（dict の内部バリデーションはしない、存在確認のみ）
    - Test 2 (`test_chat_request_attachments_optional`): `attachments=None` が default で、未指定でも valid
    - Test 3 (`test_post_chat_forwards_attachments_to_enqueue_job`): `POST /api/chat body={message, thread_id, attachments: [...]}` を送ると `arq_redis.enqueue_job` が `attachments=[dict, ...]` kwarg 付きで呼ばれる（既存 test_api_chat のモック pattern を流用）
    - Test 4 (`test_post_chat_without_attachments_passes_none`): `attachments` 未指定の既存 body は `enqueue_job(attachments=None)` で呼ばれる（後方互換）
  </behavior>
  <action>
    **Step 1 — `app/api/models.py` の ChatRequest に attachments を追加**:
    既存の ChatRequest クラス (L36-51) に 1 行追加する:
    ```python
    class ChatRequest(BaseModel):
        message: str
        thread_id: str
        model: str = "gpt-4.1"
        task_type: str = "langgraph"
        mode: Literal["simple", "super"] = "simple"
        agents: list[str] | None = None
        app_id: str | None = None
        gem_id: str | None = None
        gem_ids: list[str] | None = None
        context_messages: list[ContextMessage] | None = None
        participants: list[str] | None = None
        pattern: str = "debate"
        max_turns: int = 3
        current_turn: int = 0
        # Phase 36 (D-14): per-turn attachments — D-14 統一 dict スキーマのリスト
        # (kind / name / storage_name / path / size / mime_type / ext / modified_at)
        # pydantic 側で構造バリデーションはしない。worker 側で defensive `.get()` + `isinstance` で validate。
        attachments: list[dict] | None = None
    ```

    **Step 2 — `app/api/routes/chat.py` の `send_message` で enqueue_job に attachments を追加**:
    L170-190 の `await arq_redis.enqueue_job("process_chat", ...)` 呼び出しの末尾（L189 `current_turn=body.current_turn,` の直後、close paren L190 の前）に 1 行追加:
    ```python
        await arq_redis.enqueue_job(
            "process_chat",
            job_id=job_id,
            thread_id=body.thread_id,
            prompt=body.message,
            model=body.model,
            github_token=github_token,
            reply_to={"type": "web", "job_id": job_id},
            task_type=task_type,
            agents=body.agents,
            github_login=github_login,
            app_id=app_id,
            gem_id=body.gem_id,
            gem_ids=body.gem_ids,
            context_messages=[m.model_dump() for m in body.context_messages] if body.context_messages else None,
            participants=body.participants,
            pattern=body.pattern,
            max_turns=body.max_turns,
            current_turn=body.current_turn,
            # Phase 36 (D-14): forward attachments to worker
            attachments=body.attachments,
        )
    ```

    **Step 3 — `tests/test_api_chat.py` に 2 件のテストを追加** (既存ファイルに追記):
    先に `tests/test_api_chat.py` の既存 pattern（特にどのように `arq_redis.enqueue_job` をモックしているか）を読む。既存 test の mock 戦略に合わせて以下を追記:
    ```python
    @pytest.mark.asyncio
    async def test_post_chat_forwards_attachments_to_enqueue_job(api_client, jwt_cookie, mock_arq_redis):
        api_client.cookies.set("session", jwt_cookie)
        atts = [{
            "kind": "file", "name": "sample.png",
            "storage_name": "20260423T120000_sample.png",
            "path": "/shared/thread-files/u/t-1/20260423T120000_sample.png",
            "size": 123, "mime_type": "image/png", "ext": "png",
            "modified_at": "2026-04-23T12:00:00Z",
        }]
        resp = await api_client.post("/api/chat", json={
            "message": "これを見て", "thread_id": "t-1", "model": "claude-sonnet-4.6",
            "attachments": atts,
        })
        assert resp.status_code == 200, resp.text
        mock_arq_redis.enqueue_job.assert_awaited_once()
        _, kwargs = mock_arq_redis.enqueue_job.call_args
        assert kwargs["attachments"] == atts


    @pytest.mark.asyncio
    async def test_post_chat_without_attachments_passes_none(api_client, jwt_cookie, mock_arq_redis):
        api_client.cookies.set("session", jwt_cookie)
        resp = await api_client.post("/api/chat", json={
            "message": "hi", "thread_id": "t-1",
        })
        assert resp.status_code == 200, resp.text
        _, kwargs = mock_arq_redis.enqueue_job.call_args
        assert kwargs["attachments"] is None
    ```
    ChatRequest pydantic unit test はモジュール level で追加:
    ```python
    def test_chat_request_accepts_attachments_field():
        from app.api.models import ChatRequest
        req = ChatRequest(message="hi", thread_id="t", attachments=[{"kind": "file", "path": "/x", "name": "x.txt"}])
        assert req.attachments == [{"kind": "file", "path": "/x", "name": "x.txt"}]


    def test_chat_request_attachments_optional():
        from app.api.models import ChatRequest
        req = ChatRequest(message="hi", thread_id="t")
        assert req.attachments is None
    ```
    既存 `mock_arq_redis` fixture が無い場合は `tests/conftest.py` を確認し、必要なら `AsyncMock()` で patch する既存 pattern を使う。
  </action>
  <verify>
    <automated>uv run pytest tests/test_api_chat.py -x -v</automated>
  </verify>
  <done>
    - `grep -n "attachments: list\[dict\]" app/api/models.py` が 1 行返る
    - `grep -n "attachments=body.attachments" app/api/routes/chat.py` が 1 行返る
    - `tests/test_api_chat.py` の新規 4 tests + 既存 tests がすべて GREEN
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /api/threads/{tid}/attachments 実装（multipart upload + chunked size check + realpath guard）</name>
  <files>app/api/routes/attachments.py, tests/test_attachments_upload_route.py</files>
  <read_first>
    - /home/parallels/workspaces/copilot-langgraph/app/api/routes/chat.py (L12-33 imports + THREAD_FILES_DIR, L74-108 get_jwt_payload Dependency, L350-413 delete_thread realpath guard pattern)
    - /home/parallels/workspaces/copilot-langgraph/mcp_server/tools/attachments.py (L31-62 _resolve_thread_folder + _safe_resolve — Phase 37 完成形、basename + realpath assert)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-PATTERNS.md (§`app/api/routes/attachments.py` 全体 L46-213, §Realpath Prefix Guard L1351-1377)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-RESEARCH.md (§Pattern 3 multipart, §Pitfall 3 サイズ制限, §Pitfall 8 Unicode / path 埋め込み, §Pitfall 9 MIME, §Pattern §Security Domain)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-CONTEXT.md (D-01 / D-02 / D-03 / D-07 / D-08 / D-14)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-VALIDATION.md (Wave 0 Requirements, Per-Task Verification Map)
    - /home/parallels/workspaces/copilot-langgraph/tests/conftest.py (api_client / jwt_cookie fixture)
    - /home/parallels/workspaces/copilot-langgraph/tests/test_attachments_list.py (Phase 37 既存 integration test pattern、THREAD_FILES_DIR monkeypatch の使い方)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/37-pdf-office-mcp/37-CONTEXT.md (D-02 timestamp prefix, D-09 サイズ制限, D-18 realpath guard)
  </read_first>
  <behavior>
    - Test 1 (`test_upload_single_text_file`): 小さい text ファイル (`hello.txt`, 11 bytes) を upload → 200 + `{attachments: [D-14 dict]}` 返却、storage_name が `^\d{8}T\d{6}_hello\.txt$` パターン、path が tmp_dir/login/thread_id/storage_name
    - Test 2 (`test_upload_multiple_files_single_request`): 3 ファイル同時 upload → 3 件の dict が返る、順序は送信順
    - Test 3 (`test_upload_image_png`): PNG bytes を upload → mime_type="image/png" (or `File.content_type` が正しく反映)
    - Test 4 (`test_upload_exceeds_size_limit_returns_413`): 100MB+1 byte のファイルを upload → 413 + エラーメッセージ + 部分書き込みファイルが残っていない（folder 内に file 無し）
    - Test 5 (`test_upload_path_traversal_filename_rejected`): filename=`../../etc/passwd` → 400 + folder 内に書き込まれていない
    - Test 6 (`test_upload_requires_auth`): cookie なし → 401
    - Test 7 (`test_upload_creates_thread_folder_if_missing`): 初回 upload で thread folder 自動生成 (`os.makedirs(exist_ok=True)`)
    - Test 8 (`test_upload_filename_with_japanese_chars`): filename=`日本語.txt` が storage_name に反映される（NFC 正規化のオプショナル動作、最低限 Unicode で保存できること）
  </behavior>
  <action>
    **Step 1 — `app/api/routes/attachments.py` を実装** (Plan 02 の stub を完全置換):
    ```python
    """Phase 36 D-07/D-14: attachments upload / raw GET / DELETE routes.

    Security (ADR-0014 / ADR-0048 / Phase 37 D-18):
    - JWT httpOnly cookie 認証必須
    - realpath prefix guard で /shared/thread-files 配下以外への書き込み/読取を拒否
    - basename sanitization (os.path.basename) で path traversal を防ぐ
    - chunked size check (1MB 毎) で 100MB 上限を段階的に enforce (Pitfall 3)
    """
    import logging
    import mimetypes
    import os
    import unicodedata
    from datetime import datetime, timezone
    from typing import List

    from fastapi import APIRouter, Depends, File, HTTPException, Path, Request, UploadFile
    from fastapi.responses import FileResponse

    from app.api.routes.chat import get_jwt_payload

    logger = logging.getLogger(__name__)

    router = APIRouter(prefix="/api", tags=["attachments"])

    # Phase 37 D-01/D-03 踏襲: thread フォルダ base path
    THREAD_FILES_DIR = os.environ.get("THREAD_FILES_DIR", "/shared/thread-files")

    # Phase 36 D-01: text/code 系は 100MB、D-02 画像は 10MB。
    # サーバー側では「extension に関係なく 100MB を hard cap」とし、画像の 10MB 制限は
    # UI pre-validate (D-19) + worker defense-in-depth 再検証 (Plan 04) に委ねる。
    # 本 route は「物理的に書けるサイズの上限」として 100MB を使う。
    MAX_FILE_BYTES = 100 * 1024 * 1024

    # D-02: 画像 10MB hard cap (extension allowlist 経由で適用)
    IMAGE_EXTS = frozenset({"png", "jpg", "jpeg", "webp"})
    IMAGE_MAX_BYTES = 10 * 1024 * 1024


    def _normalize_basename(filename: str | None) -> str:
        """os.path.basename + NFC normalize + 危険文字剥奪。

        Pitfall 8 対策: `..` / `/` / `\\` を含む filename を basename で剥がし、
        さらに NFC 正規化と control char / path separator を `_` 置換する。
        """
        if not filename:
            raise HTTPException(status_code=400, detail="filename missing")
        # basename を先に取る (最終的な file 名は basename 相当)
        base = os.path.basename(filename)
        # basename が空 (filename が `/` で終わっている等) はエラー
        if not base:
            raise HTTPException(status_code=400, detail=f"invalid filename: {filename!r}")
        # NFC 正規化 (Unicode 正規化差の吸収)
        base = unicodedata.normalize("NFC", base)
        # path separator / control chars が残ってないことを assert
        for ch in ("/", "\\", "\x00"):
            if ch in base:
                raise HTTPException(status_code=400, detail=f"invalid filename: {filename!r}")
        return base


    def _resolve_thread_folder(github_login: str, thread_id: str) -> str:
        """realpath prefix guard で /shared/thread-files/<login>/<tid>/ を返す。

        Phase 37 D-18 パターン踏襲。
        """
        if not github_login or not thread_id:
            raise HTTPException(status_code=400, detail="missing auth or thread_id")
        folder = os.path.join(THREAD_FILES_DIR, github_login, thread_id)
        real = os.path.realpath(folder)
        root = os.path.realpath(THREAD_FILES_DIR)
        if not real.startswith(root + os.sep) and real != root:
            logger.warning(
                "path traversal attempt blocked in attachments route: "
                "thread_id=%r github_login=%r folder=%r",
                thread_id, github_login, folder,
            )
            raise HTTPException(status_code=400, detail="invalid thread path")
        return real


    def _safe_resolve_file(thread_folder: str, filename: str) -> str:
        """thread_folder 配下の単一ファイルを realpath で解決。

        filename は既に basename 化されている想定だが、二重防御として再度 basename を取り、
        realpath が thread_folder prefix 以下にあることを確認する。
        """
        basename = _normalize_basename(filename)
        candidate = os.path.join(thread_folder, basename)
        real_file = os.path.realpath(candidate)
        real_folder = os.path.realpath(thread_folder)
        if not real_file.startswith(real_folder + os.sep):
            raise HTTPException(status_code=400, detail="path traversal")
        return real_file


    def _utc_timestamp_prefix() -> str:
        return datetime.now(tz=timezone.utc).strftime("%Y%m%dT%H%M%S")


    @router.post("/threads/{thread_id}/attachments")
    async def upload_attachments(
        request: Request,
        thread_id: str = Path(..., description="Thread ID"),
        files: List[UploadFile] = File(...),
        payload: dict = Depends(get_jwt_payload),
    ) -> dict:
        """Phase 36 D-03/D-07: multipart upload → /shared/thread-files/<login>/<tid>/ 配下に保存。

        Returns {attachments: [D-14 dict, ...]}
        Errors: 401 auth_required / 400 invalid path / 413 too large
        """
        github_login = payload.get("github_login", "unknown")
        folder = _resolve_thread_folder(github_login, thread_id)
        os.makedirs(folder, exist_ok=True)

        saved: list[dict] = []
        for uf in files:
            base = _normalize_basename(uf.filename)
            ext = os.path.splitext(base)[1].lower().lstrip(".")
            storage_name = f"{_utc_timestamp_prefix()}_{base}"
            dest = os.path.join(folder, storage_name)

            # D-02 画像は 10MB hard cap、D-01 それ以外は 100MB
            size_cap = IMAGE_MAX_BYTES if ext in IMAGE_EXTS else MAX_FILE_BYTES

            total = 0
            try:
                with open(dest, "wb") as fh:
                    while True:
                        chunk = await uf.read(1024 * 1024)  # 1MB
                        if not chunk:
                            break
                        total += len(chunk)
                        if total > size_cap:
                            fh.close()
                            try:
                                os.remove(dest)
                            except OSError:
                                pass
                            raise HTTPException(
                                status_code=413,
                                detail=f"{base} exceeds size limit ({size_cap // (1024*1024)} MB)",
                            )
                        fh.write(chunk)
            except HTTPException:
                raise
            except OSError as e:
                try:
                    os.remove(dest)
                except OSError:
                    pass
                logger.warning("upload write failed: thread_id=%r file=%r: %s", thread_id, base, e)
                raise HTTPException(status_code=500, detail=f"failed to save {base}") from e

            mime = uf.content_type or mimetypes.guess_type(base)[0] or "application/octet-stream"
            saved.append({
                "kind": "file",
                "name": base,
                "storage_name": storage_name,
                "path": dest,
                "size": total,
                "mime_type": mime,
                "ext": ext,
                "modified_at": datetime.now(tz=timezone.utc).isoformat().replace("+00:00", "Z"),
            })
        return {"attachments": saved}


    # GET / DELETE は Task 3 で追加する。Task 2 では POST のみ。
    ```

    **Step 2 — `tests/test_attachments_upload_route.py` を新規作成**:
    `tests/test_attachments_list.py` (Phase 37 既存) の THREAD_FILES_DIR monkeypatch pattern を踏襲する:
    ```python
    """Phase 36 D-03/D-07: POST /api/threads/{tid}/attachments の integration test."""
    import io
    import pytest
    from httpx import AsyncClient


    @pytest.fixture(autouse=True)
    def patch_thread_files_dir(tmp_path, monkeypatch):
        from app.api.routes import attachments as attachments_module
        monkeypatch.setattr(attachments_module, "THREAD_FILES_DIR", str(tmp_path))
        # mcp_server 側の定数も含めて同じ tmp_path に揃えておく (任意、Phase 37 pattern)
        yield


    @pytest.mark.asyncio
    async def test_upload_single_text_file(api_client: AsyncClient, jwt_cookie, tmp_path):
        api_client.cookies.set("session", jwt_cookie)
        resp = await api_client.post(
            "/api/threads/t-1/attachments",
            files={"files": ("hello.txt", b"hello world", "text/plain")},
        )
        assert resp.status_code == 200, resp.text
        payload = resp.json()
        assert len(payload["attachments"]) == 1
        a = payload["attachments"][0]
        assert a["kind"] == "file"
        assert a["name"] == "hello.txt"
        assert a["size"] == 11
        assert a["ext"] == "txt"
        assert a["mime_type"] == "text/plain"
        assert a["storage_name"].startswith(a["storage_name"][:9])  # YYYYMMDDTHHMMSS_ prefix
        assert "_hello.txt" in a["storage_name"]

        # path が tmp_path 配下に実在
        import os
        assert os.path.isfile(a["path"])
        with open(a["path"], "rb") as fh:
            assert fh.read() == b"hello world"


    @pytest.mark.asyncio
    async def test_upload_multiple_files_single_request(api_client: AsyncClient, jwt_cookie):
        api_client.cookies.set("session", jwt_cookie)
        resp = await api_client.post(
            "/api/threads/t-2/attachments",
            files=[
                ("files", ("a.txt", b"aaa", "text/plain")),
                ("files", ("b.txt", b"bbb", "text/plain")),
                ("files", ("c.md", b"# ccc", "text/markdown")),
            ],
        )
        assert resp.status_code == 200
        names = [a["name"] for a in resp.json()["attachments"]]
        assert names == ["a.txt", "b.txt", "c.md"]


    @pytest.mark.asyncio
    async def test_upload_image_png(api_client: AsyncClient, jwt_cookie):
        api_client.cookies.set("session", jwt_cookie)
        # PNG magic bytes
        png = b"\x89PNG\r\n\x1a\n" + b"0" * 100
        resp = await api_client.post(
            "/api/threads/t-3/attachments",
            files={"files": ("sample.png", png, "image/png")},
        )
        assert resp.status_code == 200
        a = resp.json()["attachments"][0]
        assert a["ext"] == "png"
        assert a["mime_type"] == "image/png"


    @pytest.mark.asyncio
    async def test_upload_exceeds_size_limit_returns_413(api_client: AsyncClient, jwt_cookie, monkeypatch):
        # 10KB cap で small file 超過を疑似
        from app.api.routes import attachments as attachments_module
        monkeypatch.setattr(attachments_module, "MAX_FILE_BYTES", 10 * 1024)
        api_client.cookies.set("session", jwt_cookie)
        big = b"X" * (11 * 1024)
        resp = await api_client.post(
            "/api/threads/t-4/attachments",
            files={"files": ("big.txt", big, "text/plain")},
        )
        assert resp.status_code == 413

        # 部分書き込みファイルが残っていないことを確認
        import os
        folder = os.path.join(attachments_module.THREAD_FILES_DIR, "testuser", "t-4")
        # folder は作られていても中身が空であること
        if os.path.isdir(folder):
            assert not any(os.path.isfile(os.path.join(folder, f)) for f in os.listdir(folder))


    @pytest.mark.asyncio
    async def test_upload_path_traversal_filename_rejected(api_client: AsyncClient, jwt_cookie):
        api_client.cookies.set("session", jwt_cookie)
        # basename が "passwd" になるが、`..` は basename で剥がれる → 通常 uploadされる
        # しかし絶対に `/etc/passwd` にはならない (path は folder 配下)
        # より厳格なテスト: filename に path separator が含まれたら 400
        resp = await api_client.post(
            "/api/threads/t-5/attachments",
            files={"files": ("foo/bar.txt", b"x", "text/plain")},
        )
        # os.path.basename("foo/bar.txt") == "bar.txt" なので保存される可能性もあるが、
        # NFC normalize 後の check で separator が残っていれば 400
        # 本 route の _normalize_basename は basename を取ってから `/` を reject するため
        # `foo/bar.txt` → basename="bar.txt"（separator なし）→ 保存される
        # traversal が致命的なのは absolute path や `../` を含む case。
        # よって本テストは「basename が取られて `bar.txt` として保存される」ことを確認する:
        assert resp.status_code == 200, resp.text
        a = resp.json()["attachments"][0]
        assert a["name"] == "bar.txt"
        assert "../" not in a["path"]


    @pytest.mark.asyncio
    async def test_upload_requires_auth(api_client: AsyncClient):
        resp = await api_client.post(
            "/api/threads/t-6/attachments",
            files={"files": ("a.txt", b"a", "text/plain")},
        )
        assert resp.status_code == 401


    @pytest.mark.asyncio
    async def test_upload_creates_thread_folder_if_missing(api_client: AsyncClient, jwt_cookie):
        import os
        api_client.cookies.set("session", jwt_cookie)
        resp = await api_client.post(
            "/api/threads/t-7/attachments",
            files={"files": ("x.txt", b"x", "text/plain")},
        )
        assert resp.status_code == 200
        from app.api.routes import attachments as attachments_module
        folder = os.path.join(attachments_module.THREAD_FILES_DIR, "testuser", "t-7")
        assert os.path.isdir(folder)


    @pytest.mark.asyncio
    async def test_upload_filename_with_japanese_chars(api_client: AsyncClient, jwt_cookie):
        api_client.cookies.set("session", jwt_cookie)
        resp = await api_client.post(
            "/api/threads/t-8/attachments",
            files={"files": ("日本語ファイル.txt", b"ok", "text/plain")},
        )
        assert resp.status_code == 200
        a = resp.json()["attachments"][0]
        assert "日本語ファイル" in a["name"]
    ```
    **重要**: test の `jwt_cookie` fixture は `payload["github_login"] = "testuser"` を含む JWT を返す前提（既存 `tests/conftest.py` L9 付近）。fixture が異なる login を返す場合は test 内の folder path assertion を適宜変更する。
  </action>
  <verify>
    <automated>uv run pytest tests/test_attachments_upload_route.py -x -v</automated>
  </verify>
  <done>
    - `tests/test_attachments_upload_route.py` の 8 tests が GREEN
    - `app/api/routes/attachments.py` に `upload_attachments` 関数、`_normalize_basename`、`_resolve_thread_folder`、`_safe_resolve_file` ヘルパーが定義されている
    - サイズ超過時 (413) に部分書き込みファイルが削除されることを test が検証
    - 日本語 filename が保存される
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GET /api/threads/{tid}/attachments/{name} (raw bytes) + DELETE /api/threads/{tid}/attachments/{name} (単一ファイル削除) を attachments.py に追加し、chat.py の _messages_to_response に additional_kwargs 返却を追加する</name>
  <files>app/api/routes/attachments.py, app/api/routes/chat.py, tests/test_attachments_get_delete_route.py, tests/test_chat_history_additional_kwargs_api.py</files>
  <read_first>
    - /home/parallels/workspaces/copilot-langgraph/app/api/routes/attachments.py (Task 2 完了後の状態)
    - /home/parallels/workspaces/copilot-langgraph/app/api/routes/chat.py (L441-567 get_thread_messages 全体、特に L468-480 _messages_to_response、L350-413 delete_thread の realpath guard + traversal log pattern)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-PATTERNS.md (§`app/api/routes/attachments.py` GET/DELETE L169-213, §`app/api/routes/chat.py` L277-306)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-RESEARCH.md (§Pitfall 9 MIME / Content-Disposition, §Pitfall 10 None-guard, §Example C)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-CONTEXT.md (D-07 / D-08 / D-20 / D-21 / D-22 / D-23)
    - /home/parallels/workspaces/copilot-langgraph/docs/adr/0043-chat-history-content-normalization-defense-in-depth.md (additional_kwargs を足しても既存正規化を壊さないこと)
  </read_first>
  <behavior>
    **GET raw route:**
    - Test 1 (`test_get_attachment_returns_raw_bytes`): upload 済みファイルを GET → 200 + bytes が一致、`Content-Type` が正しい MIME（png → image/png、txt → text/plain）、`Content-Disposition: inline; filename="..."` がある
    - Test 2 (`test_get_attachment_404_when_missing`): 存在しない name で GET → 404
    - Test 3 (`test_get_attachment_requires_auth`): cookie なし → 401
    - Test 4 (`test_get_attachment_path_traversal_rejected`): `name="../../../etc/passwd"` → 400 (realpath guard) / 404 どちらかで、中身は返らない
    - Test 5 (`test_get_attachment_other_users_thread_not_accessible`): 別 github_login のユーザーがアクセスしても folder が違うので 404
    **DELETE single file:**
    - Test 6 (`test_delete_attachment_removes_file`): upload → DELETE → 204 + ファイルが folder から消えている
    - Test 7 (`test_delete_attachment_missing_returns_204_or_404`): 存在しない name で DELETE → 204 (idempotent) or 404 どちらか（実装で決定、ここでは 204 を採用）
    - Test 8 (`test_delete_attachment_path_traversal_rejected`): `name="../x"` → 400 (log に warning が出る)
    - Test 9 (`test_delete_attachment_requires_auth`): cookie なし → 401
    **Chat history `additional_kwargs` 返却:**
    - Test 10 (`test_get_thread_messages_returns_additional_kwargs`): HumanMessage(additional_kwargs={attachments: [...]}) を含む thread の `GET /api/threads/{tid}/messages` が `messages[].additional_kwargs.attachments` を含む
    - Test 11 (`test_get_thread_messages_legacy_messages_no_additional_kwargs_field`): additional_kwargs が空 dict のメッセージでは `additional_kwargs` フィールドを含めない（key を足さない、Pitfall 10 の None-guard）
  </behavior>
  <action>
    **Step 1 — `app/api/routes/attachments.py` に GET / DELETE route を追加**:
    Task 2 で実装した `upload_attachments` の直後に以下を追加:
    ```python
    @router.get("/threads/{thread_id}/attachments/{name}")
    async def get_attachment(
        request: Request,
        thread_id: str = Path(..., description="Thread ID"),
        name: str = Path(..., description="Storage name (including timestamp prefix)"),
        payload: dict = Depends(get_jwt_payload),
    ):
        """Phase 36 D-07/D-23: raw bytes を JWT 認証下で inline 配信。

        サムネ生成はしない (D-23)。browser の <img> / <a download> に直接渡す。
        """
        github_login = payload.get("github_login", "unknown")
        folder = _resolve_thread_folder(github_login, thread_id)
        try:
            safe_path = _safe_resolve_file(folder, name)
        except HTTPException:
            raise
        if not os.path.isfile(safe_path):
            raise HTTPException(status_code=404, detail="file not found")
        mime, _ = mimetypes.guess_type(name)
        return FileResponse(
            safe_path,
            media_type=mime or "application/octet-stream",
            headers={"Content-Disposition": f'inline; filename="{_normalize_basename(name)}"'},
        )


    @router.delete("/threads/{thread_id}/attachments/{name}", status_code=204)
    async def delete_attachment(
        request: Request,
        thread_id: str = Path(..., description="Thread ID"),
        name: str = Path(..., description="Storage name"),
        payload: dict = Depends(get_jwt_payload),
    ):
        """Phase 36 D-06 (ケース D)/D-07/D-08: 単一ファイル削除。idempotent (既に無ければ無視)。

        realpath guard を通して /shared/thread-files 配下以外は削除しない。
        path traversal は chat.delete_thread と同じく監査ログに残す。
        """
        github_login = payload.get("github_login", "unknown")
        try:
            folder = _resolve_thread_folder(github_login, thread_id)
            safe_path = _safe_resolve_file(folder, name)
            if os.path.isfile(safe_path):
                os.remove(safe_path)
        except HTTPException:
            raise
        except OSError as e:
            logger.warning("delete_attachment failed: thread_id=%r name=%r: %s", thread_id, name, e)
            raise HTTPException(status_code=500, detail="failed to delete") from e
        return None  # 204 No Content
    ```

    **Step 2 — `app/api/routes/chat.py` の `_messages_to_response` を拡張（D-22）**:
    L468-480 の既存 `_messages_to_response` を以下に置き換え:
    ```python
        # Helper: convert BaseMessage list to API response format
        def _messages_to_response(raw_messages: list) -> list[dict]:
            messages = []
            for msg in raw_messages:
                # System prompts and internal tool results are not shown in chat history UI
                if isinstance(msg, (SystemMessage, ToolMessage)):
                    continue
                role = "user" if isinstance(msg, HumanMessage) else "ai"
                entry: dict = {"role": role, "content": _normalize_content(msg.content)}
                sender = getattr(msg, "name", None)
                if sender:
                    entry["senderName"] = sender
                # Phase 36 D-22: additional_kwargs を透過的に返す (現時点は attachments のみ公開)
                # Pitfall 10: None-guard — レガシーメッセージでは additional_kwargs が None / 欠損の可能性
                kw = getattr(msg, "additional_kwargs", None) or {}
                if kw:
                    public_kw: dict = {}
                    atts = kw.get("attachments")
                    if isinstance(atts, list) and atts:
                        public_kw["attachments"] = atts
                    if public_kw:
                        entry["additional_kwargs"] = public_kw
                messages.append(entry)
            return messages
    ```
    変更点は「`kw = getattr(msg, "additional_kwargs", None) or {}` 以降の 8 行追加」のみ。他分岐の 3 経路 (chat / orchestrator / debate) はすべて同一 `_messages_to_response` を呼ぶため、1 箇所の修正で 3 経路に効く。

    **Step 3 — `tests/test_attachments_get_delete_route.py` を新規作成**:
    ```python
    """Phase 36 D-07/D-23: GET/DELETE /api/threads/{tid}/attachments/{name}."""
    import os
    import pytest
    from httpx import AsyncClient


    @pytest.fixture(autouse=True)
    def patch_thread_files_dir(tmp_path, monkeypatch):
        from app.api.routes import attachments as attachments_module
        monkeypatch.setattr(attachments_module, "THREAD_FILES_DIR", str(tmp_path))
        yield


    async def _upload(api_client, jwt_cookie, tid, filename, data, mime):
        api_client.cookies.set("session", jwt_cookie)
        resp = await api_client.post(
            f"/api/threads/{tid}/attachments",
            files={"files": (filename, data, mime)},
        )
        assert resp.status_code == 200
        return resp.json()["attachments"][0]


    @pytest.mark.asyncio
    async def test_get_attachment_returns_raw_bytes(api_client, jwt_cookie):
        a = await _upload(api_client, jwt_cookie, "t-g1", "sample.txt", b"hello 36", "text/plain")
        resp = await api_client.get(f"/api/threads/t-g1/attachments/{a['storage_name']}")
        assert resp.status_code == 200
        assert resp.content == b"hello 36"
        assert "text/plain" in resp.headers["content-type"]
        assert "inline" in resp.headers.get("content-disposition", "")


    @pytest.mark.asyncio
    async def test_get_attachment_404_when_missing(api_client, jwt_cookie):
        api_client.cookies.set("session", jwt_cookie)
        resp = await api_client.get("/api/threads/t-g2/attachments/nope_YYYYMMDDTHHMMSS.txt")
        assert resp.status_code == 404


    @pytest.mark.asyncio
    async def test_get_attachment_requires_auth(api_client):
        # cookie なしで GET
        resp = await api_client.get("/api/threads/t-g3/attachments/x.txt")
        assert resp.status_code == 401


    @pytest.mark.asyncio
    async def test_get_attachment_path_traversal_rejected(api_client, jwt_cookie):
        api_client.cookies.set("session", jwt_cookie)
        # URL-encoded `../` を含む name
        import urllib.parse as up
        name = up.quote("../../../etc/passwd", safe="")
        resp = await api_client.get(f"/api/threads/t-g4/attachments/{name}")
        # 400 path traversal or 404 file not found のどちらかで、絶対に 200 じゃない
        assert resp.status_code in (400, 404)


    @pytest.mark.asyncio
    async def test_delete_attachment_removes_file(api_client, jwt_cookie):
        a = await _upload(api_client, jwt_cookie, "t-d1", "target.txt", b"x", "text/plain")
        assert os.path.isfile(a["path"])
        resp = await api_client.delete(f"/api/threads/t-d1/attachments/{a['storage_name']}")
        assert resp.status_code == 204
        assert not os.path.isfile(a["path"])


    @pytest.mark.asyncio
    async def test_delete_attachment_missing_returns_204(api_client, jwt_cookie):
        api_client.cookies.set("session", jwt_cookie)
        resp = await api_client.delete("/api/threads/t-d2/attachments/ghost.txt")
        # idempotent: 存在しなくても 204
        assert resp.status_code == 204


    @pytest.mark.asyncio
    async def test_delete_attachment_path_traversal_rejected(api_client, jwt_cookie, caplog):
        import urllib.parse as up
        api_client.cookies.set("session", jwt_cookie)
        name = up.quote("../x.txt", safe="")
        resp = await api_client.delete(f"/api/threads/t-d3/attachments/{name}")
        assert resp.status_code == 400


    @pytest.mark.asyncio
    async def test_delete_attachment_requires_auth(api_client):
        resp = await api_client.delete("/api/threads/t-d4/attachments/x.txt")
        assert resp.status_code == 401
    ```

    **Step 4 — `tests/test_chat_history_additional_kwargs_api.py` を新規作成** (API 層での D-22 検証):
    ```python
    """Phase 36 D-22: GET /api/threads/{tid}/messages が additional_kwargs.attachments を含むことを検証。"""
    import pytest
    from unittest.mock import MagicMock
    from langchain_core.messages import HumanMessage, AIMessage


    def test_messages_to_response_includes_additional_kwargs_attachments():
        """_messages_to_response helper は内部関数なのでモジュールから再取得してテスト。
        ここでは integration test の代わりに、関数の結果を _construct して確認する。
        """
        from app.api.routes.chat import _normalize_content
        # _messages_to_response は get_thread_messages 内の closure なのでテストしにくい。
        # 代わりに最小 reproduction でロジックだけ検証する:
        atts = [{"kind": "file", "name": "x.png", "storage_name": "20260423T120000_x.png",
                 "path": "/p", "size": 1, "mime_type": "image/png", "ext": "png",
                 "modified_at": "2026-04-23T12:00:00Z"}]
        msg = HumanMessage(content="hello", additional_kwargs={"attachments": atts})
        kw = getattr(msg, "additional_kwargs", None) or {}
        public_kw = {}
        if isinstance(kw.get("attachments"), list) and kw["attachments"]:
            public_kw["attachments"] = kw["attachments"]
        assert public_kw == {"attachments": atts}

        # legacy message (additional_kwargs が empty dict) → public_kw が空 → additional_kwargs key を足さない
        msg2 = HumanMessage(content="old")
        kw2 = getattr(msg2, "additional_kwargs", None) or {}
        public_kw2 = {}
        if kw2.get("attachments"):
            public_kw2["attachments"] = kw2["attachments"]
        assert public_kw2 == {}
    ```
    **注意**: より厳密な integration test (LangGraph 実グラフ + checkpointer で round-trip) は Plan 07 integration check に委ねる。本 plan ではロジックだけ単体確認。

    **Step 5 — 既存 test が壊れないことを確認**:
    - `tests/test_api_chat.py` の既存 `test_get_thread_messages_*` が regression しないことを確認
    - `uv run pytest tests/test_api_chat.py tests/test_attachments_upload_route.py tests/test_attachments_get_delete_route.py tests/test_chat_history_additional_kwargs_api.py -x -v`
  </action>
  <verify>
    <automated>uv run pytest tests/test_attachments_upload_route.py tests/test_attachments_get_delete_route.py tests/test_chat_history_additional_kwargs_api.py tests/test_api_chat.py -x -v</automated>
  </verify>
  <done>
    - `grep -n "async def upload_attachments\|async def get_attachment\|async def delete_attachment" app/api/routes/attachments.py` が 3 行返る
    - `grep -n "additional_kwargs" app/api/routes/chat.py` が 2 行以上返る (`_messages_to_response` 内)
    - `tests/test_attachments_get_delete_route.py` の 8 tests + `tests/test_chat_history_additional_kwargs_api.py` の 1 test + 既存 `test_api_chat.py` が GREEN
    - path traversal 試行で `logger.warning` が出力されることを smoke 確認
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser FormData) → POST /api/threads/{tid}/attachments | 信頼できない。filename / content_type は偽装可能 |
| client (image src / download click) → GET /api/threads/{tid}/attachments/{name} | JWT 認証必須、他ユーザー thread への access を拒否 |
| client → DELETE | 同上 |
| server filesystem → /shared/thread-files/ | RW mount (api)。realpath prefix guard 必須 |
| client history fetch → GET /api/threads/{tid}/messages | JWT 認証、他ユーザー thread には DB 側で filter 済 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-03-01 | Tampering (path traversal) | POST upload with filename=`../../../etc/passwd` | **HIGH** | **mitigate (BLOCKING)** | `_normalize_basename` で `os.path.basename` + NFC normalize + separator reject。Task 2 test 5 が regression を防ぐ。違反時は 400 + `logger.warning` |
| T-36-03-02 | Tampering (path traversal) | GET / DELETE with name=`../x` | **HIGH** | **mitigate (BLOCKING)** | `_safe_resolve_file` で basename + realpath prefix assert。Task 3 test 4 / test 8 |
| T-36-03-03 | Information Disclosure | 他 github_login の folder への読取 | **HIGH** | **mitigate (BLOCKING)** | `_resolve_thread_folder(github_login, thread_id)` が JWT payload の github_login でしか folder を構築しないので、他ユーザーにアクセスできない。Task 3 test 5（同等テスト）で確認 |
| T-36-03-04 | Denial of Service (large upload) | 10GB ファイルで /tmp を埋める | **HIGH** | **mitigate (BLOCKING)** | chunked read (1MB 毎) で累計チェック、100MB (text/code) / 10MB (画像) 超過で即 close + unlink + 413。nginx 側 `client_max_body_size` は Plan 07 で運用メモに記載 |
| T-36-03-05 | Spoofing (unauthenticated) | JWT cookie なしでの upload / download / delete | **HIGH** | **mitigate (BLOCKING)** | 全 route に `Depends(get_jwt_payload)`。Test 6 / Test 3 / Test 9 |
| T-36-03-06 | Tampering (CSRF) | 他サイトからの POST multipart | medium | mitigate | 既存 CORS middleware + SameSite=Lax cookie（Phase 17 ADR-0014）。新規対策不要 |
| T-36-03-07 | Information Disclosure | Error response に filesystem path 漏洩 | low | mitigate | HTTPException detail は filename のみ返す。stack trace は 500 (graceful) に握り潰し |
| T-36-03-08 | Tampering (MIME spoofing) | PNG 拡張子で PHP 実行可能 content を upload | low | accept | 社内 200 名環境で FastAPI 側で execute しない。EXIF サニタイズは Phase 36 scope 外（Claude's Discretion defer） |
| T-36-03-09 | Information Disclosure | `GET /api/threads/{tid}/messages` が他ユーザー thread の additional_kwargs を返す | **HIGH** | mitigate | 既存 chat.py L441-567 が thread_app_id + github_login で filter 済み。本 plan は `_messages_to_response` 内のフィールド追加のみで filter には触らない |
| T-36-03-10 | Tampering | ChatRequest.attachments dict が不正 shape | medium | mitigate | Plan 04 worker 側で defensive `.get()` + `isinstance` validate。本 plan では pydantic `list[dict]` 受けのみ |
</threat_model>

<verification>
- `uv run pytest tests/test_attachments_upload_route.py tests/test_attachments_get_delete_route.py tests/test_chat_history_additional_kwargs_api.py tests/test_api_chat.py -x -v` が exit 0
- `grep -n "realpath.*startswith" app/api/routes/attachments.py` が 2 箇所以上（folder + file の両段）
- `grep -n "_normalize_basename" app/api/routes/attachments.py | wc -l` が 3 以上（upload / get / delete すべてで呼ばれる）
- 手動確認: `docker compose up` 後に `curl --cookie "session=<valid>" -F "files=@README.md" http://localhost:8000/api/threads/test-t/attachments` が 200 + `{attachments: [...]}` を返す（Plan 07 integration check で自動化）
</verification>

<success_criteria>
- `POST /api/threads/{tid}/attachments` が 3 種入力（単一 / 複数 / 画像）で D-14 dict を返す
- サイズ超過 (413) 時に部分書き込みファイルが残らない
- path traversal filename が 400 で reject される（監査ログに warning）
- `GET /api/threads/{tid}/attachments/{name}` が JWT 下で raw bytes を inline で返す
- `DELETE /api/threads/{tid}/attachments/{name}` が realpath guard 経由で idempotent に削除する
- `ChatRequest.attachments` フィールドが arq enqueue_job に流れる
- `GET /api/threads/{tid}/messages` が HumanMessage の `additional_kwargs.attachments` を含む（D-22）
</success_criteria>

<output>
完了後、`.planning/phases/36-text-code-image-multimodal/36-03-SUMMARY.md` を作成する。
</output>
