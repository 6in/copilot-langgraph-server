---
phase: 36-text-code-image-multimodal
plan: 05
type: execute
wave: 4
depends_on:
  - 03
files_modified:
  - frontend/src/types.ts
  - frontend/src/api/client.ts
  - frontend/src/hooks/useAttachments.ts
  - frontend/src/components/AttachmentButton.tsx
  - frontend/src/components/AttachmentChips.tsx
  - frontend/src/components/InputBar.tsx
  - frontend/src/components/ChatApp.tsx
  - frontend/src/theme.css
autonomous: true
requirements:
  - FIN-01
  - FIN-02
must_haves:
  truths:
    - "ユーザーが ChatApp の 📎 ボタンをクリックして text/code/画像ファイルを staging できる"
    - "ユーザーが InputBar または MessageArea 領域にファイルを drag & drop して staging できる"
    - "ユーザーが textarea focus 中に Ctrl+V / Cmd+V で画像を paste して staging できる"
    - "staging した添付が InputBar previewSlot に 48×48 サムネ（画像）または [📄 name size ×] pill（text/code）として表示される"
    - "× ボタンで staging アイテムを手動削除すると、即座にサーバーからも DELETE される（D-06 ケース D）"
    - "Uploading 中のアイテムは半透明 + spinner 表示で、× で AbortController キャンセル可能"
    - "所在地は staging (InputBar preview) と persisted (folder) の両方で一致し、送信時に persisted のみを useChat に渡す"
  artifacts:
    - path: "frontend/src/hooks/useAttachments.ts"
      provides: "3 入り口統一 staging hook (click/drop/paste → upload → state)"
      contains: "useAttachments"
    - path: "frontend/src/components/AttachmentButton.tsx"
      provides: "📎 button + hidden input[type=file]"
      contains: "AttachmentButton"
    - path: "frontend/src/components/AttachmentChips.tsx"
      provides: "staging chip rendering (画像サムネ / text pill / × delete / uploading spinner)"
      contains: "AttachmentChips"
    - path: "frontend/src/components/ChatApp.tsx"
      provides: "ChatApp で useAttachments + InputBar.toolbarSlot / previewSlot + drop zone listener"
      contains: "useAttachments"
    - path: "frontend/src/api/client.ts"
      provides: "postAttachments / deleteAttachment / getModels (multipart対応)"
      exports: ["postAttachments", "deleteAttachment", "getModels"]
    - path: "frontend/src/types.ts"
      provides: "AttachmentMeta / ModelInfo 型 + ChatRequest / ChatMessage 拡張"
      contains: "AttachmentMeta"
  key_links:
    - from: "AttachmentButton onFilesSelected"
      to: "useAttachments.upload"
      via: "props drilling"
      pattern: "onFilesSelected=\\{.*upload"
    - from: "ChatApp 親 div (InputBar + MessageArea 包含)"
      to: "useAttachments.upload"
      via: "onDragOver / onDrop listener"
      pattern: "onDrop"
    - from: "InputBar previewSlot"
      to: "<AttachmentChips items={staging} />"
      via: "Plan 06 で ChatApp から差し込み"
      pattern: "previewSlot=\\{"
---

<objective>
Phase 36 の **frontend staging + upload 経路** を実装する。具体的には (1) `useAttachments` hook を新規作成（3 入り口 click/drop/paste を 1 関数に集約 + AbortController + サーバー連動 CRUD）、(2) `<AttachmentButton>` / `<AttachmentChips>` 2 components 新規作成（UI-SPEC 仕様完全準拠）、(3) ChatApp.tsx で useAttachments を配線して InputBar の toolbarSlot / previewSlot に差し込み + ChatApp 全体の drop zone listener を追加、(4) types.ts に `AttachmentMeta` 型追加、(5) client.ts に `postAttachments` / `deleteAttachment` 追加。**Plan 06（vision banner + 送信 payload + 履歴描画）は本 plan 完了後に独立して進める**。

Purpose: ユーザーの "ファイルを添付する" という最初のインタラクションを完成させ、サーバーへの即時永続化 (D-03) を通す。staging 状態は useAttachments の state のみで管理し、送信成功まで UI 側で保持する。
Output: 1 hook + 2 components + 5 修正ファイル（types / client / InputBar / ChatApp / theme.css）。テストはフロントエンド vitest がプロジェクト内に無いため、目視 + chromium MCP での人間確認（Plan 07 で実施）に委ねる（RESEARCH.md §Wave 0 の "フロントエンド unit は Claude's Discretion" に基づく）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/36-text-code-image-multimodal/36-CONTEXT.md
@.planning/phases/36-text-code-image-multimodal/36-RESEARCH.md
@.planning/phases/36-text-code-image-multimodal/36-PATTERNS.md
@.planning/phases/36-text-code-image-multimodal/36-UI-SPEC.md
@.planning/phases/36-text-code-image-multimodal/36-VALIDATION.md
@.planning/phases/36-text-code-image-multimodal/36-03-SUMMARY.md
@.planning/patterns.md
@docs/adr/INDEX.md
@CLAUDE.md

<interfaces>
<!-- Plan 03 で提供済みの backend route -->
- `POST /api/threads/{thread_id}/attachments` (multipart/form-data, FormData の "files" に複数) → `{attachments: [D-14 dict, ...]}`
- `DELETE /api/threads/{thread_id}/attachments/{name}` → 204
- `GET /api/threads/{thread_id}/attachments/{name}` → raw bytes (次 plan で Message bubble に使用)

<!-- 既存 frontend pattern -->
From frontend/src/components/InputBar.tsx (Phase 35 既存, L1-228):
```typescript
export interface InputBarProps {
  value: string;
  onChange: (next: string) => void;
  onSend: (text: string, contextMessages?: ContextMessage[]) => void;
  onCancel?: () => void;
  onAskMe?: () => void;
  disabled?: boolean;
  isThinking?: boolean;
  placeholder?: string;
  toolbarSlot?: ReactNode;   // L22 — Phase 36 で AttachmentButton を差し込む
  previewSlot?: ReactNode;   // L23 — Phase 36 で AttachmentChips を差し込む
  copyAllSlot?: ReactNode;
  // warningSlot は Plan 06 で追加する (本 plan では触らない)
}
```

From frontend/src/api/client.ts (L1-10):
```typescript
const API_BASE = (import.meta.env.VITE_APP_BASE ?? '').replace(/\/$/, '');

async function apiFetch<T>(path: string, init?: RequestInit): Promise<T> { ... }
// 既存 deleteThread は multipart 扱わず直接 fetch を使う L88-96 pattern を流用
```

<!-- 本 plan が提供する消費者契約 -->
```typescript
// frontend/src/hooks/useAttachments.ts (新規)
export interface StagingItem extends AttachmentMeta {
  localId: string;           // crypto.randomUUID()
  status: 'uploading' | 'done' | 'error';
  error?: string;
  abortCtrl?: AbortController;
  threadId?: string;
}

export function useAttachments(threadId: string | null): {
  items: StagingItem[];
  upload: (files: File[]) => Promise<void>;  // 3 入り口共通
  removeItem: (localId: string) => Promise<void>;  // abort or DELETE
  clearAll: () => void;
  getReadyItems: () => StagingItem[];
};

// frontend/src/api/client.ts (新規 exports)
export const postAttachments = (threadId: string, files: File[]): Promise<{attachments: AttachmentMeta[]}>;
export const deleteAttachment = (threadId: string, name: string): Promise<void>;
// getModels は Plan 06 で追加 (本 plan では触らない)

// frontend/src/types.ts (拡張)
export interface AttachmentMeta {
  kind: 'file';
  name: string;
  storage_name: string;
  path: string;
  size: number;
  mime_type: string;
  ext: string;
  modified_at: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: types.ts に AttachmentMeta / StagingItem 型を追加し、client.ts に postAttachments / deleteAttachment を追加</name>
  <files>frontend/src/types.ts, frontend/src/api/client.ts</files>
  <read_first>
    - /home/parallels/workspaces/copilot-langgraph/frontend/src/types.ts (L1-164 全体)
    - /home/parallels/workspaces/copilot-langgraph/frontend/src/api/client.ts (L1-215 全体、特に L88-96 deleteThread pattern、L56-61 postChat pattern)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-PATTERNS.md (§`frontend/src/types.ts` L1195-1225, §`frontend/src/api/client.ts` L1149-1192)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-CONTEXT.md (D-14)
  </read_first>
  <action>
    **Step 1 — `frontend/src/types.ts` に型を追加**:
    既存 `ChatMessage` (L50-54) の直後、または既存 `ChatRequest` (L81-97) に紐づけて以下を追加する:
    ```typescript
    // --- Phase 36: Attachments ---

    export interface AttachmentMeta {
      kind: 'file';
      name: string;
      storage_name: string;
      path: string;
      size: number;
      mime_type: string;
      ext: string;
      modified_at: string;
    }

    // ChatMessage 拡張 (既存の interface に additional_kwargs フィールドを追加)
    // 元の定義 L50-54 を以下に差し替え:
    export interface ChatMessage {
      role: 'user' | 'ai';
      content: string;
      senderName?: string;
      // Phase 36 D-22: 添付履歴は additional_kwargs.attachments として HumanMessage に載る
      additional_kwargs?: {
        attachments?: AttachmentMeta[];
      };
    }

    // ChatRequest 拡張 (L81-97 の interface に attachments を追加)
    // 元の定義の末尾に追加:
    export interface ChatRequest {
      message: string;
      thread_id: string;
      model: string;
      task_type?: string;
      mode?: 'simple' | 'super';
      agents?: string[];
      app_id?: string;
      gem_id?: string | null;
      gem_ids?: string[];
      context_messages?: ContextMessage[];
      participants?: string[];
      pattern?: string;
      max_turns?: number;
      current_turn?: number;
      // Phase 36 (D-14): per-turn 新規添付
      attachments?: AttachmentMeta[];
    }

    // Phase 36 D-16: モデル一覧 (useModels / Header で消費) — Plan 06 で使うが型は先に定義しておく
    export interface ModelInfo {
      id: string;
      name: string;
      vision: boolean;
      vision_limits?: {
        supported_media_types?: string[] | null;
        max_prompt_images?: number | null;
        max_prompt_image_size?: number | null;
      } | null;
      billing_multiplier?: number | null;
    }
    ```
    既存の `ChatMessage` / `ChatRequest` interface をそのまま差し替えること（`export interface` の旧版が残らないようにする）。

    **Step 2 — `frontend/src/api/client.ts` に postAttachments / deleteAttachment を追加**:
    既存 imports (L11-29) の末尾に `AttachmentMeta` を追加:
    ```typescript
    import type {
      AgentInfo,
      AppDefinition,
      AuthStartResponse,
      AuthPollResponse,
      AuthStatusResponse,
      AuthLogoutResponse,
      ChatAsyncResponse,
      JobStatusResponse,
      ThreadInfo,
      UserInfoResponse,
      ChatMessage,
      ThreadMessagesResponse,
      ChatRequest,
      GemInfo,
      GemCreate,
      CanvasAppInfo,
      CanvasDeployResponse,
      AttachmentMeta,   // Phase 36
    } from '../types';
    ```
    既存 `deleteThread` (L88-96) の直後、`renameThread` の前に以下を追加:
    ```typescript
    // --- Phase 36: Attachments (D-07) ---

    // postAttachments: multipart/form-data。Content-Type は browser が boundary 付きで自動設定するので
    // 明示的に Content-Type を指定しない (指定すると boundary が壊れる)。
    // apiFetch wrapper は JSON 前提なので使わず直接 fetch で投げる (deleteThread と同 pattern)。
    export const postAttachments = async (
      threadId: string,
      files: File[],
      signal?: AbortSignal,
    ): Promise<{ attachments: AttachmentMeta[] }> => {
      const fd = new FormData();
      for (const f of files) fd.append('files', f, f.name);
      const resp = await fetch(
        `${API_BASE}/api/threads/${encodeURIComponent(threadId)}/attachments`,
        { method: 'POST', body: fd, credentials: 'include', signal },
      );
      if (!resp.ok) {
        throw new Error(`postAttachments failed: ${resp.status}`);
      }
      return resp.json();
    };

    // deleteAttachment: 204 No Content 期待。idempotent。
    export const deleteAttachment = async (threadId: string, name: string): Promise<void> => {
      const resp = await fetch(
        `${API_BASE}/api/threads/${encodeURIComponent(threadId)}/attachments/${encodeURIComponent(name)}`,
        { method: 'DELETE', credentials: 'include' },
      );
      if (resp.status !== 204 && !resp.ok) {
        throw new Error(`deleteAttachment failed: ${resp.status}`);
      }
    };
    ```

    **Step 3 — 検証**:
    - `cd frontend && bun run typecheck` (or `bun tsc --noEmit`) で型エラーが出ないこと
    - 既存 import (`import type { ChatMessage } from '../types'`) が壊れていないこと (`ChatMessage` の新フィールドは optional)
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph/frontend && bun tsc --noEmit 2>&1 | head -50</automated>
    <manual>
      1. `grep -n "export interface AttachmentMeta" frontend/src/types.ts` が 1 行返ることを確認
      2. `grep -n "export const postAttachments\|export const deleteAttachment" frontend/src/api/client.ts` が 2 行返ることを確認
      3. `ChatMessage` interface に `additional_kwargs?: { attachments?: AttachmentMeta[] }` が定義されていることを確認
      4. `ChatRequest` interface に `attachments?: AttachmentMeta[]` が定義されていることを確認
    </manual>
    <integration_check>runtime behavior (client.ts multipart POST / DELETE) は Plan 07 Task 1 シナリオ 1 / シナリオ 9 (Network タブで POST/DELETE 確認) で E2E 検証</integration_check>
  </verify>
  <done>
    - `grep -n "export interface AttachmentMeta" frontend/src/types.ts` が 1 行
    - `grep -n "export interface ModelInfo" frontend/src/types.ts` が 1 行
    - `grep -n "additional_kwargs" frontend/src/types.ts` が 1 行（ChatMessage 内）
    - `grep -n "attachments?: AttachmentMeta" frontend/src/types.ts` が 1 行（ChatRequest 内）
    - `grep -n "export const postAttachments\|export const deleteAttachment" frontend/src/api/client.ts` が 2 行
    - `bun tsc --noEmit` が exit 0（既存コードの型エラーも出ない）
  </done>
</task>

<task type="auto">
  <name>Task 2: useAttachments hook と AttachmentButton / AttachmentChips components を新規作成</name>
  <files>frontend/src/hooks/useAttachments.ts, frontend/src/components/AttachmentButton.tsx, frontend/src/components/AttachmentChips.tsx, frontend/src/theme.css</files>
  <read_first>
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-UI-SPEC.md (§Component Contracts 全体 L271-440, §Copywriting Contract 全体 L192-267, §Interaction Contract L444-485, §Visual Accessibility Baseline L520-533)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-PATTERNS.md (§AttachmentButton L568-635, §AttachmentChips L641-762, §useAttachments L965-1063)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-RESEARCH.md (§Pattern 5 React file/drop/paste, §Pitfall 4 iOS Safari, §Pitfall 5 複数タブ, §Code Example D)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-CONTEXT.md (D-01 / D-02 / D-03 / D-04 / D-05 / D-06 / D-14 / D-19)
    - /home/parallels/workspaces/copilot-langgraph/frontend/src/components/InputBar.tsx (L155-180 AskMe button 参考 pattern、hover/focus style)
    - /home/parallels/workspaces/copilot-langgraph/frontend/src/components/MessageArea.tsx (L39-67 CopyButton, L333 typing-dot pattern)
    - /home/parallels/workspaces/copilot-langgraph/frontend/src/hooks/useChat.ts (L119-130 AbortController/ref pattern)
    - /home/parallels/workspaces/copilot-langgraph/frontend/src/theme.css (既存 CSS 変数、chat-attach-btn の追加位置)
  </read_first>
  <action>
    **Step 1 — `frontend/src/hooks/useAttachments.ts` を新規作成**:
    PATTERNS.md L965-1063 の実装を 1:1 でコピーし、D-02 画像 hard cap と D-01 extension pre-validate を追加する:
    ```typescript
    // frontend/src/hooks/useAttachments.ts
    // Phase 36 D-03/D-04/D-06/D-14/D-19: 3 入り口 (click/drop/paste) 統一 staging + upload + cancel.

    import { useCallback, useRef, useState } from 'react';
    import { postAttachments, deleteAttachment } from '../api/client';
    import type { AttachmentMeta, ModelInfo } from '../types';

    // D-01/D-02: pre-validate 用定数
    const IMAGE_EXTS = new Set(['png', 'jpg', 'jpeg', 'webp']);
    const IMAGE_MAX_BYTES = 10 * 1024 * 1024;       // 10MB (D-02)
    const TEXT_MAX_BYTES = 100 * 1024 * 1024;       // 100MB (D-01)
    const MAX_IMAGES_PER_MESSAGE = 5;               // D-02
    const ALLOWED_TEXT_EXTS = new Set([
      'txt', 'md', 'json', 'csv', 'py', 'js', 'ts', 'tsx', 'jsx',
      'html', 'css', 'yaml', 'yml', 'toml', 'xml', 'log', 'sh', 'sql',
    ]);
    // text/* MIME はすべて許容 (D-01)

    export interface StagingItem extends AttachmentMeta {
      localId: string;
      status: 'uploading' | 'done' | 'error';
      error?: string;
      abortCtrl?: AbortController;
      threadId?: string;
    }

    interface ValidationError {
      file: File;
      reason: string;
    }

    export function useAttachments(
      threadId: string | null,
      selectedModelInfo?: ModelInfo | null,
    ) {
      const [items, setItems] = useState<StagingItem[]>([]);
      const [validationError, setValidationError] = useState<ValidationError | null>(null);
      const latestItemsRef = useRef(items);
      latestItemsRef.current = items;

      const _currentImageCount = () =>
        latestItemsRef.current.filter((it) => IMAGE_EXTS.has(it.ext.toLowerCase())).length;

      const _validate = useCallback((file: File): string | null => {
        const ext = (file.name.split('.').pop() ?? '').toLowerCase();
        const isImage = IMAGE_EXTS.has(ext);
        const isTextLike = file.type.startsWith('text/') || ALLOWED_TEXT_EXTS.has(ext);

        if (isImage) {
          if (file.size > IMAGE_MAX_BYTES) {
            return `${file.name} は 10 MB を超えるため添付できません。`;
          }
          // D-19: model vision_limits による追加制約
          if (selectedModelInfo?.vision_limits) {
            const mps = selectedModelInfo.vision_limits.max_prompt_image_size;
            if (mps && file.size > mps) {
              return `${file.name} はモデルの上限サイズ (${Math.floor(mps / 1024 / 1024)} MB) を超えるため添付できません。`;
            }
            const allowed = selectedModelInfo.vision_limits.supported_media_types;
            if (allowed && allowed.length > 0 && !allowed.includes(file.type)) {
              return `${file.name} はモデルが対応していない形式です。`;
            }
          }
          // 枚数 cap
          if (_currentImageCount() >= MAX_IMAGES_PER_MESSAGE) {
            return `画像は 1 メッセージあたり ${MAX_IMAGES_PER_MESSAGE} 枚までです。`;
          }
          if (selectedModelInfo?.vision_limits?.max_prompt_images) {
            const cap = Math.min(MAX_IMAGES_PER_MESSAGE, selectedModelInfo.vision_limits.max_prompt_images);
            if (_currentImageCount() >= cap) {
              return `画像は 1 メッセージあたり ${cap} 枚までです。`;
            }
          }
          return null;
        }

        // text / code 系
        if (!isTextLike) {
          return `${ext} 形式は対応していません。対応形式: PNG / JPG / WebP / テキスト・コード系。`;
        }
        if (file.size > TEXT_MAX_BYTES) {
          return `${file.name} は 100 MB を超えるため添付できません。`;
        }
        return null;
      }, [selectedModelInfo]);

      const upload = useCallback(async (files: File[]) => {
        if (!threadId) {
          setValidationError({ file: files[0], reason: 'スレッドが未作成のため添付できません。' });
          return;
        }
        for (const f of files) {
          const invalid = _validate(f);
          if (invalid) {
            setValidationError({ file: f, reason: invalid });
            // validation 失敗分は staging に入れない
            continue;
          }
          const ext = (f.name.split('.').pop() ?? '').toLowerCase();
          const localId = crypto.randomUUID();
          const ctrl = new AbortController();
          setItems((prev) => [...prev, {
            kind: 'file',
            name: f.name,
            storage_name: '',
            path: '',
            size: f.size,
            mime_type: f.type || 'application/octet-stream',
            ext,
            modified_at: new Date().toISOString(),
            localId,
            status: 'uploading',
            abortCtrl: ctrl,
            threadId,
          }]);
          try {
            const resp = await postAttachments(threadId, [f], ctrl.signal);
            const served = resp.attachments[0];
            setItems((prev) => prev.map((x) => x.localId === localId
              ? { ...x, ...served, threadId, status: 'done' as const } : x));
          } catch (e) {
            const msg = (e as Error).name === 'AbortError' ? 'cancelled' : (e as Error).message;
            if (msg === 'cancelled') {
              // AbortController で abort した場合は staging からも消す
              setItems((prev) => prev.filter((x) => x.localId !== localId));
            } else {
              setItems((prev) => prev.map((x) => x.localId === localId
                ? { ...x, status: 'error' as const, error: msg } : x));
            }
          }
        }
      }, [threadId, _validate]);

      const removeItem = useCallback(async (localId: string) => {
        const item = latestItemsRef.current.find((x) => x.localId === localId);
        // 楽観削除 — まず UI から消す
        setItems((prev) => prev.filter((x) => x.localId !== localId));
        if (!item) return;
        if (item.status === 'uploading' && item.abortCtrl) {
          item.abortCtrl.abort();
          return;
        }
        if (item.status === 'done' && threadId && item.storage_name) {
          // D-06 ケース D: サーバーも削除
          try {
            await deleteAttachment(threadId, item.storage_name);
          } catch {
            // best effort — UI 側は既に消しているので restore しない
          }
        }
      }, [threadId]);

      const clearAll = useCallback(() => setItems([]), []);

      const getReadyItems = useCallback(
        () => latestItemsRef.current.filter((x) => x.status === 'done').map((x) => ({
          kind: x.kind,
          name: x.name,
          storage_name: x.storage_name,
          path: x.path,
          size: x.size,
          mime_type: x.mime_type,
          ext: x.ext,
          modified_at: x.modified_at,
        }) as AttachmentMeta),
        [],
      );

      const dismissValidationError = useCallback(() => setValidationError(null), []);

      return {
        items,
        upload,
        removeItem,
        clearAll,
        getReadyItems,
        validationError,
        dismissValidationError,
      };
    }
    ```

    **Step 2 — `frontend/src/components/AttachmentButton.tsx` を新規作成**:
    PATTERNS.md L584-635 と UI-SPEC §Component Contracts §`<AttachmentButton>` (L271-300) の仕様通り:
    ```tsx
    // frontend/src/components/AttachmentButton.tsx
    // Phase 36 D-04/D-05: 📎 button + hidden input[type=file multiple]。
    // InputBar.toolbarSlot に差し込まれる想定。click 経路の file 選択を onFilesSelected で通知。

    import { useRef, type ChangeEvent } from 'react';

    export interface AttachmentButtonProps {
      onFilesSelected: (files: File[]) => void;
      disabled?: boolean;
      acceptedExtensions?: string[];  // HTML accept 属性（.png / .jpg / .webp / text/* / etc）
    }

    const DEFAULT_ACCEPT = [
      '.png', '.jpg', '.jpeg', '.webp',
      '.txt', '.md', '.json', '.csv', '.py', '.js', '.ts', '.tsx', '.jsx',
      '.html', '.css', '.yaml', '.yml', '.toml', '.xml', '.log', '.sh', '.sql',
      'text/*',
    ];

    export function AttachmentButton({
      onFilesSelected,
      disabled,
      acceptedExtensions,
    }: AttachmentButtonProps) {
      const fileInputRef = useRef<HTMLInputElement>(null);

      const handleClick = () => {
        if (disabled) return;
        fileInputRef.current?.click();
      };

      const handleChange = (e: ChangeEvent<HTMLInputElement>) => {
        const files = Array.from(e.target.files ?? []);
        if (files.length > 0) onFilesSelected(files);
        // 同名ファイル再添付のため reset
        e.target.value = '';
      };

      return (
        <>
          <button
            type="button"
            onClick={handleClick}
            disabled={disabled}
            aria-label={disabled ? '添付を追加できません（送信中）' : 'ファイルを添付'}
            title="ファイルを添付（最大 100MB / 画像は 10MB × 5 枚まで）"
            className="chat-attach-btn"
            style={{
              width: 36,
              height: 36,
              borderRadius: 'var(--radius-md)',
              border: '1px solid var(--color-border)',
              background: 'transparent',
              color: 'var(--color-text-muted)',
              cursor: disabled ? 'not-allowed' : 'pointer',
              opacity: disabled ? 0.5 : 1,
              fontSize: '18px',
              lineHeight: 1,
              flexShrink: 0,
              display: 'inline-flex',
              alignItems: 'center',
              justifyContent: 'center',
            }}
          >
            <span aria-hidden="true">📎</span>
            <span style={{
              position: 'absolute',
              width: 1, height: 1, padding: 0, overflow: 'hidden',
              clip: 'rect(0,0,0,0)', whiteSpace: 'nowrap', border: 0,
            }}>ファイルを添付</span>
          </button>
          <input
            ref={fileInputRef}
            type="file"
            multiple
            hidden
            accept={(acceptedExtensions ?? DEFAULT_ACCEPT).join(',')}
            onChange={handleChange}
          />
        </>
      );
    }
    ```

    **Step 3 — `frontend/src/components/AttachmentChips.tsx` を新規作成**:
    UI-SPEC §`<AttachmentChips>` (L302-342) + PATTERNS.md L658-762 を 1:1 で:
    ```tsx
    // frontend/src/components/AttachmentChips.tsx
    // Phase 36 D-05: staging chip 描画 (画像 48×48 サムネ / text pill / × 削除 / uploading spinner / error)

    import type { StagingItem } from '../hooks/useAttachments';

    const API_BASE = (import.meta.env.VITE_APP_BASE ?? '').replace(/\/$/, '');
    const IMAGE_EXTS = new Set(['png', 'jpg', 'jpeg', 'webp']);
    const THUMB_SIZE = 48;

    interface AttachmentChipsProps {
      items: StagingItem[];
      onRemove: (localId: string) => void;
    }

    export function AttachmentChips({ items, onRemove }: AttachmentChipsProps) {
      if (items.length === 0) return null;
      return (
        <div role="list" style={{
          display: 'flex',
          flexWrap: 'wrap',
          gap: 'var(--space-2)',
          alignItems: 'center',
        }}>
          {items.map((item) => {
            const isImage = IMAGE_EXTS.has(item.ext.toLowerCase());
            return isImage
              ? <ImageChip key={item.localId} item={item} onRemove={onRemove} />
              : <FileChip key={item.localId} item={item} onRemove={onRemove} />;
          })}
        </div>
      );
    }

    function ImageChip({ item, onRemove }: { item: StagingItem; onRemove: (id: string) => void }) {
      const url = item.threadId && item.storage_name
        ? `${API_BASE}/api/threads/${encodeURIComponent(item.threadId)}/attachments/${encodeURIComponent(item.storage_name)}`
        : null;
      return (
        <div
          role="listitem"
          aria-label={`画像: ${item.name}（${_formatSize(item.size)}）。削除ボタン付き。`}
          aria-busy={item.status === 'uploading'}
          style={{
            position: 'relative',
            width: THUMB_SIZE,
            height: THUMB_SIZE,
            borderRadius: 'var(--radius-md)',
            border: item.status === 'error'
              ? '1px solid var(--color-destructive)'
              : '1px solid var(--color-border)',
            overflow: 'hidden',
            opacity: item.status === 'uploading' ? 0.5 : 1,
            flexShrink: 0,
          }}
        >
          {item.status === 'done' && url && (
            <img src={url} alt={item.name}
                 width={THUMB_SIZE} height={THUMB_SIZE}
                 style={{ objectFit: 'cover' }} />
          )}
          {item.status === 'uploading' && (
            <div style={{
              display: 'flex', justifyContent: 'center', alignItems: 'center',
              width: '100%', height: '100%',
            }}>
              <span className="typing-dot" />
            </div>
          )}
          {item.status === 'error' && (
            <div style={{
              display: 'flex', justifyContent: 'center', alignItems: 'center',
              width: '100%', height: '100%', fontSize: 20, color: 'var(--color-text-muted)',
            }}>🖼</div>
          )}
          <button
            onClick={() => onRemove(item.localId)}
            aria-label={`${item.name} を添付から削除`}
            style={{
              position: 'absolute', top: 2, right: 2,
              width: 20, height: 20, borderRadius: '50%',
              border: 'none', background: 'rgba(0,0,0,0.5)',
              color: 'white', cursor: 'pointer', fontSize: '12px',
              lineHeight: 1, padding: 0,
            }}
          >×</button>
        </div>
      );
    }

    function FileChip({ item, onRemove }: { item: StagingItem; onRemove: (id: string) => void }) {
      return (
        <div
          role="listitem"
          aria-label={`ファイル: ${item.name}（${_formatSize(item.size)}）。削除ボタン付き。`}
          aria-busy={item.status === 'uploading'}
          style={{
            display: 'inline-flex',
            alignItems: 'center',
            gap: 'var(--space-1)',
            height: 28,
            padding: '0 var(--space-2)',
            borderRadius: 'var(--radius-full)',
            border: item.status === 'error'
              ? '1px solid var(--color-destructive)'
              : '1px solid var(--color-border)',
            background: 'var(--color-surface)',
            color: 'var(--color-text)',
            fontSize: '14px',
            maxWidth: 240,
            opacity: item.status === 'uploading' ? 0.5 : 1,
          }}
        >
          <span aria-hidden="true">
            {item.status === 'error' ? '⚠' : '📄'}
          </span>
          <span style={{
            overflow: 'hidden',
            textOverflow: 'ellipsis',
            whiteSpace: 'nowrap',
          }}>
            {item.name}
          </span>
          <span style={{
            color: 'var(--color-text-muted)',
            fontSize: '12px',
          }}>
            {_formatSize(item.size)}
          </span>
          {item.status === 'uploading' && <span className="typing-dot" style={{ marginLeft: 2 }} />}
          <button
            onClick={() => onRemove(item.localId)}
            aria-label={`${item.name} を添付から削除`}
            style={{
              border: 'none',
              background: 'transparent',
              color: 'var(--color-text-muted)',
              cursor: 'pointer',
              padding: 0,
              fontSize: '16px',
              lineHeight: 1,
              marginLeft: 'var(--space-1)',
            }}
          >×</button>
        </div>
      );
    }

    function _formatSize(bytes: number): string {
      if (bytes < 1024) return `${bytes} B`;
      if (bytes < 1024 * 1024) return `${(bytes / 1024).toFixed(1)} KB`;
      return `${(bytes / 1024 / 1024).toFixed(1)} MB`;
    }
    ```

    **Step 4 — `frontend/src/theme.css` に hover / focus style を追加**:
    既存 `.chat-send-btn` などの button hover style が定義されている箇所を `grep -n "chat-askme-btn\|chat-cancel-btn\|chat-send-btn" frontend/src/theme.css` で特定し、その近くに以下を追加:
    ```css
    /* Phase 36: AttachmentButton hover/focus (UI-SPEC §Accent Reserved-For Extension #8) */
    .chat-attach-btn:hover:not(:disabled) {
      color: var(--color-accent);
      border-color: var(--color-accent);
    }
    .chat-attach-btn:focus-visible {
      outline: 2px solid var(--color-accent);
      outline-offset: 1px;
    }

    /* Phase 36: AttachmentChips × button hover (UI-SPEC §Color §Destructive #1) */
    .chat-attach-btn,
    [role="listitem"] button[aria-label$="を添付から削除"]:hover {
      color: var(--color-destructive);
    }
    [role="listitem"] button[aria-label$="を添付から削除"]:focus-visible {
      outline: 2px solid var(--color-accent);
      outline-offset: 1px;
    }
    ```
    ※ セレクタが既存 `[role="listitem"]` と衝突する場合は `.attachment-chip-remove-btn` などの class 名を付与する代案も可。実装段階で既存 CSS との衝突を確認してから判断。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph/frontend && bun tsc --noEmit 2>&1 | head -50</automated>
    <manual>
      1. `ls frontend/src/hooks/useAttachments.ts frontend/src/components/AttachmentButton.tsx frontend/src/components/AttachmentChips.tsx` が 3 ファイル全て存在することを確認
      2. `grep -n "aria-label=\"ファイルを添付\"" frontend/src/components/AttachmentButton.tsx` が 1 行返る（UI-SPEC §Copywriting 準拠）
      3. `grep -n "画像は 1 メッセージあたり" frontend/src/hooks/useAttachments.ts` が 1 行以上（D-02 枚数 cap 検証メッセージ）
      4. AttachmentChips の IMAGE_EXTS 定義が `{png, jpg, jpeg, webp}` と一致することを `grep -n "IMAGE_EXTS" frontend/src/components/AttachmentChips.tsx` で確認
      5. `git diff frontend/src/theme.css` で既存 CSS 変数参照 (var(--color-*)) のみ使用、新規 CSS 変数が追加されていないことを確認
    </manual>
    <integration_check>useAttachments hook + AttachmentButton + AttachmentChips の runtime behavior (click/drop/paste の 3 入り口、uploading spinner、× 削除、validation error 表示) は Plan 07 Task 1 シナリオ 1/2/D-06 で E2E 検証</integration_check>
  </verify>
  <done>
    - `ls frontend/src/hooks/useAttachments.ts frontend/src/components/AttachmentButton.tsx frontend/src/components/AttachmentChips.tsx` が 3 行全て存在
    - `grep -n "useAttachments\|AttachmentButton\|AttachmentChips" frontend/src/ -r` で 3 つの export が検出
    - `grep -n "aria-label=\"ファイルを添付\"" frontend/src/components/AttachmentButton.tsx` が 1 行（UI-SPEC §Copywriting 準拠）
    - `grep -n "画像は 1 メッセージあたり" frontend/src/hooks/useAttachments.ts` が 1 行以上
    - `bun tsc --noEmit` が exit 0（型エラーなし）
    - 新規 CSS 変数が追加されていない（`git diff frontend/src/theme.css | grep "^\+.*--color-" | grep -v "var(--color-"` が 0 行）
  </done>
</task>

<task type="auto">
  <name>Task 3: ChatApp.tsx で useAttachments を配線し InputBar の toolbarSlot / previewSlot に AttachmentButton / AttachmentChips を差し込み、ChatApp 全体に drop zone listener + paste listener を追加</name>
  <files>frontend/src/components/ChatApp.tsx, frontend/src/components/MessageArea.tsx, frontend/src/components/InputBar.tsx</files>
  <read_first>
    - /home/parallels/workspaces/copilot-langgraph/frontend/src/components/ChatApp.tsx (L1-201 全体、特に L22-104 props / state 管理)
    - /home/parallels/workspaces/copilot-langgraph/frontend/src/components/MessageArea.tsx (L1-180 + L412-425 InputBar の差し込み)
    - /home/parallels/workspaces/copilot-langgraph/frontend/src/components/InputBar.tsx (L1-228 全体 — toolbarSlot / previewSlot の消費点 L114-126, L94-101)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-UI-SPEC.md (§Interaction Contract L444-485, §Component Contracts, §Responsive Breakpoints L491-506)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-PATTERNS.md (§InputBar.tsx L831-862, §MessageArea.tsx L866-926)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-RESEARCH.md (§Pattern 5 drop+paste listener, §Pitfall 4)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-CONTEXT.md (D-04 3 入り口)
  </read_first>
  <action>
    **Step 1 — `frontend/src/components/MessageArea.tsx` に attachment staging UI 配線用 props を追加**:
    既存の MessageArea interface (L24-37) はそのまま、`InputBar` コンポーネント呼出し箇所 (L412-425) で `toolbarSlot` と `previewSlot` を受け取るように props を追加する:
    ```tsx
    interface MessageAreaProps {
      // ... 既存 ...
      // Phase 36: InputBar slots
      inputToolbarSlot?: React.ReactNode;   // AttachmentButton
      inputPreviewSlot?: React.ReactNode;   // AttachmentChips
    }
    ```
    export function MessageArea の destructure 部で受け取り、L413-425 の `<InputBar ...>` に差し込む:
    ```tsx
    export function MessageArea({
      messages, isThinking, /* ... 既存 ... */,
      inputToolbarSlot,
      inputPreviewSlot,
    }: MessageAreaProps) {
      // ... 既存 ...
      return (
        // ... L412-425 付近:
        <InputBar
          value={inputValue}
          onChange={setInputValue}
          onSend={handleSendWrapped}
          onCancel={onCancel}
          onAskMe={handleAskMeWrapped}
          isThinking={isThinking}
          disabled={disabled}
          placeholder={placeholder}
          copyAllSlot={messages.length > 0 ? <CopyAllButton messages={messages} /> : undefined}
          // Phase 36 — Plan 05 で配線:
          toolbarSlot={inputToolbarSlot}
          previewSlot={inputPreviewSlot}
        />
      );
    ```

    **Step 2 — `frontend/src/components/ChatApp.tsx` に useAttachments を配線し、drop zone + paste listener を追加**:
    既存 imports (L7-17) に追加:
    ```tsx
    import { useCallback, useEffect, useRef, useState } from 'react';
    import { useParams, useNavigate } from 'react-router';
    import { MainContainer } from '@chatscope/chat-ui-kit-react';
    import { ThreadSidebar } from './ThreadSidebar';
    import { MessageArea } from './MessageArea';
    import { CanvasPane } from './CanvasPane';
    import { AttachmentButton } from './AttachmentButton';
    import { AttachmentChips } from './AttachmentChips';
    import { useThreads } from '../hooks/useThreads';
    import { useChat } from '../hooks/useChat';
    import { useCanvas } from '../hooks/useCanvas';
    import { useAttachments } from '../hooks/useAttachments';
    import { renameThread } from '../api/client';
    ```
    `export function ChatApp` 内で useAttachments を呼び出し、drop/paste listener を追加:
    ```tsx
    export function ChatApp({ selectedModel }: ChatAppProps) {
      const { threadId: urlThreadId } = useParams<{ threadId?: string }>();
      const navigate = useNavigate();

      const {
        threads, activeThreadId, messages, isLoadingMessages,
        switchThread, createNewThread, removeThread, bulkRemoveThreads,
        setMessages, refreshThreads,
      } = useThreads('chat');

      // 既存 URL sync useEffect (L45-51)

      const [sidebarCollapsed, setSidebarCollapsed] = useState(false);
      const [sidebarWidth, setSidebarWidth] = useState(SIDEBAR_DEFAULT);

      const { canvasApp, setCanvasApp, /* ... */ } = useCanvas();

      // Phase 36: staging attachments (vision pre-validate は Plan 06 の useModels から model info を取得して渡す)
      // 本 plan では selectedModelInfo は null 固定で pre-validate は size/ext のみ。vision_limits 連携は Plan 06 で有効化。
      const attachments = useAttachments(activeThreadId, null);

      // Phase 36 D-04: drop zone + paste listener
      const [dragOver, setDragOver] = useState(false);
      const rootRef = useRef<HTMLDivElement>(null);

      const onDragOver = useCallback((e: React.DragEvent) => {
        if (e.dataTransfer.types.includes('Files')) {
          e.preventDefault();
          setDragOver(true);
        }
      }, []);

      const onDragLeave = useCallback((e: React.DragEvent) => {
        // 子要素に移った dragleave を無視
        if (e.currentTarget === e.target) setDragOver(false);
      }, []);

      const onDrop = useCallback((e: React.DragEvent) => {
        e.preventDefault();
        setDragOver(false);
        const files = Array.from(e.dataTransfer.files);
        if (files.length > 0) attachments.upload(files);
      }, [attachments]);

      // Ctrl+V / Cmd+V で image blob を paste する (textarea focus 時のみ反応 — document level listener)
      useEffect(() => {
        const onPaste = (e: ClipboardEvent) => {
          if (!e.clipboardData) return;
          const imageFiles: File[] = [];
          for (let i = 0; i < e.clipboardData.items.length; i++) {
            const item = e.clipboardData.items[i];
            if (item.kind === 'file' && item.type.startsWith('image/')) {
              const f = item.getAsFile();
              if (f) imageFiles.push(f);
            }
          }
          if (imageFiles.length > 0) {
            e.preventDefault();
            attachments.upload(imageFiles);
          }
        };
        document.addEventListener('paste', onPaste);
        return () => document.removeEventListener('paste', onPaste);
      }, [attachments]);

      // 既存 handleNewChat / handleSelectThread / handleRenameThread (L70-83)

      const { isThinking, streamPreview, sendMessage, cancelJob, pendingQuestion, handleQuestionSubmit } = useChat({
        activeThreadId,
        selectedModel,
        setMessages,
        refreshThreads,
        onCanvasResponse: (app) => setCanvasApp(app),
      });

      // Phase 36: sendMessage wrapper で attachments を getReadyItems 経由で送信
      // 本 plan では attachments 連携は useChat 引数に渡す箇所だけを追加し、
      // 実際の "postChat payload に attachments を載せる" ロジックは Plan 06 で useChat に拡張する。
      // 本 plan scope は staging UI 完成までなので、送信 = clearAll だけ担保する。
      const handleSend = async (text: string) => {
        let threadId = activeThreadId;
        if (!threadId) {
          threadId = await createNewThread();
          navigate(`/chat/${threadId}`, { replace: true });
        }
        await sendMessage(text, threadId);
        // D-06 ケース成功時: clearAll。ケース A (ユーザー明示キャンセル) は cancelJob 側で clear しない。
        attachments.clearAll();
        await refreshThreads();
      };

      // Drag handle handler (既存 L107-127)

      return (
        <div
          ref={rootRef}
          style={{ flex: 1, minHeight: 0, overflow: 'hidden', position: 'relative' }}
          onDragOver={onDragOver}
          onDragLeave={onDragLeave}
          onDrop={onDrop}
        >
          {dragOver && (
            <div
              aria-hidden="true"
              style={{
                position: 'absolute',
                inset: 0,
                zIndex: 100,
                background: 'var(--color-accent-subtle)',
                border: '2px dashed var(--color-accent)',
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                justifyContent: 'center',
                gap: 'var(--space-2)',
                pointerEvents: 'none',
                opacity: 0.9,
              }}
            >
              <div style={{
                fontFamily: "'Rajdhani', sans-serif",
                fontSize: '20px',
                fontWeight: 600,
                color: 'var(--color-text)',
              }}>ファイルをドロップして添付</div>
              <div style={{ fontSize: 14, color: 'var(--color-text-muted)' }}>
                テキスト・コード・画像（PNG / JPG / WebP）に対応
              </div>
            </div>
          )}

          {/* validation error banner (D-04 で reject された file の表示) */}
          {attachments.validationError && (
            <div style={{
              padding: 'var(--space-2) var(--space-3)',
              borderBottom: '1px solid var(--color-destructive)',
              background: 'var(--color-surface)',
              fontSize: 14,
              color: 'var(--color-destructive)',
              display: 'flex',
              alignItems: 'center',
              gap: 'var(--space-2)',
            }}>
              <span aria-hidden="true">⚠</span>
              <span style={{ flex: 1 }}>{attachments.validationError.reason}</span>
              <button
                onClick={attachments.dismissValidationError}
                aria-label="エラーを閉じる"
                style={{
                  border: 'none', background: 'transparent',
                  color: 'var(--color-text-muted)', cursor: 'pointer',
                }}
              >×</button>
            </div>
          )}

          <MainContainer style={{ overflow: 'hidden' }}>
            <ThreadSidebar /* ... 既存 props ... */ />
            {!sidebarCollapsed && (
              <div /* drag handle 既存 */ />
            )}

            {isLoadingMessages ? (
              <div>Loading messages...</div>
            ) : (
              <>
                <MessageArea
                  messages={messages}
                  isThinking={isThinking}
                  streamPreview={streamPreview}
                  onSend={handleSend}
                  onCancel={cancelJob}
                  pendingQuestion={pendingQuestion}
                  onQuestionSubmit={handleQuestionSubmit}
                  // Phase 36:
                  inputToolbarSlot={
                    <AttachmentButton
                      onFilesSelected={(files) => attachments.upload(files)}
                      disabled={isThinking || !activeThreadId}
                    />
                  }
                  inputPreviewSlot={
                    <AttachmentChips
                      items={attachments.items}
                      onRemove={attachments.removeItem}
                    />
                  }
                />
                {canvasApp && (
                  <CanvasPane /* 既存 */ />
                )}
              </>
            )}
          </MainContainer>
        </div>
      );
    }
    ```

    **重要**: 本 plan の変更で「活性 threadId が無いまま 📎 クリック」した場合に useAttachments が即 setValidationError を返す設計 (「スレッドが未作成のため添付できません」) にしている。もし UX として「📎 クリックで thread 自動作成」にしたければ実装段階で `onFilesSelected` をラップして `await createNewThread()` してから upload する選択も可能。本 plan ではシンプル優先。

    **Step 3 — InputBar.tsx の再確認**:
    既存 InputBar は toolbarSlot / previewSlot を既に受けるので変更不要。ただし drop zone listener は ChatApp に置くため InputBar では onDragOver / onDrop を受けない。既存の InputBar.tsx は本 plan で修正しない (warningSlot 追加は Plan 06 で実施)。

    **Step 4 — 動作確認（手動、chromium MCP は Plan 07）**:
    - `docker compose up -d` で既に frontend が立ち上がっていれば、`http://localhost:5173/orochi/` の ChatApp で:
      1. 📎 ボタンクリック → file dialog が出て txt ファイルを選ぶ → chips に pill 表示
      2. 画像ファイルを drag & drop → dragover overlay が出る → drop → chips に 48×48 サムネ
      3. Chrome DevTools で `Network` タブを開き、チップ追加時に `POST /api/threads/{tid}/attachments` が 200 で返る
      4. × ボタンで削除 → chips から消える + `DELETE /api/threads/{tid}/attachments/{storage_name}` が 204 で返る
      5. 送信 → chips が空になる
    - すべて Plan 07 で chromium MCP E2E として自動化するので、本 plan では開発者の軽い smoke 確認で OK
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph/frontend && bun tsc --noEmit 2>&1 | head -50</automated>
    <manual>
      1. `grep -n "useAttachments\|AttachmentButton\|AttachmentChips" frontend/src/components/ChatApp.tsx` が 3 行以上
      2. `grep -n "onDragOver\|onDrop\|addEventListener('paste'" frontend/src/components/ChatApp.tsx` が 3 箇所以上（drop/paste listener 配線）
      3. `grep -n "inputToolbarSlot\|inputPreviewSlot" frontend/src/components/ChatApp.tsx frontend/src/components/MessageArea.tsx` が 4 箇所以上（slot 名前付け forward）
      4. `grep -n "toolbarSlot={" frontend/src/components/MessageArea.tsx` が 1 行（inputToolbarSlot を InputBar に渡す）
      5. MessageArea 側で InputBar へ `previewSlot={inputPreviewSlot}` を forward していることを確認
    </manual>
    <integration_check>ChatApp の 3 入り口 staging 動作検証 (📎 click / drag & drop / Ctrl+V paste で chip が追加される、drop zone overlay 表示、validation error banner) は Plan 07 Task 1 シナリオ 1/2/D-06 で E2E 検証</integration_check>
  </verify>
  <done>
    - `grep -n "useAttachments\|AttachmentButton\|AttachmentChips" frontend/src/components/ChatApp.tsx` が 3 行以上
    - `grep -n "onDragOver\|onDrop\|onPaste\|addEventListener('paste'" frontend/src/components/ChatApp.tsx` が 3 箇所以上
    - `grep -n "inputToolbarSlot\|inputPreviewSlot" frontend/src/components/ChatApp.tsx frontend/src/components/MessageArea.tsx` が 4 箇所以上
    - `grep -n "toolbarSlot={" frontend/src/components/MessageArea.tsx` が 1 行（inputToolbarSlot を InputBar に渡す）
    - `bun tsc --noEmit` が exit 0
    - 3 入り口 (📎 click / drag & drop / Ctrl+V paste) の runtime 検証は Plan 07 integration check (シナリオ 1/2/D-06) に移動
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser DOM (file input / drop / clipboard) → useAttachments.upload | ユーザー操作。validate で reject 可能。サーバー側 Plan 03 が二重防御 |
| useAttachments → POST/DELETE /api/threads/{tid}/attachments | credentials:include で JWT cookie を付けて送信 |
| AttachmentChips img src → GET /api/threads/{tid}/attachments/{name} | JWT cookie (credentials is automatic for same origin) で認証付き画像配信 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-05-01 | Tampering | Drop zone が JavaScript を含む exec 可能ファイル受け入れ | low | mitigate | accept 属性 + サーバー側 extension allowlist (Plan 03)。browser 実行は起きない（SPA 内 fetch のみ） |
| T-36-05-02 | Information Disclosure | validationError が filesystem path を露出 | low | mitigate | エラーメッセージは filename + 理由のみ、path を含めない |
| T-36-05-03 | Denial of Service | paste / drop で大量ファイルを一度に添付 | medium | mitigate | MAX_IMAGES_PER_MESSAGE=5 を useAttachments 内で enforce (D-02)。サーバー側も 100MB cap |
| T-36-05-04 | Spoofing | activeThreadId が他ユーザー thread ID に差し替えられる | **HIGH** | mitigate | useParams (URL) → activeThreadId のみ受け付ける。JWT cookie の github_login と thread 所有者 mismatch の場合はサーバー側 (Plan 03) で 404 返却 |
| T-36-05-05 | Tampering | `drag & drop` で同じ file が複数 upload される | low | mitigate | localId を `crypto.randomUUID()` で生成し独立扱い。既存 chip と衝突しない |
| T-36-05-06 | XSS | filename に `<script>` 等が含まれた chip 表示 | low | mitigate | React の自動エスケープで text node として render。`dangerouslySetInnerHTML` 未使用 |
| T-36-05-07 | Information Disclosure | GET /api/threads/{tid}/attachments/{name} が 別ユーザー thread の画像を返す | **HIGH** | mitigate | Plan 03 で `_resolve_thread_folder(github_login, thread_id)` により JWT payload の github_login 配下しか取れない。URL を他人が共有しても cookie 保持者しかアクセスできない |
| T-36-05-08 | Tampering | Image chip の src URL を browser 外ツールで取得 | low | accept | credentials:include が要るので同じ JWT cookie を持つブラウザからのみアクセス可能。社内 200 名環境で追加リスク低 |
</threat_model>

<verification>
- `bun tsc --noEmit` が exit 0
- 開発者が docker compose で立ち上げて 3 入り口 (click/drop/paste) + × 削除 + 送信クリア の 5 動作を確認
- chrome devtools MCP で staging chip が 48×48 サイズ、text pill が適切なスタイル、× ボタンが hover で destructive 色（ダーク/ライト両対応）を目視確認（Plan 07 で網羅）
</verification>

<success_criteria>
- `useAttachments` hook が 3 入り口統一 staging + サーバー連動 CRUD + validation を提供
- `AttachmentButton` / `AttachmentChips` が UI-SPEC 仕様（48×48 画像サムネ / pill text / × 削除 / uploading spinner）通り
- ChatApp.tsx で drop zone overlay + paste listener + InputBar slot 差し込みが動く
- types.ts / client.ts が D-14 AttachmentMeta と multipart endpoint を export
- 既存 ChatApp の機能（送信 / スレッド切替 / 再送信）が regression しない
- 新規 CSS 変数がゼロ追加（UI-SPEC Checker #1）
</success_criteria>

<output>
完了後、`.planning/phases/36-text-code-image-multimodal/36-05-SUMMARY.md` を作成する。
</output>
