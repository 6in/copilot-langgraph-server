---
phase: 36-text-code-image-multimodal
plan: 04
type: execute
wave: 3
depends_on:
  - 02
  - 03
files_modified:
  - app/jobs/worker.py
  - app/jobs/handlers/langgraph_handler.py
  - app/jobs/handlers/orchestrator_handler.py
  - tests/test_langgraph_handler_attachments_v2.py
  - tests/test_orchestrator_handler_attachments.py
  - tests/test_worker_attachments_payload.py
autonomous: true
requirements:
  - FIN-01
  - FIN-02
must_haves:
  truths:
    - "arq worker の process_chat が attachments パラメータ (D-14 dict のリスト) を受け、job dict に詰めて各 handler に渡せる"
    - "LangGraphHandler が job.attachments を HumanMessage.additional_kwargs['attachments'] に注入する"
    - "OrchestratorHandler が SuperChat 経路で AgentState.new_attachments に attachments を積むところまで配線する (SubAgent 側 HumanMessage 注入は v6.1 defer)"
    - "DebateChat handler の attachments 対応は v6.1 defer (CONTEXT.md Claude's Discretion: ChatApp 中心)"
    - "vision 非対応モデル選択時に worker 側で画像 attachment を drop し、SystemMessage 警告を SystemMessage prepend に追加する (D-18)"
    - "text/code attachment は vision 非対応モデルでも常に pass through する"
    - "既存 Phase 37 の folder scan + attachments_hint pattern が regression しない"
  artifacts:
    - path: "app/jobs/worker.py"
      provides: "process_chat シグネチャ拡張 + job dict への attachments 注入"
      contains: "attachments=attachments"
    - path: "app/jobs/handlers/langgraph_handler.py"
      provides: "job.attachments → HumanMessage.additional_kwargs, D-18 vision drop + SystemMessage 警告"
      contains: "additional_kwargs"
    - path: "app/jobs/handlers/orchestrator_handler.py"
      provides: "SuperChat 経路での attachments 配線 (state.new_attachments まで、SubAgent 側注入は v6.1 defer)"
      contains: "new_attachments"
  key_links:
    - from: "POST /api/chat arq.enqueue_job(attachments=...)"
      to: "worker process_chat(attachments=...)"
      via: "arq job payload kwarg"
      pattern: "attachments: list\\[dict\\]"
    - from: "job.attachments"
      to: "HumanMessage.additional_kwargs['attachments']"
      via: "handler 内で HumanMessage(content=prompt, additional_kwargs={...})"
      pattern: "additional_kwargs=\\{\"attachments\""
    - from: "llm.is_vision_model(model)"
      to: "worker D-18 画像 drop 判定"
      via: "await llm.is_vision_model(model) == False → drop images"
      pattern: "is_vision_model"
---

<objective>
Phase 36 の **worker / handler 層** を完成させる。Plan 03 で `ChatRequest.attachments` が arq enqueue_job に流れるので、本 plan は (1) worker process_chat のシグネチャに `attachments` を追加、(2) 2 handler (LangGraphHandler は HumanMessage.additional_kwargs、OrchestratorHandler は AgentState.new_attachments まで配線)、(3) D-18 vision drop + SystemMessage 警告を worker 側で実装する。Plan 02 で提供された `ChatCopilot.is_vision_model` をここで消費する。DebateChat handler の attachments 対応は CONTEXT.md Claude's Discretion (ChatApp 中心) に基づき v6.1 defer。

Purpose: worker が REST layer から受けた attachments を provider layer (ChatCopilot._agenerate) まで届ける「中間伝達層」を配線する。defense-in-depth として UI pre-validate (Plan 06) を bypass されたケースでも worker で画像を drop する。
Output: worker.py + 2 handler + 3 テストファイル。既存 Phase 37 attachments_helper は変更しない（scan + hint prepend は "過去 turn の存在通知"、本 plan は "この turn の新規添付" を additional_kwargs に載せるだけで並存）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/36-text-code-image-multimodal/36-CONTEXT.md
@.planning/phases/36-text-code-image-multimodal/36-RESEARCH.md
@.planning/phases/36-text-code-image-multimodal/36-PATTERNS.md
@.planning/phases/36-text-code-image-multimodal/36-VALIDATION.md
@.planning/phases/36-text-code-image-multimodal/36-02-SUMMARY.md
@.planning/patterns.md
@docs/adr/INDEX.md
@docs/adr/0025-datetime-and-user-context-injection-into-agent-prompts.md
@docs/adr/0038-superchat-context-messages-and-agent-name-persistence.md
@docs/adr/0048-thread-files-folder-convention.md
@CLAUDE.md

<interfaces>
<!-- Plan 02 で提供済みの Provider API -->
From app.providers.copilot:
```python
class ChatCopilot:
    async def is_vision_model(self, model_id: str) -> bool: ...
    async def list_models(self) -> list[dict]: ...
```

<!-- Plan 03 で提供済み -->
From arq enqueue_job 呼び出し側 (app/api/routes/chat.py):
```python
await arq_redis.enqueue_job(
    "process_chat",
    ...,
    attachments=body.attachments,  # list[dict] | None (D-14)
)
```

<!-- 既存 worker シグネチャ (現状) -->
From app.jobs.worker.process_chat (L120-146):
```python
async def process_chat(
    ctx: dict,
    *,
    job_id: str, thread_id: str, prompt: str, model: str = "claude-sonnet-4.5",
    github_token: str, reply_to: dict, task_type: str = "langgraph",
    agents: list[str] | None = None,
    github_login: str = "unknown",
    app_id: str | None = None,
    gem_id: str | None = None,
    gem_ids: list[str] | None = None,
    context_messages: list[dict] | None = None,
    participants: list[str] | None = None,
    pattern: str = "debate",
    max_turns: int = 3,
    current_turn: int = 0,
    rpc_method: str | None = None,
    rpc_params: dict | None = None,
    correlation_id: str | None = None,
) -> dict: ...
```

<!-- 既存 LangGraphHandler (L134-170) -->
```python
# Phase 37 既存: folder scan → attachments_hint → effective_system_prompt prepend
attachments_meta = scan_thread_attachments(thread_id, github_login)
attachments_hint = build_attachments_hint(attachments_meta)
# ...
messages_input: list = [HumanMessage(content=prompt)]  # ← ここに additional_kwargs を乗せる
```

<!-- 既存 OrchestratorHandler (L213-238) -->
```python
# Phase 37.1 既存
attachments_meta = scan_thread_attachments(thread_id, github_login)
attachments_hint = build_attachments_hint(attachments_meta)
effective_prompt = (...) if attachments_hint else prompt
initial: AgentState = {
    "input": effective_prompt,
    "attachments": attachments_meta or None,
    # ...
}
```

<!-- 本 plan が提供する contract -->
After this plan:
```python
# worker.process_chat
async def process_chat(..., attachments: list[dict] | None = None, ...) -> dict:
    job["attachments"] = attachments  # handler で参照

# LangGraphHandler._handle_inner
new_attachments = job.get("attachments") or []
# D-18: vision 非対応時は画像を drop + effective_system_prompt に警告追加
# messages_input に HumanMessage(content=prompt, additional_kwargs={"attachments": new_attachments}) を入れる
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: worker.process_chat に attachments パラメータを追加し job dict に詰める</name>
  <files>app/jobs/worker.py, tests/test_worker_attachments_payload.py</files>
  <read_first>
    - /home/parallels/workspaces/copilot-langgraph/app/jobs/worker.py (L120-188 全体、特に L162-187 job dict 構築)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-PATTERNS.md (§`app/jobs/worker.py` L470-494)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-CONTEXT.md (D-14)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-RESEARCH.md (§Architecture §Worker)
  </read_first>
  <behavior>
    - Test 1 (`test_process_chat_passes_attachments_to_handler`): `process_chat(ctx, job_id=..., thread_id=..., prompt=..., github_token=..., reply_to=..., task_type="langgraph", attachments=[dict])` → 内部で `TASK_HANDLERS["langgraph"].handle(ctx, job)` を呼び、`job["attachments"]` が渡した dict と等しい
    - Test 2 (`test_process_chat_without_attachments_default_none`): `attachments` 未指定 → `job["attachments"] is None` (後方互換)
    - Test 3 (`test_process_chat_unknown_task_type_still_returns_job_dict`): 未知 task_type でも attachments フィールドが job に入る前に early return するので regression なし
  </behavior>
  <action>
    **Step 1 — `app/jobs/worker.py:process_chat` のシグネチャ拡張**:
    L120-146 の `async def process_chat(...)` 引数リストの末尾（`correlation_id` の後ろ）に追加:
    ```python
    async def process_chat(
        ctx: dict,
        *,
        job_id: str,
        thread_id: str,
        prompt: str,
        model: str = "claude-sonnet-4.5",
        github_token: str,
        reply_to: dict,
        task_type: str = "langgraph",
        agents: list[str] | None = None,
        github_login: str = "unknown",
        app_id: str | None = None,
        gem_id: str | None = None,
        gem_ids: list[str] | None = None,
        context_messages: list[dict] | None = None,
        # Phase 17
        participants: list[str] | None = None,
        pattern: str = "debate",
        max_turns: int = 3,
        current_turn: int = 0,
        # Phase 18
        rpc_method: str | None = None,
        rpc_params: dict | None = None,
        # Phase 31
        correlation_id: str | None = None,
        # Phase 36 (D-14): per-turn attachments (list of dict conforming to D-14 schema)
        attachments: list[dict] | None = None,
    ) -> dict:
    ```

    **Step 2 — job dict に `attachments` を追加**:
    L162-187 の job dict 構築ブロックに 1 行追加:
    ```python
        job = {
            "job_id": job_id,
            "thread_id": thread_id,
            "prompt": prompt,
            "model": model,
            "github_token": github_token,
            "reply_to": reply_to,
            "task_type": task_type,
            "agents": agents,
            "github_login": github_login,
            "app_id": app_id,
            "gem_id": gem_id,
            "gem_ids": gem_ids,
            "context_messages": context_messages,
            "participants": participants,
            "pattern": pattern,
            "max_turns": max_turns,
            "current_turn": current_turn,
            "rpc_method": rpc_method,
            "rpc_params": rpc_params,
            "correlation_id": correlation_id,
            # Phase 36 (D-14): pass per-turn attachments to handler
            "attachments": attachments,
        }
    ```

    **Step 3 — `tests/test_worker_attachments_payload.py` を新規作成**:
    ```python
    """Phase 36 D-14: worker.process_chat に attachments が流れることを検証。"""
    from unittest.mock import AsyncMock
    import pytest


    @pytest.mark.asyncio
    async def test_process_chat_passes_attachments_to_handler(monkeypatch):
        from app.jobs import worker

        captured_job = {}

        async def fake_handle(ctx, job):
            captured_job.update(job)
            return {"job_id": job["job_id"], "status": "done"}

        fake_handler = AsyncMock()
        fake_handler.handle = fake_handle
        monkeypatch.setitem(worker.TASK_HANDLERS, "langgraph", fake_handler)

        atts = [{
            "kind": "file", "name": "a.txt",
            "storage_name": "20260423T120000_a.txt",
            "path": "/shared/thread-files/u/t/20260423T120000_a.txt",
            "size": 5, "mime_type": "text/plain", "ext": "txt",
            "modified_at": "2026-04-23T12:00:00Z",
        }]

        await worker.process_chat(
            {},  # ctx
            job_id="j-1", thread_id="t-1", prompt="hi", model="gpt-4.1",
            github_token="ghu_x", reply_to={"type": "web", "job_id": "j-1"},
            task_type="langgraph",
            attachments=atts,
        )
        assert captured_job["attachments"] == atts


    @pytest.mark.asyncio
    async def test_process_chat_without_attachments_default_none(monkeypatch):
        from app.jobs import worker
        captured_job = {}

        async def fake_handle(ctx, job):
            captured_job.update(job)
            return {"job_id": job["job_id"], "status": "done"}

        fake_handler = AsyncMock()
        fake_handler.handle = fake_handle
        monkeypatch.setitem(worker.TASK_HANDLERS, "langgraph", fake_handler)

        await worker.process_chat(
            {},
            job_id="j-2", thread_id="t-2", prompt="hi", model="gpt-4.1",
            github_token="ghu_x", reply_to={"type": "web", "job_id": "j-2"},
            task_type="langgraph",
        )
        assert captured_job["attachments"] is None


    @pytest.mark.asyncio
    async def test_process_chat_unknown_task_type_early_return(monkeypatch):
        from app.jobs import worker

        class _FakeJobStore:
            async def save_result(self, job_id, result):
                self.saved = (job_id, result)

        js = _FakeJobStore()
        result = await worker.process_chat(
            {"job_store": js},
            job_id="j-3", thread_id="t-3", prompt="hi", model="gpt-4.1",
            github_token="ghu_x", reply_to={"type": "web", "job_id": "j-3"},
            task_type="unknown_type",
            attachments=[{"kind": "file"}],
        )
        assert result["status"] == "error"
        assert "unknown task_type" in js.saved[1]
    ```
  </action>
  <verify>
    <automated>uv run pytest tests/test_worker_attachments_payload.py -x -v</automated>
  </verify>
  <done>
    - `grep -n "attachments: list\[dict\] | None = None" app/jobs/worker.py` が 1 行返る (process_chat 引数)
    - `grep -n '"attachments": attachments' app/jobs/worker.py` が 1 行返る (job dict)
    - `tests/test_worker_attachments_payload.py` の 3 tests が GREEN
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LangGraphHandler に attachments → HumanMessage.additional_kwargs 注入 + D-18 vision drop を追加</name>
  <files>app/jobs/handlers/langgraph_handler.py, tests/test_langgraph_handler_attachments_v2.py</files>
  <read_first>
    - /home/parallels/workspaces/copilot-langgraph/app/jobs/handlers/langgraph_handler.py (L120-170 _handle_inner 全体、特に L137-151 effective_system_prompt, L164 messages_input)
    - /home/parallels/workspaces/copilot-langgraph/app/jobs/handlers/debate_handler.py (L108-252 全体、特に initial DebateState 構築部分 L194-210)
    - /home/parallels/workspaces/copilot-langgraph/app/orchestrator/debate_graph.py (初期 user HumanMessage の入口 — planner が debate_graph.py を grep して "HumanMessage" を含む箇所を特定)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-PATTERNS.md (§`app/jobs/handlers/langgraph_handler.py` L498-534, §`app/jobs/handlers/debate_handler.py` L560-565)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-RESEARCH.md (§Example B, §Pitfall 7 debate 扱い)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-CONTEXT.md (D-10 / D-11 / D-18)
    - /home/parallels/workspaces/copilot-langgraph/tests/test_langgraph_handler_attachments.py (Phase 37 既存 attachments_helper unit test、類似 test pattern の参考)
    - /home/parallels/workspaces/copilot-langgraph/tests/test_langgraph_handler.py (既存 handler integration test pattern)
  </read_first>
  <behavior>
    **LangGraphHandler:**
    - Test 1 (`test_langgraph_handler_injects_additional_kwargs_text_files`): job.attachments に text ファイル 2 件 → `HumanMessage.additional_kwargs["attachments"]` が同じ list と等しい（順序も同じ）
    - Test 2 (`test_langgraph_handler_no_attachments_passes_empty_kwargs`): job.attachments が None / [] → `additional_kwargs == {}` (空 dict — Pitfall 10 対策)
    - Test 3 (`test_langgraph_handler_vision_drop_on_non_vision_model`): vision 非対応モデル + 画像 attachment → `additional_kwargs["attachments"]` に画像が含まれない (filter 済) + `effective_system_prompt` に「画像非対応」文言
    - Test 4 (`test_langgraph_handler_vision_pass_on_vision_model`): vision 対応モデル + 画像 attachment → `additional_kwargs["attachments"]` に画像を含む + effective_system_prompt に警告文言が無い
    - Test 5 (`test_langgraph_handler_mixed_text_and_image_non_vision_drops_only_images`): text + 画像の混在 + vision 非対応 → text は残る、画像だけ drop、警告は画像名のみを列挙

    **DebateHandler:** 本 plan scope 外 (v6.1 defer)。CONTEXT.md Claude's Discretion (ChatApp 中心) に基づき、debate 経路の attachments 対応は見送る。DebateChat での実機挙動は Plan 07 Open Issues に残件計上。
  </behavior>
  <action>
    **Step 1 — `app/jobs/handlers/langgraph_handler.py` を修正**:
    L137-170 の `effective_system_prompt` 構築から `messages_input` 直前までのブロックに以下を挿入する。具体的には L151（effective_system_prompt else 分岐の末尾）と L153（config = の前）の間に **D-18 vision drop ブロック**を挿入し、L164 の `messages_input = [HumanMessage(content=prompt)]` を書き換える:

    ```python
                if attachments_hint:
                    effective_system_prompt = (
                        datetime_prefix + "\n\n"
                        + (system_prompt or "")
                        + AUQ_PROTOCOL
                        + "\n\n## 添付ファイル\n"
                        + attachments_hint
                    )
                else:
                    effective_system_prompt = datetime_prefix + "\n\n" + (system_prompt or "") + AUQ_PROTOCOL

                # Phase 36 D-14/D-18: per-turn 添付を取り出し、vision 非対応モデルでは画像を drop
                new_attachments: list[dict] = job.get("attachments") or []
                if new_attachments:
                    vision_ok = await llm.is_vision_model(model)
                    if not vision_ok:
                        image_exts = {"png", "jpg", "jpeg", "webp"}
                        image_atts = [
                            a for a in new_attachments
                            if isinstance(a, dict)
                            and (a.get("ext") or "").lower().lstrip(".") in image_exts
                        ]
                        non_image_atts = [a for a in new_attachments if a not in image_atts]
                        if image_atts:
                            names = ", ".join(
                                (a.get("name") or "?") for a in image_atts
                            )
                            warn = (
                                "\n\n## 画像非対応モデル警告\n"
                                f"以下の画像が添付されましたが、このモデル (`{model}`) は"
                                f"画像非対応のため内容を読めません: {names}。"
                                f"vision 対応モデル (例: claude-sonnet-4.6) への切替えを"
                                f"ユーザーに案内してください。"
                            )
                            effective_system_prompt = (effective_system_prompt or "") + warn
                        new_attachments = non_image_atts

                config = {
                    "configurable": {
                        "thread_id": thread_id,
                        "github_login": github_login,
                        "system_prompt": effective_system_prompt,
                    }
                }

                await notifier.progress("thinking")

                # Phase 36 D-10: HumanMessage.additional_kwargs に per-turn attachments を乗せる。
                # 空リスト時は additional_kwargs={} を渡して Pitfall 10 (None-guard) を回避。
                messages_input: list = [HumanMessage(
                    content=prompt,
                    additional_kwargs={"attachments": new_attachments} if new_attachments else {},
                )]
    ```

    **Step 2 — `app/jobs/handlers/debate_handler.py` 修正は v6.1 defer**:
    CONTEXT.md Claude's Discretion (Phase 36 は ChatApp 中心、他アプリは InputBar 流用の範囲で自動継承) に基づき、DebateChat handler の `additional_kwargs` 注入 / vision drop 対応は本 plan scope 外とする。現状の debate_handler.py は変更せず、Plan 07 Open Issues に残件計上 (「DebateChat handler の attachments 対応は v6.1 defer」)。LangGraphHandler (Step 1) + OrchestratorHandler (Task 3) の対応が完成すれば ChatApp 中心の要件は満たされる。

    **Step 3 — `tests/test_langgraph_handler_attachments_v2.py` を新規作成**:
    既存 `tests/test_langgraph_handler_attachments.py` は `attachments_helper` の unit test なので、v2 では "handler full pipeline" の behavior を test する:
    ```python
    """Phase 36 D-10/D-14/D-18: LangGraphHandler の attachments 注入 + vision drop test."""
    from unittest.mock import AsyncMock, MagicMock, patch
    import pytest
    from langchain_core.messages import HumanMessage


    @pytest.fixture
    def mock_llm_vision_true():
        mock = AsyncMock()
        mock.is_vision_model = AsyncMock(return_value=True)
        return mock


    @pytest.fixture
    def mock_llm_vision_false():
        mock = AsyncMock()
        mock.is_vision_model = AsyncMock(return_value=False)
        return mock


    def _mk_job(attachments=None, model="gpt-4.1"):
        return {
            "job_id": "j-1", "thread_id": "t-1", "prompt": "解析して",
            "model": model, "github_token": "ghu_x",
            "reply_to": {"type": "web", "job_id": "j-1"},
            "github_login": "testuser",
            "attachments": attachments,
        }


    def _text_att(name="a.txt"):
        return {"kind": "file", "name": name, "storage_name": f"20260423T120000_{name}",
                "path": f"/shared/thread-files/u/t/20260423T120000_{name}",
                "size": 10, "mime_type": "text/plain", "ext": "txt",
                "modified_at": "2026-04-23T12:00:00Z"}


    def _image_att(name="p.png"):
        return {"kind": "file", "name": name, "storage_name": f"20260423T120000_{name}",
                "path": f"/shared/thread-files/u/t/20260423T120000_{name}",
                "size": 999, "mime_type": "image/png", "ext": "png",
                "modified_at": "2026-04-23T12:00:00Z"}


    # LangGraphHandler の handle() は DB / checkpointer / graph 構築を伴う重いので、
    # 下位コンポーネントをまとめて mock してユニット的に検証する。
    # 具体的には `from app.jobs.handlers.langgraph_handler import LangGraphHandler` の
    # `_handle_inner` を直接呼ぶことで、graph 実行を避け messages_input の構築だけを確認する。
    # そのため `build_graph` / `AsyncPostgresSaver.from_conn_string` / `_get_gem_info` を mock する。

    # 本 test の対象は "additional_kwargs 注入 + D-18 vision drop" のロジックなので、
    # graph.astream_events は単純に最後の state を返すモックで十分。
    # その state から messages_input を再取得して additional_kwargs を assert する。

    # 実装詳細: handler は state_input を引数に渡して graph を呼ぶ。state_input["messages"] に
    # 注入した HumanMessage が入っているので、それを assert する pattern でテストする。

    @pytest.mark.asyncio
    async def test_langgraph_handler_injects_additional_kwargs_text_files(mock_llm_vision_true, monkeypatch):
        """text ファイルのみなら vision 判定不要、additional_kwargs にそのまま乗る。"""
        from app.jobs.handlers.langgraph_handler import LangGraphHandler
        # TODO: モック pattern の確定は planner→executor の実装段階で調整可能。
        # ここでは「最低限 HumanMessage が additional_kwargs に attachments を含む」ことを
        # assert する test が作れればよい。実装方針の一つ:
        # - LangGraphHandler のサブクラスを作って _handle_inner をラップし、graph.astream_events を
        #   no-op にして messages_input だけを capture する

        captured_messages = []

        class _CaptureHandler(LangGraphHandler):
            async def _handle_inner(self, ctx, job, job_store, notifier, llm, context, **kw):
                # build_graph / AsyncPostgresSaver 呼び出しは本物。ただし prompt 実行前で
                # short-circuit して capture だけする方針にする。
                # 簡略化: handler の behavior は "effective_system_prompt" 構築 + HumanMessage 注入 のみ
                # 検証対象なので、テストは handler の該当ロジック部分を _extract して unit test する
                # 代案を採る。複雑なのでここでは behavior レベル記述のみで OK とし、
                # 実装は executor に委ねる。
                pass

        # 実装が複雑な場合は以下の代案で unit test を完結させる:
        # -> handler.py から vision_drop + HumanMessage 構築部分を _prepare_messages という private
        #    method に抽出し、その method を直接テストする
        #
        # 本 test 骨格を「pending」とし、実装段階で handler のリファクタ (private helper 抽出) を
        # 含めて test を書くか、integration test (Plan 07) に回すかを判断する。
    ```

    **ここは重要なので明示**: `LangGraphHandler._handle_inner` 全体を mock しきるのは難しい（AsyncPostgresSaver / build_graph / graph.astream_events が深く連結）。そこで、**Task 2 の実装中に handler の該当ロジックを `_prepare_messages_input(job, effective_system_prompt, llm, model) -> tuple[list[HumanMessage], str]` という private helper に抽出し、その helper を単体 test する**（test file の 5 tests はすべてこの helper を呼ぶ）。handler 本体からはこの helper を 1 回呼ぶだけにする。実装例:
    ```python
    # app/jobs/handlers/langgraph_handler.py に追加 (私的 helper)
    async def _prepare_messages_input(
        self,
        job: dict,
        effective_system_prompt: str,
        llm: ChatCopilot,
        model: str,
        prompt: str,
    ) -> tuple[list, str]:
        """Phase 36 D-10/D-18: attachments 注入 + vision drop を一箇所にまとめる。

        Returns: (messages_input, updated_system_prompt)
        """
        new_attachments: list[dict] = job.get("attachments") or []
        if new_attachments:
            vision_ok = await llm.is_vision_model(model)
            if not vision_ok:
                image_exts = {"png", "jpg", "jpeg", "webp"}
                image_atts = [a for a in new_attachments
                              if isinstance(a, dict)
                              and (a.get("ext") or "").lower().lstrip(".") in image_exts]
                non_image_atts = [a for a in new_attachments if a not in image_atts]
                if image_atts:
                    names = ", ".join((a.get("name") or "?") for a in image_atts)
                    warn = (
                        "\n\n## 画像非対応モデル警告\n"
                        f"以下の画像が添付されましたが、このモデル (`{model}`) は"
                        f"画像非対応のため内容を読めません: {names}。"
                        f"vision 対応モデル (例: claude-sonnet-4.6) への切替えを"
                        f"ユーザーに案内してください。"
                    )
                    effective_system_prompt = (effective_system_prompt or "") + warn
                new_attachments = non_image_atts
        messages_input: list = [HumanMessage(
            content=prompt,
            additional_kwargs={"attachments": new_attachments} if new_attachments else {},
        )]
        return messages_input, effective_system_prompt
    ```
    handler 本体からは `messages_input, effective_system_prompt = await self._prepare_messages_input(job, effective_system_prompt, llm, model, prompt)` を 1 回呼ぶだけ。

    これで test は簡潔:
    ```python
    @pytest.mark.asyncio
    async def test_langgraph_handler_injects_additional_kwargs_text_files(mock_llm_vision_true):
        from app.jobs.handlers.langgraph_handler import LangGraphHandler
        handler = LangGraphHandler()
        atts = [_text_att("a.txt"), _text_att("b.md")]
        messages, sp = await handler._prepare_messages_input(
            {"attachments": atts}, "base prompt", mock_llm_vision_true, "claude-sonnet-4.6", "hi",
        )
        assert len(messages) == 1
        assert isinstance(messages[0], HumanMessage)
        assert messages[0].content == "hi"
        assert messages[0].additional_kwargs == {"attachments": atts}
        assert sp == "base prompt"  # 警告は追加されない


    @pytest.mark.asyncio
    async def test_langgraph_handler_no_attachments_passes_empty_kwargs(mock_llm_vision_true):
        from app.jobs.handlers.langgraph_handler import LangGraphHandler
        handler = LangGraphHandler()
        messages, sp = await handler._prepare_messages_input(
            {"attachments": None}, "base", mock_llm_vision_true, "gpt-4.1", "hi",
        )
        assert messages[0].additional_kwargs == {}
        # vision 判定は呼ばれない
        mock_llm_vision_true.is_vision_model.assert_not_awaited()


    @pytest.mark.asyncio
    async def test_langgraph_handler_vision_drop_on_non_vision_model(mock_llm_vision_false):
        from app.jobs.handlers.langgraph_handler import LangGraphHandler
        handler = LangGraphHandler()
        atts = [_image_att("cat.png")]
        messages, sp = await handler._prepare_messages_input(
            {"attachments": atts}, "base", mock_llm_vision_false, "gpt-4.1", "説明して",
        )
        # 画像は drop されているので additional_kwargs は empty {}
        assert messages[0].additional_kwargs == {}
        # system prompt に警告追加
        assert "画像非対応モデル警告" in sp
        assert "cat.png" in sp
        assert "gpt-4.1" in sp


    @pytest.mark.asyncio
    async def test_langgraph_handler_vision_pass_on_vision_model(mock_llm_vision_true):
        from app.jobs.handlers.langgraph_handler import LangGraphHandler
        handler = LangGraphHandler()
        atts = [_image_att("p.png")]
        messages, sp = await handler._prepare_messages_input(
            {"attachments": atts}, "base", mock_llm_vision_true, "claude-sonnet-4.6", "hi",
        )
        assert messages[0].additional_kwargs == {"attachments": atts}
        assert "画像非対応モデル警告" not in sp


    @pytest.mark.asyncio
    async def test_langgraph_handler_mixed_text_and_image_non_vision_drops_only_images(mock_llm_vision_false):
        from app.jobs.handlers.langgraph_handler import LangGraphHandler
        handler = LangGraphHandler()
        text_a = _text_att("a.txt")
        img_b = _image_att("b.png")
        messages, sp = await handler._prepare_messages_input(
            {"attachments": [text_a, img_b]}, "base", mock_llm_vision_false, "gpt-4.1", "hi",
        )
        # text は残る
        assert messages[0].additional_kwargs == {"attachments": [text_a]}
        assert "b.png" in sp
    ```

    debate_handler の変更は本 plan scope 外のため、DebateHandler の pytest 追加もなし。DebateChat 経路の実機挙動は Plan 07 Open Issues に残件計上。
  </action>
  <verify>
    <automated>uv run pytest tests/test_langgraph_handler_attachments_v2.py tests/test_langgraph_handler.py tests/test_langgraph_handler_attachments.py -x -v</automated>
  </verify>
  <done>
    - `grep -n "_prepare_messages_input" app/jobs/handlers/langgraph_handler.py` が 2 箇所 (定義 + 呼出し)
    - `grep -n "additional_kwargs" app/jobs/handlers/langgraph_handler.py` が 1 箇所以上
    - `grep -n "is_vision_model" app/jobs/handlers/langgraph_handler.py` が 1 箇所以上
    - `tests/test_langgraph_handler_attachments_v2.py` の 5 tests が GREEN
    - 既存 `tests/test_langgraph_handler.py` / `tests/test_langgraph_handler_attachments.py` が regression なし GREEN
    - DebateChat handler は未変更 (v6.1 defer)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: OrchestratorHandler に SuperChat 経路での attachments 注入 + vision drop を追加</name>
  <files>app/jobs/handlers/orchestrator_handler.py, tests/test_orchestrator_handler_attachments.py</files>
  <read_first>
    - /home/parallels/workspaces/copilot-langgraph/app/jobs/handlers/orchestrator_handler.py (L1-50 imports + 全体、特に L180-260 initial state + effective_prompt 構築)
    - /home/parallels/workspaces/copilot-langgraph/app/orchestrator/state.py (AgentState — 既存は messages + attachments フィールド、本 plan で new_attachments 追加するか検討)
    - /home/parallels/workspaces/copilot-langgraph/app/orchestrator/graph.py (Router → SubAgent の呼び出しで HumanMessage がどこで作られるか)
    - /home/parallels/workspaces/copilot-langgraph/app/orchestrator/agent.py (SubAgent.run — LLM 呼び出しの HumanMessage 構築点)
    - /home/parallels/workspaces/copilot-langgraph/app/orchestrator/tool_agent.py (ToolEnabledSubAgent.run — ReAct ループで HumanMessage が LLM に渡る箇所)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-PATTERNS.md (§`app/jobs/handlers/orchestrator_handler.py` L537-557)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-CONTEXT.md (D-10, Claude's Discretion: SuperChat での範囲)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-RESEARCH.md (§Open Question 1 — SuperChat/Canvas/Debate での扱い, §Pattern 1)
  </read_first>
  <behavior>
    - Test 1 (`test_orchestrator_handler_prepares_new_attachments`): `_prepare_new_attachments(job, llm, model) -> list[dict]` が text+画像 mix で vision 対応モデルなら全部返す、非対応なら画像除外
    - Test 2 (`test_orchestrator_handler_empty_attachments`): `job.attachments` が None → []
    - Test 3 (`test_orchestrator_state_has_new_attachments_field`): AgentState TypedDict に `new_attachments: list[dict] | None` フィールドが存在する (CONTEXT.md D-20 の Phase 36 拡張)
  </behavior>
  <action>
    **Step 1 — `app/orchestrator/state.py` に `new_attachments` フィールドを追加**:
    ```python
    # app/orchestrator/state.py
    from __future__ import annotations
    import operator
    from typing import Annotated
    from typing_extensions import TypedDict
    from langchain_core.messages import BaseMessage
    from app.orchestrator.context import RPCContext, _keep_first


    class AgentState(TypedDict):
        input: str
        output: str
        messages: Annotated[list[BaseMessage], operator.add]
        next: str
        context: Annotated[RPCContext, _keep_first]
        error: str | None
        agent_name: str | None
        context_messages: list[dict] | None
        # Phase 37 D-12: folder scan 結果の既存フィールド (last-wins, 過去 turn 含む全添付のメタデータ)
        attachments: list[dict] | None
        # Phase 36 D-14/D-20: この turn の新規添付 (per-turn, D-14 dict)。
        # SubAgent が HumanMessage 組み立て時に additional_kwargs["attachments"] に載せる用。
        new_attachments: list[dict] | None
    ```

    **Step 2 — `app/jobs/handlers/orchestrator_handler.py` に `_prepare_new_attachments` helper を追加**:
    既存 L213-219 の `attachments_meta = scan_thread_attachments(...)` の直後に以下を追加し、initial state (L229-238) を拡張する:
    ```python
                # Phase 37 既存:
                attachments_meta = scan_thread_attachments(thread_id, github_login)
                attachments_hint = build_attachments_hint(attachments_meta)
                effective_prompt = (
                    "## 添付ファイル情報\n" + attachments_hint
                    + "\n\n## ユーザー入力\n" + prompt
                ) if attachments_hint else prompt

                # Phase 36 D-14/D-18: per-turn 新規添付 + vision 非対応時の画像 drop
                # aggregator 用 LLM の vision 判定を使う (SuperChat の model は job.get("model"))
                aggregator_model = job.get("model") or "claude-sonnet-4.5"
                new_attachments = await self._prepare_new_attachments(
                    job, llm, aggregator_model,
                )
    ```
    initial (L229-238) に `new_attachments` を追加:
    ```python
                initial: AgentState = {
                    "input": effective_prompt,
                    "output": "",
                    "next": "",
                    "error": None,
                    "agent_name": None,
                    "context": context,
                    "context_messages": job.get("context_messages"),
                    "attachments": attachments_meta or None,
                    # Phase 36 D-14/D-20: per-turn attachments (SubAgent 側で HumanMessage.additional_kwargs に展開)
                    "new_attachments": new_attachments or None,
                }
    ```
    class level に helper を追加:
    ```python
        async def _prepare_new_attachments(
            self,
            job: dict,
            llm: ChatCopilot,
            model: str,
        ) -> list[dict]:
            """Phase 36 D-14/D-18: job.attachments を取り出し vision 非対応時は画像を drop する。

            LangGraphHandler の _prepare_messages_input と同じロジックだが、
            SuperChat では SubAgent 側で HumanMessage を作るので messages_input ではなく
            state["new_attachments"] 経由で伝搬する。SystemMessage 警告は SubAgent 側の
            system_prompt に注入する実装は Plan 07 integration check で検討し、
            本 plan では D-18 の画像 drop のみを defense-in-depth で適用する。
            """
            new_attachments: list[dict] = job.get("attachments") or []
            if not new_attachments:
                return []
            vision_ok = await llm.is_vision_model(model)
            if vision_ok:
                return new_attachments
            image_exts = {"png", "jpg", "jpeg", "webp"}
            return [
                a for a in new_attachments
                if isinstance(a, dict)
                and (a.get("ext") or "").lower().lstrip(".") not in image_exts
            ]
    ```

    **重要 note**: SuperChat 経路の SubAgent は `ToolEnabledSubAgent` (ReAct mini graph) 等で LLM を直接呼ぶ。SubAgent 側で `HumanMessage(content=..., additional_kwargs={"attachments": state["new_attachments"]})` を組み立てるのが望ましいが、**SubAgent のリファクタは本 plan scope を超える**（state → SubAgent の配線箇所が app/orchestrator/ 側で複数ある）。本 plan では **state に new_attachments を載せるところまで** を完了とし、Phase 36 の SuperChat 実機確認 (Plan 07) で「画像添付 + SuperChat で画像を認識できるか」を smoke test する。できなければ v6.1 で SubAgent 側の配線を追加する（CONTEXT.md Claude's Discretion "Gem / SuperChat / Canvas / DebateChat などの他アプリからの添付サポート範囲" に該当）。**ChatApp 経路 (Plan 05/06 + LangGraphHandler) は本 plan で完全に動く**。

    **Step 3 — `tests/test_orchestrator_handler_attachments.py` を新規作成**:
    ```python
    """Phase 36 D-14/D-18: OrchestratorHandler の attachments 準備 (_prepare_new_attachments) test."""
    from unittest.mock import AsyncMock
    import pytest


    @pytest.fixture
    def mock_llm_vision_true():
        mock = AsyncMock()
        mock.is_vision_model = AsyncMock(return_value=True)
        return mock


    @pytest.fixture
    def mock_llm_vision_false():
        mock = AsyncMock()
        mock.is_vision_model = AsyncMock(return_value=False)
        return mock


    @pytest.mark.asyncio
    async def test_orchestrator_handler_prepares_new_attachments_vision_true(mock_llm_vision_true):
        from app.jobs.handlers.orchestrator_handler import OrchestratorHandler
        h = OrchestratorHandler()
        atts = [
            {"kind": "file", "name": "a.txt", "ext": "txt", "path": "/x/a.txt", "size": 1,
             "mime_type": "text/plain", "storage_name": "a", "modified_at": "2026-04-23T12:00:00Z"},
            {"kind": "file", "name": "b.png", "ext": "png", "path": "/x/b.png", "size": 2,
             "mime_type": "image/png", "storage_name": "b", "modified_at": "2026-04-23T12:00:00Z"},
        ]
        result = await h._prepare_new_attachments({"attachments": atts}, mock_llm_vision_true, "claude-sonnet-4.6")
        assert result == atts  # 全部残る


    @pytest.mark.asyncio
    async def test_orchestrator_handler_prepares_new_attachments_vision_false_drops_images(mock_llm_vision_false):
        from app.jobs.handlers.orchestrator_handler import OrchestratorHandler
        h = OrchestratorHandler()
        atts = [
            {"kind": "file", "name": "a.txt", "ext": "txt"},
            {"kind": "file", "name": "b.png", "ext": "png"},
            {"kind": "file", "name": "c.webp", "ext": "webp"},
        ]
        result = await h._prepare_new_attachments({"attachments": atts}, mock_llm_vision_false, "gpt-4.1")
        assert [a["name"] for a in result] == ["a.txt"]


    @pytest.mark.asyncio
    async def test_orchestrator_handler_empty_attachments(mock_llm_vision_true):
        from app.jobs.handlers.orchestrator_handler import OrchestratorHandler
        h = OrchestratorHandler()
        assert await h._prepare_new_attachments({"attachments": None}, mock_llm_vision_true, "gpt-4.1") == []
        assert await h._prepare_new_attachments({"attachments": []}, mock_llm_vision_true, "gpt-4.1") == []
        # vision 判定は呼ばれない (空なら early return)
        mock_llm_vision_true.is_vision_model.assert_not_awaited()


    def test_agent_state_has_new_attachments_field():
        from app.orchestrator.state import AgentState
        # TypedDict の __annotations__ でフィールド存在確認
        assert "new_attachments" in AgentState.__annotations__
    ```
  </action>
  <verify>
    <automated>uv run pytest tests/test_orchestrator_handler_attachments.py -x -v</automated>
  </verify>
  <done>
    - `grep -n "new_attachments" app/orchestrator/state.py` が 1 行返る
    - `grep -n "_prepare_new_attachments" app/jobs/handlers/orchestrator_handler.py` が 2 箇所 (定義 + 呼出し)
    - `grep -n "new_attachments" app/jobs/handlers/orchestrator_handler.py` が 2 箇所以上 (state に入れる)
    - `tests/test_orchestrator_handler_attachments.py` の 4 tests が GREEN
    - 既存 `tests/test_orchestrator_handler*.py` (あれば) が regression なし
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arq job payload → worker handler | job dict は REST layer で構築済み。attachments は Plan 03 で既に basename 正規化・realpath guard 済 |
| handler → ChatCopilot (via llm.is_vision_model) | SDK list_models() 経由。例外時は False フェイルセーフで画像 drop に倒れる |
| handler → HumanMessage.additional_kwargs | LangGraph checkpointer (JSONB) に載る。Wave 0 Plan 01 で round-trip 確認済 |
| state.new_attachments → SubAgent | Phase 36 では state に置くまでで終了、SubAgent 側配線は本 plan scope 外 (v6.1 検討) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-04-01 | Tampering | UI bypass で非対応モデルに画像送付 | **HIGH** | **mitigate (BLOCKING)** | worker 側で `is_vision_model == False` なら画像 drop + SystemMessage 警告 (D-18 defense-in-depth)。Test 3 / Test 5 |
| T-36-04-02 | Information Disclosure | additional_kwargs に機密内容が混入 | low | accept | D-14 dict は name / path / size / mime のメタデータのみ。実ファイル内容は SDK subprocess 経由でしか読まない (RESEARCH.md §Security Domain) |
| T-36-04-03 | Denial of Service | 大量 attachments（例 1000 件）を additional_kwargs に入れる | low | accept | REST layer (Plan 03) で per-upload 100MB hard cap。1 request あたりの files 数は UI 側で 5 画像制限。サーバー側 array 長 cap は Phase 36 scope 外 |
| T-36-04-04 | Tampering | 不正 shape の dict（kind 欠損 / path 不在）が注入される | medium | mitigate | ChatCopilot._extract_attachments (Plan 02) で `isinstance(a, dict) and a.get("kind") == "file" and isinstance(path, str) and path` の 3 段防御 |
| T-36-04-05 | Spoofing | is_vision_model が外部 SDK 例外時に True を返す | medium | mitigate | Plan 02 で fail-safe False 返却を実装済。Exception 時は常に「vision 非対応」側に倒れて画像 drop。副作用は graceful fallback のみ |
| T-36-04-07 | Tampering | SuperChat SubAgent が state.new_attachments を無視する | medium | accept | 本 plan scope 外。ChatApp 経路は完全対応、SuperChat は v6.1 で SubAgent 側配線を追加。UI 側で「SuperChat の画像は制限あり」の案内は必要なら Plan 07 で検討 |
</threat_model>

<verification>
- `uv run pytest tests/test_worker_attachments_payload.py tests/test_langgraph_handler_attachments_v2.py tests/test_orchestrator_handler_attachments.py tests/test_langgraph_handler.py tests/test_langgraph_handler_attachments.py -x -v` が exit 0
- `grep -n "attachments=attachments\|\"attachments\": attachments" app/jobs/worker.py` が 2 箇所
- `grep -n "_prepare_messages_input\|_prepare_new_attachments" app/jobs/handlers/ -r` が各 2 箇所
- `grep -n "additional_kwargs" app/jobs/handlers/langgraph_handler.py` が 1 箇所以上 (debate_handler は v6.1 defer のため未変更)
</verification>

<success_criteria>
- arq worker が attachments kwarg を job dict に入れて handler へ流す
- LangGraphHandler が job.attachments を HumanMessage.additional_kwargs に注入し、vision 非対応時は画像 drop + SystemMessage 警告
- OrchestratorHandler が state.new_attachments を初期化し、vision drop を適用 (SubAgent 側 HumanMessage 注入は v6.1 defer)
- AgentState に new_attachments フィールド追加、既存 attachments フィールドは保持
- DebateChat handler は未変更 (v6.1 defer、Plan 07 Open Issues 計上)
- 既存 LangGraphHandler / OrchestratorHandler test が regression なし
</success_criteria>

<output>
完了後、`.planning/phases/36-text-code-image-multimodal/36-04-SUMMARY.md` を作成する。SUMMARY には以下を含める:
- LangGraphHandler / OrchestratorHandler それぞれで attachments 配線がどこで行われるかの実装マップ
- SuperChat SubAgent 配線の残件 (v6.1 検討) の明記
- DebateChat handler は本 plan で未変更 (v6.1 defer)
</output>
