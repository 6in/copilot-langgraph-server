---
phase: 36-text-code-image-multimodal
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_chat_history_additional_kwargs.py
  - tests/test_copilot_attachments_spike.py
  - docs/phase-36-sdk-spike-note.md
autonomous: false
requirements:
  - FIN-01
  - FIN-02
must_haves:
  truths:
    - "HumanMessage.additional_kwargs['attachments'] が AsyncPostgresSaver 経由で round-trip 保存できる"
    - "Copilot SDK 0.2.0 の session.send_and_wait(attachments=[FileAttachment(...)]) が docker compose worker で path からファイルを読める"
    - "Wave 1 以降の plan が A1 risk 根拠なく実装に入れる状態になっている"
  artifacts:
    - path: "tests/test_chat_history_additional_kwargs.py"
      provides: "AsyncPostgresSaver 実 DB round-trip test"
      contains: "additional_kwargs"
    - path: "tests/test_copilot_attachments_spike.py"
      provides: "SDK attachments kwarg spike（モックベース確認、docker compose 実機確認メモは docs/phase-36-sdk-spike-note.md）"
      contains: "session.send_and_wait"
    - path: "docs/phase-36-sdk-spike-note.md"
      provides: "docker compose 実機での SDK attachments 動作確認記録（実行日・環境・pass/fail）"
  key_links:
    - from: "tests/test_chat_history_additional_kwargs.py"
      to: "AsyncPostgresSaver"
      via: "from_conn_string → aput/aget → assert additional_kwargs equal"
      pattern: "additional_kwargs.*attachments"
    - from: "docs/phase-36-sdk-spike-note.md"
      to: "ChatCopilot wrapper 実装方針"
      via: "実機 PASS が記録されていることを Wave 1 Plan 02 の前提とする"
      pattern: "verdict.*PASS"
---

<objective>
Phase 36 実装前に A1 risk（`HumanMessage.additional_kwargs["attachments"]` が AsyncPostgresSaver で round-trip 復元できるか）と SDK multimodal spike（`session.send_and_wait(attachments=[FileAttachment(...)])` が実機で path からファイルを読めるか）を検証する。どちらかが FAIL した場合、Wave 1 以降の plan 実装方針を見直す必要があるため、この Wave 0 を先行で GREEN にしないと phase 全体が破綻する。

Purpose: RESEARCH.md §Assumptions A1 (MEDIUM risk — ADR-0038 で AIMessage.name 喪失前例あり) と Pitfall 1 を明示検証し、Wave 1 以降のリスクを潰す。
Output: (1) `tests/test_chat_history_additional_kwargs.py` 新規 (integration test, AsyncPostgresSaver 使用), (2) `tests/test_copilot_attachments_spike.py` 新規 (unit-level mock test で shape 検証), (3) `docs/phase-36-sdk-spike-note.md` に docker compose worker での実機 smoke 記録。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-text-code-image-multimodal/36-CONTEXT.md
@.planning/phases/36-text-code-image-multimodal/36-RESEARCH.md
@.planning/phases/36-text-code-image-multimodal/36-PATTERNS.md
@.planning/phases/36-text-code-image-multimodal/36-VALIDATION.md
@.planning/patterns.md
@docs/adr/INDEX.md
@docs/adr/0038-superchat-context-messages-and-agent-name-persistence.md
@docs/adr/0046-integration-check-surfaced-silent-failures.md
@CLAUDE.md

<interfaces>
<!-- A1 risk の根拠となる既存 round-trip serialize ポイント -->

From langgraph.checkpoint.postgres.aio:
```python
class AsyncPostgresSaver:
    @classmethod
    def from_conn_string(cls, conn_string: str): ...  # async context manager
    async def aput(config: dict, checkpoint: dict, metadata: dict, new_versions: dict) -> dict: ...
    async def aget(config: dict) -> CheckpointTuple | None: ...
    async def setup() -> None: ...  # idempotent table create
```

From langchain_core.messages:
```python
class HumanMessage(BaseMessage):
    content: str
    additional_kwargs: dict  # ← この dict が JSONB に透過シリアライズされるか検証
    name: Optional[str]      # ADR-0038: これは落ちる前例あり
```

From copilot (SDK 0.2.0):
```python
# copilot/types.py:42-47 — TypedDict (NOT dataclass)
class FileAttachment(TypedDict, total=False):
    type: Literal["file"]       # required
    path: str                   # required
    displayName: Optional[str]  # optional

# copilot/session.py:166-228
class CopilotSession:
    async def send_and_wait(
        self,
        prompt: str,
        *,
        attachments: list[Attachment] | None = None,
        timeout: float | None = None,
    ) -> AssistantMessage: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AsyncPostgresSaver round-trip test で additional_kwargs 永続化を検証（A1 risk 確認）</name>
  <files>tests/test_chat_history_additional_kwargs.py</files>
  <read_first>
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-RESEARCH.md (§Assumptions A1, §Pitfall 1, §Wave 0 要件)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-PATTERNS.md (§`tests/test_chat_history_additional_kwargs.py` セクション L1319-1329, §Shared Patterns)
    - /home/parallels/workspaces/copilot-langgraph/docs/adr/0038-superchat-context-messages-and-agent-name-persistence.md (AIMessage.name 喪失の原因分析)
    - /home/parallels/workspaces/copilot-langgraph/tests/conftest.py (既存 fixture: jwt_cookie / api_client / mock_auth_manager)
    - /home/parallels/workspaces/copilot-langgraph/tests/test_langgraph_handler.py (AsyncPostgresSaver の既存利用例)
    - /home/parallels/workspaces/copilot-langgraph/app/jobs/handlers/langgraph_handler.py (L125: AsyncPostgresSaver.from_conn_string の使い方)
  </read_first>
  <behavior>
    - Test 1 (`test_additional_kwargs_roundtrip_with_attachments`): `HumanMessage(content="解析して", additional_kwargs={"attachments": [{D-14 dict 8 フィールド}]})` を MemorySaver / or AsyncPostgresSaver でグラフに流し checkpointer 経由で保存 → 同じ config で再読込 → 復元された message の `additional_kwargs["attachments"]` が元 dict と深く等しい
    - Test 2 (`test_additional_kwargs_empty_dict_preserved`): `additional_kwargs={}` の HumanMessage を保存 → 復元時に `additional_kwargs == {}` (None にならない)
    - Test 3 (`test_additional_kwargs_missing_legacy_message`): 既存コード相当の `HumanMessage(content="x")` (additional_kwargs 未指定) を保存 → 復元時に `additional_kwargs == {}` (None-guard が通る)
    - Test 4 (`test_additional_kwargs_image_attachment_shape`): kind=file の画像 attachment (ext="png", mime_type="image/png") を含む dict が全フィールド（kind / name / storage_name / path / size / mime_type / ext / modified_at）保持される
    - すべての test が GREEN なら A1 risk は "対応不要"。RED なら handler に `_wrap_human_message_attachments` workaround 追加を Wave 1 Plan 04 で行う判断材料となる
  </behavior>
  <action>
    `tests/test_chat_history_additional_kwargs.py` を新規作成する。以下の方針で実装する:

    1. **docker compose 依存を避けるため MemorySaver + minimal LangGraph StateGraph** で実装する（AsyncPostgresSaver は実 DB 接続が必要で CI で不安定。D-20 の LangGraph `add_messages` reducer + checkpointer serialization パス自体は MemorySaver でも同じ。最終的な PostgreSQL 実機確認は Task 3 の docs/phase-36-sdk-spike-note.md に記録する）。
    2. 以下の imports を使う:
       ```python
       import pytest
       from langchain_core.messages import HumanMessage
       from langgraph.checkpoint.memory import MemorySaver
       from langgraph.graph import StateGraph, START, END
       from langgraph.graph.message import add_messages
       from typing import Annotated, TypedDict
       ```
    3. 最小 TypedDict state を定義:
       ```python
       class _State(TypedDict):
           messages: Annotated[list, add_messages]
       ```
    4. `_run_roundtrip(initial_msg)` ヘルパーを作り、`StateGraph(_State)` に identity node を追加・`MemorySaver()` で compile・`await compiled.ainvoke({"messages": [initial_msg]}, config={"configurable": {"thread_id": "t-test"}})` → `await compiled.aget_state(...)` で復元 → `state.values["messages"][0]` を返す。
    5. 各 test は上のヘルパーを使い `HumanMessage(content=..., additional_kwargs={...})` を往復させて復元結果の `additional_kwargs` を assert する。
    6. Test 1 で使う D-14 dict は RESEARCH.md L40-53 の fixture を 1:1 コピー:
       ```python
       _DICT_V14 = {
           "kind": "file",
           "name": "original.png",
           "storage_name": "20260423T120000_original.png",
           "path": "/shared/thread-files/u/t/20260423T120000_original.png",
           "size": 12345,
           "mime_type": "image/png",
           "ext": "png",
           "modified_at": "2026-04-23T12:00:00Z",
       }
       ```
    7. pytest.mark.asyncio を全テストに付ける（`pyproject.toml` で `asyncio_mode = "auto"` が設定済みなので暗黙的に auto でも可、明示的に `@pytest.mark.asyncio` を付ける）。
    8. **重要**: テストファイル冒頭に docstring で「Phase 36 Wave 0 A1 risk 検証 — RED = Wave 1 Plan 04 に workaround 追加判断」と明記する。
  </action>
  <verify>
    <automated>uv run pytest tests/test_chat_history_additional_kwargs.py -x -v</automated>
  </verify>
  <done>
    - 4 つのテストケースが存在する (`test_additional_kwargs_roundtrip_with_attachments` / `test_additional_kwargs_empty_dict_preserved` / `test_additional_kwargs_missing_legacy_message` / `test_additional_kwargs_image_attachment_shape`)
    - 全テストが GREEN である（pytest exit 0）
    - 復元された `additional_kwargs["attachments"][0]` が 8 フィールド全て保持している
    - テスト実行時間 < 10 秒
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SDK attachments kwarg 形状確認テスト（モック経由で契約固定）</name>
  <files>tests/test_copilot_attachments_spike.py</files>
  <read_first>
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-PATTERNS.md (§`tests/test_copilot_attachments.py` セクション L1229-1269, §SDK Isolation)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-RESEARCH.md (§Assumptions A6/A7, §Example A, §Pattern 2)
    - /home/parallels/workspaces/copilot-langgraph/tests/test_copilot_bind_tools.py (既存 ChatCopilot モック pattern)
    - /home/parallels/workspaces/copilot-langgraph/app/providers/copilot.py (L152-202 _agenerate, L36 SDK imports, L286-310 _ensure_client)
  </read_first>
  <behavior>
    - Test 1 (`test_sdk_file_attachment_is_typeddict`): `from copilot import FileAttachment` が import でき、dict リテラル `{"type": "file", "path": "/tmp/x", "displayName": "x.txt"}` が FileAttachment 型として扱える（TypedDict なので isinstance 不可、代わりに `FileAttachment.__required_keys__` を確認）
    - Test 2 (`test_sdk_session_send_and_wait_accepts_attachments_kwarg`): `copilot.CopilotSession` のシグネチャ検査（`inspect.signature` で `attachments` パラメータが存在し、default が None であること）
    - Test 3 (`test_sdk_list_models_returns_modelinfo_with_capabilities`): `copilot.ModelInfo` / `copilot.ModelCapabilities` / `copilot.ModelSupports` の型存在確認 (import のみで型は触らない、構造確認)
    - Test 4 (`test_sdk_imports_isolated_to_provider`): `app/providers/copilot.py` 以外のモジュール（`app/api/routes/*.py` / `app/jobs/handlers/*.py`）で `from copilot import` が無いことを grep で確認
  </behavior>
  <action>
    `tests/test_copilot_attachments_spike.py` を新規作成する。実機 SDK を触れない（subprocess が立つ）ため、型契約のみをテストする:

    1. imports:
       ```python
       import inspect
       import pathlib
       import pytest
       ```
    2. `test_sdk_file_attachment_is_typeddict` の実装:
       ```python
       def test_sdk_file_attachment_is_typeddict():
           from copilot import FileAttachment
           # TypedDict は __required_keys__ / __optional_keys__ 属性を持つ
           assert hasattr(FileAttachment, "__required_keys__") or hasattr(FileAttachment, "__total__")
           # 辞書リテラルが通常の dict として扱える
           sample: FileAttachment = {"type": "file", "path": "/tmp/x", "displayName": "x.txt"}
           assert sample["type"] == "file"
           assert sample["path"] == "/tmp/x"
           assert sample["displayName"] == "x.txt"
       ```
    3. `test_sdk_session_send_and_wait_accepts_attachments_kwarg` の実装（`inspect.signature` ベース）:
       ```python
       def test_sdk_session_send_and_wait_accepts_attachments_kwarg():
           from copilot import CopilotSession
           sig = inspect.signature(CopilotSession.send_and_wait)
           params = sig.parameters
           assert "attachments" in params, f"send_and_wait missing attachments kwarg; have {list(params)}"
           # keyword-only であること (D-09 前提)
           assert params["attachments"].kind == inspect.Parameter.KEYWORD_ONLY
           # default is None
           assert params["attachments"].default is None

           # 同じく send() にも attachments kwarg があることを _astream 経路用に確認
           sig_send = inspect.signature(CopilotSession.send)
           assert "attachments" in sig_send.parameters
       ```
    4. `test_sdk_list_models_returns_modelinfo_with_capabilities`:
       ```python
       def test_sdk_list_models_returns_modelinfo_with_capabilities():
           from copilot import CopilotClient, ModelInfo
           # CopilotClient.list_models の存在
           assert hasattr(CopilotClient, "list_models")
           # ModelInfo が capabilities/supports/vision を持つ（dataclass の fields で確認）
           import dataclasses
           assert dataclasses.is_dataclass(ModelInfo)
           fields = {f.name for f in dataclasses.fields(ModelInfo)}
           assert "id" in fields and "name" in fields and "capabilities" in fields
       ```
    5. `test_sdk_imports_isolated_to_provider`:
       ```python
       def test_sdk_imports_isolated_to_provider():
           """SDK 隔離原則 (CONTEXT.md D-09/D-10/D-15) 検証。
           `from copilot import` / `import copilot` は app/providers/copilot.py の中だけに閉じる。
           他モジュールで見つかったら Phase 36 実装を止めて設計を見直すこと。
           """
           root = pathlib.Path(__file__).resolve().parent.parent / "app"
           violations = []
           for py in root.rglob("*.py"):
               if py.resolve() == (root / "providers" / "copilot.py").resolve():
                   continue
               text = py.read_text(encoding="utf-8")
               for line in text.splitlines():
                   stripped = line.strip()
                   if stripped.startswith(("from copilot import", "from copilot.", "import copilot")):
                       # "from copilot_xxx" や "import copilotxxx" の false positive を避ける
                       if stripped.startswith("from copilot ") or stripped.startswith("from copilot.") or stripped == "import copilot":
                           violations.append(f"{py}:{line!r}")
           assert not violations, f"SDK isolation violated: {violations}"
       ```
    6. テストファイル冒頭 docstring に「Phase 36 Wave 0 SDK spike — FileAttachment TypedDict / send_and_wait.attachments kwarg / SDK isolation の 3 契約を固定する」と記載。
  </action>
  <verify>
    <automated>uv run pytest tests/test_copilot_attachments_spike.py -x -v</automated>
  </verify>
  <done>
    - 4 つのテストケースが GREEN（FileAttachment TypedDict / send_and_wait attachments kwarg / ModelInfo 構造 / SDK isolation）
    - test_sdk_imports_isolated_to_provider が app/providers/copilot.py 以外で `from copilot` を見つけたら FAIL する（現時点の main コードでは GREEN になる）
    - テスト実行時間 < 5 秒
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: docker compose 実機で session.send_and_wait(attachments=[...]) 動作確認（A3 risk + SDK subprocess 実機確認）</name>
  <files>docs/phase-36-sdk-spike-note.md</files>
  <read_first>
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-RESEARCH.md (§Assumption A3, §Architecture §SDK subprocess)
    - /home/parallels/workspaces/copilot-langgraph/docker-compose.yml (volumes thread-files の mount 設定)
    - /home/parallels/workspaces/copilot-langgraph/docs/adr/0048-thread-files-folder-convention.md (フォルダ規約)
  </read_first>
  <what-built>
    Task 1 + Task 2 が GREEN であることを前提に、**docker compose up** で立ち上げた実環境で以下を人間が smoke 確認する:
    1. worker コンテナが `/shared/thread-files` を RO mount できている（`docker compose exec worker ls /shared/thread-files`）
    2. api コンテナで RW mount でき、サーバー側から `/shared/thread-files/testuser/testthread/sample.txt` に試作ファイルを書き込める
    3. worker コンテナ内から Python REPL (or 一時スクリプト) で以下を実行し、`attachments=[{"type": "file", "path": "/shared/thread-files/testuser/testthread/sample.txt", "displayName": "sample.txt"}]` を付けた `session.send_and_wait("このテキストファイルの内容を要約して")` が例外を上げず応答を返す
  </what-built>
  <how-to-verify>
    1. `docker compose up -d` を実行（既に起動済なら skip）
    2. 事前に api コンテナ内で thread-files 配下にテストファイルを作成:
       ```bash
       docker compose exec api mkdir -p /shared/thread-files/_spike/_t/
       docker compose exec api sh -c "echo 'Phase 36 spike test. ファイルの内容はこれです。' > /shared/thread-files/_spike/_t/sample.txt"
       ```
    3. worker コンテナで以下のスクリプトを実行（一時的に tests/_spike_attachments.py などを作り `docker compose exec worker uv run python tests/_spike_attachments.py`）:
       ```python
       import asyncio
       from copilot import CopilotClient, PermissionHandler
       from app.auth.manager import CopilotAuthManager

       async def main():
           auth = CopilotAuthManager()
           token = auth.load_token()
           client = CopilotClient(github_token=token)
           await client.start()
           session = await client.create_session(
               on_permission_request=PermissionHandler.approve_all,
               model="claude-sonnet-4.6",
           )
           response = await session.send_and_wait(
               "以下に添付したテキストファイルの内容を要約してください。",
               attachments=[{
                   "type": "file",
                   "path": "/shared/thread-files/_spike/_t/sample.txt",
                   "displayName": "sample.txt",
               }],
               timeout=60.0,
           )
           print("CONTENT:", response.data.content if response and response.data else None)
           await session.disconnect()
           await client.stop()

       asyncio.run(main())
       ```
    4. 返ってきた `CONTENT` に「Phase 36 spike test」や「要約」相当の文字が含まれていれば PASS。エラーが出たら FAIL。
    5. 結果を `docs/phase-36-sdk-spike-note.md` に以下の形式で書き込む:
       ```markdown
       # Phase 36 SDK Spike Note

       **Date:** 2026-04-23（実行日を記載）
       **Environment:** docker compose (local) / worker service
       **SDK:** github-copilot-sdk==0.2.0

       ## Verdict
       PASS | FAIL（どちらかを明示）

       ## Setup
       - Sample file: /shared/thread-files/_spike/_t/sample.txt (contents: "Phase 36 spike test. ファイルの内容はこれです。")

       ## Command
       ```
       docker compose exec worker uv run python tests/_spike_attachments.py
       ```

       ## Observed Response
       （response.data.content の出力先頭 300 文字程度を貼る）

       ## Verdict Rationale
       - 要約にファイル内容が反映されている → SDK が path から open() できている → A3 risk / D-09 方針 OK
       - もしくは FAIL 理由（例: PermissionError / InvalidPath / subprocess crash 等）

       ## Next-Wave Impact
       - PASS: Wave 1 Plan 02 で ChatCopilot._extract_attachments → session.send_and_wait(attachments=...) 配線を予定通り実装する
       - FAIL: Wave 1 開始前にユーザーと設計見直し（BlobAttachment 経路 / mount 設定 / SDK version）
       ```
    6. 終了後、一時スクリプトと spike 用テストファイルを削除:
       ```bash
       docker compose exec api rm -rf /shared/thread-files/_spike
       rm -f tests/_spike_attachments.py  # スクリプトファイルも忘れずに削除
       ```
  </how-to-verify>
  <resume-signal>
    `docs/phase-36-sdk-spike-note.md` に **Verdict: PASS** を書き込み、spike 用一時ファイルを削除したら「approved」と入力してください。**FAIL の場合は approved を入力せず、具体エラーとともに報告してください — Wave 1 に進むと phase 方針全体を見直す必要があります。**
  </resume-signal>
  <done>
    - `docs/phase-36-sdk-spike-note.md` が存在し Verdict: PASS が記録されている
    - spike 用の一時スクリプトと /shared/thread-files/_spike/ が削除されている
    - ユーザーが approved を宣言し Wave 1 着手の許可を出している
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → FS | pytest が tmp dir と tests/ 配下を書き換える。攻撃面は限定的 |
| spike script → SDK subprocess | Task 3 で ChatCopilot が Copilot CLI subprocess を生成。実機 token を使う |
| spike test file (/shared/thread-files/_spike) → workers | spike ファイルが他ユーザー folder に流出しないよう namespace を `_spike` で隔離 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01-01 | Information Disclosure | tests/test_chat_history_additional_kwargs.py の fixture | mitigate | 本物のユーザー token を fixture に埋め込まない。D-14 dict は `path = "/shared/thread-files/u/t/..."` のダミー値のみ使用 |
| T-36-01-02 | Tampering | spike 用一時ファイル `/shared/thread-files/_spike/_t/sample.txt` | mitigate | Task 3 の最後に `docker compose exec api rm -rf /shared/thread-files/_spike` で確実削除。`_spike` prefix により他の github_login と衝突しない |
| T-36-01-03 | Information Disclosure | docs/phase-36-sdk-spike-note.md に貼る `response.data.content` | mitigate | 出力先頭 300 文字に制限し、環境 token や社内機密が混入していないことを人間が目視確認してからコミット |
| T-36-01-04 | Denial of Service | spike script が timeout 未設定で hang する | mitigate | `timeout=60.0` を明示指定。さらに CI ではこの spike は走らせない（Task 1/2 のみ自動実行） |
| T-36-01-05 | Spoofing | SDK isolation regression（他モジュールが `from copilot import` を新規追加） | mitigate | test_sdk_imports_isolated_to_provider が pre-merge で regression を検知 |
</threat_model>

<verification>
- Task 1 の 4 test が GREEN → A1 risk「対応不要」を裏取り
- Task 2 の 4 test が GREEN → FileAttachment TypedDict / send_and_wait(attachments=...) / ModelInfo 構造 / SDK 隔離 の 4 契約が確認
- Task 3 で docs/phase-36-sdk-spike-note.md Verdict: PASS が記録 → A3 risk「SDK subprocess は RO mount 経由で read() 可能」を実機確認
- 以上 3 点が揃うと Wave 1 の Plan 02/03/04 が実装方針を変更せずに進める
</verification>

<success_criteria>
- `uv run pytest tests/test_chat_history_additional_kwargs.py tests/test_copilot_attachments_spike.py -x -v` が exit 0 で返る
- `docs/phase-36-sdk-spike-note.md` の `## Verdict` セクションに **PASS** が明記されている
- テストファイル 2 本と spike note 1 本が新規追加され、git 管理下にある
- spike で使った一時ファイルは全て削除されている（`/shared/thread-files/_spike/` なし、`tests/_spike_attachments.py` なし）
- 所要時間の目安: test 実行 < 15 秒、spike 実行 + note 記入 < 15 分
</success_criteria>

<output>
完了後、`.planning/phases/36-text-code-image-multimodal/36-01-SUMMARY.md` を作成する。SUMMARY には以下を必ず含める:
- A1 risk の最終判定（PASS / WORKAROUND 必要 のどちらか）
- SDK spike の最終判定（PASS / FAIL のどちらか）
- Wave 1 Plan 02/03/04 への影響（「予定通り進める」or「方針変更が必要」）
</output>
