---
phase: 36-text-code-image-multimodal
plan: 02
type: execute
wave: 1
depends_on:
  - 01
files_modified:
  - app/providers/copilot.py
  - app/api/routes/models.py
  - app/api/routes/attachments.py
  - app/api/main.py
  - tests/test_copilot_attachments.py
  - tests/test_api_models_route.py
autonomous: true
requirements:
  - FIN-01
  - FIN-02
must_haves:
  truths:
    - "ChatCopilot._agenerate と _astream が最後の HumanMessage から additional_kwargs['attachments'] を取り出し SDK FileAttachment に変換して渡せる"
    - "ChatCopilot.list_models() が SDK ModelInfo を D-14 準拠 dict のリストに変換して返す"
    - "ChatCopilot.is_vision_model(model_id) が vision: True/False を返す（未取得 / エラー時は False フェイルセーフ）"
    - "GET /api/models が JWT 認証下で [{id, name, vision, vision_limits, billing_multiplier}, ...] を TTL 1h キャッシュで返す"
    - "main.py で attachments / models の両 router が include 済みで、他 route からの collision なく起動する"
    - "copilot.* の import が app/providers/copilot.py 以外に無い（SDK 隔離原則）"
  artifacts:
    - path: "app/providers/copilot.py"
      provides: "_extract_attachments / list_models / is_vision_model ヘルパー + _agenerate / _astream の attachments 配線"
      contains: "_extract_attachments"
    - path: "app/api/routes/models.py"
      provides: "GET /api/models (TTL 1h キャッシュ, JWT 認証)"
      exports: ["router"]
    - path: "app/api/routes/attachments.py"
      provides: "stub router (Plan 03 で中身を実装) — main.py include のため空 APIRouter だけ先に作る"
      exports: ["router"]
    - path: "app/api/main.py"
      provides: "include_router(attachments.router) + include_router(models.router) 追加"
      contains: "attachments.router"
  key_links:
    - from: "ChatCopilot._agenerate"
      to: "session.send_and_wait(attachments=...)"
      via: "_extract_attachments(messages) ヘルパー"
      pattern: "attachments=sdk_atts"
    - from: "ChatCopilot._astream"
      to: "session.send(attachments=...)"
      via: "_extract_attachments(messages) ヘルパー"
      pattern: "attachments=sdk_atts"
    - from: "GET /api/models route"
      to: "ChatCopilot.list_models"
      via: "request.app.state.llm.list_models()"
      pattern: "await.*list_models"
---

<objective>
Phase 36 の **SDK 配線コア層** を実装する。ChatCopilot wrapper を拡張して (1) HumanMessage.additional_kwargs["attachments"] → copilot.FileAttachment TypedDict への変換、(2) SDK `list_models()` の dict 化ヘルパー、(3) vision 判定ヘルパー、(4) `GET /api/models` REST エンドポイント、(5) main.py での両新規 router の include を一括で揃える。Plan 03（upload routes 実装）と Plan 04（worker/handlers 配線）が本 plan の成果物（`ChatCopilot.is_vision_model` / `llm.list_models()` / `attachments.router` stub / `models.router`）に直接依存する。

Purpose: SDK 隔離原則（D-09/D-10/D-15）を守りつつ、Phase 36 の multimodal 配線の「SDK に面する層」を一箇所で完結させる。これで Plan 03 以降は D-14 統一 dict スキーマのみで実装できる。
Output: ChatCopilot に 3 ヘルパー追加 + models route 新規 + attachments route stub + main.py ルート登録 + provider/models route 両方の unit / integration test。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/36-text-code-image-multimodal/36-CONTEXT.md
@.planning/phases/36-text-code-image-multimodal/36-RESEARCH.md
@.planning/phases/36-text-code-image-multimodal/36-PATTERNS.md
@.planning/phases/36-text-code-image-multimodal/36-VALIDATION.md
@.planning/phases/36-text-code-image-multimodal/36-01-SUMMARY.md
@.planning/patterns.md
@docs/adr/INDEX.md
@docs/adr/0021-langgraph-bind-tools-toolnode-via-prompt-engineering.md
@docs/adr/0031-copilot-sdk-token-streaming-three-layer-plumbing.md
@docs/adr/0014-phase17-security-hardening-jwt-blocklist-redis-and-endpoint-auth.md
@CLAUDE.md

<interfaces>
<!-- SDK 側の契約 (Wave 0 Plan 01 で確認済み) -->

From copilot (SDK 0.2.0):
```python
# copilot/types.py:42-47 — TypedDict
class FileAttachment(TypedDict, total=False):
    type: Literal["file"]
    path: str
    displayName: Optional[str]

# copilot/session.py:166-228
class CopilotSession:
    async def send_and_wait(self, prompt: str, *, attachments: list[Attachment] | None = None, timeout: float | None = None) -> AssistantMessage: ...
    async def send(self, prompt: str, *, attachments: list[Attachment] | None = None) -> None: ...

# copilot/client.py:976-1024
class CopilotClient:
    async def list_models(self) -> list[ModelInfo]: ...
    # 内部 _models_cache 有り、一度取得後はキャッシュから返す

# copilot/types.py:748-956 — dataclass
@dataclass
class ModelInfo:
    id: str
    name: str
    capabilities: ModelCapabilities  # .supports.vision: bool / .limits.vision: ModelVisionLimits | None
    billing: ModelBilling | None      # .multiplier: float | None
    # ...
```

From app.api.routes.chat (既存 Dependency, L74-108):
```python
async def get_jwt_payload(request: Request) -> dict: ...
async def get_github_token(payload: dict = Depends(get_jwt_payload)) -> str: ...
```

From app.providers.copilot (現状, 既存):
```python
class ChatCopilot(BaseChatModel):
    model: str = "gpt-4.1"
    github_token: Optional[str] = None
    auth_manager: Optional[Any] = None
    send_timeout: float
    _client: Any  # CopilotClient (PrivateAttr)

    async def _ensure_client(self) -> None: ...  # L286
    async def close(self) -> None: ...            # L311
    def _messages_to_prompt(self, messages: Sequence[BaseMessage]) -> str: ...  # L316-336
    async def _agenerate(self, messages, stop=None, run_manager=None, **kwargs) -> ChatResult: ...  # L152-202
    async def _astream(self, messages, stop=None, run_manager=None, **kwargs) -> AsyncIterator[ChatGenerationChunk]: ...  # L204-280
```

Consumer contract (本 plan 提供):
```python
class ChatCopilot(BaseChatModel):
    # ... 既存 ...
    def _extract_attachments(self, messages: Sequence[BaseMessage]) -> list[FileAttachment] | None: ...
    async def list_models(self) -> list[dict]: ...
    async def is_vision_model(self, model_id: str) -> bool: ...
```

```python
# app/api/routes/models.py route (新規)
@router.get("/models") -> list[dict]  # [{id, name, vision, vision_limits, billing_multiplier}, ...]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChatCopilot に _extract_attachments / list_models / is_vision_model ヘルパーを追加し _agenerate / _astream に attachments kwarg を配線する</name>
  <files>app/providers/copilot.py, tests/test_copilot_attachments.py</files>
  <read_first>
    - /home/parallels/workspaces/copilot-langgraph/app/providers/copilot.py (L1-310 全体、特に L36 imports / L152-202 _agenerate / L204-280 _astream / L316-336 _messages_to_prompt)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-PATTERNS.md (§`app/providers/copilot.py` セクション L362-466、Example A)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-RESEARCH.md (§Pattern 2 SDK 隔離原則, §Example A, §Pitfall 6 重複コード回避)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-CONTEXT.md (D-09 / D-10 / D-13 / D-14 / D-15)
    - /home/parallels/workspaces/copilot-langgraph/tests/test_copilot_bind_tools.py (既存 ChatCopilot モック pattern、特に `@pytest.mark.asyncio` + AsyncMock(session) の使い方)
    - /home/parallels/workspaces/copilot-langgraph/.venv/lib/python3.12/site-packages/copilot/types.py (L42-47 FileAttachment / L748-956 ModelInfo 各 dataclass)
  </read_first>
  <behavior>
    - Test 1 (`test_extract_attachments_from_last_human_message`): messages に HumanMessage(additional_kwargs={"attachments": [D-14 dict 2件]}) + AIMessage を混ぜ、`ChatCopilot()._extract_attachments(messages)` が `[{"type": "file", "path": "/shared/.../a.txt", "displayName": "a.txt"}, {"type": "file", "path": "/shared/.../b.png", "displayName": "b.png"}]` を返す
    - Test 2 (`test_extract_attachments_empty_returns_none`): additional_kwargs が無い / attachments が空 / HumanMessage が無い → None
    - Test 3 (`test_extract_attachments_skips_non_file_kinds`): kind="blob" の項目は skip（本 phase 未採用）
    - Test 4 (`test_extract_attachments_skips_missing_path`): path フィールドが欠損している項目は skip
    - Test 5 (`test_agenerate_calls_send_and_wait_with_attachments`): _agenerate を呼ぶと `session.send_and_wait.assert_awaited_once()` + `kwargs["attachments"]` が期待 list と等しい（`timeout` kwarg も保持）
    - Test 6 (`test_agenerate_sends_none_when_no_attachments`): additional_kwargs 無しの HumanMessage では `kwargs["attachments"]` が None
    - Test 7 (`test_list_models_converts_modelinfo_to_dict`): `llm.list_models()` を呼ぶと mock した `ModelInfo` リスト（`capabilities.supports.vision=True` + `capabilities.limits.vision.to_dict()={"max_prompt_images": 5}` + `billing.multiplier=1.0`）を `[{id, name, vision, vision_limits, billing_multiplier}]` の dict に変換
    - Test 8 (`test_list_models_vision_false_returns_none_limits`): `vision=False` のモデルは `vision_limits: None` が返る
    - Test 9 (`test_is_vision_model_lookup`): list_models に `{id: "claude-sonnet-4.6", vision: True}` と `{id: "gpt-4.1", vision: False}` を仕込んで `is_vision_model("claude-sonnet-4.6") == True`、`is_vision_model("gpt-4.1") == False`、`is_vision_model("unknown") == False`
    - Test 10 (`test_is_vision_model_failsafe_on_exception`): `list_models()` が例外を上げるケース（client 未初期化 or SDK error）で `is_vision_model` は False を返す（fail-safe、例外を上げない）
  </behavior>
  <action>
    **Step 1 — `app/providers/copilot.py` の imports 拡張**:
    L36 の既存 import 行
    ```python
    from copilot import CopilotClient, SubprocessConfig, PermissionHandler  # type: ignore[import-untyped]
    ```
    を以下に置き換える:
    ```python
    from copilot import (  # type: ignore[import-untyped]
        CopilotClient,
        SubprocessConfig,
        PermissionHandler,
        FileAttachment,  # Phase 36 D-10/D-15: TypedDict. 辞書リテラル {"type": "file", "path": ..., "displayName": ...} で組む。
        ModelInfo,       # Phase 36 D-16: list_models() 戻り値、dataclass (capabilities / limits / billing) を持つ。
    )
    ```

    **Step 2 — `_extract_attachments` ヘルパーを ChatCopilot クラスに追加**:
    `_messages_to_prompt` (L316-336) の直前に以下のメソッドを挿入する（インデントは 4 space、メソッドは ChatCopilot クラス内の他 private method と同じレベル）:
    ```python
    def _extract_attachments(
        self,
        messages: Sequence[BaseMessage],
    ) -> Optional[List["FileAttachment"]]:
        """Phase 36 D-10/D-14/D-15: 最後の HumanMessage の additional_kwargs['attachments']
        を SDK TypedDict に変換して返す。

        - 他 BaseMessage 型（AIMessage / SystemMessage / ToolMessage）は skip
        - additional_kwargs が空 / 不在 / list が空なら None を返す（send_and_wait は None を許容）
        - kind != 'file' は本 phase 未採用なので skip（将来 BlobAttachment を追加する場合はここを拡張）
        - path が str で非空でない entry は skip（防御的）
        - displayName は a['name'] が str で非空のときのみ付ける（SDK TypedDict は optional）
        """
        last_human = None
        for m in reversed(messages):
            if isinstance(m, HumanMessage):
                last_human = m
                break
        if last_human is None:
            return None
        atts_meta = (last_human.additional_kwargs or {}).get("attachments") or []
        sdk_atts: List[FileAttachment] = []
        for a in atts_meta:
            if not isinstance(a, dict):
                continue
            if a.get("kind") != "file":
                continue
            path = a.get("path")
            if not isinstance(path, str) or not path:
                continue
            entry: FileAttachment = {"type": "file", "path": path}
            display_name = a.get("name")
            if isinstance(display_name, str) and display_name:
                entry["displayName"] = display_name
            sdk_atts.append(entry)
        return sdk_atts or None
    ```

    **Step 3 — `_agenerate` に attachments kwarg を差し込む**:
    L165 `prompt = self._messages_to_prompt(messages)` の**直後の行**に以下を挿入:
    ```python
            # Phase 36 D-09/D-10: 最後の HumanMessage の additional_kwargs["attachments"] を
            # SDK FileAttachment TypedDict に変換して send_and_wait に渡す。
            sdk_atts = self._extract_attachments(messages)
    ```
    L179 の `response = await session.send_and_wait(prompt, timeout=self.send_timeout)` を以下に変更:
    ```python
                response = await session.send_and_wait(
                    prompt,
                    attachments=sdk_atts,
                    timeout=self.send_timeout,
                )
    ```

    **Step 4 — `_astream` に attachments kwarg を差し込む**:
    L222 `prompt = self._messages_to_prompt(messages)` の**直後**に `sdk_atts = self._extract_attachments(messages)` を追加。
    L262 `await session.send(prompt)` を以下に変更:
    ```python
                await session.send(prompt, attachments=sdk_atts)
    ```

    **Step 5 — `list_models` / `is_vision_model` ヘルパーを追加**:
    `_ensure_client` メソッド (L286-310) の直後・`close` メソッド (L311-) の直前に以下を挿入:
    ```python
    async def list_models(self) -> list[dict]:
        """Phase 36 D-16: SDK list_models() を D-14 dict に変換する。

        戻り値: [{id, name, vision, vision_limits, billing_multiplier}, ...]
        - vision: bool — ModelCapabilities.supports.vision
        - vision_limits: dict | None — ModelVisionLimits.to_dict() or None
        - billing_multiplier: float | None — ModelBilling.multiplier
        """
        await self._ensure_client()
        models = await self._client.list_models()
        payload: list[dict] = []
        for m in models:
            caps = getattr(m, "capabilities", None)
            supports = getattr(caps, "supports", None)
            limits = getattr(caps, "limits", None)
            vision_flag = bool(getattr(supports, "vision", False)) if supports else False
            vision_limits_obj = getattr(limits, "vision", None) if limits else None
            vision_limits: dict | None = None
            if vision_limits_obj is not None:
                if hasattr(vision_limits_obj, "to_dict"):
                    vision_limits = vision_limits_obj.to_dict()
                else:
                    # dataclass フィールドをそのまま dict 化
                    import dataclasses
                    vision_limits = dataclasses.asdict(vision_limits_obj) if dataclasses.is_dataclass(vision_limits_obj) else None
            billing = getattr(m, "billing", None)
            multiplier = getattr(billing, "multiplier", None) if billing else None
            payload.append({
                "id": m.id,
                "name": m.name,
                "vision": vision_flag,
                "vision_limits": vision_limits,
                "billing_multiplier": multiplier,
            })
        return payload

    async def is_vision_model(self, model_id: str) -> bool:
        """Phase 36 D-18: worker 側 vision drop 判定ヘルパー。

        list_models() 取得・例外時は False を返す (fail-safe, drop 画像 + SystemMessage 警告)。
        """
        try:
            models = await self.list_models()
            for m in models:
                if m.get("id") == model_id:
                    return bool(m.get("vision"))
        except Exception:
            return False
        return False
    ```

    **Step 6 — `tests/test_copilot_attachments.py` を新規作成**:
    上記 behavior L1-L10 の 10 テストを実装。骨格:
    ```python
    """Phase 36 D-09/D-10/D-14/D-15/D-16/D-18 — provider 配線の unit test."""
    from unittest.mock import AsyncMock, MagicMock
    import pytest
    from langchain_core.messages import AIMessage, HumanMessage, SystemMessage


    @pytest.fixture
    def prov():
        from app.providers.copilot import ChatCopilot
        return ChatCopilot(github_token="ghu_test", model="claude-sonnet-4.6")


    def test_extract_attachments_from_last_human_message(prov):
        messages = [
            SystemMessage(content="sys"),
            HumanMessage(content="old", additional_kwargs={"attachments": [{"kind": "file", "name": "old.txt", "path": "/old"}]}),
            AIMessage(content="ok"),
            HumanMessage(
                content="these",
                additional_kwargs={"attachments": [
                    {"kind": "file", "name": "a.txt", "path": "/shared/thread-files/u/t/a.txt", "size": 10, "mime_type": "text/plain", "ext": "txt", "modified_at": "2026-04-23T12:00:00Z", "storage_name": "20260423T120000_a.txt"},
                    {"kind": "file", "name": "b.png", "path": "/shared/thread-files/u/t/b.png", "size": 999, "mime_type": "image/png", "ext": "png", "modified_at": "2026-04-23T12:00:00Z", "storage_name": "20260423T120000_b.png"},
                ]},
            ),
        ]
        result = prov._extract_attachments(messages)
        assert result == [
            {"type": "file", "path": "/shared/thread-files/u/t/a.txt", "displayName": "a.txt"},
            {"type": "file", "path": "/shared/thread-files/u/t/b.png", "displayName": "b.png"},
        ]

    # ... Test 2-10 を同様の粒度で実装 ...
    ```
    モック戦略 (Test 5/6 for _agenerate, Test 7-10 for list_models/is_vision_model):
    ```python
    @pytest.mark.asyncio
    async def test_agenerate_calls_send_and_wait_with_attachments(prov, monkeypatch):
        # session / client を AsyncMock で差し替え
        mock_session = AsyncMock()
        mock_response = MagicMock(data=MagicMock(content="ok"))
        mock_session.send_and_wait = AsyncMock(return_value=mock_response)
        mock_session.disconnect = AsyncMock()
        mock_client = AsyncMock()
        mock_client.create_session = AsyncMock(return_value=mock_session)
        prov._client = mock_client

        messages = [HumanMessage(
            content="x",
            additional_kwargs={"attachments": [{"kind": "file", "name": "a.txt", "path": "/p/a.txt"}]},
        )]
        await prov._agenerate(messages)

        mock_session.send_and_wait.assert_awaited_once()
        _, kwargs = mock_session.send_and_wait.call_args
        assert kwargs["attachments"] == [{"type": "file", "path": "/p/a.txt", "displayName": "a.txt"}]
        assert "timeout" in kwargs
    ```
    `list_models` テストでは `MagicMock` で dataclass 風のモデルを作る:
    ```python
    def _mk_modelinfo(id_, name_, vision, limits_dict=None, mult=None):
        m = MagicMock()
        m.id = id_
        m.name = name_
        m.capabilities = MagicMock()
        m.capabilities.supports = MagicMock(vision=vision)
        m.capabilities.limits = MagicMock()
        if limits_dict is not None:
            limits_obj = MagicMock()
            limits_obj.to_dict = MagicMock(return_value=limits_dict)
            m.capabilities.limits.vision = limits_obj
        else:
            m.capabilities.limits.vision = None
        m.billing = MagicMock(multiplier=mult) if mult is not None else None
        return m
    ```

    **Step 7 — 既存 test が壊れないことを確認**:
    `tests/test_copilot_bind_tools.py` の既存アサーションで `session.send_and_wait` の呼び出し kwargs を厳密 match している箇所があれば、`attachments` kwarg の追加で壊れるので確認し、必要なら `call_args.kwargs` の他フィールドのみ assert するように書き直す。`grep -n "send_and_wait" tests/` で調査してから判断。
  </action>
  <verify>
    <automated>uv run pytest tests/test_copilot_attachments.py tests/test_copilot_bind_tools.py -x -v</automated>
  </verify>
  <done>
    - `grep -n "from copilot import" app/providers/copilot.py` が `FileAttachment` と `ModelInfo` を含む 1 行を返す
    - `grep -n "def _extract_attachments\|async def list_models\|async def is_vision_model" app/providers/copilot.py` が 3 行返る
    - `grep -n "attachments=sdk_atts" app/providers/copilot.py` が 2 箇所（`_agenerate` と `_astream`）見つかる
    - `tests/test_copilot_attachments.py` の 10 tests が GREEN
    - `tests/test_copilot_bind_tools.py` の既存 tests も regression なく GREEN
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET /api/models route（TTL 1h キャッシュ + JWT 認証）と attachments stub router + main.py 登録</name>
  <files>app/api/routes/models.py, app/api/routes/attachments.py, app/api/main.py, tests/test_api_models_route.py</files>
  <read_first>
    - /home/parallels/workspaces/copilot-langgraph/app/api/routes/chat.py (L1-110 imports + get_jwt_payload / get_github_token Dependency 定義, L30 APIRouter(prefix="/api") パターン)
    - /home/parallels/workspaces/copilot-langgraph/app/api/main.py (L25 route imports, L371-382 include_router ブロック)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-PATTERNS.md (§`app/api/routes/models.py` L217-273, §`app/api/main.py` L309-339)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-RESEARCH.md (§Pattern 4 /api/models キャッシュ, §Open Question 2, Pitfall 10)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-CONTEXT.md (D-07 / D-16)
    - /home/parallels/workspaces/copilot-langgraph/tests/conftest.py (api_client fixture, jwt_cookie fixture, mock_auth_manager)
    - /home/parallels/workspaces/copilot-langgraph/tests/test_api_chat.py (integration test pattern for JWT-protected routes)
  </read_first>
  <behavior>
    - Test 1 (`test_get_models_returns_list_from_llm`): JWT cookie を持つ client で `GET /api/models` → 200 + list[{id, name, vision, vision_limits, billing_multiplier}] が返る（`app.state.llm.list_models` をモックして既知の dict を返させる）
    - Test 2 (`test_get_models_requires_auth`): cookie なしで `GET /api/models` → 401 `auth_required`
    - Test 3 (`test_get_models_ttl_cache_hit`): 1 回目の call で `llm.list_models` が呼ばれ、2 回目の即時 call でキャッシュが効いて呼ばれない
    - Test 4 (`test_get_models_cache_expires_after_ttl`): モジュール内 `_cache.at` を現在時刻 - 3700 秒に置き換えて 2 回目の call で再度 `list_models` が呼ばれる
    - Test 5 (`test_get_models_failure_returns_503`): `llm.list_models` が例外を上げる → 503（キャッシュが空のとき、キャッシュがあれば古い payload を返す）
  </behavior>
  <action>
    **Step 1 — `app/api/routes/models.py` を新規作成**:
    ```python
    """Phase 36 D-07/D-16: GET /api/models — SDK list_models() のラッパー + TTL 1h キャッシュ。

    JWT 認証必須（ADR-0014）。SDK 隔離原則により SDK 型は ChatCopilot.list_models() 内で dict に変換済。
    """
    import time
    from dataclasses import dataclass, field

    from fastapi import APIRouter, Depends, HTTPException, Request

    from app.api.routes.chat import get_jwt_payload

    router = APIRouter(prefix="/api", tags=["models"])

    _TTL_SECS = 3600  # 1 時間 (D-16)


    @dataclass
    class _Cache:
        at: float = 0.0
        payload: list[dict] = field(default_factory=list)


    _cache = _Cache()


    @router.get("/models")
    async def list_models(
        request: Request,
        _payload: dict = Depends(get_jwt_payload),  # JWT 認証のみ、github_token は不要 (llm は app.state 経由で共有)
    ) -> list[dict]:
        """TTL 1h キャッシュで GET /api/models を返す。

        - キャッシュ hit 時: キャッシュ payload をそのまま返す
        - キャッシュ miss 時: `llm.list_models()` を呼び結果を cache に保存して返す
        - `llm.list_models()` 例外時: キャッシュが空なら 503、キャッシュが残っていれば古い payload を返す (graceful)
        """
        now = time.time()
        if now - _cache.at < _TTL_SECS and _cache.payload:
            return _cache.payload
        llm = request.app.state.llm
        try:
            payload = await llm.list_models()
        except Exception as e:
            # キャッシュが残っていれば古い payload を返して UI 継続。なければ 503。
            if _cache.payload:
                return _cache.payload
            raise HTTPException(status_code=503, detail=f"models list unavailable: {e}") from e
        _cache.at = now
        _cache.payload = payload
        return payload
    ```

    **Step 2 — `app/api/routes/attachments.py` を新規作成（stub のみ — Plan 03 で中身実装）**:
    ```python
    """Phase 36 D-07: attachments upload / raw GET / DELETE routes.

    Plan 02: router stub のみ作成し main.py include_router を通す。
    Plan 03: multipart upload / FileResponse / delete の実装を追加する。
    """
    from fastapi import APIRouter

    router = APIRouter(prefix="/api", tags=["attachments"])

    # 実装は Plan 03 に委譲 (POST / GET / DELETE /threads/{thread_id}/attachments[...])
    ```

    **Step 3 — `app/api/main.py` 編集**:
    L25 の import 行を以下に置き換え:
    ```python
    from app.api.routes import (
        agents, apps, attachments, auth, canvas, chat, gems, health, hosted_apps,
        iframe_rpc, jobs, me, models,
    )
    ```
    L371-382 の include_router ブロックに 2 行追加（`hosted_apps.router` の直後、他 router と同じ順序で最後に足す — Phase 19 で `hosted_apps.router` は /apps 静的の前に置くという制約があるので、**attachments.router と models.router は `hosted_apps.router` の直前**に追加する。main.py L381-382 付近の現状を先に再読して位置を確定）:
    ```python
    app.include_router(iframe_rpc.router)
    app.include_router(attachments.router)  # Phase 36: POST/GET/DELETE /api/threads/{tid}/attachments
    app.include_router(models.router)       # Phase 36: GET /api/models (TTL 1h)
    app.include_router(hosted_apps.router)
    ```

    **Step 4 — `tests/test_api_models_route.py` を新規作成**:
    既存 `tests/conftest.py` の `api_client` fixture を使う（`app.state.llm` を差し替える手段が必要なので、fixture 内で mock_llm を inject する helper を自作する or conftest を拡張）。tests/conftest.py を先に読み、既存の mock 注入パターン（例: `app.state.graph = mock_graph`）に合わせる:
    ```python
    """Phase 36 D-07/D-16: GET /api/models の integration test."""
    from unittest.mock import AsyncMock
    import pytest
    from httpx import AsyncClient


    @pytest.fixture
    def mock_llm_list():
        """ChatCopilot.list_models のスタブ: 2 モデル（vision 対応 / 非対応）を返す。"""
        mock = AsyncMock()
        mock.list_models = AsyncMock(return_value=[
            {"id": "claude-sonnet-4.6", "name": "Claude Sonnet 4.6", "vision": True,
             "vision_limits": {"max_prompt_images": 5, "max_prompt_image_size": 10485760, "supported_media_types": ["image/png", "image/jpeg", "image/webp"]},
             "billing_multiplier": 1.0},
            {"id": "gpt-4.1", "name": "GPT-4.1", "vision": False, "vision_limits": None, "billing_multiplier": 0.0},
        ])
        return mock


    @pytest.mark.asyncio
    async def test_get_models_returns_list_from_llm(api_client: AsyncClient, jwt_cookie, mock_llm_list, monkeypatch):
        # キャッシュを毎回クリア
        from app.api.routes import models as models_module
        models_module._cache.at = 0.0
        models_module._cache.payload = []
        # api_client の app.state.llm を差し替え
        api_client._transport.app.state.llm = mock_llm_list

        api_client.cookies.set("session", jwt_cookie)
        resp = await api_client.get("/api/models")
        assert resp.status_code == 200, resp.text
        data = resp.json()
        assert isinstance(data, list)
        assert len(data) == 2
        assert data[0]["id"] == "claude-sonnet-4.6"
        assert data[0]["vision"] is True
        assert data[0]["vision_limits"]["max_prompt_images"] == 5
        assert data[1]["vision"] is False


    @pytest.mark.asyncio
    async def test_get_models_requires_auth(api_client: AsyncClient, mock_llm_list):
        from app.api.routes import models as models_module
        models_module._cache.at = 0.0
        models_module._cache.payload = []
        api_client._transport.app.state.llm = mock_llm_list

        resp = await api_client.get("/api/models")
        assert resp.status_code == 401
        assert resp.json()["detail"] == "auth_required"


    @pytest.mark.asyncio
    async def test_get_models_ttl_cache_hit(api_client: AsyncClient, jwt_cookie, mock_llm_list):
        from app.api.routes import models as models_module
        models_module._cache.at = 0.0
        models_module._cache.payload = []
        api_client._transport.app.state.llm = mock_llm_list
        api_client.cookies.set("session", jwt_cookie)

        resp1 = await api_client.get("/api/models")
        assert resp1.status_code == 200
        assert mock_llm_list.list_models.await_count == 1

        resp2 = await api_client.get("/api/models")
        assert resp2.status_code == 200
        # cache hit なので list_models は呼ばれない
        assert mock_llm_list.list_models.await_count == 1


    @pytest.mark.asyncio
    async def test_get_models_cache_expires_after_ttl(api_client: AsyncClient, jwt_cookie, mock_llm_list):
        from app.api.routes import models as models_module
        models_module._cache.at = 0.0
        models_module._cache.payload = []
        api_client._transport.app.state.llm = mock_llm_list
        api_client.cookies.set("session", jwt_cookie)

        await api_client.get("/api/models")
        # at を 3700s 前に強制変更 → 期限切れ
        import time as _time
        models_module._cache.at = _time.time() - 3700

        await api_client.get("/api/models")
        assert mock_llm_list.list_models.await_count == 2


    @pytest.mark.asyncio
    async def test_get_models_failure_returns_503(api_client: AsyncClient, jwt_cookie):
        from app.api.routes import models as models_module
        models_module._cache.at = 0.0
        models_module._cache.payload = []

        failing = AsyncMock()
        failing.list_models = AsyncMock(side_effect=RuntimeError("SDK down"))
        api_client._transport.app.state.llm = failing
        api_client.cookies.set("session", jwt_cookie)

        resp = await api_client.get("/api/models")
        assert resp.status_code == 503
    ```

    **Step 5 — 既存 test が壊れないことを確認**:
    - `tests/test_api_chat.py` 等の既存 integration test を実行して 200/401/404 系の挙動が変わっていないことを確認
    - main.py の変更によりアプリ起動 smoke test (`uv run uvicorn app.api.main:app --help` 相当の import チェック) が通ることを確認 — 具体的には `uv run python -c "from app.api.main import app; print(len(app.routes))"` が正の整数を返せば OK
  </action>
  <verify>
    <automated>uv run pytest tests/test_api_models_route.py tests/test_api_chat.py -x -v && uv run python -c "from app.api.main import app; assert any('/api/models' in str(r) for r in app.routes), 'models route not included'"</automated>
  </verify>
  <done>
    - `app/api/routes/models.py` が新規作成され、`GET /api/models` route を定義している
    - `app/api/routes/attachments.py` が新規作成され、空の `APIRouter(prefix="/api", tags=["attachments"])` を export している
    - `app/api/main.py` の import に `attachments, models` が含まれる
    - `app/api/main.py` の include_router ブロックに `app.include_router(attachments.router)` と `app.include_router(models.router)` の 2 行が入っている
    - `tests/test_api_models_route.py` の 5 tests が GREEN
    - 既存 test 全体が regression なし
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /api/models | HTTP request. JWT cookie 必須 (ADR-0014) |
| client → ChatCopilot (via app.state.llm) | provider インスタンス共有。list_models() は GitHub token を暗黙的に使用する (auth_manager 経由) |
| ChatCopilot → Copilot SDK subprocess | subprocess に付ける github_token は auth_manager 経由。追加の入力経路は無い (本 plan では SDK 呼出しのみ新規) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-02-01 | Spoofing | `GET /api/models` 未認証アクセスで SDK 呼出し可能 | mitigate | `Depends(get_jwt_payload)` 必須（未認証は 401 `auth_required`）。Test 2 が regression を防ぐ |
| T-36-02-02 | Information Disclosure | `/api/models` が内部モデル ID / billing multiplier を返す | accept | 同等情報は既存 Header.tsx ハードコードでクライアントに公開済み。社内 200 名環境で追加リスクなし |
| T-36-02-03 | Denial of Service | `/api/models` 高頻度呼出しで SDK subprocess 過負荷 | mitigate | TTL 1h キャッシュ (D-16)。Test 3 でキャッシュヒットが regression せず確認 |
| T-36-02-04 | Tampering | ChatCopilot._extract_attachments が不正な additional_kwargs を受け取った場合 | mitigate | `isinstance(a, dict)` / `a.get("kind") != "file"` / `isinstance(path, str) and path` の 3 段フィルタで全て skip。Test 3/4 で確認 |
| T-36-02-05 | Information Disclosure | SDK subprocess がエラー時に github_token を log に漏らす | accept | 既存 ChatCopilot._ensure_client のエラー処理 (L195-201) が握り潰す。本 plan では新規 log 追加しない |
| T-36-02-06 | Spoofing | list_models() 例外時に古いキャッシュを返す (stale data) | accept | D-16 の graceful 方針。Verdict 理由は「UI が落ちるより stale モデル表示のほうが UX 劣化小」。TTL 切れていなければ常に最新 |
</threat_model>

<verification>
- `uv run pytest tests/test_copilot_attachments.py tests/test_copilot_bind_tools.py tests/test_api_models_route.py tests/test_api_chat.py -x -v` が exit 0
- `grep -n "attachments=sdk_atts" app/providers/copilot.py | wc -l` が 2
- `grep -n "from copilot import" app/ -r | grep -v "providers/copilot.py"` が 0 行 (SDK 隔離原則維持)
- `curl -s http://localhost:8000/api/models` (auth なし) が 401 を返す (本番 smoke は Plan 07 で実施)
</verification>

<success_criteria>
- ChatCopilot が multimodal attachments / model list / vision 判定の 3 機能を SDK 隔離原則を守って提供する
- GET /api/models が JWT 認証下で D-14 準拠 dict list を返し、TTL 1h キャッシュが効く
- main.py に attachments / models の両 router が登録され、FastAPI 起動時に import エラーが出ない
- 既存 `tests/test_copilot_bind_tools.py` / `tests/test_api_chat.py` が regression なく GREEN
- Plan 03 / Plan 04 / Plan 06 が本 plan の artifact を前提に実装を開始できる
</success_criteria>

<output>
完了後、`.planning/phases/36-text-code-image-multimodal/36-02-SUMMARY.md` を作成する。
</output>
