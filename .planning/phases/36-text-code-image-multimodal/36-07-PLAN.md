---
phase: 36-text-code-image-multimodal
plan: 07
type: execute
wave: 6
depends_on:
  - 01
  - 02
  - 03
  - 04
  - 05
  - 06
files_modified:
  - docs/phase-36-integration-check.md
  - docs/adr/0050-copilot-sdk-multimodal-attachments.md
  - .planning/adr-categories.yaml
  - .planning/patterns.md
  - .planning/phases/36-text-code-image-multimodal/36-VERIFICATION.md
autonomous: false
requirements:
  - FIN-01
  - FIN-02
user_setup: []
must_haves:
  truths:
    - "text/code ファイル (README.md / data.csv) を添付 → LLM が応答で内容に言及する"
    - "png 画像を添付 + claude-sonnet-4.6 (vision: true) で送信 → AI 応答に画像の要素が反映される"
    - "png 画像を添付 + gpt-4.1 (vision: false) で送信 → エラー停止なし、SystemMessage 経由で案内が表示される"
    - "スレッド再オープン (F5 リロード) 後も bubble チップ (画像サムネ / text pill) が表示される"
    - "docker compose + nginx 経由で上記 4 シナリオが全て動作する"
    - "新規 ADR-0050 が起票され、.planning/adr-categories.yaml と docs/adr/INDEX.md に反映"
    - "patterns.md に Phase 36 の 2 エントリ (HumanMessage.additional_kwargs サイドカー / 3入り口統一 staging) が追記されている"
    - "VERIFICATION.md がクローズされ、全 Success Criteria が PASS と記録される"
  artifacts:
    - path: "docs/phase-36-integration-check.md"
      provides: "ADR-0046 integration check ゲート記録 (4 シナリオ × docker compose 実機)"
      contains: "FIN-01"
    - path: "docs/adr/0050-copilot-sdk-multimodal-attachments.md"
      provides: "Phase 36 設計判断 ADR (SDK FileAttachment 採用 + additional_kwargs envelope + SDK 隔離 + vision fallback 2 段)"
      contains: "FileAttachment"
    - path: ".planning/adr-categories.yaml"
      provides: "ADR-0050 を LangGraph・Graph カテゴリに登録 (primary), Frontend・UI secondary"
      contains: "0050"
    - path: ".planning/patterns.md"
      provides: "2 エントリ追記"
      contains: "additional_kwargs サイドカー"
    - path: ".planning/phases/36-text-code-image-multimodal/36-VERIFICATION.md"
      provides: "Phase 36 Success Criteria 1-4 の verdict + 残件メモ"
      contains: "Success Criteria"
  key_links:
    - from: "docs/phase-36-integration-check.md"
      to: "ROADMAP.md §Phase 36 Success Criteria 1-4"
      via: "各 criteria ごとに PASS/FAIL 記録"
      pattern: "Success Criteria"
    - from: "docs/adr/0050-copilot-sdk-multimodal-attachments.md"
      to: "ADR-0048 / ADR-0038 / ADR-0025 / ADR-0046"
      via: "Related ADRs セクション"
      pattern: "ADR-00"
    - from: "docs/adr/INDEX.md"
      to: "ADR-0050"
      via: "pre-commit hook による自動再生成 (.planning/adr-categories.yaml 更新で発火)"
      pattern: "0050"
---

<objective>
Phase 36 の **完了ゲート** — docker compose 実機での E2E 動作確認 (ADR-0046 integration check)、ADR-0050 起票、patterns.md 追記、VERIFICATION.md クローズを一括で行う。ここで「Phase 36 Success Criteria 1〜4 が実機で PASS」を確定させ、v6.0 milestone (Phase 32/33/34 が主スコープ) に Phase 36 を組み込める状態にする。

Purpose: unit test + integration test が GREEN でも silent failure する経路 (RESEARCH.md §Assumption A1 / Pitfall 1 / ADR-0046) を人間 + Chrome DevTools MCP で実機確認する。その結果をもって ADR 化 + patterns.md への 2 エントリ追記を行い、ADR-0048 (Phase 37) と並んで「thread-files フォルダ規約の書き込み側」の完成を履歴化する。
Output: docs/phase-36-integration-check.md + docs/adr/0050-*.md + .planning/adr-categories.yaml + .planning/patterns.md + VERIFICATION.md。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-text-code-image-multimodal/36-CONTEXT.md
@.planning/phases/36-text-code-image-multimodal/36-RESEARCH.md
@.planning/phases/36-text-code-image-multimodal/36-UI-SPEC.md
@.planning/phases/36-text-code-image-multimodal/36-VALIDATION.md
@.planning/phases/36-text-code-image-multimodal/36-01-SUMMARY.md
@.planning/phases/36-text-code-image-multimodal/36-02-SUMMARY.md
@.planning/phases/36-text-code-image-multimodal/36-03-SUMMARY.md
@.planning/phases/36-text-code-image-multimodal/36-04-SUMMARY.md
@.planning/phases/36-text-code-image-multimodal/36-05-SUMMARY.md
@.planning/phases/36-text-code-image-multimodal/36-06-SUMMARY.md
@.planning/patterns.md
@docs/adr/INDEX.md
@docs/adr/0046-integration-check-surfaced-silent-failures.md
@docs/adr/0048-thread-files-folder-convention.md
@docs/adr/0025-datetime-and-user-context-injection-into-agent-prompts.md
@docs/adr/0038-superchat-context-messages-and-agent-name-persistence.md
@docs/adr/0044-mcp-tool-catalog-single-source-of-truth.md
@.planning/adr-categories.yaml
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: docker compose 実機 + Chromium DevTools MCP で 4 シナリオ E2E 確認 (ADR-0046 integration check)</name>
  <files>docs/phase-36-integration-check.md</files>
  <read_first>
    - /home/parallels/workspaces/copilot-langgraph/docs/adr/0046-integration-check-surfaced-silent-failures.md (integration check ゲートの規格)
    - /home/parallels/workspaces/copilot-langgraph/.planning/ROADMAP.md §Phase 36 (Success Criteria 1-4 の正本)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-VALIDATION.md (Manual-Only Verifications 一覧)
    - /home/parallels/workspaces/copilot-langgraph/CLAUDE.md §Chrome DevTools MCP (起動手順)
  </read_first>
  <what-built>
    Plan 01-06 の成果物を、docker compose 上で立ち上げて以下 4 シナリオを実機検証する:
    1. **text/code 添付 → LLM 言及** (FIN-01, Success Criteria 1)
    2. **画像添付 + vision 対応モデル → 画像認識応答** (FIN-02, Success Criteria 2)
    3. **画像添付 + vision 非対応モデル → graceful fallback** (FIN-02, Success Criteria 3)
    4. **F5 リロード後も bubble チップ復元** (A1 risk / D-20 / Success Criteria 4)
  </what-built>
  <how-to-verify>
    1. **Chromium をリモートデバッグモードで起動**:
       ```bash
       curl -s http://127.0.0.1:9222/json/version || \
         echo "起動が必要: chromium --remote-debugging-port=9222 --no-first-run --no-default-browser-check &"
       ```
       (必要なら CLAUDE.md の `!` プレフィックスで Chromium を立ち上げる)
    2. **docker compose 起動 + 最新コード反映**:
       ```bash
       docker compose up -d --build
       # frontend (bun run dev) + api + worker + postgres + redis + mcp-server が Healthy になるまで待つ
       docker compose ps
       ```
    3. **ブラウザで `http://localhost:5173/orochi/` を開き Device Flow ログイン**。ユーザー `testuser` として認証完了。
    4. **シナリオ 1 (FIN-01 text/code)**:
       - Chat アプリを開き 新規スレッド
       - 📎 ボタンで `README.md` (リポジトリ直下の実ファイル、≈ 数 KB) を添付
       - InputBar preview に `[📄 README.md XXX KB ×]` pill が表示され、Network タブで `POST /api/threads/{tid}/attachments` 200 + `{attachments: [D-14 dict]}` が返っているか確認
       - 「このファイルは何？一言で要約して」と入力して送信
       - AI 応答が「Copilot LangGraph」「README」など **README 内の具体的な語** を含めて応答すれば PASS (単なる相槌は FAIL)
       - docker compose logs worker | tail -50 で `additional_kwargs={"attachments": [...]}` が worker 側に届いているか確認
    5. **シナリオ 2 (FIN-02 画像 + vision 対応)**:
       - Header の model select で `Claude Sonnet 4.6 🖼` を選択
       - 同じスレッド (or 新規)で PNG 画像 (例: GitHub profile icon を DL) を drag & drop
       - 48×48 サムネが InputBar preview に表示、`POST /api/threads/{tid}/attachments` 成功
       - 「この画像について説明して」と入力して送信
       - AI 応答が画像内の要素（色・形・オブジェクト）に具体的に言及すれば PASS
    6. **シナリオ 3 (FIN-02 画像 + vision 非対応 graceful fallback)**:
       - model select で `GPT-4.1` (vision: false) に切替
       - 画像を drop → `VisionWarningBanner` が「画像非対応モデル」と表示、CTA に `Claude Sonnet 4.6 に切り替える` が見える
       - banner の CTA を押さずに送信 (わざと vision:false のまま)
       - **AI が例外停止せず応答を返す** こと、応答内容に「画像非対応」「vision 対応モデル」などの案内が含まれていることを確認 (D-18)
       - docker compose logs worker | tail -50 で effective_system_prompt に「画像非対応モデル警告」が追加されているか確認
    7. **シナリオ 4 (D-20 履歴復元)**:
       - シナリオ 1-3 で作ったスレッドを ThreadSidebar で開き直す (or F5 リロード)
       - メッセージバブル内に 48×48 画像サムネ + text pill が復元表示される (D-21)
       - Chrome DevTools MCP で element inspect → `role="group"` + `aria-label="添付ファイル N 件"` が存在
       - Network タブで `GET /api/threads/{tid}/attachments/{storage_name}` が 200 で raw bytes を返す
    8. **VisionWarningBanner ワンクリック切替 (D-17)**:
       - 新規スレッド or clear 後、gpt-4.1 選択 + 画像 drop で banner 表示
       - `[Claude Sonnet 4.6 に切り替える]` クリック → Header select が Claude Sonnet 4.6 🖼 に変わる、banner が消える
    9. **DELETE 単一ファイル (D-06 ケース D)**:
       - 画像 staging 中に × ボタン → chip が消える
       - Network タブで `DELETE /api/threads/{tid}/attachments/{storage_name}` 204 を確認
       - docker compose exec api ls -la /shared/thread-files/testuser/{tid}/ で folder から消えている
    10. **結果を `docs/phase-36-integration-check.md` に記録**:
        ```markdown
        # Phase 36 Integration Check

        **Date:** 2026-04-??（実行日）
        **Environment:** docker compose (local) + Chromium 9222
        **Executor:** （ユーザー名）

        ## Verdict
        | # | Criteria | Result | Notes |
        |---|----------|--------|-------|
        | 1 | FIN-01 text/code 添付 → LLM 参照応答 | PASS / FAIL | モデル: claude-sonnet-4.6 / 応答抜粋: "..." |
        | 2 | FIN-02 画像 + vision 対応 | PASS / FAIL | モデル: claude-sonnet-4.6 / 画像: test.png |
        | 3 | FIN-02 画像 + vision 非対応 graceful fallback | PASS / FAIL | 応答抜粋に「vision 対応モデル」含む |
        | 4 | D-20 履歴チップ F5 復元 | PASS / FAIL | bubble チップ数: N |
        | 5 | D-17 VisionWarningBanner ワンクリック切替 | PASS / FAIL | |
        | 6 | D-06 ケース D × 削除 + サーバー削除 | PASS / FAIL | |

        ## Observed Issues (if any)
        - ...

        ## docker compose log snippets
        ### worker attachments 注入確認
        ```
        (docker compose logs worker | grep "additional_kwargs" の抜粋を 5-10 行)
        ```

        ### SystemMessage 警告注入確認 (シナリオ 3)
        ```
        (effective_system_prompt で「画像非対応モデル警告」が含まれる行)
        ```

        ## Overall Verdict
        PASS / PARTIAL / FAIL （6 シナリオ全 PASS なら PASS）

        ## Open Issues Carried Forward
        - （v6.1+ に回す issue があれば記載）
        ```
  </how-to-verify>
  <resume-signal>
    `docs/phase-36-integration-check.md` の `## Overall Verdict` に **PASS** が記録され、git に commit された状態で「approved」を入力してください。PARTIAL / FAIL の場合はどのシナリオが落ちたかと workaround 方針を報告。重度な回帰（例: text 添付で LLM が全く触れない）の場合は Plan 01-06 に戻って debug が必要です。
  </resume-signal>
  <done>
    - `docs/phase-36-integration-check.md` が存在し Overall Verdict: PASS が記録されている
    - 6 シナリオ（1-4 + D-17 + D-06）が全て PASS
    - 少なくとも 1 つのシナリオで docker compose logs worker の抜粋が記録されている（silent failure 検知の証跡）
    - ユーザーが approved を宣言
  </done>
</task>

<task type="auto">
  <name>Task 2: ADR-0050 "Copilot SDK 0.2.0 multimodal attachments の採用と隔離原則" を起票し、.planning/adr-categories.yaml と docs/adr/INDEX.md を更新</name>
  <files>docs/adr/0050-copilot-sdk-multimodal-attachments.md, .planning/adr-categories.yaml</files>
  <read_first>
    - /home/parallels/workspaces/copilot-langgraph/docs/adr/INDEX.md (既存 ADR 採番確認、0049 が最新か確認 — 0050 を新規採番)
    - /home/parallels/workspaces/copilot-langgraph/.planning/adr-categories.yaml (カテゴリ 7 種構造、既存 ADR 番号の配置)
    - /home/parallels/workspaces/copilot-langgraph/docs/adr/0048-thread-files-folder-convention.md (隣接 ADR の書式テンプレート)
    - /home/parallels/workspaces/copilot-langgraph/docs/adr/0049-per-job-mcp-client-lifecycle-and-cancel-safe-exceptions.md (最近の ADR の書式・Related ADRs の書き方)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-CONTEXT.md (D-01..D-23 が決定の根拠)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-RESEARCH.md (§Pattern 1/2/4/5 + §Pitfall 1)
    - /home/parallels/workspaces/copilot-langgraph/CLAUDE.md §ADR INDEX 自動生成 (pre-commit hook 前提)
    - /home/parallels/workspaces/copilot-langgraph/scripts/generate_adr_index.py (INDEX.md 再生成ロジック)
  </read_first>
  <action>
    **Step 1 — `docs/adr/0050-copilot-sdk-multimodal-attachments.md` を新規作成**:
    既存 ADR の書式（Frontmatter + Status + Context + Decision + Consequences + Related ADRs）を踏襲する。以下を雛形として採用:
    ```markdown
    # ADR-0050: Copilot SDK 0.2.0 multimodal attachments の採用と SDK 隔離原則

    **Date:** 2026-04-23
    **Status:** Accepted
    **Phase:** 36 (text/code + image multimodal)
    **Related ADRs:** ADR-0048 (thread-files フォルダ規約 / 読取側), ADR-0038 (AIMessage.name 喪失 / 類似 A1 risk の先行例), ADR-0025 (SystemMessage 注入), ADR-0046 (integration check), ADR-0021 (bind_tools プロンプト注入 / LangChain 層 bypass の先行例)

    ## Context

    Phase 36 では v6.0 milestone FIN-01 / FIN-02 を満たすため、Chat アプリから text/code ファイルと画像を添付し、Copilot SDK 0.2.0 の multimodal モデル (Claude Sonnet 4.5+ / 4.6) に渡せる基盤を整備する必要があった。

    制約と選択肢:
    - Copilot SDK 0.2.0 は `FileAttachment` / `BlobAttachment` TypedDict と `CopilotSession.send_and_wait(attachments=[...])` kwarg で native multimodal を提供 (Technical Preview)
    - LangChain の structured multimodal content parts (`[{"type": "text"}, {"type": "image_url"}]`) は text ファイル型が未定義で SDK 互換性なし
    - 過去 ADR-0038 で AIMessage.name が LangGraph checkpointer の (de)serialize で喪失する既知問題あり — `additional_kwargs` 経路の round-trip も事前検証必須 (A1 risk)
    - Phase 37 が `/shared/thread-files/<login>/<thread_id>/` フォルダ規約 (ADR-0048) を書き込み側未整備のまま残していた — Phase 36 がその書き込み側を埋める責務
    - SDK は Technical Preview で外部インターフェースを薄いラッパーで隔離する原則 (PROJECT.md L14 / Phase 37 D-17 継承)

    ## Decision

    ### 1. FileAttachment + path-based 渡しを採用 (D-09)
    `session.send_and_wait(prompt, attachments=[{"type": "file", "path": ..., "displayName": ...}])` を text/code・画像の両方に使う。`BlobAttachment` (base64 inline) は checkpoint JSONB 肥大を避けるため本 phase では未採用。path 渡しなら worker コンテナの RO mount (`/shared/thread-files`) 経由で SDK subprocess が直接 open() できる（Assumption A3 — Phase 36 Wave 0 で実機確認済）。

    ### 2. HumanMessage.additional_kwargs["attachments"] サイドカー envelope (D-10/D-14/D-15)
    per-turn 添付情報を `HumanMessage.additional_kwargs["attachments"]: list[D-14 dict]` で運ぶ。LangGraph `add_messages` reducer と PostgreSQL checkpointer が `BaseMessage` 全体を JSONB シリアライズするため、追加フィールドを作らずに透過的に永続化される。D-14 統一 dict スキーマ (`kind / name / storage_name / path / size / mime_type / ext / modified_at`) を全層（REST body / job payload / state / history API / MCP ツール戻り値）で共有。

    ### 3. SDK 型の完全隔離 (D-09/D-10/D-15)
    `copilot.*` のシンボル (`FileAttachment` / `BlobAttachment` / `ModelInfo`) の import は `app/providers/copilot.py` 内だけに閉じ、他モジュール（route / handler / frontend）は D-14 dict スキーマしか扱わない。ChatCopilot に `_extract_attachments(messages) -> list[FileAttachment] | None` / `list_models() -> list[dict]` / `is_vision_model(id) -> bool` の 3 ヘルパーを追加し provider 境界で変換する。`tests/test_copilot_attachments_spike.py::test_sdk_imports_isolated_to_provider` が regression を防ぐ。

    ### 4. Vision 判定 / fallback の 2 段構造 (D-16/D-17/D-18/D-19)
    - UI 層 (D-17): vision 非対応モデル選択中に画像を staging すると `<VisionWarningBanner>` を表示、CTA で `suggestedVisionModel` にワンクリック切替
    - Worker 層 (D-18): defense-in-depth として worker 側でも vision 再検証、非対応なら画像 attachment を drop + SystemMessage に「画像非対応モデル警告」を注入 (ADR-0025 pattern 踏襲)
    - model info は `GET /api/models` が `list_models()` を TTL 1h キャッシュで提供 (D-16)。hardcoded allowlist は作らず SDK 由来 `ModelInfo.capabilities.supports.vision` を single source of truth とする

    ### 5. 履歴 UI は additional_kwargs を真実のソースとする (D-20/D-21/D-22/D-23)
    `GET /api/threads/{tid}/messages` の返り値に `additional_kwargs.attachments` を含める（D-22 — `_messages_to_response` で None-guard 付き公開）。MessageArea bubble 内に `AttachmentChipRow` を描画し、画像は 48×48 サムネ (`<img src="/api/threads/{tid}/attachments/{name}">`、サムネ生成なし — D-23)、text は `[📄 name size]` pill で表示。フォルダ scan は「現在の手持ち」確認用に限定（Phase 37 pattern 踏襲）、真実は additional_kwargs。

    ### 6. A1 risk (additional_kwargs round-trip) を Wave 0 で先行検証
    ADR-0038 の AIMessage.name 喪失問題と同系統のリスクを防ぐため、Phase 36 Wave 0 Plan 01 で `tests/test_chat_history_additional_kwargs.py` を作成し LangGraph MemorySaver + add_messages reducer で round-trip 保存できることを先に確認。加えて docker compose 実機での integration check (ADR-0046 gate, Plan 07) でも確認。

    ## Consequences

    **Positive:**
    - FIN-01 / FIN-02 を満たしつつ、LangChain の multimodal 標準化を待たずに実装可能
    - SDK 隔離原則により SDK breaking change の影響範囲を `app/providers/copilot.py` の中に閉じる
    - `additional_kwargs` サイドカー方式は per-message metadata を保持する汎用 pattern として v6.1+ の他機能（トークン使用量・引用・tool_call_id 等）にも応用可能
    - vision 判定の single source of truth 化により、モデル追加時の UI 更新が不要
    - Phase 37 のフォルダ規約 (ADR-0048) と接続済み — 書き込み (Phase 36) / 読取 (Phase 37 MCP ツール) / 削除 (Phase 37 delete hook) のライフサイクルが完結

    **Negative:**
    - BlobAttachment を採用していないので subprocess が path を open() できる前提を要求 — Copilot SDK Technical Preview のバージョンアップで subprocess architecture が変わると見直しが必要
    - TTL 1h キャッシュのため、新規モデル追加時に frontend の反映が最大 1 時間遅延する（社内 200 名規模では許容）
    - SuperChat / Gem / Canvas / Debate の 4 アプリで完全対応したのは ChatApp 中心のみ。SubAgent 側の HumanMessage 組み立てへの attachments 伝搬は v6.1 検討事項として VERIFICATION.md に残件計上

    **Neutral:**
    - `GET /api/threads/{tid}/attachments/{name}` route が追加で発生するが、既存 `/api/me` や `/api/threads` 等と同じく JWT 認証下の内部 API

    ## Implementation References

    - Provider: `app/providers/copilot.py` (_extract_attachments / list_models / is_vision_model)
    - REST: `app/api/routes/attachments.py` (POST / GET / DELETE), `app/api/routes/models.py` (GET /api/models + TTL cache)
    - Worker: `app/jobs/worker.py` (process_chat 拡張), `app/jobs/handlers/langgraph_handler.py` (_prepare_messages_input), `app/jobs/handlers/orchestrator_handler.py` (_prepare_new_attachments), `app/jobs/handlers/debate_handler.py` (初回 user turn のみ)
    - Frontend: `frontend/src/hooks/useAttachments.ts` (3 入り口統一 staging), `frontend/src/hooks/useModels.ts` (TTL cache), `frontend/src/components/AttachmentButton.tsx` / `AttachmentChips.tsx` / `VisionWarningBanner.tsx`, `frontend/src/components/InputBar.tsx` (warningSlot 追加), `frontend/src/components/MessageArea.tsx` (AttachmentChipRow + type:'custom' user bubble)
    - Tests: `tests/test_copilot_attachments.py`, `tests/test_copilot_attachments_spike.py`, `tests/test_api_models_route.py`, `tests/test_attachments_upload_route.py`, `tests/test_attachments_get_delete_route.py`, `tests/test_chat_history_additional_kwargs.py`, `tests/test_worker_attachments_payload.py`, `tests/test_langgraph_handler_attachments_v2.py`, `tests/test_orchestrator_handler_attachments.py`
    - Integration: `docs/phase-36-integration-check.md` (ADR-0046 gate)

    ## Notes

    - 画像サムネ配信は Pillow 生成 (`.thumb/`) せず browser の `<img width=48>` resize で十分 (D-23, 社内 200 名規模)。帯域問題が発生した場合は Phase 39 polish で再検討
    - OCR / EXIF サニタイズ / 複数タブ競合制御は v6.1+ に defer (CONTEXT.md Deferred Ideas)
    - Phase 36 の ChatApp 以外のアプリ (Gem / Canvas / SuperChat / Debate) の attachments UX は InputBar 流用の範囲で自動継承、完全対応は v6.1 以降で検討
    ```

    **Step 2 — `.planning/adr-categories.yaml` に 0050 を追記**:
    先に既存ファイルを読んで categories 構造を確認する:
    ```bash
    cat /home/parallels/workspaces/copilot-langgraph/.planning/adr-categories.yaml
    ```
    `LangGraph・Graph` カテゴリ (primary) に 0050 を追加。Frontend・UI は secondary として同じ 0050 を追加（すでに複数カテゴリに重複登録する前例があればそれに倣う、なければ primary のみ）:
    ```yaml
    # 例 (既存構造に合わせて調整):
    LangGraph・Graph:
      - 0002
      - 0021
      - 0025
      - 0038
      - 0050   # 追加: Copilot SDK multimodal attachments (Phase 36)
      # ...

    Frontend・UI:
      - 0022
      - 0043
      - 0050   # secondary: Phase 36 フロント側の staging/warning/chip UI
      # ...
    ```
    既存カテゴリのいずれにも "LangGraph・Graph" / "Frontend・UI" という日本語名がない場合は、YAML の既存 key 名に合わせる (例: `langgraph-graph` or `langgraph` 等)。

    **Step 3 — pre-commit hook による INDEX.md 自動再生成を確認**:
    ```bash
    bash /home/parallels/workspaces/copilot-langgraph/scripts/install-hooks.sh
    # 既にインストール済でも再実行で問題ない
    # ADR + adr-categories.yaml を git add して commit 直前に INDEX.md が再生成される
    git add docs/adr/0050-copilot-sdk-multimodal-attachments.md .planning/adr-categories.yaml
    # pre-commit hook が走って docs/adr/INDEX.md を自動更新してくれる
    ```
    commit 前に `python3 scripts/generate_adr_index.py` を手動で一度実行して INDEX.md に 0050 が載っていることを確認:
    ```bash
    cd /home/parallels/workspaces/copilot-langgraph && python3 scripts/generate_adr_index.py
    grep -n "0050" docs/adr/INDEX.md
    ```
    **重要**: hook を install していない環境でも手動で `python3 scripts/generate_adr_index.py` を実行できる。hook が走らない場合の手動実行パスも同じ。
  </action>
  <verify>
    <automated>ls /home/parallels/workspaces/copilot-langgraph/docs/adr/0050-copilot-sdk-multimodal-attachments.md && grep -c "0050" /home/parallels/workspaces/copilot-langgraph/.planning/adr-categories.yaml && grep -c "0050" /home/parallels/workspaces/copilot-langgraph/docs/adr/INDEX.md</automated>
  </verify>
  <done>
    - `docs/adr/0050-copilot-sdk-multimodal-attachments.md` が存在し、Frontmatter + Status + Context + Decision (6 セクション) + Consequences + Implementation References を含む
    - `.planning/adr-categories.yaml` に 0050 が少なくとも 1 つのカテゴリ下に記載されている
    - `docs/adr/INDEX.md` が pre-commit hook または手動 `python3 scripts/generate_adr_index.py` 実行で更新され、ADR-0050 エントリが含まれる
    - ADR 内で ADR-0046 / ADR-0048 / ADR-0038 / ADR-0025 への参照が明示されている
  </done>
</task>

<task type="auto">
  <name>Task 3: .planning/patterns.md に Phase 36 の 2 エントリ追記 + VERIFICATION.md 作成</name>
  <files>.planning/patterns.md, .planning/phases/36-text-code-image-multimodal/36-VERIFICATION.md</files>
  <read_first>
    - /home/parallels/workspaces/copilot-langgraph/.planning/patterns.md (既存 7 カテゴリ構造・1 エントリ 5-10 行の format)
    - /home/parallels/workspaces/copilot-langgraph/CLAUDE.md §ADR Pattern Reference (GSD Integration) / §新規 ADR 追加時の義務 (D-15)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-VALIDATION.md (Validation Sign-Off 項目を VERIFICATION.md に反映)
    - /home/parallels/workspaces/copilot-langgraph/.planning/phases/37-pdf-office-mcp/37-VALIDATION.md (類似の Phase 37 closure 実例があれば参考)
  </read_first>
  <action>
    **Step 1 — `.planning/patterns.md` に 2 エントリ追記**:
    既存 `LangGraph・Graph` カテゴリセクションを grep で特定し、その末尾に以下を追加:
    ```markdown
    ### HumanMessage.additional_kwargs サイドカー envelope (per-turn メタデータ搬送)

    per-turn のメッセージメタデータ（添付ファイル・引用・tool_call_id 等）を `HumanMessage.additional_kwargs: dict` に載せることで、追加 state フィールド不要で LangGraph `add_messages` reducer + PostgreSQL checkpointer (AsyncPostgresSaver JSONB) に透過永続化する。`AIMessage.name` が checkpoint 経由で落ちる既知問題 (ADR-0038) とは別系統で `additional_kwargs` は保持される。provider 側 (`ChatCopilot._extract_attachments`) で最後の HumanMessage の `additional_kwargs["attachments"]` を読み、SDK 型に変換して SDK call に渡す。

    **Related:** ADR-0050 (Phase 36 multimodal), ADR-0038 (AIMessage.name 喪失)

    ### Vision / model capability fallback の 2 段構造 (UI 事前通知 + worker defense-in-depth)

    モデル能力依存の機能（vision / tool-calling / context length 等）で UI bypass を防ぐには、UI 側で事前通知 + 推奨モデル提示 + ワンクリック切替（graceful guidance）を行い、worker 側で再検証 + drop + SystemMessage 警告注入（enforcement）を行う 2 段構造が有効。hardcoded allowlist は持たず、SDK `list_models()` 経由でモデル能力を single source of truth にする（TTL 1h キャッシュ）。

    **Related:** ADR-0050 (Phase 36 vision fallback), ADR-0025 (SystemMessage 注入)
    ```

    既存 `Frontend・UI` カテゴリセクションを grep で特定し、その末尾に追加:
    ```markdown
    ### 3 入り口統一 staging hook (click / drop / paste)

    ファイル添付の 3 入力経路（`<input type="file">` クリック / DataTransfer drop / ClipboardData paste）を単一 React hook に集約し、各入口から呼ばれる `upload(files: File[])` 一本で state / AbortController / サーバー連動 CRUD を管理する。入口ごとのブラウザ差異（drop の `preventDefault` / paste の `clipboardData.items.kind === 'file'` filter）は hook 内部で吸収。サーバー側は Plan 別に用意した multipart REST と対応付く。

    **Related:** ADR-0050 (Phase 36 useAttachments), ADR-0048 (thread-files フォルダ規約)
    ```

    既存 `patterns.md` の末尾エントリに続く形で、**7 カテゴリ構造を壊さず**、各カテゴリ末尾に追記する。カテゴリの並び順は既存ファイルを読んでから決定。

    **Step 2 — `.planning/phases/36-text-code-image-multimodal/36-VERIFICATION.md` を新規作成**:
    ```markdown
    # Phase 36 — Verification

    **Phase:** 36 (text/code + image multimodal)
    **Requirements:** FIN-01, FIN-02
    **Closed:** 2026-04-??（実行日）

    ## Success Criteria Verdict (from ROADMAP.md)

    | # | Criteria | Status | Evidence |
    |---|----------|--------|----------|
    | 1 | ユーザーが .txt / .md / .json / .csv / .py / .js などの text/code 系ファイルを添付し、LLM がその内容を参照して応答できる | PASS | docs/phase-36-integration-check.md シナリオ 1 |
    | 2 | ユーザーが .png / .jpg / .webp 画像を添付でき、multimodal 対応モデルで画像内容を踏まえた応答を得られる | PASS | シナリオ 2 |
    | 3 | multimodal 非対応モデルが選択されている場合、エラーで止まらず graceful にテキスト要約や警告にフォールバックする | PASS | シナリオ 3 + UI D-17 banner + worker D-18 SystemMessage 注入 |
    | 4 | 添付ファイルがチャット履歴 (PostgreSQL checkpointer) に紐付けされ、スレッドを再オープンしたときも添付情報を確認できる | PASS | シナリオ 4 (F5 リロードで bubble チップ復元) |

    ## Test Coverage

    | Plan | Automated Tests | Manual Check |
    |------|-----------------|--------------|
    | 01 (Wave 0) | tests/test_chat_history_additional_kwargs.py (4) + tests/test_copilot_attachments_spike.py (4) | docs/phase-36-sdk-spike-note.md (docker compose spike, PASS) |
    | 02 | tests/test_copilot_attachments.py (10) + tests/test_api_models_route.py (5) | — |
    | 03 | tests/test_api_chat.py 拡張 (4) + tests/test_attachments_upload_route.py (8) + tests/test_attachments_get_delete_route.py (8) + tests/test_chat_history_additional_kwargs_api.py (1) | — |
    | 04 | tests/test_worker_attachments_payload.py (3) + tests/test_langgraph_handler_attachments_v2.py (5) + tests/test_orchestrator_handler_attachments.py (4) | — |
    | 05 | bun tsc --noEmit (型) | docker compose で 📎 / drop / paste の 3 入り口手動確認 |
    | 06 | bun tsc --noEmit (型) | VisionWarningBanner + 履歴 bubble チップの視覚確認 |
    | 07 | — | docs/phase-36-integration-check.md PASS |

    **Total automated tests added:** [executor 実装後に `pytest --collect-only -q tests/` で集計して記入] (pytest) + type-check (bun tsc)

    ## Open Issues Carried Forward to v6.1+

    - **DebateChat handler の attachments 対応**: Phase 36 は CONTEXT.md Claude's Discretion (ChatApp 中心) に基づき debate_handler.py を未変更で v6.1 defer とした。DebateChat で画像/text 添付しても initial user HumanMessage に `additional_kwargs.attachments` が載らない。v6.1 で同等の注入パターンを追加する
    - **SuperChat SubAgent 側 HumanMessage attachments 注入**: Phase 36 は OrchestratorHandler が `AgentState.new_attachments` に attachments を積むところまで配線。SubAgent 側 (agent.py / tool_agent.py の ReAct ループ) で HumanMessage.additional_kwargs として LLM 入力に載せる最終配線は v6.1 scope。SuperChat 経路で画像添付しても SubAgent が認識しない可能性あり
    - **Gem / Canvas の attachments UX**: InputBar 流用で自動継承の範囲まで。個別 UX 調整 (Gem Knowledge へのファイル取り込み等) は v6.1+
    - **Pillow サムネ生成**: 帯域問題発生時のみ Phase 39 polish で再検討
    - **EXIF / メタデータサニタイズ**: v6.1+ 検討
    - **複数タブからの同時アップロード競合制御**: v6.1+ (`modified_at` 比較 / optimistic concurrency)
    - **OCR (vision 非対応モデル用テキスト抽出)**: v6.1+ (MarkItDown + tesseract 検討)

    ## Security Posture

    - ASVS L1 全カテゴリ該当項目 PASS: V2 (Auth) / V3 (Session) / V4 (Access Control) / V5 (Input Validation) / V12 (File Upload)
    - HIGH severity 脅威 (path traversal / unauthenticated access / cross-user access / image-size DoS) は BLOCKING mitigation として Plan 03 / Plan 05 で対応済
    - 監査ログ: path traversal 試行は `logger.warning` で記録 (attachments.py + chat.py delete_thread)

    ## Related ADRs
    - ADR-0048 (thread-files フォルダ規約 — 書き込み側 Phase 36 で完了)
    - ADR-0050 (Phase 36 本 ADR)
    - ADR-0046 (integration check ゲート)
    - ADR-0038 (AIMessage.name 喪失 — A1 risk の先行例)
    - ADR-0025 (SystemMessage 注入 — D-18 base pattern)
    - ADR-0014 (JWT + endpoint auth)

    ## Sign-off

    - [x] All plans (01-07) complete
    - [x] VALIDATION.md Wave 0 Requirements 全達成
    - [x] ROADMAP.md §Phase 36 Success Criteria 1-4 PASS
    - [x] Integration check (ADR-0046 gate) PASS
    - [x] ADR-0050 起票 + patterns.md 2 エントリ追記
    - [x] PROJECT.md "Current focus" 更新は Plan 07 完了後の別コミット / state サイクルで対応

    **Phase 36 Closed:** Ready for next phase (Phase 32 or 38 による v6.0 milestone 進行)
    ```
  </action>
  <verify>
    <automated>grep -c "additional_kwargs サイドカー\|Vision / model capability fallback\|3 入り口統一 staging" /home/parallels/workspaces/copilot-langgraph/.planning/patterns.md && ls /home/parallels/workspaces/copilot-langgraph/.planning/phases/36-text-code-image-multimodal/36-VERIFICATION.md</automated>
  </verify>
  <done>
    - `.planning/patterns.md` に 3 エントリ（LangGraph・Graph 2 件 + Frontend・UI 1 件）追記
    - 既存 patterns.md の 7 カテゴリ構造が維持されている
    - `36-VERIFICATION.md` に Success Criteria 1-4 の Verdict 表 + Test Coverage + Open Issues + Security Posture + Sign-off が記載されている
    - VERIFICATION.md の Status が "Closed" 相当の記述
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| integration check → docker compose 実環境 | Task 1 で実機ユーザー token を使用。token はセッション Cookie 経由のみ |
| ADR / patterns.md → git | 本 plan のみ git commit 対象。他 plan の成果物は既に commit 済 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-07-01 | Information Disclosure | integration check で application 環境のチャット履歴が docs/phase-36-integration-check.md に記録 | medium | mitigate | Task 1 の手順で「応答抜粋: "..."」を書く際に社内機密や token が混ざらないよう人間が目視確認する |
| T-36-07-02 | Tampering | ADR-0050 の Decision 節と実装が乖離する drift | low | mitigate | Implementation References セクションで具体ファイルを明示。ADR 更新は変更があったときのみ人間が手動で追記（patterns.md 運用ルール D-15 と同じ） |
| T-36-07-03 | Spoofing | integration check を実行しないまま PASS と記録 | **HIGH** | mitigate (BLOCKING) | Task 1 が checkpoint:human-verify で ユーザー承認必須。`docs/phase-36-integration-check.md` に人間が記入 → git add → commit の流れで証跡を残す |
</threat_model>

<verification>
- `docs/phase-36-integration-check.md` に Overall Verdict: PASS が記録され git commit されている
- `docs/adr/0050-*.md` が存在し、`docs/adr/INDEX.md` の自動生成結果に反映されている
- `.planning/patterns.md` に 3 エントリ追記（2 LangGraph・Graph + 1 Frontend・UI）
- `.planning/phases/36-text-code-image-multimodal/36-VERIFICATION.md` に Success Criteria 4/4 PASS
- 全 tests (Plan 01-06 で作った unit/integration) が依然 GREEN である（本 plan で壊さない）
</verification>

<success_criteria>
- Phase 36 Success Criteria 1-4 の実機確認 PASS を ADR-0046 integration check pattern で記録
- ADR-0050 起票、INDEX.md / adr-categories.yaml 反映
- patterns.md に 3 エントリ追記 (D-15 運用ルールに沿う)
- VERIFICATION.md クローズ、次 phase への渡し残件が明示されている
</success_criteria>

<output>
完了後、`.planning/phases/36-text-code-image-multimodal/36-07-SUMMARY.md` を作成する。SUMMARY には以下を含める:
- Phase 36 全体の所要時間・テスト追加数・新規 ADR / patterns エントリ数の集計
- v6.1 検討事項として残る項目 (SuperChat SubAgent 配線 / OCR / EXIF / 複数タブ競合 / サムネ生成)
- 次 phase (32 や 38) への handoff メモ
</output>
