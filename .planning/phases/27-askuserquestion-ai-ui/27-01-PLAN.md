---
phase: 27-askuserquestion-ai-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/utils/system_prompt.py
  - app/jobs/handlers/langgraph_handler.py
  - frontend/src/types.ts
  - frontend/src/components/QuestionPanel.tsx
autonomous: true
requirements:
  - AUQ-01
  - AUQ-02
must_haves:
  truths:
    - "LangGraph 経路（Chat/GemChat/CanvasChat）のシステムプロンプトに AUQ プロトコルが含まれる"
    - "Orchestrator 経路（SuperChat）のシステムプロンプトに AUQ プロトコルが含まれる"
    - "QuestionPanel コンポーネントが single/multi/text の 3 質問タイプをレンダリングできる"
    - "AskUserQuestionPayload 型が types.ts からエクスポートされる"
  artifacts:
    - path: "frontend/src/components/QuestionPanel.tsx"
      provides: "質問パネル UI コンポーネント"
      exports: ["QuestionPanel", "parseAUQ"]
    - path: "frontend/src/types.ts"
      provides: "AUQ 型定義"
      exports: ["AUQOption", "AUQQuestion", "AskUserQuestionPayload"]
    - path: "app/utils/system_prompt.py"
      provides: "AUQ_PROTOCOL 定数 + build_system_prompt_prefix 拡張"
      contains: "AUQ_PROTOCOL"
    - path: "app/jobs/handlers/langgraph_handler.py"
      provides: "effective_system_prompt に AUQ プロトコル追記"
      contains: "AUQ_PROTOCOL"
  key_links:
    - from: "app/utils/system_prompt.py"
      to: "build_system_prompt_prefix"
      via: "AUQ_PROTOCOL 定数を末尾に追加"
      pattern: "AUQ_PROTOCOL"
    - from: "app/jobs/handlers/langgraph_handler.py"
      to: "effective_system_prompt"
      via: "AUQ_PROTOCOL をインポートして末尾連結"
      pattern: "AUQ_PROTOCOL"
---

<objective>
AskUserQuestion のバックエンド基盤（system prompt 注入）と、フロントエンドの型定義・QuestionPanel コンポーネントを作成する。

Purpose: AI エージェントが `<ask_user_question>` タグを生成できるようにし（バックエンド）、その質問を表示する UI コンポーネントの土台を作る（フロントエンド）
Output: system_prompt.py に AUQ_PROTOCOL 定数追加、langgraph_handler.py に AUQ プロトコル注入、types.ts に AUQ 型追加、QuestionPanel.tsx 新規作成
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-askuserquestion-ai-ui/27-CONTEXT.md
@.planning/phases/27-askuserquestion-ai-ui/27-RESEARCH.md
@.planning/phases/27-askuserquestion-ai-ui/27-PATTERNS.md
@.planning/phases/27-askuserquestion-ai-ui/27-UI-SPEC.md

<interfaces>
<!-- 参考実装 — TypeScript 変換元 -->
@work/uaw/AskUserQuestion.jsx
@work/uaw/system_prompt_auq.md

<!-- 既存コード — 変更対象 -->
From app/utils/system_prompt.py:
```python
SECURITY_GUARDRAIL = (...)

def build_system_prompt_prefix(user_id: str | None) -> str:
    parts = [get_datetime_context()]
    if user_id:
        parts.append(f"ログイン中のユーザー: {user_id}")
    parts.append(SECURITY_GUARDRAIL)
    return "\n".join(parts)
```

From app/jobs/handlers/langgraph_handler.py (line 75-76):
```python
datetime_prefix = get_datetime_context()
effective_system_prompt = datetime_prefix + "\n\n" + (system_prompt or "")
```

From frontend/src/types.ts (line 139-143):
```typescript
export interface CanvasResult {
  type: 'canvas';
  app_id: string;
  html: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: バックエンド — AUQ_PROTOCOL を system_prompt.py に追加し、両経路に注入する</name>
  <files>app/utils/system_prompt.py, app/jobs/handlers/langgraph_handler.py</files>
  <read_first>
    - app/utils/system_prompt.py
    - app/jobs/handlers/langgraph_handler.py
    - work/uaw/system_prompt_auq.md
  </read_first>
  <action>
**app/utils/system_prompt.py:**

1. SECURITY_GUARDRAIL 定数の後（line 21 の後）に AUQ_PROTOCOL 定数を追加する。内容は work/uaw/system_prompt_auq.md のプロトコル仕様をそのまま Python 文字列定数化したもの:

```python
AUQ_PROTOCOL = (
    "\n\n## 質問プロトコル\n\n"
    "ユーザーに確認が必要な場合、以下の <ask_user_question> フォーマットのみで応答すること。\n"
    "通常の会話文と混在させてはならない。\n\n"
    "### フォーマット\n\n"
    "<ask_user_question>\n"
    '{"questions": [{"question": "質問テキスト", "header": "ラベル（12文字以内）", '
    '"type": "single|multi|text", '
    '"options": [{"label": "選択肢", "description": "補足説明"}], '
    '"allowFreeText": true, "placeholder": "入力例", "optional": true}]}\n'
    "</ask_user_question>\n\n"
    "### type の使い分け\n"
    "- single: 1つだけ選ぶ（デフォルト）\n"
    "- multi: 複数選べる\n"
    "- text: 自由記述\n\n"
    "### ルール\n"
    "- 選択肢は 2〜4 個に絞ること\n"
    "- 1回のパネルに収める上限は 10 問まで\n"
    "- 確認事項が 10 問を超える場合はテーマ単位で分割して複数ラウンドに分けること\n"
    "- 質問が 1〜3 問程度で済む場合はまとめて 1 回で聞くこと\n"
    "- 回答を受け取ったら作業を進め、追加確認が必要なら再度同フォーマットで質問すること"
)
```

2. `build_system_prompt_prefix` 関数の末尾（line 35、`parts.append(SECURITY_GUARDRAIL)` の次の行）に `parts.append(AUQ_PROTOCOL)` を追加する。これにより Orchestrator 経路（SuperChat）の全 SubAgent に AUQ プロトコルが注入される（D-06 対応）。

**app/jobs/handlers/langgraph_handler.py:**

1. ファイル先頭のインポート部分（line 12 付近）に以下を追加:
```python
from app.utils.system_prompt import AUQ_PROTOCOL
```

2. line 76 の `effective_system_prompt` 構築行を変更:
```python
# 変更前:
effective_system_prompt = datetime_prefix + "\n\n" + (system_prompt or "")
# 変更後:
effective_system_prompt = datetime_prefix + "\n\n" + (system_prompt or "") + AUQ_PROTOCOL
```

これにより LangGraph 経路（Chat/GemChat/CanvasChat）にも AUQ プロトコルが注入される（D-06 対応）。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && python -c "from app.utils.system_prompt import AUQ_PROTOCOL, build_system_prompt_prefix; assert 'ask_user_question' in AUQ_PROTOCOL; prefix = build_system_prompt_prefix('test'); assert 'ask_user_question' in prefix; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - app/utils/system_prompt.py に `AUQ_PROTOCOL` 定数が存在し、`<ask_user_question>` を含む
    - `build_system_prompt_prefix('test')` の戻り値に `ask_user_question` が含まれる
    - app/jobs/handlers/langgraph_handler.py に `from app.utils.system_prompt import AUQ_PROTOCOL` がある
    - app/jobs/handlers/langgraph_handler.py の `effective_system_prompt` に `AUQ_PROTOCOL` が連結されている
  </acceptance_criteria>
  <done>LangGraph 経路と Orchestrator 経路の両方で AI が AUQ プロトコルを認識できる system prompt が生成される</done>
</task>

<task type="auto">
  <name>Task 2: フロントエンド — AUQ 型定義を types.ts に追加し、QuestionPanel.tsx を作成する</name>
  <files>frontend/src/types.ts, frontend/src/components/QuestionPanel.tsx</files>
  <read_first>
    - frontend/src/types.ts
    - work/uaw/AskUserQuestion.jsx
    - .planning/phases/27-askuserquestion-ai-ui/27-UI-SPEC.md
    - .planning/phases/27-askuserquestion-ai-ui/27-PATTERNS.md
  </read_first>
  <action>
**frontend/src/types.ts:**

ファイル末尾（line 144 の `}` の後）に以下の型定義を追加する:

```typescript
// --- Phase 27: AskUserQuestion types ---

export interface AUQOption {
  label: string;
  description?: string;
}

export interface AUQQuestion {
  question: string;
  header: string;
  type?: 'single' | 'multi' | 'text';
  options?: AUQOption[];
  allowFreeText?: boolean;
  placeholder?: string;
  optional?: boolean;
}

export interface AskUserQuestionPayload {
  questions: AUQQuestion[];
}
```

**frontend/src/components/QuestionPanel.tsx:**

新規ファイルを作成する。`work/uaw/AskUserQuestion.jsx` を TypeScript に変換する。主な変換ポイント:

1. `import { useState } from 'react';` + `import type { AUQQuestion, AskUserQuestionPayload } from '../types';` をインポート
2. `parseAUQ` 関数をエクスポート（正規表現 `/<ask_user_question>([\s\S]*?)<\/ask_user_question>/` でタグ検出、JSON.parse で変換）
3. `QuestionPanel` コンポーネント:
   - Props: `{ questions: AUQQuestion[]; onSubmit: (answers: Record<string, string>) => void; }` — D-07 により `locked`/`lockedAnswers` は省略
   - 状態管理: `answers` は `Record<string, { selected?: string | string[] | null; freeText?: string; text?: string }>` 型の useState
   - 関数: `toggleOption`, `setFreeOther`, `setText`, `isAnswered`, `getDisplayValue`, `handleSubmit` を TypeScript 化
   - UI-SPEC に従いインラインスタイルを維持（ダーク/ライト対応は `useCurrentTheme()` で `isDark` を取得）
4. `S.optionIndicator` は関数プロパティのため、独立したヘルパー関数 `getOptionIndicatorStyle(type: string, selected: boolean): React.CSSProperties` として取り出す（Pitfall 5 対応）
5. `locked` 関連の条件分岐（`panelLocked`, `lockedBadge`, `textInputLocked`, `optionDimmed`, `lockedFreeText`）はすべて削除して省略可

スタイル値は work/uaw/AskUserQuestion.jsx の S オブジェクトから:
- panel: `display:'flex', flexDirection:'column', gap:16`
- questionBlock: `display:'flex', flexDirection:'column', gap:8`
- headerBadge: `background:'#1e2a3a', border:'1px solid #2a3a4a', color:'#60a5fa', fontSize:10, fontWeight:700, padding:'4px 8px', borderRadius:4`（UI-SPEC により padding を '2px 8px' → '4px 8px' に修正）
- optionGrid: `display:'grid', gridTemplateColumns:'repeat(2,1fr)', gap:8`（UI-SPEC により gap を 6 → 8 に修正）
- textInput padding: `'8px 12px'`（UI-SPEC により 9px → 8px に修正）
- submitBtn: `background:'#22c55e', border:'none', borderRadius:8, color:'#fff', fontSize:13, fontWeight:700, padding:'10px', cursor:'pointer'`
- submitDisabled: `background:'#1a2a1a', color:'#2a4a2a', cursor:'default'`

ライトテーマ対応: `useCurrentTheme()` hook で isDark を取得し、以下のスタイルを分岐:
- panel 背景: dark `transparent` / light `#ffffff`
- optionBtn 背景: dark `#1a1a22` / light `#f6f8fa`
- optionBtn border: dark `#2a2a35` / light `#d1dbe3`
- textInput 背景: dark `#1a1a22` / light `#fff`
- textInput border: dark `#2a2a35` / light `#d1dbe3`
- テキスト色: dark `#e8e8ec` / light `#24292e`
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/types.ts に `export interface AUQOption` が存在する
    - frontend/src/types.ts に `export interface AUQQuestion` が存在する
    - frontend/src/types.ts に `export interface AskUserQuestionPayload` が存在する
    - frontend/src/components/QuestionPanel.tsx に `export function QuestionPanel` が存在する
    - frontend/src/components/QuestionPanel.tsx に `export function parseAUQ` が存在する
    - QuestionPanel の props に `locked` / `lockedAnswers` が含まれない（D-07）
    - `getOptionIndicatorStyle` がヘルパー関数として独立している（Pitfall 5）
    - `npx tsc --noEmit` が型エラーなしで通る
  </acceptance_criteria>
  <done>AUQ 型定義が types.ts にエクスポートされ、QuestionPanel.tsx が TypeScript コンパイルを通過し、single/multi/text の 3 タイプに対応している</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI レスポンス → フロントエンド | AI が生成した `<ask_user_question>` タグ内 JSON を parseAUQ でパースする |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering | parseAUQ (QuestionPanel.tsx) | mitigate | try-catch で不正 JSON を無視し null を返す。パース失敗は plain text として扱う |
| T-27-02 | Information Disclosure | AUQ_PROTOCOL (system_prompt.py) | accept | プロトコル仕様がプロンプトに含まれるが、社内ツールのため情報漏洩リスクは低い |
</threat_model>

<verification>
1. `python -c "from app.utils.system_prompt import AUQ_PROTOCOL; assert '<ask_user_question>' in AUQ_PROTOCOL"` が成功する
2. `python -c "from app.utils.system_prompt import build_system_prompt_prefix; p = build_system_prompt_prefix('user1'); assert 'ask_user_question' in p"` が成功する
3. `cd frontend && npx tsc --noEmit` が型エラーなし
4. QuestionPanel.tsx に `export function QuestionPanel` と `export function parseAUQ` が存在する
</verification>

<success_criteria>
- AI が AUQ プロトコルを認識できる system prompt が LangGraph 経路・Orchestrator 経路の両方で生成される
- QuestionPanel コンポーネントが TypeScript で正しくコンパイルされ、single/multi/text の 3 質問タイプに対応している
- AUQ 型定義（AUQOption, AUQQuestion, AskUserQuestionPayload）が types.ts からエクスポートされている
</success_criteria>

<output>
After completion, create `.planning/phases/27-askuserquestion-ai-ui/27-01-SUMMARY.md`
</output>
