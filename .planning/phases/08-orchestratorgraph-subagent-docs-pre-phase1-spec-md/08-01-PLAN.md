---
phase: 08-orchestratorgraph-subagent-docs-pre-phase1-spec-md
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - super-agent-sample/pyproject.toml
  - super-agent-sample/src/state.py
  - super-agent-sample/agents/code-reviewer/AGENT.md
  - super-agent-sample/agents/code-reviewer/rules.md
  - super-agent-sample/agents/sql-analyst/AGENT.md
  - super-agent-sample/menus/super-chat.yaml
  - super-agent-sample/menus/simple-chat.yaml
autonomous: true
requirements:
  - SAMPLE-01
  - SAMPLE-02
  - SAMPLE-06
  - SAMPLE-07

must_haves:
  truths:
    - "super-agent-sample/ directory exists as standalone project at repo root"
    - "uv sync succeeds in super-agent-sample/ without errors"
    - "AgentState TypedDict has input, output, messages, next fields"
    - "code-reviewer and sql-analyst AGENT.md parse with python-frontmatter"
    - "Both menu YAML files parse with yaml.safe_load"
  artifacts:
    - path: "super-agent-sample/pyproject.toml"
      provides: "Project metadata and dependencies"
      contains: "python-frontmatter"
    - path: "super-agent-sample/src/state.py"
      provides: "AgentState TypedDict"
      contains: "class AgentState"
    - path: "super-agent-sample/agents/code-reviewer/AGENT.md"
      provides: "Code reviewer agent definition"
      contains: "name: code-reviewer"
    - path: "super-agent-sample/agents/sql-analyst/AGENT.md"
      provides: "SQL analyst agent definition"
      contains: "name: sql-analyst"
    - path: "super-agent-sample/menus/super-chat.yaml"
      provides: "Orchestrator menu definition"
      contains: "graph: orchestrator"
    - path: "super-agent-sample/menus/simple-chat.yaml"
      provides: "Simple chat menu definition"
      contains: "graph: simple"
  key_links: []
---

<objective>
Create feature branch and scaffold the super-agent-sample project: directory structure, pyproject.toml with all dependencies, AgentState, AGENT.md definitions, and menu YAML files.

Purpose: Establish the standalone project foundation that all subsequent modules depend on.
Output: Working uv project with state module, agent definitions, and menu configs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/pre/phase1_spec.md
@.planning/phases/08-orchestratorgraph-subagent-docs-pre-phase1-spec-md/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create feature branch + project scaffold + pyproject.toml + uv sync</name>
  <files>super-agent-sample/pyproject.toml</files>
  <read_first>
    - docs/pre/phase1_spec.md (sections 2, 10 — directory structure and dependencies)
    - .planning/phases/08-orchestratorgraph-subagent-docs-pre-phase1-spec-md/08-RESEARCH.md (pyproject.toml example, pitfalls 4 and 6)
  </read_first>
  <action>
1. Create feature branch from main:
   ```
   git checkout -b feat/super-agent-sample
   ```

2. Create directory structure:
   ```
   mkdir -p super-agent-sample/agents/code-reviewer
   mkdir -p super-agent-sample/agents/sql-analyst
   mkdir -p super-agent-sample/menus
   mkdir -p super-agent-sample/src
   mkdir -p super-agent-sample/tests
   ```

3. Write `super-agent-sample/pyproject.toml` with this exact content:
   ```toml
   [project]
   name = "super-agent-sample"
   version = "0.1.0"
   requires-python = ">=3.11"

   dependencies = [
       "langchain-anthropic>=1.4.0",
       "langgraph>=1.1.4",
       "langchain-core>=0.3.0",
       "python-frontmatter>=1.1.0",
       "pyyaml>=6.0.0",
   ]

   [dependency-groups]
   dev = [
       "pytest>=8.0",
       "pytest-mock>=3.0",
   ]

   [tool.pytest.ini_options]
   testpaths = ["tests"]
   pythonpath = ["src"]
   ```

   CRITICAL: Use `python-frontmatter` (not `frontmatter`) as the package name — they are different PyPI packages (Pitfall 4).

4. Run `cd super-agent-sample && uv sync` to create venv and install all dependencies.

5. Verify install: `cd super-agent-sample && uv run python -c "import frontmatter; import langchain_anthropic; import langgraph; import yaml; print('OK')"`
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph/super-agent-sample && git branch --show-current | grep -q feat/super-agent-sample && uv run python -c "import frontmatter; import langchain_anthropic; import langgraph; import yaml; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - git branch output includes `feat/super-agent-sample`
    - super-agent-sample/pyproject.toml contains `"python-frontmatter>=1.1.0"`
    - super-agent-sample/pyproject.toml contains `pythonpath = ["src"]`
    - super-agent-sample/pyproject.toml contains `"langchain-anthropic>=1.4.0"`
    - `uv run python -c "import frontmatter"` exits 0
    - `uv run python -c "import langchain_anthropic"` exits 0
  </acceptance_criteria>
  <done>Feature branch created and verified, pyproject.toml written, uv sync successful, all 4 core imports work</done>
</task>

<task type="auto">
  <name>Task 2: Create state.py, AGENT.md files, rules.md, and menu YAML files</name>
  <files>
    super-agent-sample/src/state.py,
    super-agent-sample/agents/code-reviewer/AGENT.md,
    super-agent-sample/agents/code-reviewer/rules.md,
    super-agent-sample/agents/sql-analyst/AGENT.md,
    super-agent-sample/menus/super-chat.yaml,
    super-agent-sample/menus/simple-chat.yaml
  </files>
  <read_first>
    - docs/pre/phase1_spec.md (sections 3, 4, 8 — AgentState, AGENT.md format, menu YAML)
  </read_first>
  <action>
1. Write `super-agent-sample/src/state.py` verbatim from spec section 4:
   ```python
   # src/state.py
   from __future__ import annotations
   import operator
   from typing import Annotated
   from typing_extensions import TypedDict
   from langchain_core.messages import BaseMessage


   class AgentState(TypedDict):
       input: str
       output: str
       messages: Annotated[list[BaseMessage], operator.add]
       next: str
   ```

2. Write `super-agent-sample/agents/code-reviewer/AGENT.md` verbatim from spec section 3:
   ```
   ---
   name: code-reviewer
   description: |
     Python/JavaScript/TypeScript コードの静的解析・リント・フォーマットチェックを行う。
     入力: コードスニペットまたはファイルパス
     出力: 指摘リストと修正提案
     対象外: テスト実行 / デプロイ / DB操作
   model: claude-opus-4-6
   ---

   あなたは厳格なコードレビュアーです。
   指摘は重大度（error/warning/info）付きで箇条書きにしてください。
   ```

3. Write `super-agent-sample/agents/code-reviewer/rules.md` as a minimal placeholder:
   ```markdown
   # code-reviewer rules

   > Reserved for Phase 2+. Not loaded in Phase 1.
   ```

4. Write `super-agent-sample/agents/sql-analyst/AGENT.md` — NOT in spec, create reasonable content:
   ```
   ---
   name: sql-analyst
   description: |
     SQLクエリのパフォーマンス分析・最適化を行う。
     入力: SQLクエリまたはテーブル定義
     出力: 実行計画の解説と改善提案
     対象外: データ挿入 / マイグレーション実行 / スキーマ変更の実行
   model: claude-sonnet-4-6
   ---

   あなたはSQLパフォーマンスの専門家です。
   クエリを分析し、以下の観点で改善提案を行ってください：
   - インデックスの活用状況
   - JOIN の効率性
   - サブクエリの最適化
   - 実行計画の改善ポイント
   ```

5. Write `super-agent-sample/menus/super-chat.yaml` verbatim from spec section 8:
   ```yaml
   name: super-chat
   label: "🚀 スーパーチャット"
   description: "複数エージェントを使った高度なタスク処理"
   graph: orchestrator
   enabled: true
   ```

6. Write `super-agent-sample/menus/simple-chat.yaml` verbatim from spec section 8:
   ```yaml
   name: simple-chat
   label: "💬 チャット"
   description: "シンプルな1対1の会話"
   graph: simple
   enabled: true
   ```
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph/super-agent-sample && PYTHONPATH=src uv run python -c "
from state import AgentState
import frontmatter
import yaml

# Verify AgentState fields
assert 'input' in AgentState.__annotations__
assert 'output' in AgentState.__annotations__
assert 'messages' in AgentState.__annotations__
assert 'next' in AgentState.__annotations__

# Verify AGENT.md files parse
cr = frontmatter.load('agents/code-reviewer/AGENT.md')
assert cr.metadata['name'] == 'code-reviewer'
assert cr.metadata['model'] == 'claude-opus-4-6'
assert len(cr.content) > 0

sa = frontmatter.load('agents/sql-analyst/AGENT.md')
assert sa.metadata['name'] == 'sql-analyst'
assert len(sa.content) > 0

# Verify menu YAMLs parse
sc = yaml.safe_load(open('menus/super-chat.yaml'))
assert sc['graph'] == 'orchestrator'
si = yaml.safe_load(open('menus/simple-chat.yaml'))
assert si['graph'] == 'simple'

print('ALL OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - super-agent-sample/src/state.py contains `class AgentState(TypedDict):`
    - super-agent-sample/src/state.py contains `messages: Annotated[list[BaseMessage], operator.add]`
    - super-agent-sample/agents/code-reviewer/AGENT.md contains `name: code-reviewer`
    - super-agent-sample/agents/code-reviewer/AGENT.md contains `model: claude-opus-4-6`
    - super-agent-sample/agents/sql-analyst/AGENT.md contains `name: sql-analyst`
    - super-agent-sample/menus/super-chat.yaml contains `graph: orchestrator`
    - super-agent-sample/menus/simple-chat.yaml contains `graph: simple`
    - Verification script prints `ALL OK`
  </acceptance_criteria>
  <done>AgentState, both AGENT.md files, rules.md placeholder, and both menu YAMLs created and parseable</done>
</task>

</tasks>

<verification>
- `cd super-agent-sample && uv run python -c "from state import AgentState; print('state OK')"` — with PYTHONPATH=src
- All AGENT.md files have valid frontmatter (name, description, model fields)
- Both menu YAML files have name, graph, enabled fields
- `git branch --show-current` shows `feat/super-agent-sample`
</verification>

<success_criteria>
- Feature branch `feat/super-agent-sample` exists and is checked out
- `super-agent-sample/` directory has complete scaffold (agents/, menus/, src/, tests/)
- `uv sync` has run successfully, all imports work
- AgentState TypedDict has 4 fields: input, output, messages, next
- 2 AGENT.md files parse correctly with python-frontmatter
- 2 menu YAML files parse correctly with yaml.safe_load
</success_criteria>

<output>
After completion, create `.planning/phases/08-orchestratorgraph-subagent-docs-pre-phase1-spec-md/08-01-SUMMARY.md`
</output>
