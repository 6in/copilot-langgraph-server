---
phase: 18-canvas-iframe-postmessage-json-rpc-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/jobs/handlers/iframe_rpc_handler.py
  - app/api/routes/iframe_rpc.py
  - app/api/main.py
  - tests/test_iframe_rpc_handler.py
  - tests/test_iframe_rpc_route.py
autonomous: true
requirements: [D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14]

must_haves:
  truths:
    - "IframeRpcHandler が QUERY メソッドで SELECT クエリを実行し rows を返す"
    - "IframeRpcHandler が AI メソッドで ChatCopilot ワンショット呼び出しを行い responseText を返す"
    - "IframeRpcHandler が不明なメソッドでエラーを返す"
    - "is_select_only が SELECT/WITH を許可し INSERT/UPDATE/DELETE を拒否する"
    - "POST /api/iframe-rpc が JWT 認証付きで job_id を返す"
  artifacts:
    - path: "app/jobs/handlers/iframe_rpc_handler.py"
      provides: "IframeRpcHandler class + is_select_only function"
      exports: ["IframeRpcHandler", "is_select_only"]
    - path: "app/api/routes/iframe_rpc.py"
      provides: "POST /api/iframe-rpc endpoint"
      exports: ["router"]
    - path: "tests/test_iframe_rpc_handler.py"
      provides: "Unit tests for handler and SQL validation"
    - path: "tests/test_iframe_rpc_route.py"
      provides: "Unit tests for route endpoint"
  key_links:
    - from: "app/api/routes/iframe_rpc.py"
      to: "arq enqueue_job"
      via: "request.app.state.arq_redis.enqueue_job('process_chat', task_type='iframe_app_api', ...)"
      pattern: "enqueue_job.*iframe_app_api"
    - from: "app/api/main.py"
      to: "app/api/routes/iframe_rpc.py"
      via: "app.include_router(iframe_rpc.router)"
      pattern: "include_router.*iframe_rpc"
---

<objective>
IframeRpcHandler 実装 + POST /api/iframe-rpc エンドポイント + テスト

Purpose: iframe から JSON-RPC リクエストを受け取るバックエンド処理の中核を構築する。QUERY（DB）と AI（Copilot）の 2 メソッドを処理するハンドラと、arq キューへの enqueue エンドポイントを実装する。
Output: IframeRpcHandler クラス、iframe-rpc ルート、テスト一式
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-canvas-iframe-postmessage-json-rpc-api/18-CONTEXT.md
@.planning/phases/18-canvas-iframe-postmessage-json-rpc-api/18-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From app/jobs/handlers/base.py:
```python
class TaskHandler(ABC):
    @abstractmethod
    async def handle(self, ctx: dict, job: dict) -> dict:
        """Execute the task. Returns dict with {"job_id": ..., "status": "done"}."""
```

From app/jobs/notifier.py:
```python
def build_notifier(reply_to: dict, job_store: JobStore) -> Notifier:
    ...
# notifier.done() — signals SSE completion
# notifier.progress(msg) — sends progress event
```

From app/jobs/job_store.py:
```python
class JobStore:
    async def save_result(self, job_id: str, result: str) -> None: ...
    async def get(self, job_id: str) -> dict | None: ...
```

From app/providers/copilot.py:
```python
class ChatCopilot(BaseChatModel):
    def __init__(self, *, github_token: str = "", model: str = "claude-sonnet-4.5", ...): ...
    async def ainvoke(self, messages: list) -> AIMessage: ...
    async def close(self) -> None: ...
```

From app/api/routes/chat.py:
```python
async def get_jwt_payload(request: Request) -> dict: ...
async def get_github_token(payload: dict = Depends(get_jwt_payload)) -> str: ...
```

From app/jobs/worker.py (process_chat signature — will be extended in Plan 02):
```python
async def process_chat(ctx: dict, *, job_id: str, thread_id: str, prompt: str,
    model: str = "claude-sonnet-4.5", github_token: str, reply_to: dict,
    task_type: str = "langgraph", ...) -> dict:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IframeRpcHandler + is_select_only + テスト</name>
  <files>app/jobs/handlers/iframe_rpc_handler.py, tests/test_iframe_rpc_handler.py</files>
  <read_first>
    - app/jobs/handlers/base.py
    - app/jobs/handlers/langgraph_handler.py
    - app/jobs/notifier.py
    - app/jobs/job_store.py
    - app/providers/copilot.py
    - .planning/phases/18-canvas-iframe-postmessage-json-rpc-api/18-RESEARCH.md (sections 1, 3, code examples)
  </read_first>
  <behavior>
    - is_select_only("SELECT * FROM users") → True
    - is_select_only("select count(*) from items") → True (case insensitive)
    - is_select_only("WITH cte AS (SELECT 1) SELECT * FROM cte") → True
    - is_select_only("INSERT INTO users VALUES (1)") → False
    - is_select_only("UPDATE users SET name='x'") → False
    - is_select_only("DELETE FROM users") → False
    - is_select_only("DROP TABLE users") → False
    - is_select_only("SELECT 1; DROP TABLE users") → False (multiple statements)
    - is_select_only("/* comment */ SELECT 1") → True (SQL comments stripped)
    - is_select_only("-- comment\nSELECT 1") → True
    - is_select_only("") → False (empty string)
    - _handle_query with unknown pool_name → {"result": False, "error": "Unknown pool: xxx"}
    - _handle_query with non-SELECT → {"result": False, "error": "Only SELECT queries are allowed"}
    - _handle_query with valid SELECT + mock pool → {"result": True, "rows": [...]}
    - _handle_ai with mock ChatCopilot → {"result": True, "responseText": "..."}
    - handle with unknown method → {"result": False, "error": "Unknown method: XXX"}
  </behavior>
  <action>
    1. Create `app/jobs/handlers/iframe_rpc_handler.py`:

    ```python
    import json
    import re
    from psycopg.rows import dict_row
    from app.jobs.handlers.base import TaskHandler
    from app.jobs.notifier import build_notifier
    from app.providers.copilot import ChatCopilot
    from langchain_core.messages import HumanMessage

    _COMMENT_RE = re.compile(r'/\*.*?\*/|--[^\n]*', re.DOTALL)
    _ALLOWED_PREFIXES = frozenset(["SELECT", "WITH"])

    def is_select_only(sql: str) -> bool:
        """SELECT or WITH prefixed queries only. Multi-statement (;) rejected."""
        cleaned = _COMMENT_RE.sub('', sql).strip()
        body = cleaned.rstrip(';')
        if ';' in body:
            return False
        tokens = body.split()
        return bool(tokens) and tokens[0].upper() in _ALLOWED_PREFIXES

    class IframeRpcHandler(TaskHandler):
        async def handle(self, ctx: dict, job: dict) -> dict:
            job_id = job["job_id"]
            reply_to = job["reply_to"]
            method = job.get("rpc_method", "")
            params = job.get("rpc_params") or {}
            job_store = ctx["job_store"]
            notifier = build_notifier(reply_to, job_store)
            try:
                if method == "QUERY":
                    result = await self._handle_query(ctx, params)
                elif method == "AI":
                    result = await self._handle_ai(job, params)
                else:
                    result = {"result": False, "error": f"Unknown method: {method}"}
                await job_store.save_result(job_id, json.dumps(result))
                await notifier.done()
            except Exception as e:
                await job_store.save_result(job_id, json.dumps({"result": False, "error": str(e)}))
                await notifier.done()
            return {"job_id": job_id, "status": "done"}

        async def _handle_query(self, ctx: dict, params: dict) -> dict:
            pool_name = params.get("pool_name", "")
            sql = params.get("sql", "")
            if not is_select_only(sql):
                return {"result": False, "error": "Only SELECT queries are allowed"}
            db_pools = ctx.get("db_pools", {})
            if pool_name not in db_pools:
                return {"result": False, "error": f"Unknown pool: {pool_name}"}
            pool = db_pools[pool_name]
            async with pool.connection() as conn:
                async with conn.cursor(row_factory=dict_row) as cur:
                    await cur.execute(sql)
                    rows = await cur.fetchall()
            return {"result": True, "rows": [dict(r) for r in rows]}

        async def _handle_ai(self, job: dict, params: dict) -> dict:
            model = params.get("model", "claude-sonnet-4.5")
            prompt = params.get("prompt", "")
            github_token = job.get("github_token", "")
            llm = ChatCopilot(github_token=github_token, model=model)
            try:
                result = await llm.ainvoke([HumanMessage(content=prompt)])
                return {"result": True, "responseText": result.content}
            finally:
                await llm.close()
    ```

    2. Create `tests/test_iframe_rpc_handler.py` with pytest-asyncio tests:
       - Test `is_select_only` with all behavior cases above (parametrize)
       - Test `_handle_query` with mock pool (AsyncMock for connection/cursor/execute/fetchall)
       - Test `_handle_query` with unknown pool_name
       - Test `_handle_query` with non-SELECT rejection
       - Test `_handle_ai` with mock ChatCopilot (patch `app.providers.copilot.ChatCopilot`)
       - Test `handle` with unknown method
       - Mock `ctx["job_store"]` with AsyncMock (save_result, get)
       - Mock `build_notifier` to return AsyncMock with done/progress methods

    Per D-09: SELECT only. Per D-10/D-11: result format {"result": true/false, ...}. Per D-12/D-13/D-14: AI params and ChatCopilot direct.
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && uv run pytest tests/test_iframe_rpc_handler.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - app/jobs/handlers/iframe_rpc_handler.py contains `class IframeRpcHandler(TaskHandler):`
    - app/jobs/handlers/iframe_rpc_handler.py contains `def is_select_only(sql: str) -> bool:`
    - app/jobs/handlers/iframe_rpc_handler.py contains `async def _handle_query(self, ctx: dict, params: dict) -> dict:`
    - app/jobs/handlers/iframe_rpc_handler.py contains `async def _handle_ai(self, job: dict, params: dict) -> dict:`
    - app/jobs/handlers/iframe_rpc_handler.py contains `"Unknown method:"`
    - app/jobs/handlers/iframe_rpc_handler.py contains `"Only SELECT queries are allowed"`
    - app/jobs/handlers/iframe_rpc_handler.py contains `"Unknown pool:"`
    - app/jobs/handlers/iframe_rpc_handler.py contains `ChatCopilot(github_token=`
    - app/jobs/handlers/iframe_rpc_handler.py contains `await llm.close()`
    - tests/test_iframe_rpc_handler.py contains `test_is_select_only`
    - tests/test_iframe_rpc_handler.py contains `test_handle_query`
    - tests/test_iframe_rpc_handler.py contains `test_handle_ai`
    - `uv run pytest tests/test_iframe_rpc_handler.py -x -q` exits 0
  </acceptance_criteria>
  <done>IframeRpcHandler が QUERY/AI メソッドを処理し、is_select_only が正しく SQL を判定する。全テスト合格。</done>
</task>

<task type="auto">
  <name>Task 2: POST /api/iframe-rpc エンドポイント + router 登録 + テスト</name>
  <files>app/api/routes/iframe_rpc.py, app/api/main.py, tests/test_iframe_rpc_route.py</files>
  <read_first>
    - app/api/routes/chat.py (enqueue_job pattern, get_jwt_payload, get_github_token)
    - app/api/main.py (include_router pattern)
    - app/api/models.py (ChatAsyncResponse pattern)
    - .planning/phases/18-canvas-iframe-postmessage-json-rpc-api/18-RESEARCH.md (section 5)
  </read_first>
  <action>
    1. Create `app/api/routes/iframe_rpc.py`:

    ```python
    """Iframe JSON-RPC bridge endpoint (Phase 18).

    POST /api/iframe-rpc — receives JSON-RPC request from CanvasPane,
    enqueues as arq job with task_type='iframe_app_api', returns job_id.
    JWT authentication required (parent frame is authenticated).
    """
    import uuid
    from fastapi import APIRouter, Depends, Request
    from pydantic import BaseModel
    from app.api.routes.chat import get_jwt_payload, get_github_token

    router = APIRouter(prefix="/api", tags=["iframe-rpc"])

    class IframeRpcRequest(BaseModel):
        id: str
        method: str
        params: dict | None = None

    class IframeRpcResponse(BaseModel):
        job_id: str

    @router.post("/iframe-rpc", response_model=IframeRpcResponse)
    async def iframe_rpc(
        request: Request,
        body: IframeRpcRequest,
        github_token: str = Depends(get_github_token),
        payload: dict = Depends(get_jwt_payload),
    ):
        from arq import ArqRedis
        arq_redis: ArqRedis = request.app.state.arq_redis
        job_id = str(uuid.uuid4())
        github_login = payload.get("github_login", "unknown")

        await arq_redis.enqueue_job(
            "process_chat",
            job_id=job_id,
            thread_id="",  # iframe-rpc does not use threads
            prompt="",     # not used by IframeRpcHandler
            github_token=github_token,
            reply_to={"type": "web", "job_id": job_id},
            task_type="iframe_app_api",
            github_login=github_login,
            rpc_method=body.method,
            rpc_params=body.params or {},
        )

        return IframeRpcResponse(job_id=job_id)
    ```

    2. Update `app/api/main.py`:
       - Add import: `from app.api.routes import iframe_rpc` (add to existing import line 25 after `canvas`)
       - Add `app.include_router(iframe_rpc.router)` after the `canvas.router` line (after line 292)

    3. Create `tests/test_iframe_rpc_route.py`:
       - Test POST /api/iframe-rpc with valid JWT → returns 200 + {"job_id": "..."}
       - Test POST /api/iframe-rpc without JWT → returns 401
       - Mock `request.app.state.arq_redis.enqueue_job` as AsyncMock
       - Mock `get_jwt_payload` and `get_github_token` dependencies
       - Verify enqueue_job was called with task_type="iframe_app_api", rpc_method, rpc_params
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && uv run pytest tests/test_iframe_rpc_route.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - app/api/routes/iframe_rpc.py contains `router = APIRouter(prefix="/api", tags=["iframe-rpc"])`
    - app/api/routes/iframe_rpc.py contains `@router.post("/iframe-rpc"`
    - app/api/routes/iframe_rpc.py contains `task_type="iframe_app_api"`
    - app/api/routes/iframe_rpc.py contains `rpc_method=body.method`
    - app/api/routes/iframe_rpc.py contains `rpc_params=body.params`
    - app/api/routes/iframe_rpc.py contains `Depends(get_github_token)`
    - app/api/main.py contains `iframe_rpc`
    - app/api/main.py contains `include_router(iframe_rpc.router)`
    - tests/test_iframe_rpc_route.py contains `test_` (at least one test function)
    - `uv run pytest tests/test_iframe_rpc_route.py -x -q` exits 0
  </acceptance_criteria>
  <done>POST /api/iframe-rpc エンドポイントが JWT 認証付きで動作し、arq に iframe_app_api タスクを enqueue する。テスト合格。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iframe → parent (postMessage) | 信頼されていない iframe JS コードからの入力 |
| parent → POST /api/iframe-rpc | 認証済みユーザーからの API リクエスト |
| worker → external DB | QUERY メソッドによる外部 DB への SQL 実行 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | is_select_only | mitigate | SQL コメント除去 + 先頭トークン比較 + 複数文拒否。第 2 防御として db_pools.yaml に読み取り専用 DB ユーザー使用を明記 |
| T-18-02 | Tampering | _handle_query | mitigate | pool_name を ctx["db_pools"] の既知キーに限定。未知の pool_name はエラー返却 |
| T-18-03 | Spoofing | POST /api/iframe-rpc | mitigate | Depends(get_github_token) で JWT 認証必須。未認証は 401 |
| T-18-04 | Information Disclosure | error messages | mitigate | 例外メッセージは str(e) で返却するが、DB 接続文字列等の内部情報は漏洩しない（psycopg エラーはクエリ情報のみ）。将来的により厳格なサニタイズを検討 |
| T-18-05 | Tampering | CTE + 書き込み | accept | WITH ... DELETE は is_select_only をすり抜ける可能性あり。DB ユーザーを読み取り専用ロールに制限する第 2 防御で実害防止。db_pools.yaml コメントに明記 |
</threat_model>

<verification>
- `uv run pytest tests/test_iframe_rpc_handler.py tests/test_iframe_rpc_route.py -x -q` — 全テスト合格
- `grep -r "iframe_app_api" app/` — handler + route に存在確認
- `grep "iframe_rpc" app/api/main.py` — router 登録確認
</verification>

<success_criteria>
- IframeRpcHandler が QUERY/AI を処理し、正しいフォーマットで結果を返す
- is_select_only が SELECT/WITH のみ許可、他を拒否
- POST /api/iframe-rpc が JWT 認証付きで job_id を返す
- 全テストが合格する
</success_criteria>

<output>
After completion, create `.planning/phases/18-canvas-iframe-postmessage-json-rpc-api/18-01-SUMMARY.md`
</output>
