---
phase: 18-canvas-iframe-postmessage-json-rpc-api
plan: 02
type: execute
wave: 1
depends_on: [18-01]
files_modified:
  - app/jobs/worker.py
  - config/db_pools.yaml
  - config/db_pools.yaml.example
  - .gitignore
  - docker-compose.yml
autonomous: true
requirements: [D-06, D-15, D-16, D-17, D-18]

must_haves:
  truths:
    - "arq ワーカーが TASK_HANDLERS に iframe_app_api を持つ"
    - "process_chat シグネチャに rpc_method/rpc_params が追加されている"
    - "ワーカー startup で db_pools.yaml を読み込み AsyncConnectionPool を初期化する"
    - "ワーカー shutdown で全 pool を close する"
    - "config/db_pools.yaml.example にフォーマット例が存在する"
    - ".gitignore に config/db_pools.yaml が含まれる"
  artifacts:
    - path: "app/jobs/worker.py"
      provides: "Extended worker with IframeRpcHandler + DB pool lifecycle"
      contains: "iframe_app_api"
    - path: "config/db_pools.yaml.example"
      provides: "DB pool configuration example"
      contains: "pools:"
    - path: "config/db_pools.yaml"
      provides: "Actual DB pool config (gitignored)"
      contains: "pools:"
    - path: "docker-compose.yml"
      provides: "Explicit config/ volume mount for api and worker services (D-17)"
      contains: "./config:/app/config"
  key_links:
    - from: "app/jobs/worker.py"
      to: "app/jobs/handlers/iframe_rpc_handler.py"
      via: "import IframeRpcHandler, add to TASK_HANDLERS"
      pattern: "iframe_app_api.*IframeRpcHandler"
    - from: "app/jobs/worker.py"
      to: "config/db_pools.yaml"
      via: "yaml.safe_load in startup hook"
      pattern: "db_pools.*yaml"
---

<objective>
arq ワーカー拡張 + DB プール管理 + config 設定

Purpose: IframeRpcHandler をワーカーに登録し、DB 接続プールのライフサイクル管理と設定ファイルを整備する。process_chat シグネチャを拡張して rpc_method/rpc_params を受け渡しできるようにする。
Output: 拡張された worker.py、db_pools.yaml 設定ファイル
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-canvas-iframe-postmessage-json-rpc-api/18-CONTEXT.md
@.planning/phases/18-canvas-iframe-postmessage-json-rpc-api/18-RESEARCH.md
@.planning/phases/18-canvas-iframe-postmessage-json-rpc-api/18-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->
From app/jobs/handlers/iframe_rpc_handler.py:
```python
from app.jobs.handlers.base import TaskHandler

class IframeRpcHandler(TaskHandler):
    async def handle(self, ctx: dict, job: dict) -> dict:
        # Expects: job["rpc_method"], job["rpc_params"], ctx["db_pools"]
        ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: config/db_pools.yaml + .gitignore + example ファイル</name>
  <files>config/db_pools.yaml, config/db_pools.yaml.example, .gitignore</files>
  <read_first>
    - .gitignore
    - .planning/phases/18-canvas-iframe-postmessage-json-rpc-api/18-RESEARCH.md (section 7, db_pools.yaml format)
  </read_first>
  <action>
    1. Create directory `config/` if not exists.

    2. Create `config/db_pools.yaml.example` (committed to git):
    ```yaml
    # DB connection pool configuration for iframe-rpc QUERY method.
    # Copy this file to db_pools.yaml and configure your DSN values.
    #
    # SECURITY: Use a READ-ONLY database user for all pools.
    # The is_select_only() check is a first defense; a read-only DB role
    # is the second defense against CTE+write bypass (WITH ... DELETE).
    #
    # Format:
    #   pools:
    #     <pool_name>:
    #       dsn: postgresql://<user>:<password>@<host>:<port>/<database>
    pools:
      main:
        dsn: postgresql://readonly_user:password@postgres:5432/app
      # analytics:
      #   dsn: postgresql://readonly_user:password@analytics-db:5432/data
    ```

    3. Create `config/db_pools.yaml` (gitignored, actual config for dev):
    ```yaml
    # Local development DB pool config.
    # Uses the same postgres instance as the main app.
    pools:
      main:
        dsn: postgresql://postgres:postgres@postgres:5432/postgres?sslmode=disable
    ```

    4. Append to `.gitignore` (if not already present):
    ```
    # DB pool config (contains DSN credentials)
    config/db_pools.yaml
    ```

    Per D-15: config/db_pools.yaml に DSN のみ記述。Per D-16: pools: { name: { dsn: ... } } 形式。
  </action>
  <verify>
    <automated>test -f /home/parallels/workspaces/copilot-langgraph/config/db_pools.yaml.example && test -f /home/parallels/workspaces/copilot-langgraph/config/db_pools.yaml && grep "db_pools.yaml" /home/parallels/workspaces/copilot-langgraph/.gitignore && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - config/db_pools.yaml.example contains `pools:`
    - config/db_pools.yaml.example contains `dsn:`
    - config/db_pools.yaml.example contains `READ-ONLY` (security comment)
    - config/db_pools.yaml contains `pools:`
    - .gitignore contains `config/db_pools.yaml`
  </acceptance_criteria>
  <done>db_pools.yaml 設定ファイルと example が作成され、.gitignore に追加済み。</done>
</task>

<task type="auto">
  <name>Task 2: worker.py 拡張 — IframeRpcHandler 登録 + DB プールライフサイクル + process_chat シグネチャ拡張</name>
  <files>app/jobs/worker.py</files>
  <read_first>
    - app/jobs/worker.py
    - app/jobs/handlers/iframe_rpc_handler.py
    - .planning/phases/18-canvas-iframe-postmessage-json-rpc-api/18-RESEARCH.md (sections 1, 2, code examples for startup/shutdown)
  </read_first>
  <action>
    1. Add imports at top of worker.py:
    ```python
    import yaml
    from psycopg_pool import AsyncConnectionPool
    from app.jobs.handlers.iframe_rpc_handler import IframeRpcHandler
    ```

    2. Add to TASK_HANDLERS dict (after "debate" entry, line ~32):
    ```python
    "iframe_app_api": IframeRpcHandler(),  # Phase 18: iframe JSON-RPC bridge
    ```

    3. Add env var for config path (after REDIS_URL line ~27):
    ```python
    DB_POOLS_CONFIG = os.getenv("DB_POOLS_CONFIG", "config/db_pools.yaml")
    ```

    4. Extend `startup(ctx)` function — add DB pool initialization AFTER the existing Redis/JobStore/checkpointer setup:
    ```python
    # Phase 18: DB connection pools for iframe-rpc QUERY method
    ctx["db_pools"] = {}
    try:
        with open(DB_POOLS_CONFIG) as f:
            pools_cfg = yaml.safe_load(f) or {}
        for name, cfg in pools_cfg.get("pools", {}).items():
            pool = AsyncConnectionPool(conninfo=cfg["dsn"], open=False, min_size=1, max_size=5)
            await pool.open(wait=True, timeout=30.0)
            ctx["db_pools"][name] = pool
    except FileNotFoundError:
        pass  # No config file — QUERY method unavailable (non-fatal)
    ```

    5. Extend `shutdown(ctx)` function — add pool cleanup BEFORE the existing Redis close:
    ```python
    # Phase 18: Close DB pools
    for pool in ctx.get("db_pools", {}).values():
        await pool.close()
    ```

    6. Add to `process_chat` function signature (after `current_turn: int = 0,` around line 73):
    ```python
    # Phase 18: iframe JSON-RPC bridge
    rpc_method: str | None = None,
    rpc_params: dict | None = None,
    ```

    7. Add to `job` dict construction (after `"current_turn": current_turn,` around line 107):
    ```python
    # Phase 18
    "rpc_method": rpc_method,
    "rpc_params": rpc_params,
    ```

    Per D-06: task_type="iframe_app_api"。Per D-18: AsyncConnectionPool, open=False + await pool.open(wait=True)。
    Pitfall 1 (RESEARCH.md): open=False で作成し startup hook 内で await pool.open()。
    Pitfall 2 (RESEARCH.md): rpc_method/rpc_params はデフォルト None で後方互換。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && grep "iframe_app_api" app/jobs/worker.py && grep "rpc_method" app/jobs/worker.py && grep "db_pools" app/jobs/worker.py && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - app/jobs/worker.py contains `from app.jobs.handlers.iframe_rpc_handler import IframeRpcHandler`
    - app/jobs/worker.py contains `"iframe_app_api": IframeRpcHandler()`
    - app/jobs/worker.py contains `DB_POOLS_CONFIG = os.getenv("DB_POOLS_CONFIG"`
    - app/jobs/worker.py contains `import yaml`
    - app/jobs/worker.py contains `from psycopg_pool import AsyncConnectionPool`
    - app/jobs/worker.py contains `ctx["db_pools"] = {}`
    - app/jobs/worker.py contains `await pool.open(wait=True`
    - app/jobs/worker.py contains `await pool.close()`
    - app/jobs/worker.py contains `rpc_method: str | None = None`
    - app/jobs/worker.py contains `rpc_params: dict | None = None`
    - app/jobs/worker.py contains `"rpc_method": rpc_method`
    - app/jobs/worker.py contains `"rpc_params": rpc_params`
    - Existing tests still pass: `uv run pytest tests/ -x -q --ignore=tests/test_iframe_rpc_handler.py --ignore=tests/test_iframe_rpc_route.py -q` (or no regressions)
  </acceptance_criteria>
  <done>worker.py に IframeRpcHandler 登録、DB プール初期化/終了、process_chat シグネチャ拡張が完了。</done>
</task>

<task type="auto">
  <name>Task 3: docker-compose.yml — config/ ボリュームマウント追加（D-17）</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (既存の volumes セクションと api/worker サービスの volumes 設定を確認)
  </read_first>
  <action>
    D-17 の locked decision に従い、docker-compose.yml の api サービスと worker サービスに `./config:/app/config:ro` を明示的に追加する。

    `./config:/app/config:ro` は `.:/app` で既にカバーされているが、意図を明示するために追加する。

    1. `api` サービスの `volumes:` セクションに追加:
    ```yaml
    - ./config:/app/config:ro  # iframe-rpc DB pool config (Phase 18)
    ```

    2. `worker` サービスの `volumes:` セクションに追加:
    ```yaml
    - ./config:/app/config:ro  # iframe-rpc DB pool config (Phase 18)
    ```

    既存の volumes エントリの後（または既存の行に合わせた適切な位置）に追加する。
    docker-compose.yml のインデントと既存スタイルに合わせること（スペース 6 個インデントが標準）。
  </action>
  <verify>
    <automated>grep "./config:/app/config" /home/parallels/workspaces/copilot-langgraph/docker-compose.yml | wc -l | grep -E "^[2-9]|^[1-9][0-9]" && echo "OK" || (grep "./config:/app/config" /home/parallels/workspaces/copilot-langgraph/docker-compose.yml && echo "found but count check failed")</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml contains `./config:/app/config:ro` (at least 2 occurrences — api and worker services)
    - `docker compose config` exits 0 (valid YAML)
  </acceptance_criteria>
  <done>docker-compose.yml の api/worker サービスに ./config:/app/config:ro マウントが明示的に追加済み（D-17 実装）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config/db_pools.yaml → worker | YAML ファイルから DSN を読み込む |
| worker → external DB | AsyncConnectionPool 経由で外部 DB に接続 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06 | Information Disclosure | config/db_pools.yaml | mitigate | .gitignore に追加。DSN にパスワードが含まれるためリポジトリにコミットしない |
| T-18-07 | Tampering | CTE write bypass | accept | is_select_only の WITH 許可により WITH...DELETE が通過する可能性。db_pools.yaml.example に READ-ONLY ユーザー使用を明記（第 2 防御）|
| T-18-08 | Denial of Service | DB pool exhaustion | mitigate | max_size=5 で接続数上限を設定。pool.open(wait=True, timeout=30.0) でタイムアウト付き |
</threat_model>

<verification>
- `grep "iframe_app_api" app/jobs/worker.py` — handler 登録確認
- `grep "rpc_method" app/jobs/worker.py` — シグネチャ拡張確認
- `grep "db_pools" app/jobs/worker.py` — pool 管理コード確認
- `test -f config/db_pools.yaml.example` — example ファイル存在確認
</verification>

<success_criteria>
- worker.py が IframeRpcHandler を iframe_app_api として登録
- process_chat が rpc_method/rpc_params を受け取り job dict に含める
- startup/shutdown で DB プールのライフサイクルを管理
- config ファイルが適切に作成され、秘匿情報が gitignore に含まれる
</success_criteria>

<output>
After completion, create `.planning/phases/18-canvas-iframe-postmessage-json-rpc-api/18-02-SUMMARY.md`
</output>
