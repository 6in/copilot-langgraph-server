---
phase: 39-ui-polish
plan: 06
type: execute
wave: 1
depends_on: [01]
files_modified:
  - tests/test_generate_mcp_artifacts.py
  - tests/test_mcp_server.py
  - tests/test_tool_catalog_js.py
  - tests/test_tool_registry.py
autonomous: true
requirements: [UIFIX-04]
tags: [polish, test, mcp-catalog, drift]

must_haves:
  decision_citations:
    - "`D-09:` pytest 数値 drift (6→8) + cwd= 引数 6 件削除。"
  truths:
    - "tests/test_generate_mcp_artifacts.py の 4 件 (test_load_tools_has_six_tools / test_build_helper_has_four_functions / test_build_js_order / test_build_docs_header_and_table) が all green"
    - "tests/test_mcp_server.py の claude_code 系 6 件 (L293/316/341/372/394/410) から `cwd=` 引数が削除されている"
    - "tests/test_tool_catalog_js.py::test_catalog_contains_six_tools が green (8 件対応もしくは命名更新)"
    - "tests/test_tool_registry.py::test_tool_registry_real_yaml_contract が green (8 件対応)"
    - "pytest tests/ --ignore=tests/test_mcp_server.py の failed 件数が Plan 04 完了時点から 6 件減 (D-10 Pattern E 6 件解消)"
  artifacts:
    - path: "tests/test_generate_mcp_artifacts.py"
      provides: "実 YAML 8 ツールの assertion (6→8 drift 解消)"
      contains: "len(tools) == 8"
    - path: "tests/test_mcp_server.py"
      provides: "claude_code 系テストから cwd= 引数を削除した signature 互換テスト"
  key_links:
    - from: "config/mcp_tools.yaml (8 ツール SSoT)"
      to: "tests/test_generate_mcp_artifacts.py / test_tool_catalog_js.py / test_tool_registry.py の assertion"
      via: "YAML 順 = ping, web_search, db_query, claude_code, execute_python, get_current_datetime, attachments_list, attachments_extract"
      pattern: "attachments_list|attachments_extract"
---

<objective>
UIFIX-04 D-09 (pytest 数値 drift + cwd 引数の bundle 修正) と D-10 Pattern E (tool catalog drift 6 件) を 1 plan で潰す。両方とも `config/mcp_tools.yaml` の Phase 37 で attachments_list / attachments_extract が 6→8 に増えたときの test 側更新漏れと、Phase 38 Plan 03 で claude_code signature から `cwd` 引数が破壊的削除された際の test 側更新漏れの 2 種の drift。

Purpose:
- D-09: tests/test_generate_mcp_artifacts.py の `len(tools) == 6` を `== 8` に更新 + `names` list / `sections` list / `found` list (build_js_order) / build_docs sections を実 YAML 順 (ping, web_search, db_query, claude_code, execute_python, get_current_datetime, attachments_list, attachments_extract) に更新
- D-09: tests/test_mcp_server.py の `cwd="/tmp"` 引数を 6 箇所 (L293, L316, L341, L372, L394, L410) から削除
- D-10 Pattern E: tests/test_tool_catalog_js.py::test_catalog_contains_six_tools / tests/test_tool_registry.py::test_tool_registry_real_yaml_contract の assertion も 6→8 で更新

Output:
- 上記 4 test ファイルの assertion / kwargs 更新のみ。production code (app/ や mcp_server/) には**触らない**
- pytest --ignore=tests/test_mcp_server.py の failed 件数が Plan 04 完了後の 23 件から 17 件以下に (Pattern E 6 件減)
- `grep -c 'cwd=' tests/test_mcp_server.py` が 0 (D-09 cwd= 削除完了)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-ui-polish/39-CONTEXT.md
@.planning/phases/39-ui-polish/39-RESEARCH.md
@.planning/phases/39-ui-polish/39-PATTERNS.md
@.planning/phases/39-ui-polish/39-BASELINE.md
@config/mcp_tools.yaml
@tests/test_generate_mcp_artifacts.py
@tests/test_mcp_server.py
@tests/test_tool_catalog_js.py
@tests/test_tool_registry.py

<interfaces>
config/mcp_tools.yaml 実装上の tool 順 (8 件、ファイル内の `^  - name:` grep 確認済):
  1. ping
  2. web_search
  3. db_query
  4. claude_code
  5. execute_python
  6. get_current_datetime
  7. attachments_list      ← Phase 37 で追加
  8. attachments_extract   ← Phase 37 で追加

test_generate_mcp_artifacts.py の更新対象 (4 箇所):

1) test_load_tools_has_six_tools (L37-49):
   現状: `assert len(tools) == 6`
   修正: `assert len(tools) == 8` + names list に attachments_list / attachments_extract 追加 (YAML 順末尾)
   関数名も `test_load_tools_has_eight_tools` にリネーム (テスト内容と合致させる)

2) test_build_helper_has_four_functions (L65-82):
   現状: build_helper 出力に search / query_db / get_datetime / ping の 4 関数 (これは python_wrapper を持つ tool のみ、attachments_list / attachments_extract の python_wrapper の有無を確認して assertion 更新)
   - もし attachments_list / attachments_extract に python_wrapper があれば 4 → 6 関数に更新 + 関数名リネーム
   - python_wrapper なしの場合 (claude_code / execute_python と同じく) 4 関数のままで OK、本 task は assertion 内容を変更しない (実行で green になるのみ)

3) test_build_js_order (L121-135):
   現状: `found == ["ping", "web_search", "db_query", "claude_code", "execute_python", "get_current_datetime"]`
   修正: 末尾に `"attachments_list", "attachments_extract"` を追加 (YAML 順)

4) test_build_docs_header_and_table (L143-158):
   現状: `sections == ["ping", "web_search", "db_query", "claude_code", "execute_python", "get_current_datetime"]` + docstring「6 セクションを含む」
   修正: 末尾に "attachments_list", "attachments_extract" 追加 + docstring を「8 セクションを含む」に更新

test_mcp_server.py の cwd= 引数 6 箇所 (D-09):
  L293: await claude_code(prompt="test", cwd="/tmp")          → await claude_code(prompt="test")
  L316: result = await claude_code(prompt="test", cwd="/tmp") → 同上
  L341: result = await claude_code(prompt="test", cwd="/tmp") → 同上
  L372: result = await claude_code(prompt="test", cwd="/tmp") → 同上
  L394: result = await claude_code(prompt="test", cwd="/tmp") → 同上
  L410: result = await claude_code(prompt="test", cwd="/tmp") → 同上

各行で `, cwd="/tmp"` を削除するのみ (関数呼び出しのカッコ + 引数構造は維持)。

test_tool_catalog_js.py::test_catalog_contains_six_tools:
  実際の failure 内容を実測して 6 → 8 (もしくは関連 assertion を YAML 順 8 件で更新)。関数名も `test_catalog_contains_eight_tools` にリネーム。

test_tool_registry.py::test_tool_registry_real_yaml_contract:
  ツールセット contract が yaml と一致することを確認するテスト。yaml の attachments_list / attachments_extract を含めて assertion を更新。

Pitfall 5 (RESEARCH L331-336):
  test_mcp_server.py は冒頭で `pytest.importorskip("fastmcp", ...)` がかかっており、root env で `fastmcp` が install されていない環境では skip される。本 plan の cwd= 削除は **grep ベースで file-line 単位に固定** する手法を採用し、`fastmcp` install 環境での実行検証は Wave 3 (close plan) もしくは docker compose env で行う。

D-12 上限ポリシー:
  本 plan の scope は **YAML 順序 + 件数 6→8 の更新 + build_helper 4→6 関数更新 (planner 事前確定済) + cwd= 削除のみ**。これら以外の test_generate_mcp_artifacts.py の他テスト (e.g. test_determinism / test_check_all 系) は touch しない。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tests/test_generate_mcp_artifacts.py の 6→8 件 + YAML 順更新</name>
  <files>tests/test_generate_mcp_artifacts.py</files>
  <read_first>
    - tests/test_generate_mcp_artifacts.py (L37-49, L65-82, L121-135, L143-158 の 4 assertion ブロック、関数 docstring)
    - config/mcp_tools.yaml (`^  - name:` で 8 件の正確な順序を再確認: ping, web_search, db_query, claude_code, execute_python, get_current_datetime, attachments_list, attachments_extract)
    - scripts/generate_mcp_artifacts.py (実装。本 task で読むのは load_tools / build_helper / build_js / build_docs の動作確認用、コード変更は行わない)
    - .planning/phases/39-ui-polish/39-RESEARCH.md (D-09 Code Examples L540-545、planner 注意「3-4 件の数値 drift が連鎖修正対象」)
  </read_first>
  <action>
    tests/test_generate_mcp_artifacts.py の 4 箇所を以下の順で更新する:

    Step A: test_load_tools_has_six_tools (L37-49):
    - 関数名を `test_load_tools_has_eight_tools` にリネーム
    - docstring を「実 YAML を読み込んで 8 ツールが返る。」に更新
    - `assert len(tools) == 6` → `assert len(tools) == 8`
    - `names == [...]` リストに `"attachments_list"`, `"attachments_extract"` を YAML 順 (現リストの末尾 `"get_current_datetime"` の後) に追加

    Step B: test_build_helper_has_four_functions (L65-82):
    - 本テストは python_wrapper を持つ tool のみが build_helper に含まれるかを検査
    - **planner 事前確定 (grep ベース実測 2026-05-13)**: `grep -n '^  - name:\|python_wrapper:' config/mcp_tools.yaml` の結果、python_wrapper を持つ tool は **6 件** (ping / web_search / db_query / get_current_datetime / attachments_list / attachments_extract)、持たない tool は **2 件** (claude_code / execute_python)
    - したがって python_wrapper ありの 6 件で build_helper が生成される (4 → 6 に増加)
    - 以下の更新を確定的に実行する:
      - 関数名を `test_build_helper_has_four_functions` → `test_build_helper_has_six_functions` にリネーム
      - docstring「4 関数 (search / query_db / get_datetime / ping) が build_helper に含まれる」を「6 関数 (search / query_db / get_datetime / ping / attachments_list / attachments_extract) が build_helper に含まれる」に更新 (function 名は scripts/generate_mcp_artifacts.py の build_helper 実装での命名規則に従う、`grep -E 'def search|def query_db|def get_datetime|def ping' tests/test_generate_mcp_artifacts.py` で既存命名を確認してから揃える)
      - assertion (`assert "def search(" in helper` 等の検証行) に `attachments_list` / `attachments_extract` の 2 関数の存在 assertion を追加 (例: `assert "def attachments_list(" in helper` / `assert "def attachments_extract(" in helper`)
      - 関連の `assert "def <not_present>(" not in helper` で claude_code / execute_python が含まれないことを確認する assertion は維持 (python_wrapper なしのため依然 build_helper 対象外)

    Step C: test_build_js_order (L121-135):
    - `found == [...]` リストに `"attachments_list"`, `"attachments_extract"` を YAML 順末尾に追加
    - docstring は無変更で OK (「name: "..." の出現順が YAML tools 順と一致する」は変わらない)

    Step D: test_build_docs_header_and_table (L143-158):
    - docstring の「6 セクションを含む」を「8 セクションを含む」に更新
    - `sections == [...]` リストに `"attachments_list"`, `"attachments_extract"` を YAML 順末尾に追加

    Step E: その他のテスト関数 (test_build_helper_header_and_imports / test_build_helper_web_search_uses_clean_content / test_build_helper_db_query_maps_pool_to_pool_name / test_build_js_header_and_array / test_build_js_privileged_tag / test_build_docs_privileged_warning / test_determinism / test_trailing_newline / test_check_all 系) は今回 touch しない (YAML 順序や件数に依存しないため drift と無関係)。

    変更後の検証:
    ```
    pytest tests/test_generate_mcp_artifacts.py -v --tb=short
    ```
    全テスト green を確認 (Step B で python_wrapper 有無の確認結果次第で関数名・assertion 数が変動するため、最終的に全テスト green が達成基準)。
  </action>
  <verify>
    <automated>grep -c 'len(tools) == 8' tests/test_generate_mcp_artifacts.py</automated>
    Expected: 1
    <automated>grep -cE '"attachments_list".*"attachments_extract"|attachments_list.*\n.*attachments_extract' tests/test_generate_mcp_artifacts.py</automated>
    Expected: 3 or more (test_load_tools / test_build_js_order / test_build_docs に出現)
    <automated>grep -c 'def test_build_helper_has_six_functions' tests/test_generate_mcp_artifacts.py</automated>
    Expected: 1
    <automated>grep -c 'def test_build_helper_has_four_functions' tests/test_generate_mcp_artifacts.py</automated>
    Expected: 0
    <automated>pytest tests/test_generate_mcp_artifacts.py -v --tb=short 2>&1 | tail -3</automated>
    Expected: all passed
  </verify>
  <acceptance_criteria>
    - `grep -c 'len(tools) == 8' tests/test_generate_mcp_artifacts.py` の出力が `1` 以上
    - `grep -c 'len(tools) == 6' tests/test_generate_mcp_artifacts.py` の出力が `0` (6 件主張のテストが残っていない)
    - `grep -c '"attachments_list"' tests/test_generate_mcp_artifacts.py` の出力が `3` 以上 (test_load_tools_has_eight_tools / test_build_js_order / test_build_docs_header_and_table の 3 assertion で出現)
    - `grep -c '"attachments_extract"' tests/test_generate_mcp_artifacts.py` の出力が `3` 以上
    - `pytest tests/test_generate_mcp_artifacts.py -v --tb=short` の exit code が 0
    - 関数 `test_load_tools_has_six_tools` がリネーム済 (`grep -c 'def test_load_tools_has_six_tools' tests/test_generate_mcp_artifacts.py` が `0`)
    - 関数 `test_build_helper_has_four_functions` がリネーム済 (`grep -c 'def test_build_helper_has_four_functions' tests/test_generate_mcp_artifacts.py` が `0`)
    - 関数 `test_build_helper_has_six_functions` が存在 (`grep -c 'def test_build_helper_has_six_functions' tests/test_generate_mcp_artifacts.py` が `1`)
    - assertion に `attachments_list` / `attachments_extract` の build_helper 含有を検証する行が追加されている (`grep -cE 'def attachments_(list|extract)' tests/test_generate_mcp_artifacts.py` が `2` 以上)
  </acceptance_criteria>
  <done>D-09 / D-10 Pattern E の test_generate_mcp_artifacts 4 件すべて green</done>
</task>

<task type="auto">
  <name>Task 2: tests/test_mcp_server.py から cwd="/tmp" 引数を 6 箇所削除</name>
  <files>tests/test_mcp_server.py</files>
  <read_first>
    - tests/test_mcp_server.py (L280-415 範囲、cwd= 6 箇所の前後コンテキスト確認)
    - mcp_server/tools/ (claude_code 関数の現状 signature を確認、cwd 引数が削除されている根拠)
    - .planning/phases/38-worker-dl/deferred-items.md (D-09 由来の cwd= 削除 7 件主張 vs 実測 6 件の乖離説明)
    - .planning/phases/39-ui-polish/39-RESEARCH.md (Pitfall 5: fastmcp 非 install 環境で skip される注意点、Open Question 3: grep ベース確認で代用可)
  </read_first>
  <action>
    tests/test_mcp_server.py の 6 箇所すべてで `, cwd="/tmp"` を削除する。

    対象行 (実測 grep 結果に基づく):
    - L293: `await claude_code(prompt="test", cwd="/tmp")` → `await claude_code(prompt="test")`
    - L316: `result = await claude_code(prompt="test", cwd="/tmp")` → `result = await claude_code(prompt="test")`
    - L341: `result = await claude_code(prompt="test", cwd="/tmp")` → 同上
    - L372: `result = await claude_code(prompt="test", cwd="/tmp")` → 同上
    - L394: `result = await claude_code(prompt="test", cwd="/tmp")` → 同上
    - L410: `result = await claude_code(prompt="test", cwd="/tmp")` → 同上

    各行で `, cwd="/tmp"` の 14 文字 (カンマ + スペース + cwd="/tmp") のみを削除する。それ以外 (関数名 / 他 kwargs / 行末括弧) は無変更。

    検証コマンド:
    ```
    grep -c 'cwd=' tests/test_mcp_server.py
    ```
    出力が `0` であることを確認 (Pitfall 5 のとおり、本 task は grep ベース確認で完結。`fastmcp` install env での実行は Wave 3 で別途試行)。

    本 task では production code (mcp_server/tools/claude_code.py 等) を**触らない**。signature 変更は Phase 38 Plan 03 で完了済、Phase 39 は test 側の追従修正のみ。

    D-12 上限ポリシー注意: 本 task で test_mcp_server.py の他のテスト関数や fixture を touch しない (例: `pytest.importorskip("fastmcp")` の解除や fastmcp install 手順の追加は scope 外、deferred-items.md 行きを検討)。
  </action>
  <verify>
    <automated>grep -c 'cwd=' tests/test_mcp_server.py</automated>
    Expected: 0
    <automated>grep -c 'claude_code(prompt="test"' tests/test_mcp_server.py</automated>
    Expected: 6
    <automated>git diff tests/test_mcp_server.py | grep -cE '^-.*cwd='</automated>
    Expected: 6
  </verify>
  <acceptance_criteria>
    - `grep -c 'cwd=' tests/test_mcp_server.py` の出力が `0`
    - `grep -c 'claude_code(prompt="test"' tests/test_mcp_server.py` の出力が `6` (6 箇所の呼び出しが残存、cwd= だけが除去)
    - 削除行数 = 追加行数 (diff で `cwd="/tmp"` 削除は 6 件、追加は 0 件、行構造変更のみ)
    - test_mcp_server.py の他のテスト関数 (collect される or skip される) の構造が無変更 (`git diff tests/test_mcp_server.py` で変更が L293/316/341/372/394/410 周辺のみ)
  </acceptance_criteria>
  <done>D-09 cwd= 削除 6 件完了 (grep ベース)。fastmcp install env での実行検証は Wave 3 (close plan) で行うため Phase 39 内で defer 可</done>
</task>

<task type="auto">
  <name>Task 3: tests/test_tool_catalog_js.py + tests/test_tool_registry.py の 6→8 更新</name>
  <files>
    tests/test_tool_catalog_js.py
    tests/test_tool_registry.py
  </files>
  <read_first>
    - tests/test_tool_catalog_js.py (全体、特に test_catalog_contains_six_tools 関数本体と assertion 内容)
    - tests/test_tool_registry.py (全体、特に test_tool_registry_real_yaml_contract 関数本体)
    - config/mcp_tools.yaml (8 件の YAML 順序)
    - static/js/tool-catalog-generated.js (test_tool_catalog_js が validation する自動生成 JS catalog、ファイル存在確認のみ)
    - .planning/phases/39-ui-polish/39-RESEARCH.md (D-10 Pattern E — tool catalog drift、test_generate_mcp_artifacts と同根)
  </read_first>
  <action>
    Step A: tests/test_tool_catalog_js.py::test_catalog_contains_six_tools を更新する。

    まず本テストの内容を確認:
    1. `pytest tests/test_tool_catalog_js.py -v --tb=short` を実行して現状の失敗内容を取得 (e.g. `AssertionError: 6 != 8`)
    2. 関数本体を読み、何を assert しているか確認 (例: `static/js/tool-catalog-generated.js` の AVAILABLE_TOOLS 配列の要素数、tool name の集合)
    3. 6→8 への更新内容を確定 (関数名リネーム + assertion 値更新 + 期待 tool name 配列の更新)

    具体的な更新:
    - 関数名 `test_catalog_contains_six_tools` → `test_catalog_contains_eight_tools`
    - docstring 「6 ツール」→「8 ツール」
    - assertion 値・期待配列を YAML 順 (ping, web_search, db_query, claude_code, execute_python, get_current_datetime, attachments_list, attachments_extract) に揃える

    Step B: tests/test_tool_registry.py::test_tool_registry_real_yaml_contract を更新する。

    まず本テストの内容を確認:
    1. `pytest tests/test_tool_registry.py -v --tb=short` で失敗内容を取得
    2. 関数本体を読み、ToolRegistry の yaml contract 検証ロジックを確認

    具体的な更新:
    - assertion 値・期待 tool 名集合に attachments_list / attachments_extract を追加
    - 6 → 8 の数値 drift があれば更新
    - docstring 内の「6 ツール」表記があれば「8 ツール」に更新

    Step C: 検証
    - `pytest tests/test_tool_catalog_js.py tests/test_tool_registry.py -v --tb=short` で両方 green
    - `pytest tests/ --ignore=tests/test_mcp_server.py -q --tb=no 2>&1 | tail -1` で failed 件数が Plan 04 完了時点 (23 件) から **17 件以下** に減少 (Pattern E 6 件 = test_generate_mcp_artifacts 4 + test_tool_catalog_js 1 + test_tool_registry 1)

    D-12 上限ポリシー: 本 task の scope は **2 テスト関数の assertion 更新のみ**。他のテスト関数 (例: tool registry の不要な lint 系テスト) や production code (app/orchestrator/agent.py 等) には触らない。
  </action>
  <verify>
    <automated>pytest tests/test_tool_catalog_js.py -v --tb=short 2>&1 | tail -3</automated>
    Expected: passed
    <automated>pytest tests/test_tool_registry.py -v --tb=short 2>&1 | tail -3</automated>
    Expected: passed
    <automated>pytest tests/ --ignore=tests/test_mcp_server.py -q --tb=no 2>&1 | tail -1</automated>
    Expected: failed 数が 17 以下
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_tool_catalog_js.py -v --tb=short` の exit code が 0
    - `pytest tests/test_tool_registry.py -v --tb=short` の exit code が 0
    - test_tool_catalog_js.py 内に "attachments_list" と "attachments_extract" の両方が出現 (`grep -cE 'attachments_list|attachments_extract' tests/test_tool_catalog_js.py` が `2` 以上)
    - test_tool_registry.py 内に "attachments_list" と "attachments_extract" の両方が出現 (`grep -cE 'attachments_list|attachments_extract' tests/test_tool_registry.py` が `2` 以上)
    - `pytest tests/ --ignore=tests/test_mcp_server.py -q --tb=no` の failed 件数が **17 以下** (Plan 04 完了後 23 件 → Pattern E 6 件減で 17 件以下、test_generate_mcp_artifacts 4 + test_tool_catalog_js 1 + test_tool_registry 1)
    - production code (app/ / mcp_server/) に変更なし (`git diff app mcp_server | wc -l` の出力が `0`)
  </acceptance_criteria>
  <done>D-09 / D-10 Pattern E すべて解消、pytest baseline 23 → ≤17 件に短縮</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new attack surface — test ファイルの assertion / kwargs 更新のみ。production code に変更なし。

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-06-01 | T | test_mcp_server.py の cwd= 削除で fastmcp install 環境では別の test 失敗を誘発 | accept | Pitfall 5 通り、fastmcp 非 install env では skip される。本 plan は grep ベースで 0 件確認、fastmcp install env での実行検証は Wave 3 で別途試行 (RESEARCH Open Question 3) |
| T-39-06-02 | T | test catalog 8→N drift が再発 (Phase 40 以降で tool 追加時) | accept | 本 plan は現時点の 8 件確定値で固定。Phase 30 single-source-of-truth + pre-commit hook が新規 tool 追加時に test 更新を強制する仕組み (Phase 30 Plan 04/05 の drift 検知) でカバー済 |
</threat_model>

<verification>
## Decision Citations

- `D-09:` pytest 数値 drift (6→8) + cwd= 引数 6 件削除。

## Verification Checks

- Task 1 完了: test_generate_mcp_artifacts.py の 4 assertion が all green
- Task 2 完了: test_mcp_server.py の cwd= grep 0 件
- Task 3 完了: test_tool_catalog_js.py / test_tool_registry.py の 2 件 all green
- 累積影響: pytest --ignore=tests/test_mcp_server.py の failed 件数が Plan 04 完了時 23 件 → 17 件以下 (Pattern E 6 件減)
</verification>

<success_criteria>
- UIFIX-04 D-09 (pytest 数値 drift + cwd 引数) 解消
- UIFIX-04 D-10 Pattern E (tool catalog drift 6 件) 解消
- production code 不変 (test only の修正)
</success_criteria>

<output>
After completion, create `.planning/phases/39-ui-polish/39-06-SUMMARY.md`
</output>
