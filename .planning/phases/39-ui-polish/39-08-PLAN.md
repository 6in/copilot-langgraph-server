---
phase: 39-ui-polish
plan: 08
type: execute
wave: 2
depends_on: [01, 04]
files_modified:
  - tests/test_graph.py
  - tests/test_worker.py
  - tests/test_debate_handler.py
  - tests/test_rpc_integration.py
  - tests/test_tool_enabled_subagent.py
autonomous: true
requirements: [UIFIX-04]
tags: [polish, test, mock-pattern, astream, async-generator, psycopg]

must_haves:
  truths:
    - "tests/test_graph.py の 3 件 (test_messages_accumulate / test_single_message_response / test_thread_isolation) が green (LLM astream を async generator mock に書き換え)"
    - "tests/test_worker.py の 5 件 (test_process_chat_saves_result / test_process_chat_error_handling / test_process_chat_closes_llm / test_startup_creates_redis_and_jobstore / test_orchestrator_handler_uses_checkpointer) が green"
    - "tests/test_worker.py の Pattern B 該当 1 件 (psycopg AsyncMock パターン更新) が green — Plan 07 が test_api_chat 3 件、本 plan が test_worker 1 件で Pattern B 全 4 件を scope 内完遂"
    - "tests/test_debate_handler.py / test_rpc_integration.py / test_tool_enabled_subagent.py の 3 件が green"
    - "pytest tests/ --ignore=tests/test_mcp_server.py の failed 件数が Plan 07 完了時点 ≤ 6 件 から **0 件** に到達 (target failed: 27 → 0)"
  artifacts:
    - path: "tests/test_graph.py"
      provides: "async generator pattern による astream mock を用いた LangGraph テスト"
      contains: "async def "
    - path: "tests/test_worker.py"
      provides: "process_chat / startup / orchestrator handler の mock 更新 + Pattern B psycopg AsyncMock パターン更新済テスト"
  key_links:
    - from: "tests/test_graph.py / tests/test_worker.py の LLM mock"
      to: "ChatCopilot.astream() async iteration 経路"
      via: "async generator (yield AIMessage) で `async for` 互換、AsyncMock では不可"
      pattern: "yield "
    - from: "tests/test_worker.py の psycopg mock (Pattern B)"
      to: "app/jobs/worker.py の startup() / handler 内の psycopg.AsyncConnection chain"
      via: "AsyncContextManager pattern で `async with conn.cursor() as cur:` 経路を満たす mock"
      pattern: "AsyncConnection|__aenter__|asynccontextmanager"
---

<objective>
UIFIX-04 D-10 Pattern C (LLM mock astream — 6 件) + Pattern D (mock 経路 — 5 件) + Pattern B test_worker 1 件 (Plan 07 と分担で Pattern B 全 4 件解消) を 1 plan で潰す。test_worker.py が Pattern C / Pattern D / Pattern B の 3 種にまたがるため、files_modified 重複を避けるべく統合。

Purpose:
- Pattern C: `astream` を AsyncMock ではなく **async generator** として mock する (Phase 31 token streaming 経路の標準パターン)
- Pattern D: mock 経路の具体的不整合 (mock chain attribute / checkpointer 等) を各 test ファイルの実 failure 内容に応じて修正
- Pattern B test_worker 1 件: psycopg AsyncConnection の AsyncContextManager 対応 (Plan 07 で test_api_chat 3 件は完遂済、本 plan で残り 1 件を解消し Pattern B 全 4 件完遂)
- production code (app/) は touch せず、test の mock 設計のみを更新
- defer 経路は撤廃。target failed: 27 → 0 (user decision)

Output:
- tests/test_graph.py: mock_llm fixture もしくは個別テスト内で `astream` を async generator に変更 (Pattern C 3 件解消)
- tests/test_worker.py: Pattern C 3 件 (process_chat 系) + Pattern D 2 件 (startup / orchestrator handler) + Pattern B 1 件 (psycopg AsyncContextManager) の 6 件統合修正
- tests/test_debate_handler.py / test_rpc_integration.py / test_tool_enabled_subagent.py: 各 1 件の Pattern D mock 経路修正
- production code 不変
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-ui-polish/39-CONTEXT.md
@.planning/phases/39-ui-polish/39-RESEARCH.md
@.planning/phases/39-ui-polish/39-PATTERNS.md
@.planning/phases/39-ui-polish/39-BASELINE.md
@tests/test_graph.py
@tests/test_worker.py
@tests/test_debate_handler.py
@tests/test_rpc_integration.py
@tests/test_tool_enabled_subagent.py
@tests/conftest.py
@app/providers/copilot.py
@app/jobs/worker.py
@app/graph/builder.py

<interfaces>
LLM astream mock 修正パターン (RESEARCH L596-612):

NG (AsyncMock では `async for` 不可):
  llm = AsyncMock()
  llm.astream = AsyncMock(return_value=[AIMessage(...)])  # async for 互換でない

OK (async generator):
  async def _astream_gen(*args, **kwargs):
      yield AIMessage(content="chunk1")
      yield AIMessage(content="chunk2")
  llm.astream = _astream_gen   # NOT AsyncMock

Pattern B psycopg AsyncMock 修正パターン (test_worker 1 件、Plan 07 と同パターン):

OK (AsyncContextManager 対応):
  mock_cursor = AsyncMock()
  mock_cursor.execute = AsyncMock()
  mock_cursor.fetchall = AsyncMock(return_value=[...])
  mock_cursor_cm = AsyncMock()
  mock_cursor_cm.__aenter__ = AsyncMock(return_value=mock_cursor)
  mock_cursor_cm.__aexit__ = AsyncMock(return_value=None)
  mock_conn.cursor = MagicMock(return_value=mock_cursor_cm)

実 failure (実測 27 件 baseline から、本 plan 対象):

Pattern C (6 件):
  tests/test_graph.py::test_messages_accumulate         — TypeError: 'async for' requires ...
  tests/test_graph.py::test_single_message_response     — 同上
  tests/test_graph.py::test_thread_isolation            — 同上
  tests/test_worker.py::test_process_chat_saves_result  — AttributeError: <... astream ...>
  tests/test_worker.py::test_process_chat_error_handling — 同上
  tests/test_worker.py::test_process_chat_closes_llm    — 同上

Pattern D (5 件):
  tests/test_worker.py::test_startup_creates_redis_and_jobstore  — psycopg connection 系 (Pattern D vs Pattern B 境界、後述)
  tests/test_worker.py::test_orchestrator_handler_uses_checkpointer — AssertionError
  tests/test_debate_handler.py::test_handle_calls_build_debate_graph — AttributeError
  tests/test_rpc_integration.py::test_orchestrator_handler_injects_context — AttributeError
  tests/test_tool_enabled_subagent.py::test_tool_enabled_subagent_runs_react_loop — (実 failure msg を Plan 実行時に取得)

Pattern B test_worker (1 件、Plan 07 と分担):
  tests/test_worker.py で psycopg.AsyncConnection の AsyncContextManager 対応が必要なテスト。失敗メッセージで以下のいずれかが該当する候補:
  - test_startup_creates_redis_and_jobstore (Pattern D と Pattern B の境界 — startup が psycopg pool を初期化するかで判定)
  - その他、worker.py が direct に AsyncConnection を扱う箇所がある場合

  実 failure msg で「TypeError: object AsyncMock can't be used in 'async with' expression」もしくは「object MagicMock can't be used in 'async with' expression」が出ているテストが Pattern B 該当。AttributeError 系は Pattern D に分類。

mock_llm fixture (tests/test_graph.py:10-15、Pattern C 修正の起点):
  @pytest.fixture
  def mock_llm():
      """Mock BaseChatModel that returns a fixed AIMessage."""
      llm = AsyncMock()
      llm.ainvoke = AsyncMock(return_value=AIMessage(content="mocked response"))
      return llm

修正後候補:
  @pytest.fixture
  def mock_llm():
      llm = AsyncMock()
      llm.ainvoke = AsyncMock(return_value=AIMessage(content="mocked response"))
      async def _astream_gen(*args, **kwargs):
          yield AIMessage(content="mocked response")
      llm.astream = _astream_gen
      return llm

D-12 上限ポリシー (改訂):
  本 plan の scope は Pattern C 6 件 + Pattern D 5 件 + Pattern B test_worker 1 件 = 12 件 (test_worker.py の Pattern B 1 件は Pattern C/D 内訳とは別ユニーク件)。

  Pattern B 全 4 件 (test_api_chat 3 + test_worker 1) は **scope 内完遂** (user decision)。Plan 07 と本 plan で分担、defer 経路は撤廃。

  「同じ test_worker.py を触るついでに別のテストも直す」は禁止。各 failure の root cause が Pattern B/C/D の枠内であることを失敗メッセージで確認してから修正する。

  実装中に新規 failure が露出した場合、Phase 39 内で潰せるなら本 plan で即修正、潰せない設計負債なら deferred-items.md に triage (Pattern B 完遂後の境界判断は executor 裁量)。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tests/test_graph.py の mock_llm fixture に async generator astream を追加 (Pattern C 3 件)</name>
  <files>tests/test_graph.py</files>
  <read_first>
    - tests/test_graph.py (全体、特に L10-15 mock_llm fixture と test_messages_accumulate / test_single_message_response / test_thread_isolation 3 件の本体)
    - app/graph/builder.py (build_graph 内で LLM が ainvoke / astream どちらを呼ぶか確認)
    - app/providers/copilot.py (BoundChatCopilot の astream 実装、yield する型と引数シグネチャ)
    - .planning/phases/39-ui-polish/39-RESEARCH.md (Pattern C Code Example L596-612)
  </read_first>
  <action>
    Step A: tests/test_graph.py の `mock_llm` fixture (L10-15) を以下のとおり拡張する。

    現状:
    ```python
    @pytest.fixture
    def mock_llm():
        llm = AsyncMock()
        llm.ainvoke = AsyncMock(return_value=AIMessage(content="mocked response"))
        return llm
    ```

    修正後 (async generator を追加):
    ```python
    @pytest.fixture
    def mock_llm():
        llm = AsyncMock()
        llm.ainvoke = AsyncMock(return_value=AIMessage(content="mocked response"))
        async def _astream_gen(*args, **kwargs):
            yield AIMessage(content="mocked response")
        llm.astream = _astream_gen
        return llm
    ```

    Step B: テスト本体 3 件 (test_messages_accumulate / test_single_message_response / test_thread_isolation) は本 fixture を使うため、fixture 修正のみで自動的に green になることを確認する。

    もし 3 件いずれかが追加の修正を必要とする (例: assertion で astream の戻り値を直接検証している) 場合、最小限の修正を加える。

    Step C: `pytest tests/test_graph.py -v --tb=short` で 3 件 green を確認。

    本 task は test_graph.py のみ touch、他のファイルは無変更。
  </action>
  <verify>
    <automated>grep -c 'async def _astream_gen' tests/test_graph.py</automated>
    Expected: 1
    <automated>grep -c 'llm.astream = _astream_gen' tests/test_graph.py</automated>
    Expected: 1
    <automated>pytest tests/test_graph.py -v --tb=short 2>&1 | tail -3</automated>
    Expected: 3 passed
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_graph.py -v` の出力で test_messages_accumulate / test_single_message_response / test_thread_isolation の 3 件すべてが pass
    - mock_llm fixture に `async def _astream_gen` 関数が含まれる
    - mock_llm fixture が `llm.astream = _astream_gen` (AsyncMock ではなく関数オブジェクト) を返す
    - test_graph.py の他のテスト関数 (例: lint 系) が無変更
  </acceptance_criteria>
  <done>Pattern C のうち test_graph.py 3 件解消</done>
</task>

<task type="auto">
  <name>Task 2: tests/test_worker.py の 5 件 + Pattern B 1 件 統合修正 (計 6 件)</name>
  <files>tests/test_worker.py</files>
  <read_first>
    - tests/test_worker.py (全体、特に test_process_chat_saves_result / test_process_chat_error_handling / test_process_chat_closes_llm / test_startup_creates_redis_and_jobstore / test_orchestrator_handler_uses_checkpointer)
    - app/jobs/worker.py (process_chat と startup の実装、AsyncMock 対象の attribute と呼び出しチェーン、psycopg AsyncConnection の使用箇所)
    - app/jobs/handlers/langgraph_handler.py / orchestrator_handler.py (handler 内で astream / ainvoke を呼ぶ箇所、test 側 mock の対象 attribute 名)
    - app/jobs/notifier.py (Plan 04 で no-op stub 化された notify() — handler テストへの影響なし)
    - .planning/phases/39-ui-polish/39-RESEARCH.md (Pattern C / Pattern D / Pattern B 失敗内訳)
    - tests/test_api_chat.py (Plan 07 で導入された Pattern B AsyncContextManager パターンを参照、同パターンを test_worker にも適用)
  </read_first>
  <action>
    Step A: 失敗メッセージを取得して各テストの mock 不整合の具体的箇所を特定:
    ```
    pytest tests/test_worker.py -v --tb=long 2>&1 | grep -E '^(FAILED|AttributeError|TypeError|AssertionError)' | head -20
    ```

    特に重要: 「TypeError: object AsyncMock can't be used in 'async with' expression」または「'object MagicMock can't be used in 'async with' expression」が出ているテストを Pattern B 該当として識別。

    Step B: Pattern C 3 件 (process_chat 系) の修正:
    - test_process_chat_saves_result / test_process_chat_error_handling / test_process_chat_closes_llm はいずれも `astream` を呼ぶハンドラを mock 対象とする可能性が高い
    - mock_llm 相当の fixture もしくは inline mock がある場合、Task 1 と同じ async generator pattern で `astream` を書き換える
    - インライン mock の場合、各テスト関数の冒頭でローカル LLM mock を構築している箇所を以下の形に書き換える:
      ```python
      llm = AsyncMock()
      llm.ainvoke = AsyncMock(return_value=AIMessage(...))
      async def _astream_gen(*args, **kwargs):
          yield AIMessage(...)
      llm.astream = _astream_gen
      ```

    Step C: Pattern D 2 件 (startup_creates_redis_and_jobstore / orchestrator_handler_uses_checkpointer) の修正:
    - test_startup_creates_redis_and_jobstore は worker.startup() の Redis / psycopg AsyncConnection 接続を mock する箇所が現実装とずれている。失敗メッセージから具体的 attribute / 呼び出しチェーンを特定し、AsyncMock の構成を更新
    - test_orchestrator_handler_uses_checkpointer は OrchestratorHandler の checkpointer mock 経路を更新

    Step D: Pattern B 1 件の修正 (Plan 07 と同パターン適用):
    - 失敗メッセージで `"can't be used in 'async with' expression"` が出ているテスト 1 件 (test_startup_creates_redis_and_jobstore が候補だが実 failure msg で確定) に対し、Plan 07 Task 2 で導入した AsyncContextManager パターンを適用:
      ```python
      mock_cursor = AsyncMock()
      mock_cursor.execute = AsyncMock()
      mock_cursor.fetchall = AsyncMock(return_value=[<expected>])
      mock_cursor_cm = AsyncMock()
      mock_cursor_cm.__aenter__ = AsyncMock(return_value=mock_cursor)
      mock_cursor_cm.__aexit__ = AsyncMock(return_value=None)
      mock_conn.cursor = MagicMock(return_value=mock_cursor_cm)
      ```
    - test_startup_creates_redis_and_jobstore は Pattern D と Pattern B が同 test 内で混在する可能性あり (Redis mock = Pattern D、psycopg mock = Pattern B)。両方を 1 つの test 内で修正する

    Step E: 各テストの修正方針:
    - production code (app/jobs/worker.py / handlers/*) は touch しない
    - mock の attribute 階層を実装側の呼び出しチェーン (例: `arq_redis.enqueue_job` / `psycopg.AsyncConnection.cursor` / `AsyncPostgresSaver`) と一致させる
    - 「失敗メッセージで特定された attribute / 呼び出し」だけを最小限修正、推測で他を触らない (D-12 上限ポリシー)

    Step F: 検証
    ```
    pytest tests/test_worker.py -v --tb=short
    ```
    5 件すべて green (Pattern B 1 件は Pattern D 2 件のいずれかと同テスト内で統合修正されているため、ユニークテスト数は 5)。
  </action>
  <verify>
    <automated>pytest tests/test_worker.py -v --tb=short 2>&1 | tail -10</automated>
    Expected: 5 passed (test_process_chat_saves_result / test_process_chat_error_handling / test_process_chat_closes_llm / test_startup_creates_redis_and_jobstore / test_orchestrator_handler_uses_checkpointer)
    <automated>grep -cE '__aenter__|asynccontextmanager' tests/test_worker.py</automated>
    Expected: 1 or more (Pattern B AsyncContextManager パターン適用済)
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_worker.py -v` で本 plan 対象の 5 件すべて pass
    - Pattern B 該当箇所 (失敗メッセージで `async with` エラーが出ていた箇所) に AsyncContextManager パターン (`__aenter__` / `__aexit__` または `asynccontextmanager`) が適用されている
    - production code (app/jobs/worker.py / app/jobs/handlers/) が無変更 (`git diff app/jobs | wc -l` が `0`)
  </acceptance_criteria>
  <done>Pattern C test_worker 3 件 + Pattern D test_worker 2 件 + Pattern B test_worker 1 件 (重複統合) すべて解消、Plan 07 と合わせて Pattern B 全 4 件完遂</done>
</task>

<task type="auto">
  <name>Task 3: test_debate_handler.py / test_rpc_integration.py / test_tool_enabled_subagent.py の 3 件 Pattern D 修正</name>
  <files>
    tests/test_debate_handler.py
    tests/test_rpc_integration.py
    tests/test_tool_enabled_subagent.py
  </files>
  <read_first>
    - tests/test_debate_handler.py (test_handle_calls_build_debate_graph の本体、mock chain)
    - tests/test_rpc_integration.py (test_orchestrator_handler_injects_context の本体、context 注入経路の mock)
    - tests/test_tool_enabled_subagent.py (test_tool_enabled_subagent_runs_react_loop の本体)
    - app/jobs/handlers/debate_handler.py / orchestrator_handler.py / iframe_rpc_handler.py (production code、touch しない、mock 対象の attribute 確認用)
    - app/orchestrator/tool_agent.py (ToolEnabledSubAgent の react ループ実装、astream / ainvoke の呼び方確認)
    - .planning/phases/39-ui-polish/39-RESEARCH.md (Pattern D Code Example)
  </read_first>
  <action>
    Step A: 3 件の失敗メッセージを取得:
    ```
    pytest tests/test_debate_handler.py::test_handle_calls_build_debate_graph -v --tb=long 2>&1 | tail -20
    pytest tests/test_rpc_integration.py::test_orchestrator_handler_injects_context -v --tb=long 2>&1 | tail -20
    pytest tests/test_tool_enabled_subagent.py::test_tool_enabled_subagent_runs_react_loop -v --tb=long 2>&1 | tail -20
    ```

    Step B: 各テストの mock 経路を実装に合わせて修正:

    - test_debate_handler::test_handle_calls_build_debate_graph:
      - DebateHandler.handle() が呼ぶ build_debate_graph / debate_graph.ainvoke 等を mock している箇所を確認
      - AttributeError が出ているなら mock 対象 attribute と実装が乖離している、attribute 名を実装に合わせて修正
      - astream を使う経路なら Task 1/2 と同じ async generator pattern を適用

    - test_rpc_integration::test_orchestrator_handler_injects_context:
      - RPCContext 注入経路のテスト、OrchestratorHandler 内で `state["context"]` への注入を assert
      - mock の attribute (例: `mock_graph.ainvoke` の引数) が実装と一致するか確認、AttributeError の root cause を解消

    - test_tool_enabled_subagent::test_tool_enabled_subagent_runs_react_loop:
      - ToolEnabledSubAgent の build_react_graph + 10 ステップループの mock 経路
      - LLM mock が `ainvoke` を呼ぶか `astream` を呼ぶかを実装で確認、astream なら async generator pattern を適用

    Step C: 各テスト 1 件ずつ修正 → pytest -v で個別確認 → 次のテストに進む (バッチ修正で副作用を出さない)

    本 task の制約:
    - production code (app/jobs/handlers/ / app/orchestrator/) を **絶対に touch しない**
    - もし production code の修正を要する failure が出た場合、それは Pattern D の範疇を超える設計負債のため deferred-items.md に追記して別 plan / v6.1+ defer (ただし Pattern B のような scope 内完遂対象は撤廃済のため、ここに該当するのは真に設計負債のケースのみ)
    - 各テストの修正は失敗メッセージで特定された箇所のみ、推測で他の mock を触らない (D-12 上限ポリシー)
  </action>
  <verify>
    <automated>pytest tests/test_debate_handler.py::test_handle_calls_build_debate_graph tests/test_rpc_integration.py::test_orchestrator_handler_injects_context tests/test_tool_enabled_subagent.py::test_tool_enabled_subagent_runs_react_loop -v --tb=short 2>&1 | tail -6</automated>
    Expected: 3 passed
  </verify>
  <acceptance_criteria>
    - test_debate_handler.py::test_handle_calls_build_debate_graph が pass
    - test_rpc_integration.py::test_orchestrator_handler_injects_context が pass
    - test_tool_enabled_subagent.py::test_tool_enabled_subagent_runs_react_loop が pass
    - production code (app/) が無変更 (`git diff app | wc -l` が `0`)
  </acceptance_criteria>
  <done>Pattern D 残り 3 件 (test_debate_handler / test_rpc_integration / test_tool_enabled_subagent) 解消</done>
</task>

<task type="auto">
  <name>Task 4: pytest full suite 確認 — target failed 27 → 0 の到達検証</name>
  <files>
    .planning/phases/39-ui-polish/39-BASELINE.md
  </files>
  <read_first>
    - .planning/phases/39-ui-polish/deferred-items.md (Plan 01 で作成、Pattern B scope 内完遂のため triage エントリは原則無し)
    - .planning/phases/39-ui-polish/39-BASELINE.md (Phase 39 開始時の 27 件失敗の baseline)
  </read_first>
  <action>
    Step A: pytest full suite を実行し、本 plan 完了後の failed 件数を計測:
    ```
    pytest tests/ --ignore=tests/test_mcp_server.py -q --tb=no 2>&1 | tail -3
    pytest tests/ --ignore=tests/test_mcp_server.py -q --tb=no 2>&1 | grep '^FAILED' | sort
    ```

    Step B: Phase 39 ベースライン 27 件からの差分計算 (target failed: 27 → 0):
    - Plan 04 で 4 件解消 (test_sse 2 + test_register_and_notify + test_unregister_sse 2 削除)
    - Plan 06 で 6 件解消 (test_generate_mcp_artifacts 4 + test_tool_catalog_js 1 + test_tool_registry 1)
    - Plan 07 で 9-11 件解消 (Pattern A test_api_chat 6 + test_api_jobs 2 = 8、Pattern B test_api_chat 3 件 — 重複カウントなし 8 件分のユニークテスト = 8、もしくは Pattern A/B 別々で 11 件)
    - Plan 08 (本 plan) で 12 件解消想定 (Pattern C 6 + Pattern D 5 + Pattern B test_worker 1 = 12、test_worker 内の重複分を引いて実質 11-12 件)

    合計: 27 件すべて解消 (target failed: 0 件)

    Step C: 残失敗の triage
    - failed 件数が 0 件: target 達成、Phase 39 完璧
    - failed 件数が 1 件以上: 想定外の新規 failure。失敗メッセージを精査し以下のいずれかで対応:
      a) Phase 39 内で潰せる範囲 (Pattern A-E のいずれか) なら本 plan で即修正 (target 0 件死守、defer 経路は撤廃)
      b) 真に設計負債で本 plan の scope を超える場合のみ deferred-items.md に triage 記録、ただしその場合は user 通知のうえで承認を得る (D-12 改訂方針: Pattern B は完遂、それ以外の defer は明示承認制)

    Step D: 39-BASELINE.md の末尾に「Plan 08 完了時の実測値」セクションを追記:
    ```markdown
    ## Plan 08 完了時の実測値 (YYYY-MM-DD)

    - pytest --ignore=tests/test_mcp_server.py: failed 0 件 (baseline 27 → 0、target 達成)
    - Pattern 別の解消件数:
      - Plan 04: test_sse 2 件 + test_job_store 2 件削除 = 4 件
      - Plan 06: Pattern E (mcp catalog drift) 6 件
      - Plan 07: Pattern A 8 件 + Pattern B test_api_chat 3 件
      - Plan 08: Pattern C 6 件 + Pattern D 5 件 + Pattern B test_worker 1 件
    - target failed: 27 → 0 (user decision で defer 経路撤廃、scope 内完遂)
    ```

    本 task は test 修正を行わない (Task 1-3 で完結)、観測 + 記録のみ。万一 Step C で defer 判定する場合のみ deferred-items.md への追記が発生する (例外ケース)。
  </action>
  <verify>
    <automated>pytest tests/ --ignore=tests/test_mcp_server.py -q --tb=no 2>&1 | tail -1</automated>
    Expected: 0 failed (target 達成)
    <automated>grep -c 'Plan 08 完了時の実測値' .planning/phases/39-ui-polish/39-BASELINE.md</automated>
    Expected: 1
    <automated>grep -c 'target failed: 27 → 0' .planning/phases/39-ui-polish/39-BASELINE.md</automated>
    Expected: 1
  </verify>
  <acceptance_criteria>
    - `pytest tests/ --ignore=tests/test_mcp_server.py -q --tb=no` の failed 件数が **0 件** (target 達成)
    - 39-BASELINE.md の末尾に「Plan 08 完了時の実測値」セクションが追加され、`target failed: 27 → 0` が記載されている
    - Pattern B 全 4 件 (test_api_chat 3 + test_worker 1) が完遂、deferred-items.md に Pattern B エントリが**存在しない**
    - production code (app/ / mcp_server/) が本 plan 全体で無変更
  </acceptance_criteria>
  <done>Phase 39 内で潰す test failure 群を Plan 04/06/07/08 で完遂、target failed: 27 → 0 達成</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new attack surface — test mock の修正のみ、production code 不変。

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-08-01 | T | mock の async generator pattern が production の astream 戻り値型と不整合 (例: AIMessage 以外の型 yield) | mitigate | Task 1-3 の read_first で app/providers/copilot.py / app/jobs/handlers/ を読み、yield する型 (AIMessage / dict / chunk) を実装で確認してから fixture に反映 |
| T-39-08-02 | T | test mock が pre-existing test を巻き込む (fixture 拡張で他テスト副作用) | mitigate | Task 1 は mock_llm fixture を拡張のみ (既存 ainvoke は touch しない)、Task 2-3 は各テストの inline mock を個別修正 (fixture グローバル変更を避ける) |
| T-39-08-03 | T | Pattern B test_worker の AsyncContextManager 修正が他の psycopg mock 経路を巻き込む | mitigate | Plan 07 と同パターン (AsyncContextManager) を test_worker.py の inline mock のみに適用、グローバル fixture 化は行わない |
</threat_model>

<verification>
- Task 1 完了: test_graph.py 3 件 green
- Task 2 完了: test_worker.py 5 件 green (Pattern C 3 + Pattern D 2 + Pattern B 1 = 6 件分の修正が test 関数 5 件に統合)
- Task 3 完了: test_debate_handler / test_rpc_integration / test_tool_enabled_subagent 3 件 green
- Task 4 完了: pytest full failed = 0 (target 27 → 0 達成)、39-BASELINE.md 更新済
- 累積影響: pytest --ignore=tests/test_mcp_server.py の failed 件数が Plan 07 完了 ≤6 件 → Plan 08 完了 0 件
</verification>

<success_criteria>
- UIFIX-04 D-10 Pattern C (LLM mock astream 6 件) 解消
- UIFIX-04 D-10 Pattern D (mock 経路 5 件) 解消
- UIFIX-04 D-10 Pattern B test_worker 1 件 解消 (Plan 07 と分担で Pattern B 全 4 件完遂)
- production code 不変 (test only)
- target failed: 27 → 0 (user decision で defer 経路撤廃、scope 内完遂)
</success_criteria>

<output>
After completion, create `.planning/phases/39-ui-polish/39-08-SUMMARY.md`
</output>
