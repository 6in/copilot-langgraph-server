---
phase: 39-ui-polish
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/39-ui-polish/deferred-items.md
  - .planning/phases/39-ui-polish/39-VALIDATION.md
autonomous: true
requirements: [UIFIX-04]
tags: [polish, baseline, gsd-meta]

must_haves:
  truths:
    - "Phase 39 開始時点の pytest failed 件数 (実測 27 件) が baseline として記録されている"
    - "Phase 39 開始時点の bun tsc エラー件数 (実測 7 件) が baseline として記録されている"
    - "deferred-items.md ファイルが Phase 39 directory に空ヘッダーで作成され、実行中の新規発見項目を蓄積できる"
    - "VALIDATION.md frontmatter の wave_0_complete が true に更新されている"
  artifacts:
    - path: ".planning/phases/39-ui-polish/deferred-items.md"
      provides: "実行中に発見された scope 外項目の蓄積先 (D-12 上限ポリシー)"
      contains: "# Phase 39 — Deferred Items"
    - path: ".planning/phases/39-ui-polish/39-BASELINE.md"
      provides: "Phase 39 開始時点の pytest/tsc 数値 baseline (Wave 3 close 時の差分計測用)"
      contains: "## pytest baseline"
  key_links:
    - from: ".planning/phases/39-ui-polish/deferred-items.md"
      to: "後続 plan (39-02..39-08)"
      via: "executor が scope 外を発見した時に追記、無条件で「ついで修正」しない"
      pattern: "## Plan 39-XX で発見された"
---

<objective>
Phase 39 着手前の bookkeeping wave。
- pytest / tsc の現状ベースライン数値を 39-BASELINE.md に固定し、Wave 3 close 時の差分計測に使う
- deferred-items.md を空ヘッダーで作成し、D-12 上限ポリシー (実行中の新規発見は本ファイル行き) の運用基盤を整える
- 39-VALIDATION.md frontmatter を `wave_0_complete: true` に更新

Purpose: D-12 上限ポリシーを実装上の運用機構として固定する。後続 plan の executor が「ついで修正」を抑制し、Phase 39 のサイズを予測可能に保つ。

Output:
- `.planning/phases/39-ui-polish/deferred-items.md` (新規、空ヘッダー)
- `.planning/phases/39-ui-polish/39-BASELINE.md` (新規、pytest/tsc baseline)
- `.planning/phases/39-ui-polish/39-VALIDATION.md` (frontmatter 更新)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-ui-polish/39-CONTEXT.md
@.planning/phases/39-ui-polish/39-RESEARCH.md
@.planning/phases/39-ui-polish/39-PATTERNS.md
@.planning/phases/39-ui-polish/39-VALIDATION.md
@CLAUDE.md
@.planning/phases/38-worker-dl/deferred-items.md
@.planning/phases/36-text-code-image-multimodal/deferred-items.md

<interfaces>
<!-- D-12 上限ポリシー: 実行中の新規発見は確定リスト (D-07..D-11) 以外を拾わず deferred-items.md に書く -->
<!-- 既存 deferred-items.md パターン: 各項目に「本 plan で扱わない理由」+「いつ取り上げるか」を必ず書く -->

Phase 38 deferred-items.md ヘッダー (踏襲する):
  # Phase {N} — Deferred Items

  本 phase の plan 実行中に発見された、scope 外の pre-existing 問題を記録する。
  ファイルは executor が出会った時点で書き加える。

  ---

各項目フォーマット (Phase 38 L43-53 形式):
  ## Plan {N}-{P} で発見された {short-name}
  - **File:** ...
  - **Error / 由来:** ...
  - **本 plan で扱わない理由:** ...
  - **いつ取り上げるか:** v6.1+ / Phase 34 候補 / etc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pytest / tsc ベースライン数値を 39-BASELINE.md に固定</name>
  <files>.planning/phases/39-ui-polish/39-BASELINE.md</files>
  <read_first>
    - .planning/phases/39-ui-polish/39-RESEARCH.md (Summary L19-25 の実測 27 failures / 5 パターン分類、Assumptions Log A4 の test_install_hooks 0 errors 確認)
    - .planning/phases/39-ui-polish/39-CONTEXT.md (D-10 の "14 failures + 4 errors" 主張 — これは obsolete、本 task で正規化)
  </read_first>
  <action>
    新規ファイル `.planning/phases/39-ui-polish/39-BASELINE.md` を作成する。

    冒頭に「Phase 39 開始時点の test/tsc 数値 baseline」とタイトルを付け、計測日 2026-05-13 を明記する。

    以下のコマンドをこの順番で実行し、出力結果を本ファイルに転記する:
    1. `pytest tests/ --ignore=tests/test_mcp_server.py -q --tb=no 2>&1 | tail -5` — pytest 全体サマリ (failed/passed/skipped 件数)
    2. `pytest tests/ --ignore=tests/test_mcp_server.py -q --tb=no 2>&1 | grep '^FAILED' | sort` — 失敗テストのフルリスト
    3. `docker compose exec frontend bun run tsc -b --force 2>&1 | head -40` — TS error フルリスト (docker が起動していない場合は host で `cd frontend && bun run tsc -b --force` で代用、その旨注記)
    4. `grep -c 'cwd=' tests/test_mcp_server.py` — D-09 cwd= 削除前件数
    5. `grep -cE 'register_sse|unregister_sse|self\.queues' app/jobs/job_store.py` — D-04 dead code 削除前件数

    ファイル構成は以下のセクション順:
    - `## pytest baseline (Python)` — failed 件数 + フル失敗リスト + 5 パターン分類表 (Pattern A/C/D/E の件数を実測値で固定)
    - `## TS baseline (frontend)` — エラー件数 + フル TS エラーリスト
    - `## Dead code grep baseline (UIFIX-03)` — register_sse / unregister_sse / self.queues 件数
    - `## cwd= grep baseline (D-09)` — tests/test_mcp_server.py 内 6 件 (line 番号付き)
    - `## D-10 target reconciliation` — CONTEXT.md "14 failures + 4 errors" は obsolete、実測 27 failed / 0 errors を採用する旨明記
    - `## Phase 39 完了時の target` — pytest failed ≤ 2 件 (test_sse 2 件は 39-04 で潰すため余裕は他 plan に吸収)、tsc error 0 件、cwd= 0 件、register_sse|unregister_sse|self.queues 0 件

    target の具体的な確定値は本 task で固定する (後続 plan の verify はこの数値を参照する)。
  </action>
  <verify>
    <automated>test -f .planning/phases/39-ui-polish/39-BASELINE.md && grep -cE 'pytest baseline|TS baseline|target' .planning/phases/39-ui-polish/39-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/39-ui-polish/39-BASELINE.md` が存在する
    - ファイル内に "27 failed" / "397 passed" の実測数値が記載されている
    - ファイル内に "7" (TS error 件数) の実測数値が記載されている
    - ファイル内に "6" (cwd= 件数) の実測数値が記載されている
    - ファイル内に "Phase 39 完了時の target" セクションが存在し、pytest failed ≤ 2、tsc error 0、cwd= 0、register_sse/unregister_sse/self.queues 0 の各 target が記載されている
    - `grep -c "D-10 target reconciliation" .planning/phases/39-ui-polish/39-BASELINE.md` が 1 以上
  </acceptance_criteria>
  <done>baseline 数値が実測ベースで固定され、後続 plan の verify がこの数値を参照できる</done>
</task>

<task type="auto">
  <name>Task 2: deferred-items.md scaffold + 39-VALIDATION.md frontmatter 更新</name>
  <files>
    .planning/phases/39-ui-polish/deferred-items.md
    .planning/phases/39-ui-polish/39-VALIDATION.md
  </files>
  <read_first>
    - .planning/phases/38-worker-dl/deferred-items.md (Phase 38 で確立した deferred-items パターン、ヘッダー部とエントリ形式を踏襲する)
    - .planning/phases/36-text-code-image-multimodal/deferred-items.md (Phase 36 で確立した同形式のヘッダー)
    - .planning/phases/39-ui-polish/39-CONTEXT.md (D-12 上限ポリシーの文言、本ファイルで参照する)
    - .planning/phases/39-ui-polish/39-VALIDATION.md (frontmatter の現状 — wave_0_complete: false)
  </read_first>
  <action>
    Step A: `.planning/phases/39-ui-polish/deferred-items.md` を新規作成する。

    冒頭ヘッダーは Phase 38/36 パターンを踏襲しつつ、Phase 39 固有の D-12 上限ポリシーを明記する:
    - 1 行目: `# Phase 39 — Deferred Items`
    - 2-4 行目: 「本 phase の plan 実行中に発見された、scope 外の小バグ・改善案を記録する」「D-12 上限ポリシーに従い、確定リスト (D-07..D-11) 以外で発見された項目はここに積み、v6.1+ で観察ベース再評価する」「同一ファイル・同一テストスイートを触る際の trivial fix も無条件で拾わず本ファイルに列挙してから判断する」
    - 5 行目: `---` (水平線)
    - 6 行目以降は空のまま (実行中に executor が追記する)

    本ファイルは Phase 39 開始時点で**項目数 0 件** の状態で作成する。

    Step B: `.planning/phases/39-ui-polish/39-VALIDATION.md` の frontmatter を更新する。
    - `wave_0_complete: false` → `wave_0_complete: true`
    - `nyquist_compliant: false` → `nyquist_compliant: true`
    - 既存 frontmatter の他フィールド (phase/slug/status/created) は触らない
    - frontmatter 以外の本文 (`# Phase 39 — Validation Strategy` 以降) は変更しない
  </action>
  <verify>
    <automated>test -f .planning/phases/39-ui-polish/deferred-items.md && grep -c 'Phase 39 — Deferred Items' .planning/phases/39-ui-polish/deferred-items.md && grep -c 'wave_0_complete: true' .planning/phases/39-ui-polish/39-VALIDATION.md && grep -c 'nyquist_compliant: true' .planning/phases/39-ui-polish/39-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/39-ui-polish/deferred-items.md` が存在し、`# Phase 39 — Deferred Items` 行を含む
    - 同ファイルに `D-12` という識別子が文中に含まれる (上限ポリシーへの言及)
    - 同ファイルの「項目」セクション (## Plan N で発見された …) は 0 件
    - `.planning/phases/39-ui-polish/39-VALIDATION.md` の frontmatter で `wave_0_complete: true` と `nyquist_compliant: true` が両方 true になっている
    - frontmatter 以外の VALIDATION 本文 (Per-Task Verification Map 等) は無変更 (`git diff` で frontmatter のみが変更行)
  </acceptance_criteria>
  <done>D-12 上限ポリシーの運用基盤が整い、後続 plan の executor が新規発見を deferred-items.md へ送れる</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new attack surface — UI / test / dead code cleanup. Wave 0 は documentation のみ (deferred-items.md / BASELINE.md は内部 planning artifact、外部公開なし)。

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01-01 | I | 39-BASELINE.md に test secret や production env 値を記載 | mitigate | pytest/tsc コマンド標準出力のみ転記、env 変数や JWT secret は記載しない |
| T-39-01-02 | I | deferred-items.md に内部 user データを記載 | accept | 本ファイルは技術的記録のみ、ユーザーデータを扱うコードパスを触らない |
</threat_model>

<verification>
- Task 1 完了: `test -f .planning/phases/39-ui-polish/39-BASELINE.md` が成功し、pytest/tsc/cwd=/dead code grep の 4 種 baseline が記載されている
- Task 2 完了: `test -f .planning/phases/39-ui-polish/deferred-items.md` が成功し、`grep -c 'wave_0_complete: true' .planning/phases/39-ui-polish/39-VALIDATION.md` が 1
- Full suite: `pytest tests/ --ignore=tests/test_mcp_server.py -q --tb=no 2>&1 | tail -1` の出力が `27 failed` のまま (本 plan は test ロジックを触らない、確認のみ)
</verification>

<success_criteria>
- deferred-items.md が空ヘッダー (0 項目) で存在
- 39-BASELINE.md が pytest 27 failed / TS 7 errors / cwd= 6 件 / dead code 5+ 件の実測値を固定して記録
- 39-VALIDATION.md frontmatter が `wave_0_complete: true` + `nyquist_compliant: true`
- pytest / tsc / 既存コード状態に変更なし (本 plan は planning artifact のみ touch)
</success_criteria>

<output>
After completion, create `.planning/phases/39-ui-polish/39-01-SUMMARY.md`
</output>
