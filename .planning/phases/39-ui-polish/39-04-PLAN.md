---
phase: 39-ui-polish
plan: 04
type: execute
wave: 1
depends_on: [01]
files_modified:
  - app/jobs/job_store.py
  - tests/test_sse.py
  - tests/test_job_store.py
  - tests/conftest.py
autonomous: true
requirements: [UIFIX-03]
tags: [polish, dead-code, sse, jobstore, test]

must_haves:
  decision_citations:
    - "`D-04:` JobStore dead code 削除 3 タスク (test_sse_done_signal 書き直し / queues 削除 / test_job_store 削除)。"
    - "`D-06:` JobStore.notify() は no-op stub 残置 (notifier.py 書き換え回避)。"
  truths:
    - "JobStore.queues / register_sse / unregister_sse / notify() の self.queues 経路が削除されている"
    - "JobStore.notify() は no-op stub として残置され notifier.py の表面 API 互換が壊れていない"
    - "tests/test_sse.py の test_sse_done_signal と test_sse_already_done が両方 green (jwt_cookie fixture 注入で 401 解消、Redis polling mock 経路で動作)"
    - "tests/test_job_store.py から test_register_and_notify / test_unregister_sse が削除され、test_notify_no_queue が no-op stub の契約テストとして残置されている"
    - "app/jobs/notifier.py が無変更 (D-05 表面 API 温存)"
  artifacts:
    - path: "app/jobs/job_store.py"
      provides: "JobStore (Redis polling 用、SSE queue 経路撤去済)"
      contains: "class JobStore"
    - path: "tests/test_sse.py"
      provides: "Redis polling mock ベースの SSE テスト 2 件 (両方 green)"
      contains: "jwt_cookie"
    - path: "tests/test_job_store.py"
      provides: "save_result / get / notify (no-op stub 契約) のテスト"
  key_links:
    - from: "app/jobs/notifier.py"
      to: "app/jobs/job_store.py の notify() no-op stub"
      via: "WebNotifier.progress() / done() の self.job_store.notify(...) 呼び出し"
      pattern: "self\\.job_store\\.notify"
    - from: "tests/test_sse.py"
      to: "tests/conftest.py の jwt_cookie fixture"
      via: "AsyncClient cookies={...session...} で注入"
      pattern: "jwt_cookie|session"
---

<objective>
UIFIX-03 (test_sse hang + JobStore dead code) を D-04 / D-05 / D-06 の確定方針で解消する:
- D-04: JobStore.queues / register_sse / unregister_sse / notify() の self.queues[job_id].put 枝を削除
- D-05: notifier.py の表面 API (progress / done / send_token) を**そのまま温存** (git diff で 0 行変更を担保)
- D-06: notify() 関数本体は **no-op stub で残置** (notifier.py 4 経路からの呼び出し互換維持、RESEARCH Open Question 2 で stub 推奨)
- test_sse.py の 2 テストは実測で hang ではなく **401 失敗** のため、既存 jwt_cookie fixture を AsyncClient cookies で渡して解消 + Redis polling mock 経路で書き直し
- test_job_store.py から register_sse / unregister_sse 専用テスト 2 件を削除、test_notify_no_queue は no-op stub の契約テストとして残す

Purpose: Production SSE 経路 (app/api/routes/chat.py:219-251) が完全に Redis polling のみで動作している事実を確定させ、Phase 4 SSE 導入時の in-memory queue 経路を dead code として正式に撤去する。notifier.py の Redis pub/sub 再設計 (中・大) は v6.1+ defer。

Output:
- app/jobs/job_store.py: queues dict / register_sse / unregister_sse 撤去、notify() を no-op stub 化
- tests/test_sse.py: 両テストに jwt_cookie 注入、test_sse_done_signal を Redis polling mock 経路で書き直し
- tests/test_job_store.py: test_register_and_notify / test_unregister_sse 削除、test_notify_no_queue を no-op stub 契約テストに簡素化
- tests/conftest.py: mock_job_store fixture から register_sse / unregister_sse の MagicMock 行を削除 (L53-54)
- app/jobs/notifier.py: **無変更** (D-05、検証で diff 0 行を確認)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-ui-polish/39-CONTEXT.md
@.planning/phases/39-ui-polish/39-RESEARCH.md
@.planning/phases/39-ui-polish/39-PATTERNS.md
@.planning/phases/39-ui-polish/39-BASELINE.md
@app/jobs/job_store.py
@app/jobs/notifier.py
@app/api/routes/chat.py
@tests/test_sse.py
@tests/test_job_store.py
@tests/conftest.py

<interfaces>
JobStore 削除対象 (app/jobs/job_store.py 現状):
- L1 `import asyncio` (self.queues 撤去後不要)
- L13 `self.queues: dict[str, asyncio.Queue] = {}`
- L15-19 `register_sse(self, job_id) -> asyncio.Queue` (関数まるごと)
- L21-23 `unregister_sse(self, job_id) -> None` (関数まるごと)
- L33-37 notify() の `if job_id in self.queues: ...await self.queues[job_id].put(...)` 枝

JobStore 残置対象 (変更なし):
- save_result (L25-31)
- push_turn / push_token (L39-48)
- get_tokens / get_turns (L50-58)
- push_tool_event / clear_tool_event / get_tool_event (L60-73)
- get (L75-78)

notify() の修正後 signature (Pitfall 3 厳守、完全互換):
  async def notify(self, job_id: str, status: str, **extra) -> None:
      return None

app/jobs/notifier.py L23-37 (D-05: 無変更、参照のみ):
- WebNotifier.progress() が self.job_store.notify(self.job_id, status) を呼ぶ
- WebNotifier.done() が self.job_store.notify(self.job_id, "done") を呼ぶ
- WebNotifier.send_turn() / send_token() は notify() を呼ばないので影響なし

tests/conftest.py 既存 jwt_cookie fixture (L8-12、変更しない):
  @pytest.fixture
  def jwt_cookie():
      from app.auth.jwt_utils import create_jwt
      return create_jwt("ghu_test_token_for_chat")

tests/conftest.py mock_job_store fixture (L46-55、register_sse/unregister_sse 行のみ削除):
  store.register_sse = MagicMock()   ← 削除 (L53)
  store.unregister_sse = MagicMock() ← 削除 (L54)

tests/test_sse.py 修正方針:
- 両テストの関数シグネチャに jwt_cookie 引数を追加 (`async def test_xxx(mock_job_store, mock_arq_redis, jwt_cookie)`)
- AsyncClient に cookies={"session": jwt_cookie} を渡す
- test_sse_done_signal は polling 経路用に追加 mock 必要:
    mock_job_store.get_turns = AsyncMock(return_value=[])
    mock_job_store.get_tokens = AsyncMock(return_value=[])
    mock_job_store.get_tool_event = AsyncMock(return_value=None)

app/api/routes/chat.py SSE generator (L197-261、参照のみ、touch しない):
- SSE エンドポイントは Depends(get_jwt_payload) → JWT cookie 必須が 401 の原因
- SSE 内ループは job_store.get / get_turns / get_tokens / get_tool_event を polling、queue 不使用
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: app/jobs/job_store.py の dead code 削除 + notify() を no-op stub 化</name>
  <files>app/jobs/job_store.py</files>
  <read_first>
    - app/jobs/job_store.py (L1-78 全体、削除対象 L1/L13/L15-19/L21-23/L33-37 と保持対象 L25-31, L39-78 の境界)
    - app/jobs/notifier.py (L23-37、self.job_store.notify 呼び出しのシグネチャ要件 — Pitfall 3 厳守)
    - app/api/routes/chat.py (L197-261、production SSE が queue を使わないことの一次確認)
    - .planning/phases/39-ui-polish/39-RESEARCH.md (Pitfall 3: signature 完全互換、Code Examples L412-446)
  </read_first>
  <behavior>
    - JobStore() 初期化後、store.queues 属性アクセスが AttributeError を投げる (削除済確認)
    - hasattr(store, "register_sse") が False (関数削除確認)
    - hasattr(store, "unregister_sse") が False (関数削除確認)
    - await store.notify("any-job-id", "any-status") が None を返す (no-op stub 確認、exception 投げない)
    - await store.notify("job", "status", extra_key="extra_value") が None を返す (**extra kwargs 受容、signature 互換確認)
    - store.save_result / store.get / store.push_turn / store.push_token / store.get_turns / store.get_tokens / store.push_tool_event / store.clear_tool_event / store.get_tool_event がいずれも存在し動作変更なし (回帰確認)
  </behavior>
  <action>
    app/jobs/job_store.py を以下のとおり変更する:

    1. L1 の `import asyncio` を削除 (self.queues 撤去後、本ファイル内で asyncio 不要)

    2. クラス docstring (L9) を更新。「Stores job results in Redis. SSE は Redis polling 経路に統一済 (Phase 39 / UIFIX-03)。in-memory queue 経路 (register_sse / unregister_sse) は dead code として削除済。notify() のみ notifier.py 経由の呼び出し互換のため no-op stub で残置。」の趣旨を 3 行で書く

    3. `__init__` から self.queues 行 (L13) を削除。残るのは self.redis = redis のみ

    4. register_sse メソッド (L15-19) を関数まるごと削除 (docstring 含む)

    5. unregister_sse メソッド (L21-23) を関数まるごと削除 (docstring 含む)

    6. notify メソッド (L33-37) を no-op stub 化。signature は完全互換のまま (async def notify(self, job_id: str, status: str, **extra) -> None)、body は return None のみ。docstring に「No-op stub (Phase 39 UIFIX-03 D-06)」「in-memory queue 経路は廃止済」「production SSE は Redis polling (app/api/routes/chat.py:219-251) で完結」の 3 文を書く

    7. L39 以降のメソッド (push_turn / push_token / get_tokens / get_turns / push_tool_event / clear_tool_event / get_tool_event / get) は**無変更**で残す

    変更後の総行数は約 60 行前後 (現状 78 行から 15-20 行減)。`from typing import Optional` / `from redis.asyncio import Redis` import は維持。Pitfall 3 厳守: notify() の signature を絶対に変えない。
  </action>
  <verify>
    <automated>grep -cE 'register_sse|unregister_sse|self\.queues' app/jobs/job_store.py</automated>
    Expected: 0
    <automated>grep -c 'No-op stub' app/jobs/job_store.py</automated>
    Expected: 1 or more
    <automated>grep -cE 'async def notify\(self, job_id: str, status: str, \*\*extra\) -> None' app/jobs/job_store.py</automated>
    Expected: 1
    <automated>pytest tests/test_job_store.py::test_notify_no_queue -v</automated>
    Expected: green (Task 2 で test 側を書き換えた後)
  </verify>
  <acceptance_criteria>
    - `grep -cE 'register_sse|unregister_sse|self\.queues' app/jobs/job_store.py` の出力が `0`
    - `grep -c 'No-op stub' app/jobs/job_store.py` の出力が `1` 以上
    - `grep -cE 'async def notify\(self, job_id: str, status: str, \*\*extra\) -> None' app/jobs/job_store.py` の出力が `1` (signature 完全互換)
    - `grep -c 'import asyncio' app/jobs/job_store.py` の出力が `0`
    - JobStore クラス本体は残存 (`grep -c 'class JobStore' app/jobs/job_store.py` が `1`)
    - save_result / get / push_turn / push_token / get_tokens / get_turns / push_tool_event / clear_tool_event / get_tool_event の 9 メソッドはすべて残存 (`grep -cE 'async def (save_result|get|push_turn|push_token|get_tokens|get_turns|push_tool_event|clear_tool_event|get_tool_event)' app/jobs/job_store.py` が `9` 以上)
  </acceptance_criteria>
  <done>JobStore の SSE queue 経路 dead code が撤去され、notify() は no-op stub で notifier.py 表面 API と互換</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/test_sse.py に jwt_cookie 注入 + Redis polling mock 経路で書き直し</name>
  <files>tests/test_sse.py</files>
  <read_first>
    - tests/test_sse.py (L1-61 全体、2 テストの現状)
    - tests/conftest.py (L8-12 既存 jwt_cookie fixture、L46-55 mock_job_store fixture、L58-63 mock_arq_redis fixture)
    - app/api/routes/chat.py (L197-261 production SSE generator — Redis polling 経路の呼び出し API: job_store.get / get_turns / get_tokens / get_tool_event)
    - .planning/phases/39-ui-polish/39-RESEARCH.md (Code Examples L448-477 の書き直し sketch、Pattern 3: jwt_cookie 再利用)
  </read_first>
  <behavior>
    - test_sse_already_done: jwt_cookie 注入後、`assert resp.status_code == 200` が成立、SSE レスポンス内に `status: "done"` のイベントが含まれる
    - test_sse_done_signal: jwt_cookie 注入 + Redis polling mock 経路 (`get` の side_effect = [None, done]) で、`status_code == 200` かつ SSE 内に `done` イベントが含まれる
    - 両テストとも 401 は出ない (`assert resp.status_code != 401`)
  </behavior>
  <action>
    tests/test_sse.py の 2 テストを以下のとおり書き直す。conftest.py の `jwt_cookie` fixture (L8-12) はそのまま再利用 (新規 fixture 作成しない、D-12 上限ポリシーと一致)。

    Step A: test_sse_already_done (L9-32) の修正:
    - 関数シグネチャを `async def test_sse_already_done(mock_job_store, mock_arq_redis, jwt_cookie)` に変更 (jwt_cookie 引数追加)
    - `AsyncClient(transport=transport, base_url="http://test")` を `AsyncClient(transport=transport, base_url="http://test", cookies={"session": jwt_cookie})` に変更
    - それ以外 (`mock_job_store.get = AsyncMock(return_value={"status": "done", "result": "done"})` 等の mock setup、assertion ループ) は無変更

    Step B: test_sse_done_signal (L35-60) の修正:
    - 関数シグネチャに jwt_cookie 引数追加
    - AsyncClient の cookies 注入を Step A と同じく追加
    - mock_job_store の追加 mock setup を以下の順で書く:
      - `mock_job_store.get = AsyncMock(side_effect=[None, {"status": "done", "result": "hello"}])` (現状通り、無変更)
      - `mock_job_store.get_turns = AsyncMock(return_value=[])` (新規追加、polling 経路 stub)
      - `mock_job_store.get_tokens = AsyncMock(return_value=[])` (新規追加)
      - `mock_job_store.get_tool_event = AsyncMock(return_value=None)` (新規追加)
    - assertion 部分 (events list 解析 + `any(e["status"] == "done" for e in events)`) は無変更

    Step C: ファイル冒頭 import (L1-6) は無変更。`from unittest.mock import AsyncMock, MagicMock` が既にあるため追加 import 不要。

    Step D: テスト docstring を以下のとおり微調整 (任意、ASYNC-04 → UIFIX-03 D-06 への参照更新):
    - test_sse_already_done docstring に `(Phase 39 UIFIX-03 D-04 — JWT cookie 注入)` を 1 行追加
    - test_sse_done_signal docstring に `(Phase 39 UIFIX-03 D-04 — Redis polling mock 経路で書き直し)` を 1 行追加

    本 task で Step A の test を**先に**書いて Task 1 が完了していれば green になることを確認 (TDD red→green)。Task 1 が未完了状態なら Step A は依然 401 で red のまま — `depends_on: [01]` で Wave 0 後の Wave 1 並列実行が前提。
  </action>
  <verify>
    <automated>grep -c 'jwt_cookie' tests/test_sse.py</automated>
    Expected: 2 or more (両テスト関数の引数で出現)
    <automated>grep -c 'cookies=' tests/test_sse.py</automated>
    Expected: 2 (両 AsyncClient で出現)
    <automated>pytest tests/test_sse.py -v --tb=short</automated>
    Expected: 2 passed, 0 failed
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_sse.py::test_sse_already_done -v` の exit code が 0 (green)
    - `pytest tests/test_sse.py::test_sse_done_signal -v` の exit code が 0 (green)
    - `grep -c 'jwt_cookie' tests/test_sse.py` の出力が `2` 以上 (両テストで fixture 使用)
    - `grep -cE 'mock_job_store\.get_(turns|tokens|tool_event)' tests/test_sse.py` の出力が `3` (test_sse_done_signal で polling 経路 mock 3 種追加)
    - 401 を assert する行が**存在しない** (`grep -c 'assert.*401' tests/test_sse.py` が `0`)
    - 既存 import 行 (L1-6) が無変更 (`git diff tests/test_sse.py` で L1-L6 範囲に変更なし)
  </acceptance_criteria>
  <done>test_sse.py 2 テストが両方 green。Phase 39 baseline の 27 → 25 failed に短縮 (test_sse 2 件減)</done>
</task>

<task type="auto">
  <name>Task 3: tests/test_job_store.py の dead code テスト削除 + tests/conftest.py の mock_job_store 整理</name>
  <files>
    tests/test_job_store.py
    tests/conftest.py
  </files>
  <read_first>
    - tests/test_job_store.py (L1-73 全体、削除対象 L51-60 / L63-72 と保持対象 L17-48 の境界)
    - tests/conftest.py (L46-55 mock_job_store fixture、削除対象 L53-54 の register_sse / unregister_sse の 2 行)
    - app/jobs/job_store.py (Task 1 完了後の状態、no-op stub notify の signature)
    - .planning/phases/39-ui-polish/39-RESEARCH.md (Pattern map L288-307 の test_job_store 整理方針、Pitfall 4 — interface 追加で type narrowing は壊れない)
  </read_first>
  <action>
    Step A: tests/test_job_store.py から以下 2 テストを関数まるごと削除する (decorator `@pytest.mark.asyncio` 含む):
    - L51-60 の test_register_and_notify (register_sse 撤去のため意味喪失)
    - L63-72 の test_unregister_sse (unregister_sse 撤去のため意味喪失)

    残すテスト (無変更 or docstring 微調整):
    - test_save_and_get (L17-30、無変更)
    - test_get_missing (L33-40、無変更)
    - test_notify_no_queue (L43-48、無変更 — 元々 "no registered SSE queue does not raise" の契約で、no-op stub の契約と等価)

    test_notify_no_queue の docstring に `(Phase 39 UIFIX-03 D-06 — no-op stub 契約)` を 1 行追加してもよい (任意、planner 判断で executor 裁量)。

    Step B: tests/conftest.py の mock_job_store fixture (L46-55) から register_sse / unregister_sse の MagicMock 行を削除する:
    - L53 `store.register_sse = MagicMock()` を削除
    - L54 `store.unregister_sse = MagicMock()` を削除
    - 残すのは store.get / store.save_result / store.notify の 3 行 + return store

    本 step は `MagicMock` import を維持するか確認 — 削除後に MagicMock を他で使う fixture (例: mock_auth_manager L31-43) が残っているため import 行 (L3) は touch しない。

    Step C: 影響範囲確認のため pytest 全体を実行し、Step A/B が他テストを壊していないことを確認する。本 task は test 数値修正で他 Pattern (D-10) には触れないため、対前 wave 比で 4 件減 (test_sse 2 + test_register_and_notify / test_unregister_sse 2 件) を期待。

    実測 27 → 23 件 への低下を確認する。
  </action>
  <verify>
    <automated>grep -cE 'test_register_and_notify|test_unregister_sse' tests/test_job_store.py</automated>
    Expected: 0
    <automated>grep -cE 'store\.register_sse|store\.unregister_sse' tests/conftest.py</automated>
    Expected: 0
    <automated>pytest tests/test_job_store.py -v --tb=short</automated>
    Expected: 3 passed (test_save_and_get / test_get_missing / test_notify_no_queue)
    <automated>pytest tests/ --ignore=tests/test_mcp_server.py -q --tb=no 2>&1 | tail -1</automated>
    Expected: failed 数が 23 以下 (27 - 2 test_sse - 2 test_job_store)
  </verify>
  <acceptance_criteria>
    - `grep -cE 'test_register_and_notify|test_unregister_sse' tests/test_job_store.py` の出力が `0`
    - `grep -cE 'store\.register_sse|store\.unregister_sse' tests/conftest.py` の出力が `0`
    - `pytest tests/test_job_store.py -v --tb=short` が 3 passed (test_save_and_get / test_get_missing / test_notify_no_queue)
    - `pytest tests/ --ignore=tests/test_mcp_server.py -q --tb=no` の failed 数が **23 以下** (baseline 27 から少なくとも 4 件減: test_sse 2 + test_register_and_notify + test_unregister_sse)
    - tests/conftest.py の他 fixture (jwt_cookie / mock_auth_manager / mock_arq_redis / api_client / capture_trace_logs) が無変更 (`git diff tests/conftest.py` で L46-L55 範囲内のみ変更)
  </acceptance_criteria>
  <done>test_job_store 2 件削除 + conftest 2 行削除で baseline 27 → ≤23 件に短縮</done>
</task>

<task type="auto">
  <name>Task 4: app/jobs/notifier.py 無変更確認 (D-05 表面 API 温存の検証)</name>
  <files>(read-only — diff verification)</files>
  <read_first>
    - app/jobs/notifier.py (L1-44 全体、変更しないことを確認する基準)
    - .planning/phases/39-ui-polish/39-CONTEXT.md (D-05: progress() / done() / send_token() 表面 API 温存)
    - .planning/phases/39-ui-polish/39-RESEARCH.md (Pitfall 3 — notify() シグネチャ削除後の notifier.py 不整合警告)
  </read_first>
  <action>
    本 task は **read-only 検証** で、コード変更を行わない。以下のいずれかのコマンドで `app/jobs/notifier.py` が main ブランチから無変更であることを確認する:

    1. `git diff main..HEAD -- app/jobs/notifier.py` の出力が空行のみ (変更 0 行)
    2. もしくは `git diff HEAD -- app/jobs/notifier.py` (worktree との差分) も同様に 0 行

    変更が混入していた場合は executor が手動で revert する (`git checkout -- app/jobs/notifier.py`)。

    本 task は Task 1-3 の作業完了後の最終チェック。Task 1 で notify() の signature を完全互換に維持しているため、notifier.py の WebNotifier.progress / done が呼ぶ `self.job_store.notify(self.job_id, status)` は AttributeError を起こさず、何もせず None を返す (silent success)。

    handler 単体テスト (langgraph_handler / orchestrator_handler / debate_handler / iframe_rpc_handler) が conftest.py の `mock_job_store.notify = AsyncMock()` で stub されているため、本 task の no-op stub 化は単体テスト群に影響しない (Pitfall 3 mitigation 確認)。
  </action>
  <verify>
    <automated>git diff main..HEAD -- app/jobs/notifier.py | wc -l</automated>
    Expected: 0
    <automated>grep -cE 'async def (progress|done|send_token|send_turn)' app/jobs/notifier.py</automated>
    Expected: 4
    <automated>grep -c 'self\.job_store\.notify' app/jobs/notifier.py</automated>
    Expected: 2 (progress + done の 2 箇所)
  </verify>
  <acceptance_criteria>
    - `git diff main..HEAD -- app/jobs/notifier.py | wc -l` の出力が `0` (変更 0 行)
    - `grep -cE 'async def (progress|done|send_token|send_turn)' app/jobs/notifier.py` の出力が `4` (表面 API 4 メソッド全部残存)
    - `grep -c 'self\.job_store\.notify' app/jobs/notifier.py` の出力が `2` (progress + done の 2 箇所)
    - `WebNotifier` クラスが存在 (`grep -c 'class WebNotifier' app/jobs/notifier.py` が `1`)
    - `BaseNotifier` クラスが存在 (`grep -c 'class BaseNotifier' app/jobs/notifier.py` が `1`)
  </acceptance_criteria>
  <done>D-05 表面 API 温存が diff 0 行で担保。handlers 4 経路の互換性に影響なし</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new attack surface — UI / test / dead code cleanup. SSE エンドポイント (`/api/chat/{job_id}/stream`) の認証経路 (`Depends(get_jwt_payload)`) は無変更、JobStore dead code 削除のみ。

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-04-01 | T | notify() の signature 変更で notifier.py 4 経路が silent fail (Pitfall 3) | mitigate | Task 1 acceptance_criteria で signature 完全互換 (`async def notify(self, job_id: str, status: str, **extra) -> None`) を grep 検証、Task 4 で notifier.py 0 行変更を git diff で確認 |
| T-39-04-02 | T | test_sse の JWT cookie 注入で production secret が漏洩 | mitigate | 既存 jwt_cookie fixture (conftest.py L8-12) を再利用、test 用 "ghu_test_token_for_chat" 文字列のみ使用、production env を override しない |
| T-39-04-03 | D | dead code 削除で他 handler テストが silent break | mitigate | Task 3 acceptance_criteria で pytest full suite failed ≤ 23 件を担保、test_worker / test_debate_handler 等の handler テストが新規 fail しないことを baseline 差分で確認 |
</threat_model>

<verification>
## Decision Citations

- `D-04:` JobStore dead code 削除 3 タスク (test_sse_done_signal 書き直し / queues 削除 / test_job_store 削除)。
- `D-06:` JobStore.notify() は no-op stub 残置 (notifier.py 書き換え回避)。

## Verification Checks

- Task 1 完了: job_store.py の register_sse / unregister_sse / self.queues / import asyncio が grep 0 件、notify() の signature 互換
- Task 2 完了: test_sse.py の 2 テストが両方 green
- Task 3 完了: test_job_store.py の register_sse / unregister_sse 2 テスト削除、conftest.py 整理、pytest baseline 27 → 23 件以下
- Task 4 完了: notifier.py git diff 0 行
- 累積影響: pytest baseline 27 件 → 23 件以下 (D-10 plan で残り潰し継続)
</verification>

<success_criteria>
- UIFIX-03 success criteria 「`tests/test_sse.py::test_sse_done_signal` の hang が修正または削除され、JobStore.register_sse / unregister_sse の dead code が整理されている」を満たす
- D-04 (JobStore dead code 削除 3 タスク) 完了
- D-05 (notifier.py 表面 API 温存) 完了 — git diff 0 行
- D-06 (notify() を no-op stub で残置) 完了 — planner 判断で stub 採用
- Phase 4 SSE 導入時の in-memory queue 経路が正式に撤去され、Production SSE は Redis polling 一択
</success_criteria>

<output>
After completion, create `.planning/phases/39-ui-polish/39-04-SUMMARY.md`
</output>
