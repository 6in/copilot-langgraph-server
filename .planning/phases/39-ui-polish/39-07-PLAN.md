---
phase: 39-ui-polish
plan: 07
type: execute
wave: 2
depends_on: [01, 04]
files_modified:
  - tests/test_api_chat.py
  - tests/test_api_jobs.py
  - tests/conftest.py
autonomous: true
requirements: [UIFIX-04]
tags: [polish, test, jwt, auth, psycopg]

must_haves:
  truths:
    - "tests/test_api_chat.py の 6 件 (test_new_thread_returns_uuid / test_list_threads_empty / test_list_threads_left_join / test_list_threads_app_id_filter / test_list_threads_no_app_id_returns_all / test_delete_thread_calls_adelete) が green (Pattern A: JWT cookie 注入で 401 解消)"
    - "tests/test_api_jobs.py の 2 件 (test_get_job_done / test_get_job_pending) が green (Pattern A: JWT cookie 注入)"
    - "tests/test_api_chat.py の Pattern B 該当 3 件 (psycopg AsyncConnection AsyncMock パターン) が green — 401 解消後に露出する psycopg mock 経路の不整合を本 plan で同時に修正する"
    - "api_client fixture が JWT cookie 注入を含むよう拡張されている、もしくは個別テストで cookies={...} を渡す方式で 401 が解消されている"
    - "pytest tests/ --ignore=tests/test_mcp_server.py の failed 件数が Plan 06 完了時点 17 件から 11 件減 (Pattern A 残り 8 件 + Pattern B test_api_chat 3 件)、本 plan 完了時点で ≤ 6 件 (残りは Plan 08 が消化)"
  artifacts:
    - path: "tests/test_api_chat.py"
      provides: "JWT cookie 注入 + psycopg AsyncMock パターン更新済の API chat エンドポイントテスト"
      contains: "jwt_cookie"
    - path: "tests/test_api_jobs.py"
      provides: "JWT cookie 注入後の API jobs エンドポイントテスト"
      contains: "jwt_cookie"
  key_links:
    - from: "tests/test_api_chat.py / tests/test_api_jobs.py"
      to: "tests/conftest.py の jwt_cookie fixture (L8-12)"
      via: "AsyncClient cookies={\"session\": jwt_cookie} or api_client fixture 拡張"
      pattern: "jwt_cookie|session"
    - from: "tests/test_api_chat.py の psycopg mock 経路 (Pattern B)"
      to: "app/api/routes/chat.py が呼ぶ psycopg.AsyncConnection / cursor チェーン"
      via: "AsyncMock の attribute 階層を実装と一致させる修正"
      pattern: "AsyncConnection|cursor|fetchall|fetchone"
---

<objective>
UIFIX-04 D-10 Pattern A (JWT cookie 不足) + Pattern B の test_api_chat 該当 3 件 (psycopg AsyncMock パターン更新) を 1 plan で潰す。

実測 27 件失敗の内訳:
- Pattern A 実測 10 件 = test_sse 2 (Plan 04 で解消済) + test_api_chat 6 + test_api_jobs 2 = 本 plan で 8 件解消
- Pattern B 実測 4 件 = test_api_chat 3 + test_worker 1。本 plan で test_api_chat 3 件、Plan 08 で test_worker 1 件を分担し **scope 内完遂** (user decision、target failed 27 → 0)

Purpose:
- 既存 `tests/conftest.py:8-12` の `jwt_cookie` fixture を再利用 (新規 fixture 追加せず、D-12 上限ポリシーと整合)
- conftest.py の `api_client` fixture を JWT cookie 自動注入に拡張することで 8 件 fan-out 解決 (RESEARCH "Don't Hand-Roll" L282 推奨)
- 401 から 200 へ移行後に露出する Pattern B (psycopg AsyncConnection の AsyncMock 不整合) を test_api_chat 3 件で同時に修正
- Pattern B test_worker 1 件は Plan 08 が同 file を触るため、files_modified 重複回避のため Plan 08 で分担解消
- defer 経路は撤廃。Pattern B 全 4 件は Phase 39 scope 内で完遂する (user decision)

Output:
- tests/test_api_chat.py: jwt_cookie 注入 (api_client fixture 経由) + Pattern B psycopg mock 経路 3 件修正、計 9 件 → green
- tests/test_api_jobs.py: jwt_cookie 注入で 2 件 → green
- tests/conftest.py: api_client fixture を拡張して jwt_cookie を bake in
- production code 不変
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-ui-polish/39-CONTEXT.md
@.planning/phases/39-ui-polish/39-RESEARCH.md
@.planning/phases/39-ui-polish/39-PATTERNS.md
@.planning/phases/39-ui-polish/39-BASELINE.md
@tests/test_api_chat.py
@tests/test_api_jobs.py
@tests/conftest.py
@tests/test_sse.py
@app/api/routes/chat.py
@app/api/routes/jobs.py
@app/auth/jwt_utils.py

<interfaces>
tests/conftest.py L8-12 既存 jwt_cookie fixture (再利用、変更しない):
  @pytest.fixture
  def jwt_cookie():
      """A valid JWT session cookie value for use in chat endpoint tests."""
      from app.auth.jwt_utils import create_jwt
      return create_jwt("ghu_test_token_for_chat")

tests/conftest.py L66-91 api_client fixture (拡張対象):
  @pytest.fixture
  async def api_client(mock_graph, mock_auth_manager, mock_job_store, mock_arq_redis):
      ...
      transport = ASGITransport(app=app)
      async with AsyncClient(transport=transport, base_url="http://test") as client:
          yield client

拡張案 (Don't Hand-Roll L282 推奨): api_client fixture に jwt_cookie を bake in。

Pattern A 対象 8 件 (本 plan):
  tests/test_api_chat.py:
    - test_new_thread_returns_uuid
    - test_list_threads_empty
    - test_list_threads_left_join
    - test_list_threads_app_id_filter
    - test_list_threads_no_app_id_returns_all
    - test_delete_thread_calls_adelete
  tests/test_api_jobs.py:
    - test_get_job_done
    - test_get_job_pending

Pattern B 対象 (本 plan で test_api_chat 3 件、Plan 08 で test_worker 1 件):
  tests/test_api_chat.py (3 件):
    - Pattern A 解消後に露出する psycopg AsyncConnection AsyncMock 経路の不整合
    - 失敗メッセージから対象テスト関数を特定 (test_list_threads_left_join / app_id_filter / no_app_id_returns_all 等が候補。実 failure msg で確定)
    - psycopg.AsyncConnection.cursor() の AsyncContextManager / cursor().execute() / cursor().fetchall() の async chain mock パターンが Phase 36 由来で古い

Pattern B psycopg AsyncMock 修正パターン:
  NG (旧):
    mock_conn = AsyncMock()
    mock_conn.cursor.return_value = AsyncMock()
    cursor = mock_conn.cursor.return_value
    cursor.fetchall = AsyncMock(return_value=[...])

  OK (新、AsyncContextManager 対応):
    mock_cursor = AsyncMock()
    mock_cursor.fetchall = AsyncMock(return_value=[...])
    mock_cursor.execute = AsyncMock()
    mock_conn = AsyncMock()
    # AsyncConnection.cursor() returns an async context manager
    mock_conn.cursor.return_value.__aenter__ = AsyncMock(return_value=mock_cursor)
    mock_conn.cursor.return_value.__aexit__ = AsyncMock(return_value=None)

  もしくは contextlib.asynccontextmanager で fixture を書き、`async with conn.cursor() as cur:` チェーンに対応させる。失敗メッセージで mock chain の具体的不整合を特定してから書き換える。

app/api/routes/chat.py:
  各エンドポイント (/api/threads / /api/threads/{id} / /api/job/{id}) が `Depends(get_jwt_payload)` を持つ。
  list_threads は psycopg connection pool から取得した AsyncConnection を `async with conn.cursor() as cur:` で使う想定。
  jwt_cookie が cookies に "session" キーで載っていれば 200 が返る。

app/auth/jwt_utils.py:
  create_jwt(github_login) → str (HS256 JWT)
  jwt_cookie fixture が返す値は str (JWT token そのもの、cookies={"session": jwt_cookie} で cookie に注入)

Pitfall (RESEARCH Pitfall 1 / Security V2):
  test 用の JWT secret は CopilotAuthManager / jwt_utils.py 内で env var もしくは ~/.copilot_sdk/.jwt_secret から読む。
  既存 jwt_cookie fixture は `create_jwt("ghu_test_token_for_chat")` で test 専用文字列、production env を override しない (V2 標準遵守)。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: conftest.py の api_client fixture に jwt_cookie を bake in</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (L8-12 jwt_cookie fixture + L46-55 mock_job_store + L58-63 mock_arq_redis + L66-91 api_client、依存関係)
    - tests/test_api_chat.py / tests/test_api_jobs.py (api_client fixture を使っている 8 テスト関数の現状)
    - app/api/routes/chat.py (Depends(get_jwt_payload) がかかっている各エンドポイント、cookie name = "session")
    - app/auth/jwt_utils.py (cookie 名と検証ロジック、session という cookie キーで JWT を受け取る)
    - .planning/phases/39-ui-polish/39-RESEARCH.md ("Don't Hand-Roll" L282: 「conftest.py で 1 個の fixture を共有」推奨)
  </read_first>
  <action>
    tests/conftest.py の api_client fixture (L66-91) を拡張し、jwt_cookie を **fixture 依存** として受け取り `AsyncClient` の cookies に注入する。

    Step A: api_client fixture 関数シグネチャに `jwt_cookie` 引数を追加:
    - 現状: `async def api_client(mock_graph, mock_auth_manager, mock_job_store, mock_arq_redis):`
    - 修正後: `async def api_client(mock_graph, mock_auth_manager, mock_job_store, mock_arq_redis, jwt_cookie):`

    Step B: `AsyncClient` 初期化箇所 (L89) に cookies 注入を追加:
    - 現状: `async with AsyncClient(transport=transport, base_url="http://test") as client:`
    - 修正後: `async with AsyncClient(transport=transport, base_url="http://test", cookies={"session": jwt_cookie}) as client:`

    Step C: docstring (L68-71) を更新 (任意、JWT cookie 自動注入を明示):
    現状の "Lifespan does NOT fire with ASGITransport — inject mocks directly into app.state." の直後に以下を追加:
    "JWT cookie は jwt_cookie fixture (L8-12) 経由で自動注入される (Phase 39 UIFIX-04 D-10 Pattern A)。"

    本 task で `pytest tests/test_api_jobs.py -v --tb=short` を実行し、Pattern A pure (psycopg を経由しない) 2 件が green になることを確認する。test_api_chat 6 件は 401 は解消されるものの Pattern B psycopg mock 経路で一部 fail する可能性があり、Task 2 でまとめて green 化する。

    本 task で他の fixture (jwt_cookie / mock_graph / mock_auth_manager / mock_job_store / mock_arq_redis / capture_trace_logs / auth_manager) は無変更。
  </action>
  <verify>
    <automated>grep -cE 'cookies=\{"session": jwt_cookie\}' tests/conftest.py</automated>
    Expected: 1
    <automated>grep -cE 'async def api_client\(.*jwt_cookie' tests/conftest.py</automated>
    Expected: 1
    <automated>pytest tests/test_api_jobs.py -v --tb=short 2>&1 | tail -3</automated>
    Expected: 2 passed (Pattern A pure、psycopg 経由なし)
  </verify>
  <acceptance_criteria>
    - `grep -cE 'cookies=\{"session": jwt_cookie\}' tests/conftest.py` の出力が `1`
    - `grep -cE 'async def api_client\(.*jwt_cookie' tests/conftest.py` の出力が `1`
    - `pytest tests/test_api_jobs.py -v` の全テストが pass (2 件 green)
    - test_api_chat 6 件のうち、Pattern A pure 経路 (psycopg を経由しない test_new_thread_returns_uuid / test_delete_thread_calls_adelete 等) が pass。Pattern B が露出する 3 件は Task 2 で green 化するため、本 task の acceptance ではまだ red 許容
    - 他の fixture (jwt_cookie / mock_graph / mock_auth_manager / mock_job_store / mock_arq_redis / capture_trace_logs / auth_manager) が無変更 (`git diff tests/conftest.py` で変更が L66-L91 範囲内のみ)
  </acceptance_criteria>
  <done>D-10 Pattern A test_api_jobs 2 件 + test_api_chat の 401 解消、Pattern B 残存箇所が Task 2 の特定対象として可視化</done>
</task>

<task type="auto">
  <name>Task 2: tests/test_api_chat.py の Pattern A + Pattern B 統合修正 (6 + 3 件)</name>
  <files>tests/test_api_chat.py</files>
  <read_first>
    - tests/test_api_chat.py (全体、6 テスト関数の現状)
    - tests/conftest.py (Task 1 で拡張済の api_client fixture、jwt_cookie fixture)
    - app/api/routes/chat.py (list_threads / new_thread / delete_thread の実装、psycopg AsyncConnection.cursor() の async with 経路)
    - app/api/routes/jobs.py (jobs エンドポイント、psycopg 不使用の経路と比較)
    - .planning/phases/36-text-code-image-multimodal/deferred-items.md (Phase 36 由来の Pattern B 主張 = psycopg AsyncMock パターン更新の原典)
    - .planning/phases/39-ui-polish/39-RESEARCH.md (Pattern B Code Examples、AsyncContextManager 対応)
  </read_first>
  <action>
    Step A: Task 1 完了後の failure 状況を取得し、Pattern B 該当箇所を特定:
    ```
    pytest tests/test_api_chat.py -v --tb=long 2>&1 | grep -E '^(FAILED|AttributeError|TypeError|AssertionError|TypeError.*async)' | head -30
    ```

    401 が消えた後、psycopg.AsyncConnection の cursor() を `async with conn.cursor() as cur:` で開けないことに起因する TypeError / AttributeError が出ているはず。失敗テスト 3 件を確定する (実 failure msg ベースで test_list_threads_left_join / test_list_threads_app_id_filter / test_list_threads_no_app_id_returns_all が候補、ただし実測で確定する)。

    Step B: Pattern B 該当 3 件の psycopg mock 経路を AsyncContextManager 対応に修正:

    各テストで psycopg.AsyncConnection の mock を以下の形に書き換える (失敗メッセージで特定された具体的 mock 行を最小限修正):

    ```python
    # OK パターン (AsyncConnection.cursor() が async context manager を返す):
    mock_cursor = AsyncMock()
    mock_cursor.execute = AsyncMock()
    mock_cursor.fetchall = AsyncMock(return_value=[<expected rows>])
    # AsyncConnection.cursor() を async context manager として動作させる
    mock_cursor_cm = AsyncMock()
    mock_cursor_cm.__aenter__ = AsyncMock(return_value=mock_cursor)
    mock_cursor_cm.__aexit__ = AsyncMock(return_value=None)
    mock_conn.cursor = MagicMock(return_value=mock_cursor_cm)
    ```

    もしくは pytest fixture として `psycopg_async_cursor_mock` を tests/conftest.py に新規追加する判断もあり (executor 判断、ただし D-12 上限ポリシーに従い 1 fixture 以上は追加しない方針を優先)。

    Step C: Pattern A pure 3 件 (Task 1 後に既に green、Pattern B 経由しないテスト) の docstring に必要に応じて `(Phase 39 UIFIX-04 D-10 Pattern A — JWT cookie 注入)` を追記 (任意、視認用)。

    Pattern B 該当 3 件の docstring に `(Phase 39 UIFIX-04 D-10 Pattern B — psycopg AsyncMock AsyncContextManager 対応)` を追記。

    Step D: 検証
    ```
    pytest tests/test_api_chat.py -v --tb=short
    ```
    6 件すべて green であることを確認 (Pattern A pure 3 件 + Pattern B 該当 3 件)。

    本 task で production code (app/api/routes/chat.py) は **絶対に touch しない**。Pattern B は test 側 mock 設計の更新のみで解消する設計。
  </action>
  <verify>
    <automated>pytest tests/test_api_chat.py -v --tb=short 2>&1 | tail -10</automated>
    Expected: 6 passed
    <automated>pytest tests/test_api_chat.py tests/test_api_jobs.py -v --tb=short 2>&1 | grep -cE '^PASSED|passed$' || pytest tests/test_api_chat.py tests/test_api_jobs.py -q 2>&1 | tail -1</automated>
    Expected: 8 passed total
    <automated>git diff app/api/routes/chat.py | wc -l</automated>
    Expected: 0 (production code 無変更)
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_api_chat.py -v` の全 6 件が pass (Pattern A 解消 + Pattern B 解消)
    - `pytest tests/test_api_chat.py tests/test_api_jobs.py -q` で 8 passed
    - tests/test_api_chat.py 内に `__aenter__` または `asynccontextmanager` パターンが追加されている (Pattern B psycopg AsyncContextManager 対応、`grep -cE '__aenter__|asynccontextmanager' tests/test_api_chat.py` が `1` 以上)
    - production code が無変更 (`git diff app/api/routes/chat.py app/api/routes/jobs.py | wc -l` の出力が `0`)
    - test_api_chat.py の他のテスト関数 (本 plan scope 外) が無変更
  </acceptance_criteria>
  <done>D-10 Pattern A test_api_chat 6 件 + Pattern B test_api_chat 3 件すべて green、合計 11 件解消 (test_api_jobs 2 + test_api_chat 6 + Pattern B 3、ただし Pattern A 内訳で Pattern B 該当 3 件は既に 6 件に含まれるため net 8 件減: Pattern A 8 件 = test_api_chat 6 + test_api_jobs 2、Pattern B test_api_chat 3 件は Pattern A 6 件と同じ test 関数のため重複カウントせず)</done>
</task>

<task type="auto">
  <name>Task 3: pytest baseline 確認 (Pattern A + Pattern B test_api_chat 完遂の検証)</name>
  <files>(read-only — verification only)</files>
  <read_first>
    - .planning/phases/39-ui-polish/39-BASELINE.md (Phase 39 開始時 27 failed、Plan 04 後 23 件、Plan 06 後 17 件)
    - Task 1-2 完了後の pytest 実行結果
  </read_first>
  <action>
    Step A: pytest full suite を実行し、本 plan 完了後の failed 件数を計測:
    ```
    pytest tests/ --ignore=tests/test_mcp_server.py -q --tb=no 2>&1 | tail -3
    pytest tests/ --ignore=tests/test_mcp_server.py -q --tb=no 2>&1 | grep '^FAILED' | sort
    ```

    Step B: 期待される件数:
    - Plan 06 完了時: 17 件
    - 本 plan で減少: Pattern A 8 件 (test_api_chat 6 + test_api_jobs 2) + Pattern B test_api_chat 3 件 = 計 8 件 (test_api_chat の 6 件は Pattern A と B の重複、ユニークな失敗テスト関数数は 8) もしくは実装次第で最大 11 件
    - 本 plan 完了時 expected: 6 件以下 (残りは Plan 08 が消化する Pattern C+D 11 件のうち、test_api_chat と無関係な test_graph 3 + test_worker 5 + test_debate/rpc/subagent 3 = 11 件)

    Step C: 残失敗が Plan 08 scope (Pattern C + Pattern D + Pattern B test_worker 1 件) のみであることを確認:
    - failed リストの test file が tests/test_graph.py / tests/test_worker.py / tests/test_debate_handler.py / tests/test_rpc_integration.py / tests/test_tool_enabled_subagent.py のいずれかに限定される
    - test_api_chat / test_api_jobs に残失敗がないこと (本 plan で完全解消)
    - test_sse / test_job_store / test_generate_mcp_artifacts / test_tool_catalog_js / test_tool_registry に残失敗がないこと (Plan 04/06 で解消済)

    Step D: 万一 Plan 08 scope 外の新規 failure (例: Plan 04 / 06 で解消したはずのテストが Task 1 の conftest.py 変更で再発した) が露出した場合、deferred-items.md に triage 記録するのではなく、本 plan で即修正する (Pattern B scope 内完遂と同じ思想 — defer 経路は撤廃済)。

    本 task は test 修正を行わない (Task 1-2 で完結)、観測のみ。
  </action>
  <verify>
    <automated>pytest tests/ --ignore=tests/test_mcp_server.py -q --tb=no 2>&1 | tail -1</automated>
    Expected: failed 数が 6 件以下 (Plan 06 完了 17 件 → Pattern A 8 + Pattern B test_api_chat 3 解消で 8 件減、ただし test_api_chat の 6 件は Pattern A/B 重複のためユニーク 8 件減 = 9 件 ± 残余)
    <automated>pytest tests/ --ignore=tests/test_mcp_server.py -q --tb=no 2>&1 | grep '^FAILED' | grep -cE 'test_api_(chat|jobs)\.py'</automated>
    Expected: 0 (Pattern A + Pattern B test_api_chat 完全解消)
    <automated>pytest tests/ --ignore=tests/test_mcp_server.py -q --tb=no 2>&1 | grep '^FAILED' | grep -cvE 'test_(graph|worker|debate_handler|rpc_integration|tool_enabled_subagent)\.py'</automated>
    Expected: 0 (残失敗は Plan 08 scope のみに限定)
  </verify>
  <acceptance_criteria>
    - `pytest tests/ --ignore=tests/test_mcp_server.py -q --tb=no` の failed 件数が **6 件以下** (Plan 06 完了 17 件 → 本 plan で 11 件以上解消)
    - test_api_chat / test_api_jobs の残失敗が 0 件 (Pattern A + B 完遂)
    - 残失敗のテストファイルが Plan 08 scope (test_graph / test_worker / test_debate_handler / test_rpc_integration / test_tool_enabled_subagent) に限定される
    - Phase 39 内で潰せない failure (deferred-items.md 行き) がない (scope 内完遂)
  </acceptance_criteria>
  <done>D-10 Pattern A 全 10 件 (test_sse 2 = Plan 04 + test_api_chat 6 + test_api_jobs 2 = 本 plan) と Pattern B test_api_chat 3 件が完遂、target failed 27 → ≤ 6</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new attack surface — test の JWT cookie 注入は既存 jwt_cookie fixture (L8-12) の再利用のみ。Pattern B 修正も test mock 設計の更新のみ、production secret や認証経路・データ経路に変更なし。

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-07-01 | S | api_client fixture が production env の JWT_SECRET を漏らす | mitigate | jwt_cookie fixture (conftest.py L8-12) は `create_jwt("ghu_test_token_for_chat")` で test 専用 GitHub login を使う、JWT_SECRET は env var or 専用ファイル経由でロード (jwt_utils.py 標準)。本 plan は fixture 設計を変えず再利用するだけ |
| T-39-07-02 | T | jwt_cookie 自動注入で 401 が必須のテスト (例: 認証なしで 401 を期待するテスト) が誤って 200 を返す | accept | 現状 conftest.py に「認証なしで 401 を期待」するテスト fixture は存在せず、api_client 経由のテストはすべて認証済を期待 |
| T-39-07-03 | T | psycopg AsyncMock パターン更新で他の psycopg 経由テスト (例: test_worker の startup) を壊す | mitigate | Task 2 は tests/test_api_chat.py の inline mock のみ修正、グローバル fixture 化は行わない。test_worker の psycopg mock は Plan 08 で別途修正 |
</threat_model>

<verification>
- Task 1 完了: api_client fixture が jwt_cookie を bake in、test_api_jobs 2 件 green、test_api_chat の 401 解消
- Task 2 完了: test_api_chat 6 件 + Pattern B 3 件統合修正で 6 件全 green
- Task 3 完了: pytest --ignore=tests/test_mcp_server.py の failed 件数が Plan 06 完了 17 件 → 本 plan 完了 ≤ 6 件、残失敗は Plan 08 scope に限定
</verification>

<success_criteria>
- UIFIX-04 D-10 Pattern A (JWT cookie 不足 10 件のうち test_sse 2 = Plan 04 + test_api_chat 6 + test_api_jobs 2 = 8 件) 完全解消
- UIFIX-04 D-10 Pattern B test_api_chat 3 件 完遂 (Plan 08 で test_worker 1 件を分担、合わせて Pattern B 全 4 件解消)
- conftest.py の api_client fixture が 1 箇所拡張で 8 件 fan-out 解決
- D-12 上限ポリシー遵守 — 新規 fixture 追加なし、defer 経路は撤廃 (Pattern B scope 内完遂)
- target failed: 27 → 0 への到達経路を Plan 04 (4 件) + Plan 06 (6 件) + 本 plan (11 件 unique) + Plan 08 (12 件) で完遂
</success_criteria>

<output>
After completion, create `.planning/phases/39-ui-polish/39-07-SUMMARY.md`
</output>
