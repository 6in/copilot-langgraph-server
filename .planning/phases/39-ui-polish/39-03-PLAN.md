---
phase: 39-ui-polish
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - frontend/src/theme.css
autonomous: false
requirements: [UIFIX-02]
tags: [polish, css, chatscope, ui]

must_haves:
  decision_citations:
    - "`D-03:` 修正範囲はユーザー視認で 2 点 (縦長コード潰れない / 他 Markdown 要素一貫表示) を確認できれば足りる。"
  truths:
    - "縦長コード (50 行 python ブロック等) を含む incoming message で chat バルーンが fit-content に潰れず、利用可能な全幅 (theme.css 既定の `.cs-message--incoming` 100%) を取る"
    - "他の Markdown 要素 (表 / 引用 / Mermaid) も同じく incoming バルーン内でフルバルーン幅で表示される"
    - "outgoing バルーン (ユーザー発言) の右側 max-width が 85% mobile rule を含めて壊れていない (Pitfall 2 回避)"
    - "新規 CSS rule は `.cs-message--incoming` prefix を必ず持ち、CollapsibleCodeBlock 単体への min-width/max-width 直書きは無い (D-02 厳守)"
  artifacts:
    - path: "frontend/src/theme.css"
      provides: "CollapsibleCodeBlock 等が `.cs-message__custom-content` の fit-content 子要素として潰れないための追補 override"
      contains: "cs-message__custom-content > div"
      min_lines_added: 3
  key_links:
    - from: "frontend/src/theme.css §\"Monaco editor — break out of chatscope bubble width constraint\""
      to: "incoming bubble の CollapsibleCodeBlock / table / blockquote / Mermaid SVG container"
      via: "`.cs-message--incoming .cs-message__custom-content > div { width: 100% }` (またはWave 1 reality-check で確定する rule)"
      pattern: "cs-message__custom-content > div"
---

<objective>
UIFIX-02 (CollapsibleCodeBlock バルーン幅 chatscope fit-content 問題) を D-02 / D-03 の方針で解消する:
- chatscope `.cs-message__content` 側を CSS override で full-width 固定 (Phase 35 base layer 「変数駆動置換 + `!important` 据え置き」パターン継承)
- CollapsibleCodeBlock 単体に min-width / max-width を打たない
- 1-3 行の追加 rule で済むことを Wave 1 冒頭の chrome-devtools reality-check で先に確認

Purpose: 縦長コードや表・引用・Mermaid 図を含む incoming メッセージで、`.cs-message__custom-content` の `display: flex` / 子要素 fit-content 連鎖により bubble が縦に長く細く潰れる現象を解消し、Phase 35 base layer と整合する形で全 Markdown 要素を一貫してフルバルーン幅で表示する。

Output:
- `frontend/src/theme.css` の既存 §"Monaco editor — break out of chatscope bubble width constraint" ブロック (L147-169) に追補 rule を追加 (合計 +3〜+6 行、確定値は reality-check 後に Task 2 で書く)
- 修正範囲は Phase 35 で確立した override layer 内に限定 (新規 CSS file 作成しない、CollapsibleCodeBlock.tsx を触らない)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-ui-polish/39-CONTEXT.md
@.planning/phases/39-ui-polish/39-RESEARCH.md
@.planning/phases/39-ui-polish/39-PATTERNS.md
@.planning/phases/39-ui-polish/39-BASELINE.md
@.planning/phases/35-dashboard-design-system/35-CONTEXT.md
@CLAUDE.md
@frontend/src/theme.css
@frontend/src/components/MarkdownMessage.tsx

<interfaces>
<!-- Phase 35 既存 chatscope override (frontend/src/theme.css L147-169) -->
.cs-message--incoming {
  max-width: 100% !important;
  width: 100% !important;
}

.cs-message--incoming .cs-message__content-wrapper,
.cs-message--incoming .cs-message__content,
.cs-message--incoming .cs-message__custom-content {
  max-width: 100% !important;
  width: 100% !important;
  box-sizing: border-box;
}

.cs-message--incoming pre {
  width: 100%;
  max-width: 100%;
}

<!-- Phase 39 追補候補 (RESEARCH Pattern 1 / Code Examples L401-409) -->
/* CollapsibleCodeBlock の最外 div が必ず親バルーンの幅を引き継ぐ担保。
   chatscope の .cs-message__custom-content の display:flex / fit-content 子要素として
   潰れる現象を防ぐ。 */
.cs-message--incoming .cs-message__custom-content > div {
  width: 100%;
}

<!-- 制約 (D-02 / Pitfall 1-2) -->
- `!important` は既存ルールと同じ程度のみ。新規 selector で抗争しない
- `.cs-message--incoming` prefix を必ず付ける (outgoing バルーンを巻き込まない、Pitfall 2)
- 値は 100% / max-width 100% のみ、CollapsibleCodeBlock 単体に min-width / max-width を打ち込まない
- specificity 不足時は `.cs-message--single` / `--first` / `--last` を AND で書き足す (Pitfall 1)
- ※ `--cs-message-content-width` 系 token は codebase に不在 (RESEARCH A6)、新規 token 導入しない
- ※ theme.css L549-552 の outgoing 用 mobile max-width 85% rule と矛盾しないこと
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Chrome DevTools reality-check で UIFIX-02 の壊れ方を確認し、追加 CSS rule を確定</name>
  <files>(read-only — observation only)</files>
  <read_first>
    - .planning/phases/39-ui-polish/39-RESEARCH.md (Open Question 1: 1 行か複数 selector か、Pitfall 1-2: specificity / outgoing 巻き込み、Code Examples L401-409 のたたき台 rule)
    - frontend/src/theme.css (L147-169 既存 override + L540-636 mobile media query、修正範囲の境界)
    - frontend/src/components/MarkdownMessage.tsx (L200-290 CollapsibleCodeBlock 実装、最外 div の構造)
    - CLAUDE.md §"Chrome DevTools MCP" (chromium remote-debug 起動コマンドと運用パターン)
  </read_first>
  <action>
    本 task は **checkpoint task** で、executor は (1) Chrome DevTools MCP 経由で `/orochi/chat` を開く、(2) 50 行以上の python コードブロックを含む応答を生成させて inspect する、(3) 案 A (1 行) / 案 B (specificity 上げ) / 案 C (別 selector) のうちどれを Task 2 で採用すべきかを user 承認のもと確定する。コード変更は行わず、観察と判断のみ。詳細手順は <how-to-verify> セクション参照。
  </action>
  <what-built>
    Task 2 で確定 CSS rule を書く前段。executor (Claude) は chrome-devtools MCP 経由でローカル `/orochi/chat` を開き、現状の bubble 幅潰れを観察してから、追加 rule が 1 行で足りるか追加 selector が必要かを確定する。
  </what-built>
  <how-to-verify>
    User 側の準備:
    1. Chromium をリモートデバッグモードで起動: `! chromium --remote-debugging-port=9222 --no-first-run --no-default-browser-check &`
    2. `curl -s http://127.0.0.1:9222/json/version` で起動確認
    3. `docker compose up frontend api worker mcp-server postgres redis -d` で dev 環境起動
    4. ブラウザで http://localhost:5173/orochi/ を開きログイン (Device Flow / 既存 JWT cookie)

    Executor (Claude) 側の確認手順:
    A. chrome-devtools MCP で `/orochi/chat` ページに遷移、新規スレッドを作成
    B. 50 行以上の python コードブロックを生成させる (AI へのプロンプト例: "次のコードを返してください: ヘッダーコメント 5 行 + 関数定義 + 30 行のロジック + 末尾 if __name__ ガード")
    C. 応答の incoming bubble を inspect し以下を計測:
       - `.cs-message--incoming` の computed `width` / `max-width`
       - `.cs-message__content-wrapper` / `.cs-message__content` / `.cs-message__custom-content` の computed `width` / `display`
       - **CollapsibleCodeBlock の最外 `<div>`** の computed `width` (これが fit-content / 100% / その他 のいずれか確認)
    D. 確認結果から下記いずれかを Task 2 用に確定:
       - 案 A (RESEARCH 推奨): `.cs-message--incoming .cs-message__custom-content > div { width: 100% }` の 1 ルール (3-4 行)
       - 案 B: 案 A + specificity 上げ (`.cs-message--incoming.cs-message--single .cs-message__custom-content > div { width: 100% !important }` 等の AND 条件追記、6-8 行)
       - 案 C: 別 selector (例えば `.cs-message--incoming .cs-message__custom-content` に `display: block;` 強制等) — Task 2 で具体的に書き下す
    E. 追加 rule で「他の Markdown 要素 (表 / 引用 / Mermaid)」も連鎖的に同じ動作になるか机上確認 (case-by-case で再 inspect は不要、CSS 構造で `.cs-message__custom-content > div` は CollapsibleCodeBlock / table 親 / blockquote 親 / MermaidBlock 親いずれも該当)

    User 側の最終確認:
    1. Executor が報告する「確定 rule」(案 A/B/C どれを採用するか + 具体的 CSS)を読む
    2. D-02 / Pitfall 2 (`.cs-message--incoming` prefix) が守られているか確認
    3. CollapsibleCodeBlock.tsx を**触らない**方針が維持されているか確認
    4. Wave 1 reality-check の所要は 15-30 分以内 (オーバーランしたら一旦 case A で進める判断もユーザー側で)
  </how-to-verify>
  <resume-signal>Type "approved {案 A|B|C}" or describe the exact CSS rule to write in Task 2</resume-signal>
  <done>Task 2 で書く確定 CSS rule (selectors + properties + 行数) がユーザー承認済</done>
</task>

<task type="auto">
  <name>Task 2: theme.css §"Monaco editor — break out..." に確定 rule を追補</name>
  <files>frontend/src/theme.css</files>
  <read_first>
    - frontend/src/theme.css (L147-169 既存 override ブロック、追補位置 = L169 直後)
    - .planning/phases/39-ui-polish/39-RESEARCH.md (Pitfall 1-2、Anti-Patterns "CollapsibleCodeBlock 単体に min-width / max-width を打ち込む" 禁止)
    - .planning/phases/35-dashboard-design-system/35-CONTEXT.md (D-02 `!important` 据え置きルール)
    - Task 1 checkpoint で承認された案 (A / B / C のうちユーザーが選択した CSS)
  </read_first>
  <action>
    `frontend/src/theme.css` の既存 §"Monaco editor — break out of chatscope bubble width constraint" ブロック (L147-169) に、Task 1 で承認された CSS rule を**末尾 (L169 直後)** に追補する。

    制約:
    - 全ての追加 selector に `.cs-message--incoming` prefix を付ける (Pitfall 2、outgoing 巻き込み禁止)
    - 値は `width: 100%` / `max-width: 100%` / `box-sizing: border-box` / `display: block` のうち必要最小限のみ。pixel 値・em 値・固定数値は使わない
    - `!important` は既存ブロック内のルール (L153/154/160/161) と同じ程度のみ追加可、新規 selector で抗争目的の `!important` 連打は禁止 (D-02)
    - CollapsibleCodeBlock 単体 (selector `.collapsible-code-block` 等の component-local class) には `min-width` / `max-width` を**絶対に書かない** (Anti-Patterns 厳禁)
    - 追補は既存コメント `/* ============================================================ ... */` ブロック内 (L147-169 範囲) の論理的延長として、専用サブヘッダーコメント (例: `/* Phase 39 UIFIX-02: ... */`) を 1 行付けてから rule を書く
    - 既存 L147-169 の行を編集・移動しない (追補は L169 の直後にのみ書く)

    Task 1 で承認された案が案 A の場合の具体例 (executor は承認された案に合わせて書く):

    ```
    /* Phase 39 UIFIX-02: CollapsibleCodeBlock 等の最外 div が
       .cs-message__custom-content の display:flex / fit-content 子要素として
       潰れる現象を防ぐ担保。 */
    .cs-message--incoming .cs-message__custom-content > div {
      width: 100%;
    }
    ```

    Task 1 で案 B (specificity 上げ) が承認された場合は、`.cs-message--incoming.cs-message--single .cs-message__custom-content > div` 等 AND 条件 selector + 必要なら `!important` を追加。

    Task 1 で案 C (別 selector) が承認された場合は、その内容を完全踏襲。

    実装後、L549-552 範囲の mobile media query (outgoing 用 max-width 85%) が**変更されていないこと**を `git diff frontend/src/theme.css` で確認する。
  </action>
  <verify>
    <automated>grep -cE 'cs-message--incoming.*cs-message__custom-content > div|cs-message--incoming.*cs-message__custom-content.*display' frontend/src/theme.css && grep -c 'Phase 39 UIFIX-02' frontend/src/theme.css && ! grep -E '\.collapsible-code-block.*(min-width|max-width:[^1])' frontend/src/theme.css</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/theme.css` 内に Task 1 で承認された CSS rule が追加されている
    - 追加 rule の selector はすべて `.cs-message--incoming` で始まる (`grep -n '.cs-message__custom-content > div' frontend/src/theme.css` の全マッチ行で確認)
    - 追加コメント `Phase 39 UIFIX-02` がコード内に 1 回以上出現
    - `grep -E '\.collapsible-code-block.*(min-width|max-width)' frontend/src/theme.css` が 0 件 (CollapsibleCodeBlock 単体への min/max-width 注入なし)
    - `grep -cE 'cs-message__custom-content' frontend/src/theme.css` が 2 以上 (既存 1 件 + Phase 39 追加 1 件以上)
    - L540-636 範囲の mobile media query が変更されていない (`git diff frontend/src/theme.css` で変更行が L147-L200 範囲のみ)
    - 既存 L147-169 のルール (`.cs-message--incoming` / `.cs-message__content-wrapper` 系 4 selector) が無変更
  </acceptance_criteria>
  <done>theme.css に Phase 39 UIFIX-02 追補 rule が入り、CollapsibleCodeBlock を含む incoming bubble がフル幅を取る形になった</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 視認確認 — 縦長コード / 表 / 引用 / Mermaid のバルーン幅</name>
  <files>(observation only)</files>
  <action>
    本 task は **checkpoint:human-verify task** で、executor (Claude) は user が docker compose を起動して chromium で `/orochi/chat` を開いた状態で、4 種 Markdown 要素 (縦長コード / table / blockquote / Mermaid) を AI に生成させ各バルーン幅を視認確認する。outgoing バルーン (Pitfall 2 回避確認) + mobile 幅 (375/768px) も合わせて 6 項目を 1 セッションで完了させる。コード変更は行わず、視認のみ。詳細手順は <how-to-verify> セクション参照。
  </action>
  <what-built>Task 2 で theme.css に追補した CSS rule が、incoming bubble 内の各種 Markdown 要素 (CollapsibleCodeBlock / table / blockquote / Mermaid) を一貫してフルバルーン幅で表示することを実機で確認する。</what-built>
  <how-to-verify>
    User 確認手順:
    1. `docker compose restart frontend` (CSS 変更を Vite HMR で反映、念のため再起動)
    2. ブラウザで http://localhost:5173/orochi/ を開きチャット画面に遷移
    3. **縦長コード**: AI に「Python で 50 行のクラス定義を返して」と依頼、incoming bubble がフルバルーン幅で表示され縦に細長く潰れていないことを目視
    4. **table**: AI に「3 列 × 5 行の Markdown table を返して」、incoming bubble 内の table 要素が同じくフル幅
    5. **blockquote**: AI に「3 段落の引用 (> で始まる) を含む説明文を返して」、同様にフル幅
    6. **Mermaid**: AI に「フローチャートを返して (Mermaid)」、MermaidBlock も同様にフル幅 (Source モード表示でも View モード表示でも幅は同じ)
    7. **outgoing 確認**: 自分が送信したメッセージ (outgoing bubble) の右側 max-width が壊れていないこと (モバイル幅でも 85% を超えないこと、Pitfall 2 回避確認)
    8. **mobile (375-768px)**: ブラウザ DevTools で 375px / 768px に切り替え、incoming/outgoing 双方が崩れていないこと

    各確認項目は manual な目視で OK。問題があれば Task 2 に戻り case-by-case で rule を調整 (例えば specificity 不足なら案 B 寄りに切替)。
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 checks pass, or describe the issue (どの要素・どの幅で崩れたか) and propose a follow-up fix</resume-signal>
  <done>UIFIX-02 success criteria の 2 項目 (縦長コードで bubble 潰れない / 表・引用・Mermaid でフル幅一貫) が user 視認で達成</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new attack surface — UI / test / dead code cleanup. CSS override の追加のみ、認証経路・データ経路に変更なし。

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-03-01 | T | outgoing bubble の幅指定を巻き込んで chat UI 全体が崩れる (Pitfall 2) | mitigate | 全ての追加 selector に `.cs-message--incoming` prefix 必須を acceptance_criteria で grep 検証、mobile media query が無変更であることを diff 範囲で担保 |
| T-39-03-02 | T | `!important` 連打で specificity 抗争に発展、後続 Phase で更に override 必要に | accept | D-02 で `!important` 据え置きルール明文化、Task 1 reality-check で必要最小限に絞る判断を user 承認に置く |
</threat_model>

<verification>
## Decision Citations

- `D-03:` 修正範囲はユーザー視認で 2 点 (縦長コード潰れない / 他 Markdown 要素一貫表示) を確認できれば足りる。

## Verification Checks

- Task 1 完了: Chrome DevTools で現状確認、ユーザー承認済 (案 A/B/C 確定)
- Task 2 完了: theme.css に Phase 39 UIFIX-02 ブロックが追加、acceptance_criteria の grep が全て通過
- Task 3 完了: 4 種 Markdown 要素 (縦長コード / 表 / 引用 / Mermaid) + outgoing + mobile の 7 ケース視認 OK
- pre-existing test failures は本 plan で touch しないため 27 件のまま (Wave 3 で再計測)
</verification>

<success_criteria>
- UIFIX-02 success criteria 「CollapsibleCodeBlock のバルーン幅 chatscope fit-content 問題が、chat 内コードブロックが縦長で潰れず横幅が安定する形で解消されている」を満たす
- D-02 (CSS override で full-width 固定 / `!important` 据え置き / CollapsibleCodeBlock 単体への min-width/max-width 禁止) を遵守
- D-03 (縦長コードで潰れない / 他 Markdown 要素一貫) を視認で確認
- Phase 35 base layer の override 構造を破壊せず追補のみで実装
</success_criteria>

<output>
After completion, create `.planning/phases/39-ui-polish/39-03-SUMMARY.md`
</output>
