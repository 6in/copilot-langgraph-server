---
phase: 39-ui-polish
plan: 05
type: execute
wave: 1
depends_on: [01]
files_modified:
  - frontend/src/contexts/ThemeContext.ts
  - frontend/src/hooks/useThreads.ts
  - frontend/src/components/ChatApp.tsx
  - frontend/src/components/SuperChatApp.tsx
  - frontend/src/components/GemChatApp.tsx
  - frontend/src/components/CanvasChatApp.tsx
  - frontend/src/components/DebateChatApp.tsx
  - frontend/src/components/AttachmentButton.tsx
  - .planning/phases/39-ui-polish/deferred-items.md
autonomous: true
requirements: [UIFIX-04]
tags: [polish, frontend, askme-regression, ts-types, tooltip]

must_haves:
  decision_citations:
    - "`D-07:` AskMe button regression を 5 chat apps の `<MessageArea onAskMe={...}>` 1 行ずつ復活で解消。"
    - "`D-11:` 📎 tooltip option A — `activeThreadId === null` 時の文言出し分け。option B (lazy auto-create) は Phase 34 候補のまま defer。"
  truths:
    - "5 chat apps (ChatApp / SuperChatApp / GemChatApp / CanvasChatApp / DebateChatApp) の <MessageArea> 要素に onAskMe prop が渡されている (truthy callback)"
    - "useThreads.ts の UseThreadsReturn interface に bulkRemoveThreads: (ids: string[]) => Promise<void> が追加されている"
    - "ThemeContext.ts で `export type Theme` が公開されている (MermaidBlock.tsx:13 の lazy import が解決)"
    - "AttachmentButton.tsx に disabledReason?: 'thinking' | 'no-thread' props が追加され、activeThreadId === null 時に「スレッドが未作成のため添付できません」tooltip が表示される"
    - "docker compose exec frontend bun run tsc -b --force の出力で UIFIX-04 D-08 関連 7 件 (bulkRemoveThreads 6 + TS2459 Theme 1) のエラーが消えている"
  artifacts:
    - path: "frontend/src/contexts/ThemeContext.ts"
      provides: "Theme 型を型 export 経由で利用可能にする"
      contains: "export type Theme"
    - path: "frontend/src/hooks/useThreads.ts"
      provides: "bulkRemoveThreads を含む UseThreadsReturn interface"
      contains: "bulkRemoveThreads"
    - path: "frontend/src/components/AttachmentButton.tsx"
      provides: "disabledReason prop による tooltip 出し分け対応"
      contains: "disabledReason"
  key_links:
    - from: "ChatApp / SuperChatApp / GemChatApp / CanvasChatApp / DebateChatApp の <MessageArea>"
      to: "InputBar.tsx:164-165 の `{onAskMe && !isThinking}` 描画条件"
      via: "MessageArea の onAskMe prop pass-through + handleAskMeWrapped"
      pattern: "onAskMe="
    - from: "frontend/src/components/MermaidBlock.tsx:13"
      to: "frontend/src/contexts/ThemeContext.ts の Theme 型 export"
      via: "`import type { Theme } from '../contexts/ThemeContext'`"
      pattern: "import type \\{ Theme \\}"
    - from: "AttachmentButton.tsx disabledReason"
      to: "useAttachments.ts upload() の activeThreadId === null ガード"
      via: "UI 文言出し分けは presentation only、hook 内の backstop 不変 (V5 Input Validation 維持)"
      pattern: "disabledReason"
---

<objective>
UIFIX-04 の D-07 (AskMe button regression) / D-08 (TS エラー 7 件) / D-11 (📎 tooltip 出し分け option A) を 1 plan で潰す。3 つとも frontend types/components の小修正で、互いに独立 (files_modified 重複なし) のため統合可能。

Purpose:
- D-07: Phase 35-03 InputBar split 時に 5 chat apps すべてで <MessageArea onAskMe={...}> 配線が落ちた regression を 1 行追加 ×5 ファイルで復活
- D-08: useThreads return 型 6 consumer の TS エラー + ThemeContext の Theme 型 export 漏れ 1 件を、interface 追加 1 行 + export 1 word 追加で一斉解決
- D-11: 📎 入口段差の option A (tooltip 文言改善)。activeThreadId === null と「送信中」で aria-label / title を分岐、useAttachments.upload の activeThreadId === null ガードは backstop として温存

Output:
- 5 chat app .tsx ファイル各 1 行追加 (`onAskMe={() => {}}` 形式)
- useThreads.ts interface 1 行追加 (bulkRemoveThreads)
- ThemeContext.ts 1 word 追加 (export)
- AttachmentButton.tsx props + tooltip 分岐 (約 +10 行)
- useAttachments.ts は touch 不要 (D-11 は AttachmentButton 側で完結、hook の文言は既存)
- 5 chat apps の AttachmentButton 呼び出し箇所で disabledReason prop を渡す (各 1 行修正、合計 5 箇所)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-ui-polish/39-CONTEXT.md
@.planning/phases/39-ui-polish/39-RESEARCH.md
@.planning/phases/39-ui-polish/39-PATTERNS.md
@.planning/phases/39-ui-polish/39-BASELINE.md
@.planning/todos/pending/2026-04-23-fix-askme-button-regression-in-chat-apps-after-phase-35-inpu.md
@frontend/src/components/ChatApp.tsx
@frontend/src/components/SuperChatApp.tsx
@frontend/src/components/GemChatApp.tsx
@frontend/src/components/CanvasChatApp.tsx
@frontend/src/components/DebateChatApp.tsx
@frontend/src/components/MessageArea.tsx
@frontend/src/components/InputBar.tsx
@frontend/src/components/AttachmentButton.tsx
@frontend/src/hooks/useThreads.ts
@frontend/src/hooks/useAttachments.ts
@frontend/src/contexts/ThemeContext.ts
@frontend/src/components/MermaidBlock.tsx

<interfaces>
useThreads.ts UseThreadsReturn 現状 (L14-24):
  interface UseThreadsReturn {
    threads: ThreadInfo[];
    activeThreadId: string | null;
    messages: ChatMessage[];
    isLoadingMessages: boolean;
    switchThread: (threadId: string) => Promise<void>;
    createNewThread: (gemId?: string | null) => Promise<string>;
    removeThread: (threadId: string) => Promise<void>;
    setMessages: React.Dispatch<React.SetStateAction<ChatMessage[]>>;
    refreshThreads: () => Promise<void>;
  }

useThreads.ts 実装側 bulkRemoveThreads は既に存在 (L76-84、Pitfall 4 — 実装側は触らない):
  const bulkRemoveThreads = useCallback(async (threadIds: string[]) => { ... }, [activeThreadId]);

return block (L86-97) も既に bulkRemoveThreads を返している:
  return {
    ...
    removeThread,
    bulkRemoveThreads,    ← 既に return 済 (interface に追加するだけで TS 解決)
    setMessages,
    refreshThreads,
  };

ThemeContext.ts 現状 (全 12 行):
  type Theme = 'light' | 'dark';            ← L6 (export 無し)
  export const ThemeContext = createContext<Theme>('light');   ← L8 (既存 export pattern)

修正後 L6:
  export type Theme = 'light' | 'dark';     ← export 1 word 追加のみ

InputBar.tsx 描画条件 (変更しない):
  {onAskMe && !isThinking && (
    <button ...>AskMe</button>
  )}

MessageArea.tsx の handleAskMeWrapped (L378-386、変更しない):
  const handleAskMeWrapped = onAskMe
    ? () => {
        const text = inputValue.trim();
        if (!text) return;
        handleSendWrapped(text + AUQ_SUFFIX);
        setInputValue('');
      }
    : undefined;

5 chat apps の <MessageArea> 呼び出し箇所 (existing):
  - frontend/src/components/ChatApp.tsx:337
  - frontend/src/components/SuperChatApp.tsx:293
  - frontend/src/components/GemChatApp.tsx:207
  - frontend/src/components/CanvasChatApp.tsx:305
  - frontend/src/components/DebateChatApp.tsx:782

5 chat apps 共通の追加形式:
  <MessageArea
    ... existing props ...
    onAskMe={() => { /* AUQ trigger flag — handler は MessageArea/InputBar 内で完結 */ }}
    ... rest ...
  />

AttachmentButtonProps 現状:
  export interface AttachmentButtonProps {
    onFilesSelected: (files: File[]) => void;
    disabled?: boolean;
    acceptedExtensions?: string[];
  }

修正後:
  export interface AttachmentButtonProps {
    onFilesSelected: (files: File[]) => void;
    disabled?: boolean;
    disabledReason?: 'thinking' | 'no-thread';   ← 追加
    acceptedExtensions?: string[];
  }

AttachmentButton.tsx の aria-label / title 修正 (L45-46):
  aria-label={
    disabled
      ? (disabledReason === 'no-thread'
          ? 'スレッドが未作成のため添付できません'
          : '添付を追加できません（送信中）')
      : 'ファイルを添付'
  }
  title={
    disabled
      ? (disabledReason === 'no-thread'
          ? 'スレッドを作成してから添付してください'
          : '送信中は添付できません')
      : 'ファイルを添付（最大 100MB / 画像は 10MB × 5 枚まで）'
  }

5 chat apps の AttachmentButton 呼び出し箇所 (disabledReason 追加):
  <AttachmentButton
    onFilesSelected={...}
    disabled={!activeThreadId || isThinking}
    disabledReason={!activeThreadId ? 'no-thread' : 'thinking'}    ← 追加
  />

useAttachments.ts L87-93 (V5 Input Validation backstop 維持、touch しない):
  const upload = useCallback(async (files: File[]) => {
    if (!threadId) {
      if (files.length > 0) {
        setValidationError({ file: files[0], reason: 'スレッドが未作成のため添付できません。' });
      }
      return;
    }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ThemeContext.ts の Theme 型 export + useThreads.ts の UseThreadsReturn interface 拡張</name>
  <files>
    frontend/src/contexts/ThemeContext.ts
    frontend/src/hooks/useThreads.ts
  </files>
  <read_first>
    - frontend/src/contexts/ThemeContext.ts (全 12 行、L6 の `type Theme = 'light' | 'dark';` に export を追加)
    - frontend/src/hooks/useThreads.ts (L14-24 UseThreadsReturn interface + L76-84 既存 bulkRemoveThreads 実装 + L86-97 return block — Pitfall 4 で実装側無変更を厳守)
    - frontend/src/components/MermaidBlock.tsx (L13 `import type { Theme } from '../contexts/ThemeContext';` 行が export 追加で解決することを確認)
    - .planning/phases/39-ui-polish/39-RESEARCH.md (Pitfall 4 — 実装を読む限り `bulkRemoveThreads` は既に存在のため、interface 追加だけで OK)
  </read_first>
  <action>
    Step A: frontend/src/contexts/ThemeContext.ts の L6 を以下に変更する:
    - 現状: `type Theme = 'light' | 'dark';`
    - 修正後: `export type Theme = 'light' | 'dark';`

    export キーワードを 1 word 追加するのみ。他の行 (L1 コメント, L4 import, L8 export const ThemeContext, L10 useCurrentTheme) は無変更。

    Step B: frontend/src/hooks/useThreads.ts の UseThreadsReturn interface (L14-24) に **L21 の removeThread 直後**に 1 行追加する:
    - 追加位置: 現状 L21 (`removeThread: (threadId: string) => Promise<void>;`) の次行
    - 追加内容: `  bulkRemoveThreads: (threadIds: string[]) => Promise<void>;`
    - インデント・行末セミコロンは既存行と完全一致

    interface 追加のみで完結 (L26-98 の実装 + return block は既に bulkRemoveThreads を返しているため、Pitfall 4 通り実装側を touch しない)。

    本 task で `bun run tsc -b --force` を実行し、UIFIX-04 D-08 関連エラー 7 件 (bulkRemoveThreads 6 + TS2459 Theme 1) がすべて消えることを確認する。`docker compose exec frontend bun run tsc -b --force 2>&1` の出力に上記 7 件が出ないこと。
  </action>
  <verify>
    <automated>grep -c 'export type Theme' frontend/src/contexts/ThemeContext.ts</automated>
    Expected: 1
    <automated>grep -c 'bulkRemoveThreads: (threadIds: string\[\]) => Promise<void>' frontend/src/hooks/useThreads.ts</automated>
    Expected: 1
    <automated>docker compose exec frontend bun run tsc -b --force 2>&1 | grep -cE "bulkRemoveThreads|TS2459.*Theme"</automated>
    Expected: 0
  </verify>
  <acceptance_criteria>
    - `grep -c 'export type Theme' frontend/src/contexts/ThemeContext.ts` の出力が `1`
    - `grep -c 'type Theme = ' frontend/src/contexts/ThemeContext.ts` の出力が `1` (export 付きで 1 回)
    - `grep -c 'bulkRemoveThreads' frontend/src/hooks/useThreads.ts` の出力が `3` 以上 (interface 1 + 実装 1 + return 1)
    - `docker compose exec frontend bun run tsc -b --force 2>&1 | grep -cE "bulkRemoveThreads|TS2459.*Theme"` の出力が `0`
    - useThreads.ts の L26-98 範囲 (実装 + return block) が無変更 (`git diff frontend/src/hooks/useThreads.ts` で変更が L14-L25 のみ)
    - ThemeContext.ts の L1-L4 (コメント + import) と L8-L12 (createContext + useCurrentTheme) が無変更
  </acceptance_criteria>
  <done>D-08 確定 7 件の TS エラーが解消、ThemeContext.Theme が型 export 経由でアクセス可能</done>
</task>

<task type="auto">
  <name>Task 2: 5 chat apps の <MessageArea> に onAskMe={() => {}} を 1 行ずつ追加</name>
  <files>
    frontend/src/components/ChatApp.tsx
    frontend/src/components/SuperChatApp.tsx
    frontend/src/components/GemChatApp.tsx
    frontend/src/components/CanvasChatApp.tsx
    frontend/src/components/DebateChatApp.tsx
  </files>
  <read_first>
    - frontend/src/components/MessageArea.tsx (L36-100 props 定義 + L53 onAskMe 型 + L379-386 handleAskMeWrapped 実装、AUQ suffix は MessageArea 内で完結する設計を確認)
    - frontend/src/components/InputBar.tsx (L15 onAskMe?: () => void; + L164-165 描画条件 `{onAskMe && !isThinking}`)
    - frontend/src/components/ChatApp.tsx L337-358 (現状 `<MessageArea>` props block、onAskMe 配線箇所)
    - frontend/src/components/SuperChatApp.tsx L293-... (同上)
    - frontend/src/components/GemChatApp.tsx L207-... (同上)
    - frontend/src/components/CanvasChatApp.tsx L305-... (同上)
    - frontend/src/components/DebateChatApp.tsx L782-... (同上)
    - .planning/todos/pending/2026-04-23-fix-askme-button-regression-in-chat-apps-after-phase-35-inpu.md (AskMe regression 詳細、5 ファイル + 修正箇所)
    - .planning/phases/39-ui-polish/39-RESEARCH.md (Code Examples L489-505、AskMe callback は noop で十分の根拠)
  </read_first>
  <action>
    5 chat apps の `<MessageArea>` JSX block に **`onAskMe={() => { /* AUQ trigger flag — handler は MessageArea/InputBar 内で完結 */ }}`** を 1 行ずつ追加する。

    追加位置の規則: 既存の `activeThreadId={activeThreadId}` 行の直後 (もしくは存在しない場合は `onCancel={cancelJob}` 直後 / `pendingQuestion={pendingQuestion}` の直前)。各ファイルで以下の grep で位置を確定する:

    ```
    grep -n 'activeThreadId={activeThreadId}' frontend/src/components/{ChatApp,SuperChatApp,GemChatApp,CanvasChatApp,DebateChatApp}.tsx
    ```

    上記 grep でヒットしないファイルがあれば、`<MessageArea` から `/>` までの JSX block 内で `onQuestionSubmit={handleQuestionSubmit}` 行の直後を採用 (5 ファイルで一貫させる)。

    挿入する正確な行 (インデントは各ファイルの既存 JSX に合わせる、約 12 スペースインデント):
    ```
    onAskMe={() => { /* AUQ trigger flag — handler は MessageArea/InputBar 内で完結 */ }}
    ```

    本 task は 5 ファイル × 1 行追加のみ。`useChat` の return signature を変えない、AUQ 用 handler を追加しない (RESEARCH "Don't Hand-Roll" — AUQ 起動 callback は noop callback で十分)。

    検証 grep:
    ```
    grep -lE 'MessageArea[^>]*onAskMe' frontend/src/components/{ChatApp,SuperChatApp,GemChatApp,CanvasChatApp,DebateChatApp}.tsx | wc -l
    ```
    出力が `5` であることを確認 (5 ファイルすべてに onAskMe が出現)。

    上記 grep は 1 行内マッチではなく複数行マッチでも検出するため、各 .tsx の `<MessageArea` 行から `/>` までの範囲内に `onAskMe` が含まれているかを保証する。
  </action>
  <verify>
    <automated>for f in frontend/src/components/ChatApp.tsx frontend/src/components/SuperChatApp.tsx frontend/src/components/GemChatApp.tsx frontend/src/components/CanvasChatApp.tsx frontend/src/components/DebateChatApp.tsx; do awk '/<MessageArea/,/\/>/' "$f" | grep -q onAskMe && echo "$f: OK" || echo "$f: MISSING"; done | grep -c OK</automated>
    Expected: 5
    <automated>docker compose exec frontend bun run tsc -b --force 2>&1 | grep -cE "TS[0-9]+"</automated>
    Expected: 0 (TS error が増えていないこと)
  </verify>
  <acceptance_criteria>
    - 5 chat apps すべての `<MessageArea>` JSX block 内に `onAskMe` prop が含まれる (上記 awk + grep で 5 件)
    - 各 onAskMe の値は truthy callback (空関数 or trivial body の関数式)。`onAskMe={null}` や `onAskMe={undefined}` は不可
    - `docker compose exec frontend bun run tsc -b --force` の TS error 数が **Task 1 完了後の状態から増えていない** (Task 1 で 7 → 0 に解消済、Task 2 完了後も 0 のまま)
    - 5 ファイルの `<MessageArea>` block 以外の箇所が無変更 (`git diff` で各ファイルの変更が 1-2 行のみ)
  </acceptance_criteria>
  <done>D-07 確定、AskMe ボタンが 5 chat apps 全てで表示可能になった (InputBar 描画条件 `onAskMe && !isThinking` が truthy 評価)</done>
</task>

<task type="auto">
  <name>Task 3: AttachmentButton.tsx に disabledReason prop 追加 + 5 chat apps から disabledReason を渡す</name>
  <files>
    frontend/src/components/AttachmentButton.tsx
    frontend/src/components/ChatApp.tsx
    frontend/src/components/SuperChatApp.tsx
    frontend/src/components/GemChatApp.tsx
    frontend/src/components/CanvasChatApp.tsx
    frontend/src/components/DebateChatApp.tsx
  </files>
  <read_first>
    - frontend/src/components/AttachmentButton.tsx (全 83 行、L7-11 AttachmentButtonProps、L20-37 component 関数、L41-47 button の disabled / aria-label / title)
    - frontend/src/hooks/useAttachments.ts (L80-100 upload 関数の activeThreadId === null backstop validation、touch しないこと)
    - .planning/phases/39-ui-polish/39-RESEARCH.md (Code Examples L554-580 の AttachmentButton 修正案、Pitfall — V5 Input Validation 維持)
    - 5 chat apps の AttachmentButton 呼び出し箇所 (grep `<AttachmentButton` で位置確認):
      - frontend/src/components/ChatApp.tsx (AttachmentButton usage)
      - frontend/src/components/SuperChatApp.tsx
      - frontend/src/components/GemChatApp.tsx
      - frontend/src/components/CanvasChatApp.tsx
      - frontend/src/components/DebateChatApp.tsx
  </read_first>
  <action>
    Step A: frontend/src/components/AttachmentButton.tsx を以下のとおり修正する:

    1. **AttachmentButtonProps interface (L7-11)** に 1 行追加:
       ```
       export interface AttachmentButtonProps {
         onFilesSelected: (files: File[]) => void;
         disabled?: boolean;
         disabledReason?: 'thinking' | 'no-thread';   // ← 追加
         acceptedExtensions?: string[];
       }
       ```

    2. **コンポーネント関数 (L20-24)** の destructuring に `disabledReason` を追加:
       ```
       export function AttachmentButton({
         onFilesSelected,
         disabled,
         disabledReason,                              // ← 追加
         acceptedExtensions,
       }: AttachmentButtonProps) {
       ```

    3. **button 要素の aria-label / title (L45-46)** を 3 分岐に変更:
       ```
       aria-label={
         disabled
           ? (disabledReason === 'no-thread'
               ? 'スレッドが未作成のため添付できません'
               : '添付を追加できません（送信中）')
           : 'ファイルを添付'
       }
       title={
         disabled
           ? (disabledReason === 'no-thread'
               ? 'スレッドを作成してから添付してください'
               : '送信中は添付できません')
           : 'ファイルを添付（最大 100MB / 画像は 10MB × 5 枚まで）'
       }
       ```

    4. L48-64 の style block、L66-71 の hidden span、L73-80 の input は無変更。

    Step B: 5 chat apps の AttachmentButton 呼び出し箇所に `disabledReason` prop を渡す:

    各ファイルで `grep -n '<AttachmentButton' frontend/src/components/{ChatApp,SuperChatApp,GemChatApp,CanvasChatApp,DebateChatApp}.tsx` で位置を確認し、既存の `disabled={...}` prop の直後に以下を追加する:

    ```
    disabledReason={!activeThreadId ? 'no-thread' : 'thinking'}
    ```

    変数名は各ファイルでの実装に合わせる:
    - `activeThreadId` が直接利用可能なファイルは `!activeThreadId ? 'no-thread' : 'thinking'`
    - thread が変数名違いの場合 (例: DebateChatApp は `threadId` 等の場合) は適切な変数名に置換、ただし意味的に「thread 未作成判定 vs それ以外」の条件式を維持

    Step C: useAttachments.ts は **touch しない** (V5 Input Validation backstop 維持、D-11 は presentation only の修正で hook の validation はそのまま)。

    検証:
    - `grep -c 'disabledReason' frontend/src/components/AttachmentButton.tsx` の出力が `5` 以上 (interface 1 + destructuring 1 + aria-label 1 + title 1 + 任意 comment)
    - `grep -lE '<AttachmentButton[^>]*disabledReason' frontend/src/components/{ChatApp,SuperChatApp,GemChatApp,CanvasChatApp,DebateChatApp}.tsx | wc -l` の出力が `5` (5 ファイルすべてで disabledReason 渡し済)
    - 文言 "スレッドが未作成のため添付できません" が AttachmentButton.tsx 内に出現 (`grep -c 'スレッドが未作成' frontend/src/components/AttachmentButton.tsx` が `1`)
  </action>
  <verify>
    <automated>grep -c 'disabledReason' frontend/src/components/AttachmentButton.tsx</automated>
    Expected: 5 or more
    <automated>grep -c 'スレッドが未作成' frontend/src/components/AttachmentButton.tsx</automated>
    Expected: 1 or more
    <automated>for f in frontend/src/components/ChatApp.tsx frontend/src/components/SuperChatApp.tsx frontend/src/components/GemChatApp.tsx frontend/src/components/CanvasChatApp.tsx frontend/src/components/DebateChatApp.tsx; do awk '/<AttachmentButton/,/\/>/' "$f" | grep -q disabledReason && echo "$f: OK" || echo "$f: MISSING"; done | grep -c OK</automated>
    Expected: 5
    <automated>docker compose exec frontend bun run tsc -b --force 2>&1 | grep -cE "TS[0-9]+"</automated>
    Expected: 0
    <automated>git diff frontend/src/hooks/useAttachments.ts | wc -l</automated>
    Expected: 0
  </verify>
  <acceptance_criteria>
    - AttachmentButton.tsx の AttachmentButtonProps に `disabledReason?: 'thinking' | 'no-thread';` が含まれる
    - AttachmentButton.tsx の関数引数 destructuring に `disabledReason` が含まれる
    - AttachmentButton.tsx の aria-label に「スレッドが未作成のため添付できません」が出現
    - AttachmentButton.tsx の title に「スレッドを作成してから添付してください」が出現
    - 5 chat apps すべての AttachmentButton JSX block 内に `disabledReason` prop が含まれる
    - 5 chat apps の disabledReason 値が `!activeThreadId ? 'no-thread' : 'thinking'` 形式 (もしくは同等の意味的式) になっている
    - `git diff frontend/src/hooks/useAttachments.ts` の出力が空 (hook 不変、V5 backstop 維持)
    - `docker compose exec frontend bun run tsc -b --force` の TS error が 0 件のまま (本 task で新規 TS error 導入なし)
  </acceptance_criteria>
  <done>D-11 option A 完了、活性スレッド未作成時の 📎 tooltip 文言が「スレッドが未作成のため添付できません」に正しく表示される</done>
</task>

<task type="auto">
  <name>Task 4: TS error 残り 4 件 (html-to-image 1 + MermaidBlock implicit any 3) を deferred-items.md に追記</name>
  <files>.planning/phases/39-ui-polish/deferred-items.md</files>
  <read_first>
    - .planning/phases/39-ui-polish/deferred-items.md (Plan 01 で作成、空ヘッダー)
    - .planning/phases/39-ui-polish/39-RESEARCH.md (Assumptions Log A7 — 実測 TS error 11 件中 scope 内 7 件、残り 4 件は scope 外)
    - .planning/phases/39-ui-polish/39-CONTEXT.md (D-08 scope は 7 件、D-12 上限ポリシー)
  </read_first>
  <action>
    Task 1 完了後 (`bun run tsc -b --force` で D-08 scope の 7 件解消) の TS error 残り 4 件 (html-to-image 解決 1 件 + MermaidBlock 関連 implicit any 3 件) は CONTEXT.md D-08 scope (7 件) 外のため、deferred-items.md に追記して v6.1+ 候補として記録する (D-12 上限ポリシー整合)。

    Step A: 残り 4 件の具体的内容を確認:
    ```
    docker compose exec frontend bun run tsc -b --force 2>&1 | grep -E 'TS[0-9]+' | head -10
    ```

    bulkRemoveThreads 関連 6 件 + TS2459 Theme 1 件 (= D-08 scope の 7 件) が Task 1 で解消されているため、残るのは:
    - html-to-image 解決 1 件 (`frontend/src/components/MermaidBlock.tsx` で `import { toPng } from 'html-to-image'` 経路、node_modules install 状態により出る)
    - MermaidBlock implicit any 3 件 (MermaidBlock 内の callback / params で型注釈が不足)

    Step B: deferred-items.md に以下のエントリを追記:

    ```markdown
    ## Plan 39-05 で発見された TS error 残り 4 件 (scope 外)

    - **File:** frontend/src/components/MermaidBlock.tsx (html-to-image 解決 1 件 + implicit any 3 件)
    - **Error / 由来:** D-08 scope (bulkRemoveThreads 6 + TS2459 Theme 1 = 7 件) 解消後に残存する scope 外の TS error。
      - html-to-image 解決 1 件: `node_modules` 内 `html-to-image` 型解決不能 (frontend/package.json に記載あり、docker compose build frontend で再 install 必要)
      - MermaidBlock implicit any 3 件: callback / params の型注釈不足、本来 explicit any もしくは具体型を付けるべき箇所
    - **本 plan で扱わない理由:** CONTEXT.md D-08 が明示的に scope を 7 件と定義、D-12 上限ポリシーに従い「ついで修正」を抑制
    - **いつ取り上げるか:** v6.1+ で観察ベース再評価。html-to-image は build re-install で自動解消する可能性大、MermaidBlock implicit any は Mermaid 関連 spike (UIFIX-01 defer 案件) と同時に対応するのが効率的
    ```

    既存 deferred-items.md のヘッダーフォーマット (Plan 01 で作成済) を踏襲。エントリ追記のみ、ヘッダーは touch しない。
  </action>
  <verify>
    <automated>grep -c 'Plan 39-05 で発見された TS error 残り 4 件' .planning/phases/39-ui-polish/deferred-items.md</automated>
    Expected: 1
    <automated>grep -c 'html-to-image' .planning/phases/39-ui-polish/deferred-items.md</automated>
    Expected: 1 or more
    <automated>grep -c 'implicit any' .planning/phases/39-ui-polish/deferred-items.md</automated>
    Expected: 1 or more
  </verify>
  <acceptance_criteria>
    - `.planning/phases/39-ui-polish/deferred-items.md` に `## Plan 39-05 で発見された TS error 残り 4 件` セクションが含まれる
    - セクション内に `html-to-image` への言及がある
    - セクション内に `MermaidBlock` または `implicit any` への言及がある
    - セクション内に「v6.1+」もしくは「本 plan で扱わない理由」が記載されている
    - deferred-items.md ヘッダー (`# Phase 39 — Deferred Items` 等) は無変更
  </acceptance_criteria>
  <done>D-08 scope 外の TS error 残り 4 件が deferred-items.md に triage 記録された (D-12 上限ポリシー整合)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new attack surface — frontend types/components の小修正のみ。認証経路・API 経路・データ経路に変更なし。

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-05-01 | T | AttachmentButton tooltip 改善で disabled ガードが緩む (D-11 / V5 Input Validation) | mitigate | useAttachments.ts L87-93 の `if (!threadId) { ... return }` early-return を temper しない (Task 3 acceptance で git diff useAttachments 0 行を担保)。UI presentation のみ変更、hook 内 validation は backstop として温存 |
| T-39-05-02 | T | onAskMe を全 chat apps で truthy callback として渡すことで AUQ 経路が誤動作 | accept | MessageArea.tsx の handleAskMeWrapped (L378-386) は親 callback を呼ばず、AUQ suffix 付与は内部で完結。AUQ trigger は presentation only の boolean フラグ用途、ロジック追加なし (RESEARCH Code Examples L501-506) |
| T-39-05-03 | I | Theme 型 export 公開で他コンポーネントから内部 enum 構造を読み取られる | accept | Theme は 'light' | 'dark' の 2 値、機密性なし。MermaidBlock など他コンポーネントで既に同じ値を import している |
</threat_model>

<verification>
## Decision Citations

- `D-07:` AskMe button regression を 5 chat apps の `<MessageArea onAskMe={...}>` 1 行ずつ復活で解消。
- `D-11:` 📎 tooltip option A — `activeThreadId === null` 時の文言出し分け。option B (lazy auto-create) は Phase 34 候補のまま defer。

## Verification Checks

- Task 1 完了: TS error 0 件 (UIFIX-04 D-08 確定 7 件解消)
- Task 2 完了: 5 chat apps の <MessageArea> に onAskMe 配線あり、TS error 増加なし
- Task 3 完了: AttachmentButton の disabledReason 3 分岐実装、5 chat apps から呼び出し済、useAttachments.ts は git diff 0 行
- 累積影響: pytest baseline は本 plan で変更なし (frontend のみ touch)
- Task 1+2+3 完了後の bun run tsc は 0 error
</verification>

<success_criteria>
- UIFIX-04 D-07 (AskMe regression 5 chat apps) 解消
- UIFIX-04 D-08 (TS エラー 7 件 — bulkRemoveThreads 6 + Theme 1) 解消
- UIFIX-04 D-11 (📎 tooltip 文言 option A) 解消
- useAttachments.ts は無変更 (V5 backstop validation 維持)
- D-11 option B (lazy auto-create) は Phase 34 候補のまま defer (本 plan で触らない)
</success_criteria>

<output>
After completion, create `.planning/phases/39-ui-polish/39-05-SUMMARY.md`
</output>
