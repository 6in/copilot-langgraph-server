---
phase: 21-langgraph-bind-tools-toolnode
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - app/orchestrator/tool_agent.py
  - app/orchestrator/agent.py
  - agents/general-assistant/AGENT.md
  - tests/test_tool_enabled_subagent.py
  - tests/test_subagent_registry_tools.py
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02
  - TOOL-03

must_haves:
  truths:
    - "ToolEnabledSubAgent.run() が mini ReAct グラフを実行し、ToolNode 経由でツールを呼び出す"
    - "recursion_limit=25 により 10 ステップ超過時に GraphRecursionError が catch され部分結果を返す"
    - "ToolMessage が AgentState.messages に追加され会話履歴として蓄積される"
    - "SubAgentRegistry が AGENT.md の tools: フラグを読み取り、tools 非空なら ToolEnabledSubAgent を生成する"
    - "tools: が未指定または空のエージェントは従来の SubAgent のまま動作する"
  artifacts:
    - path: "app/orchestrator/tool_agent.py"
      provides: "ToolEnabledSubAgent + build_react_graph + MiniReActState"
      contains: "class ToolEnabledSubAgent"
    - path: "app/orchestrator/agent.py"
      provides: "SubAgentRegistry の tools フラグ対応拡張"
      contains: "mcp_tools"
    - path: "agents/general-assistant/AGENT.md"
      provides: "tools: フラグ追加"
      contains: "tools:"
    - path: "tests/test_tool_enabled_subagent.py"
      provides: "ToolEnabledSubAgent のユニットテスト"
      min_lines: 80
    - path: "tests/test_subagent_registry_tools.py"
      provides: "SubAgentRegistry の tools フラグ読み取りテスト"
      min_lines: 40
  key_links:
    - from: "app/orchestrator/tool_agent.py"
      to: "app/providers/copilot.py"
      via: "self._llm.bind_tools(tools)"
      pattern: "bind_tools"
    - from: "app/orchestrator/tool_agent.py"
      to: "langgraph.prebuilt"
      via: "ToolNode, tools_condition import"
      pattern: "from langgraph.prebuilt import ToolNode"
    - from: "app/orchestrator/agent.py"
      to: "app/orchestrator/tool_agent.py"
      via: "ToolEnabledSubAgent import"
      pattern: "from app.orchestrator.tool_agent import ToolEnabledSubAgent"
---

<objective>
ToolEnabledSubAgent クラスと mini ReAct グラフを実装し、SubAgentRegistry を tools フラグ対応に拡張する。

Purpose: SubAgent 単位で ReAct ループ（agent -> ToolNode -> agent -> ... -> END）を実行可能にし、AGENT.md の宣言的 tools: フラグでツール対応エージェントを自動識別する（TOOL-01/02/03 のコア実装）。
Output: app/orchestrator/tool_agent.py、app/orchestrator/agent.py 拡張、agents/general-assistant/AGENT.md 更新、テスト 2 ファイル。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-langgraph-bind-tools-toolnode/21-CONTEXT.md
@.planning/phases/21-langgraph-bind-tools-toolnode/21-RESEARCH.md
@.planning/phases/21-langgraph-bind-tools-toolnode/21-01-SUMMARY.md

@app/orchestrator/agent.py
@app/orchestrator/state.py
@app/providers/copilot.py
</context>

<interfaces>
<!-- Key types the executor needs -->

From app/providers/copilot.py (Plan 01 で追加):
```python
class ChatCopilot(BaseChatModel):
    def bind_tools(self, tools, *, tool_choice=None, **kwargs) -> "BoundChatCopilot": ...

class BoundChatCopilot(ChatCopilot):
    # JSON prompt injection + tool_call parsing
    async def _agenerate(self, messages, ...) -> ChatResult: ...
```

From app/orchestrator/agent.py (current):
```python
class SubAgent:
    def __init__(self, name, description, model, system_prompt, github_token, keywords=None): ...
    @classmethod
    def from_dir(cls, agent_dir, github_token) -> "SubAgent": ...
    async def run(self, state: AgentState) -> AgentState: ...

class SubAgentRegistry:
    def __init__(self, agent_dir: str, github_token: str): ...
    # Uses frontmatter.load(AGENT.md).metadata
```

From app/orchestrator/state.py:
```python
class AgentState(TypedDict):
    input: str
    output: str
    messages: Annotated[list[BaseMessage], operator.add]
    next: str
    context: Annotated[RPCContext, _keep_first]
    error: str | None
    agent_name: str | None
```

From langgraph (RESEARCH.md verified):
```python
from langgraph.prebuilt import ToolNode, tools_condition
from langgraph.errors import GraphRecursionError
from langgraph.graph import StateGraph, END, START
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 テストスタブ作成</name>
  <files>tests/test_tool_enabled_subagent.py, tests/test_subagent_registry_tools.py</files>
  <read_first>
    - app/orchestrator/agent.py
    - app/orchestrator/state.py
    - tests/test_provider.py
  </read_first>
  <action>
**tests/test_tool_enabled_subagent.py を新規作成:**

以下のテストケースを実装（mock ベース、Copilot SDK 不要）:

1. `test_tool_enabled_subagent_runs_react_loop()`:
   - mock_llm (AsyncMock) を用意。bind_tools() が自分自身を返すようにする
   - mock_llm.ainvoke の side_effect を設定:
     - 1回目: `AIMessage(content="", tool_calls=[ToolCall(name="ping", args={"message":"hi"}, id="c1")])` を返す
     - 2回目: `AIMessage(content="pong result")` を返す（tool_calls 空 → ループ終了）
   - mock_tool = MagicMock(spec=BaseTool, name="ping") で tool.ainvoke = AsyncMock(return_value="pong") を設定
   - ToolEnabledSubAgent を構築（_llm を mock_llm に差し替え、_tools=[mock_tool]）
   - state = {"input": "ping test", "output": "", "messages": [], "next": "", "error": None, "agent_name": None, "context": RPCContext.from_http(user_id="test", app_id="test", thread_id="t1")}
   - result = await agent.run(state)
   - assert result["output"] == "pong result"
   - assert any(isinstance(m, ToolMessage) for m in result["messages"]) -- ToolMessage が含まれる (TOOL-03)
   - assert result["agent_name"] が設定されている

2. `test_react_loop_stops_at_limit()`:
   - mock_llm.ainvoke が常に ToolCall を含む AIMessage を返す（無限ループシミュレーション）
   - recursion_limit=5 (テスト用の小さい値) で build_react_graph を呼ぶ
   - GraphRecursionError が catch され、result["output"] に部分結果が含まれることを assert (TOOL-02)

3. `test_tool_enabled_subagent_no_tool_call()`:
   - mock_llm.ainvoke が AIMessage(content="direct answer") を返す（tool_calls なし）
   - ToolNode を経由せず直接 END に遷移し、result["output"] == "direct answer" を assert

RPCContext のインポート: `from app.orchestrator.context import RPCContext`
ToolMessage のインポート: `from langchain_core.messages import ToolMessage`
ToolCall のインポート: `from langchain_core.messages.tool import ToolCall`

**tests/test_subagent_registry_tools.py を新規作成:**

1. `test_registry_creates_tool_enabled_agent(tmp_path)`:
   - tmp_path に `test-agent/AGENT.md` を作成。frontmatter に `tools: ["ping", "web_search_stub"]` を含める
   - mock_mcp_tools として `[MagicMock(spec=BaseTool, name="ping"), MagicMock(spec=BaseTool, name="web_search_stub")]` を用意
   - SubAgentRegistry(str(tmp_path), "ghu_test", mcp_tools=mock_mcp_tools) を呼ぶ
   - registry.agents["test-agent"] が ToolEnabledSubAgent のインスタンスであることを assert

2. `test_registry_creates_normal_agent_without_tools(tmp_path)`:
   - tmp_path に `normal-agent/AGENT.md` を作成。tools: なし
   - SubAgentRegistry(str(tmp_path), "ghu_test") を呼ぶ
   - registry.agents["normal-agent"] が SubAgent のインスタンス（ToolEnabledSubAgent ではない）を assert

3. `test_registry_creates_normal_agent_when_no_mcp_tools(tmp_path)`:
   - tmp_path に `test-agent/AGENT.md` を作成。tools: ["ping"] あり
   - SubAgentRegistry(str(tmp_path), "ghu_test", mcp_tools=None) を呼ぶ（MCP 未接続想定）
   - registry.agents["test-agent"] が SubAgent のインスタンス（通常降格）を assert

テスト内で AGENT.md を作成する際、python-frontmatter の `frontmatter.dumps(post)` を使うか、テキストを直接書き出す。以下のような内容を書き出す:

    name: test-agent
    keywords: []
    description: テスト用エージェント。対象外: なし
    model: gpt-4.1
    tools: [ping, web_search_stub]

    (本文) テスト用プロンプト。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && uv run python -c "import tests.test_tool_enabled_subagent; import tests.test_subagent_registry_tools; print('imports OK')" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_tool_enabled_subagent.py が存在し、test_tool_enabled_subagent_runs_react_loop, test_react_loop_stops_at_limit, test_tool_enabled_subagent_no_tool_call の 3 関数がある
    - tests/test_subagent_registry_tools.py が存在し、test_registry_creates_tool_enabled_agent, test_registry_creates_normal_agent_without_tools, test_registry_creates_normal_agent_when_no_mcp_tools の 3 関数がある
    - `from langchain_core.messages import ToolMessage` がインポートされている
    - `from langchain_core.messages.tool import ToolCall` がインポートされている
  </acceptance_criteria>
  <done>6 つのテストスタブが作成され、ToolEnabledSubAgent 未実装のためインポートエラーで失敗する（RED 状態）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ToolEnabledSubAgent + build_react_graph 実装</name>
  <files>app/orchestrator/tool_agent.py</files>
  <read_first>
    - app/orchestrator/agent.py
    - app/orchestrator/state.py
    - app/providers/copilot.py
    - tests/test_tool_enabled_subagent.py
  </read_first>
  <behavior>
    - ToolEnabledSubAgent.run() が mini ReAct グラフを実行してツールを呼び出す
    - recursion_limit 超過時に GraphRecursionError を catch して部分結果を返す
    - ツール呼び出しなしの場合は直接 END に遷移して結果を返す
  </behavior>
  <action>
app/orchestrator/tool_agent.py を新規作成する。

**インポート:**
```python
from __future__ import annotations
import logging
import operator
from typing import Annotated, Any
from typing_extensions import TypedDict
from langchain_core.messages import AIMessage, BaseMessage, HumanMessage, SystemMessage
from langchain_core.tools import BaseTool
from langgraph.graph import StateGraph, END
from langgraph.prebuilt import ToolNode, tools_condition
from langgraph.errors import GraphRecursionError
from app.orchestrator.agent import SubAgent
from app.orchestrator.state import AgentState
```

**MiniReActState TypedDict:**
```python
class MiniReActState(TypedDict):
    messages: Annotated[list[BaseMessage], operator.add]
```

**build_react_graph(llm_with_tools, tool_node: ToolNode) -> CompiledGraph:**
- agent_node: async def agent_node(state: MiniReActState) -> dict -- `response = await llm_with_tools.ainvoke(state["messages"])` -> `{"messages": [response]}`
- graph = StateGraph(MiniReActState)
- graph.add_node("agent", agent_node)
- graph.add_node("tools", tool_node)
- graph.set_entry_point("agent")
- graph.add_conditional_edges("agent", tools_condition)
- graph.add_edge("tools", "agent")
- return graph.compile() -- checkpointer なし（D-02: mini グラフは stateless）

**ToolEnabledSubAgent(SubAgent):**
```python
class ToolEnabledSubAgent(SubAgent):
    DEFAULT_RECURSION_LIMIT = 25  # 10 loops x 2 nodes + buffer (TOOL-02, Pitfall 4)

    def __init__(
        self,
        name: str,
        description: str,
        model: str,
        system_prompt: str,
        github_token: str,
        tools: list[BaseTool],
        keywords: list[str] | None = None,
    ):
        super().__init__(name, description, model, system_prompt, github_token, keywords)
        self._tools = tools
        self._tool_node = ToolNode(tools)
        self._llm_with_tools = self._llm.bind_tools(tools)

    @classmethod
    def from_dir(cls, agent_dir, github_token, tools=None):
        # frontmatter.load で AGENT.md を読み、tools 引数でツールを受け取る
        import frontmatter
        post = frontmatter.load(agent_dir / "AGENT.md")
        meta = post.metadata
        return cls(
            name=meta["name"],
            description=meta["description"],
            model=meta.get("model", "claude-sonnet-4-6"),
            system_prompt=post.content,
            github_token=github_token,
            tools=tools or [],
            keywords=meta.get("keywords", []),
        )

    async def run(self, state: AgentState) -> AgentState:
        mini_graph = build_react_graph(self._llm_with_tools, self._tool_node)
        init_messages = [
            SystemMessage(content=self._system_prompt),
            HumanMessage(content=state["input"]),
        ]
        try:
            result = await mini_graph.ainvoke(
                {"messages": init_messages},
                config={"recursion_limit": self.DEFAULT_RECURSION_LIMIT},
            )
            all_messages = result["messages"]
        except GraphRecursionError:
            logger.warning(
                "[tool-agent] %s: recursion limit reached (%d), returning partial result",
                self.name, self.DEFAULT_RECURSION_LIMIT,
            )
            all_messages = init_messages  # fallback

        last_ai = next(
            (m for m in reversed(all_messages) if isinstance(m, AIMessage) and m.content),
            AIMessage(content="(ツール呼び出しが上限に達しました)"),
        )
        return {
            "output": last_ai.content,
            "messages": all_messages,
            "agent_name": self.name,
        }
```

**Pitfall 1 対策:** ToolEnabledSubAgent.__init__ で tools が空リストの場合 logger.warning を出す。ただし空でもインスタンス化は許可（SubAgentRegistry 側でフォールバック判定）。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && uv run pytest tests/test_tool_enabled_subagent.py -x -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app/orchestrator/tool_agent.py が存在する
    - `class MiniReActState(TypedDict)` が定義されている
    - `def build_react_graph(` が定義されている
    - `class ToolEnabledSubAgent(SubAgent):` が定義されている
    - `DEFAULT_RECURSION_LIMIT = 25` が設定されている
    - `from langgraph.prebuilt import ToolNode, tools_condition` がインポートされている
    - `from langgraph.errors import GraphRecursionError` がインポートされている
    - `graph.compile()` に checkpointer 引数がないこと（D-02 遵守）
    - tests/test_tool_enabled_subagent.py の 3 テストが PASSED
  </acceptance_criteria>
  <done>ToolEnabledSubAgent が mini ReAct グラフでツール呼び出しを実行でき、recursion_limit で自動停止し、全テストが GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SubAgentRegistry 拡張 + AGENT.MD 更新</name>
  <files>app/orchestrator/agent.py, agents/general-assistant/AGENT.md</files>
  <read_first>
    - app/orchestrator/agent.py
    - app/orchestrator/tool_agent.py
    - agents/general-assistant/AGENT.md
    - tests/test_subagent_registry_tools.py
  </read_first>
  <behavior>
    - SubAgentRegistry が mcp_tools 引数を受け取り、AGENT.md の tools: が非空かつ mcp_tools ありなら ToolEnabledSubAgent を生成
    - tools: 未指定または mcp_tools なしの場合は従来の SubAgent を生成
    - general-assistant の AGENT.md に tools: [web_search_stub, ping] が追加されている
  </behavior>
  <action>
**1. app/orchestrator/agent.py の SubAgentRegistry.__init__ を拡張:**

コンストラクタのシグネチャを変更:
```python
def __init__(self, agent_dir: str, github_token: str, mcp_tools: list | None = None):
```

`mcp_tools` はオプション引数（後方互換性: 既存の呼び出し元は mcp_tools なしで動作）。

ファイル先頭に import 追加:
```python
from app.orchestrator.tool_agent import ToolEnabledSubAgent
```

`__init__` 内の for ループ中、folder-type エージェントのロード部分を変更:

```python
# 既存の SubAgent.from_dir の代わりに:
post = frontmatter.load(path)
meta = post.metadata
tools_list = meta.get("tools", [])

if tools_list and mcp_tools:
    # ツール名でフィルタして BaseTool リストを組み立てる (D-03)
    tool_map = {t.name: t for t in mcp_tools}
    selected_tools = [tool_map[name] for name in tools_list if name in tool_map]
    if selected_tools:
        agent = ToolEnabledSubAgent(
            name=meta["name"],
            description=meta["description"],
            model=meta.get("model", "claude-sonnet-4-6"),
            system_prompt=post.content,
            github_token=github_token,
            tools=selected_tools,
            keywords=meta.get("keywords", []),
        )
        agent_type = "folder+tools"
    else:
        logger.warning(
            "[registry] agent '%s' declares tools %s but none found in mcp_tools",
            meta["name"], tools_list,
        )
        agent = SubAgent.from_dir(path.parent, github_token)
        agent_type = "folder"
else:
    agent = SubAgent.from_dir(path.parent, github_token)
    agent_type = "folder"
```

code-type エージェント（agent.py あり）の分岐は変更しない（code-type は自前のロジックを持つ）。

**2. agents/general-assistant/AGENT.md に tools: フラグを追加:**

既存の frontmatter（name, keywords, description, model）はそのまま保持し、`model:` の次の行に以下を追加:

```
tools:
  - web_search_stub
  - ping
```

本文（システムプロンプト）は変更しない。最終的な frontmatter フィールドは: name, keywords, description, model, tools の 5 つ。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && uv run pytest tests/test_subagent_registry_tools.py tests/test_tool_enabled_subagent.py -x -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app/orchestrator/agent.py の SubAgentRegistry.__init__ に `mcp_tools: list | None = None` パラメータがある
    - app/orchestrator/agent.py に `from app.orchestrator.tool_agent import ToolEnabledSubAgent` がある
    - app/orchestrator/agent.py に `meta.get("tools", [])` がある
    - app/orchestrator/agent.py に `agent_type = "folder+tools"` がある
    - agents/general-assistant/AGENT.md の frontmatter に `tools:` が含まれる
    - agents/general-assistant/AGENT.md の tools に `web_search_stub` と `ping` が含まれる
    - tests/test_subagent_registry_tools.py の 3 テストが PASSED
  </acceptance_criteria>
  <done>SubAgentRegistry が tools フラグを読み取り ToolEnabledSubAgent/SubAgent を適切に切り替え、general-assistant に tools: が追加済み、全テストが GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AGENT.md tools: -> tool_map lookup | AGENT.md で宣言されたツール名が mcp_tools に存在するかの検証 |
| LLM response -> ToolNode | AI の tool_calls が ToolNode に渡り実行される |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Tampering | AGENT.md tools: field | accept | AGENT.md は開発者管理ファイル。Phase 24 で config.yaml allowlist を追加 |
| T-21-04 | Denial of Service | ReAct infinite loop | mitigate | recursion_limit=25 で 10 ループ上限。GraphRecursionError catch で部分結果返却 |
| T-21-05 | Tampering | ToolNode execution | mitigate | ToolNode._validate_tool_call() が未登録ツール名をエラー処理。スタブツールは副作用なし |
</threat_model>

<verification>
- `uv run pytest tests/test_tool_enabled_subagent.py tests/test_subagent_registry_tools.py -x -q` が全て PASSED
- ToolEnabledSubAgent が mini ReAct グラフ経由でツールを呼び出し、ToolMessage が messages に含まれる
- SubAgentRegistry が tools: あり -> ToolEnabledSubAgent、なし -> SubAgent を正しく切り替える
</verification>

<success_criteria>
- ToolEnabledSubAgent が ReAct ループを実行しツール結果を返せる (TOOL-01)
- recursion_limit で自動停止する (TOOL-02)
- ToolMessage が messages に蓄積される (TOOL-03)
- SubAgentRegistry の tools フラグ対応が後方互換 (D-03)
- 6 つのテストが GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/21-langgraph-bind-tools-toolnode/21-02-SUMMARY.md`
</output>
