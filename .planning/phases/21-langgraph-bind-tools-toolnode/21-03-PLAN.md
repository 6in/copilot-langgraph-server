---
phase: 21-langgraph-bind-tools-toolnode
plan: 03
type: execute
wave: 3
depends_on:
  - 21-02
files_modified:
  - app/jobs/worker.py
  - app/jobs/handlers/orchestrator_handler.py
  - tests/test_react_e2e.py
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02
  - TOOL-03

must_haves:
  truths:
    - "Worker startup で MCP client Singleton が初期化され ctx['mcp_tools'] に BaseTool リストが格納される"
    - "MCP サーバー接続失敗時に worker が DEGRADED 状態（ctx['mcp_tools'] = []）で起動継続する"
    - "OrchestratorHandler が ctx['mcp_tools'] を SubAgentRegistry に渡す"
    - "e2e テストで ToolEnabledSubAgent がツールを呼び出し、ToolMessage を含む結果を返す"
  artifacts:
    - path: "app/jobs/worker.py"
      provides: "MCP Singleton initialization in startup()"
      contains: "mcp_tools"
    - path: "app/jobs/handlers/orchestrator_handler.py"
      provides: "mcp_tools を SubAgentRegistry に渡す配線"
      contains: "mcp_tools"
    - path: "tests/test_react_e2e.py"
      provides: "end-to-end ReAct ループのスモークテスト"
      min_lines: 50
  key_links:
    - from: "app/jobs/worker.py"
      to: "langchain_mcp_adapters.client"
      via: "MultiServerMCPClient import"
      pattern: "MultiServerMCPClient"
    - from: "app/jobs/handlers/orchestrator_handler.py"
      to: "app/orchestrator/agent.py"
      via: "SubAgentRegistry(AGENT_DIR, github_token, mcp_tools=ctx['mcp_tools'])"
      pattern: "mcp_tools"
---

<objective>
Worker に MCP クライアント Singleton を追加し、OrchestratorHandler 経由で SubAgentRegistry にツールを渡す配線を完成させ、e2e テストで全体フローを検証する。

Purpose: Phase 21 の最終統合。Worker 起動時に MCP ツールを取得し、SubAgent がツールを使えるようにする end-to-end パイプラインを完成させる（D-04 実装 + 全要件の統合検証）。
Output: worker.py 拡張、orchestrator_handler.py 配線、tests/test_react_e2e.py 統合テスト。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-langgraph-bind-tools-toolnode/21-CONTEXT.md
@.planning/phases/21-langgraph-bind-tools-toolnode/21-RESEARCH.md
@.planning/phases/21-langgraph-bind-tools-toolnode/21-01-SUMMARY.md
@.planning/phases/21-langgraph-bind-tools-toolnode/21-02-SUMMARY.md

@app/jobs/worker.py
@app/jobs/handlers/orchestrator_handler.py
@app/orchestrator/agent.py
</context>

<interfaces>
<!-- Key types the executor needs -->

From app/jobs/worker.py (current):
```python
async def startup(ctx: dict) -> None:
    ctx["redis_client"] = Redis.from_url(REDIS_URL)
    ctx["job_store"] = JobStore(ctx["redis_client"])
    # ... checkpointer setup, db_pools setup
```

From app/jobs/handlers/orchestrator_handler.py (current):
```python
class OrchestratorHandler(TaskHandler):
    async def handle(self, ctx: dict, job: dict) -> dict:
        # ...
        registry = SubAgentRegistry(AGENT_DIR, github_token)
        # ...
```

From app/orchestrator/agent.py (Plan 02 で拡張):
```python
class SubAgentRegistry:
    def __init__(self, agent_dir: str, github_token: str, mcp_tools: list | None = None):
        # tools フラグ対応済み
```

From langchain_mcp_adapters (RESEARCH.md verified, Docker only):
```python
from langchain_mcp_adapters.client import MultiServerMCPClient
# client = MultiServerMCPClient({"server": {"transport": "streamable_http", "url": url}})
# tools = await client.get_tools()  # returns list[BaseTool]
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Worker startup MCP Singleton + OrchestratorHandler 配線</name>
  <files>app/jobs/worker.py, app/jobs/handlers/orchestrator_handler.py</files>
  <read_first>
    - app/jobs/worker.py
    - app/jobs/handlers/orchestrator_handler.py
    - app/orchestrator/agent.py
    - scripts/test_mcp_tools.py
  </read_first>
  <action>
**1. app/jobs/worker.py の startup() に MCP client 初期化を追加:**

ファイル先頭の import に追加:
```python
import logging

logger = logging.getLogger(__name__)
```

startup() の末尾（db_pools 初期化の後）に以下を追加:
```python
# Phase 21: MCP client Singleton (D-04)
# Pitfall 5: MCP server may not be running — graceful degradation
ctx["mcp_tools"] = []
ctx["mcp_client"] = None
try:
    from langchain_mcp_adapters.client import MultiServerMCPClient

    mcp_url = os.environ.get("MCP_SERVER_URL", "http://mcp-server:8001") + "/mcp"
    mcp_client = MultiServerMCPClient({
        "copilot-tools": {
            "transport": "streamable_http",
            "url": mcp_url,
        }
    })
    ctx["mcp_tools"] = await mcp_client.get_tools()
    ctx["mcp_client"] = mcp_client
    logger.info("[worker] MCP tools loaded: %s", [t.name for t in ctx["mcp_tools"]])
except Exception as e:
    logger.warning("[worker] MCP client init failed (DEGRADED): %s", e)
    # mcp_tools remains [] — ToolEnabledSubAgent will fall back to normal SubAgent
```

shutdown() に MCP client cleanup を追加:
```python
# Phase 21: MCP client cleanup
# (MultiServerMCPClient has no explicit close, but keep reference cleanup)
ctx.pop("mcp_client", None)
ctx.pop("mcp_tools", None)
```

**2. app/jobs/handlers/orchestrator_handler.py の SubAgentRegistry 呼び出しに mcp_tools を追加:**

handle() メソッド内の既存行:
```python
registry = SubAgentRegistry(AGENT_DIR, github_token)
```
を以下に変更:
```python
mcp_tools = ctx.get("mcp_tools", [])
registry = SubAgentRegistry(AGENT_DIR, github_token, mcp_tools=mcp_tools or None)
```

`mcp_tools or None` は空リスト時に None を渡すことで SubAgentRegistry がツールフィルタ処理をスキップする（後方互換性）。

他の行は変更しない。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && uv run python -c "from app.jobs.worker import startup, shutdown; print('worker imports OK')" && uv run python -c "from app.jobs.handlers.orchestrator_handler import OrchestratorHandler; print('handler imports OK')" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - app/jobs/worker.py の startup() に `ctx["mcp_tools"]` の初期化コードがある
    - app/jobs/worker.py の startup() に `from langchain_mcp_adapters.client import MultiServerMCPClient` がある（try ブロック内）
    - app/jobs/worker.py の startup() に `except Exception as e:` で DEGRADED フォールバックがある
    - app/jobs/worker.py の startup() に `logger.info("[worker] MCP tools loaded:` がある
    - app/jobs/worker.py の shutdown() に `ctx.pop("mcp_client", None)` がある
    - app/jobs/handlers/orchestrator_handler.py に `mcp_tools = ctx.get("mcp_tools", [])` がある
    - app/jobs/handlers/orchestrator_handler.py に `SubAgentRegistry(AGENT_DIR, github_token, mcp_tools=` がある
    - 両ファイルがインポートエラーなくロードできる
  </acceptance_criteria>
  <done>Worker startup に MCP Singleton が追加され、OrchestratorHandler が mcp_tools を SubAgentRegistry に渡す配線が完成</done>
</task>

<task type="auto">
  <name>Task 2: e2e テスト作成 + 全スイート確認</name>
  <files>tests/test_react_e2e.py</files>
  <read_first>
    - app/orchestrator/tool_agent.py
    - app/orchestrator/agent.py
    - app/providers/copilot.py
    - tests/test_tool_enabled_subagent.py
  </read_first>
  <action>
tests/test_react_e2e.py を新規作成する。mock ベースの統合テスト（Copilot SDK、MCP 不要）。

**テストケース:**

1. `test_e2e_react_loop_with_tool_call()`:
   フルパイプラインを mock で再現する。

   セットアップ:
   - mock_tool = MagicMock(spec=BaseTool) で name="ping", description="Ping test", get_input_jsonschema=MagicMock(return_value={"type":"object","properties":{"message":{"type":"string"}}})
   - mock_tool.ainvoke = AsyncMock(return_value="pong: hello")
   - ChatCopilot の CopilotClient/SubprocessConfig/PermissionHandler を patch（test_provider.py パターン）
   - 1回目 response.data.content = '{"tool": "ping", "args": {"message": "hello"}}'
   - 2回目 response.data.content = "Tool result: pong: hello"
   - send_and_wait の side_effect を使って 2 回のレスポンスを順次返す

   実行:
   - ToolEnabledSubAgent を直接構築（from_dir ではなく __init__）
   - state を作成（AgentState 互換 dict）
   - result = await agent.run(state)

   アサーション:
   - result["output"] に "pong" が含まれる（ツール結果が最終回答に反映）
   - result["messages"] に ToolMessage が含まれる (TOOL-03)
   - result["messages"] の中に tool_calls を持つ AIMessage がある (TOOL-01)
   - result["agent_name"] が設定されている
   - len(result["messages"]) >= 4（SystemMessage + HumanMessage + AIMessage(tool_calls) + ToolMessage + AIMessage(final)）

2. `test_e2e_no_tool_call_passthrough()`:
   ツール呼び出しが不要なプロンプトの場合。

   セットアップ:
   - response.data.content = "こんにちは！何かお手伝いできることはありますか？"（通常テキスト）

   アサーション:
   - result["output"] == "こんにちは！何かお手伝いできることはありますか？"
   - result["messages"] に ToolMessage が含まれない
   - len(result["messages"]) == 3（SystemMessage + HumanMessage + AIMessage）

3. `test_e2e_tool_message_recorded_in_messages()`:
   ToolMessage が正しい構造で記録されることを確認 (TOOL-03 集中検証)。

   - test 1 と同じセットアップ
   - tool_messages = [m for m in result["messages"] if isinstance(m, ToolMessage)]
   - assert len(tool_messages) == 1
   - assert tool_messages[0].content == "pong: hello"
   - assert tool_messages[0].name == "ping"
   - assert tool_messages[0].tool_call_id が非空文字列

**共通フィクスチャ:**
- `@pytest.fixture` で mock_copilot_sdk を定義（patch パターンは test_provider.py と同一）
- `@pytest.fixture` で mock_ping_tool を定義

テスト実行後、全既存テストスイートも確認:
```
uv run pytest tests/ -x -q --tb=short
```
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && uv run pytest tests/test_react_e2e.py tests/test_copilot_bind_tools.py tests/test_tool_enabled_subagent.py tests/test_subagent_registry_tools.py -x -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_react_e2e.py が存在する
    - test_e2e_react_loop_with_tool_call, test_e2e_no_tool_call_passthrough, test_e2e_tool_message_recorded_in_messages の 3 テスト関数がある
    - `from langchain_core.messages import ToolMessage` がインポートされている
    - `from app.orchestrator.tool_agent import ToolEnabledSubAgent` がインポートされている
    - 3 テストが全て PASSED
    - Phase 21 全テスト（test_copilot_bind_tools.py, test_tool_enabled_subagent.py, test_subagent_registry_tools.py, test_react_e2e.py）が全て PASSED
    - 既存テスト（test_provider.py 等）に regression がない
  </acceptance_criteria>
  <done>e2e テストで ToolEnabledSubAgent のフルパイプラインが検証され、全 Phase 21 テスト + 既存テストが GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP server -> worker | MCP サーバーからツール定義を取得。接続失敗時の graceful degradation が必要 |
| ctx["mcp_tools"] -> SubAgentRegistry | Worker context から Registry へのツール受け渡し |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-06 | Denial of Service | worker startup MCP init | mitigate | try/except で DEGRADED 動作（ctx["mcp_tools"] = []）。worker 全体の起動を妨げない |
| T-21-07 | Information Disclosure | MCP tool results in logs | accept | スタブツールのみ。Phase 22/23 で実データ扱い時にログレベルを見直す |
</threat_model>

<verification>
- `uv run pytest tests/test_react_e2e.py tests/test_copilot_bind_tools.py tests/test_tool_enabled_subagent.py tests/test_subagent_registry_tools.py -x -q` が全て PASSED
- `uv run pytest tests/ -x -q` で既存テストに regression がない
- Worker startup の MCP 初期化がインポートエラーなく動作
</verification>

<success_criteria>
- Worker MCP Singleton が初期化され ctx["mcp_tools"] にツールリストが格納される (D-04)
- MCP 接続失敗時に DEGRADED で起動継続する (Pitfall 5 対策)
- e2e テストで bind_tools -> ToolNode -> ToolMessage の全フローが検証される (TOOL-01/02/03)
- 全 Phase 21 テスト (11 テスト) + 既存テストが GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/21-langgraph-bind-tools-toolnode/21-03-SUMMARY.md`
</output>
