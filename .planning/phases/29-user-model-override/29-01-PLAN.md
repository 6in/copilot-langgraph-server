---
phase: 29-user-model-override
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/orchestrator/agent.py
  - app/jobs/handlers/orchestrator_handler.py
  - tests/test_model_override.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "SuperChat でフロントエンドからモデルを選択すると、AGENT.md の model フィールドではなく選択モデルで推論される"
    - "モデル未選択（空文字）の場合は AGENT.md の model フィールドがフォールバックとして使われる"
    - "SubAgent / ToolEnabledSubAgent / CodeActSubAgent / GemSubAgent の全種別で model_override が機能する"
    - "通常 Chat モード（langgraph_handler 経路）に影響がない"
  artifacts:
    - path: "app/orchestrator/agent.py"
      provides: "SubAgentRegistry に model_override パラメータ追加、SubAgent.from_dir に model_override 伝播"
      contains: "model_override"
    - path: "app/jobs/handlers/orchestrator_handler.py"
      provides: "job dict から model_override を読み取り SubAgentRegistry に渡す"
      contains: "model_override"
    - path: "tests/test_model_override.py"
      provides: "model_override の unit テスト"
      contains: "test_registry_model_override"
  key_links:
    - from: "app/jobs/handlers/orchestrator_handler.py"
      to: "app/orchestrator/agent.py"
      via: "SubAgentRegistry(model_override=model_override)"
      pattern: "model_override=model_override"
    - from: "app/orchestrator/agent.py"
      to: "app/providers/copilot.py"
      via: "ChatCopilot(model=model_override or agent_default)"
      pattern: "model_override or meta\\.get"
---

<objective>
フロントエンドで選択したモデルが SuperChat モードのエージェントデフォルト（AGENT.md `model` フィールド）より優先される `model_override` 伝播を実装する。

Purpose: ユーザーが SuperChat UI でモデルを選択しても、各エージェントが AGENT.md に書かれた固定モデルで推論してしまう問題を解消する。
Output: `app/orchestrator/agent.py`、`app/jobs/handlers/orchestrator_handler.py` の修正 + `tests/test_model_override.py` の新規テスト
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-user-model-override/29-RESEARCH.md
@.planning/phases/29-user-model-override/29-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From app/orchestrator/agent.py:
```python
class SubAgent:
    def __init__(self, name: str, description: str, model: str, system_prompt: str, github_token: str, keywords: list[str] | None = None):
        self._llm = ChatCopilot(model=model, github_token=github_token)

    @classmethod
    def from_dir(cls, agent_dir: Path, github_token: str) -> "SubAgent":
        # model=meta.get("model", "claude-sonnet-4-6")

class SubAgentRegistry:
    def __init__(self, agent_dir: str, github_token: str, mcp_tools: list | None = None, privileged_tool_names: frozenset[str] | set[str] | None = None):
```

From app/orchestrator/tool_agent.py:
```python
class ToolEnabledSubAgent(SubAgent):
    def __init__(self, name, description, model, system_prompt, github_token, tools, keywords=None):
```

From app/orchestrator/codeact_agent.py:
```python
class CodeActSubAgent(SubAgent):
    def __init__(self, name, description, model, system_prompt, github_token, tools, keywords=None, max_iterations=3):
```

From app/orchestrator/gem_agent.py:
```python
DEFAULT_MODEL = "claude-sonnet-4-6"
class GemSubAgent(SubAgent):
    def __init__(self, name: str, system_prompt: str, github_token: str, model: str = DEFAULT_MODEL):
```

From app/jobs/handlers/orchestrator_handler.py (line 30):
```python
# model is intentionally unused in super mode; each agent's AGENT.md defines its own model
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: model_override テスト作成 + agent.py / orchestrator_handler.py の実装</name>
  <files>tests/test_model_override.py, app/orchestrator/agent.py, app/jobs/handlers/orchestrator_handler.py</files>
  <read_first>
    - app/orchestrator/agent.py
    - app/jobs/handlers/orchestrator_handler.py
    - app/orchestrator/gem_agent.py
    - tests/test_subagent_registry_tools.py
  </read_first>
  <behavior>
    - Test 1: SubAgentRegistry に model_override="gpt-4o" を渡すと、生成された folder-type SubAgent の `_llm.model` が `"gpt-4o"` になる
    - Test 2: SubAgentRegistry に model_override=None を渡すと、AGENT.md に書かれた model（例: `gpt-4.1`）が `_llm.model` に反映される
    - Test 3: SubAgentRegistry に model_override="" を渡すと、AGENT.md の model がフォールバックとして使われる（空文字は None 扱い）
    - Test 4: ToolEnabledSubAgent に model_override が渡ると、`_llm.model` が override 値になる
    - Test 5: CodeActSubAgent に model_override が渡ると、`_llm.model` が override 値になる
    - Test 6: SubAgent.from_dir に model_override を渡すと、AGENT.md の model よりも優先される
  </behavior>
  <action>
**Step 1: テスト作成（RED）**

`tests/test_model_override.py` を新規作成する。`tests/test_subagent_registry_tools.py` のフィクスチャパターンを踏襲する。

```python
"""Tests for model_override propagation in SubAgentRegistry."""
import textwrap
from pathlib import Path
from unittest.mock import patch, MagicMock

import pytest


def write_agent_md(agent_dir: Path, *, model: str = "gpt-4.1", tools: list[str] | None = None, agent_type: str | None = None) -> None:
    """Create a minimal AGENT.md with optional tools and agent_type."""
    agent_dir.mkdir(parents=True, exist_ok=True)
    tools_line = f"tools: {tools}" if tools else ""
    agent_type_line = f"agent_type: {agent_type}" if agent_type else ""
    content = textwrap.dedent(f"""\
        ---
        name: {agent_dir.name}
        keywords: []
        description: |
          Test agent.
          対象外: なし
        model: {model}
        {tools_line}
        {agent_type_line}
        ---

        Test system prompt.
    """)
    (agent_dir / "AGENT.md").write_text(content)


@pytest.fixture
def mock_copilot_cls():
    """Patch ChatCopilot so no Copilot SDK is instantiated."""
    with patch("app.providers.copilot.CopilotClient"), \
         patch("app.providers.copilot.SubprocessConfig"), \
         patch("app.providers.copilot.PermissionHandler"):
        yield


def make_mock_tools(names: list[str] | None = None) -> list:
    """Create mock LangChain BaseTools."""
    names = names or ["web_search"]
    tools = []
    for name in names:
        tool = MagicMock()
        tool.name = name
        tools.append(tool)
    return tools
```

テストケース 6 本を `<behavior>` に基づいて作成する。各テストは `SubAgentRegistry` を `model_override` 引数付きで生成し、生成されたエージェントの `_llm.model` を assert する。

**Step 2: agent.py 修正（GREEN）**

2a. `SubAgent.from_dir` のシグネチャに `model_override: str | None = None` を追加:
```python
@classmethod
def from_dir(
    cls,
    agent_dir: Path,
    github_token: str,
    model_override: str | None = None,
) -> "SubAgent":
    post = frontmatter.load(agent_dir / "AGENT.md")
    meta = post.metadata
    return cls(
        name=meta["name"],
        description=meta["description"],
        model=model_override or meta.get("model", "claude-sonnet-4-6"),
        system_prompt=post.content,
        github_token=github_token,
        keywords=meta.get("keywords", []),
    )
```

2b. `SubAgentRegistry.__init__` のシグネチャに `model_override: str | None = None` を追加:
```python
def __init__(
    self,
    agent_dir: str,
    github_token: str,
    mcp_tools: list | None = None,
    privileged_tool_names: frozenset[str] | set[str] | None = None,
    model_override: str | None = None,
):
```

2c. `self._model_override = model_override` をインスタンス変数として保存。

2d. Registry 内のエージェント生成箇所 5 か所を修正:
- CodeActSubAgent 生成（行 190 付近）: `model=meta.get("model", "gpt-4.1")` を `model=model_override or meta.get("model", "gpt-4.1")` に変更
- ToolEnabledSubAgent 生成（行 202 付近）: `model=meta.get("model", "claude-sonnet-4-6")` を `model=model_override or meta.get("model", "claude-sonnet-4-6")` に変更
- SubAgent.from_dir 呼び出し 2 か所（行 214、217 付近）: `SubAgent.from_dir(path.parent, github_token)` を `SubAgent.from_dir(path.parent, github_token, model_override=model_override)` に変更

注意: code-type エージェント（`_load_code_agent` 呼び出し、行 164）は本フェーズの対象外。コメントで明示:
```python
# code-type agents: model_override not supported (requires custom from_dir signature)
agent = _load_code_agent(path.parent, github_token)
```

**Step 3: orchestrator_handler.py 修正（GREEN）**

3a. 行 30 のコメント `# model is intentionally unused in super mode; ...` を削除し、代わりに:
```python
# model_override: フロントで選択したモデル（未選択/空文字時は None → AGENT.md の model を使用）
model_override: str | None = job.get("model") or None
```

3b. SubAgentRegistry 呼び出し（行 42-47）に `model_override=model_override` を追加:
```python
registry = SubAgentRegistry(
    AGENT_DIR,
    github_token,
    mcp_tools=mcp_tools or None,
    privileged_tool_names=privileged_names,
    model_override=model_override,
)
```

3c. GemSubAgent 生成部分（行 78-82）に `model=model_override or DEFAULT_MODEL` を追加:
```python
from app.orchestrator.gem_agent import GemSubAgent, DEFAULT_MODEL
```
既存の行 61 `from app.orchestrator.gem_agent import GemSubAgent` を `from app.orchestrator.gem_agent import GemSubAgent, DEFAULT_MODEL` に変更する。

```python
gem_agent = GemSubAgent(
    name=gem_name,
    system_prompt=gem_system_prompt or "",
    github_token=github_token,
    model=model_override or DEFAULT_MODEL,
)
```
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && uv run pytest tests/test_model_override.py -x -q 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_model_override.py が存在し、6 テストケース全てが pass する
    - app/orchestrator/agent.py に `model_override: str | None = None` が SubAgentRegistry.__init__ と SubAgent.from_dir の両方に含まれる
    - app/orchestrator/agent.py の CodeActSubAgent 生成行に `model_override or meta.get("model"` が含まれる
    - app/orchestrator/agent.py の ToolEnabledSubAgent 生成行に `model_override or meta.get("model"` が含まれる
    - app/orchestrator/agent.py の SubAgent.from_dir 呼び出し 2 か所に `model_override=model_override` が含まれる
    - app/jobs/handlers/orchestrator_handler.py に `model_override: str | None = job.get("model") or None` が含まれる
    - app/jobs/handlers/orchestrator_handler.py の SubAgentRegistry 呼び出しに `model_override=model_override` が含まれる
    - app/jobs/handlers/orchestrator_handler.py の GemSubAgent 生成に `model=model_override or DEFAULT_MODEL` が含まれる
    - 既存テスト `uv run pytest tests/ -x -q` が全て pass する
  </acceptance_criteria>
  <done>
    model_override パラメータが SubAgentRegistry / SubAgent.from_dir / OrchestratorHandler の 3 層で伝播し、SubAgent / ToolEnabledSubAgent / CodeActSubAgent / GemSubAgent の全種別で model_override が AGENT.md の model フィールドに優先される。テストが全件 pass。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | model フィールドは POST /api/chat body から渡される |
| API -> worker | arq ジョブペイロードとして model が伝播 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | model フィールド | accept | model 名は ChatCopilot 経由で Copilot SDK の create_session に渡されるだけ。SDK が不正モデル名をエラーにする。ユーザー入力が SQL/shell に渡ることはない |
| T-29-02 | Information Disclosure | model 名がログに出る | accept | model 名は機密情報ではない。既存のログレベル（info/warning）で十分 |
</threat_model>

<verification>
1. `uv run pytest tests/test_model_override.py -x -q` が全件 pass
2. `uv run pytest tests/ -x -q` が全件 pass（既存テストへの影響なし）
3. `grep -n "model_override" app/orchestrator/agent.py` で 6 箇所以上ヒット
4. `grep -n "model_override" app/jobs/handlers/orchestrator_handler.py` で 3 箇所以上ヒット
</verification>

<success_criteria>
- SuperChat で model を選択すると、全エージェント種別でその model が使われる
- model 未選択（空文字）時は AGENT.md のデフォルト model が使われる
- 通常 Chat モードに影響がない
- 全テストが pass する
</success_criteria>

<output>
After completion, create `.planning/phases/29-user-model-override/29-01-SUMMARY.md`
</output>
