---
phase: 31.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-VALIDATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Phase 30 ディレクトリ配下に 30-VALIDATION.md が新規作成される"
    - "新 30-VALIDATION.md の frontmatter が status: validated / nyquist_compliant: true / wave_0_complete: true / created: 2026-04-20 / validated: 2026-04-20"
    - "Phase 31 VALIDATION.md と同じ 6 セクション（Test Infrastructure / Sampling Rate / Per-Task Verification Map / Wave 0 Requirements / Manual-Only Verifications / Validation Sign-Off）が揃う"
    - "Per-Task Verification Map に Phase 30 の 6 plan 全てが参照される行（計 6 行以上）が入る"
    - "Validation Sign-Off の Approval 行が validated 2026-04-20 (Phase 30 VERIFICATION.md 7/7 PASS を根拠に遡及作成)"
  artifacts:
    - path: ".planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-VALIDATION.md"
      provides: "Phase 30 の VALIDATION.md（遡及作成）"
      contains: "status: validated"
      min_lines: 70
  key_links:
    - from: ".planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/VERIFICATION.md"
      to: ".planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-VALIDATION.md"
      via: "7/7 goals PASS を根拠に validated ステータスで新規作成"
      pattern: "status: validated"
    - from: ".planning/milestones/v5.0-MILESTONE-AUDIT.md"
      to: "Phase 30 の 30-VALIDATION.md"
      via: "missing_phases: [30] の解消"
      pattern: "phase: 30"
---

<objective>
Phase 30（MCP ツールカタログ single-source-of-truth 化）の VALIDATION.md を遡及的に新規作成する。既存 `VERIFICATION.md`（2026-04-18 verified、7/7 goals PASS）を一次根拠として、Phase 31 の VALIDATION.md と同じ構造・フォーマットで書き下ろす。

Phase 30 実行時には VALIDATION.md 運用規律（Phase 26 以降導入、Phase 31 で確立）がまだ適用されておらず artifact 自体が欠損している。v5.0 milestone audit の tech_debt セクション `missing_phases: [30]` に対応し、bookkeeping として `status: validated` で直接遡及作成する（verify ワークフローを再実行する必要はなし — 既に全 test / drift check / 決定論性チェックが VERIFICATION.md で実証済み）。

Purpose: v5.0 milestone を archive 可能な状態にする最後のピースを埋める。
Output: `.planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-VALIDATION.md`（新規、約 70〜90 行）。

本 plan は Plan 01 と独立。`files_modified` が重ならないため Wave 1 で並列実行可能。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v5.0-MILESTONE-AUDIT.md
@.planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/VERIFICATION.md
@.planning/phases/31-agent-mcp-observability/31-VALIDATION.md
</context>

<interfaces>
<!-- Phase 30 の plan 構成（VALIDATION.md の Per-Task Verification Map に写像する） -->

Phase 30 Plans（全 6 plan、wave 構造含む）:
- 30-01-PLAN.md (wave 1): `config/mcp_tools.yaml` 拡張スキーマ定義 + 6 ツールを新スキーマで書き下ろし
- 30-02-PLAN.md (wave 1): `scripts/generate_mcp_artifacts.py` ジェネレータ本体 + `--check` モード
- 30-03-PLAN.md (wave 2): `mcp_helper.py` を自動生成版に、`mcp_helper_utils.py` を手書き基盤として分離
- 30-04-PLAN.md (wave 2): `static/js/tool-catalog-generated.js` 独立 ES module 化、`iframe-rpc.js` は re-export のみ、`sync-tool-list-to-js.py` 削除
- 30-05-PLAN.md (wave 3): `scripts/install-hooks.sh` 拡張 — pre-commit hook に MCP drift 検知追加（ADR INDEX 自動生成と共存）
- 30-06-PLAN.md (wave 2): `docs/mcp-tools.md` / `docs/mcp-tool-add-manual.md` / `.claude/commands/add-mcp-tool.md` / CLAUDE.md / patterns.md の文書・コマンド整備

Phase 30 VERIFICATION.md の 7 Truths（Test Map の Secure Behavior 列に写像可能）:
1. config/mcp_tools.yaml が拡張スキーマで 6 ツール宣言
2. ジェネレータが決定論的で drift なし（`--check` exit 0、byte-identical）
3. ランタイム後方互換（sandbox-style import 4 関数 OK）
4. JS カタログ分離（tool-catalog-generated.js + iframe-rpc.js re-export、sync-tool-list-to-js.py 削除）
5. Docs & 運用ドキュメント完備
6. pre-commit hook が ADR INDEX + MCP drift 両方を持つ
7. テストスイート全体 PASS（新規 51 件 + 隣接回帰 12 passed, 1 skipped）

Phase 30 で実在する test ファイル（コマンドに使う — 確認済み）:
- tests/test_tool_registry.py
- tests/test_generate_mcp_artifacts.py
- tests/test_mcp_helper_generated.py
- tests/test_tool_catalog_js.py
- tests/test_install_hooks.py
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 30-VALIDATION.md を Phase 31 形式で遡及的に新規作成</name>
  <files>.planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-VALIDATION.md</files>
  <read_first>
    必ず以下を `Read` で読んでから Write すること:
    - .planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/VERIFICATION.md（一次根拠 — 7/7 goals PASS の evidence がすべてある）
    - .planning/phases/31-agent-mcp-observability/31-VALIDATION.md（フォーマットリファレンス — 6 セクション構成・frontmatter 順序をこれに合わせる）
    - .planning/milestones/v5.0-MILESTONE-AUDIT.md（missing_phases: [30] の文脈）
    - `tests/test_tool_registry.py` / `tests/test_generate_mcp_artifacts.py` など（ファイル存在のみ確認 — 内容は読まなくて OK）
  </read_first>
  <action>
`.planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-VALIDATION.md` を以下の内容で**完全新規作成**する。Phase 31 の 6 セクション構成に正確に合わせる。

書く内容（Write ツールで一発書き込み）:

```markdown
---
phase: 30
slug: mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-20
validated: 2026-04-20
---

# Phase 30 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> 本ファイルは Phase 30 実行当時 (2026-04-18) には存在せず、v5.0 milestone audit cleanup
> (phase 31.1) で `.planning/phases/30-.../VERIFICATION.md` の 7/7 PASS を根拠として
> 遡及的に `status: validated` で作成したもの。

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest >= 8.0 + pytest-asyncio >= 0.25 (pyproject.toml `[tool.pytest.ini_options]` asyncio_mode=auto) |
| **Config file** | `pyproject.toml` (root) |
| **Quick run command** | `uv run pytest tests/test_tool_registry.py tests/test_generate_mcp_artifacts.py tests/test_mcp_helper_generated.py tests/test_tool_catalog_js.py tests/test_install_hooks.py -x` |
| **Full suite command** | `uv run pytest tests/ -x` |
| **Determinism check** | `python3 scripts/generate_mcp_artifacts.py --check`（pre-commit hook と同一コマンド、exit 0 を要求） |
| **Estimated runtime** | Phase 30 固有 51 件 ~1s / 全体 ~30-60s |

---

## Sampling Rate

- **After every task commit:** `uv run pytest tests/test_tool_registry.py tests/test_generate_mcp_artifacts.py tests/test_mcp_helper_generated.py tests/test_tool_catalog_js.py tests/test_install_hooks.py -x`
- **After every plan wave:** `uv run pytest tests/ -x` + `python3 scripts/generate_mcp_artifacts.py --check`
- **Before `/gsd-verify-work`:** Full suite green + drift check exit 0 + sandbox-style import smoke (`cd mcp_server/tools && python3 -c 'from mcp_helper import search, query_db, get_datetime, ping'`)
- **Max feedback latency:** 5 秒（focused）/ 60 秒（full）

---

## Per-Task Verification Map

> Phase 30 の 6 plan を VERIFICATION.md の 7 Truths に対応させて記録。Phase 30 実行当時は per-task `<automated>` を事前宣言する Nyquist 運用規律が確立していなかったため、実際の verify は Plan 完了後の VERIFICATION.md で事後に行われた。本 map は事後的にトレーサビリティを補完する目的で作成。

| Plan | Wave | Truth (VERIFICATION.md) | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|------|------|--------------------------|-----------------|-----------|-------------------|-------------|--------|
| 30-01 | 1 | Truth 1 | config/mcp_tools.yaml が 6 ツールを拡張スキーマ (python_wrapper / sandbox_exposed / privileged) で宣言 | yaml load | `python3 -c 'import yaml; print(len(yaml.safe_load(open("config/mcp_tools.yaml"))["tools"]))'` == 6 | ✅ | ✅ green |
| 30-01 | 1 | Truth 1 (regression) | ToolRegistry が拡張フィールドを無視しつつ既存契約を維持 | unit | `uv run pytest tests/test_tool_registry.py -x` | ✅ | ✅ green |
| 30-02 | 1 | Truth 2 | ジェネレータ 3 ターゲットが byte-identical で決定論的 | unit | `uv run pytest tests/test_generate_mcp_artifacts.py -x` | ✅ | ✅ green |
| 30-02 | 1 | Truth 2 | drift 検知モード (`--check`) が exit 0 / 非 0 を正しく返す | integration | `python3 scripts/generate_mcp_artifacts.py --check` → exit 0 | ✅ | ✅ green |
| 30-03 | 2 | Truth 3 | 自動生成 mcp_helper.py が手書き utils を import して 4 wrapper を再現 | unit | `uv run pytest tests/test_mcp_helper_generated.py -x` | ✅ | ✅ green |
| 30-03 | 2 | Truth 3 (smoke) | sandbox 互換 import が機能 | smoke | `cd mcp_server/tools && python3 -c 'from mcp_helper import search, query_db, get_datetime, ping'` | ✅ | ✅ green |
| 30-04 | 2 | Truth 4 | tool-catalog-generated.js が AVAILABLE_TOOLS を 6 entry export、privileged: true が 2 件 | unit | `uv run pytest tests/test_tool_catalog_js.py -x` | ✅ | ✅ green |
| 30-04 | 2 | Truth 4 | iframe-rpc.js が re-export、旧 sync-tool-list-to-js.py が削除済み | grep | `grep -q "export { AVAILABLE_TOOLS } from './tool-catalog-generated.js'" static/js/iframe-rpc.js && ! test -f scripts/sync-tool-list-to-js.py` | ✅ | ✅ green |
| 30-05 | 3 | Truth 6 | pre-commit hook が ADR INDEX + MCP drift 両方のブロックを保持 | unit | `uv run pytest tests/test_install_hooks.py -x` | ✅ | ✅ green |
| 30-05 | 3 | Truth 6 | hook 再インストール後 `.git/hooks/pre-commit` に両ブロックが共存 | integration | `bash scripts/install-hooks.sh && grep -cE 'generate_(adr_index|mcp_artifacts)' .git/hooks/pre-commit` >= 2 | ✅ | ✅ green |
| 30-06 | 2 | Truth 5 | docs/mcp-tools.md が自動生成 (DO NOT EDIT ヘッダ付 / 6 ツール詳細) | grep | `head -2 docs/mcp-tools.md \| grep -q 'DO NOT EDIT' && grep -c '^## ' docs/mcp-tools.md` >= 6 | ✅ | ✅ green |
| 30-06 | 2 | Truth 5 | /add-mcp-tool スラッシュコマンド + docs/mcp-tool-add-manual.md + CLAUDE.md セクション + patterns.md 更新 | grep | `test -f .claude/commands/add-mcp-tool.md && test -f docs/mcp-tool-add-manual.md && grep -q '## MCP Tool Catalog (Phase 30)' CLAUDE.md` | ✅ | ✅ green |
| all | — | Truth 7 | Phase 30 新規 51 件 + 隣接回帰 12 passed, 1 skipped | full | `uv run pytest tests/test_tool_registry.py tests/test_generate_mcp_artifacts.py tests/test_mcp_helper_generated.py tests/test_tool_catalog_js.py tests/test_install_hooks.py tests/test_generate_adr_index.py tests/test_mcp_server.py tests/test_subagent_registry_tools.py` | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky — 全行 VERIFICATION.md (2026-04-18) の evidence を根拠に green 固定*

---

## Wave 0 Requirements

- [x] `tests/test_tool_registry.py` — YAML 拡張スキーマを ToolRegistry が無視しつつ既存契約を維持することを保証（30-01）
- [x] `tests/test_generate_mcp_artifacts.py` — ジェネレータ 3 ターゲットの決定論性 + `--check` モード（30-02）
- [x] `tests/test_mcp_helper_generated.py` — 自動生成 mcp_helper.py が 4 wrapper を正しく提供することの回帰テスト（30-03）
- [x] `tests/test_tool_catalog_js.py` — tool-catalog-generated.js のスキーマと privileged フラグ検証（30-04）
- [x] `tests/test_install_hooks.py` — install-hooks.sh の ADR + MCP 両ブロック同居 + 冪等性（30-05）
- [x] `pyproject.toml` への依存追加不要（pytest / pytest-asyncio / PyYAML は既存で充足）

*本 Wave 0 要件はすべて Phase 30 実行中に実ファイルとして追加済み。VERIFICATION.md Truth 7 で「Phase 30 新規 51 件が全 PASS」として実証済み。*

---

## Manual-Only Verifications

| Behavior | Requirement (Truth) | Why Manual | Test Instructions |
|----------|---------------------|------------|-------------------|
| 新規 MCP ツール追加手順 `/add-mcp-tool` の end-to-end 実行 | Truth 5 (運用ドキュメント完備) | スラッシュコマンドは人間インタラクティブ前提、追加後の手動レビューが必要 | 将来新規ツール追加時に `/add-mcp-tool <name>` を実行し、生成される YAML + 3 artifacts + pre-commit 通過を手動確認 |
| Canvas iframe 経由で AVAILABLE_TOOLS が実ランタイムに届くこと | Truth 4 (JS カタログ移行) | iframe postMessage + ブラウザ実行が必要 | `docker compose up` → Canvas アプリ起動 → devtools console で `AVAILABLE_TOOLS` の length が 6 であることを確認 |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies（Per-Task Verification Map に全 plan の automated コマンド記載）
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references（Phase 30 で作成された 5 test file がすべて実在）
- [x] No watch-mode flags
- [x] Feedback latency < 60s（focused 51 件 ~1s、full suite ~30-60s）
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** validated 2026-04-20 (Phase 30 VERIFICATION.md 7/7 PASS を根拠に遡及作成、v5.0 milestone audit cleanup phase 31.1)
```

### 実装上の注意
- `.planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/` ディレクトリは既に存在する。`Write` ツールで直接 `30-VALIDATION.md` を新規作成する。
- frontmatter の `slug:` は Phase 30 ディレクトリ名と一致させる（末尾の `-` もそのまま）。
- 本文中のテーブル表記は **すべて Markdown の生テキスト**として書き込み、レンダ済み HTML にしない。
- Phase 31 の書式と構造的に同じ 6 セクション（Test Infrastructure / Sampling Rate / Per-Task Verification Map / Wave 0 Requirements / Manual-Only Verifications / Validation Sign-Off）を揃える。
- Per-Task Verification Map の行数は 13 行（ヘッダ + セパレータ除いた data row 数）。Phase 30 の 6 plan をカバー。「plan 数 6 以上」という受け入れ条件は余裕で満たす。
  </action>
  <verify>
    <automated>f=.planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-VALIDATION.md; test -f "$f" && echo "exists OK"; grep -c '^phase: 30$' "$f"; grep -c '^status: validated$' "$f"; grep -c '^nyquist_compliant: true$' "$f"; grep -c '^wave_0_complete: true$' "$f"; grep -c '^created: 2026-04-20$' "$f"; grep -c '^validated: 2026-04-20$' "$f"; grep -c '^## Test Infrastructure$' "$f"; grep -c '^## Sampling Rate$' "$f"; grep -c '^## Per-Task Verification Map$' "$f"; grep -c '^## Wave 0 Requirements$' "$f"; grep -c '^## Manual-Only Verifications$' "$f"; grep -c '^## Validation Sign-Off$' "$f"; grep -c '^| 30-0' "$f"; grep -c '^\*\*Approval:\*\* validated 2026-04-20' "$f"; wc -l "$f"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/30-.../30-VALIDATION.md` が成功（`exists OK` 出力）
    - `grep -c '^phase: 30$'` が `1`
    - `grep -c '^status: validated$'` が `1`
    - `grep -c '^nyquist_compliant: true$'` が `1`
    - `grep -c '^wave_0_complete: true$'` が `1`
    - `grep -c '^created: 2026-04-20$'` が `1`
    - `grep -c '^validated: 2026-04-20$'` が `1`
    - `grep -c '^## Test Infrastructure$'` が `1`
    - `grep -c '^## Sampling Rate$'` が `1`
    - `grep -c '^## Per-Task Verification Map$'` が `1`
    - `grep -c '^## Wave 0 Requirements$'` が `1`
    - `grep -c '^## Manual-Only Verifications$'` が `1`
    - `grep -c '^## Validation Sign-Off$'` が `1`
    - `grep -c '^| 30-0'`（Per-Task Verification Map の Phase 30 plan 参照行数）が `6` 以上（6 plan カバー必須）
    - `grep -c '^\*\*Approval:\*\* validated 2026-04-20'` が `1`
    - `wc -l` が `70` 以上（Phase 31 VALIDATION.md と同程度のボリューム）
  </acceptance_criteria>
  <done>
    Phase 30 配下に `30-VALIDATION.md` が存在し、6 セクションが揃い、Per-Task Verification Map で 6 plan すべてがカバーされ、frontmatter が validated 固定されている状態。
  </done>
</task>

<task type="auto">
  <name>Task 2: 新規 30-VALIDATION.md をコミット</name>
  <files>.planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-VALIDATION.md</files>
  <read_first>
    Task 1 の直後に実行する前提のため追加読込は不要。`git status` で新規追加状態を確認する。
  </read_first>
  <action>
手順:
1. `git status .planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/` で `30-VALIDATION.md` が `new file` または `untracked` として検出されることを確認する。既存 VERIFICATION.md / PLAN.md / SUMMARY.md に変更が入っていないことも確認（差分なしが期待値）。
2. `gsd-sdk query commit "docs(phase-31.1): Phase 30 VALIDATION.md を遡及作成 (v5.0 milestone Nyquist missing_phases 解消)" .planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-VALIDATION.md` でコミット。
3. `git log -1 --name-only` で 30-VALIDATION.md が 1 ファイルだけ含まれるコミットになっていることを確認する。
  </action>
  <verify>
    <automated>f=.planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-VALIDATION.md; test -f "$f" && echo "file exists"; git log -1 --name-only | grep -q "$f" && echo "committed OK"; git log -1 --pretty=%s | grep -q '^docs(phase-31.1): Phase 30 VALIDATION.md' && echo "commit message OK"; git status --porcelain | grep -v '^??' | grep "$f" && echo "UNEXPECTED: uncommitted change" || echo "clean"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f` で 30-VALIDATION.md 存在 (`file exists`)
    - `git log -1 --name-only` の 1 行目に 30-VALIDATION.md が含まれる (`committed OK`)
    - 最新 commit のタイトルが `docs(phase-31.1): Phase 30 VALIDATION.md` で始まる (`commit message OK`)
    - `git status --porcelain` に 30-VALIDATION.md の未コミット変更が残っていない (`clean`)
    - `git diff HEAD~1 HEAD --stat` で変更ファイルが 30-VALIDATION.md の 1 件のみ（本 plan は単一ファイル新規追加のため）
  </acceptance_criteria>
  <done>
    `30-VALIDATION.md` が git 管理下に入り、`docs(phase-31.1):` プレフィックス付きで単一ファイルコミットが作成された状態。v5.0 milestone audit の `missing_phases: [30]` が解消。
  </done>
</task>

</tasks>

<verification>
全タスク完了後に以下を連続で確認:

1. `test -f .planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-VALIDATION.md` 成功
2. `grep -c '^## ' .planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-VALIDATION.md` が 6 以上（6 セクション全 present）
3. `grep -c '^| 30-0' .planning/phases/30-mcp-single-source-of-truth-config-mcp-tools-yaml-mcp-helper-/30-VALIDATION.md` が 6 以上（全 plan カバー）
4. Phase 30 の既存ファイル（VERIFICATION.md / 30-0{1..6}-PLAN.md / 30-0{1..6}-SUMMARY.md）が改変されていない（`git log --oneline -- .planning/phases/30-mcp-.../`  の最新 commit が本 plan のものだけ）
5. v5.0 milestone audit 的には `missing_phases: [30]` → 解消、`partial_phases` から 30 は元々入っていないので影響なし（Plan 01 が `partial_phases: [20,21,22,23,24,25,27,28,29]` を解消）
</verification>

<success_criteria>
- `.planning/phases/30-.../30-VALIDATION.md` が新規作成され `status: validated` 固定
- Per-Task Verification Map で Phase 30 の 6 plan 全てがカバーされている
- Phase 31 VALIDATION.md と同じ 6 セクション構成
- 単一ファイル 1 commit でまとまっている
- Phase 30 の既存 artifact に一切変更なし
</success_criteria>

<output>
完了後、`.planning/phases/31.1-v5-milestone-cleanup/31.1-02-SUMMARY.md` を作成する。テンプレートは `$HOME/.claude/get-shit-done/templates/summary.md` に従い、以下を必ず含める:
- 作成した 30-VALIDATION.md の構造サマリ（6 セクション + 13 行 Test Map）
- 一次根拠として Phase 30 VERIFICATION.md (7/7 PASS, 2026-04-18) を参照していること
- Phase 30 既存 artifact への影響がないこと
- v5.0 milestone audit `missing_phases: [30]` が本 plan で解消されたこと
</output>
