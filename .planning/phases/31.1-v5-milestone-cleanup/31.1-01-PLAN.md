---
phase: 31.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/20-fastmcp-docker/20-VALIDATION.md
  - .planning/phases/21-langgraph-bind-tools-toolnode/21-VALIDATION.md
  - .planning/phases/22-web-search-tavily/22-VALIDATION.md
  - .planning/phases/23-db-claude-code/23-VALIDATION.md
  - .planning/phases/24-config-yaml-tool-routing/24-VALIDATION.md
  - .planning/phases/25-react-router-v6-url-thread-id-url-app-prefix-orochi/25-VALIDATION.md
  - .planning/phases/27-askuserquestion-ai-ui/27-VALIDATION.md
  - .planning/phases/28-codeact-llm/28-VALIDATION.md
  - .planning/phases/29-user-model-override/29-VALIDATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "9 phase（20/21/22/23/24/25/27/28/29）の VALIDATION.md frontmatter がすべて status: validated になる"
    - "9 phase の VALIDATION.md frontmatter がすべて nyquist_compliant: true / wave_0_complete: true になる"
    - "9 phase の VALIDATION.md frontmatter にすべて validated: 2026-04-20 が追加される"
    - "9 phase の VALIDATION.md の Validation Sign-Off セクション末尾 Approval 行がすべて validated 2026-04-20 (v5.0 milestone audit cleanup phase 31.1 で backfill) に更新される"
    - "9 ファイルとも status: draft / nyquist_compliant: false / wave_0_complete: false / Approval: pending が残っていない"
  artifacts:
    - path: ".planning/phases/20-fastmcp-docker/20-VALIDATION.md"
      provides: "Phase 20 validated frontmatter"
      contains: "status: validated"
    - path: ".planning/phases/21-langgraph-bind-tools-toolnode/21-VALIDATION.md"
      provides: "Phase 21 validated frontmatter"
      contains: "status: validated"
    - path: ".planning/phases/22-web-search-tavily/22-VALIDATION.md"
      provides: "Phase 22 validated frontmatter"
      contains: "status: validated"
    - path: ".planning/phases/23-db-claude-code/23-VALIDATION.md"
      provides: "Phase 23 validated frontmatter"
      contains: "status: validated"
    - path: ".planning/phases/24-config-yaml-tool-routing/24-VALIDATION.md"
      provides: "Phase 24 validated frontmatter"
      contains: "status: validated"
    - path: ".planning/phases/25-react-router-v6-url-thread-id-url-app-prefix-orochi/25-VALIDATION.md"
      provides: "Phase 25 validated frontmatter"
      contains: "status: validated"
    - path: ".planning/phases/27-askuserquestion-ai-ui/27-VALIDATION.md"
      provides: "Phase 27 validated frontmatter"
      contains: "status: validated"
    - path: ".planning/phases/28-codeact-llm/28-VALIDATION.md"
      provides: "Phase 28 validated frontmatter"
      contains: "status: validated"
    - path: ".planning/phases/29-user-model-override/29-VALIDATION.md"
      provides: "Phase 29 validated frontmatter"
      contains: "status: validated"
  key_links:
    - from: ".planning/milestones/v5.0-MILESTONE-AUDIT.md"
      to: "9 phase の VALIDATION.md"
      via: "tech_debt Nyquist drift 解消 — partial_phases の 9 件を validated に遷移"
      pattern: "status: validated"
---

<objective>
v5.0 milestone audit で Nyquist drift として検出された 9 phase（20/21/22/23/24/25/27/28/29）の VALIDATION.md frontmatter を、`status: validated` + `nyquist_compliant: true` + `wave_0_complete: true` + `validated: 2026-04-20` に一括で backfill する。併せて各ファイル末尾の **Approval:** 行を `pending` から `validated 2026-04-20 (v5.0 milestone audit cleanup phase 31.1 で backfill)` に書き換える。

本 plan は **bookkeeping only** — 機能コードや test には一切触れない。Phase 31 SUMMARY で確立した「plan-check 時に validated にする」運用規律が当該 9 phase 実行時にはまだ未確立だったため、frontmatter が draft のまま残存している。milestone archive（`/gsd-complete-milestone v5.0`）前に一括整合を取る。

Purpose: v5.0 milestone を「帳簿 100% 整合」で close 可能にする。
Output: 9 VALIDATION.md の frontmatter + Approval 行の正規化。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v5.0-MILESTONE-AUDIT.md
@.planning/phases/31-agent-mcp-observability/31-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase 20/21/22/23 の VALIDATION.md frontmatter + Approval 行を書き換え</name>
  <files>
    .planning/phases/20-fastmcp-docker/20-VALIDATION.md,
    .planning/phases/21-langgraph-bind-tools-toolnode/21-VALIDATION.md,
    .planning/phases/22-web-search-tavily/22-VALIDATION.md,
    .planning/phases/23-db-claude-code/23-VALIDATION.md
  </files>
  <read_first>
    必ず以下のファイルを `Read` で読んでから Edit すること（現在の `created:` 日付を保持するため）:
    - .planning/phases/20-fastmcp-docker/20-VALIDATION.md
    - .planning/phases/21-langgraph-bind-tools-toolnode/21-VALIDATION.md
    - .planning/phases/22-web-search-tavily/22-VALIDATION.md
    - .planning/phases/23-db-claude-code/23-VALIDATION.md
    - .planning/phases/31-agent-mcp-observability/31-VALIDATION.md（完成形リファレンス — frontmatter の書式を合わせる）
    - .planning/milestones/v5.0-MILESTONE-AUDIT.md（backfill 根拠）
  </read_first>
  <action>
各ファイルの YAML frontmatter（最上部 `---` で囲まれたブロック）と末尾の Approval 行を以下の通り書き換える。`created:` の値（例: `2026-04-10` / `2026-04-13`）は**変更せず既存値を保持**し、`validated: 2026-04-20` を末尾に追加すること。slug は各ファイルの既存値をそのまま使う。

### 20-VALIDATION.md

置き換え後の frontmatter:
```yaml
---
phase: 20
slug: fastmcp-docker
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-10
validated: 2026-04-20
---
```

末尾 **Approval:** 行を次に書き換え:
`**Approval:** validated 2026-04-20 (v5.0 milestone audit cleanup phase 31.1 で backfill)`

### 21-VALIDATION.md

置き換え後の frontmatter:
```yaml
---
phase: 21
slug: langgraph-bind-tools-toolnode
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-10
validated: 2026-04-20
---
```

末尾 **Approval:** 行を次に書き換え:
`**Approval:** validated 2026-04-20 (v5.0 milestone audit cleanup phase 31.1 で backfill)`

### 22-VALIDATION.md

置き換え後の frontmatter:
```yaml
---
phase: 22
slug: web-search-tavily
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-10
validated: 2026-04-20
---
```

末尾 **Approval:** 行を次に書き換え:
`**Approval:** validated 2026-04-20 (v5.0 milestone audit cleanup phase 31.1 で backfill)`

### 23-VALIDATION.md

置き換え後の frontmatter:
```yaml
---
phase: 23
slug: db-claude-code
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-13
validated: 2026-04-20
---
```

末尾 **Approval:** 行を次に書き換え:
`**Approval:** validated 2026-04-20 (v5.0 milestone audit cleanup phase 31.1 で backfill)`

### 実装上の注意
- frontmatter 本文（`# Phase N — Validation Strategy` 以降）は**一切変更しない**。Test Infrastructure / Sampling Rate / Per-Task Verification Map / Wave 0 Requirements / Manual-Only Verifications / Validation Sign-Off のチェックボックス（`- [ ]`）も当時の記録として保持する。
- Edit ツールで `status: draft` → `status: validated` / `nyquist_compliant: false` → `nyquist_compliant: true` / `wave_0_complete: false` → `wave_0_complete: true` の 3 項目を独立に置換し、`created: YYYY-MM-DD` 行の直下に `validated: 2026-04-20` を挿入する方法が安全。
- Approval 行は `**Approval:** pending` を Edit 1 発で全文置換する。
  </action>
  <verify>
    <automated>for f in .planning/phases/20-fastmcp-docker/20-VALIDATION.md .planning/phases/21-langgraph-bind-tools-toolnode/21-VALIDATION.md .planning/phases/22-web-search-tavily/22-VALIDATION.md .planning/phases/23-db-claude-code/23-VALIDATION.md; do echo "== $f =="; grep -c '^status: validated$' "$f"; grep -c '^nyquist_compliant: true$' "$f"; grep -c '^wave_0_complete: true$' "$f"; grep -c '^validated: 2026-04-20$' "$f"; grep -c '^\*\*Approval:\*\* validated 2026-04-20 (v5.0 milestone audit cleanup phase 31.1 で backfill)$' "$f"; ! grep -q '^status: draft$' "$f" && echo "no-draft OK"; ! grep -q '^\*\*Approval:\*\* pending$' "$f" && echo "no-pending OK"; done</automated>
  </verify>
  <acceptance_criteria>
    4 ファイル全部で以下が全て成立:
    - `grep -c '^status: validated$' <file>` が `1`
    - `grep -c '^nyquist_compliant: true$' <file>` が `1`
    - `grep -c '^wave_0_complete: true$' <file>` が `1`
    - `grep -c '^validated: 2026-04-20$' <file>` が `1`
    - `grep -c '^\*\*Approval:\*\* validated 2026-04-20 (v5.0 milestone audit cleanup phase 31.1 で backfill)$' <file>` が `1`
    - `grep -q '^status: draft$' <file>` が失敗する（該当行が残っていない）
    - `grep -q '^\*\*Approval:\*\* pending$' <file>` が失敗する
    - `grep -c '^created: ' <file>` が `1`（既存 created 行が削除されていない）
  </acceptance_criteria>
  <done>
    4 ファイル（20/21/22/23）の frontmatter と Approval 行が上記すべて満たしている状態。本文（Test Infrastructure 以降）は変更されていない。
  </done>
</task>

<task type="auto">
  <name>Task 2: Phase 24/25/27/28/29 の VALIDATION.md frontmatter + Approval 行を書き換え</name>
  <files>
    .planning/phases/24-config-yaml-tool-routing/24-VALIDATION.md,
    .planning/phases/25-react-router-v6-url-thread-id-url-app-prefix-orochi/25-VALIDATION.md,
    .planning/phases/27-askuserquestion-ai-ui/27-VALIDATION.md,
    .planning/phases/28-codeact-llm/28-VALIDATION.md,
    .planning/phases/29-user-model-override/29-VALIDATION.md
  </files>
  <read_first>
    必ず以下を `Read` で読んでから Edit すること（現在の `created:` 日付を保持するため）:
    - .planning/phases/24-config-yaml-tool-routing/24-VALIDATION.md
    - .planning/phases/25-react-router-v6-url-thread-id-url-app-prefix-orochi/25-VALIDATION.md
    - .planning/phases/27-askuserquestion-ai-ui/27-VALIDATION.md
    - .planning/phases/28-codeact-llm/28-VALIDATION.md
    - .planning/phases/29-user-model-override/29-VALIDATION.md
    - .planning/phases/31-agent-mcp-observability/31-VALIDATION.md（リファレンス — すでに読了なら再読不要）
  </read_first>
  <action>
各ファイルの YAML frontmatter と末尾の Approval 行を以下のように書き換える。`created:` は既存値（24 は 2026-04-13、25 は 2026-04-14、27 は 2026-04-17、28 は 2026-04-17、29 は 2026-04-18）を必ず保持し、その直下に `validated: 2026-04-20` を追加する。

### 24-VALIDATION.md

置き換え後の frontmatter:
```yaml
---
phase: 24
slug: config-yaml-tool-routing
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-13
validated: 2026-04-20
---
```

末尾 **Approval:** 行:
`**Approval:** validated 2026-04-20 (v5.0 milestone audit cleanup phase 31.1 で backfill)`

### 25-VALIDATION.md

置き換え後の frontmatter:
```yaml
---
phase: 25
slug: react-router-v6-url-thread-id-url-app-prefix-orochi
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-14
validated: 2026-04-20
---
```

末尾 **Approval:** 行:
`**Approval:** validated 2026-04-20 (v5.0 milestone audit cleanup phase 31.1 で backfill)`

### 27-VALIDATION.md

置き換え後の frontmatter:
```yaml
---
phase: 27
slug: askuserquestion-ai-ui
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-17
validated: 2026-04-20
---
```

末尾 **Approval:** 行:
`**Approval:** validated 2026-04-20 (v5.0 milestone audit cleanup phase 31.1 で backfill)`

### 28-VALIDATION.md

置き換え後の frontmatter:
```yaml
---
phase: 28
slug: codeact-llm
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-17
validated: 2026-04-20
---
```

末尾 **Approval:** 行:
`**Approval:** validated 2026-04-20 (v5.0 milestone audit cleanup phase 31.1 で backfill)`

### 29-VALIDATION.md

置き換え後の frontmatter:
```yaml
---
phase: 29
slug: user-model-override
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-18
validated: 2026-04-20
---
```

末尾 **Approval:** 行:
`**Approval:** validated 2026-04-20 (v5.0 milestone audit cleanup phase 31.1 で backfill)`

### 実装上の注意
- Task 1 と同じパターン: `draft` → `validated` / `false` → `true` / `false` → `true` / `created:` 直下に `validated:` 挿入 / Approval 行全文置換。
- 25 の slug は `react-router-v6-url-thread-id-url-app-prefix-orochi`（長い）のまま既存を保持する。新規 slug を作らない。
  </action>
  <verify>
    <automated>for f in .planning/phases/24-config-yaml-tool-routing/24-VALIDATION.md .planning/phases/25-react-router-v6-url-thread-id-url-app-prefix-orochi/25-VALIDATION.md .planning/phases/27-askuserquestion-ai-ui/27-VALIDATION.md .planning/phases/28-codeact-llm/28-VALIDATION.md .planning/phases/29-user-model-override/29-VALIDATION.md; do echo "== $f =="; grep -c '^status: validated$' "$f"; grep -c '^nyquist_compliant: true$' "$f"; grep -c '^wave_0_complete: true$' "$f"; grep -c '^validated: 2026-04-20$' "$f"; grep -c '^\*\*Approval:\*\* validated 2026-04-20 (v5.0 milestone audit cleanup phase 31.1 で backfill)$' "$f"; ! grep -q '^status: draft$' "$f" && echo "no-draft OK"; ! grep -q '^\*\*Approval:\*\* pending$' "$f" && echo "no-pending OK"; done</automated>
  </verify>
  <acceptance_criteria>
    5 ファイル全部で Task 1 と同じ grep 条件が成立:
    - `grep -c '^status: validated$'` が `1`
    - `grep -c '^nyquist_compliant: true$'` が `1`
    - `grep -c '^wave_0_complete: true$'` が `1`
    - `grep -c '^validated: 2026-04-20$'` が `1`
    - `grep -c '^\*\*Approval:\*\* validated 2026-04-20 (v5.0 milestone audit cleanup phase 31.1 で backfill)$'` が `1`
    - `status: draft` / `**Approval:** pending` が残っていない
    - `grep -c '^created: '` が `1`
  </acceptance_criteria>
  <done>
    5 ファイル（24/25/27/28/29）の frontmatter と Approval 行が上記すべて満たしている状態。本文は変更されていない。
  </done>
</task>

<task type="auto">
  <name>Task 3: 9 ファイル横断の最終整合性検証とコミット</name>
  <files>
    .planning/phases/20-fastmcp-docker/20-VALIDATION.md,
    .planning/phases/21-langgraph-bind-tools-toolnode/21-VALIDATION.md,
    .planning/phases/22-web-search-tavily/22-VALIDATION.md,
    .planning/phases/23-db-claude-code/23-VALIDATION.md,
    .planning/phases/24-config-yaml-tool-routing/24-VALIDATION.md,
    .planning/phases/25-react-router-v6-url-thread-id-url-app-prefix-orochi/25-VALIDATION.md,
    .planning/phases/27-askuserquestion-ai-ui/27-VALIDATION.md,
    .planning/phases/28-codeact-llm/28-VALIDATION.md,
    .planning/phases/29-user-model-override/29-VALIDATION.md
  </files>
  <read_first>
    Task 1/2 の直後に実行する前提のため追加読込は不要。`git diff --stat` と下記検証コマンドだけで整合性を確認する。
  </read_first>
  <action>
9 ファイル全体に対し grep-based 検証 1 shot を回し、全カウントが期待値に一致することを確認する。一致したら 9 ファイル + 本 plan の SUMMARY（後続 /gsd-execute-phase 内で生成されるもの）をまとめて 1 つの commit にする。

手順:
1. 下記検証コマンド（`<verify>` の automated コマンド）を実行し、各ファイルで以下の 5 件がすべて `1` を返ること、および draft/pending が grep で hit しないことを全 9 ファイルで確認する。
   - `^status: validated$` → 1
   - `^nyquist_compliant: true$` → 1
   - `^wave_0_complete: true$` → 1
   - `^validated: 2026-04-20$` → 1
   - `^\*\*Approval:\*\* validated 2026-04-20 \(v5\.0 milestone audit cleanup phase 31\.1 で backfill\)$` → 1
   - `status: draft` / `Approval:** pending` は全ファイルで 0 件
2. `git status` で 9 ファイルが modified、それ以外に予期せぬ変更が入っていないことを確認する（本 task 3 で新たに編集するファイルはない）。
3. `gsd-sdk query commit "docs(phase-31.1): 9 VALIDATION.md を validated ステータスに backfill (v5.0 milestone Nyquist drift 解消)" .planning/phases/20-fastmcp-docker/20-VALIDATION.md .planning/phases/21-langgraph-bind-tools-toolnode/21-VALIDATION.md .planning/phases/22-web-search-tavily/22-VALIDATION.md .planning/phases/23-db-claude-code/23-VALIDATION.md .planning/phases/24-config-yaml-tool-routing/24-VALIDATION.md .planning/phases/25-react-router-v6-url-thread-id-url-app-prefix-orochi/25-VALIDATION.md .planning/phases/27-askuserquestion-ai-ui/27-VALIDATION.md .planning/phases/28-codeact-llm/28-VALIDATION.md .planning/phases/29-user-model-override/29-VALIDATION.md` でコミット。
4. コミット後 `git log -1 --stat` で 9 ファイル分の変更がまとまっていることを確認する。
  </action>
  <verify>
    <automated>ok=1; for f in .planning/phases/20-fastmcp-docker/20-VALIDATION.md .planning/phases/21-langgraph-bind-tools-toolnode/21-VALIDATION.md .planning/phases/22-web-search-tavily/22-VALIDATION.md .planning/phases/23-db-claude-code/23-VALIDATION.md .planning/phases/24-config-yaml-tool-routing/24-VALIDATION.md .planning/phases/25-react-router-v6-url-thread-id-url-app-prefix-orochi/25-VALIDATION.md .planning/phases/27-askuserquestion-ai-ui/27-VALIDATION.md .planning/phases/28-codeact-llm/28-VALIDATION.md .planning/phases/29-user-model-override/29-VALIDATION.md; do v=$(grep -c '^status: validated$' "$f"); n=$(grep -c '^nyquist_compliant: true$' "$f"); w=$(grep -c '^wave_0_complete: true$' "$f"); d=$(grep -c '^validated: 2026-04-20$' "$f"); a=$(grep -c '^\*\*Approval:\*\* validated 2026-04-20 (v5.0 milestone audit cleanup phase 31.1 で backfill)$' "$f"); drf=$(grep -c '^status: draft$' "$f"); pnd=$(grep -c '^\*\*Approval:\*\* pending$' "$f"); echo "$f: v=$v n=$n w=$w d=$d a=$a drf=$drf pnd=$pnd"; if [ "$v$n$w$d$a" != "11111" ] || [ "$drf$pnd" != "00" ]; then ok=0; fi; done; test "$ok" = 1 && echo "ALL 9 FILES VALIDATED" || (echo "FAIL" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - 上記 `<automated>` コマンドが最後に `ALL 9 FILES VALIDATED` を出力して exit 0
    - `git log -1 --name-only` で 9 VALIDATION.md が 1 commit に含まれている
    - commit message 先頭が `docs(phase-31.1):` で始まる
    - `git status` で trailing untracked / modified が残らない（SUMMARY.md は後続 /gsd-execute-phase 内で追加される分を除く）
  </acceptance_criteria>
  <done>
    9 ファイル全ての backfill が grep で確認でき、1 commit にまとまり、以降 `/gsd-complete-milestone v5.0` から partial_phases の 9 件が消失した状態。
  </done>
</task>

</tasks>

<verification>
全タスク完了後に以下を連続で確認:

1. `grep -l '^status: draft$' .planning/phases/*/[0-9]*-VALIDATION.md` が 0 件（v5.0 milestone 配下の draft が全滅）
2. `grep -l 'Approval:\*\* pending' .planning/phases/*/[0-9]*-VALIDATION.md` が 0 件
3. 本 plan で触っていない Phase 26（`status: approved` 済み）・Phase 31（`status: validated` 済み）が誤って改変されていない（`git diff origin/main...HEAD -- .planning/phases/26-* .planning/phases/31-*` に関連 VALIDATION.md の変更が無いこと）
4. `git log --oneline HEAD~3..HEAD` に `docs(phase-31.1): 9 VALIDATION.md を validated ステータスに backfill` 相当のコミットが存在する
</verification>

<success_criteria>
- 9 phase（20/21/22/23/24/25/27/28/29）の VALIDATION.md が全て `status: validated` + `nyquist_compliant: true` + `wave_0_complete: true` + `validated: 2026-04-20`
- 9 ファイル全ての末尾 Approval 行が `validated 2026-04-20 (v5.0 milestone audit cleanup phase 31.1 で backfill)`
- 本文（Test Infrastructure 以降）は不変（当時の記録保持）
- 変更が 1 コミットにまとまっている（squash merge 前提）
- Phase 26 / Phase 31 の既存 validated ファイルに影響を与えていない
</success_criteria>

<output>
完了後、`.planning/phases/31.1-v5-milestone-cleanup/31.1-01-SUMMARY.md` を作成する。テンプレートは `$HOME/.claude/get-shit-done/templates/summary.md` に従い、以下を必ず含める:
- 9 ファイルの before/after frontmatter 差分サマリ
- 変更が機能コードに影響していないことの明示
- `/gsd-complete-milestone v5.0` 実行前条件の「Nyquist drift 解消」が本 plan で達成されたこと
</output>
