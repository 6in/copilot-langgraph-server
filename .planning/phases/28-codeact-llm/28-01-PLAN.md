---
phase: 28-codeact-llm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp_server/tools/execute_python.py
  - config/sandbox_allowlist.yaml
  - mcp_server/server.py
  - config/mcp_tools.yaml
  - tests/test_mcp_server.py
autonomous: true
requirements:
  - EXEC-01
  - EXEC-02
  - EXEC-03
  - EXEC-04
  - EXEC-05
  - EXEC-06
must_haves:
  truths:
    - "execute_python MCP ツールが Python コードを受け取り stdout/stderr/exit_code を返す"
    - "ホワイトリスト外の import が AST チェックでブロックされる"
    - "env サニタイズにより DATABASE_URL 等がサブプロセスに渡らない"
    - "60 秒タイムアウトでプロセスが SIGTERM→SIGKILL で終了する"
    - "sandbox_allowlist.yaml の allowed_modules リストでインポート許可を制御できる"
  artifacts:
    - path: "mcp_server/tools/execute_python.py"
      provides: "Python サンドボックス実行 MCP ツール"
      exports: ["execute_python", "register_tools"]
    - path: "config/sandbox_allowlist.yaml"
      provides: "インポートホワイトリスト設定"
      contains: "allowed_modules"
    - path: "tests/test_mcp_server.py"
      provides: "execute_python テストケース群"
      contains: "test_execute_python"
  key_links:
    - from: "mcp_server/server.py"
      to: "mcp_server/tools/execute_python.py"
      via: "register_execute_python_tools(mcp)"
      pattern: "register_execute_python_tools"
    - from: "mcp_server/tools/execute_python.py"
      to: "config/sandbox_allowlist.yaml"
      via: "yaml.safe_load for allowed_modules"
      pattern: "_load_allowlist"
    - from: "config/mcp_tools.yaml"
      to: "mcp_server/tools/execute_python.py"
      via: "ToolRegistry validation"
      pattern: "execute_python"
---

<objective>
execute_python MCP ツールの実装と MCP サーバーへの登録。claude_code.py のサブプロセス実行パターンを踏襲し、AST インポートホワイトリスト + メモリ制限 + タイムアウトによるサンドボックスを構築する。

Purpose: CodeAct パターンの基盤となる Python コード実行機能を MCP ツールとして提供する（D-01, D-04）
Output: execute_python MCP ツール + sandbox_allowlist.yaml + テスト群
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-codeact-llm/28-CONTEXT.md
@.planning/phases/28-codeact-llm/28-RESEARCH.md
@.planning/phases/28-codeact-llm/28-PATTERNS.md

<interfaces>
<!-- mcp_server/tools/claude_code.py — 踏襲するサブプロセス実行パターン -->
From mcp_server/tools/claude_code.py:
```python
ALLOWED_ENV_KEYS: frozenset[str] = frozenset({"PATH", "HOME", "LANG", "LC_ALL", "TERM"})
TIMEOUT_SECS: int = 60
SIGTERM_GRACE_SECS: int = 5
MAX_INLINE_CHARS: int = 4000

async def claude_code(prompt: str, cwd: str = "/tmp") -> dict:
    # Returns: {"output": str, "exit_code": int, "truncated": bool, "file_path": str | None}

def register_tools(mcp: "FastMCP") -> None:
    mcp.tool(claude_code)
```

From mcp_server/server.py:
```python
from tools.claude_code import register_tools as register_claude_code_tools
# ...
register_claude_code_tools(mcp)
```

From config/mcp_tools.yaml:
```yaml
tools:
  - name: claude_code
    description: Claude Code CLI をサブプロセスとして実行
    privileged: true
  - name: get_current_datetime
    description: 現在の日時を JST で返す（日付・時刻・曜日）
```

From tests/test_mcp_server.py:
```python
EXPECTED_TOOLS = {"ping", "web_search", "db_query", "claude_code"}
# NOTE: get_current_datetime が漏れている既存不整合 — 本プランで修正する

cases = {
    "web_search": "query",
    "db_query": "sql",
    "claude_code": "prompt",
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: execute_python MCP ツール + sandbox_allowlist.yaml 作成</name>
  <files>mcp_server/tools/execute_python.py, config/sandbox_allowlist.yaml</files>
  <read_first>
    - mcp_server/tools/claude_code.py (サブプロセス実行パターンの参照実装)
    - mcp_server/server.py (ツール登録パターン)
    - config/mcp_tools.yaml (既存ツールカタログ)
  </read_first>
  <action>
1. `config/sandbox_allowlist.yaml` を新規作成する。内容:
```yaml
# Phase 28 (D-11): execute_python ツールで許可するトップレベルモジュール名一覧
# 変更後はコンテナ再起動で反映される
allowed_modules:
  # 標準ライブラリ — 安全な計算・データ処理系
  - math
  - statistics
  - itertools
  - functools
  - collections
  - datetime
  - json
  - re
  - string
  - random
  - pathlib
  - io
  - base64
  - hashlib
  - typing
  - dataclasses
  - enum
  - abc
  - decimal
  - fractions
  - operator
  - textwrap
  - csv
  - pprint
  - copy
  - time
  - calendar
  - struct
  - array
  - heapq
  - bisect
  - unicodedata
```

2. `mcp_server/tools/execute_python.py` を新規作成する。claude_code.py パターンを踏襲（D-01）:

```python
"""Python サンドボックス実行ツール (Phase 28).

D-01: worker コンテナ内サブプロセスで python3 -c を実行
D-02: タイムアウト + メモリ制限 + 一時ディレクトリでファイルI/O制限
D-03: 60 秒タイムアウト（claude_code.py と同じ）
D-06: stdout + stderr + exit_code を返す
D-09: Python のみ対応
D-10: AST 解析によるインポートホワイトリストチェック
D-11: config/sandbox_allowlist.yaml で許可モジュール管理
"""
from __future__ import annotations

import ast
import asyncio
import os
import resource
from typing import TYPE_CHECKING

import yaml

if TYPE_CHECKING:
    from fastmcp import FastMCP

# D-08 踏襲: 許可リスト env キー — これ以外はサブプロセスに渡さない
ALLOWED_ENV_KEYS: frozenset[str] = frozenset({"PATH", "HOME", "LANG", "LC_ALL", "TERM"})

# D-03: サブプロセスの最大実行時間 (秒)
TIMEOUT_SECS: int = 60

# claude_code.py 踏襲: SIGTERM 後の猶予時間 (秒) — 超過すると SIGKILL に昇格
SIGTERM_GRACE_SECS: int = 5

# D-06 踏襲: インラインで返す最大文字数
MAX_INLINE_CHARS: int = 4000

# D-02: メモリ上限 512MB
MEMORY_LIMIT_BYTES: int = 512 * 1024 * 1024

# D-11: ホワイトリスト設定ファイルパス（環境変数で上書き可能）
ALLOWLIST_PATH: str = os.environ.get(
    "SANDBOX_ALLOWLIST", "/mcp_server/config/sandbox_allowlist.yaml"
)

# キャッシュ — プロセス起動時に一度だけ読み込む
_cached_allowlist: frozenset[str] | None = None


def _load_allowlist(config_path: str | None = None) -> frozenset[str]:
    """YAML からインポートホワイトリストを読み込む（キャッシュあり）。"""
    global _cached_allowlist
    if _cached_allowlist is not None:
        return _cached_allowlist
    path = config_path or ALLOWLIST_PATH
    try:
        with open(path) as f:
            cfg = yaml.safe_load(f) or {}
        _cached_allowlist = frozenset(cfg.get("allowed_modules", []))
    except FileNotFoundError:
        _cached_allowlist = frozenset()
    return _cached_allowlist


def _check_imports(code: str, allowed: frozenset[str]) -> list[str]:
    """AST 解析でインポートされるモジュール名を抽出し、ホワイトリスト外のものを返す。

    SyntaxError は ValueError として re-raise する。
    """
    try:
        tree = ast.parse(code)
    except SyntaxError as e:
        raise ValueError(f"SyntaxError: {e}") from e
    violations = []
    for node in ast.walk(tree):
        if isinstance(node, ast.Import):
            for alias in node.names:
                top = alias.name.split(".")[0]
                if top not in allowed:
                    violations.append(alias.name)
        elif isinstance(node, ast.ImportFrom):
            if node.module:
                top = node.module.split(".")[0]
                if top not in allowed:
                    violations.append(node.module)
    return violations


def _set_limits():
    """preexec_fn — 子プロセスの仮想メモリ上限を設定する (D-02)。

    注意: 純粋な同期関数のみ使用すること (Pitfall 1)。
    """
    resource.setrlimit(resource.RLIMIT_AS, (MEMORY_LIMIT_BYTES, MEMORY_LIMIT_BYTES))


async def execute_python(code: str, timeout: int = 60) -> dict:
    """Python コードをサンドボックス実行する (D-01, D-09)。

    Args:
        code: 実行する Python コード文字列
        timeout: タイムアウト秒数（デフォルト 60、D-03）

    Returns:
        {"stdout": str, "stderr": str, "exit_code": int, "truncated": bool}
        エラー時は "error" キーを追加
    """
    # 1. AST インポートホワイトリストチェック (D-10)
    allowlist = _load_allowlist()
    try:
        violations = _check_imports(code, allowlist)
    except ValueError as e:
        return {
            "stdout": "",
            "stderr": str(e),
            "exit_code": 1,
            "truncated": False,
            "error": str(e),
        }
    if violations:
        msg = f"Blocked imports: {violations}"
        return {
            "stdout": "",
            "stderr": "",
            "exit_code": 1,
            "truncated": False,
            "error": msg,
        }

    # 2. env サニタイズ (claude_code.py D-08 踏襲)
    sanitized_env = {k: v for k, v in os.environ.items() if k in ALLOWED_ENV_KEYS}

    # 3. サブプロセス実行 (D-01, D-02)
    try:
        proc = await asyncio.create_subprocess_exec(
            "python3", "-c", code,
            stdout=asyncio.subprocess.PIPE,
            stderr=asyncio.subprocess.PIPE,
            cwd="/tmp",
            env=sanitized_env,
            preexec_fn=_set_limits,
        )
    except Exception as e:
        return {
            "stdout": "",
            "stderr": "",
            "exit_code": -1,
            "truncated": False,
            "error": f"spawn failed: {e}",
        }

    # 4. タイムアウト + SIGTERM→SIGKILL エスカレーション (D-03)
    try:
        stdout_b, stderr_b = await asyncio.wait_for(
            proc.communicate(), timeout=timeout
        )
        exit_code = proc.returncode if proc.returncode is not None else 0
    except asyncio.TimeoutError:
        proc.terminate()
        try:
            await asyncio.wait_for(proc.wait(), timeout=SIGTERM_GRACE_SECS)
        except asyncio.TimeoutError:
            proc.kill()
            await proc.wait()
        return {
            "stdout": "",
            "stderr": "",
            "exit_code": -1,
            "truncated": False,
            "error": f"Timeout after {timeout}s",
        }

    stdout = stdout_b.decode(errors="replace")
    stderr = stderr_b.decode(errors="replace")

    # 5. 出力切り捨て (D-06 踏襲)
    truncated = len(stdout) > MAX_INLINE_CHARS
    return {
        "stdout": stdout[:MAX_INLINE_CHARS],
        "stderr": stderr[:500] if stderr else "",
        "exit_code": exit_code,
        "truncated": truncated,
    }


def register_tools(mcp: "FastMCP") -> None:
    """Register execute_python tool on the given FastMCP instance."""
    mcp.tool(execute_python)
```

注意点:
- `preexec_fn=_set_limits` で子プロセスにメモリ上限を適用（Pitfall 1: asyncio を参照しない純粋同期関数）
- `cwd="/tmp"` 固定でファイルI/O を一時ディレクトリに制限（D-02）
- `_cached_allowlist` でプロセス起動時に一度だけ YAML を読み込む（毎回のファイルI/O を回避）
- stderr は 500 文字に切り捨て（stdout が主要出力、stderr はデバッグ情報）
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && python3 -c "import yaml; d = yaml.safe_load(open('config/sandbox_allowlist.yaml')); assert 'allowed_modules' in d; assert 'math' in d['allowed_modules']; print('sandbox_allowlist.yaml OK')"</automated>
  </verify>
  <acceptance_criteria>
    - mcp_server/tools/execute_python.py が存在し `async def execute_python(code: str, timeout: int = 60) -> dict:` を含む
    - mcp_server/tools/execute_python.py が `def register_tools(mcp: "FastMCP") -> None:` を含む
    - mcp_server/tools/execute_python.py が `def _check_imports(code: str, allowed: frozenset[str]) -> list[str]:` を含む
    - mcp_server/tools/execute_python.py が `def _set_limits():` を含む
    - mcp_server/tools/execute_python.py が `MEMORY_LIMIT_BYTES: int = 512 * 1024 * 1024` を含む
    - mcp_server/tools/execute_python.py が `preexec_fn=_set_limits` を含む
    - mcp_server/tools/execute_python.py が `"python3", "-c", code` を含む
    - config/sandbox_allowlist.yaml が存在し `allowed_modules:` リストに `math`, `json`, `datetime`, `re` を含む
    - config/sandbox_allowlist.yaml が `os`, `subprocess`, `sys`, `shutil` を含まない
  </acceptance_criteria>
  <done>execute_python.py と sandbox_allowlist.yaml が作成され、AST ホワイトリスト + env サニタイズ + メモリ制限 + タイムアウトの全安全機構が実装されている</done>
</task>

<task type="auto">
  <name>Task 2: MCP サーバー登録 + mcp_tools.yaml 更新 + テスト追加</name>
  <files>mcp_server/server.py, config/mcp_tools.yaml, tests/test_mcp_server.py</files>
  <read_first>
    - mcp_server/server.py (現在のツール登録状態)
    - config/mcp_tools.yaml (現在のツールカタログ)
    - tests/test_mcp_server.py (現在の EXPECTED_TOOLS と既存テストパターン)
    - mcp_server/tools/execute_python.py (Task 1 で作成済み)
  </read_first>
  <action>
1. `mcp_server/server.py` を更新:
   - import セクションに追加: `from tools.execute_python import register_tools as register_execute_python_tools`
   - ツール登録に追加: `register_execute_python_tools(mcp)` を `register_claude_code_tools(mcp)` の次の行に追加

2. `config/mcp_tools.yaml` を更新:
   - `claude_code` エントリの後、`get_current_datetime` の前に以下を追加:
```yaml
  - name: execute_python
    description: Python コードをサンドボックス内で実行し stdout/stderr/exit_code を返す
    # privileged: 任意の Python コードをサブプロセスで実行できる。
    # AST ホワイトリストチェックがあるが __import__() 等で迂回可能（社内ツール前提で中程度の制御）。
    # このツールを AGENT.md の tools: に宣言すると SubAgent はコード実行可能になる。
    privileged: true
```

3. `tests/test_mcp_server.py` を更新:
   - `EXPECTED_TOOLS` を `{"ping", "web_search", "db_query", "claude_code", "execute_python", "get_current_datetime"}` に更新（既存の `get_current_datetime` 漏れも修正）
   - `test_stub_schemas_have_required_params` の `cases` dict に `"execute_python": "code"` を追加
   - 以下のテストケースを末尾に追加:

```python
# ─────────────────────────────────────────────
# Phase 28: execute_python ツール ユニットテスト
# EXEC-01〜06
# ─────────────────────────────────────────────

@pytest.mark.asyncio
async def test_execute_python_returns_stdout():
    """EXEC-01: execute_python が正常出力を返す。"""
    from unittest.mock import AsyncMock, MagicMock, patch

    from tools.execute_python import execute_python, _load_allowlist

    # allowlist キャッシュをリセットして math を許可
    import tools.execute_python as ep_mod
    ep_mod._cached_allowlist = frozenset(["math", "json"])

    mock_proc = MagicMock()
    mock_proc.communicate = AsyncMock(return_value=(b"hello\n", b""))
    mock_proc.returncode = 0

    try:
        with patch("asyncio.create_subprocess_exec", return_value=mock_proc):
            result = await execute_python(code="print('hello')")
    finally:
        ep_mod._cached_allowlist = None

    assert result["stdout"] == "hello\n"
    assert result["exit_code"] == 0
    assert result["truncated"] is False
    assert result["stderr"] == ""


@pytest.mark.asyncio
async def test_execute_python_env_sanitized():
    """EXEC-02: DATABASE_URL 等がサブプロセスに渡らない。"""
    import os
    from unittest.mock import AsyncMock, MagicMock, patch

    from tools.execute_python import execute_python
    import tools.execute_python as ep_mod
    ep_mod._cached_allowlist = frozenset()

    mock_proc = MagicMock()
    mock_proc.communicate = AsyncMock(return_value=(b"ok", b""))
    mock_proc.returncode = 0

    try:
        with patch.dict(os.environ, {
            "DATABASE_URL": "postgresql://localhost/db",
            "SECRET_KEY": "secret123",
            "PATH": "/usr/bin",
            "HOME": "/root",
        }):
            with patch("asyncio.create_subprocess_exec", return_value=mock_proc) as mock_exec:
                await execute_python(code="print('ok')")

        _args, kwargs = mock_exec.call_args
        env = kwargs.get("env", {})
        assert set(env.keys()).issubset({"PATH", "HOME", "LANG", "LC_ALL", "TERM"}), \
            f"Unexpected env keys: {set(env.keys())}"
        assert "DATABASE_URL" not in env
        assert "SECRET_KEY" not in env
    finally:
        ep_mod._cached_allowlist = None


@pytest.mark.asyncio
async def test_execute_python_timeout():
    """EXEC-03: タイムアウト時に error を返しプロセスを terminate する。"""
    import asyncio as _asyncio
    from unittest.mock import AsyncMock, MagicMock, patch

    from tools.execute_python import execute_python
    import tools.execute_python as ep_mod
    ep_mod._cached_allowlist = frozenset()

    mock_proc = MagicMock()
    mock_proc.communicate = AsyncMock(side_effect=_asyncio.TimeoutError())
    mock_proc.wait = AsyncMock(return_value=None)
    mock_proc.terminate = MagicMock()
    mock_proc.kill = MagicMock()

    try:
        with patch("asyncio.create_subprocess_exec", return_value=mock_proc):
            result = await execute_python(code="print('slow')", timeout=1)
    finally:
        ep_mod._cached_allowlist = None

    mock_proc.terminate.assert_called_once()
    assert "Timeout" in result.get("error", "")
    assert result["exit_code"] == -1


@pytest.mark.asyncio
async def test_execute_python_blocks_disallowed_import():
    """EXEC-04: ホワイトリスト外 import がブロックされる。"""
    from tools.execute_python import execute_python
    import tools.execute_python as ep_mod
    ep_mod._cached_allowlist = frozenset(["math", "json"])

    try:
        result = await execute_python(code="import subprocess\nprint('hi')")
    finally:
        ep_mod._cached_allowlist = None

    assert result["exit_code"] == 1
    assert "Blocked imports" in result.get("error", "")
    assert "subprocess" in result.get("error", "")


@pytest.mark.asyncio
async def test_execute_python_allows_whitelisted_import():
    """EXEC-05: 許可モジュールの import は通過する。"""
    from unittest.mock import AsyncMock, MagicMock, patch

    from tools.execute_python import execute_python
    import tools.execute_python as ep_mod
    ep_mod._cached_allowlist = frozenset(["math", "json"])

    mock_proc = MagicMock()
    mock_proc.communicate = AsyncMock(return_value=(b"3.14\n", b""))
    mock_proc.returncode = 0

    try:
        with patch("asyncio.create_subprocess_exec", return_value=mock_proc):
            result = await execute_python(code="import math\nprint(math.pi)")
    finally:
        ep_mod._cached_allowlist = None

    assert result["exit_code"] == 0
    assert result["stdout"] == "3.14\n"
    assert "error" not in result
```

注意:
- 各テストで `ep_mod._cached_allowlist` をセット/リセットしてテスト間の副作用を防ぐ
- `EXPECTED_TOOLS` に `get_current_datetime` を追加して既存の不整合も修正する（Pitfall 5）
- `"execute_python": "code"` を schema テストの cases に追加する
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && python3 -m pytest tests/test_mcp_server.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - mcp_server/server.py が `from tools.execute_python import register_tools as register_execute_python_tools` を含む
    - mcp_server/server.py が `register_execute_python_tools(mcp)` を含む
    - config/mcp_tools.yaml が `name: execute_python` エントリと `privileged: true` を含む
    - tests/test_mcp_server.py の EXPECTED_TOOLS が `"execute_python"` と `"get_current_datetime"` を含む
    - tests/test_mcp_server.py の cases dict が `"execute_python": "code"` を含む
    - tests/test_mcp_server.py に `test_execute_python_returns_stdout`, `test_execute_python_env_sanitized`, `test_execute_python_timeout`, `test_execute_python_blocks_disallowed_import`, `test_execute_python_allows_whitelisted_import` の 5 テストが存在する
    - `python3 -m pytest tests/test_mcp_server.py -x -q` が全テスト PASS で終了する
  </acceptance_criteria>
  <done>execute_python が MCP サーバーに登録され、mcp_tools.yaml カタログに追加され、EXEC-01〜06 のテストが全て PASS する</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM -> execute_python | LLM が生成した任意のコード文字列がツールに渡る |
| execute_python -> subprocess | サニタイズ済みコードがサブプロセスで実行される |
| subprocess -> host FS | サブプロセスのファイルシステムアクセス |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | execute_python | mitigate | AST ホワイトリストで `os`, `subprocess`, `sys`, `shutil` をブロック。`_check_imports()` で exec 前に検証 |
| T-28-02 | Information Disclosure | execute_python env | mitigate | ALLOWED_ENV_KEYS frozenset で DATABASE_URL, SECRET_KEY 等を子プロセスに渡さない |
| T-28-03 | Denial of Service | execute_python subprocess | mitigate | RLIMIT_AS 512MB + 60s タイムアウト + SIGTERM→SIGKILL エスカレーション |
| T-28-04 | Elevation of Privilege | AST bypass | accept | `__import__()`, `importlib.import_module()` で迂回可能。社内ツール（200名、信頼済みユーザー）前提で中程度の制御（RESEARCH.md Pitfall 2） |
| T-28-05 | Tampering | file system write | mitigate | `cwd="/tmp"` 固定でサブプロセスの作業ディレクトリを一時領域に制限（D-02） |
</threat_model>

<verification>
1. `python3 -m pytest tests/test_mcp_server.py -x -q` — 全テスト PASS
2. `grep -c "execute_python" mcp_server/server.py` — 2 以上（import + 登録）
3. `grep "execute_python" config/mcp_tools.yaml` — エントリ存在
4. `grep "privileged: true" config/mcp_tools.yaml | wc -l` — 2（claude_code + execute_python）
</verification>

<success_criteria>
- execute_python MCP ツールが FastMCP サーバーに登録され、code パラメータで Python コードを受け取り stdout/stderr/exit_code を返す
- ホワイトリスト外の import が AST チェックでブロックされる（EXEC-04 テスト PASS）
- env サニタイズで DATABASE_URL 等が子プロセスに渡らない（EXEC-02 テスト PASS）
- タイムアウトでプロセスが適切に終了する（EXEC-03 テスト PASS）
- mcp_tools.yaml に execute_python エントリが privileged: true で追加されている
- 全テストが PASS する
</success_criteria>

<output>
After completion, create `.planning/phases/28-codeact-llm/28-01-SUMMARY.md`
</output>
