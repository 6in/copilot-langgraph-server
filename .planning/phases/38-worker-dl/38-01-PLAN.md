---
phase: 38
plan: 1
plan_id: 38-01-types-and-roundtrip-gate
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/types.ts
  - frontend/src/hooks/useAttachments.ts
  - frontend/src/components/MessageArea.tsx
  - tests/test_langgraph_handler_outputs_bundle.py
  - tests/test_post_process_rename.py
  - tests/test_outputs_route.py
  - tests/test_mcp_attachments_kind.py
  - tests/test_execute_python_output.py
  - tests/test_claude_code_no_cwd_arg.py
autonomous: true
requirements: [FOUT-04]
tags: [worker-output, attachments, kind-discriminator, checkpoint-round-trip]
objective: "AttachmentMeta.kind を 'user_upload' | 'generated' に enum 化 (D-30 案 A 確定) し、AIMessage.additional_kwargs.attachments の AsyncPostgresSaver round-trip を Wave 0 で検証、後続 wave 用に MISSING テストファイルを scaffold する"

must_haves:
  truths:
    - "AttachmentMeta.kind は discriminator 列挙 'user_upload' | 'generated' であり、'file' 値は **TypeScript 型定義・hook 層 (useAttachments)・MessageArea 描画層** から削除される (Python 側の attachments.py upload route hardcoded `'kind': 'file'` 正規化は Plan 04 が担当 — D-30 案 A 完遂は Plan 04 まで段階的)"
    - "AIMessage に additional_kwargs={'attachments': [...]} を載せて AsyncPostgresSaver で 1 回 checkpoint → 別 thread で復元したとき、attachments list が値・order・kind とも同一で round-trip する"
    - "MISSING テストファイル (test_outputs_route.py / test_mcp_attachments_kind.py / test_post_process_rename.py / test_langgraph_handler_outputs_bundle.py / test_execute_python_output.py / test_claude_code_no_cwd_arg.py) が scaffold 済で、未実装ケースは pytest.mark.skip(reason='38-NN で実装') で明示される"
    - "D-30: AttachmentMeta.kind を 'user_upload' | 'generated' 単一 discriminator に enum 化 (案 A 確定、本 plan は TypeScript 型 + hook + MessageArea スコープに限定、Python 側正規化は Plan 04 へ段階的委譲) (CONTEXT.md)"
    - "D-18: AIMessage.additional_kwargs.attachments の kind フィールド値は SystemMessage prepend で扱う input/output flat list (件数制限なし) と同一 enum を共有し、本 plan の round-trip テストで kind 値が JSONB 経由でも一致することを担保 (CONTEXT.md)"
  artifacts:
    - path: "frontend/src/types.ts"
      provides: "AttachmentMeta.kind: 'user_upload' | 'generated' (D-30 案 A) と AIMessageAttachmentsKind alias 型"
      contains: "kind: 'user_upload' | 'generated'"
    - path: "tests/test_langgraph_handler_outputs_bundle.py"
      provides: "test_round_trip_postgres (38-01-01) を実装、他 ID は skip scaffold"
      contains: "async def test_round_trip_postgres"
    - path: "tests/test_post_process_rename.py"
      provides: "snapshot diff / _is_already_prefixed / .pyc 除外の skip scaffold"
    - path: "tests/test_outputs_route.py"
      provides: "GET raw bytes / path traversal / isolation の skip scaffold"
    - path: "tests/test_mcp_attachments_kind.py"
      provides: "attachments_list_core が kind を返すケースの skip scaffold"
    - path: "tests/test_execute_python_output.py"
      provides: "cwd 切替 + /tmp fallback + mkdir 冪等の skip scaffold"
    - path: "tests/test_claude_code_no_cwd_arg.py"
      provides: "inspect.signature による cwd 引数なし検証 skip scaffold"
  key_links:
    - from: "frontend/src/types.ts AttachmentMeta"
      to: "frontend/src/hooks/useAttachments.ts:105 (staging 構築) と :159 (getReadyItems passthrough)"
      via: "kind フィールドの値域を 'user_upload' に変更"
      pattern: "kind:\\s*['\"]user_upload['\"]"
    - from: "frontend/src/types.ts AttachmentMeta"
      to: "frontend/src/components/MessageArea.tsx AttachmentChipRow"
      via: "ext + kind 参照で type-check pass"
      pattern: "a\\.kind"
    - from: "tests/test_langgraph_handler_outputs_bundle.py::test_round_trip_postgres"
      to: "AsyncPostgresSaver round-trip 検証 (patterns.md L94-99)"
      via: "AIMessage(additional_kwargs={'attachments': [...]}) → checkpoint → restore"
      pattern: "AsyncPostgresSaver|additional_kwargs"
---

<objective>
Phase 38 は新規データ (kind='generated' を持つ AttachmentMeta) を AIMessage.additional_kwargs に載せて AsyncPostgresSaver で永続化する設計 (D-15)。patterns.md §Wave 0 risk-gate (L94-99) に従い、データモデル全体が後続 wave に依存する前に round-trip を 1 plan で潰す。

同時に、CONTEXT.md `critical_unresolved_decision` で planner 判断とされていた `AttachmentMeta.kind` 衝突問題を本 plan で **D-30 案 A** として確定する: 既存 `kind: 'file'` DTO 識別子を削除し、`kind: 'user_upload' | 'generated'` 単一 discriminator に置換する。これは UI-SPEC L302-307 / CONTEXT.md D-06 の要求と一致し、grep で外部参照ゼロ (ChatApp.tsx:147 の `item.kind` は `DataTransferItem.kind` browser API、関係なし) のため後方互換性破壊リスクなし。

Purpose: Wave 0 risk-gate (AIMessage round-trip) + 型契約確定 + テスト scaffold で、Plan 02/03 (Wave 1) を安全に並列展開できる土台を作る。

Output:
- `frontend/src/types.ts` の AttachmentMeta が `kind: 'user_upload' | 'generated'` 必須フィールドを持つ
- `frontend/src/hooks/useAttachments.ts` / `MessageArea.tsx` が新型に整合する (`kind: 'user_upload'` 固定)
- AIMessage round-trip テストが 1 ケース green (`test_round_trip_postgres`)
- 後続 wave 用の 6 テストファイルが scaffold 済で、ID が VALIDATION.md と 1:1 対応する
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/patterns.md
@.planning/phases/38-worker-dl/38-CONTEXT.md
@.planning/phases/38-worker-dl/38-RESEARCH.md
@.planning/phases/38-worker-dl/38-PATTERNS.md
@.planning/phases/38-worker-dl/38-UI-SPEC.md
@.planning/phases/38-worker-dl/38-VALIDATION.md
@frontend/src/types.ts
@frontend/src/hooks/useAttachments.ts
@frontend/src/components/MessageArea.tsx
@frontend/src/components/ChatApp.tsx
@app/api/routes/chat.py
@app/jobs/handlers/langgraph_handler.py

<interfaces>
<!-- 既存 AttachmentMeta (frontend/src/types.ts L62-71) — 本 plan で kind を enum 化 -->
```ts
export interface AttachmentMeta {
  kind: 'file';   // ← 本 plan で 'user_upload' | 'generated' へ置換
  name: string;
  storage_name: string;
  path: string;
  size: number;
  mime_type: string;
  ext: string;
  modified_at: string;
}
```

<!-- patterns.md §"HumanMessage.additional_kwargs サイドカー envelope" (L79-85) -->
- AIMessage.additional_kwargs に dict を載せて AsyncPostgresSaver の JSONB に round-trip する設計は HumanMessage 側で検証済 (Phase 36 Wave 0)
- AIMessage 側にも同 envelope を適用するが「AIMessage.name 喪失問題 (ADR-0038)」と隣接するため、本 plan で round-trip を独立検証する

<!-- VALIDATION.md Task ID と test ファイル対応 (38-VALIDATION.md より) -->
- 38-01-01 → tests/test_langgraph_handler_outputs_bundle.py::test_round_trip_postgres (本 plan で実装)
- 38-01-02 → tests/test_outputs_route.py::test_isolation_other_user_blocked (Plan 02 で実装、本 plan は skip)
- 38-01-03 → tests/test_outputs_route.py::test_path_traversal_rejected (Plan 02 で実装、本 plan は skip)
- 38-02-01 → tests/test_mcp_attachments_kind.py::test_returns_both_kinds (Plan 02 で実装、本 plan は skip)
- 38-03-01..04 → tests/test_post_process_rename.py 等 (Plan 03 で実装、本 plan は skip)
- 38-04-03 → tests/test_langgraph_handler_outputs_bundle.py::test_bundles_generated_files (Plan 04 で実装、本 plan は skip)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AttachmentMeta.kind を enum 化し staging / 描画整合をとる (D-30 案 A 確定)</name>
  <files>frontend/src/types.ts, frontend/src/hooks/useAttachments.ts, frontend/src/components/MessageArea.tsx</files>
  <read_first>
    - frontend/src/types.ts L62-71 (`AttachmentMeta`) — 既存定義
    - frontend/src/hooks/useAttachments.ts L100-117 (staging item 構築) + L155-170 (getReadyItems passthrough)
    - frontend/src/components/MessageArea.tsx L52-115 (`AttachmentChipRow`) と L1-50 (import 部) — 型参照箇所
    - frontend/src/components/ChatApp.tsx L140-160 (paste handler) — `item.kind === 'file'` は `DataTransferItem.kind`、AttachmentMeta とは無関係であることを確認 (grep で誤検出回避)
    - .planning/phases/38-worker-dl/38-PATTERNS.md §"Plan 05-G frontend/src/types.ts (MODIFY)" §"Plan 05-H useAttachments" — 案 A 採用根拠
    - .planning/phases/38-worker-dl/38-UI-SPEC.md L302-307 (`kind: 'user_upload' | 'generated'` 要件) と L621-642 (Checker Acceptance Criteria #4)
    - .planning/phases/38-worker-dl/38-CONTEXT.md D-06 (`kind` を single discriminator として通す) と D-14 (チップに「AI 生成」「添付」ラベル)
  </read_first>
  <behavior>
    - frontend/src/types.ts: AttachmentMeta.kind が `'user_upload' | 'generated'` の literal union 必須フィールドであること。`'file'` 値は型レベルで拒否される (TS strict 環境で `kind: 'file'` を代入するとコンパイルエラー)。
    - useAttachments.ts: staging 構築時に `kind: 'user_upload'` を埋め込む。getReadyItems が同 kind を passthrough する。
    - MessageArea.tsx: AttachmentChipRow が `a.kind` を読んでも type-check pass する (kind 別描画は Plan 05 で実装、本 plan では既存挙動を破壊しないこと)。
    - `npm run -- tsc --noEmit` (or frontend ビルド) が型エラーゼロで通る。
    - 既存 paste handler (ChatApp.tsx:147 `item.kind === 'file'`) は `DataTransferItem.kind` で別 namespace、変更不要。
  </behavior>
  <action>
    1. `frontend/src/types.ts` L62-71 の `AttachmentMeta` を編集: `kind: 'file'` を `kind: 'user_upload' | 'generated'` に置換 (D-30 案 A、PATTERNS.md §"Plan 05-G")。コメント `// Phase 38 D-30 案 A: discriminator で user_upload (Phase 36) と generated (Phase 38) を識別` を追加。

    2. `frontend/src/hooks/useAttachments.ts` L105 の staging 構築 `kind: 'file'` を `kind: 'user_upload'` に置換 (PATTERNS.md §"Plan 05-H")。L159 の `getReadyItems` 内 `kind: x.kind` は型推論で union を passthrough するため変更不要だが、型定義が新 enum に切り替わったため自動的に新型を返す。

    3. `frontend/src/components/MessageArea.tsx` AttachmentChipRow (L52-115) は本 plan では描画ロジック変更しない。ただし `a.kind` を参照する箇所が将来追加されるため型衝突がないことを `bun run tsc --noEmit` で確認する。

    4. `app/api/routes/attachments.py` L160 の `"kind": "file"` も同時に置換するか? → **本 plan では触らない**。Plan 04 で `_messages_to_response` 経由の legacy 値正規化と合わせて扱う (DB の既存メッセージに `'file'` が残っているため正規化が必要)。本 plan は **型レベルの enum 化のみ** に絞り、attachments.py の upload route 内 hardcoded 値は Plan 04 で `'user_upload'` に書き換える。

    Note: Phase 36 で書かれた既存 DB データの `kind: 'file'` は Plan 04 の `_messages_to_response` 正規化で吸収するため、本 plan では DB マイグレーション不要。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph/frontend &amp;&amp; bun run tsc --noEmit 2>&amp;1 | tee /tmp/38-01-tsc.log; grep -E "error TS" /tmp/38-01-tsc.log | wc -l | xargs -I {} test {} -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - source assertion: `grep -E "kind:\s*'user_upload'\s*\|\s*'generated'" frontend/src/types.ts` ヒット 1 件以上
    - source assertion: `grep -c "kind: 'file'" frontend/src/types.ts` exit 0 で count 0 (既存定義が消えている)
    - source assertion: `grep -c "kind: 'file'" frontend/src/hooks/useAttachments.ts` exit 0 で count 0
    - source assertion: `grep -c "kind: 'user_upload'" frontend/src/hooks/useAttachments.ts` count >= 1
    - behavior assertion: `bun run tsc --noEmit` exit 0 (型エラーゼロ)
    - source assertion: `frontend/src/components/ChatApp.tsx:147` の `item.kind === 'file'` は **そのまま残す** (DataTransferItem.kind は browser API で別 namespace) — `grep -c "item.kind === 'file'" frontend/src/components/ChatApp.tsx` count == 1
  </acceptance_criteria>
  <done>AttachmentMeta が D-30 案 A に従って enum 化され、TypeScript ビルドが型エラーゼロで通る。useAttachments の staging item が `kind: 'user_upload'` を埋め込む。ChatApp.tsx の DataTransferItem.kind は誤って書き換えていない。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AIMessage.additional_kwargs.attachments の AsyncPostgresSaver round-trip 検証 (Wave 0 risk-gate)</name>
  <files>tests/test_langgraph_handler_outputs_bundle.py</files>
  <read_first>
    - .planning/patterns.md §"HumanMessage.additional_kwargs サイドカー envelope" (L79-85) と §"Wave 0 risk-gate" (L94-99)
    - .planning/phases/38-worker-dl/38-RESEARCH.md §"Pattern 2: AIMessage.additional_kwargs.attachments で AI 生成ファイルを turn 単位で bundle" (L363-413) と §"Assumptions Log A2" (L862-863)
    - .planning/phases/38-worker-dl/38-VALIDATION.md 38-01-01 行 (`test_round_trip_postgres` 仕様)
    - tests/test_langgraph_handler_attachments.py / tests/test_langgraph_handler_attachments_v2.py — analog (HumanMessage 側 round-trip テストパターン)
    - app/jobs/handlers/langgraph_handler.py L177 (`AsyncPostgresSaver.from_conn_string(DB_URI)` の使い方) と L228-243 (final_state の取得経路)
    - docs/adr/0038-superchat-context-messages-and-agent-name-persistence.md — AIMessage.name 喪失問題の境界 (additional_kwargs はこの問題から **独立** している点を確認)
  </read_first>
  <behavior>
    - test_round_trip_postgres (38-01-01): AsyncPostgresSaver を用いた最小 graph で `AIMessage(content='ok', additional_kwargs={'attachments': [{'kind': 'generated', 'name': '20260512T120000_chart.png', 'size': 123, 'ext': '.png', 'modified_at': 1.0}]})` を 1 回 checkpoint → 別 thread_id で aget → 復元されたメッセージリストの最後の AIMessage の additional_kwargs.attachments が **値・order・kind とも同一** で round-trip すること。
    - patterns.md L94-99 のとおり、データモデル全体が後続 wave に依存する前に round-trip を 1 plan で潰す目的。
    - その他テストケース (test_bundles_generated_files 等、38-04-03) は本 plan では `pytest.mark.skip(reason='Plan 04 で実装')` で scaffold のみ。
  </behavior>
  <action>
    1. `tests/test_langgraph_handler_outputs_bundle.py` を新規作成。tests/test_langgraph_handler_attachments.py の header (import + AsyncPostgresSaver fixture) を analog として参照しつつ、本 plan では追加依存なしで AsyncPostgresSaver round-trip のみを単体で組む。

    2. `test_round_trip_postgres` (38-01-01) の手順:
       - psycopg AsyncConnectionPool + AsyncPostgresSaver.from_conn_string(DB_URI) または既存 conftest fixture を流用 (PATTERNS.md §"Test fixture pattern" を踏襲)
       - 最小 StateGraph: messages: Annotated[list[BaseMessage], add_messages] の単一 reducer state、ノード 1 つで `AIMessage(content='ok', additional_kwargs={'attachments': [generated_meta]})` を返すだけ
       - thread_id="rt-1" で 1 回 ainvoke → checkpoint 保存
       - 別接続から `saver.aget(config={'configurable': {'thread_id': 'rt-1'}})` で復元
       - assert: 復元された state["messages"][-1].additional_kwargs.attachments が元と同一 (kind=='generated', name, size, ext, modified_at 全フィールド一致)

    3. 他のテストケース scaffold:
       - `test_bundles_generated_files` (38-04-03) — `@pytest.mark.skip(reason='Plan 04 で実装: turn 完了時に AIMessage.additional_kwargs.attachments へ bundle される')`
       - 必要なら test_handler_does_not_double_count 等の skip stub も Plan 04 で追加予定として記述だけ残す

    4. **テスト実行環境注意:** DB_URI は conftest.py の既存 fixture (postgres docker compose) を参照する。CI で postgres が起動できない場合は `pytest.mark.requires_postgres` マーカーで skip する既存パターン (tests/test_langgraph_handler_attachments.py header 確認) を踏襲する。

    Note (RESEARCH §Assumption A2 / patterns.md L82): HumanMessage 側の round-trip は Phase 36 Wave 0 で検証済だが、AIMessage 側は厳密には未検証。ADR-0038 の `AIMessage.name` 喪失問題と隣接するため独立検証する。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph &amp;&amp; uv run pytest tests/test_langgraph_handler_outputs_bundle.py::test_round_trip_postgres -x -v 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - test command: `uv run pytest tests/test_langgraph_handler_outputs_bundle.py::test_round_trip_postgres -x` exit 0
    - source assertion: `grep -c "async def test_round_trip_postgres" tests/test_langgraph_handler_outputs_bundle.py` count >= 1
    - source assertion: `grep -E "additional_kwargs.*attachments|kind.*generated" tests/test_langgraph_handler_outputs_bundle.py` ヒット (assertion で kind=='generated' を assert している)
    - source assertion: `grep -c "AsyncPostgresSaver" tests/test_langgraph_handler_outputs_bundle.py` count >= 1
    - source assertion: `grep -c "test_bundles_generated_files" tests/test_langgraph_handler_outputs_bundle.py` count >= 1 (scaffold 存在)
    - source assertion: `grep -c "pytest.mark.skip" tests/test_langgraph_handler_outputs_bundle.py` count >= 1 (Plan 04 用 skip stub)
  </acceptance_criteria>
  <done>AIMessage.additional_kwargs.attachments が AsyncPostgresSaver JSONB を経由しても 'kind' フィールド含めて完全 round-trip することを green テスト 1 本で証明。Plan 04 用の skip stub も scaffold 済。</done>
</task>

<task type="auto">
  <name>Task 3: 後続 wave 用 5 つの MISSING テストファイルを skip scaffold で作成</name>
  <files>tests/test_outputs_route.py, tests/test_mcp_attachments_kind.py, tests/test_post_process_rename.py, tests/test_execute_python_output.py, tests/test_claude_code_no_cwd_arg.py</files>
  <read_first>
    - .planning/phases/38-worker-dl/38-VALIDATION.md (Per-Task Verification Map 全行) — task ID → test 関数名 1:1 マッピング
    - .planning/phases/38-worker-dl/38-PATTERNS.md §"Plan 06 Tests" (L905-1003) — 各テストの shape
    - tests/test_attachments_get_delete_route.py:1-30 (`patch_thread_files_dir` fixture analog)
    - tests/test_attachments_list.py:20-46 (`test_list_returns_metadata` analog)
    - pyproject.toml `[tool.pytest.ini_options]` — `asyncio_mode = "auto"` 確認
  </read_first>
  <behavior>
    - 5 つのテストファイルが新規作成され、それぞれに必要な test 関数が `pytest.mark.skip(reason='38-NN-NN で実装: <短い要約>')` で scaffold される。
    - `uv run pytest tests/test_outputs_route.py tests/test_mcp_attachments_kind.py tests/test_post_process_rename.py tests/test_execute_python_output.py tests/test_claude_code_no_cwd_arg.py --collect-only -q` で全 ID が VALIDATION.md と 1:1 で列挙される。
    - skip 対象テストは pytest で collect されるが実行はされない (skip マーカー)。
  </behavior>
  <action>
    1. **tests/test_outputs_route.py** (NEW) — VALIDATION.md ID: 38-01-02 / 38-01-03 / 38-04-01 / 38-04-02 (Plan 02 / 04 で実装)
       PATTERNS.md §"Plan 06 Tests" §"tests/test_outputs_route.py" の analog を参照。fixture `patch_thread_files_dir` を tmp_path で monkeypatch する shape を **header に書く**。本 plan では各テスト関数 (`test_get_output_returns_raw_bytes` / `test_path_traversal_rejected` / `test_isolation_other_user_blocked` / `test_get_output_works_for_claude_code`) を `pytest.mark.skip(reason="Plan 02 (38-02) で outputs route 実装と同時に green 化")` で scaffold。

    2. **tests/test_mcp_attachments_kind.py** (NEW) — VALIDATION.md ID: 38-02-01 (Plan 02)
       PATTERNS.md §"Plan 06 Tests" §"tests/test_mcp_attachments_kind.py" の analog を参照。tmp_path monkeypatch + `attachments_list_core` 呼び出し shape を header に書く。`test_returns_both_kinds` を skip scaffold (`reason="Plan 02 (38-02) で attachments_list_core 拡張と同時に green 化"`)。

    3. **tests/test_post_process_rename.py** (NEW) — VALIDATION.md ID: 38-03-02 / 38-03-03 (Plan 03)
       PATTERNS.md §"Plan 06 Tests" §"tests/test_post_process_rename.py" の GREENFIELD shape を参照。`test_snapshot_diff_renames_only_new` / `test_skips_already_prefixed` / `test_excludes_pyc_files` の 3 関数を skip scaffold (`reason="Plan 03 (38-03) で _rename_new_outputs / _is_already_prefixed 実装と同時に green 化"`)。

    4. **tests/test_execute_python_output.py** (NEW) — VALIDATION.md ID: 38-03-01 (Plan 03)
       GREENFIELD。`test_writes_to_generated_folder` (mock subprocess で cwd 切替検証) と `test_falls_back_to_tmp_without_headers` を skip scaffold (`reason="Plan 03 (38-03) で _resolve_generated_folder + cwd 切替実装と同時に green 化"`)。

    5. **tests/test_claude_code_no_cwd_arg.py** (NEW) — VALIDATION.md ID: 38-03-04 (Plan 03)
       trivial GREENFIELD: `inspect.signature(claude_code).parameters` を確認する 1 関数のみ。`test_signature_has_no_cwd` を skip scaffold (`reason="Plan 03 (38-03) で claude_code シグネチャから cwd 削除と同時に green 化"`)。

    各ファイルは shape (import / 必要 fixture の宣言) + skip マーカー付き test 関数 def までを書く。テスト本体は `pass` または `assert True  # see plan-NN action` 程度で OK。Plan 02/03/04 の executor が skip マーカーを外して assertion 本体を書く運用。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph &amp;&amp; uv run pytest tests/test_outputs_route.py tests/test_mcp_attachments_kind.py tests/test_post_process_rename.py tests/test_execute_python_output.py tests/test_claude_code_no_cwd_arg.py --collect-only -q 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - test command: `uv run pytest tests/test_outputs_route.py tests/test_mcp_attachments_kind.py tests/test_post_process_rename.py tests/test_execute_python_output.py tests/test_claude_code_no_cwd_arg.py --collect-only -q` exit 0
    - source assertion: `grep -l "test_get_output_returns_raw_bytes" tests/test_outputs_route.py` 該当ファイルが返る
    - source assertion: `grep -l "test_path_traversal_rejected" tests/test_outputs_route.py` 該当
    - source assertion: `grep -l "test_isolation_other_user_blocked" tests/test_outputs_route.py` 該当
    - source assertion: `grep -l "test_returns_both_kinds" tests/test_mcp_attachments_kind.py` 該当
    - source assertion: `grep -l "test_snapshot_diff_renames_only_new" tests/test_post_process_rename.py` 該当
    - source assertion: `grep -l "test_skips_already_prefixed" tests/test_post_process_rename.py` 該当
    - source assertion: `grep -l "test_signature_has_no_cwd" tests/test_claude_code_no_cwd_arg.py` 該当
    - behavior assertion: `uv run pytest tests/test_outputs_route.py -v 2>&1 | grep -c SKIPPED` count >= 3 (Plan 02 で実装される ID は skip)
    - behavior assertion: full suite `uv run pytest tests/ -x --ignore=tests/test_api_chat.py` で本 plan 由来の新規 failed テストゼロ (skip / passed のみ)
  </acceptance_criteria>
  <done>5 つのテストファイルが scaffold 済で、VALIDATION.md の全 Task ID と 1:1 で対応する関数名が存在し、すべて collect 可能。後続 plan の executor は skip マーカーを外して assertion 本体を書くだけで進められる。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend ↔ backend `additional_kwargs` JSON | LangGraph checkpoint 経由で復元される dict、type-narrowing が必要 |
| psycopg JSONB ↔ Python dict | AsyncPostgresSaver の serialize/deserialize に dict 値が依存 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01-01 | Tampering | AsyncPostgresSaver round-trip での kind 値混入 | mitigate | test_round_trip_postgres で kind=='generated' を assert、Plan 04 の `_messages_to_response` で legacy 'file' を 'user_upload' に正規化 |
| T-38-01-02 | Information Disclosure | additional_kwargs に PII / 内部 path が露出 | accept | 本 plan で扱う dict は name/size/ext/modified_at/kind のみ、PII なし。RESEARCH §Pitfall 6 で Enum 混在禁止を遵守 |
| T-38-01-03 | DoS | round-trip 検証中の DB connection leak | mitigate | AsyncPostgresSaver.from_conn_string を `async with` で囲み、テスト完了時に必ず close する (既存 tests/test_langgraph_handler_attachments.py パターン踏襲) |
</threat_model>

<verification>
- Wave 0 risk-gate: `uv run pytest tests/test_langgraph_handler_outputs_bundle.py::test_round_trip_postgres -x` が green
- 型契約: `cd frontend && bun run tsc --noEmit` exit 0
- テスト基盤: `uv run pytest tests/test_outputs_route.py tests/test_mcp_attachments_kind.py tests/test_post_process_rename.py tests/test_execute_python_output.py tests/test_claude_code_no_cwd_arg.py --collect-only -q` exit 0
- 後方互換性: full suite `uv run pytest tests/ -x --ignore=tests/test_api_chat.py` で Phase 38 由来の新規 failed ゼロ
</verification>

<success_criteria>
- AttachmentMeta.kind が `'user_upload' | 'generated'` literal union に enum 化され、TypeScript ビルドが green
- useAttachments staging item が `kind: 'user_upload'` を埋め込み、`grep -c "kind: 'file'" frontend/src/types.ts frontend/src/hooks/useAttachments.ts` が 0
- ChatApp.tsx:147 の DataTransferItem.kind は触らず維持
- test_round_trip_postgres (38-01-01) が green で AIMessage.additional_kwargs.attachments の AsyncPostgresSaver round-trip を証明
- 5 つの新規テストファイル (test_outputs_route.py / test_mcp_attachments_kind.py / test_post_process_rename.py / test_execute_python_output.py / test_claude_code_no_cwd_arg.py) が scaffold 済で VALIDATION.md と ID 1:1 対応
- 後続 Plan 02 (Wave 1) が並列に着手可能
</success_criteria>

<output>
After completion, create `.planning/phases/38-worker-dl/38-01-SUMMARY.md`
</output>
