---
phase: 38
plan: 5
plan_id: 38-05-frontend-modal-and-renderers
type: execute
wave: 3
depends_on:
  - 38-01-types-and-roundtrip-gate
  - 38-02-backend-route-and-mcp-list
  - 38-03-sandbox-cwd-and-rename
  - 38-04-handler-bundle-and-helper
files_modified:
  - frontend/src/components/AttachmentModal.tsx
  - frontend/src/components/preview/ImagePreview.tsx
  - frontend/src/components/preview/MarkdownPreview.tsx
  - frontend/src/components/preview/CsvPreview.tsx
  - frontend/src/components/preview/TextPreview.tsx
  - frontend/src/components/MessageArea.tsx
autonomous: false
requirements: [FOUT-01, FOUT-02, FOUT-03, FOUT-04]
tags: [frontend, attachment-chip, attachment-modal, image-preview, markdown-preview, csv-preview, text-preview, monaco]
objective: "AttachmentChipRow を kind=user_upload/generated 別 micro-badge 表示 + クリックで AttachmentModal 起動できるよう拡張し、AttachmentModal 本体と 4 種 renderer (Image / Markdown / CSV / Monaco-text) を新規実装する。kind ベースの URL 解決 (`/attachments/` vs `/outputs/`) で input/output を統一 UX で扱う"

must_haves:
  truths:
    - "AttachmentChipRow が `a.kind === 'generated'` 時に「✨ AI 生成」micro-badge を、それ以外に「📎 添付」micro-badge を表示する (UI-SPEC §Color §kind ラベル / §AttachmentChipRow kind 拡張)"
    - "AttachmentChipRow チップ全体が `<button>` 要素になり、`aria-haspopup=\"dialog\"` 付きでクリックすると AttachmentModal が開く (UI-SPEC L341-344)"
    - "AttachmentModal が role=\"dialog\" + aria-modal=\"true\"、Esc / overlay click / × ボタンで閉じる、Tab で focus trap を循環する (UI-SPEC §Keyboard / Focus 管理 L469-477)"
    - "ext から PreviewKind を classify する関数で 4 種 renderer (image / markdown / csv / text) + unsupported フォールバックに分岐する (UI-SPEC L418-428)"
    - "kind ベースの URL 解決: `kind === 'generated'` で `/api/threads/{tid}/outputs/{name}`、それ以外で `/api/threads/{tid}/attachments/{storage_name}` に振り分ける (UI-SPEC L485-494)"
    - "ImagePreview = `<img src>` raw bytes 直配信 (サムネ生成しない、size cap 10MB)、MarkdownPreview = react-markdown 直呼び (MarkdownMessage は呼ばない、size cap 1MB)、CsvPreview = ChatAgGridTable 流用 (size cap 1MB)、TextPreview = Monaco read-only (size cap 1MB) (UI-SPEC §Component Contracts §AttachmentModal)"
    - "新規 npm パッケージゼロ (UI-SPEC Checker #2)、新規 CSS 変数ゼロ (UI-SPEC Checker #1)"
    - "D-12: プレビュー対象は **画像 / Markdown / CSV / プレーンテキスト (Monaco syntax highlight)** の 4 種、PDF は preview せず DL のみ — Modal が unsupported フォールバックで「Download only」案内 + DL CTA を返す (CONTEXT.md)"
    - "D-13: チップ一覧 + クリックでモーダルプレビューの UX に統一し、AI 応答テキスト内 inline 描画 (`![]()`) は行わない。Phase 36 の AttachmentChip / AttachmentModal を kind 対応に拡張する (新規コンポーネントは作らない方針との整合) (CONTEXT.md)"
    - "D-14: 「AI 生成」「添付」の 2 種類の micro-badge ラベルでチップを区別、kind 値と 1:1 で対応 (CONTEXT.md)"
  artifacts:
    - path: "frontend/src/components/AttachmentModal.tsx"
      provides: "モーダル本体 + 4 種 renderer dispatch + size cap + error banner + focus trap"
      contains: "role=\"dialog\""
    - path: "frontend/src/components/preview/ImagePreview.tsx"
      provides: "raw bytes 直配信、size cap 10MB"
    - path: "frontend/src/components/preview/MarkdownPreview.tsx"
      provides: "fetch text + react-markdown + remark-gfm 薄ラッパー"
    - path: "frontend/src/components/preview/CsvPreview.tsx"
      provides: "fetch text + 簡易 CSV パース + ChatAgGridTable 流用"
    - path: "frontend/src/components/preview/TextPreview.tsx"
      provides: "fetch text + Monaco read-only + LANG_ALIASES"
    - path: "frontend/src/components/MessageArea.tsx"
      provides: "AttachmentChipRow に kind 別 micro-badge + button 化 + AttachmentModal mount"
      contains: "AttachmentModal"
  key_links:
    - from: "AttachmentChipRow チップ button"
      to: "MessageArea state activeAttachment + AttachmentModal mount"
      via: "setActiveAttachment(a) → AttachmentModal が open"
      pattern: "setActiveAttachment|AttachmentModal"
    - from: "AttachmentModal classify(ext)"
      to: "ImagePreview / MarkdownPreview / CsvPreview / TextPreview"
      via: "lazy import + Suspense + switch dispatch"
      pattern: "lazy\\(.*preview"
    - from: "buildFileUrl(threadId, name, kind)"
      to: "/api/threads/{tid}/outputs/{name} or /api/threads/{tid}/attachments/{storage_name}"
      via: "kind === 'generated' ? 'outputs' : 'attachments'"
      pattern: "kind === 'generated' \\? 'outputs' : 'attachments'"
---

<objective>
Phase 36 で実装済の `AttachmentChipRow` + Phase 38 D-30 で enum 化された `AttachmentMeta.kind` を活用して、AI 生成ファイルもユーザー添付も同じ UX (チップ + モーダルプレビュー) で扱えるようにする。

Purpose:
- **FOUT-01 / FOUT-02 (DL)**: モーダル内の「ダウンロード」CTA で `<a download>` 経由のサーバー直 DL
- **FOUT-03 (プレビュー)**: 画像 / Markdown / CSV / プレーンテキスト系を kind 別 renderer でチャット画面上に表示
- **FOUT-04 (履歴復元)**: スレッド再オープン時に `AIMessage.additional_kwargs.attachments` (Plan 04 で永続化) から自動復元、チップ起点でモーダルを再表示
- **UX 不連続の解消**: Phase 36 アップロード添付と同じ UX に統一し、AI 応答テキスト内 inline 描画を回避 (D-13)

Output:
- `AttachmentModal.tsx` 新規 (~200 行、PATTERNS.md §"Plan 05-A" のスニペット完全踏襲)
- 4 つの preview component (`ImagePreview` / `MarkdownPreview` / `CsvPreview` / `TextPreview`) — 各 ~50-80 行
- `MessageArea.tsx` の AttachmentChipRow 拡張 + modal state + AttachmentModal mount

このプランは **`autonomous: false`** — 最後に **checkpoint:human-verify** で実機 docker compose + Chrome DevTools MCP による視覚検証が必要。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-worker-dl/38-CONTEXT.md
@.planning/phases/38-worker-dl/38-RESEARCH.md
@.planning/phases/38-worker-dl/38-PATTERNS.md
@.planning/phases/38-worker-dl/38-UI-SPEC.md
@.planning/phases/38-worker-dl/38-01-SUMMARY.md
@.planning/phases/38-worker-dl/38-04-SUMMARY.md
@frontend/src/types.ts
@frontend/src/components/MessageArea.tsx
@frontend/src/components/ConfirmModal.tsx
@frontend/src/components/MarkdownMessage.tsx
@frontend/src/components/ChatAgGridTable.tsx
@frontend/src/components/AttachmentChips.tsx
@frontend/src/hooks/useAttachments.ts
@frontend/src/contexts/ThemeContext.ts

<interfaces>
<!-- Plan 01 で確定した型 -->
```ts
// frontend/src/types.ts L62-71
export interface AttachmentMeta {
  kind: 'user_upload' | 'generated';   // Plan 01 で enum 化 (D-30 案 A)
  name: string;
  storage_name: string;
  path: string;
  size: number;
  mime_type: string;
  ext: string;
  modified_at: string;
}
```

<!-- 既存 portal モーダル analog: ConfirmModal.tsx L31-95 -->
<!-- 既存 Monaco analog: MarkdownMessage.tsx L50-180 (LANG_ALIASES + <Editor>) -->
<!-- 既存 ag-grid analog: ChatAgGridTable.tsx (MarkdownTableData → AgGridReact) -->
<!-- 既存 ReactMarkdown analog: MarkdownMessage.tsx (ReactMarkdown + remarkGfm) -->

<!-- Plan 02 で実装済の outputs route -->
<!-- GET /api/threads/{tid}/outputs/{name} → FileResponse with inline Content-Disposition -->

<!-- Plan 04 で確定した API 出力契約 -->
<!-- ChatMessage.additional_kwargs.attachments[*].kind ∈ {"user_upload", "generated"} -->
<!-- legacy 'file' は API 層で 'user_upload' に正規化済み -->

<!-- 既存 AttachmentChipRow (MessageArea.tsx L52-115) を本 plan で拡張する -->
```tsx
function AttachmentChipRow({ attachments, threadId }: { attachments: AttachmentMeta[]; threadId: string | null }) {
  if (!attachments || attachments.length === 0) return null;
  // ... 既存 map: 画像チップ <img> or text/code チップ <span>
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AttachmentModal.tsx と 4 種 preview component を新規作成する</name>
  <files>frontend/src/components/AttachmentModal.tsx, frontend/src/components/preview/ImagePreview.tsx, frontend/src/components/preview/MarkdownPreview.tsx, frontend/src/components/preview/CsvPreview.tsx, frontend/src/components/preview/TextPreview.tsx</files>
  <read_first>
    - frontend/src/components/ConfirmModal.tsx (98 行全体) — portal + dialog overlay の analog
    - frontend/src/components/MarkdownMessage.tsx L1-180 — Monaco editor + LANG_ALIASES の analog (ただし AttachmentModal からは **MarkdownMessage を直接呼ばない** — RESEARCH §Pitfall 7、UI-SPEC L437-441)
    - frontend/src/components/ChatAgGridTable.tsx — `MarkdownTableData` shape と AgGridReact のレンダリング pattern (CsvPreview で流用)
    - frontend/src/components/AttachmentChips.tsx L55-60 — `<img src>` raw bytes 直配信パターン (ImagePreview の analog)
    - frontend/src/contexts/ThemeContext.ts — `useCurrentTheme()` の使い方 (Monaco theme と ag-grid theme で必要)
    - .planning/phases/38-worker-dl/38-PATTERNS.md §"Plan 05-A AttachmentModal.tsx" (L531-617) — portal + classify + lazy + focus trap の完全スニペット
    - .planning/phases/38-worker-dl/38-PATTERNS.md §"Plan 05-B-E preview/*.tsx" (L620-775) — 各 renderer の analog
    - .planning/phases/38-worker-dl/38-UI-SPEC.md §"Component Contracts" L295-477 — DOM 構造、token reuse、size cap、エラーバナー文言
    - .planning/phases/38-worker-dl/38-UI-SPEC.md §"Phase 38 Checker Acceptance Criteria" L615-643 — 22 個の checker 基準
    - .planning/phases/38-worker-dl/38-RESEARCH.md §"Pitfall 7" — Monaco 1MB cap の根拠
  </read_first>
  <behavior>
    - `AttachmentModal.tsx`: createPortal で document.body に dialog を mount、role="dialog" aria-modal="true"、Esc / overlay click / × ボタンで close、Tab で focus trap、Body scroll lock (`document.body.style.overflow = 'hidden'`)
    - URL 解決関数 `buildFileUrl(threadId, name, kind)` が `kind === 'generated' ? 'outputs' : 'attachments'` で segment を切替 (UI-SPEC L485-494、user_upload は storage_name、generated は name を使う点に注意 — D-05 で identity 文字列を AI / UI / API で同一化)
    - classify(ext) 関数で 5 種 (image / markdown / csv / text / unsupported) を判定 (UI-SPEC L418-428)
    - 4 種 renderer は lazy import + Suspense でロード (PATTERNS.md §"Plan 05-A" のパターン)
    - size cap: image 10MB、text/markdown/csv は 1MB。超過時は "ファイルが大きすぎてプレビューできません" + DL CTA バナー (UI-SPEC L271-275)
    - エラーバナー: 401 (auth) / 404 (missing) / fetch fail を分けて表示 (UI-SPEC L267-271)
    - ImagePreview: `<img src={url}>` のみ、size cap 10MB
    - MarkdownPreview: `fetch(url).then(r => r.text())` → `<ReactMarkdown remarkPlugins={[remarkGfm]}>`、`MarkdownMessage` は呼ばない (UI-SPEC L437-441)
    - CsvPreview: `fetch().then(text)` → `parseCsv(text)` で `MarkdownTableData` shape に整形 → `ChatAgGridTable` を lazy import で流用、行数 1000 行 cap (UI-SPEC L443-447)
    - TextPreview: `fetch().then(text)` → Monaco `<Editor>` readOnly、ext → Monaco language ID マッピングは MarkdownMessage の LANG_ALIASES を **複製** (export 化は本 plan の scope 外、planner 判断 — UI-SPEC L737-742 の planner 判断ポイント) または直接 import (MarkdownMessage が export していなければ複製で OK)
  </behavior>
  <action>
    1. **`frontend/src/components/AttachmentModal.tsx`** (NEW、PATTERNS.md §"Plan 05-A" 完全踏襲):
       - Props: `{ threadId: string; attachment: AttachmentMeta; open: boolean; onClose: () => void }` (UI-SPEC L359-365)
       - `buildFileUrl(threadId, name, kind)`: kind ベース URL 切替 (UI-SPEC L487-492)
       - `classify(ext): PreviewKind`: image / markdown / csv / text / unsupported (UI-SPEC L671-680)
       - lazy import: `const ImagePreview = lazy(() => import('./preview/ImagePreview'))` 等 4 つ
       - useEffect: Esc keydown listener + body overflow lock + cleanup
       - useEffect: 初回 mount 時に「ダウンロード」CTA に focus
       - useRef + focus trap: Tab で `× 閉じる ↔ ダウンロード CTA ↔ body 内 focusable` を循環 (UI-SPEC L471)
       - createPortal でレンダリング (UI-SPEC L369-389)
       - Header: filename + meta (`✨ AI 生成 ・ size ・ timestamp` または `📎 添付 ・ size ・ timestamp`) + 「ダウンロード」CTA `<a download={name} href={url}>` + 「×」閉じる button
       - Body: `<Suspense fallback={<LoadingDots />}>` + dispatch (classify 結果に応じて renderer 1 つ + unsupported なら DownloadOnly フォールバック)
       - エラーバナー: 401/404/fetch fail を区別 (UI-SPEC §Empty / Error / Size-Cap States)
       - Size cap 案内: 画像 10MB / text 系 1MB 超過時に accent-subtle バナー + DL CTA (UI-SPEC L272-273)
       - Token: UI-SPEC §Token Reuse Contract の通り、`var(--color-surface-elevated)` / `var(--color-border)` / `var(--color-accent)` 等を全て使う (新規 CSS 変数追加禁止 — Checker #1)

    2. **`frontend/src/components/preview/ImagePreview.tsx`** (NEW、PATTERNS.md §"Plan 05-B"):
       ```tsx
       export default function ImagePreview({ url, alt }: { url: string; alt: string }) {
         return <img src={url} alt={alt}
                style={{ maxWidth: '100%', maxHeight: '100%', objectFit: 'contain',
                         borderRadius: 'var(--radius-md)', border: '1px solid var(--color-border)' }} />;
       }
       ```
       - サムネ生成しない (UI-SPEC L434 / D-23 同方針)
       - size cap 10MB は **AttachmentModal 側** で attachment.size を見て fetch 前に弾く

    3. **`frontend/src/components/preview/MarkdownPreview.tsx`** (NEW、PATTERNS.md §"Plan 05-C"):
       - `import ReactMarkdown from 'react-markdown'; import remarkGfm from 'remark-gfm';` (MarkdownMessage は呼ばない、UI-SPEC L437-441)
       - `useEffect` で fetch → setText、error 時は error banner
       - `<div className="attachment-modal-md" style={{ padding: 'var(--space-4)', ... }}>` で wrap
       - PREVIEW_TEXT_CAP_BYTES = 1024 * 1024、超過時は size cap 案内
       - LoadingDots / ErrorBanner は AttachmentModal で共通定義しても、Preview 内 minimal 表示でもどちらでも (planner 判断)

    4. **`frontend/src/components/preview/CsvPreview.tsx`** (NEW、PATTERNS.md §"Plan 05-D"):
       - `const ChatAgGridTable = lazy(() => import('../ChatAgGridTable'));` で lazy import
       - `parseCsv(text): MarkdownTableData` ヘルパー: split + 簡易 quote 処理、papaparse 不要 (UI-SPEC L444 / RESEARCH §Standard Stack §6)
       - MAX_PREVIEW_ROWS = 1000 で先頭 1000 行のみ抽出、超過時は preview 内 上部に「先頭 1000 行のみ表示しています」バナー (UI-SPEC L275)
       - CSV_CAP_BYTES = 1024 * 1024 (size cap 1MB)
       - `<Suspense><ChatAgGridTable data={tableData} theme={theme} /></Suspense>` で render

    5. **`frontend/src/components/preview/TextPreview.tsx`** (NEW、PATTERNS.md §"Plan 05-E"):
       - `import Editor from '@monaco-editor/react';` (MarkdownMessage と同 dep)
       - `import { useCurrentTheme } from '../../contexts/ThemeContext';` で theme 取得
       - `const LANG_ALIASES: Record<string, string>` をローカル定義 (MarkdownMessage.tsx L50-66 から複製、planner 判断)
       - `fetch().then(text)` + size cap 1MB
       - `<Editor value={text} language={LANG_ALIASES[ext] ?? 'plaintext'} theme={isDark ? 'vs-dark' : 'vs'} options={{ readOnly: true, minimap: { enabled: false }, scrollBeyondLastLine: false, fontSize: 13, wordWrap: 'on' }} height="calc(90vh - 120px)" />` (UI-SPEC L763-769)

    6. **新規 npm パッケージゼロ確認** (UI-SPEC Checker #2): `git diff main -- frontend/package.json frontend/bun.lockb` で diff なし。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph/frontend &amp;&amp; bun run tsc --noEmit 2>&amp;1 | tee /tmp/38-05-tsc.log; grep -E "error TS" /tmp/38-05-tsc.log | wc -l | xargs -I {} test {} -eq 0 ; echo "--- new dep check ---" ; git diff main -- frontend/package.json frontend/bun.lockb | head -5</automated>
  </verify>
  <acceptance_criteria>
    - source assertion: `test -f frontend/src/components/AttachmentModal.tsx` exit 0
    - source assertion: 4 つの preview component が存在 — `ls frontend/src/components/preview/ImagePreview.tsx frontend/src/components/preview/MarkdownPreview.tsx frontend/src/components/preview/CsvPreview.tsx frontend/src/components/preview/TextPreview.tsx` exit 0
    - source assertion: `grep -c "role=\"dialog\"" frontend/src/components/AttachmentModal.tsx` count >= 1 (UI-SPEC Checker #14)
    - source assertion: `grep -c "aria-modal" frontend/src/components/AttachmentModal.tsx` count >= 1 (Checker #14)
    - source assertion: `grep -c "ReactMarkdown\|react-markdown" frontend/src/components/preview/MarkdownPreview.tsx` count >= 1 (Checker #12: ReactMarkdown 直呼び)
    - source assertion: `grep -c "MarkdownMessage" frontend/src/components/AttachmentModal.tsx frontend/src/components/preview/MarkdownPreview.tsx` count 0 (Checker #12: MarkdownMessage 呼ばない)
    - source assertion: `grep -c "ChatAgGridTable" frontend/src/components/preview/CsvPreview.tsx` count >= 1 (Checker #11)
    - source assertion: `grep -c "readOnly: true" frontend/src/components/preview/TextPreview.tsx` count >= 1 (Checker #13)
    - source assertion: `grep -c "kind === 'generated' ? 'outputs' : 'attachments'\|outputs.*attachments\|attachments.*outputs" frontend/src/components/AttachmentModal.tsx` count >= 1 (Checker #21)
    - source assertion: size cap 確認 — `grep -E "10 \* 1024 \* 1024|10485760" frontend/src/components/AttachmentModal.tsx frontend/src/components/preview/*.tsx` ヒット (画像 10MB)、`grep -E "1024 \* 1024" frontend/src/components/AttachmentModal.tsx frontend/src/components/preview/*.tsx` ヒット (text 1MB) (Checker #10)
    - 新規 npm dep ゼロ: `git diff main -- frontend/package.json frontend/bun.lockb | wc -l` count 0 (Checker #2)
    - 新規 CSS 変数ゼロ: `git diff main -- frontend/src/theme.css | grep -E "^\\+\s*--color-|^\\+\s*--space-|^\\+\s*--radius-|^\\+\s*--font-" | wc -l` count 0 (Checker #1)
    - behavior assertion: `bun run tsc --noEmit` exit 0 (型エラーゼロ)
    - source assertion (FOUT-03 renderer dispatch 構造担保 — checker B2 / plan-checker Nyquist Dim 8a):
      - `grep -E "case 'image'" frontend/src/components/AttachmentModal.tsx` exit 0
      - `grep -E "case 'markdown'" frontend/src/components/AttachmentModal.tsx` exit 0
      - `grep -E "case 'csv'" frontend/src/components/AttachmentModal.tsx` exit 0
      - `grep -E "case 'text'" frontend/src/components/AttachmentModal.tsx` exit 0
      - `grep -E "function classify\(|const classify" frontend/src/components/AttachmentModal.tsx` exit 0
      これにより 4 renderer dispatch 経路と classify ヘルパー存在が automated に検証される (実機 dispatch 挙動は Task 3 manual で confirm)
  </acceptance_criteria>
  <done>AttachmentModal が 4 種 renderer dispatch + URL 切替 + size cap + focus trap で完成、TypeScript ビルド green、新規 npm/CSS dep ゼロ。MessageArea で mount するだけで動作する状態。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MessageArea.tsx の AttachmentChipRow を kind 別 micro-badge + button 化 + AttachmentModal mount に拡張する</name>
  <files>frontend/src/components/MessageArea.tsx</files>
  <read_first>
    - frontend/src/components/MessageArea.tsx L1-120 全体 (AttachmentChipRow + import 部)
    - .planning/phases/38-worker-dl/38-PATTERNS.md §"Plan 05-F AttachmentChipRow kind 対応" (L778-845) — 拡張ポイント完全スニペット
    - .planning/phases/38-worker-dl/38-UI-SPEC.md §"Copywriting Contract §AttachmentChipRow kind 拡張" L228-247 — aria-label / tooltip / micro-badge 文言
    - .planning/phases/38-worker-dl/38-UI-SPEC.md §"Component Contracts §AttachmentChipRow kind 拡張" L297-352 — DOM レイアウト
    - .planning/phases/38-worker-dl/38-UI-SPEC.md §"Token Reuse Contract" L46-80 — micro-badge token 仕様
    - Task 1 で実装した AttachmentModal の props
  </read_first>
  <behavior>
    - AttachmentChipRow の aria-label が `添付・AI 生成ファイル {n} 件` に変更される (UI-SPEC L232)
    - 各チップ (画像 / text-code) が `<button>` 要素で包まれ、`aria-haspopup="dialog"`、`aria-label` が kind に応じた文言 (UI-SPEC L233-239)、クリックで `onOpenModal(a)` が呼ばれる
    - 画像チップ: サムネ右下に micro-badge (kind=generated なら `✨ AI 生成`、kind=user_upload なら `📎 添付`)。badge の background / color は token を kind で切替 (UI-SPEC §Token Reuse Contract / §kind ラベル)
    - text/code チップ: pill 左端に同 micro-badge
    - MessageArea コンポーネントレベルで `activeAttachment` state を保持、AttachmentModal を 1 つだけ mount (UI-SPEC L513-516 — 多重 modal 禁止)
    - hover/focus ring: `outline: 1px solid var(--color-accent); outline-offset: 1px` (UI-SPEC L342, accent reserved-for #10)
  </behavior>
  <action>
    1. `MessageArea.tsx` の `AttachmentChipRow` (L52-115) を以下に拡張 (PATTERNS.md §"Plan 05-F" のスニペット完全踏襲):
       (a) Props に `onOpenModal: (a: AttachmentMeta) => void` を追加
       (b) `aria-label` を `添付・AI 生成ファイル ${attachments.length} 件` に変更
       (c) 画像チップ (`isImage && threadId && a.storage_name`) を `<button>` でラップ:
           - `type="button"`、`onClick={() => onOpenModal(a)}`、`aria-haspopup="dialog"`
           - `aria-label`: kind に応じて `AI が生成した画像: ${a.name}（${size}）` または `添付画像: ${a.name}（${size}）`
           - 内部に `<img>` + 右下 absolute の `<span>` micro-badge (kind に応じて `✨ AI 生成` / `📎 添付`、background は kind 別)
           - hover/focus ring 用 inline style: `outline: 1px solid var(--color-accent)` を `:hover` / `:focus-visible` で表現 (CSS class でも inline でも OK、Phase 36 のパターンに合わせる)
       (d) text/code チップ (`<span>` だった部分) を `<button>` でラップ:
           - `aria-label` を kind 別に
           - pill 左端に micro-badge を inline (badge → 絵文字 → filename の順)
       (e) URL 構築は kind ベース: 画像 src には `buildFileUrl(threadId, kind === 'generated' ? a.name : a.storage_name, a.kind)` を使う (UI-SPEC L487-492)
       - 注意: kind=generated の場合は `a.name` を URL の `{name}` パラメータに渡す (D-05: timestamp prefix 付き実体名を identity として通す)
       - kind=user_upload の場合は既存通り `a.storage_name`

    2. MessageArea コンポーネント本体 (AttachmentChipRow を呼んでいる箇所) に state と modal mount を追加:
       (a) `const [activeAttachment, setActiveAttachment] = useState<AttachmentMeta | null>(null);` を追加
       (b) AttachmentChipRow に `onOpenModal={setActiveAttachment}` を渡す
       (c) JSX 末尾 (input bar の後) に AttachmentModal をconditional render:
           ```tsx
           {activeAttachment && activeThreadId && (
             <AttachmentModal
               threadId={activeThreadId}
               attachment={activeAttachment}
               open
               onClose={() => setActiveAttachment(null)}
             />
           )}
           ```
       (d) `import { AttachmentModal } from './AttachmentModal';` を追加 (default export ならそれに合わせる)

    3. `_formatHistorySize` は既存 helper (L117-121) をそのまま再利用、kind ラベル取得は inline ヘルパー or 直接書く (`a.kind === 'generated' ? '✨ AI 生成' : '📎 添付'`)

    4. **buildFileUrl** ヘルパーは AttachmentModal に既に実装済 (Task 1) なので、MessageArea は AttachmentModal から import する or 同形のローカル関数を ChipRow 内に定義 (PATTERNS.md §"Plan 05-A" / 05-F どちらも export を要求していない、planner 判断で重複 OK)。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph/frontend &amp;&amp; bun run tsc --noEmit 2>&amp;1 | tee /tmp/38-05-task2-tsc.log; grep -E "error TS" /tmp/38-05-task2-tsc.log | wc -l | xargs -I {} test {} -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - source assertion: `grep -c "<button" frontend/src/components/MessageArea.tsx` count >= 2 (画像 + text/code 両チップが button 化)
    - source assertion: `grep -c "aria-haspopup=\"dialog\"" frontend/src/components/MessageArea.tsx` count >= 1 (Checker #22)
    - source assertion: `grep -E "AI 生成|user_upload|generated" frontend/src/components/MessageArea.tsx` ヒット (Checker #5)
    - source assertion: `grep -c "AttachmentModal" frontend/src/components/MessageArea.tsx` count >= 2 (import + mount)
    - source assertion: `grep -c "activeAttachment" frontend/src/components/MessageArea.tsx` count >= 1 (state)
    - source assertion: `grep -E "添付・AI 生成ファイル" frontend/src/components/MessageArea.tsx` ヒット (aria-label 更新、Checker #9)
    - source assertion: micro-badge `✨ AI 生成` と `📎 添付` の文字列が両方ヒット — `grep "✨ AI 生成" frontend/src/components/MessageArea.tsx && grep "📎 添付" frontend/src/components/MessageArea.tsx` exit 0
    - behavior assertion: `bun run tsc --noEmit` exit 0
    - source assertion: 既存 `_formatHistorySize` 関数が破壊されていない — `grep -c "_formatHistorySize" frontend/src/components/MessageArea.tsx` count >= 2
  </acceptance_criteria>
  <done>AttachmentChipRow が kind 別 micro-badge 表示 + button クリックで AttachmentModal が開く挙動になり、TypeScript ビルド green。docker compose up + Chrome DevTools MCP で実機検証する準備が整った状態。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 実機 docker compose + Chrome DevTools MCP で UI-SPEC Checker 22 項目を視覚検証する (Manual-only — VALIDATION 38-05-01)</name>
  <what-built>
    - AttachmentModal + 4 種 renderer + AttachmentChipRow kind 拡張
    - kind=generated / user_upload 別 micro-badge 表示
    - kind ベース URL 切替 (`/outputs/` vs `/attachments/`)
    - size cap 案内 + エラーバナー + focus trap + dark mode + モバイル幅
  </what-built>
  <how-to-verify>
    1. `docker compose up -d` でコンテナ起動。Chromium が `--remote-debugging-port=9222` で起動済なことを確認 (CLAUDE.md §Chrome DevTools MCP)。
    2. `http://localhost:5173/orochi/` で login → Chat アプリで execute_python が画像を生成する prompt を送信 (例: 「matplotlib で sin カーブの PNG を保存して」)。
    3. AI 応答にチップが現れる → ✨ AI 生成 micro-badge が見える → チップクリック → AttachmentModal が画像を表示。
    4. AttachmentModal の「ダウンロード」CTA をクリック → ブラウザがファイルを保存できる。
    5. 別 prompt で CSV / Markdown / .py を生成 → 各 renderer が動作する (CSV は ag-grid、MD は react-markdown、.py は Monaco syntax highlight)。
    6. **過去スレッド再取得**: スレッドを切り替えて元のスレッドに戻る → AI message に bundle されたチップが復元される (FOUT-04 sc4)。
    7. **multi-user isolation**: 別 user JWT で `curl -b "access_token=<other-user-jwt>" http://localhost:8000/api/threads/<tid>/outputs/<name>` を実行 → 401 / 404 が返る (FOUT-04 sc5)。
    8. **size cap**: 大きいファイル (>1MB text) を生成 → size cap 案内 + DL CTA が出る、画像 >10MB なら同様。
    9. **dark mode**: `[data-theme="dark"]` 切替で Modal が破綻しない (Checker #16)。
    10. **モバイル幅 375px** (Chrome DevTools MCP の viewport 切替): Modal が full-screen 化、CTA が full-width block 化 (Checker #17)。
    11. **PDF / 非対応形式**: 「Download only」フォールバック案内 + DL CTA (Checker #20)。
    12. **focus trap**: Tab で `× ↔ ダウンロード CTA ↔ body` 内を循環、外に出ない (Checker #15)。

    UI-SPEC §"Phase 38 Checker Acceptance Criteria" L617-643 の 22 項目すべてを 1 つずつ確認する。
  </how-to-verify>
  <resume-signal>Type "approved" (22 項目すべて pass) または問題箇所を列挙</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ outputs route | URL 構築時に `{name}` を encodeURIComponent する必要 |
| Modal ↔ fetched content | Markdown / CSV / Text の text は untrusted、xss 対策 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-05-01 | Tampering | filename に `../` を含むデータが UI に到達 (API は防御済) | mitigate | `encodeURIComponent(name)` で URL 構築、fetch が失敗すれば error banner 表示 |
| T-38-05-02 | Tampering | Markdown の HTML injection | mitigate | ReactMarkdown のデフォルト sanitize は HTML を escape (UI-SPEC L979) |
| T-38-05-03 | DoS | 巨大ファイルでブラウザクラッシュ | mitigate | size cap 1MB/10MB を fetch 前に attachment.size でガード (UI-SPEC L519-520, RESEARCH §Pitfall 7) |
| T-38-05-04 | Information Disclosure | 別 user の `_generated/` 取得 (Modal 経由) | accept (API 層で防御済) | outputs route が 401/404 を返す → Modal 内エラーバナー表示 (UI-SPEC L524-527) |
| T-38-05-05 | Tampering | 多重 modal で focus / scroll lock が破綻 | mitigate | 1 modal mount per MessageArea (UI-SPEC L513-516)、useEffect cleanup で確実に restore |
</threat_model>

<verification>
- TypeScript ビルド: `cd frontend && bun run tsc --noEmit` exit 0
- 新規 dep ゼロ: `git diff main -- frontend/package.json frontend/bun.lockb` 空
- UI-SPEC Checker: 22 項目すべてが Task 3 の手動検証で pass
- 既存 frontend 機能の回帰なし (Phase 36 アップロード添付の UX が壊れない)
</verification>

<success_criteria>
- AttachmentModal が 4 種 renderer (image/md/csv/text) を dispatch でき、`kind === 'generated'` で `/outputs/`、それ以外で `/attachments/` に URL を分岐する (FOUT-03 全種)
- AttachmentChipRow が kind 別 micro-badge 表示 + button 化 + AttachmentModal mount で動作する
- 過去スレッド再オープン時に AI message に bundle されたチップ (Plan 04 で永続化済) が復元され、モーダルで再閲覧できる (FOUT-04 sc4)
- 別 user JWT で `/api/threads/{tid}/outputs/{name}` を叩いて 401/404 が返り、Modal 内エラーバナーが表示される (FOUT-04 sc5、Phase 36 helper の継承)
- 新規 npm/CSS dep ゼロで実装が完了 (UI-SPEC Checker #1/2)
- 22 項目の UI-SPEC Checker Acceptance Criteria が人手で全 pass
</success_criteria>

<output>
After completion, create `.planning/phases/38-worker-dl/38-05-SUMMARY.md`
</output>
