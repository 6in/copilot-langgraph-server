# Phase 38 — Deferred Items

本 phase の plan 実行中に発見された、scope 外の pre-existing 問題を記録する。
ファイルは executor が出会った時点で書き加える。

---

## Plan 38-01 で発見された pre-existing TypeScript エラー (本 plan の scope 外)

`bun run tsc -b --force` 実行時に検出された 7 件のエラー。**本 plan の変更前から base branch (7a0c495) に存在する** ことを `git stash` で確認済。本 plan は AttachmentMeta.kind の enum 化のみを scope としており、これらは Rule 4 / Scope Boundary により未修正のまま deferred する。

| File | Error | 由来 |
|------|-------|------|
| `src/hooks/useThreads.ts:94` | TS2561: `bulkRemoveThreads` property does not exist on type `UseThreadsReturn` | useThreads return 型に `bulkRemoveThreads` が宣言されていない / 5 consumer がそれを破壊的に参照している |
| `src/components/ChatApp.tsx:53` | TS2339: 同上 | 同上 (consumer) |
| `src/components/CanvasChatApp.tsx:69` | TS2339: 同上 | 同上 (consumer) |
| `src/components/DebateChatApp.tsx:545` | TS2339: 同上 | 同上 (consumer) |
| `src/components/GemChatApp.tsx:47` | TS2339: 同上 | 同上 (consumer) |
| `src/components/SuperChatApp.tsx:135` | TS2339: 同上 | 同上 (consumer) |
| `src/components/MermaidBlock.tsx:13` | TS2459: `Theme` is not exported from `../contexts/ThemeContext` | ThemeContext.ts が `Theme` 型を export しない / Mermaid 側で import している |

**修正方針 (本 plan 外):**

- `useThreads` の return 型に `bulkRemoveThreads: (ids: string[]) => Promise<void>` を追加し、ThreadSidebar の bulk delete UI と整合させる。
- `ThemeContext.ts` を `export type Theme = ...` で公開し、`MermaidBlock` の lazy import が解決できるようにする。

**いつ取り上げるか:** Phase 38 完了後の `/gsd-verify-work` で full-suite 型チェックを行ったタイミング、もしくは v6.1+ Polish phase で。

---

## Plan 38-02 で発見された pre-existing pytest failure (本 plan の scope 外)

`tests/test_generate_mcp_artifacts.py::test_load_tools_has_six_tools` が **本 plan の変更前から失敗している** (base ブランチ `0422b3b` で同コマンドが同じ AssertionError で fail することを `git stash` で確認済)。

**現象:**

```
assert len(tools) == 6
E   assert 8 == 6
```

**原因:** Phase 37 で `attachments_list` / `attachments_extract` の 2 ツールが追加されたが、`tests/test_generate_mcp_artifacts.py` の hardcoded `== 6` を更新する作業が漏れている。Phase 37 のテスト整備の取りこぼし。

**本 plan で扱わない理由:**

- Plan 38-02 の責務は outputs.py route 新設 + attachments_list の `_generated/` + `kind` 拡張 + YAML SSoT 更新の 3 点で、tools 数は 8 のまま (本 plan で 9 にも増減もしない)。
- 数値定数を 6 → 8 に書き換えるだけだが、Phase 37 のテスト管理外データ。本 plan 由来ではない。

**修正方針 (本 plan 外):**

`tests/test_generate_mcp_artifacts.py:40` の `assert len(tools) == 6` を `== 8` に更新する。

**いつ取り上げるか:** Phase 38 完了後の `/gsd-verify-work`、もしくは v6.1+ Polish。

---

---

## Plan 38-04 で再確認した pre-existing pytest failure (本 plan の scope 外)

Plan 03 で `claude_code` の cwd 引数を破壊的に削除した余波で、`tests/test_mcp_server.py` の claude_code 関連 7 件 (`test_claude_code_env_sanitized` / `test_claude_code_returns_output` / `test_claude_code_timeout_terminate` / `test_claude_code_timeout_kill_escalation` / `test_claude_code_truncation` / `test_claude_code_cli_missing` / `test_db_query_select_returns_rows`) が `TypeError: claude_code() got an unexpected keyword argument 'cwd'` で fail している。Plan 38-03 の summary でも触れられている既知の deferred 項目。

**本 plan で扱わない理由:**

- Plan 38-04 の責務は handler bundle / API 正規化 / attachments_helper kind 拡張で、MCP tool 単体テストの assertion update 対象は本 plan の scope 外。
- 修正は 1 行 ((`cwd="/tmp"` 引数渡しを除く)) だが、Plan 03 で意図的に破壊した signature を再定義し直すような fixup は本 plan の責務外。

**いつ取り上げるか:** Phase 38 完了後の `/gsd-verify-work`、もしくは v6.1+ Polish。

---

*Generated by: gsd-executor while executing Plan 38-01 / 38-02 / 38-04*
*Date: 2026-05-12*
