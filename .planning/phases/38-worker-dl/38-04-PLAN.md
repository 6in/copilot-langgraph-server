---
phase: 38
plan: 4
plan_id: 38-04-handler-bundle-and-helper
type: execute
wave: 2
depends_on:
  - 38-01-types-and-roundtrip-gate
  - 38-02-backend-route-and-mcp-list
  - 38-03-sandbox-cwd-and-rename
files_modified:
  - app/jobs/handlers/attachments_helper.py
  - app/jobs/handlers/langgraph_handler.py
  - app/api/routes/attachments.py
  - app/api/routes/chat.py
  - tests/test_langgraph_handler_outputs_bundle.py
  - tests/test_outputs_route.py
autonomous: true
requirements: [FOUT-01, FOUT-02, FOUT-04]
tags: [langgraph-handler, attachments-helper, message-bundle, additional_kwargs, legacy-normalization]
objective: "scan_thread_attachments を `_generated/` 含む + kind 付与に拡張、langgraph_handler が turn 完了時に AIMessage.additional_kwargs.attachments に kind=generated を bundle、API _messages_to_response で legacy `kind: 'file'` を `'user_upload'` に正規化、attachments.py upload route の hardcoded `kind: 'file'` を `'user_upload'` に置換"

must_haves:
  truths:
    - "scan_thread_attachments が user_upload (直下) と generated (_generated/) の flat list を返し、各 entry に kind フィールドを持つ"
    - "build_attachments_hint が SystemMessage prepend 文字列に kind ラベル ('[AI 生成]' / '[添付]') を表示する"
    - "langgraph_handler が turn 完了 (graph.astream_events の on_chain_end) 後に `_generated/` の delta を scan し、final AIMessage.additional_kwargs.attachments に kind=generated エントリを bundle する"
    - "additional_kwargs の merge は `(existing or {}) | new_dict` で行い、既存フィールドを潰さない (RESEARCH Pitfall 2)"
    - "API _messages_to_response が DB の legacy `kind: 'file'` を API 出力時に `'user_upload'` に正規化する (RESEARCH Open Question 2 / Example 1)"
    - "attachments.py upload route の hardcoded `'kind': 'file'` が `'kind': 'user_upload'` に書き換わり、新規 upload は最新型で永続化される"
    - "claude_code 生成ファイルも GET /api/threads/{tid}/outputs/{name} で取得できる (FOUT-02、Plan 03 完了で前提条件達成)"
    - "D-13: SystemMessage prepend / AIMessage bundle はチップ + AttachmentModal プレビュー経路を支える metadata 復元の中核 — AI 応答テキスト内 inline 描画 (`![]()`) は行わず、Phase 36 の AttachmentChip / AttachmentModal を kind 対応に拡張する経路に揃える (CONTEXT.md)"
    - "D-14: build_attachments_hint の kind ラベル文字列 ('[AI 生成]' / '[添付]') と scan_thread_attachments の kind 値 ('generated' / 'user_upload') を、Plan 05 の AttachmentChipRow micro-badge ('✨ AI 生成' / '📎 添付') と意味的に同期させる (CONTEXT.md)"
    - "D-16: 過去スレッドからの再取得は ThreadSidebar 経由でスレッドを開き直し、AIMessage.additional_kwargs.attachments に bundle された metadata から AttachmentChipRow を復元する経路で閉じる — 横断 'My Files' は v6.1+ deferred (CONTEXT.md)"
    - "D-18: SystemMessage prepend は input (user upload) と output (generated) **両方** を含む flat list、件数制限なし。scan_thread_attachments の戻り値順序は基本 mtime 昇順だが、件数フィルタは prepend 側でも適用しない (CONTEXT.md)"
  artifacts:
    - path: "app/jobs/handlers/attachments_helper.py"
      provides: "scan_thread_attachments が kind + `_generated/` 含む、build_attachments_hint が kind ラベル表示"
      contains: "kind"
    - path: "app/jobs/handlers/langgraph_handler.py"
      provides: "turn 完了時の AIMessage bundle (Phase 38 D-15 ブロック)"
      contains: "additional_kwargs"
    - path: "app/api/routes/attachments.py"
      provides: "upload route の kind 値が 'user_upload' に統一"
      contains: "\"kind\": \"user_upload\""
    - path: "app/api/routes/chat.py"
      provides: "_messages_to_response で legacy 'file' → 'user_upload' 正規化"
      contains: "user_upload"
    - path: "tests/test_langgraph_handler_outputs_bundle.py"
      provides: "test_bundles_generated_files (38-04-03) green"
    - path: "tests/test_outputs_route.py"
      provides: "test_get_output_works_for_claude_code (38-04-02) green"
  key_links:
    - from: "app/jobs/handlers/langgraph_handler.py L228-243 final_state"
      to: "scan_thread_attachments(thread_id, github_login) 再呼び出し"
      via: "delta 抽出 → AIMessage.additional_kwargs.attachments merge"
      pattern: "additional_kwargs.*\\|.*\\{"
    - from: "app/jobs/handlers/langgraph_handler.py — RPCContext / config['configurable']['thread_id']"
      to: "scan_thread_attachments(thread_id, github_login) call site"
      via: "context.thread_id / context.github_login を turn-delta bundle 直前で取得 (既存 prepend 用 scan と同じ source、二重取得しない)"
      pattern: "context\\.thread_id|context\\.github_login|config\\[.configurable.\\]\\[.thread_id.\\]"
    - from: "app/api/routes/chat.py _messages_to_response"
      to: "Phase 36 legacy `kind: 'file'` の正規化"
      via: "API 出力時に 'file' → 'user_upload' 置換"
      pattern: "kind.*file.*user_upload|user_upload.*kind"
    - from: "app/jobs/handlers/attachments_helper.py scan_thread_attachments"
      to: "{folder}/_generated/ 配下"
      via: "第二ループで kind=generated を付与"
      pattern: "_generated"
---

<objective>
worker → API → DB → frontend の persistence path を Phase 38 仕様 (kind discriminator + AI 生成ファイル bundle) に整合させる。Plan 02 で確立した outputs route と Plan 03 で確立した `_generated/` への永続化を、handler 層と API 層で結びつける。

Purpose:
- **AI 視認性 (FOUT-04 success criteria 4 の前段)**: SystemMessage prepend に AI 生成ファイルが kind ラベル付きで現れ、AI が attachments_list + extract で input/output を区別して扱える
- **永続化 (FOUT-04 success criteria 4)**: turn 完了時に AI 最終 message に metadata bundle、AsyncPostgresSaver で JSONB 透過保存、スレッド再オープン時に frontend に復元される
- **legacy データ正規化 (D-30 完遂)**: Phase 36 で DB に書かれた `kind: 'file'` メッセージを API 層で `'user_upload'` に正規化、frontend は新型 union だけを扱えばよい状態にする
- **claude_code 経路の E2E (FOUT-02)**: Plan 03 で `_generated/` cwd 切替済の claude_code 生成物が GET /api/threads/{tid}/outputs/{name} で取得できることを test で証明

Output:
- `attachments_helper.py` scan + hint の kind 拡張
- `langgraph_handler.py` の turn-delta bundle (RESEARCH Pattern 2)
- `attachments.py` upload route の `kind: 'user_upload'` への置換
- `chat.py` `_messages_to_response` の legacy 正規化
- 2 つの追加 green テスト (test_bundles_generated_files + test_get_output_works_for_claude_code)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-worker-dl/38-CONTEXT.md
@.planning/phases/38-worker-dl/38-RESEARCH.md
@.planning/phases/38-worker-dl/38-PATTERNS.md
@.planning/phases/38-worker-dl/38-01-SUMMARY.md
@.planning/phases/38-worker-dl/38-02-SUMMARY.md
@.planning/phases/38-worker-dl/38-03-SUMMARY.md
@app/jobs/handlers/attachments_helper.py
@app/jobs/handlers/langgraph_handler.py
@app/api/routes/attachments.py
@app/api/routes/chat.py

<interfaces>
<!-- 既存 attachments_helper.py L15-42 — Plan 04 で kind + _generated/ 追加 -->
```python
def scan_thread_attachments(thread_id: str, github_login: str) -> list[dict]:
    if not thread_id or not github_login: return []
    folder = os.path.join(THREAD_FILES_DIR, github_login, thread_id)
    ...
    # 既存ループで {"name", "size", "modified_at", "ext"} を append
    # → kind: "user_upload" を加える + _generated/ サブフォルダの第二ループを追加
```

<!-- 既存 langgraph_handler.py L227-243 — Plan 04 で turn 完了後の bundle 追加 -->
```python
state_input = {"messages": messages_input, "attachments": attachments_meta or None}
final_state = None
async for event in graph.astream_events(...):
    ...
    elif kind == "on_chain_end" and event.get("name") == "LangGraph":
        final_state = event["data"].get("output")
if final_state is None:
    final_state = await graph.ainvoke(state_input, config=config)
# === Plan 04 でここに turn-delta bundle ロジックを挿入 ===
final_text = final_state["messages"][-1].content
```

<!-- 既存 chat.py L481-490 _messages_to_response — Plan 04 で legacy 正規化を追加 -->
```python
kw = getattr(msg, "additional_kwargs", None) or {}
if kw:
    public_kw: dict = {}
    atts = kw.get("attachments")
    if isinstance(atts, list) and atts:
        public_kw["attachments"] = atts   # ← ここで kind: 'file' → 'user_upload' 正規化
    ...
```

<!-- 既存 attachments.py upload route L160 -->
```python
saved.append({
    "kind": "file",   # ← 'user_upload' に置換
    "name": base,
    ...
})
```

<!-- Plan 03 で実装済の Pattern (本 plan の前提) -->
- mcp_server/tools/execute_python.py: _rename_new_outputs で生成ファイルが {ts}_{name} に揃う
- mcp_server/tools/claude_code.py: 同 helper を import 再利用、`_generated/` cwd 固定実行
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: attachments_helper.py の scan_thread_attachments を `_generated/` + kind 対応に拡張し build_attachments_hint で kind ラベル表示する</name>
  <files>app/jobs/handlers/attachments_helper.py</files>
  <read_first>
    - app/jobs/handlers/attachments_helper.py L1-60 全体 — self-analog 拡張対象
    - .planning/phases/38-worker-dl/38-PATTERNS.md §"Plan 04-A app/jobs/handlers/attachments_helper.py" (L390-472) — 第二ループ + kind ラベル表示の完全実装
    - .planning/phases/38-worker-dl/38-RESEARCH.md §"Pattern 3: SystemMessage Prepend の kind 拡張" (L414-460)
    - .planning/phases/38-worker-dl/38-CONTEXT.md D-18 — input/output 両方を flat list で含める、kind フィールドのみ追加
    - .planning/phases/38-worker-dl/38-RESEARCH.md §"Pitfall 1 / 4" — 両 scan 経路を同 wave で更新、サブフォルダは 1 段のみ
  </read_first>
  <behavior>
    - scan_thread_attachments(thread_id, github_login) が user_upload (直下) と generated (_generated/) の flat list を返し、各 entry に `kind` フィールドを必ず含む。
    - 戻り値の dict shape は既存 4 フィールド (name / size / modified_at / ext) + kind の 5 フィールド。
    - build_attachments_hint が `kind == "generated"` のエントリに `[AI 生成]`、それ以外に `[添付]` を表示する文字列を返す。
    - 既存テスト (Phase 37 起因の test_*) が壊れない (kind フィールド追加は backward-compatible)。
  </behavior>
  <action>
    1. `app/jobs/handlers/attachments_helper.py` の `scan_thread_attachments` を拡張 (PATTERNS.md §"Plan 04-A" の完全スニペット踏襲):
       - 既存ループ末尾の append dict に `"kind": "user_upload"` を追加
       - 既存ループ直後 (return result の前) に `_generated/` サブフォルダ用の第二ループを追加:
         - `gen_folder = os.path.join(folder, "_generated")`
         - `if os.path.isdir(gen_folder):` ガード
         - try/except OSError 込みで listdir
         - 各エントリは name / size / modified_at / ext + `"kind": "generated"`
         - サブフォルダ 1 段のみ降りる (再帰しない、Pitfall 4)
         - `os.path.isfile` チェック (Pitfall 4)

    2. `build_attachments_hint` を kind ラベル対応に拡張 (PATTERNS.md §"Plan 04-A" コード踏襲):
       ```python
       kind_label = "[AI 生成]" if a.get("kind") == "generated" else "[添付]"
       lines.append(f"- {a['name']} ({size_str}, {a['ext']}) {kind_label}")
       ```
       - 既存 entry の文末に `[添付]` / `[AI 生成]` を付ける形 (Phase 37 hint 構造を破壊しない)
       - 後段の説明文「内容を読むには `attachments_extract` ツールを...」は維持

    3. 既存テスト確認: `tests/test_attachments_helper*.py` が存在すれば、kind フィールド追加で fail しないことを確認。もし排他 assertion (`assert "kind" not in entry`) があれば緩和。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph &amp;&amp; uv run pytest tests/test_attachments_helper.py tests/test_langgraph_handler_attachments.py -x -v 2>&amp;1 | tail -30 ; echo "--- grep check ---" ; grep -c "kind" app/jobs/handlers/attachments_helper.py</automated>
  </verify>
  <acceptance_criteria>
    - source assertion: `grep -c '"kind": "user_upload"' app/jobs/handlers/attachments_helper.py` count >= 1
    - source assertion: `grep -c '"kind": "generated"' app/jobs/handlers/attachments_helper.py` count >= 1
    - source assertion: `grep -E 'gen_folder = os\.path\.join\(folder, "_generated"\)' app/jobs/handlers/attachments_helper.py` ヒット
    - source assertion: `grep -E '"\[AI 生成\]"|"\[添付\]"' app/jobs/handlers/attachments_helper.py` ヒット (kind ラベル表示)
    - source assertion: `grep -c "os.walk\|rglob" app/jobs/handlers/attachments_helper.py` count 0 (再帰禁止 — Pitfall 4)
    - test command: 既存 attachments_helper / langgraph_handler_attachments テストが green (回帰なし)
    - behavior assertion: scan 結果に user_upload と generated の両方の kind が含まれることを既存テストまたは Task 2 で間接 assert
  </acceptance_criteria>
  <done>scan_thread_attachments と build_attachments_hint が `_generated/` + kind 対応に拡張され、回帰ゼロ。Task 2 の bundle ロジックと Task 4 の Modal 描画でこの戻り値型を消費できる。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: langgraph_handler.py の turn 完了で AIMessage.additional_kwargs.attachments に kind=generated を bundle する</name>
  <files>app/jobs/handlers/langgraph_handler.py, tests/test_langgraph_handler_outputs_bundle.py</files>
  <read_first>
    - app/jobs/handlers/langgraph_handler.py L100-160 (`_prepare_messages_input`) と L177-260 (graph 実行 + final_state 取得) — 既存全体
    - .planning/phases/38-worker-dl/38-PATTERNS.md §"Plan 04-B app/jobs/handlers/langgraph_handler.py" (L476-528) — turn-delta bundle の完全実装
    - .planning/phases/38-worker-dl/38-RESEARCH.md §"Pattern 2: AIMessage.additional_kwargs.attachments で AI 生成ファイルを turn 単位で bundle" (L363-413)
    - .planning/phases/38-worker-dl/38-RESEARCH.md §"Pitfall 2" (L756-760) — `(existing or {}) | new_dict` で merge
    - .planning/phases/38-worker-dl/38-RESEARCH.md §"Pitfall 5" (L773-776) — handler は scan して bundle するだけ、二重 rename しない
    - .planning/phases/38-worker-dl/38-RESEARCH.md §"Open Question 4" (L891-894) — turn 完了 (on_chain_end) 後の scan で race condition なし
    - .planning/phases/38-worker-dl/38-CONTEXT.md D-15 — turn 完了で AI 最終 message に bundle
    - .planning/patterns.md §"HumanMessage.additional_kwargs サイドカー envelope" (L79-85)
    - Plan 01 で実装済の test_round_trip_postgres (前提担保)、Plan 01 で scaffold した test_bundles_generated_files
  </read_first>
  <behavior>
    - graph 実行後、`scan_thread_attachments(thread_id, github_login)` を再呼び出しして post-turn の flat list を取得
    - turn 開始時の `attachments_meta` (state_input に渡したもの) との差分を取り、kind=='generated' のエントリだけを抽出 (turn 中に生成された分)
    - delta が空でなければ、final AIMessage の additional_kwargs に **merge** で attachments を追加 (`(existing or {}) | {"attachments": turn_delta}` で既存フィールドを潰さない)
    - test_bundles_generated_files: 最小 graph + mock subprocess で turn 完了後の AIMessage.additional_kwargs.attachments に kind=generated エントリが入ることを assert (38-04-03)
  </behavior>
  <action>
    1. `app/jobs/handlers/langgraph_handler.py` L241 (final_state 確定直後) と L243 (`final_text = final_state["messages"][-1].content`) の間に Phase 38 D-15 bundle ロジックを挿入 (PATTERNS.md §"Plan 04-B" の完全実装):
       ```python
       # === Phase 38 D-15: turn 完了で AI 最終 message に generated delta を bundle ===
       # Pattern 2 (RESEARCH): handler レベルで再 scan して kind=generated だけ抽出
       # (tool wrapper 側の rename と二重カウントしない設計 — Pitfall 5)
       post_turn_meta = scan_thread_attachments(thread_id, github_login)
       prev_generated_names = {
           a["name"] for a in (attachments_meta or [])
           if isinstance(a, dict) and a.get("kind") == "generated"
       }
       turn_generated = [
           m for m in post_turn_meta
           if m.get("kind") == "generated"
           and m["name"] not in prev_generated_names
       ]
       if turn_generated:
           final_msg = final_state["messages"][-1]
           # Pitfall 2: additional_kwargs は None-guard + merge で既存フィールドを潰さない
           existing_kw = getattr(final_msg, "additional_kwargs", None) or {}
           final_msg.additional_kwargs = existing_kw | {"attachments": turn_generated}
           # AsyncPostgresSaver が次の checkpoint で JSONB に保存する (patterns.md L79-85)
       ```

    2. **Optional**: orchestrator_handler.py にも同 bundle ロジックを入れるか? → PATTERNS.md §"Plan 04-A/B" では POSSIBLY MODIFY と書かれている。判定基準: SuperChat (orchestrator) 経路でも execute_python / claude_code を ToolEnabledSubAgent が呼ぶなら必要。本 plan では **scope を langgraph_handler のみに限定** (FOUT-01/02 の主経路、Chat / Canvas が該当)、orchestrator (SuperChat) の bundle 対応は v6.1+ で観察ベース判断 (Plan 06 の deferred-items に明示)。

    3. `tests/test_langgraph_handler_outputs_bundle.py` の `test_bundles_generated_files` (38-04-03) を実装 (Plan 01 で skip scaffold 済の skip マーカーを外す):
       - tmp に `<login>/<tid>/` (空) と `<login>/<tid>/_generated/{ts}_new.png` を用意 (turn 中に生成された相当)
       - `THREAD_FILES_DIR` を tmp にmonkeypatch
       - 最小 graph (build_graph or mock) を AsyncPostgresSaver と組み合わせて 1 turn 走らせ、turn 完了後の final_state の最後の AIMessage の additional_kwargs.attachments に kind=='generated' エントリが含まれることを assert
       - `assert turn_generated[0]["kind"] == "generated"` + name 一致

    4. 既存テスト回帰確認: `uv run pytest tests/test_langgraph_handler.py tests/test_langgraph_handler_attachments.py -x`
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph &amp;&amp; uv run pytest tests/test_langgraph_handler_outputs_bundle.py -x -v 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - source assertion: `grep -c "scan_thread_attachments" app/jobs/handlers/langgraph_handler.py` count >= 2 (既存 prepend 用 + 新規 post-turn 用)
    - source assertion: `grep -E "turn_generated|post_turn_meta" app/jobs/handlers/langgraph_handler.py` ヒット
    - source assertion: `grep -E "existing_kw \| \{|additional_kwargs.*\|.*\{" app/jobs/handlers/langgraph_handler.py` ヒット (merge with union operator)
    - source assertion: handler 内で `_rename_new_outputs` などの **rename 操作を呼ばない** (`grep -c "_rename_new_outputs\|os.rename" app/jobs/handlers/langgraph_handler.py` count 0 — Pitfall 5)
    - test command: `uv run pytest tests/test_langgraph_handler_outputs_bundle.py::test_bundles_generated_files tests/test_langgraph_handler_outputs_bundle.py::test_round_trip_postgres -x` exit 0
    - behavior assertion: test_bundles_generated_files で final AIMessage の additional_kwargs.attachments[0]["kind"] == "generated" を assert
    - behavior assertion: 既存 `tests/test_langgraph_handler_attachments.py` が green (回帰ゼロ)
  </acceptance_criteria>
  <done>turn 完了時に AIMessage に kind=generated エントリが merge され、AsyncPostgresSaver round-trip で復元できる。test_bundles_generated_files green、Phase 38 D-15 完遂。orchestrator_handler は本 plan の scope 外 (Plan 06 deferred-items に明示)。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: API _messages_to_response で legacy kind='file' を 'user_upload' に正規化し、attachments.py upload route の hardcoded 値も置換する</name>
  <files>app/api/routes/chat.py, app/api/routes/attachments.py, tests/test_outputs_route.py</files>
  <read_first>
    - app/api/routes/chat.py L470-493 (`_messages_to_response`) — 正規化を挿入する箇所
    - app/api/routes/attachments.py L150-170 (upload route の dict 構築) — L160 の `"kind": "file"` を置換
    - .planning/phases/38-worker-dl/38-RESEARCH.md §"Example 1: AttachmentMeta 型拡張" (L805-829) — 正規化の根拠
    - .planning/phases/38-worker-dl/38-RESEARCH.md §"Open Question 2" (L881-885) — API で 'file' → 'user_upload' 正規化推奨
    - .planning/phases/38-worker-dl/38-CONTEXT.md D-06 — kind single discriminator 原則
    - Plan 01 で確定した D-30 案 A (AttachmentMeta.kind は 'user_upload' | 'generated')
    - Plan 03 完了状態: claude_code が `_generated/` に書く実装が green
    - Plan 02 で実装済の outputs route + Plan 03 完了で claude_code 経路の E2E が可能
  </read_first>
  <behavior>
    - DB から復元された legacy メッセージで `additional_kwargs.attachments[*].kind == 'file'` のエントリが API 出力時に `'user_upload'` に正規化される
    - 新規 upload (POST /api/threads/{tid}/attachments) の戻り値 dict で `kind: 'user_upload'` が直接書かれる (legacy 'file' を新規生成しない)
    - frontend は `'user_upload' | 'generated'` の union 型のみを受け取り、`'file'` を見ることがない
    - test_get_output_works_for_claude_code (38-04-02): claude_code 生成ファイル相当を `_generated/` に置いて GET /api/threads/{tid}/outputs/{name} で取得 → 200 + bytes 一致 (FOUT-02 E2E)
  </behavior>
  <action>
    1. `app/api/routes/chat.py` L484-490 の正規化 block を編集 (RESEARCH §"Open Question 2" / §"Example 1" の方針踏襲):
       ```python
       atts = kw.get("attachments")
       if isinstance(atts, list) and atts:
           # Phase 38 D-30 (案 A) 正規化: legacy 'file' → 'user_upload' で API 層で吸収
           # frontend には 'user_upload' | 'generated' の union 型のみを返す
           normalized_atts = []
           for a in atts:
               if isinstance(a, dict):
                   a_copy = dict(a)
                   if a_copy.get("kind") == "file":
                       a_copy["kind"] = "user_upload"
                   normalized_atts.append(a_copy)
               else:
                   normalized_atts.append(a)
           public_kw["attachments"] = normalized_atts
       ```
       - 元の atts を mutate せず copy を返す (LangGraph state を破壊しない)
       - kind が既に 'user_upload' / 'generated' なら touch しない

    2. `app/api/routes/attachments.py` L160 を置換:
       ```python
       # Before:
       "kind": "file",
       # After:
       "kind": "user_upload",   # Phase 38 D-30 (案 A): 新規 upload は最新型で永続化
       ```
       (新規 upload する DB 行はこれ以降 'user_upload' で書かれる、legacy 行は Task 3 の chat.py 正規化で吸収)

    3. `tests/test_outputs_route.py::test_get_output_works_for_claude_code` (Plan 02 で skip 維持) の skip マーカーを外して実装 (FOUT-02 の E2E):
       - tmp に `<login>/<tid>/_generated/{ts}_result.md` を置いて claude_code 生成物相当を擬装
       - api_client.get で取得 → 200 + content 一致を assert
       - claude_code 経路は execute_python と同じ `_generated/` cwd 設計のため、route 層から見れば同経路、本 test は **route が claude_code 起源ファイルも取得できることを路 (route) として証明**

    4. Phase 36 既存テスト回帰確認: `uv run pytest tests/test_attachments_get_delete_route.py tests/test_chat_route.py -x` で legacy データのテストがあれば、正規化後の値で assert に変化があるか確認。もし既存テストが `assert kind == "file"` のように厳密 assertion していれば、本 plan 内で `"user_upload"` に書き換える (API 出力の整合のため)。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph &amp;&amp; uv run pytest tests/test_outputs_route.py tests/test_attachments_get_delete_route.py tests/test_chat_route.py -x -v 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - source assertion: `grep -c '"kind": "user_upload"' app/api/routes/attachments.py` count >= 1
    - source assertion: `grep -c '"kind": "file"' app/api/routes/attachments.py` count 0 (legacy 値が消えている)
    - source assertion: `grep -E 'kind.*==.*"file"' app/api/routes/chat.py` ヒット (正規化分岐が存在)
    - source assertion: `grep -E 'user_upload' app/api/routes/chat.py` ヒット
    - test command: `uv run pytest tests/test_outputs_route.py::test_get_output_works_for_claude_code -x` exit 0
    - behavior assertion: 既存 `tests/test_attachments_get_delete_route.py` が green (もし `kind == "file"` を assert している箇所があれば本 task で `"user_upload"` に同時更新)
    - behavior assertion: full suite `uv run pytest tests/ -x --ignore=tests/test_api_chat.py` で Phase 38 起因の回帰ゼロ
  </acceptance_criteria>
  <done>API 層が legacy 'file' を 'user_upload' に正規化し、新規 upload も最新型で書かれる。claude_code 経路の E2E (FOUT-02) も test 1 本 green。Plan 05 (frontend) が `'user_upload' | 'generated'` の union 型のみを扱える状態。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LangGraph state ↔ AsyncPostgresSaver | dict 値が JSONB に serialize される (Pitfall 6: Enum 混在禁止) |
| API output ↔ frontend | legacy 'file' 値が漏れると frontend type-narrowing が壊れる |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-04-01 | Tampering | additional_kwargs merge で既存フィールドを潰す | mitigate | `(existing or {}) | new_dict` 形式で merge、Pitfall 2 に従う |
| T-38-04-02 | Spoofing | kind フィールドに任意値を入れる | mitigate | scan_thread_attachments は文字列リテラル 'user_upload' / 'generated' のみ生成、Pitfall 6 (Enum 禁止) も遵守 |
| T-38-04-03 | Information Disclosure | turn-delta scan で他 user の `_generated/` が漏れる | mitigate | scan_thread_attachments は `THREAD_FILES_DIR/<login>/<tid>/_generated` を構築、login が JWT 経由で worker に渡る (Phase 11-04 既存パターン) |
| T-38-04-04 | Tampering | legacy 'file' 正規化漏れで frontend type error | mitigate | API _messages_to_response で 'file' → 'user_upload' 変換、attachments.py upload route も更新で新規行は 'user_upload' |
</threat_model>

<verification>
- attachments_helper 拡張: `uv run pytest tests/test_attachments_helper.py tests/test_langgraph_handler_attachments.py -x` 全 green (回帰ゼロ)
- handler bundle: `uv run pytest tests/test_langgraph_handler_outputs_bundle.py -x` 全 green (test_round_trip_postgres + test_bundles_generated_files)
- API 正規化: `uv run pytest tests/test_outputs_route.py tests/test_attachments_get_delete_route.py tests/test_chat_route.py -x` 全 green
- 全体: `uv run pytest tests/ -x --ignore=tests/test_api_chat.py` で Phase 38 由来の回帰ゼロ
</verification>

<success_criteria>
- AI が `attachments_list()` および SystemMessage prepend で input/output を kind 付きで区別できる (FOUT-04 の AI 視認性)
- turn 完了時に AIMessage.additional_kwargs.attachments に kind=generated エントリが bundle される (FOUT-04 sc4)
- legacy `kind: 'file'` が API 層で `'user_upload'` に正規化され、frontend は新型 union のみを扱える (D-30 完遂)
- claude_code 経路の E2E (`_generated/` cwd → outputs route 取得) が test 1 本で証明される (FOUT-02)
- Plan 05 (frontend renderer) が安全な型契約のもとで進められる
</success_criteria>

<output>
After completion, create `.planning/phases/38-worker-dl/38-04-SUMMARY.md`
</output>
