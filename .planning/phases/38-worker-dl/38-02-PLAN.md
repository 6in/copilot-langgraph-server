---
phase: 38
plan: 2
plan_id: 38-02-backend-route-and-mcp-list
type: execute
wave: 1
depends_on: [38-01-types-and-roundtrip-gate]
files_modified:
  - app/api/routes/outputs.py
  - app/api/main.py
  - mcp_server/tools/attachments.py
  - config/mcp_tools.yaml
  - mcp_server/tools/mcp_helper.py
  - static/js/tool-catalog-generated.js
  - docs/mcp-tools.md
  - tests/test_outputs_route.py
  - tests/test_mcp_attachments_kind.py
autonomous: true
requirements: [FOUT-01, FOUT-02, FOUT-04]
tags: [outputs-route, mcp-attachments, ssot, _generated]
objective: "GET /api/threads/{tid}/outputs/{name} の outputs.py route 新規実装、MCP attachments_list_core を `_generated/` + `kind` フィールド対応に拡張、`config/mcp_tools.yaml` SSoT 更新と generate_mcp_artifacts.py 再生成を実施する"

must_haves:
  truths:
    - "GET /api/threads/{tid}/outputs/{name} が JWT cookie 認証下で `_generated/` 配下の raw bytes を inline 配信できる"
    - "別 user の JWT cookie で他人の thread の outputs を取得しようとすると 401/404 が返る (FOUT-04 sc5、helper 再利用による間接的継承)"
    - "URL `{name}` への `../` injection は `_safe_resolve_file` の realpath guard で 400 として拒否される"
    - "attachments_list (MCP tool) が thread フォルダ直下 + `_generated/` 配下を flat list で返し、各 entry に kind: 'user_upload' | 'generated' を含む"
    - "config/mcp_tools.yaml docstring に kind フィールドが明記され、scripts/generate_mcp_artifacts.py --check が exit 0 (自動生成 3 ファイルと drift なし)"
    - "D-05: outputs route は `GET /api/threads/{thread_id}/outputs/{name}` を新設、attachments.py の `_resolve_thread_folder` / `_safe_resolve_file` / `_normalize_basename` helper を import で再利用 (新規 helper は書かない) (CONTEXT.md)"
    - "D-04: Docker volume `thread-files` を再利用 (api=RW / mcp-server=RW / worker=RO 想定) — outputs route + MCP tool 双方が同 volume の `{login}/{tid}/_generated/` を読む (CONTEXT.md)"
    - "D-06: MCP `attachments_list` の戻り値に `kind: 'user_upload' | 'generated'` を追加して一体化拡張、新規 `outputs_list` ツールは作らない (tool 数膨張ゼロ) (CONTEXT.md)"
    - "D-07: `config/mcp_tools.yaml` を SSoT として更新し、`scripts/generate_mcp_artifacts.py --target all` で自動生成 3 ファイル (mcp_helper.py / tool-catalog-generated.js / docs/mcp-tools.md) を決定論的に再生成 (ADR-0044 規約遵守) (CONTEXT.md)"
    - "D-17: 一覧 endpoint `GET /api/threads/{tid}/outputs` は **新設しない** — Phase 38 では handler/AI 経路 (LangGraph checkpointer の message metadata 復元) で閉じる方針、本 plan の outputs route は単一ファイル取得 (`/{name}` 終端) のみ (CONTEXT.md)"
    - "D-19: multi-user isolation は Phase 36 の helper (`_resolve_thread_folder` / `_safe_resolve_file`) を import 再利用することで間接的に継承、Phase 38 では isolation **単体テストを新規追加しない** — Plan 01 scaffold の isolation/path-traversal 2 ケースは helper 再利用の smoke 検証として位置づける (CONTEXT.md)"
  artifacts:
    - path: "app/api/routes/outputs.py"
      provides: "GET /api/threads/{tid}/outputs/{name} route"
      exports: ["router", "get_output"]
      contains: "@router.get(\"/threads/{thread_id}/outputs/{name}\")"
    - path: "app/api/main.py"
      provides: "outputs router include"
      contains: "outputs.router"
    - path: "mcp_server/tools/attachments.py"
      provides: "attachments_list_core が kind と `_generated/` 配下を含めて返す"
      contains: "\"kind\": \"generated\""
    - path: "config/mcp_tools.yaml"
      provides: "attachments_list docstring に kind フィールド追記"
      contains: "kind"
    - path: "tests/test_outputs_route.py"
      provides: "GET 認可 / 404 / path traversal / isolation 4 ケース green"
    - path: "tests/test_mcp_attachments_kind.py"
      provides: "test_returns_both_kinds green"
  key_links:
    - from: "app/api/routes/outputs.py"
      to: "app/api/routes/attachments.py の _resolve_thread_folder / _safe_resolve_file / _normalize_basename"
      via: "import 経由で再利用 (新規 helper は書かない)"
      pattern: "from app\\.api\\.routes\\.attachments import"
    - from: "app/api/main.py"
      to: "outputs.router"
      via: "include_router 呼び出し"
      pattern: "include_router\\(outputs\\.router\\)"
    - from: "mcp_server/tools/attachments.py attachments_list_core"
      to: "{folder}/_generated/ の listdir"
      via: "os.path.join(folder, '_generated') + os.listdir + os.path.isfile + kind 付与"
      pattern: "_generated"
    - from: "config/mcp_tools.yaml"
      to: "auto-generated mcp_helper.py / tool-catalog-generated.js / docs/mcp-tools.md"
      via: "scripts/generate_mcp_artifacts.py --target all"
      pattern: "kind.*user_upload.*generated"
---

<objective>
バックエンド core layer の 2 系統 (HTTP route と MCP tool) を Phase 38 仕様に拡張する。Plan 03 (sandbox cwd 切替) と並列実行可能なように files_modified を完全分離する。

Purpose:
- HTTP route: `_generated/` 配下のファイルを JWT 認証下で取得可能にする (FOUT-01/FOUT-02 の前提)
- MCP tool: AI が `attachments_list()` で input/output を flat に区別できるようにする (FOUT-04 の前提、D-06 の `kind` 単一 discriminator 原則)
- YAML SSoT: ADR-0044 (Phase 30) の手書き / 自動生成境界を守り、`generate_mcp_artifacts.py --check` で drift ゼロを維持

Output:
- 新規 `app/api/routes/outputs.py` (~50 行、attachments.py helper を import で再利用)
- `app/api/main.py` への 1 行追加 (router include)
- `mcp_server/tools/attachments.py` の `attachments_list_core` 拡張 (~20 行追加 / 第二ループ)
- `config/mcp_tools.yaml` docstring 更新 (5-10 行)
- 自動生成 3 ファイル (`mcp_helper.py` / `tool-catalog-generated.js` / `docs/mcp-tools.md`) の決定論的再生成
- `tests/test_outputs_route.py` 4 ケース green + `tests/test_mcp_attachments_kind.py` 1 ケース green
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-worker-dl/38-CONTEXT.md
@.planning/phases/38-worker-dl/38-RESEARCH.md
@.planning/phases/38-worker-dl/38-PATTERNS.md
@.planning/phases/38-worker-dl/38-VALIDATION.md
@.planning/patterns.md
@CLAUDE.md
@app/api/routes/attachments.py
@app/api/main.py
@mcp_server/tools/attachments.py
@config/mcp_tools.yaml
@tests/test_outputs_route.py
@tests/test_mcp_attachments_kind.py

<interfaces>
<!-- attachments.py の helper シグネチャ (引数順序に注意) -->
```python
# app/api/routes/attachments.py L41-95
def _normalize_basename(filename: str | None) -> str:
    # NFC + path separator reject、raises HTTPException(400)

def _resolve_thread_folder(github_login: str, thread_id: str) -> str:
    # ← API route 側は (github_login, thread_id) の順序 (L63)
    # realpath prefix guard、raises HTTPException(400)

def _safe_resolve_file(thread_folder: str, filename: str) -> str:
    # basename + realpath guard で thread_folder 配下に限定、raises HTTPException(400)
```

<!-- 注意: mcp_server/tools/attachments.py 側の _resolve_thread_folder は (thread_id, github_login) と引数順が逆。
     本 plan で MCP tool 側を扱うときはそちらの順序を踏襲する。 -->

<!-- 既存 attachments.py get_attachment route (analog) -->
```python
# app/api/routes/attachments.py L172-194
@router.get("/threads/{thread_id}/attachments/{name}")
async def get_attachment(...):
    github_login = payload.get("github_login", "unknown")
    folder = _resolve_thread_folder(github_login, thread_id)
    safe_path = _safe_resolve_file(folder, name)
    if not os.path.isfile(safe_path):
        raise HTTPException(status_code=404, detail="file not found")
    return FileResponse(safe_path, media_type=..., headers={...})
```

<!-- app/api/main.py の include パターン (analog L25-26 と L384-385 周辺) -->
```python
from app.api.routes import (
    agents, apps, attachments, auth, canvas, chat, gems, health, hosted_apps,
    iframe_rpc, jobs, me, models, ...
)
app.include_router(attachments.router)
```

<!-- mcp_server/tools/attachments.py attachments_list_core (L123-164) を拡張 -->
```python
# 既存ループの末尾 (L157-163) の dict append に "kind": "user_upload" を追加し、
# 直後に _generated/ サブフォルダ用の同形ループを kind="generated" で追加する。
# サブフォルダは 1 段のみ降り、再帰 scan しない (Pitfall 4)。
```

<!-- config/mcp_tools.yaml L160-182 attachments_list ブロック -->
```yaml
- name: attachments_list
  description: 現在の thread に添付されたファイル + AI 生成ファイルの一覧を返す
  python_wrapper:
    docstring: |
      # ↑ ここに kind フィールドの説明を追加
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: app/api/routes/outputs.py を新規作成し main.py に include する</name>
  <files>app/api/routes/outputs.py, app/api/main.py, tests/test_outputs_route.py</files>
  <read_first>
    - app/api/routes/attachments.py L1-95 (helper) と L172-194 (`get_attachment` analog) — outputs.py は **import で再利用**、新規 helper を書かない
    - app/api/main.py L20-30 (routes import 部) と L380-400 (include_router 集約箇所) — `outputs` を 1 行追加するだけのパターン
    - .planning/phases/38-worker-dl/38-PATTERNS.md §"Plan 02-A app/api/routes/outputs.py" (L61-141) — analog 引用箇所と route 本体の骨組み
    - .planning/phases/38-worker-dl/38-CONTEXT.md D-05 / D-19 — outputs route 分離方針と multi-user isolation の helper 再利用根拠
    - .planning/phases/38-worker-dl/38-RESEARCH.md §"Pattern 4: GET /api/threads/{tid}/outputs/{name} ルート" (L462-526) と §"Pitfall 10" (L798-801) — `_safe_resolve_file` に `_generated/` 連結後の folder を渡す根拠
    - tests/test_attachments_get_delete_route.py L1-100 — `patch_thread_files_dir` fixture と pytest test 構造の analog
    - Plan 01 scaffold: tests/test_outputs_route.py — 既に skip マーカーで 4 関数が定義されている
  </read_first>
  <behavior>
    - outputs.py に `router = APIRouter(prefix="/api", tags=["outputs"])` と `get_output` (GET /threads/{tid}/outputs/{name}) が存在し、`_resolve_thread_folder` / `_safe_resolve_file` / `_normalize_basename` を attachments.py から import している。新規 helper を outputs.py 内に書かない (RESEARCH §"Don't Hand-Roll")。
    - thread_folder に `_generated` を join してから `_safe_resolve_file(gen_folder, name)` を呼ぶ (Pitfall 10 — 直接 thread_folder を渡すと user upload にもアクセス可能になる)。
    - main.py で `from app.api.routes import outputs` を alphabetical 順に追加 (既存の m / models の後、p の前) し、`app.include_router(outputs.router)` を attachments.router の直後に追加。
    - tests/test_outputs_route.py の Plan 01 で scaffold した 4 関数 (`test_get_output_returns_raw_bytes` / `test_path_traversal_rejected` / `test_isolation_other_user_blocked` / `test_get_output_works_for_claude_code`) のうち、本 task で 3 つを green 化する (`test_get_output_works_for_claude_code` は Plan 03 で claude_code の cwd 切替が green になってから外す。本 task では skip 維持で OK)。
    - **D-19 整合スタンス (checker W3):** Phase 38 では isolation **単体テストを新規追加しない** (CONTEXT.md D-19)。Plan 01 で scaffold 済の `test_path_traversal_rejected` / `test_isolation_other_user_blocked` の skip マーカーを外して green 化する作業は、「Phase 36 既存 helper (`_resolve_thread_folder` / `_safe_resolve_file`) を **import で再利用** することで isolation が outputs route にも自動的に効くこと」を smoke 確認する位置づけ。Phase 38 独自の isolation ロジックは書かない (helper 再利用が唯一の防御パス、D-19)。
  </behavior>
  <action>
    Approach (D-19): outputs.py 内に独自 isolation/path-traversal/auth ロジックを書かない。attachments.py の helper を import で再利用するだけで、Plan 01 scaffold の isolation/traversal 2 ケースが skip 解除 → green になることが Phase 36 防御の outputs route への automatic 適用を smoke 検証する形になる。

    1. `app/api/routes/outputs.py` を新規作成 (PATTERNS.md §"Plan 02-A" の骨組み完全踏襲):
       - module docstring "Phase 38 D-05: GET /api/threads/{tid}/outputs/{name} — AI 生成ファイル DL/preview" を付与
       - `from __future__ import annotations` + `import logging, mimetypes, os`
       - `from fastapi import APIRouter, Depends, HTTPException, Path, Request` + `from fastapi.responses import FileResponse`
       - `from app.api.routes.attachments import _resolve_thread_folder, _safe_resolve_file, _normalize_basename` (新規 helper を書かない)
       - `from app.api.routes.chat import get_jwt_payload`
       - `router = APIRouter(prefix="/api", tags=["outputs"])` (PATTERNS.md note: OpenAPI 上 attachments と分離して読めるため `tags=["outputs"]`)
       - `@router.get("/threads/{thread_id}/outputs/{name}")` async def `get_output(request, thread_id: str = Path(...), name: str = Path(..., description="Storage name (timestamp prefix)"), payload: dict = Depends(get_jwt_payload))`
         - `github_login = payload.get("github_login", "unknown")`
         - `thread_folder = _resolve_thread_folder(github_login, thread_id)` (引数順序注意: API route 側は github_login が第 1 引数)
         - `gen_folder = os.path.join(thread_folder, "_generated")`
         - `safe_path = _safe_resolve_file(gen_folder, name)` (Pitfall 10 — gen_folder を渡すことで realpath guard が `_generated/` 配下に絞り込む)
         - `if not os.path.isfile(safe_path): raise HTTPException(status_code=404, detail="output not found")`
         - `mime, _ = mimetypes.guess_type(name)` → `FileResponse(safe_path, media_type=mime or "application/octet-stream", headers={"Content-Disposition": f'inline; filename="{_normalize_basename(name)}"'})`

    2. `app/api/main.py` 編集:
       - routes import を alphabetical 順に保つよう `outputs` を追加 (例: `from app.api.routes import (..., me, models, outputs, ...)` または既存の import スタイルに合わせる)
       - `app.include_router(attachments.router)` の直後に `app.include_router(outputs.router)` を追加 (Phase 38 D-05 のコメント付き)

    3. `tests/test_outputs_route.py` (Plan 01 で scaffold 済) の以下 3 ケースから skip マーカーを外して実装:
       - `test_get_output_returns_raw_bytes`: tmp_path に `unknown/t-o1/_generated/20260512T120000_chart.png` を作って bytes 書き込み、`api_client.get("/api/threads/t-o1/outputs/20260512T120000_chart.png")` → 200 + content 一致 (PATTERNS.md §"Plan 06 Tests")
       - `test_path_traversal_rejected`: `urllib.parse.quote("../../../etc/passwd", safe="")` を URL に埋め込んで GET → 400 または 404 (analog: tests/test_attachments_get_delete_route.py L60-67)
       - `test_isolation_other_user_blocked`: 別 user の JWT cookie を使って他人 thread の outputs を取得 → 401/404 (analog: 既存 attachments の isolation テストパターン)
       - `test_get_output_works_for_claude_code` は本 task では **skip 維持** (Plan 03 で claude_code の cwd 切替が完了してから外す)

    4. fixture `patch_thread_files_dir` は tests/test_attachments_get_delete_route.py L16-21 と同形で `attachments_module.THREAD_FILES_DIR` を monkeypatch (PATTERNS.md §"Test fixture pattern")。outputs.py が attachments.py の helper を import しているため、attachments_module 側の monkeypatch のみで両 route の base path が tmp に切り替わる。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph &amp;&amp; uv run pytest tests/test_outputs_route.py::test_get_output_returns_raw_bytes tests/test_outputs_route.py::test_path_traversal_rejected tests/test_outputs_route.py::test_isolation_other_user_blocked -x -v 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - source assertion: `grep -c "async def get_output" app/api/routes/outputs.py` count 1
    - source assertion: `grep -E "from app\.api\.routes\.attachments import" app/api/routes/outputs.py` ヒット (helper 再利用)
    - source assertion: `grep -E "_generated" app/api/routes/outputs.py` ヒット (gen_folder 連結)
    - source assertion: `grep -c "include_router(outputs.router)" app/api/main.py` count 1
    - source assertion: outputs.py 内に `_resolve_thread_folder` / `_safe_resolve_file` の関数定義が **存在しない** ことを確認 (`grep -c "^def _resolve_thread_folder\|^def _safe_resolve_file" app/api/routes/outputs.py` count 0)
    - test command: `uv run pytest tests/test_outputs_route.py::test_get_output_returns_raw_bytes tests/test_outputs_route.py::test_path_traversal_rejected tests/test_outputs_route.py::test_isolation_other_user_blocked -x` exit 0
    - behavior assertion: 200 + Content-Disposition: inline; filename="..." が `test_get_output_returns_raw_bytes` で assert される
    - behavior assertion: 別 user JWT で 401/404 が `test_isolation_other_user_blocked` で assert される
  </acceptance_criteria>
  <done>GET /api/threads/{tid}/outputs/{name} が動作し、JWT cookie 認証 + realpath guard + multi-user isolation (Phase 36 helper 経由) が test 3 本 green で証明される。Plan 03 / Plan 05 が同時着手できる状態。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: mcp_server/tools/attachments.py の attachments_list_core を `_generated/` + `kind` 対応に拡張する</name>
  <files>mcp_server/tools/attachments.py, tests/test_mcp_attachments_kind.py</files>
  <read_first>
    - mcp_server/tools/attachments.py L123-164 (`attachments_list_core`) — 自分自身を in-place 拡張する analog
    - mcp_server/tools/attachments.py L31 (`def _resolve_thread_folder(thread_id: str, github_login: str)`) — **API route 側と引数順序が逆** (thread_id が第 1 引数) なので注意
    - .planning/phases/38-worker-dl/38-PATTERNS.md §"Plan 02-B mcp_server/tools/attachments.py" (L143-204) — 第二ループの shape
    - .planning/phases/38-worker-dl/38-RESEARCH.md §"Pattern 3: SystemMessage Prepend の kind 拡張" (L414-460) と §"Pitfall 1 / 4" (L750-771)
    - .planning/phases/38-worker-dl/38-CONTEXT.md D-06 (kind: 'user_upload' | 'generated')
    - tests/test_attachments_list.py L20-46 — analog: tmp_path monkeypatch + assertion 構造
    - Plan 01 scaffold: tests/test_mcp_attachments_kind.py
  </read_first>
  <behavior>
    - `attachments_list_core(thread_id, github_login)` が 1) thread フォルダ直下の user upload に `kind: "user_upload"` を付与、2) `_generated/` サブフォルダ (再帰なし、1 段のみ) 配下に `kind: "generated"` を付与した flat list を返す。
    - symlink は両ループで除外 (LOW-04)。`.pyc` 等のフィルタは MCP tool 側では行わない (post-process rename 側で扱う、責務分離)。
    - `test_returns_both_kinds`: tmp に user_upload と generated 各 1 ファイルを作って `attachments_list_core(thread_id, github_login)` を呼び、戻り値の kinds が `["generated", "user_upload"]` (sort 済) であることを assert。
  </behavior>
  <action>
    1. `mcp_server/tools/attachments.py` L142-164 の既存ループ末尾の dict append に **`"kind": "user_upload"` を追加** (PATTERNS.md §"Plan 02-B" コード踏襲)。

    2. 既存ループの直後、`return out` の前に **新規ループを追加** (PATTERNS.md §"Plan 02-B"):
       ```python
       gen_folder = os.path.join(folder, "_generated")
       if os.path.isdir(gen_folder):
           for fname in sorted(os.listdir(gen_folder)):
               fpath = os.path.join(gen_folder, fname)
               if os.path.islink(fpath) or not os.path.isfile(fpath):
                   continue
               stat = os.stat(fpath)
               ext = os.path.splitext(fname)[1].lower()
               mime_type, _ = mimetypes.guess_type(fname)
               out.append({
                   "name": fname,
                   "size": stat.st_size,
                   "modified_at": float(stat.st_mtime),
                   "ext": ext,
                   "mime_type": mime_type or "application/octet-stream",
                   "kind": "generated",
               })
       ```
       - サブフォルダ 1 段のみ降りる (Pitfall 4 — 再帰しない、`__pycache__/*.pyc` の漏出回避)
       - `os.path.islink` で symlink を除外 (LOW-04)

    3. `tests/test_mcp_attachments_kind.py` (Plan 01 で scaffold 済) の `test_returns_both_kinds` から skip マーカーを外し、PATTERNS.md §"Plan 06 Tests" §"tests/test_mcp_attachments_kind.py" の shape で実装:
       - tmp_path に `user-a/t-1/20260512T120000_input.pdf` と `user-a/t-1/_generated/20260512T120100_output.png` を作る
       - `monkeypatch.setattr(attachments, "THREAD_FILES_DIR", str(tmp_path))` (helper module への monkeypatch)
       - `result = await attachments.attachments_list_core("t-1", "user-a")` (mcp 側は thread_id, github_login 順)
       - `kinds = sorted([r["kind"] for r in result])` → `assert kinds == ["generated", "user_upload"]`
       - 追加で `names` / `ext` も assert して dict shape 全体が崩れていないことを確認

    Note: 既存テスト `tests/test_attachments_list.py::test_list_returns_metadata` に `kind` フィールドの存在 assertion が抜けている可能性がある。本 task では既存テストを **破壊しない範囲で** kind 追加が backward-compatible であることを担保 (dict にフィールド追加だけ)。既存テストで `assert "kind" not in entry` のような排他 assertion がないか grep で確認し、もし排他があれば Plan 04 で正規化 (本 plan では既存 assertion 緩和は不要)。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph &amp;&amp; uv run pytest tests/test_mcp_attachments_kind.py::test_returns_both_kinds tests/test_attachments_list.py -x -v 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - source assertion: `grep -c '"kind": "user_upload"' mcp_server/tools/attachments.py` count >= 1
    - source assertion: `grep -c '"kind": "generated"' mcp_server/tools/attachments.py` count >= 1
    - source assertion: `grep -c "gen_folder = os.path.join(folder, \"_generated\")" mcp_server/tools/attachments.py` count >= 1
    - source assertion: `grep -c "os.path.islink" mcp_server/tools/attachments.py` count >= 2 (両ループとも symlink 除外)
    - source assertion: mcp_server/tools/attachments.py 内で `_generated/` の **再帰** scan が **存在しない** ことを確認 (`grep -c "os.walk\|rglob" mcp_server/tools/attachments.py` count 0)
    - test command: `uv run pytest tests/test_mcp_attachments_kind.py::test_returns_both_kinds -x` exit 0
    - test command: `uv run pytest tests/test_attachments_list.py -x` exit 0 (既存テストを壊さない)
    - behavior assertion: 戻り値 dict に `kind` キーが必ず含まれることをテストで assert
  </acceptance_criteria>
  <done>attachments_list_core が `_generated/` 配下と `kind` discriminator を含む flat list を返し、既存テストも壊さない。AI が `attachments_list()` で input/output を 1 経路で取得できる。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: config/mcp_tools.yaml docstring を更新し scripts/generate_mcp_artifacts.py --target all を実行する</name>
  <files>config/mcp_tools.yaml, mcp_server/tools/mcp_helper.py, static/js/tool-catalog-generated.js, docs/mcp-tools.md</files>
  <read_first>
    - config/mcp_tools.yaml L160-182 (`attachments_list` ブロック) — docstring を更新する箇所
    - CLAUDE.md §"MCP Tool Catalog (Phase 30)" — 手書き / 自動生成境界。**mcp_helper.py / tool-catalog-generated.js / docs/mcp-tools.md を手で触らない**
    - .planning/phases/38-worker-dl/38-RESEARCH.md §"Pattern 7: MCP YAML 拡張 + 自動生成再実行" (L600-648)
    - .planning/phases/38-worker-dl/38-PATTERNS.md §"Shared Patterns §MCP YAML SSoT regeneration" (L1048-1062)
    - docs/adr/0044-mcp-tool-catalog-single-source-of-truth.md — pre-commit hook と drift 検知の根拠
    - scripts/generate_mcp_artifacts.py — 実行可能なエントリポイント
  </read_first>
  <behavior>
    - config/mcp_tools.yaml の `attachments_list` ブロックの `description` を「現在の thread に添付されたファイル + AI 生成ファイルの一覧を返す」に更新し、`python_wrapper.docstring` 内 Returns 例に `kind: "user_upload" | "generated"` フィールドを追記。
    - `python3 scripts/generate_mcp_artifacts.py --target all` を実行すると `mcp_helper.py` / `tool-catalog-generated.js` / `docs/mcp-tools.md` が **決定論的に再生成** され、`python3 scripts/generate_mcp_artifacts.py --check` が exit 0 で drift ゼロ。
    - 自動生成 3 ファイルを **手書きで編集しない** (CLAUDE.md / ADR-0044)。
    - claude_code の YAML 行は本 plan では **触らない** (Plan 03 で `cwd` 引数削除と同時に扱う)。claude_code は `sandbox_exposed: false` で python_wrapper を持たないため、本 plan の attachments_list docstring 変更のみで自動生成 3 ファイルが更新される。
  </behavior>
  <action>
    1. `config/mcp_tools.yaml` L160-182 `attachments_list` の編集 (RESEARCH §Pattern 7 の YAML スニペット完全踏襲):
       - L161 `description:` を `現在の thread に添付されたファイル + AI 生成ファイルの一覧を返す` に変更
       - `python_wrapper.docstring` の `Returns:` 行のサンプル dict に `kind: "user_upload" | "generated"` フィールドを追記
       - `Example:` 部のループ本体を `print(f["name"], f["kind"], f["size"])` に更新して使い方も例示
       - `mcp_args_mapping: {}` と `result_transform: { mode: passthrough }` は変更なし

    2. 自動生成スクリプトを実行:
       `python3 scripts/generate_mcp_artifacts.py --target all`
       - これで `mcp_server/tools/mcp_helper.py` の `list_attachments()` docstring と `static/js/tool-catalog-generated.js` の attachments_list エントリ、`docs/mcp-tools.md` の attachments_list セクションが同時更新される。
       - **手書きで `mcp_helper.py` / `tool-catalog-generated.js` / `docs/mcp-tools.md` を編集しない** (pre-commit hook drift 検知でブロックされる)。

    3. drift 検証:
       `python3 scripts/generate_mcp_artifacts.py --check` を実行 → exit 0 が出ることを確認。
       もし非ゼロなら再度 `--target all` を実行して再ステージング (RESEARCH §Pattern 7 の手順)。

    4. git diff で:
       - `config/mcp_tools.yaml` の attachments_list docstring が変更されている
       - `mcp_helper.py` / `tool-catalog-generated.js` / `docs/mcp-tools.md` が自動生成で変更されている (kind 言及が入る)
       - 他のツール (db_query / web_search / get_current_datetime / claude_code / execute_python) の自動生成ブロックは変更ゼロ (deterministic 再生成のため決定論)

    Note: 本 plan で claude_code の `args:` から `cwd` を削除する作業は **行わない** (Plan 03 が claude_code の関数シグネチャ変更と同時に扱う)。本 plan の責務は attachments_list の戻り値スキーマ docstring 拡張のみ。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph &amp;&amp; python3 scripts/generate_mcp_artifacts.py --check 2>&amp;1 | tail -20; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - source assertion: `grep -A 30 "name: attachments_list" config/mcp_tools.yaml | grep -E "kind.*user_upload|kind.*generated"` ヒット (docstring に kind 言及)
    - source assertion: `grep -E "kind.*user_upload.*generated|user_upload.*generated.*kind" mcp_server/tools/mcp_helper.py docs/mcp-tools.md static/js/tool-catalog-generated.js | wc -l` count >= 3 (3 ファイルすべて自動更新)
    - CLI output: `python3 scripts/generate_mcp_artifacts.py --check` exit 0
    - source assertion: 自動生成 3 ファイルのヘッダーに "DO NOT EDIT" が残っていること (`grep -c "DO NOT EDIT" mcp_server/tools/mcp_helper.py static/js/tool-catalog-generated.js docs/mcp-tools.md` count >= 3)
    - behavior assertion: 既存テスト `uv run pytest tests/test_generate_mcp_artifacts.py -x` (存在すれば) green
  </acceptance_criteria>
  <done>config/mcp_tools.yaml SSoT に kind フィールドが宣言され、自動生成 3 ファイルが drift ゼロで再生成される。pre-commit hook (scripts/install-hooks.sh) が install 済なら commit 時のチェックも通る。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → outputs route | URL `{name}` に外部 input、JWT cookie で user 認証 |
| MCP tool → mcp-server filesystem | thread_id / github_login が headers から到達 |
| YAML SSoT → auto-generated artifacts | ファイル整合性は scripts/generate_mcp_artifacts.py --check に依存 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-02-01 | Tampering | URL `{name}` への `../etc/passwd` 注入 | mitigate | `_safe_resolve_file(gen_folder, name)` で `_normalize_basename` + realpath prefix guard、`test_path_traversal_rejected` で assert |
| T-38-02-02 | Information Disclosure | 別 user の `_generated/` への直接アクセス | mitigate | `_resolve_thread_folder(github_login, thread_id)` が JWT payload `github_login` で folder 構築、別 user の login がわからない限り物理パスが作れない (Phase 36 / 37 で確立、D-19)、`test_isolation_other_user_blocked` で assert |
| T-38-02-03 | Information Disclosure | MCP `attachments_list` で他 user の `_generated/` 漏出 | mitigate | RPCContext (x-thread-id / x-github-login) で folder を構築、helper の realpath guard で物理隔離 |
| T-38-02-04 | Tampering | 自動生成ファイル (mcp_helper.py 等) の手書き drift | mitigate | pre-commit hook (scripts/install-hooks.sh) が `generate_mcp_artifacts.py --check` を実行、CI でも drift 検知 (ADR-0044) |
| T-38-02-05 | Information Disclosure | symlink で thread フォルダ外へ脱出 | mitigate | `os.path.islink` で除外 (LOW-04、Phase 37 と統一) |
</threat_model>

<verification>
- outputs route: `uv run pytest tests/test_outputs_route.py::test_get_output_returns_raw_bytes tests/test_outputs_route.py::test_path_traversal_rejected tests/test_outputs_route.py::test_isolation_other_user_blocked -x` 全 green
- MCP tool: `uv run pytest tests/test_mcp_attachments_kind.py::test_returns_both_kinds tests/test_attachments_list.py -x` 全 green
- YAML SSoT: `python3 scripts/generate_mcp_artifacts.py --check` exit 0
- 全体: `uv run pytest tests/ -x --ignore=tests/test_api_chat.py` で Phase 38 起因の回帰ゼロ
</verification>

<success_criteria>
- GET /api/threads/{tid}/outputs/{name} が動作し、JWT 認証 + realpath guard + multi-user isolation を含むテスト 3 本が green (FOUT-04 sc5 を Phase 36 helper 経由で間接的に充足)
- attachments_list MCP tool が `_generated/` + `kind` フィールド対応に拡張され、test_returns_both_kinds が green (FOUT-04 の AI 視認性を満たす)
- config/mcp_tools.yaml SSoT 更新 + 自動生成 3 ファイル drift ゼロ (FOUT-01/02 で AI が使う tool catalog の整合性)
- Plan 03 (sandbox cwd 切替) と並列に進められる
</success_criteria>

<output>
After completion, create `.planning/phases/38-worker-dl/38-02-SUMMARY.md`
</output>
