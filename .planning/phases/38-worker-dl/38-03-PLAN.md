---
phase: 38
plan: 3
plan_id: 38-03-sandbox-cwd-and-rename
type: execute
wave: 1
depends_on: [38-01-types-and-roundtrip-gate]
files_modified:
  - mcp_server/tools/execute_python.py
  - mcp_server/tools/claude_code.py
  - tests/test_post_process_rename.py
  - tests/test_execute_python_output.py
  - tests/test_claude_code_no_cwd_arg.py
autonomous: true
requirements: [FOUT-01, FOUT-02]
tags: [sandbox, execute-python, claude-code, post-process-rename, snapshot-diff]
objective: "execute_python の cwd を `/shared/thread-files/<login>/<tid>/_generated/` に切替、claude_code から cwd 引数を削除し同 folder で固定実行、tool wrapper に snapshot diff 方式の post-process rename を追加して生成ファイルを `{ts}_{name}` 規約に揃える"

must_haves:
  truths:
    - "execute_python が x-thread-id + x-github-login headers から `_generated/` folder path を構築し、subprocess cwd として使う (D-08)。headers 不足時は `/tmp` に fallback して既存 sandbox 動作と互換維持"
    - "claude_code 関数シグネチャから `cwd: str = '/tmp'` 引数が削除され、新シグネチャは `claude_code(prompt: str, headers: dict | None = None) -> dict` (D-09)"
    - "execute_python_with_headers と claude_code の register_tools wrapper が tool 実行後に snapshot diff で新規ファイルを `{ts}_{basename}` にリネームし、既に prefix 付きのファイルはスキップする (D-10 / D-11、RESEARCH Pattern 1)"
    - "post-process rename の中間ファイル除外フィルタ (`.pyc` / `__pycache__`) が動作する"
    - "claude_code の overflow output (`OUTPUT_DIR=/shared/claude-code-outputs`) は `_generated/` にマージされず debug 用 global volume として現状維持 (D-09)"
    - "D-03: 命名規則 `YYYYMMDDTHHMMSS_<original>.<ext>` を tool wrapper の post-process rename で必ず付与し、Phase 37 D-02 (`thread-files` の命名規約) と統一する。同名 `output.png` が turn を跨いで複数回生成されても timestamp prefix で衝突せず履歴 immutable を保てる (CONTEXT.md)"
    - "D-04: subprocess cwd は Docker volume `thread-files` 配下の `/shared/thread-files/<login>/<tid>/_generated/` を直接指す — worker は同 volume を mount している前提 (api=RW / mcp-server=RW / worker=RO は本 plan 外の docker-compose 設定で担保) (CONTEXT.md)"
  artifacts:
    - path: "mcp_server/tools/execute_python.py"
      provides: "_resolve_generated_folder helper + _rename_new_outputs + _is_already_prefixed + cwd 切替 + wrapper post-process loop"
      contains: "_rename_new_outputs"
    - path: "mcp_server/tools/claude_code.py"
      provides: "headers 引数追加、cwd 削除、wrapper は execute_python の helper を import で再利用"
      contains: "headers: dict | None = None"
    - path: "tests/test_post_process_rename.py"
      provides: "snapshot diff / _is_already_prefixed / .pyc 除外 3 ケース green"
    - path: "tests/test_execute_python_output.py"
      provides: "cwd 切替 + /tmp fallback 検証 green"
    - path: "tests/test_claude_code_no_cwd_arg.py"
      provides: "inspect.signature による cwd 引数なし検証 green"
  key_links:
    - from: "mcp_server/tools/execute_python.py asyncio.create_subprocess_exec"
      to: "_resolve_generated_folder(headers) の戻り値"
      via: "cwd= 引数として渡す"
      pattern: "cwd=cwd"
    - from: "mcp_server/tools/claude_code.py"
      to: "mcp_server/tools/execute_python.py の _resolve_generated_folder + _rename_new_outputs"
      via: "import で DRY 再利用 (single source of truth)"
      pattern: "from mcp_server\\.tools\\.execute_python import"
    - from: "register_tools wrapper"
      to: "snapshot diff (before/after listdir)"
      via: "wrapper 内で before snapshot を取り、tool 完了後に _rename_new_outputs(folder, before) を呼ぶ"
      pattern: "before = set\\(os\\.listdir"
---

<objective>
sandbox (execute_python + claude_code) が生成するファイルを `/shared/thread-files/<login>/<tid>/_generated/` に直接書き、tool wrapper が timestamp prefix を後付けで揃える設計を実装する。

Purpose:
- AI が `open("output.png", "w")` と素直に書けば自動的に thread スコープに永続化される (D-08 の sandbox 思想)
- timestamp prefix は AI プロンプトに依存させない (D-10 — wrapper の責任)
- snapshot diff で前 turn の生成物を誤って rename しない (D-11 / RESEARCH Pattern 1、mtime 判定の NFS / 9p 解像度問題を回避)
- claude_code の cwd 引数削除は **破壊変更だが影響範囲ゼロ** (RESEARCH §Pattern 6 grep 検証済) — シグネチャ整合は Plan 02 で更新済の YAML SSoT で吸収される

Output:
- `mcp_server/tools/execute_python.py`: `_resolve_generated_folder` / `_rename_new_outputs` / `_is_already_prefixed` 新規 helper + cwd 切替 + wrapper post-process loop
- `mcp_server/tools/claude_code.py`: cwd 引数削除 + headers 引数追加 + execute_python helper を import で再利用
- 3 テストファイル (test_post_process_rename.py / test_execute_python_output.py / test_claude_code_no_cwd_arg.py) が全 green

並列性: 本 plan は Plan 02 と files_modified 重複なし (Plan 02: app/api/routes/outputs.py + attachments.py + YAML、本 plan: mcp_server/tools/execute_python.py + claude_code.py)。両 plan は wave 1 同時着手可能。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-worker-dl/38-CONTEXT.md
@.planning/phases/38-worker-dl/38-RESEARCH.md
@.planning/phases/38-worker-dl/38-PATTERNS.md
@.planning/phases/38-worker-dl/38-VALIDATION.md
@mcp_server/tools/execute_python.py
@mcp_server/tools/claude_code.py
@config/mcp_tools.yaml
@tests/test_post_process_rename.py
@tests/test_execute_python_output.py
@tests/test_claude_code_no_cwd_arg.py

<interfaces>
<!-- 既存 execute_python シグネチャ (L95) -->
```python
async def execute_python(code: str, timeout: int = 60, headers: dict | None = None) -> dict:
```

<!-- 既存 headers 経路 (L139-148) -->
```python
_req_headers = headers or {}
_thread_id = _req_headers.get("x-thread-id", "")
_github_login = _req_headers.get("x-github-login", "")
if _thread_id: sanitized_env["X_THREAD_ID"] = _thread_id
if _github_login: sanitized_env["X_GITHUB_LOGIN"] = _github_login
```

<!-- 既存 cwd L155 -->
```python
proc = await asyncio.create_subprocess_exec(
    "python3", "-c", code,
    ...,
    cwd="/tmp",         # ← _resolve_generated_folder(headers) に差し替える
    ...,
)
```

<!-- 既存 register_tools wrapper (L208-213) -->
```python
async def execute_python_with_headers(code: str, timeout: int = 60,
                                      headers: dict = CurrentHeaders()) -> dict:
    return await execute_python(code=code, timeout=timeout, headers=headers)
```

<!-- 既存 claude_code シグネチャ (L55) — cwd 引数を削除 -->
```python
async def claude_code(prompt: str, cwd: str = "/tmp") -> dict:
```

<!-- 既存 claude_code の register_tools (L134-136) — wrapper は本 plan で新設 -->
```python
def register_tools(mcp: "FastMCP") -> None:
    mcp.tool(claude_code)
```

<!-- 環境変数: THREAD_FILES_DIR — Phase 37 で /shared/thread-files に設定済 -->
<!-- ヘッダ名: x-thread-id / x-github-login (Phase 37 Route A) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mcp_server/tools/execute_python.py に _resolve_generated_folder / _rename_new_outputs / _is_already_prefixed を追加し cwd を切り替える</name>
  <files>mcp_server/tools/execute_python.py, tests/test_execute_python_output.py, tests/test_post_process_rename.py</files>
  <read_first>
    - mcp_server/tools/execute_python.py L1-30 (import 部) と L85-160 (execute_python 本体) と L200-213 (register_tools wrapper) — 既存全体
    - .planning/phases/38-worker-dl/38-PATTERNS.md §"Plan 03-A mcp_server/tools/execute_python.py" (L207-321) — 完全な実装スニペット
    - .planning/phases/38-worker-dl/38-RESEARCH.md §"Pattern 1: Post-process Rename" (L300-361) と §"Pattern 5: execute_python の cwd 切替" (L528-580)
    - .planning/phases/38-worker-dl/38-CONTEXT.md D-08 / D-10 / D-11 — cwd 切替 + wrapper 責任 + snapshot diff 推奨
    - .planning/phases/38-worker-dl/38-RESEARCH.md §"Pitfall 3" (L762-766) — `_generated/` の `os.makedirs(exist_ok=True)` 冪等
    - .planning/phases/38-worker-dl/38-RESEARCH.md §"Pitfall 5" (L773-776) — 二重 rename 防止 (既に prefix 付きはスキップ)
    - .planning/phases/38-worker-dl/38-RESEARCH.md §"Anti-Patterns" (L716) — `.pyc` 除外
    - Plan 01 scaffold: tests/test_execute_python_output.py + tests/test_post_process_rename.py
  </read_first>
  <behavior>
    - `_resolve_generated_folder(headers)`: x-thread-id + x-github-login が両方ある場合 `/shared/thread-files/<login>/<tid>/_generated` の realpath を返し、欠落または path traversal 検出時は `/tmp` を返す (D-08 fallback policy)。
    - `_is_already_prefixed(name)`: 16 文字以上で `YYYYMMDDTHHMMSS_` 形式 (8 桁数字 + T + 6 桁数字 + _) を判定 (Pitfall 5)。
    - `_rename_new_outputs(folder, before)`: snapshot diff (after - before) で新規ファイル群を抽出、`.pyc` は除外、既に prefix 付きはスキップ、それ以外を `{utc_ts}_{name}` にリネーム、最終ファイル名リストを返す。
    - execute_python(code, timeout, headers): `cwd="/tmp"` を `_resolve_generated_folder(headers)` の戻り値に差し替え、`os.makedirs(cwd, exist_ok=True)` を呼ぶ (Pitfall 3 冪等)。
    - execute_python_with_headers wrapper: tool 実行前に `before = set(os.listdir(folder))`、実行後に `_rename_new_outputs(folder, before)` を呼んで結果に `generated_files: list[str]` を追加。folder が `/tmp` の場合は rename loop をスキップ (Pitfall: /tmp 全体の diff を取らない)。
    - test_post_process_rename.py の 3 ケースが green: `test_snapshot_diff_renames_only_new` / `test_skips_already_prefixed` / `test_excludes_pyc_files`。
    - test_execute_python_output.py で `test_writes_to_generated_folder` (mock subprocess で cwd 確認) と `test_falls_back_to_tmp_without_headers` が green。
  </behavior>
  <action>
    1. `mcp_server/tools/execute_python.py` の編集 (PATTERNS.md §"Plan 03-A" のスニペット完全踏襲):

       (a) module top に追加 (既存 import 群の直後):
       ```python
       THREAD_FILES_DIR = os.environ.get("THREAD_FILES_DIR", "/shared/thread-files")
       _PYC_EXCLUDES = {".pyc"}
       ```

       (b) 新規 module-level 関数を追加 (RESEARCH §Pattern 1 / 5 の完全実装):
       - `_utc_ts() -> str`: `datetime.datetime.now(tz=datetime.timezone.utc).strftime("%Y%m%dT%H%M%S")`
       - `_resolve_generated_folder(headers: dict | None) -> str`: ヘッダから `_generated/` path を構築 + realpath guard + 不足時 `/tmp` fallback (PATTERNS.md §"Plan 03-A" の `_resolve_generated_folder`)
       - `_is_already_prefixed(name: str) -> bool`: 16 文字以上で YYYYMMDDTHHMMSS_ パターン判定 (PATTERNS.md §"Plan 03-A" の `_is_already_prefixed`)
       - `_rename_new_outputs(folder: str, before: set[str]) -> list[str]`: snapshot diff + `.pyc` 除外 + 既存 prefix スキップ + rename (PATTERNS.md §"Plan 03-A" の `_rename_new_outputs`)

       (c) execute_python L155 周辺の subprocess 呼び出しを編集:
       - L155 の `cwd="/tmp"` を `cwd=cwd` (新ローカル変数) に置換
       - subprocess 呼び出しの **直前** に `cwd = _resolve_generated_folder(headers); os.makedirs(cwd, exist_ok=True)` を追加 (Pitfall 3 冪等)

       (d) register_tools 内 `execute_python_with_headers` wrapper を post-process 対応に拡張 (PATTERNS.md §"Plan 03-A" の wrapper 完全実装):
       ```python
       async def execute_python_with_headers(code: str, timeout: int = 60,
                                             headers: dict = CurrentHeaders()) -> dict:
           folder = _resolve_generated_folder(headers)
           os.makedirs(folder, exist_ok=True)
           before = set(os.listdir(folder)) if os.path.isdir(folder) else set()
           result = await execute_python(code=code, timeout=timeout, headers=headers)
           if folder != "/tmp":
               result["generated_files"] = _rename_new_outputs(folder, before)
           else:
               result["generated_files"] = []
           return result
       ```
       - `folder != "/tmp"` ガードで `/tmp` 全体の diff になる事故を回避 (PATTERNS.md §"Plan 03-A" Note on fallback)。

    2. `tests/test_post_process_rename.py` の 3 ケース実装 (Plan 01 で scaffold 済の skip マーカーを外す):

       (a) `test_snapshot_diff_renames_only_new`: tmp_path に `old.png` を置いて before snapshot → `new.png` + `another.csv` を追加 → `_rename_new_outputs(str(tmp_path), before)` 呼び出し → old.png はそのまま、new.png と another.csv だけ `{ts}_` prefix 付きにリネーム済 (戻り値リストで確認 + listdir で確認)

       (b) `test_skips_already_prefixed`: tmp_path に `20260601T120000_existing.png` を置いて before=空セット → `_rename_new_outputs` 呼び出し → そのまま (二重 prefix `{ts2}_20260601T120000_existing.png` にならない)

       (c) `test_excludes_pyc_files`: tmp_path に `script.pyc` と `output.txt` を置いて before=空セット → `script.pyc` は rename 対象から除外、`output.txt` だけが `{ts}_output.txt` にリネーム

    3. `tests/test_execute_python_output.py` の 2 ケース実装 (Plan 01 で scaffold 済):

       (a) `test_writes_to_generated_folder`: `mcp_server.tools.execute_python._resolve_generated_folder` を monkeypatch で固定値 (tmp_path 配下) に差し替える、または `THREAD_FILES_DIR` を tmp_path に monkeypatch、`asyncio.create_subprocess_exec` を AsyncMock で wrap して呼び出し引数の `cwd=` が `_generated/` を含む path であることを assert。

       (b) `test_falls_back_to_tmp_without_headers`: headers=None or {} で `_resolve_generated_folder` を呼ぶ → 戻り値が `/tmp` であることを assert (sandbox の既存挙動と互換性維持)

    Note (RESEARCH §Pitfall 6): `kind` を含む dict を後段で AsyncPostgresSaver の JSONB に渡すため、ここでの戻り値 dict は **Python str / int / float のみ** で構成 (Enum 混在禁止)。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph &amp;&amp; uv run pytest tests/test_post_process_rename.py tests/test_execute_python_output.py -x -v 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - source assertion: `grep -c "def _resolve_generated_folder" mcp_server/tools/execute_python.py` count 1
    - source assertion: `grep -c "def _rename_new_outputs" mcp_server/tools/execute_python.py` count 1
    - source assertion: `grep -c "def _is_already_prefixed" mcp_server/tools/execute_python.py` count 1
    - source assertion: `grep -E "cwd\s*=\s*[\"']/tmp[\"']" mcp_server/tools/execute_python.py | grep -v "return [\"']/tmp[\"']" | wc -l` count 0 (`cwd="/tmp"` ハードコードが残っていない、fallback の return 文は除外)
    - source assertion: `grep -c "_PYC_EXCLUDES" mcp_server/tools/execute_python.py` count >= 2 (定義 + 使用箇所)
    - source assertion: `grep -E "before\s*=\s*set\(os\.listdir" mcp_server/tools/execute_python.py` ヒット (wrapper 内 snapshot)
    - source assertion: `grep -E 'if folder != "/tmp"' mcp_server/tools/execute_python.py` ヒット (Pitfall ガード)
    - test command: `uv run pytest tests/test_post_process_rename.py -x` exit 0
    - test command: `uv run pytest tests/test_execute_python_output.py -x` exit 0
    - behavior assertion: `test_snapshot_diff_renames_only_new` で old.png が rename されないことを assert
    - behavior assertion: `test_skips_already_prefixed` で二重 prefix `{ts2}_20260601T120000_*` が出現しないことを assert
  </acceptance_criteria>
  <done>execute_python が `_generated/` cwd で実行され、wrapper が snapshot diff で生成ファイルを `{ts}_{name}` 規約に揃え、3 テストファイルの 5 ケースが green。Plan 02 と並列に進行可能。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: mcp_server/tools/claude_code.py から cwd 引数を削除、headers 引数を追加、execute_python の helper を import で再利用する</name>
  <files>mcp_server/tools/claude_code.py, tests/test_claude_code_no_cwd_arg.py</files>
  <read_first>
    - mcp_server/tools/claude_code.py L1-50 (import + ALLOWED_ENV_KEYS) と L55-136 (claude_code + register_tools) — 既存全体
    - .planning/phases/38-worker-dl/38-PATTERNS.md §"Plan 03-B mcp_server/tools/claude_code.py" (L323-388) — 完全な実装スニペット
    - .planning/phases/38-worker-dl/38-RESEARCH.md §"Pattern 6: claude_code の cwd 引数削除" (L582-598) — 影響範囲ゼロの根拠
    - .planning/phases/38-worker-dl/38-CONTEXT.md D-09 — cwd 引数削除 + overflow output (`OUTPUT_DIR`) は維持
    - .planning/phases/38-worker-dl/38-RESEARCH.md §"Pitfall 8" (L788-791) — overflow output と `_generated/` の混同回避
    - docs/adr/0023-mcp-db-query-and-claude-code-tools.md — `OUTPUT_DIR=/shared/claude-code-outputs` の意義 (debug 用 global volume、現状維持)
    - Task 1 で実装する `_resolve_generated_folder` / `_rename_new_outputs` (execute_python.py 内、本 task で import 再利用)
    - Plan 01 scaffold: tests/test_claude_code_no_cwd_arg.py
  </read_first>
  <behavior>
    - claude_code の関数シグネチャから `cwd: str = "/tmp"` 引数が削除され、`headers: dict | None = None` が追加されている (D-09 破壊変更、外部 caller ゼロ — RESEARCH §Pattern 6 grep で確認済)。
    - claude_code 内部で `_resolve_generated_folder(headers)` を **import 再利用** (新規 helper を claude_code.py に書かない、DRY)。
    - x-thread-id / x-github-login headers から sanitized_env に X_THREAD_ID / X_GITHUB_LOGIN を伝搬 (execute_python と同パターン)。
    - register_tools の wrapper `claude_code_with_headers` が CurrentHeaders DI を使い、tool 実行前に before snapshot を取り、実行後に `_rename_new_outputs` を呼んで `generated_files: list[str]` を結果に追加。
    - overflow output (`OUTPUT_DIR=/shared/claude-code-outputs`) の処理 (`_save_overflow_output` 等) は **触らない** — debug 用 global volume として現状維持 (D-09 / Pitfall 8)。
    - test_claude_code_no_cwd_arg.py の `test_signature_has_no_cwd` が green (inspect.signature(claude_code).parameters の keys に "cwd" が含まれない)。
  </behavior>
  <action>
    1. `mcp_server/tools/claude_code.py` の編集 (PATTERNS.md §"Plan 03-B" のスニペット完全踏襲):

       (a) `claude_code` 関数シグネチャ (L55) を編集:
       ```python
       # Before:
       async def claude_code(prompt: str, cwd: str = "/tmp") -> dict:
       # After:
       async def claude_code(prompt: str, headers: dict | None = None) -> dict:
       ```
       docstring も `cwd` 言及を削除し、「cwd は `_generated/` 配下に固定 (Phase 38 D-09)」+「overflow output (`OUTPUT_DIR`) は debug 用 global volume として維持」と記述。

       (b) 関数本体に execute_python と同形の headers 伝搬を追加 (L66-75 周辺):
       ```python
       sanitized_env = {k: v for k, v in os.environ.items() if k in ALLOWED_ENV_KEYS}
       _req_headers = headers or {}
       if _req_headers.get("x-thread-id"):
           sanitized_env["X_THREAD_ID"] = _req_headers["x-thread-id"]
       if _req_headers.get("x-github-login"):
           sanitized_env["X_GITHUB_LOGIN"] = _req_headers["x-github-login"]
       ```

       (c) subprocess L70-74 の `cwd=cwd` を編集:
       ```python
       from mcp_server.tools.execute_python import _resolve_generated_folder  # DRY 再利用
       cwd = _resolve_generated_folder(headers)
       os.makedirs(cwd, exist_ok=True)
       proc = await asyncio.create_subprocess_exec(
           "claude", "--print", prompt,
           ...,
           cwd=cwd,
           ...,
       )
       ```

       (d) `_save_overflow_output` / `OUTPUT_DIR` 関連処理 (L40-52, L116-131) は **そのまま維持** (D-09 / Pitfall 8)。overflow output は `_generated/` にマージしない。

       (e) register_tools (L134-136) を以下に置換 (PATTERNS.md §"Plan 03-B" の wrapper 完全実装):
       ```python
       def register_tools(mcp: "FastMCP") -> None:
           from fastmcp.dependencies import CurrentHeaders  # noqa: PLC0415
           from mcp_server.tools.execute_python import (
               _resolve_generated_folder,
               _rename_new_outputs,
           )

           async def claude_code_with_headers(prompt: str,
                                              headers: dict = CurrentHeaders()) -> dict:
               folder = _resolve_generated_folder(headers)
               os.makedirs(folder, exist_ok=True)
               before = set(os.listdir(folder)) if os.path.isdir(folder) else set()
               result = await claude_code(prompt=prompt, headers=headers)
               if folder != "/tmp":
                   result["generated_files"] = _rename_new_outputs(folder, before)
               else:
                   result["generated_files"] = []
               return result

           mcp.tool(claude_code_with_headers, name="claude_code")
       ```

    2. `tests/test_claude_code_no_cwd_arg.py` の `test_signature_has_no_cwd` 実装 (Plan 01 で scaffold 済):
       ```python
       import inspect
       from mcp_server.tools.claude_code import claude_code

       def test_signature_has_no_cwd():
           sig = inspect.signature(claude_code)
           assert "cwd" not in sig.parameters, \
               f"Phase 38 D-09: cwd 引数は削除されているはずだが残っている: {list(sig.parameters)}"
           # headers 引数が追加されていること
           assert "headers" in sig.parameters
       ```

    3. **影響範囲確認** (RESEARCH §Pattern 6 で grep 0 hit 既確認だが本 task で再確認):
       `grep -rn "claude_code(" app/ agents/ scripts/ 2>/dev/null | grep -v ".pyc" | head -20` を実行 → 直接呼び出しゼロを確認。MCP tool は `MultiServerMCPClient.get_tools()` 経由でしか呼ばれないため、cwd を引数で渡している外部 caller はゼロ。

    Note: `config/mcp_tools.yaml` の `claude_code` ブロック (L123-129) は `python_wrapper` を持たない (sandbox_exposed: false)。本 plan で YAML は **触らない** — Plan 02 の自動生成再実行で `mcp_helper.py` 等は touch されない (claude_code の python wrapper 行が YAML に元々ない)。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph &amp;&amp; uv run pytest tests/test_claude_code_no_cwd_arg.py -x -v 2>&amp;1 | tail -20 ; echo "--- caller check ---" ; grep -rn "claude_code(" app/ agents/ scripts/ 2>/dev/null | grep -v ".pyc" | head -5 | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - source assertion: `python3 -c "import inspect; from mcp_server.tools.claude_code import claude_code; assert 'cwd' not in inspect.signature(claude_code).parameters"` exit 0
    - source assertion: `grep -E "^async def claude_code\(prompt: str, headers" mcp_server/tools/claude_code.py` ヒット (新シグネチャ)
    - source assertion: `grep -c "cwd: str = " mcp_server/tools/claude_code.py` count 0 (cwd 引数ハードコードが消えている)
    - source assertion: `grep -E "from mcp_server\.tools\.execute_python import" mcp_server/tools/claude_code.py` ヒット (helper を import 再利用)
    - source assertion: `grep -c "claude_code_with_headers" mcp_server/tools/claude_code.py` count >= 1 (wrapper 存在)
    - source assertion: `grep -c "_save_overflow_output\|OUTPUT_DIR" mcp_server/tools/claude_code.py` count >= 2 (overflow output 処理が **残っている** — D-09 維持)
    - source assertion: 外部 caller 数 — `grep -rn "claude_code(" app/ agents/ scripts/ 2>/dev/null | grep -v ".pyc" | wc -l` count 0
    - source assertion (checker I8 — claude_code 関数シグネチャ削除と YAML SSoT の乖離防止): `config/mcp_tools.yaml` の `claude_code` セクション (L120 付近) の `args:` に `cwd` 名前付きエントリが **存在しないこと** を確認 — `grep -nE "^\s*-\s*name:\s*cwd" config/mcp_tools.yaml` の出力に claude_code ブロック (L120 付近) 配下の cwd args が **含まれていないこと**。もし元々 YAML に claude_code の cwd args 定義がない場合は本 criterion で「present でない」状態が現状維持として担保される。
    - test command: `uv run pytest tests/test_claude_code_no_cwd_arg.py::test_signature_has_no_cwd -x` exit 0
    - behavior assertion: tests/test_outputs_route.py::test_get_output_works_for_claude_code (Plan 02 で skip 維持されたケース) も本 task 完了時に外せる状態になる — VALIDATION.md 38-04-02 として Plan 04 で実装するため本 plan では skip 維持
  </acceptance_criteria>
  <done>claude_code から cwd 引数が削除され、headers 引数経由で `_generated/` 固定実行に統一。execute_python の helper を import 再利用で DRY を維持。test_signature_has_no_cwd が green、外部 caller ゼロを再確認。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP tool wrapper ↔ subprocess | subprocess の cwd と env が外部 input (headers) に依存 |
| `_generated/` filesystem | sandbox subprocess が任意ファイルを書き込み可能、AI が間接的に制御 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-03-01 | Tampering | x-thread-id headers に `../` を埋め込んで thread folder 外で subprocess 実行 | mitigate | `_resolve_generated_folder` 内の realpath prefix guard で `/shared/thread-files/` 配下外なら `/tmp` fallback、test で逸脱しないことを assert |
| T-38-03-02 | Spoofing | x-github-login を偽装して他 user の `_generated/` に書き込み | accept (Phase 11/36 の前提) | RPCContext 伝搬 (Phase 37 Route A) が信頼境界内 — worker → mcp-server は内部 RPC で JWT cookie は API 層で検証済。MCP tool 単体の防御は HTTP route 側 `_resolve_thread_folder` (Plan 02) で担う (D-19) |
| T-38-03-03 | DoS | AI が `_generated/` に巨大ファイル / 大量ファイルを書き込み (disk DoS) | accept (v6.1+ で再検討) | RESEARCH §"Phase 38 で新規出現するセキュリティ懸念" L985-986 — 観察ベースで v6.1+ に GC / quota 検討。本 plan では実装しない |
| T-38-03-04 | Tampering | snapshot diff の race condition (subprocess 実行中に外部からファイル追加) | accept | sandbox subprocess は分離プロセス、外部 actor が same folder に同時書き込みする想定がない (multi-tenant で別 thread fold を見ることもない) |
| T-38-03-05 | Information Disclosure | 中間ファイル (`.pyc`) が AI コンテキストに漏れる | mitigate | `_PYC_EXCLUDES` で `.pyc` を rename 対象から除外、test_excludes_pyc_files で assert |
</threat_model>

<verification>
- rename ロジック: `uv run pytest tests/test_post_process_rename.py -x` 全 green
- execute_python: `uv run pytest tests/test_execute_python_output.py -x` 全 green
- claude_code シグネチャ: `uv run pytest tests/test_claude_code_no_cwd_arg.py -x` green
- 外部 caller 影響: `grep -rn "claude_code(" app/ agents/ scripts/ 2>/dev/null | grep -v ".pyc" | wc -l` == 0
- 既存テスト回帰: `uv run pytest tests/test_execute_python.py tests/test_claude_code.py -x` (存在すれば) green
</verification>

<success_criteria>
- execute_python が headers から `_generated/` を解決して subprocess cwd に渡し、headers 不足時は `/tmp` fallback で既存挙動と互換維持 (FOUT-01)
- claude_code から cwd 引数が削除され、`_generated/` で固定実行 + 外部 caller への影響ゼロ (FOUT-02)
- snapshot diff 方式の post-process rename が `.pyc` 除外 + 既存 prefix スキップ + 新規ファイルのみリネームを正しく実装 (D-10 / D-11)
- 関連 5 ケース (test_post_process_rename 3 + test_execute_python_output 2) + signature 1 = 計 6 ケース green
- Plan 04 (handler turn-delta bundle) が依存できる確定実装
</success_criteria>

<output>
After completion, create `.planning/phases/38-worker-dl/38-03-SUMMARY.md`
</output>
