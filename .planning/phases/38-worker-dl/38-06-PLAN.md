---
phase: 38
plan: 6
plan_id: 38-06-integration-adr-and-patterns
type: execute
wave: 4
depends_on:
  - 38-02-backend-route-and-mcp-list
  - 38-03-sandbox-cwd-and-rename
  - 38-04-handler-bundle-and-helper
  - 38-05-frontend-modal-and-renderers
files_modified:
  - docs/adr/0052-worker-generated-outputs-storage-and-preview.md
  - docs/adr/INDEX.md
  - .planning/adr-categories.yaml
  - .planning/patterns.md
  - .planning/phases/38-worker-dl/38-VALIDATION.md
  - .planning/phases/36-text-code-image-multimodal/deferred-items.md
autonomous: false
requirements: [FOUT-01, FOUT-02, FOUT-03, FOUT-04]
tags: [integration-check, adr, patterns, milestone-close, multi-user-isolation]
objective: "docker compose 上で Phase 38 全機能の E2E acceptance check (multi-user isolation + 過去スレッド再取得 + 全 4 renderer + claude_code 経路) を実施、ADR-0052 を新規起票、patterns.md / INDEX.md / adr-categories.yaml を更新、38-VALIDATION.md を nyquist_compliant: true でクローズ、orchestrator_handler bundle 対応など v6.1+ 持ち越し項目を Phase 36 deferred-items.md と統合"

must_haves:
  truths:
    - "docker compose up で 1 ユーザーが execute_python / claude_code 経由でファイルを生成し、チップ → Modal で 4 種類すべての preview (image/md/csv/text) を確認できる"
    - "過去スレッドを開き直しても AI message に bundle されたチップが復元され、Modal を再表示できる (FOUT-04 sc4 の手動 acceptance)"
    - "別 user JWT で /api/threads/{tid}/outputs/{name} に curl を叩くと 401/404 が返る (FOUT-04 sc5 の手動 acceptance)"
    - "ADR-0052 (Worker Generated Outputs: Storage / Preview / Per-user Persistence) が docs/adr/ に起票され、INDEX.md と adr-categories.yaml に追加されている"
    - "patterns.md に Phase 38 由来のパターン (例: `kind discriminator unification` / `snapshot-diff post-process rename`) が手動追記されている (CLAUDE.md / D-15 ADR Pattern Reference 規約)"
    - "38-VALIDATION.md の frontmatter `nyquist_compliant: true` / `wave_0_complete: true` に更新され、Approval: approved に書き換わっている"
    - "v6.1+ 持ち越し項目 (orchestrator_handler bundle 対応 / 個別削除 UI / 横断 My Files / GC / PDF preview) が Phase 36 deferred-items.md に統合 or 新規セクションで明示されている"
    - "D-02: ライフサイクルは thread 削除 hook (ADR-0048 由来) の親フォルダ一括 `rm -rf` で十分とし、`_generated/` 個別削除 API は **新設しない** — v6.1+ 持ち越し項目として deferred-items.md に明示 (CONTEXT.md)"
    - "D-16 / D-17 / D-19 補完: 横断 'My Files' 画面・一覧 endpoint `GET /api/threads/{tid}/outputs`・Phase 38 独自 isolation 単体テストは **いずれも本 phase で新設しない** 否定的決定群を deferred-items.md にまとめて記録、後続 phase が再議論する際の出発点を保存する (CONTEXT.md)"
  artifacts:
    - path: "docs/adr/0052-worker-generated-outputs-storage-and-preview.md"
      provides: "Phase 38 設計決定 (D-01..D-30) のサマリ ADR"
      contains: "FOUT-01"
    - path: "docs/adr/INDEX.md"
      provides: "0052 エントリ追加 (auto-generated)"
    - path: ".planning/adr-categories.yaml"
      provides: "0052 のカテゴリマッピング (Data・Persistence または Frontend・UI)"
    - path: ".planning/patterns.md"
      provides: "Phase 38 由来パターンの手動追記"
    - path: ".planning/phases/38-worker-dl/38-VALIDATION.md"
      provides: "nyquist_compliant: true で clozed"
    - path: ".planning/phases/36-text-code-image-multimodal/deferred-items.md"
      provides: "Phase 38 完了報告 + v6.1+ 持ち越し統合"
  key_links:
    - from: "docs/adr/0052-*.md"
      to: ".planning/adr-categories.yaml"
      via: "番号 + カテゴリ追記"
      pattern: "0052"
    - from: ".planning/patterns.md"
      to: "ADR-0052 への相互リンク"
      via: "関連 ADR セクション"
      pattern: "0052"
    - from: ".planning/phases/38-worker-dl/38-VALIDATION.md frontmatter"
      to: "approval: approved"
      via: "全タスク green 確認後の手動更新"
      pattern: "nyquist_compliant: true"
---

<objective>
Phase 38 の milestone close 手前で、(1) 実機 E2E acceptance、(2) 設計ドキュメント化 (ADR + patterns)、(3) v6.1+ 持ち越し項目の整理を一括で行う。Phase 35 / 36 / 37 と同じクローズパターンを踏襲する。

Purpose:
- FOUT-01..04 success criteria が docker compose 環境で実際に動作することを「実機 E2E checklist」で証明する (VALIDATION.md 38-06-01)
- ADR-0052 で Phase 38 の設計決定を歴史的に保存し、後続の v6.1+ phase が参照できるようにする
- patterns.md に Phase 38 由来の再利用可能パターンを記録し、将来の phase で wheel-reinvention を防ぐ
- 「やらない」と決めた項目 (orchestrator bundle / 個別削除 UI / 横断 My Files / PDF preview / GC 等) を deferred-items に明示し、phase close をクリーンに保つ

このプランは **`autonomous: false`** — Task 1 (E2E acceptance) は人手検証必須。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/patterns.md
@.planning/adr-categories.yaml
@docs/adr/INDEX.md
@.planning/phases/38-worker-dl/38-VALIDATION.md
@.planning/phases/38-worker-dl/38-CONTEXT.md
@.planning/phases/38-worker-dl/38-RESEARCH.md
@.planning/phases/38-worker-dl/38-UI-SPEC.md
@.planning/phases/36-text-code-image-multimodal/deferred-items.md
@docs/adr/0048-thread-files-folder-convention.md
@docs/adr/0050-copilot-sdk-multimodal-attachments.md
@docs/adr/0044-mcp-tool-catalog-single-source-of-truth.md

<interfaces>
<!-- adr-categories.yaml 構造 (新規 ADR 追加時) -->
<!-- ファイル末尾の対応カテゴリに番号を追記 -->

<!-- patterns.md カテゴリ (7 種) -->
<!-- Auth / LangGraph・Graph / MCP・Tools / Worker・Jobs / Frontend・UI / Infra・Deploy / Data・Persistence -->

<!-- VALIDATION.md frontmatter (現状 nyquist_compliant: false, wave_0_complete: false, Approval: pending) -->

<!-- 38-CONTEXT.md / 38-RESEARCH.md の deferred セクションを参照して v6.1+ 項目を集約 -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: docker compose 上で Phase 38 acceptance checklist を手動実行する (VALIDATION 38-06-01)</name>
  <what-built>
    - Plan 01-05 で実装された Phase 38 全機能 (outputs route + MCP attachments_list + cwd 切替 + post-process rename + handler bundle + AttachmentModal + 4 renderer)
  </what-built>
  <how-to-verify>
    `docker compose up -d` 起動後、以下の checklist を 1 項目ずつ実行:

    **FOUT-01 (execute_python 生成ファイル DL):**
    - [ ] Chat アプリで「matplotlib で sin カーブを output.png に保存して」prompt を送信
    - [ ] AI 応答下にチップが現れる、`✨ AI 生成` micro-badge 表示
    - [ ] チップをクリック → AttachmentModal で画像が表示される
    - [ ] Modal の「ダウンロード」CTA → ブラウザがファイルを保存
    - [ ] DL したファイル名が `{timestamp}_output.png` 形式 (D-03 命名規約)

    **FOUT-02 (claude_code workspace 成果物取得):**
    - [ ] Chat アプリで claude_code が `.md` / `.py` 等を `_generated/` に保存する prompt を送信 (例: 「claude_code で hello.py を保存して」)
    - [ ] AI 応答下にチップが現れる
    - [ ] Modal で内容を確認できる (Markdown / Monaco)
    - [ ] DL も動作

    **FOUT-03 (4 種 preview):**
    - [ ] 画像 (.png / .jpg / .webp / .gif) → Modal で `<img>` 表示
    - [ ] Markdown (.md) → react-markdown で GFM テーブル含めて表示
    - [ ] CSV (.csv) → ag-grid でテーブル表示、1000 行超なら「先頭 1000 行のみ」バナー
    - [ ] プレーンテキスト (.txt / .py / .json / .yaml / .log) → Monaco read-only + syntax highlight
    - [ ] PDF (.pdf) → 「PDF はプレビュー非対応です」フォールバック + DL CTA

    **FOUT-04 sc4 (過去スレッド再取得):**
    - [ ] 別スレッドに切替 → 元のスレッドに戻る → AI message に bundle されたチップが復元される
    - [ ] チップクリック → Modal で同じファイルを再閲覧できる
    - [ ] ブラウザを reload しても同様に復元される (PostgreSQL checkpoint 経由)

    **FOUT-04 sc5 (multi-user isolation):**
    - [ ] User A で thread を作って画像生成
    - [ ] User B でログインして直接 `curl -b "access_token=<userB-jwt>" http://localhost:8000/api/threads/<userA-tid>/outputs/<name>` を実行 → 401 / 404 が返る
    - [ ] User B で path traversal `curl -b "..." "http://localhost:8000/api/threads/<userA-tid>/outputs/$(python3 -c 'import urllib.parse; print(urllib.parse.quote(\"../../../etc/passwd\"))')"` → 400 / 404
    - [ ] AttachmentModal のエラーバナー「このファイルにはアクセスできません」/「このファイルは削除されたか、ストレージから取得できません」が表示される (Modal を別 user で開いた場合)

    **エッジケース:**
    - [ ] 大きいファイル (>1MB text / >10MB image) で size cap 案内 + DL CTA が出る
    - [ ] AI が `_generated/` 内に同名 `output.png` を 2 回生成 → 2 ファイルとも timestamp prefix 付きで保持される (履歴 immutable)
    - [ ] thread 削除 (ThreadSidebar の delete) → `_generated/` も親フォルダごと削除される (ADR-0048 hook 経由、D-02)
    - [ ] dark mode (`[data-theme="dark"]`) で Modal が破綻しない
    - [ ] モバイル幅 375px で Modal が full-screen 化、CTA が full-width block 化

    **CI / test 整合:**
    - [ ] `uv run pytest tests/ -x --ignore=tests/test_api_chat.py` で full suite green
    - [ ] `python3 scripts/generate_mcp_artifacts.py --check` exit 0
    - [ ] `bash scripts/install-hooks.sh` を再実行 → pre-commit hook で `--check` がブロックしない
    - [ ] `cd frontend && bun run tsc --noEmit` exit 0

    **不合格時の戻り先 (checker W5):**
    - FOUT-01 fail (execute_python が `_generated/` に書かない / DL できない) → Plan 03 Task 1 revisit (`_resolve_generated_folder` + cwd 切替 + post-process rename)
    - FOUT-02 fail (claude_code 生成物が取得できない / cwd が反映されない) → Plan 03 Task 2 もしくは Plan 04 Task 2 (handler bundle scan が claude_code 経路を拾えていない) revisit
    - FOUT-03 fail (4 種 renderer が動かない / プレビュー破綻 / size cap 文言ミス / モバイル幅破綻) → Plan 05 Task 1 revisit
    - FOUT-04 sc4 fail (履歴復元できない / スレッド再オープン時にチップが消える) → Plan 04 Task 2 revisit (turn-delta bundle ロジック)
    - FOUT-04 sc5 fail (multi-user isolation 破れ / 別 user で他人ファイル取得できてしまう) → Plan 02 Task 1 revisit (`_resolve_thread_folder` import 再利用 + Phase 36 helper の outputs route 適用)
  </how-to-verify>
  <resume-signal>Type "approved" (すべて pass) または不具合を列挙 (例: "5. CSV preview で 1000 行 cap が動かない")</resume-signal>
</task>

<task type="auto">
  <name>Task 2: ADR-0052 を起票し INDEX.md と adr-categories.yaml に追加する</name>
  <files>docs/adr/0052-worker-generated-outputs-storage-and-preview.md, docs/adr/INDEX.md, .planning/adr-categories.yaml</files>
  <read_first>
    - docs/adr/0048-thread-files-folder-convention.md — Phase 37 ADR analog (構造とトーン)
    - docs/adr/0050-copilot-sdk-multimodal-attachments.md — Phase 36 ADR analog (additional_kwargs envelope)
    - docs/adr/0044-mcp-tool-catalog-single-source-of-truth.md — Phase 30 ADR analog
    - .planning/adr-categories.yaml — カテゴリ追記の YAML 構造
    - docs/adr/INDEX.md — auto-generated, 触らない (Phase 26 ADR INDEX hook 経由で再生成)
    - .planning/phases/38-worker-dl/38-CONTEXT.md (decisions D-01..D-19) と Plan 01 で確定した D-30
    - .planning/phases/38-worker-dl/38-RESEARCH.md §Summary / §Pattern 1-8 / §Common Pitfalls
    - .planning/phases/38-worker-dl/38-UI-SPEC.md
    - scripts/generate_adr_index.py — INDEX.md 自動生成スクリプト (pre-commit hook が呼ぶ)
  </read_first>
  <behavior>
    - ADR-0052 が docs/adr/0052-worker-generated-outputs-storage-and-preview.md に起票され、Phase 38 の主要 19+1 設計決定 (D-01..D-19, D-30) と patterns/pitfalls の要約、関連 ADR (0048 / 0050 / 0044) へのリンクを含む
    - `.planning/adr-categories.yaml` に 0052 が追加され、カテゴリは `Data・Persistence` (storage 規約と message bundle) または分離して `Frontend・UI` (preview Modal) のどちらか — D-15 ADR Pattern Reference 規約に従い planner 判断 (推奨: `Data・Persistence` 主、UI 側は patterns.md `Frontend・UI` セクションに別途追記)
    - `docs/adr/INDEX.md` は `scripts/generate_adr_index.py` の自動生成で 0052 エントリが追加される (手書きしない、CLAUDE.md §"ADR INDEX 自動生成 hook")
  </behavior>
  <action>
    1. `docs/adr/0052-worker-generated-outputs-storage-and-preview.md` を新規作成 (ADR-0048 / 0050 の構造踏襲):
       - **Title**: `0052: Worker Generated Outputs — Storage / Preview / Per-User Persistence`
       - **Status**: Accepted
       - **Date**: 2026-05-12 (or 完了日)
       - **Context**: Phase 38 ゴール (FOUT-01..04)、Phase 37 ADR-0048 の `thread-files` 規約と Phase 36 ADR-0050 の `additional_kwargs` envelope を前提に、`_generated/` サブフォルダ + AI 生成ファイル UX を Phase 36 アップロード添付と統一する設計を確立した。
       - **Decision (主要セクション)**:
         - D-01..D-04: ストレージ規約 (`/shared/thread-files/<login>/<tid>/_generated/`、命名 `{ts}_{name}`、volume 既存再利用、thread 削除 hook で一括削除)
         - D-05..D-07: HTTP route 分離 + MCP attachments_list 戻り値拡張 (kind discriminator) + YAML SSoT 再生成
         - D-08..D-11: sandbox 直接書き込み (execute_python cwd 切替 + claude_code cwd 引数削除 + tool wrapper の post-process snapshot-diff rename)
         - D-12..D-15: UI 統一 (4 renderer モーダル + チップ kind ラベル + 過去スレッド bundle)
         - D-16..D-19: AI 視認性 (SystemMessage prepend kind 表示 + handler turn-delta bundle + helper 再利用による isolation 継承)
         - **D-30 (Plan 01 で確定)**: AttachmentMeta.kind を 'user_upload' | 'generated' に enum 化 (D-30 案 A)、legacy 'file' は API 層で正規化
       - **Consequences**:
         - 利点: tool 数膨張ゼロ (outputs_list 新設なし)、新規 npm dep ゼロ、新規 CSS 変数ゼロ、Phase 36 multi-user isolation テストパス継承
         - 欠点 / TBD: AI が `_generated/` に disk DoS 級の書き込みをした場合の GC は v6.1+、orchestrator (SuperChat) 経路の bundle 対応は v6.1+ (本 phase scope は langgraph_handler のみ)、PDF/HTML preview は v6.1+
       - **Related ADRs**: 0048 (folder 規約)、0050 (additional_kwargs envelope)、0044 (MCP YAML SSoT)、0038 (LangGraph checkpoint 永続化)、0014 (JWT cookie 認証)、0023 (claude_code overflow output 維持理由)
       - **Implementation**: Plan 01..06 の commit リスト + 関連ファイル列挙 (`mcp_server/tools/execute_python.py` 等)

    2. `.planning/adr-categories.yaml` に 0052 のマッピングを追加 (CLAUDE.md §"ADR INDEX 自動生成 hook" 規約):
       - YAML 構造を確認 (Read で先頭 30 行) → 既存形式に従って `Data・Persistence` カテゴリに `- 0052` を追加
       - 主分類は `Data・Persistence` (storage 規約 + message bundle persistence が中核)
       - UI 関連は patterns.md `Frontend・UI` セクション側で別途扱う (本 ADR は cross-cutting なので主分類を 1 つだけ選ぶ)

    3. `docs/adr/INDEX.md` は **手書きしない** — `scripts/generate_adr_index.py` を実行 (pre-commit hook が CI で自動再生成するが、commit 前に手動実行で確認):
       `python3 scripts/generate_adr_index.py`
       → 0052 エントリが INDEX.md に追加されることを確認

    4. drift 確認: `python3 scripts/generate_adr_index.py --check` (もし `--check` フラグ実装されていれば) または `git diff docs/adr/INDEX.md` で 0052 行のみ追加されているか確認。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph &amp;&amp; ls docs/adr/0052-worker-generated-outputs-storage-and-preview.md &amp;&amp; grep -c "0052" docs/adr/INDEX.md &amp;&amp; grep -c "0052" .planning/adr-categories.yaml</automated>
  </verify>
  <acceptance_criteria>
    - source assertion: `test -f docs/adr/0052-worker-generated-outputs-storage-and-preview.md` exit 0
    - source assertion: `grep -c "Phase 38" docs/adr/0052-worker-generated-outputs-storage-and-preview.md` count >= 1
    - source assertion: `grep -E "FOUT-01.*FOUT-02.*FOUT-03.*FOUT-04|FOUT-01|FOUT-02|FOUT-03|FOUT-04" docs/adr/0052-worker-generated-outputs-storage-and-preview.md` ヒット
    - source assertion: `grep -E "0048|0050|0044|0038" docs/adr/0052-worker-generated-outputs-storage-and-preview.md` ヒット (関連 ADR リンク)
    - source assertion: `grep -c "0052" .planning/adr-categories.yaml` count >= 1
    - source assertion: `grep -c "0052" docs/adr/INDEX.md` count >= 1
    - behavior assertion: pre-commit hook (もしインストール済なら) で `python3 scripts/generate_adr_index.py --check` が exit 0 を返す (`--check` フラグが存在しなければ手動 diff 確認)
  </acceptance_criteria>
  <done>ADR-0052 が Phase 38 設計決定の歴史的記録として確立、カテゴリ YAML と INDEX.md が整合する。</done>
</task>

<task type="auto">
  <name>Task 3: patterns.md に Phase 38 由来パターンを手動追記する</name>
  <files>.planning/patterns.md</files>
  <read_first>
    - .planning/patterns.md 全体 (既存カテゴリ 7 種の構造と 1 entry の体裁 — 5-10 行、関連 ADR リンク)
    - .planning/phases/38-worker-dl/38-CONTEXT.md `<specifics>` セクション (re-usable パターンの発見)
    - .planning/phases/38-worker-dl/38-RESEARCH.md §"Pattern 1-8"
    - CLAUDE.md §"ADR Pattern Reference (GSD Integration)" — patterns.md は手動更新、ADR 唯一の真実源、1 entry 5-10 行
    - Task 2 で起票した ADR-0052
  </read_first>
  <behavior>
    - patterns.md の `Data・Persistence` カテゴリに「`_generated/` サブフォルダで input/output を分離する thread-files 拡張」エントリが追加される
    - 同 `Data・Persistence` または `LangGraph・Graph` に「AIMessage.additional_kwargs.attachments を AI 生成ファイルの per-turn 搬送に使う」エントリが追加される (HumanMessage 側との対称形を明記)
    - `MCP・Tools` カテゴリに「snapshot-diff 方式の post-process rename パターン」エントリが追加される
    - `Frontend・UI` カテゴリに「kind discriminator による input/output 統一 UX (チップ + モーダルプレビュー)」エントリが追加される
    - 各 entry は CLAUDE.md 規約通り 5-10 行、関連 ADR (0052 主、0048/0050/0044 副) へのリンクを含む
    - patterns.md の構造 (カテゴリ階層) は壊さない
  </behavior>
  <action>
    1. `.planning/patterns.md` を Read で全体構造を確認。

    2. 以下 4 エントリを各カテゴリの末尾に追記:

       **`Data・Persistence` カテゴリに追記:**
       ```markdown
       ### thread-files `_generated/` サブフォルダで input/output を分離
       Phase 37 で確立した `/shared/thread-files/<login>/<tid>/` 規約 (ADR-0048) を `_generated/` サブフォルダで拡張し、
       ユーザーアップロード (Phase 36) と AI 生成成果物 (Phase 38) を同 volume 内で明示分離する。
       ライフサイクルは親フォルダ単位 — thread 削除 hook (chat.delete_thread → shutil.rmtree) で一括削除。
       命名規約は `YYYYMMDDTHHMMSS_{name}` を MCP tool wrapper の post-process で必ず付与し、
       AI / UI / API レイヤーで同一 identity 文字列を扱う。
       関連 ADR: [0052](../docs/adr/0052-worker-generated-outputs-storage-and-preview.md), [0048](../docs/adr/0048-thread-files-folder-convention.md)
       ```

       **`LangGraph・Graph` カテゴリに追記:**
       ```markdown
       ### AIMessage.additional_kwargs.attachments で AI 生成ファイルを turn 単位で bundle
       Phase 36 で HumanMessage 側に確立した additional_kwargs envelope パターンを AIMessage 側にも適用する。
       turn 完了時 (graph.astream_events の on_chain_end) に handler が `_generated/` の delta を再 scan し、
       final AIMessage の additional_kwargs に `{"attachments": [...]}` を merge (None-guard + `|` 演算子) で追加する。
       AsyncPostgresSaver の JSONB に round-trip 保存され、スレッド再オープン時に frontend へ自動復元される。
       Wave 0 で AIMessage 側の round-trip を独立検証することで、AIMessage.name 喪失問題 (ADR-0038) と分離して扱える。
       関連 ADR: [0052](../docs/adr/0052-worker-generated-outputs-storage-and-preview.md), [0050](../docs/adr/0050-copilot-sdk-multimodal-attachments.md), [0038](../docs/adr/0038-superchat-context-messages-and-agent-name-persistence.md)
       ```

       **`MCP・Tools` カテゴリに追記:**
       ```markdown
       ### snapshot-diff 方式の post-process rename パターン
       sandbox subprocess (execute_python / claude_code) が cwd に書いた新規ファイルを、
       MCP tool wrapper が実行前後の `os.listdir` snapshot 差分で検出して `{ts}_{basename}` にリネームする。
       mtime 判定は Docker volume の NFS / 9p mount で解像度が劣化するため不採用、
       inotify は Linux 専用で過剰。snapshot diff は ≤ 20 LOC で実装でき、`.pyc` 等の中間ファイルを除外フィルタで弾く。
       既に `YYYYMMDDTHHMMSS_` prefix が付いているファイルは二重 prefix 回避でスキップ。
       handler 側 (turn-delta scan) と二重 rename しないため、wrapper のみが authoritative。
       関連 ADR: [0052](../docs/adr/0052-worker-generated-outputs-storage-and-preview.md)
       ```

       **`Frontend・UI` カテゴリに追記:**
       ```markdown
       ### kind discriminator による input/output 統一 UX (チップ + モーダルプレビュー)
       AttachmentMeta に `kind: 'user_upload' | 'generated'` 単一 discriminator を持たせ、
       MCP 戻り値 / SystemMessage prepend / AIMessage bundle / AttachmentChipRow props / URL 解決すべてで同 enum を通す。
       URL 解決は `kind === 'generated' ? '/outputs/' : '/attachments/'` の segment 切替で 1 関数化、
       AI 応答テキスト内の inline 描画 (`![]()` 経路) は明示的に避けて UX の不連続を解消する。
       4 種 renderer (image=`<img>` / markdown=react-markdown / csv=ag-grid / text=Monaco) を lazy import + Suspense で dispatch、
       MarkdownPreview は重い MarkdownMessage を呼ばず react-markdown を薄ラッパーで直接呼ぶ。
       新規 npm dep / 新規 CSS 変数ゼロで実装可能 (既存 token と Phase 35/36 で導入済の dep のみ流用)。
       関連 ADR: [0052](../docs/adr/0052-worker-generated-outputs-storage-and-preview.md)
       ```

    3. 各エントリは CLAUDE.md 規約 (5-10 行、関連 ADR リンク必須、ADR にないパターンは載せない) を遵守。

    4. 既存 entry を **変更しない** (D-08: ADR が唯一の真実源、patterns.md は要約)。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph &amp;&amp; grep -c "0052" .planning/patterns.md ; echo "---" ; grep -E "^###" .planning/patterns.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - source assertion: `grep -c "0052" .planning/patterns.md` count >= 4 (4 エントリすべてに ADR-0052 リンク)
    - source assertion: `grep -E "_generated/" .planning/patterns.md` ヒット (Data・Persistence エントリ)
    - source assertion: `grep -E "snapshot.{0,3}diff|snapshot diff" .planning/patterns.md` ヒット (MCP・Tools エントリ)
    - source assertion: `grep -E "kind discriminator|kind 単一 discriminator" .planning/patterns.md` ヒット (Frontend・UI エントリ)
    - source assertion: `grep -E "AIMessage.additional_kwargs|additional_kwargs.attachments" .planning/patterns.md` count >= 2 (Phase 38 + Phase 36 既存)
    - source assertion: `grep -c "^### " .planning/patterns.md` が事前カウント + 4 と一致 (新規 4 エントリ追加、既存破壊なし)
  </acceptance_criteria>
  <done>patterns.md に Phase 38 由来パターンが 4 エントリ手動追記され、将来の phase が ADR-0052 経由でパターンを再利用できる状態。</done>
</task>

<task type="auto">
  <name>Task 4: 38-VALIDATION.md を nyquist_compliant: true でクローズ、deferred-items を v6.1+ 持ち越しで更新する</name>
  <files>.planning/phases/38-worker-dl/38-VALIDATION.md, .planning/phases/36-text-code-image-multimodal/deferred-items.md</files>
  <read_first>
    - .planning/phases/38-worker-dl/38-VALIDATION.md 全体 (現状 frontmatter `nyquist_compliant: false, wave_0_complete: false`, Approval: pending)
    - .planning/phases/36-text-code-image-multimodal/deferred-items.md — Phase 36 から繰り越された項目
    - .planning/phases/38-worker-dl/38-CONTEXT.md `<deferred>` セクション (L189-205) — v6.1+ 持ち越し候補
    - .planning/phases/38-worker-dl/38-RESEARCH.md §"State of the Art" / §"Phase 38 で新規出現するセキュリティ懸念"
    - .planning/STATE.md — Phase 38 完了時に更新する位置 (今回はこの plan ではなく phase verifier で更新)
  </read_first>
  <behavior>
    - 38-VALIDATION.md frontmatter:
      - `status: complete`
      - `nyquist_compliant: true`
      - `wave_0_complete: true`
      - 末尾の `**Approval:** pending` → `**Approval:** approved (2026-05-12)` に更新
      - Per-Task Verification Map の全行の `Status` 列を実際の実行結果に応じて `✅ green` / `manual` 等で埋める
    - deferred-items.md (Phase 36):
      - 新セクション「Phase 38 完了報告 + v6.1+ 持ち越し」を追加
      - 持ち越し項目: (a) orchestrator_handler の AIMessage bundle 対応、(b) 個別削除 UI、(c) 横断 My Files / Header dropdown、(d) PDF preview、(e) HTML preview、(f) AI 生成ファイル GC / TTL、(g) AttachmentModal サイズ cap 観察ベース調整、(h) papaparse 追加検討、(i) `session-state/files/` AI 応答テキスト残留マッピング、(j) AI 生成ファイル toast/通知
      - 各項目に 1 行の根拠と Phase 38 CONTEXT.md / RESEARCH.md の参照
  </behavior>
  <action>
    1. `.planning/phases/38-worker-dl/38-VALIDATION.md` の frontmatter を編集:
       ```yaml
       ---
       phase: 38
       slug: worker-dl
       status: complete           # ← draft から complete
       nyquist_compliant: true    # ← false から true
       wave_0_complete: true      # ← false から true
       created: 2026-05-12
       closed: 2026-05-12         # 新規追加
       ---
       ```

    2. Per-Task Verification Map (38-01-01 〜 38-06-01) の `Status` 列をすべて更新:
       - 38-01-01..38-04-03 (automated): 全 ✅ green
       - 38-05-01 (manual): ✅ manual passed
       - 38-06-01 (manual): ✅ manual passed
       Task 1 (本 plan の checkpoint) が approved の場合に限り更新。もし checkpoint で未解決の項目があれば手戻り → 該当 Plan に戻して修正後再実行。

    3. 末尾の `**Approval:** pending` を `**Approval:** approved (2026-05-12)` に置換 + 一文「全 14 タスク (12 automated + 2 manual) PASS、Phase 38 success criteria 5 件すべて充足」を追加。

    4. `.planning/phases/36-text-code-image-multimodal/deferred-items.md` の末尾に新セクション追加:
       ```markdown
       ---

       ## Phase 38 完了報告 (2026-05-12) + v6.1+ 持ち越し

       Phase 38 (ファイル出力 — worker 生成 DL + プレビュー + ユーザー別保持) は FOUT-01..04 充足で完了。
       本 phase で **意図的にやらなかった項目** は v6.1+ で観察ベース判断 (CONTEXT.md `<deferred>` 参照):

       - **orchestrator_handler の AIMessage bundle 対応**: 本 phase scope は langgraph_handler (Chat / Canvas) のみ。SuperChat (OrchestratorGraph) の bundle 対応は実利用観察次第で v6.1+ — 参照: 38-04-PLAN.md Task 2 action #2
       - **個別ファイル削除 UI** (`DELETE /api/threads/{tid}/outputs/{name}`): D-02 / 38-CONTEXT.md `<deferred>` で却下、thread 削除 hook で十分
       - **横断 "My Files" 画面 / Header dropdown**: D-16 / 38-CONTEXT.md `<deferred>` で v6.1+ — スレッド経由で十分の判断
       - **PDF preview (pdf.js / iframe)**: D-12 / 38-CONTEXT.md `<deferred>` — bundle サイズと CSP 設計が必要
       - **HTML preview**: D-12 / 38-CONTEXT.md `<deferred>` — Canvas (Phase 16/18) と用途衝突
       - **AI 生成ファイル GC / TTL**: 38-CONTEXT.md `<deferred>` + 38-RESEARCH.md §"Phase 38 で新規出現するセキュリティ懸念" — 観察ベースで quota / 件数上限 / TTL を検討
       - **AttachmentModal サイズ cap の閾値調整**: 38-RESEARCH.md §"Open Question 3" — 暫定 1MB (text/CSV/MD) / 10MB (画像)、観察ベースで再検討
       - **papaparse 追加**: 38-RESEARCH.md §"Standard Stack §Supporting" — embedded comma / multiline quoted cells の edge case 頻出時に検討
       - **`session-state/files/` AI 応答テキスト残留マッピング**: 38-CONTEXT.md `<deferred>` — D-13 で inline 描画しない方針のため不要、prompt 抑制 hint は planner 判断で SystemMessage に 1 文追加 (38-RESEARCH.md Open Question 1)
       - **AI 生成完了 toast / 通知**: 38-CONTEXT.md `<deferred>` — v6.1+ UI polish
       ```

    5. STATE.md は本 plan では更新しない (phase verifier 経由で更新する別フロー)。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph &amp;&amp; grep "nyquist_compliant: true" .planning/phases/38-worker-dl/38-VALIDATION.md &amp;&amp; grep "approved" .planning/phases/38-worker-dl/38-VALIDATION.md &amp;&amp; grep "Phase 38 完了報告" .planning/phases/36-text-code-image-multimodal/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - source assertion: `grep -E "^nyquist_compliant:\s*true" .planning/phases/38-worker-dl/38-VALIDATION.md` ヒット
    - source assertion: `grep -E "^wave_0_complete:\s*true" .planning/phases/38-worker-dl/38-VALIDATION.md` ヒット
    - source assertion: `grep -E "^status:\s*complete" .planning/phases/38-worker-dl/38-VALIDATION.md` ヒット
    - source assertion: `grep -c "✅" .planning/phases/38-worker-dl/38-VALIDATION.md` count >= 14 (全 task 行が green)
    - source assertion: `grep -c "Approval.*approved" .planning/phases/38-worker-dl/38-VALIDATION.md` count >= 1
    - source assertion: `grep -c "Phase 38 完了報告" .planning/phases/36-text-code-image-multimodal/deferred-items.md` count >= 1
    - source assertion: `grep -c "orchestrator_handler" .planning/phases/36-text-code-image-multimodal/deferred-items.md` count >= 1 (持ち越し項目存在)
    - source assertion: `grep -c "v6.1+" .planning/phases/36-text-code-image-multimodal/deferred-items.md` count >= 5 (複数項目に v6.1+ 注記)
  </acceptance_criteria>
  <done>38-VALIDATION.md が approved でクローズされ、v6.1+ 持ち越しが Phase 36 deferred-items.md に統合される。Phase 38 milestone close 準備完了。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docker compose 環境 ↔ 実 user | manual test は実機 JWT cookie / multi-user 環境で実施 |
| ADR / patterns ↔ 将来 phase | ドキュメントの正確性が将来の wheel-reinvention 防止に直結 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-06-01 | Information Disclosure | manual isolation test で別 user の JWT を誤って入手 | accept (developer test 環境) | docker compose のテスト用 2 user は 0hya6in@gmail.com + 別 fake user、production credentials は使わない |
| T-38-06-02 | Tampering | ADR-0052 の記述ミスで将来 phase が誤った前提に立つ | mitigate | ADR は 0048 / 0050 / 0044 / 0038 を関連 ADR として明示参照、patterns.md も 4 エントリで cross-link |
| T-38-06-03 | Repudiation | VALIDATION.md の approved を後から書き換えて milestone を spoof | accept | git 履歴に commit が残るため改竄追跡可能、本 plan の手動 close は commit メッセージで明示 |
</threat_model>

<verification>
- E2E acceptance: Task 1 checklist で 22+ 項目すべて pass (FOUT-01..04 + edge case + CI)
- ADR / INDEX 整合: `python3 scripts/generate_adr_index.py` 実行後の `git diff docs/adr/INDEX.md` で 0052 行のみ追加
- patterns.md: 4 エントリ追記、既存破壊ゼロ
- VALIDATION.md: nyquist_compliant: true、Approval: approved、全 task ✅
- deferred-items.md: Phase 38 完了報告 + v6.1+ 持ち越し 10 項目明示
</verification>

<success_criteria>
- Phase 38 success criteria 5 件すべてが docker compose 実機で manual に充足 (FOUT-01..04)
- ADR-0052 + patterns.md 4 エントリで設計記録が確立、将来 phase が安全に再利用できる
- 38-VALIDATION.md が approved 状態でクローズ、phase verifier がそのまま STATE.md 更新できる
- v6.1+ 持ち越し 10 項目が deferred-items.md に明示され、milestone close 時に「やり残し」がドキュメント化されている
- Phase 38 が v6.0 milestone close への path に乗る
</success_criteria>

<output>
After completion, create `.planning/phases/38-worker-dl/38-06-SUMMARY.md`
</output>
