---
phase: 17-debate-chat
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - app/jobs/handlers/debate_handler.py
  - app/jobs/worker.py
  - app/api/models.py
  - app/api/routes/chat.py
  - tests/test_debate_handler.py
autonomous: true
requirements:
  - DEBATE-05
  - DEBATE-03

must_haves:
  truths:
    - "task_type='debate' を指定した process_chat が DebateHandler を呼び出す"
    - "DebateHandler が build_debate_graph を呼び出し、結果を job_store に保存する"
    - "ChatRequest に participants, pattern, max_turns, current_turn フィールドが存在する"
    - "enqueue_job が participants, pattern, max_turns, current_turn を job に含める"
  artifacts:
    - path: "app/jobs/handlers/debate_handler.py"
      provides: "DebateHandler(TaskHandler)"
      exports: ["DebateHandler"]
    - path: "tests/test_debate_handler.py"
      provides: "DebateHandler のユニットテスト"
      min_lines: 40
  key_links:
    - from: "app/jobs/worker.py"
      to: "app/jobs/handlers/debate_handler.py"
      via: "TASK_HANDLERS['debate']"
      pattern: '"debate": DebateHandler'
    - from: "app/api/routes/chat.py"
      to: "app/jobs/worker.py"
      via: "enqueue_job with participants/pattern/max_turns/current_turn"
      pattern: "participants=body.participants"
---

<objective>
DebateHandler + API 拡張 -- バックエンド統合レイヤー

Purpose: DebateGraph をバックエンドのジョブシステムに統合する。DebateHandler を実装し、ChatRequest/process_chat/enqueue_job に討論用フィールドを追加する。
Output: `debate_handler.py` (新規) + `worker.py`, `models.py`, `chat.py` (変更) + テスト
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-debate-chat/17-CONTEXT.md
@.planning/phases/17-debate-chat/17-RESEARCH.md
@.planning/phases/17-debate-chat/17-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->
From app/orchestrator/debate_graph.py:
```python
from typing import Annotated
from typing_extensions import TypedDict

class DebateState(TypedDict):
    turn: int
    max_turns: int
    pattern: str
    participants: list[str]
    messages: Annotated[list[BaseMessage], operator.add]
    current_agent_idx: int
    awaiting_extension: bool

def build_debate_graph(
    participants: list[str],
    pattern: str,
    max_turns: int,
    agents: dict[str, Any],
    llm: BaseChatModel,
    checkpointer=None,
) -> CompiledGraph: ...
```

From app/jobs/handlers/base.py:
```python
class TaskHandler(ABC):
    @abstractmethod
    async def handle(self, ctx: dict, job: dict) -> dict: ...
```

From app/jobs/handlers/orchestrator_handler.py:
```python
# Pattern reference: OrchestratorHandler.handle() structure
# - Extract job fields
# - Build SubAgentRegistry + GemSubAgent
# - AsyncPostgresSaver context manager
# - graph.ainvoke()
# - save_result() BEFORE notifier.done()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DebateHandler 実装</name>
  <files>app/jobs/handlers/debate_handler.py, tests/test_debate_handler.py</files>
  <read_first>
    - app/jobs/handlers/orchestrator_handler.py (参照実装 -- 完全に読むこと)
    - app/jobs/handlers/base.py (TaskHandler 基底クラス)
    - app/orchestrator/debate_graph.py (Plan 01 で作成した DebateGraph)
    - app/orchestrator/agent.py (SubAgentRegistry)
    - app/orchestrator/gem_agent.py (GemSubAgent)
    - app/jobs/notifier.py (build_notifier)
    - app/jobs/job_store.py (JobStore.save_result)
  </read_first>
  <behavior>
    - Test: DebateHandler().handle(ctx, job) が build_debate_graph を呼び出して ainvoke し、結果を job_store.save_result に保存する (mock)
    - Test: participants が 2 名未満の場合、エラーメッセージを job_store に保存して done する
    - Test: max_turns が 20 を超える場合、20 に切り詰められる（DoS 防止）
  </behavior>
  <action>
    `app/jobs/handlers/debate_handler.py` を新規作成。OrchestratorHandler と同じ構造で実装する。

    **クラス定義:**
    ```python
    class DebateHandler(TaskHandler):
        async def handle(self, ctx: dict, job: dict) -> dict:
    ```

    **handle() の処理フロー（OrchestratorHandler を踏襲）:**
    1. job dict からフィールドを取り出す:
       - `job_id`, `thread_id`, `prompt`, `github_token`, `reply_to`
       - `participants: list[str] = job.get("participants") or []`
       - `pattern: str = job.get("pattern", "debate")`
       - `max_turns: int = min(job.get("max_turns", 3), 20)` -- D-09 + DoS防止 (T-17-04)
       - `current_turn: int = job.get("current_turn", 0)`
       - `github_login: str = job.get("github_login", "unknown")`
       - `gem_ids: list[str] = job.get("gem_ids") or []`
    2. `job_store = ctx["job_store"]`, `notifier = build_notifier(reply_to, job_store)`
    3. バリデーション: `len(participants) < 2` => save_result + notifier.done + return
    4. `await notifier.progress("thinking")`
    5. SubAgentRegistry 構築 (AGENT_DIR, github_token)
    6. gem_ids があれば DB から Gem 取得 -> GemSubAgent 生成 -> registry.agents にマージ (OrchestratorHandler L82-L116 と同じパターン)
    7. `agents_map` を構築: participants に含まれる名前だけ registry.agents からフィルター
    8. ChatCopilot を aggregator 用 LLM として生成
    9. `AsyncPostgresSaver.from_conn_string(DB_URI)` コンテキストマネージャ
    10. `build_debate_graph(participants, pattern, max_turns, agents_map, llm, checkpointer)`
    11. initial DebateState:
        ```python
        initial = {
            "turn": current_turn,
            "max_turns": max_turns,
            "pattern": pattern,
            "participants": participants,
            "messages": [HumanMessage(content=prompt)] if current_turn == 0 else [],
            "current_agent_idx": current_turn,
            "awaiting_extension": False,
        }
        ```
        **重要:** current_turn > 0 の場合（再エンキュー）、messages は空リストにする（Pitfall 2: checkpointer が蓄積分を保持するため二重蓄積を防ぐ）
    12. `result = await graph.ainvoke(initial, config={"configurable": {"thread_id": thread_id}})`
    13. `_format_debate_result(result["messages"])` で結果テキスト生成
    14. JSON 結果を save_result:
        ```python
        import json
        result_payload = json.dumps({
            "type": "debate_result",
            "debate_text": final_text,
            "final_turn": result["turn"],
            "max_turns": max_turns,
            "is_complete": True,
        })
        await job_store.save_result(job_id, result_payload)
        ```
    15. `await notifier.done()`
    16. except/finally は OrchestratorHandler と同じパターン

    **_format_debate_result は debate_graph.py から import する** (Plan 01 で作成済み。無ければここに定義)。

    **_build_debate_agents ヘルパー:**
    ```python
    def _build_debate_agents(registry, gem_ids, participants, github_token, github_login):
        # OrchestratorHandler L86-L116 と同じ gem マージパターン
        # 最後に participants でフィルター
        return {k: v for k, v in registry.agents.items() if k in participants}
    ```

    **テスト (tests/test_debate_handler.py):**
    - mock_job_store (AsyncMock with save_result, get)
    - mock_ctx = {"job_store": mock_job_store}
    - build_debate_graph と SubAgentRegistry を mock.patch
    - `test_handle_calls_build_debate_graph`: handle() が build_debate_graph を呼び出すことを確認
    - `test_handle_invalid_participants`: participants=["A"] で呼ぶと save_result にエラーメッセージが保存される
    - `test_max_turns_capped_at_20`: max_turns=100 の job を渡して build_debate_graph に渡る max_turns が 20 であることを確認
  </action>
  <verify>
    <automated>python -m pytest tests/test_debate_handler.py -x -q</automated>
  </verify>
  <done>
    - DebateHandler が TaskHandler を継承し handle() メソッドを持つ
    - participants < 2 の場合エラーハンドリング
    - max_turns が 20 で切り詰められる
    - テスト 3 件以上が PASS
  </done>
  <acceptance_criteria>
    - `app/jobs/handlers/debate_handler.py` に `class DebateHandler(TaskHandler):` が存在する
    - `app/jobs/handlers/debate_handler.py` に `min(job.get("max_turns"` が存在する (DoS 防止)
    - `app/jobs/handlers/debate_handler.py` に `if len(participants) < 2` が存在する
    - `app/jobs/handlers/debate_handler.py` に `messages": [] if current_turn` が存在する（Pitfall 2 防止）
    - `python -m pytest tests/test_debate_handler.py -x -q` が exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ChatRequest + process_chat + enqueue_job 拡張</name>
  <files>app/api/models.py, app/jobs/worker.py, app/api/routes/chat.py</files>
  <read_first>
    - app/api/models.py (現在の ChatRequest フィールド)
    - app/jobs/worker.py (現在の process_chat シグネチャ + TASK_HANDLERS)
    - app/api/routes/chat.py (enqueue_job 呼び出し箇所 L95-L108)
  </read_first>
  <action>
    **1. app/api/models.py -- ChatRequest に 4 フィールド追加 (D-09):**

    既存の `gem_ids` フィールドの下に追加:
    ```python
    # Phase 17: 討論チャット用フィールド
    participants: list[str] | None = None  # 討論参加者エージェント名リスト
    pattern: str = "debate"                # "debate" | "panel" | "chain"
    max_turns: int = 3                     # 討論ターン数
    current_turn: int = 0                  # 再エンキュー時に前回の終了ターンを渡す
    ```

    **2. app/jobs/worker.py -- 2 箇所変更:**

    a) import 追加 (ファイル先頭):
    ```python
    from app.jobs.handlers.debate_handler import DebateHandler
    ```

    b) TASK_HANDLERS に 1 行追加 (D-08):
    ```python
    TASK_HANDLERS: dict[str, TaskHandler] = {
        "langgraph": LangGraphHandler(),
        "orchestrator": OrchestratorHandler(),
        "debate": DebateHandler(),  # Phase 17: 討論チャット
    }
    ```

    c) process_chat シグネチャに 4 引数追加 (D-10):
    ```python
    async def process_chat(
        ctx: dict,
        *,
        # ... 既存引数 ...
        gem_ids: list[str] | None = None,
        # Phase 17: 討論チャット
        participants: list[str] | None = None,
        pattern: str = "debate",
        max_turns: int = 3,
        current_turn: int = 0,
    ) -> dict:
    ```

    d) job dict に 4 フィールド追加 (L82-L94 の job = { ... } 内):
    ```python
    job = {
        # ... 既存フィールド ...
        "gem_ids": gem_ids,
        # Phase 17
        "participants": participants,
        "pattern": pattern,
        "max_turns": max_turns,
        "current_turn": current_turn,
    }
    ```

    **3. app/api/routes/chat.py -- enqueue_job 呼び出しに 4 フィールド追加:**

    L95-L108 の `await arq_redis.enqueue_job("process_chat", ...)` 呼び出しに追加:
    ```python
    await arq_redis.enqueue_job(
        "process_chat",
        # ... 既存引数 ...
        gem_ids=body.gem_ids,
        # Phase 17: 討論チャット
        participants=body.participants,
        pattern=body.pattern,
        max_turns=body.max_turns,
        current_turn=body.current_turn,
    )
    ```
  </action>
  <verify>
    <automated>python -m pytest tests/test_debate_handler.py tests/ -x -q --ignore=tests/test_debate_graph.py -k "not test_debate_pattern and not test_chain and not test_panel and not test_reenqueue" 2>&1 | tail -5</automated>
  </verify>
  <done>
    - ChatRequest に participants, pattern, max_turns, current_turn フィールドが存在する
    - TASK_HANDLERS に "debate": DebateHandler() が登録されている
    - process_chat が participants/pattern/max_turns/current_turn を受け取り job dict に含める
    - enqueue_job が 4 フィールドを渡す
  </done>
  <acceptance_criteria>
    - `grep -c 'participants' app/api/models.py` が 1 以上
    - `grep -c 'pattern.*debate' app/api/models.py` が 1 以上
    - `grep -c 'max_turns' app/api/models.py` が 1 以上
    - `grep -c 'current_turn' app/api/models.py` が 1 以上
    - `grep '"debate": DebateHandler()' app/jobs/worker.py` がマッチする
    - `grep 'participants=body.participants' app/api/routes/chat.py` がマッチする
    - `grep 'pattern=body.pattern' app/api/routes/chat.py` がマッチする
    - `python -c "from app.jobs.worker import TASK_HANDLERS; assert 'debate' in TASK_HANDLERS"` が exit code 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> POST /api/chat | participants, pattern, max_turns, current_turn は外部入力 |
| DebateHandler -> SubAgentRegistry | 不正な participants 名でエージェント解決失敗の可能性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Elevation of Privilege | DebateHandler gem fetch | mitigate | DB クエリで `is_public = true OR github_login = %s` フィルター（OrchestratorHandler と同パターン） |
| T-17-04 | Denial of Service | max_turns | mitigate | `min(max_turns, 20)` で上限をキャップ。DebateHandler 先頭で実施 |
| T-17-05 | Tampering | pattern field | mitigate | Pydantic の ChatRequest で型制約。DebateHandler でも `pattern not in ("debate","panel","chain")` ガード |
| T-17-06 | Tampering | participants | mitigate | DebateHandler でエージェント解決時に KeyError をキャッチし、エラーメッセージを job_store に保存 |
</threat_model>

<verification>
python -m pytest tests/test_debate_handler.py -x -q
python -c "from app.jobs.worker import TASK_HANDLERS; assert 'debate' in TASK_HANDLERS; print('OK')"
</verification>

<success_criteria>
- DebateHandler が TASK_HANDLERS["debate"] に登録されている
- ChatRequest に 4 フィールド追加済み
- process_chat シグネチャに 4 引数追加済み
- enqueue_job に 4 フィールド追加済み
- テストすべて PASS
</success_criteria>

<output>
After completion, create `.planning/phases/17-debate-chat/17-02-SUMMARY.md`
</output>
