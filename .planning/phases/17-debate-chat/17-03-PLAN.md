---
phase: 17-debate-chat
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - frontend/src/components/DebateChatApp.tsx
  - frontend/src/App.tsx
  - frontend/src/types.ts
  - frontend/src/components/MenuScreen.tsx
  - frontend/src/hooks/useChat.ts
autonomous: false
requirements:
  - DEBATE-04

must_haves:
  truths:
    - "MenuScreen に「討論チャット」カードが表示され、クリックすると DebateChatApp に遷移する"
    - "DebateChatApp の設定パネルでパターン選択・参加者選択・ターン数入力ができる"
    - "討論を開始ボタンでチャット画面に遷移し、各エージェントの発言がプレフィックス付きで表示される"
    - "ターン終了後に ExtensionBanner が表示され、延長または終了を選択できる"
  artifacts:
    - path: "frontend/src/components/DebateChatApp.tsx"
      provides: "討論チャットアプリ UI"
      min_lines: 200
    - path: "frontend/src/App.tsx"
      provides: "Screen 型に 'debate' 追加"
      contains: "'debate'"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/DebateChatApp.tsx"
      via: "currentScreen === 'debate'"
      pattern: "DebateChatApp"
    - from: "frontend/src/components/DebateChatApp.tsx"
      to: "frontend/src/hooks/useChat.ts"
      via: "useChat({ selectedTaskType: 'debate' })"
      pattern: "selectedTaskType.*debate"
    - from: "frontend/src/components/MenuScreen.tsx"
      to: "frontend/src/App.tsx"
      via: "onOpenDebate callback"
      pattern: "onOpenDebate"
---

<objective>
DebateChatApp フロントエンド -- 設定パネル + チャット UI + 延長承認

Purpose: 討論チャットのフロントエンド全体を実装する。設定パネル（パターン選択・参加者選択・ターン数入力）、チャット画面（既存 MessageArea 流用）、ターン終了後の延長承認バナーを含む。
Output: `DebateChatApp.tsx` (新規) + `App.tsx`, `types.ts`, `MenuScreen.tsx`, `useChat.ts` (変更)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-debate-chat/17-CONTEXT.md
@.planning/phases/17-debate-chat/17-RESEARCH.md
@.planning/phases/17-debate-chat/17-UI-SPEC.md
@.planning/phases/17-debate-chat/17-02-SUMMARY.md

<interfaces>
<!-- From frontend/src/types.ts (current): -->
```typescript
export interface ChatRequest {
  message: string;
  thread_id: string;
  model: string;
  task_type?: string;
  mode?: 'simple' | 'super';
  agents?: string[];
  app_id?: string;
  gem_id?: string | null;
  gem_ids?: string[];
  // Plan 02 で追加済みのバックエンド側のみ。フロントの型も追加が必要:
  // participants?: string[];
  // pattern?: string;
  // max_turns?: number;
  // current_turn?: number;
}
```

<!-- From frontend/src/hooks/useChat.ts: -->
```typescript
interface UseChatOptions {
  activeThreadId: string | null;
  selectedModel: string;
  selectedTaskType?: string;
  selectedMode?: 'simple' | 'super';
  agents?: string[];
  appId?: string;
  gemId?: string | null;
  gemIds?: string[];
  setMessages: React.Dispatch<React.SetStateAction<ChatMessage[]>>;
  _onThreadCreated?: (threadId: string) => void;
  refreshThreads?: () => Promise<void>;
  onCanvasResponse?: (app: CanvasAppInfo) => void;
}
```

<!-- From frontend/src/App.tsx (current): -->
```typescript
type Screen = 'menu' | 'superchat' | 'gems' | 'gemchat';
```

<!-- From frontend/src/components/MenuScreen.tsx: -->
```typescript
interface MenuScreenProps {
  onNavigate: (app: AppDefinition) => void;
  onOpenGems: () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DebateChatApp コンポーネント + useChat 拡張</name>
  <files>frontend/src/components/DebateChatApp.tsx, frontend/src/types.ts, frontend/src/hooks/useChat.ts</files>
  <read_first>
    - frontend/src/components/SuperChatApp.tsx (参照実装 -- レイアウト・フック利用パターン)
    - frontend/src/hooks/useChat.ts (sendMessage フロー全体。特に postChat 呼び出し L70-L90)
    - frontend/src/hooks/useThreads.ts (useThreads のシグネチャ)
    - frontend/src/hooks/useGemSelector.ts (useGemSelector のシグネチャ)
    - frontend/src/hooks/useAgents.ts (useAgents のシグネチャ)
    - frontend/src/components/MessageArea.tsx (MessageArea props)
    - frontend/src/components/ThreadSidebar.tsx (ThreadSidebar props)
    - frontend/src/api/client.ts (postChat 関数)
    - frontend/src/types.ts (ChatRequest, ChatMessage, AgentInfo, GemInfo)
    - .planning/phases/17-debate-chat/17-UI-SPEC.md (デザイン仕様全体)
  </read_first>
  <action>
    **1. frontend/src/types.ts -- ChatRequest に 4 フィールド追加:**

    既存の `gem_ids` の下に追加:
    ```typescript
    // Phase 17: 討論チャット
    participants?: string[];
    pattern?: string;        // "debate" | "panel" | "chain"
    max_turns?: number;
    current_turn?: number;
    ```

    **2. frontend/src/hooks/useChat.ts -- 拡張:**

    a) UseChatOptions に追加:
    ```typescript
    participants?: string[];     // Phase 17: 討論参加者名
    pattern?: string;            // Phase 17: 討論パターン
    maxTurns?: number;           // Phase 17: ターン数
    currentTurn?: number;        // Phase 17: 再エンキュー用現在ターン
    onDebateResult?: (result: { debate_text: string; final_turn: number; max_turns: number; is_complete: boolean }) => void;
    ```

    b) useChat のデストラクチャに追加: `participants, pattern, maxTurns, currentTurn, onDebateResult`

    c) postChat 呼び出し (L71) に追加:
    ```typescript
    const { job_id } = await postChat({
      // ... 既存フィールド ...
      ...(participants ? { participants } : {}),
      ...(pattern ? { pattern } : {}),
      ...(maxTurns !== undefined ? { max_turns: maxTurns } : {}),
      ...(currentTurn !== undefined ? { current_turn: currentTurn } : {}),
    });
    ```

    d) parseJobResult を拡張して `debate_result` タイプを検出:
    ```typescript
    function parseJobResult(raw: string): { text: string; canvas: CanvasResult | null; debate: DebateResult | null } {
      try {
        const parsed = JSON.parse(raw);
        if (parsed && parsed.type === 'canvas') {
          return { text: raw, canvas: parsed as CanvasResult, debate: null };
        }
        if (parsed && parsed.type === 'debate_result') {
          return { text: parsed.debate_text, canvas: null, debate: parsed };
        }
      } catch { /* plain text */ }
      return { text: raw, canvas: null, debate: null };
    }
    ```

    e) job result 取得後に `onDebateResult` コールバック呼び出し追加（parseJobResult の debate が非 null の場合）

    **3. frontend/src/components/DebateChatApp.tsx -- 新規作成:**

    SuperChatApp.tsx をベースに以下の構造で実装:

    ```typescript
    interface DebateChatAppProps {
      selectedModel: string;
    }

    interface DebateConfig {
      pattern: 'debate' | 'panel' | 'chain';
      participants: string[];
      gemIds: string[];
      maxTurns: number;
    }
    ```

    **State 管理:**
    - `config: DebateConfig | null` -- null のとき設定パネル表示、非 null でチャット表示
    - `currentTurn: number` -- 現在のターン（延長用）
    - `awaitingExtension: boolean` -- ターン終了後の延長確認中
    - `debateEnded: boolean` -- 討論完了（終了ボタン押下後）
    - `extensionTurns: number` -- 延長ターン数入力（デフォルト 3）

    **設定パネル (DebateConfigPanel) -- config === null のとき表示:**

    UI-SPEC.md の「設定フェーズ」レイアウトに準拠:
    - セクション1: PatternRadioGroup -- debate/panel/chain ラジオ選択
      - debate: 「討論 (Debate) -- A と B が交互に反論する」
      - panel: 「パネル (Panel) -- 複数エージェントが順に意見を述べる」
      - chain: 「チェーン (Chain) -- A -> B -> C の順に引き継ぐ」
    - セクション2: ParticipantChecklist
      - useAgents() でエージェントリスト取得、チェックボックスで複数選択
      - useGemSelector() で Gem リスト取得、チェックボックスで複数選択
      - 選択済みエージェント: blue (#0366d6) チップ表示
      - 選択済み Gem: green (#28a745) チップ表示
    - セクション3: ターン数入力 -- type="number", min=1, max=20, default=3
    - 「討論を開始」ボタン:
      - 参加者 0 名の場合: 「参加者を1名以上選択してください」エラー表示 (color: #e05252)
      - 有効な場合: config を確定、useThreads で新スレッド作成、sendMessage でお題送信

    **設定パネルのスタイリング (UI-SPEC 準拠):**
    - max-width: 480px, width: 100%, margin: 0 auto
    - padding: 32px, border: 1px solid #d1dbe3 (light) / #3a3a52 (dark)
    - background: #fff (light) / #2a2a3e (dark)
    - セクション間: gap 24px
    - セクション見出し: 20px, fontWeight 600
    - ラジオ/チェックボックステキスト: 16px
    - 補足テキスト: 12.5px, color #777 (light) / #9090a8 (dark)
    - 「討論を開始」ボタン: width 100%, background #0366d6 (light) / #7c6ff7 (dark), color white, padding 12px, border-radius 8px

    **チャット画面 (config !== null):**

    SuperChatApp.tsx と同一構造:
    - ThreadSidebar (appId="debate" で討論スレッドのみ表示)
    - resize divider
    - Chat area: MessageArea + ExtensionBanner + MessageInput

    **フック接続:**
    ```typescript
    const { threads, ... } = useThreads("debate");
    const { isThinking, sendMessage } = useChat({
      activeThreadId,
      selectedModel,
      selectedTaskType: 'debate',  // Pitfall 4 防止
      appId: 'debate',
      participants: config.participants,
      pattern: config.pattern,
      maxTurns: config.maxTurns,
      currentTurn,
      gemIds: config.gemIds,
      setMessages,
      refreshThreads,
      onDebateResult: handleDebateResult,
    });
    ```

    **handleDebateResult コールバック:**
    ```typescript
    const handleDebateResult = (result) => {
      setCurrentTurn(result.final_turn);
      if (result.is_complete) {
        setAwaitingExtension(true);
      }
    };
    ```

    **ExtensionBanner (awaitingExtension === true のとき表示):**

    UI-SPEC.md の ExtensionBanner レイアウトに準拠:
    - 位置: MessageArea と MessageInput の間
    - テキスト: 「討論が {currentTurn} ターン終了しました」
    - ターン数入力: type="number", min=1, max=20, value=extensionTurns
    - 「延長する」ボタン: accent fill -- クリックで:
      1. `setAwaitingExtension(false)`
      2. `setCurrentTurn` を更新
      3. useChat の maxTurns を `currentTurn + extensionTurns` に更新
      4. sendMessage で空文字またはシステムメッセージ送信（再エンキュー）。
         実際には新しい PostChat を送る: `message: "延長", max_turns: currentTurn + extensionTurns, current_turn: currentTurn`
    - 「終了する」ボタン: transparent -- クリックで `setAwaitingExtension(false)`, `setDebateEnded(true)`
    - 背景: `rgba(3, 102, 214, 0.08)` (light) / `rgba(124, 111, 247, 0.15)` (dark)
    - Border: `1px solid #0366d6` (light) / `1px solid #7c6ff7` (dark)
    - `role="alert"` 属性 (a11y)

    **MessageInput の状態:**
    - isThinking === true: disabled, placeholder 「討論進行中...」
    - debateEnded === true: disabled, placeholder 「討論が終了しました」
    - awaitingExtension === true: disabled
    - 通常: enabled (最初のお題入力時のみ)

    **アクセシビリティ (UI-SPEC 準拠):**
    - radio group: `role="radiogroup"`, `aria-labelledby`
    - チェックリスト: `<fieldset>` + `<legend>`
    - ExtensionBanner: `role="alert"`
    - disabled ボタン: `aria-disabled="true"` + `disabled` 属性
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - DebateChatApp.tsx が存在し、設定パネル + チャット画面 + ExtensionBanner を含む
    - types.ts の ChatRequest に participants/pattern/max_turns/current_turn が追加されている
    - useChat.ts に onDebateResult コールバックと debate_result パース対応が追加されている
    - TypeScript コンパイルエラーなし
  </done>
  <acceptance_criteria>
    - `grep 'participants' frontend/src/types.ts` がマッチ
    - `grep 'max_turns' frontend/src/types.ts` がマッチ
    - `grep "pattern" frontend/src/types.ts` がマッチ (少なくとも pattern フィールド行)
    - `grep 'debate_result' frontend/src/hooks/useChat.ts` がマッチ
    - `grep 'onDebateResult' frontend/src/hooks/useChat.ts` がマッチ
    - `grep 'DebateConfigPanel\|DebateConfig\|ExtensionBanner' frontend/src/components/DebateChatApp.tsx` がマッチ
    - `grep "role.*alert" frontend/src/components/DebateChatApp.tsx` がマッチ (a11y)
    - `grep "selectedTaskType.*debate\|task_type.*debate" frontend/src/components/DebateChatApp.tsx` がマッチ (Pitfall 4 防止)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: App.tsx + MenuScreen ナビゲーション統合</name>
  <files>frontend/src/App.tsx, frontend/src/components/MenuScreen.tsx</files>
  <read_first>
    - frontend/src/App.tsx (現在の Screen 型とナビゲーション)
    - frontend/src/components/MenuScreen.tsx (カード追加パターン -- 特に Gems カード部分)
    - frontend/src/components/DebateChatApp.tsx (Task 1 で作成した props)
  </read_first>
  <action>
    **1. frontend/src/App.tsx:**

    a) Screen 型に 'debate' 追加 (D-12):
    ```typescript
    type Screen = 'menu' | 'superchat' | 'gems' | 'gemchat' | 'debate';
    ```

    b) DebateChatApp import 追加:
    ```typescript
    import { DebateChatApp } from './components/DebateChatApp';
    ```

    c) handleOpenDebate コールバック追加 (handleOpenGems と同パターン):
    ```typescript
    const handleOpenDebate = () => { setCurrentScreen('debate'); };
    ```

    d) handleBackFromDebate コールバック追加:
    ```typescript
    const handleBackFromDebate = () => { setCurrentScreen('menu'); };
    ```

    e) MenuScreen に onOpenDebate prop 追加:
    ```tsx
    <MenuScreen onNavigate={handleNavigate} onOpenGems={handleOpenGems} onOpenDebate={handleOpenDebate} />
    ```

    f) debate 画面のレンダリング追加 (gemchat ブロックの後):
    ```tsx
    {currentScreen === 'debate' && (
      <>
        <Header
          selectedModel={selectedModel}
          onModelChange={setSelectedModel}
          theme={theme}
          onToggleTheme={toggleTheme}
          onBackToMenu={handleBackFromDebate}
          appName="討論チャット"
        />
        <DebateChatApp selectedModel={selectedModel} />
      </>
    )}
    ```

    **2. frontend/src/components/MenuScreen.tsx:**

    a) MenuScreenProps に追加:
    ```typescript
    interface MenuScreenProps {
      onNavigate: (app: AppDefinition) => void;
      onOpenGems: () => void;
      onOpenDebate: () => void;  // Phase 17
    }
    ```

    b) props デストラクチャに追加:
    ```typescript
    export function MenuScreen({ onNavigate, onOpenGems, onOpenDebate }: MenuScreenProps) {
    ```

    c) Gems カードの後に討論チャットカードを追加（Gems カードと同じスタイルパターン）:

    UI-SPEC のコピーライティング契約に準拠:
    - タイトル: 「討論チャット」
    - 説明: 「複数のエージェントがターン制で議論する」
    - アイコン: スピーチバルーン絵文字
    - onClick: `onOpenDebate()`
    - スタイル: 既存カードと完全に同一のスタイル定義を使用
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Screen 型に 'debate' が含まれる
    - MenuScreen に討論チャットカードが表示される
    - 討論チャットカードのクリックで DebateChatApp に遷移する
    - DebateChatApp から「戻る」で MenuScreen に遷移する
    - TypeScript コンパイルエラーなし
  </done>
  <acceptance_criteria>
    - `grep "'debate'" frontend/src/App.tsx` がマッチ (Screen 型に含まれる)
    - `grep 'DebateChatApp' frontend/src/App.tsx` がマッチ (import + JSX)
    - `grep 'onOpenDebate' frontend/src/App.tsx` がマッチ
    - `grep 'onOpenDebate' frontend/src/components/MenuScreen.tsx` がマッチ
    - `cd frontend && npx tsc --noEmit` が exit code 0
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 討論チャット機能の手動検証</name>
  <files>N/A</files>
  <action>ユーザーが docker compose up で起動後、ブラウザで全フローを手動確認する。自動化対象外のビジュアル・インタラクション検証。</action>
  <what-built>
    討論チャット機能のフルスタック実装:
    1. DebateGraph (3パターン: debate/panel/chain)
    2. DebateHandler (arq worker 統合)
    3. API 拡張 (ChatRequest + process_chat + enqueue_job)
    4. DebateChatApp フロントエンド (設定パネル + チャット + 延長バナー)
    5. MenuScreen ナビゲーション統合
  </what-built>
  <how-to-verify>
    1. docker compose up で起動
    2. /app にアクセスしてログイン
    3. メニュー画面に「討論チャット」カードが表示されることを確認
    4. 「討論チャット」をクリック -- 設定パネルが表示される
    5. 「討論」パターンを選択、2つ以上のエージェント/Gem を選択、ターン数 2 に設定
    6. お題を入力して「討論を開始」-- エージェントが交互に発言する
    7. ターン終了後に延長バナーが表示される
    8. 「延長する」で追加ターンが実行される
    9. 「終了する」で討論が終了する
    10. ヘッダーの「戻る」でメニューに戻れる
  </how-to-verify>
  <verify>
    <automated>echo "Manual checkpoint -- human verification required"</automated>
  </verify>
  <done>ユーザーが全 10 ステップを確認し「approved」を入力</done>
  <resume-signal>「approved」と入力するか、問題があれば内容を記述してください</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> DebateChatApp | パターン・参加者・ターン数はフロントエンドで入力 |
| DebateChatApp -> POST /api/chat | participants/pattern/max_turns/current_turn を送信 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Spoofing | debate_result JSON | accept | job result は backend のみが生成。フロントは JSON.parse 失敗時 plain text フォールバック |
| T-17-08 | Information Disclosure | participants list | accept | エージェント名は機密情報ではない。公開 API で取得可能 |
| T-17-09 | Denial of Service | max_turns input | mitigate | フロント側で max=20 制約。バックエンドでも min(max_turns, 20) でキャップ済み (Plan 02 T-17-04) |
</threat_model>

<verification>
cd /home/parallels/workspaces/copilot-langgraph/frontend && npx tsc --noEmit
docker compose up (手動確認)
</verification>

<success_criteria>
- DebateChatApp.tsx が存在し、設定パネル + チャット画面 + ExtensionBanner を含む
- App.tsx の Screen 型に 'debate' が含まれる
- MenuScreen に「討論チャット」カードが表示される
- TypeScript コンパイルエラーなし
- 手動検証で全フロー（設定 -> 討論 -> 延長 -> 終了）が動作する
</success_criteria>

<output>
After completion, create `.planning/phases/17-debate-chat/17-03-SUMMARY.md`
</output>
