# Phase 20-02 手動確認チェックリスト

Docker Compose 統合の目視確認。`approved` または `issue: <内容>` を記入して完了。

---

## 前提

- メインブランチのスタック (`docker compose up`) が起動済みであること
- または worktree 環境 (`/home/parallels/workspaces/copilot-langgraph/.claude/worktrees/agent-af5a6286`) で起動済みであること

---

## チェックリスト

### 1. コンテナ状態

```bash
docker compose ps
```

| 確認項目 | 期待値 | 結果 |
|---------|--------|------|
| `mcp-server` の Status | `Up (healthy)` | [ ] |
| `worker` の Status | `Up` または `running` | [ ] |

---

### 2. mcp-server ログ目視

```bash
docker compose logs mcp-server --tail 30
```

| 確認項目 | 期待値 | 結果 |
|---------|--------|------|
| `Uvicorn running on http://0.0.0.0:8001` が含まれる | あり | [ ] |
| traceback / ERROR が含まれない | なし | [ ] |

---

### 3. worker スモーク再実行

```bash
docker compose exec worker uv run python scripts/verify_mcp_worker.py
```

| 確認項目 | 期待値 | 結果 |
|---------|--------|------|
| `[verify] got 4 tools` が出力される | あり | [ ] |
| ツール名に `ping`, `web_search_stub`, `db_query_stub`, `claude_code_stub` が含まれる | あり | [ ] |
| `[verify] PASS -- MCP-01 and MCP-02 verified end-to-end` が出力される | あり | [ ] |

---

### 4. ホストポート非公開確認 (D-06)

```bash
curl -sS http://localhost:8001/health || echo "expected: connection refused"
```

| 確認項目 | 期待値 | 結果 |
|---------|--------|------|
| ホスト側から 8001 に接続できない | `connection refused` またはエラー | [ ] |

---

### 5. MCP ツール呼び出しテスト (scripts/test_mcp_tools.py)

```bash
# ホスト側から直接テスト（mcp-server のホストポートが公開されている場合のみ）
uv run python scripts/test_mcp_tools.py

# または worker コンテナ内から
docker compose exec worker uv run python scripts/test_mcp_tools.py
```

| 確認項目 | 期待値 | 結果 |
|---------|--------|------|
| `ping` が `{"status": "ok", "timestamp": "..."}` を返す | あり | [ ] |
| `web_search_stub` が stub 文字列を返す | あり | [ ] |
| `db_query_stub` が `[{"id": 1, ...}]` を返す | あり | [ ] |
| `claude_code_stub` が stub 文字列を返す | あり | [ ] |

---

### 6. 既存機能の非退行確認

ブラウザで `http://localhost:5173/orochi/` にアクセス。

| 確認項目 | 期待値 | 結果 |
|---------|--------|------|
| ログインできる | OK | [ ] |
| チャットにメッセージを送信すると応答が返る | OK | [ ] |

---

## 判定

```
結果: [ ] approved / [ ] issue: <内容を記入>

確認日時:
確認者:
```
