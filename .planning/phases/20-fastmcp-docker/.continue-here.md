---
context: phase
phase: 20-fastmcp-docker
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-04-10T03:45:01.207Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: 大量削除マージ — worktree をマージする前に `git diff --stat HEAD <branch>` で削除規模を必ず確認する。削除行数 > 追加行数×2 はストップサイン。アプリコードの削除（`app/`, `static/`, `tests/`）は1件ずつ意図を確認してからマージすること。

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| 大量削除マージ | worktree が `main` を起点に生成されたため、feature branch との差分が 81 files / 8933 deletions として現れた。`iframe_rpc.py` / `parent-bridge.js` / `iframe-rpc.js` 等が削除された。 | blocking | マージ前に `git diff --stat HEAD <worktree-branch>` を実行し、削除行数が多い場合は内容を精査してからマージする |
| マージ結果の目的適合確認省略 | ワークフローの「merge the worktree」手順を機械的に実行し、削除規模の異常に気づきながらスルーした | advisory | マージ後に削除ファイルリストを確認し、Phase の目的（mcp_server/ 追加のみ）と整合しているか検証する |

<current_state>
Phase 20 Wave 2 (Plan 20-02) のチェックポイントで停止中。
worktree `worktree-agent-af5a6286` がまだ存在し、Task 3（human-verify）の承認待ち。

今セッションで発生した大量削除マージのデグレ修正に時間を費やした：
- `iframe-rpc.js`, `parent-bridge.js`, `iframe_rpc.py`, `hosted_apps.py`, `iframe_rpc_handler.py`, `worker.py` を復元
- Vite `/js/` プロキシ追加（VITE_APP_BASE=/orochi との不整合修正）
- `canvas.py` deploy の `$URL_PREFIX` 置換追加
- FastAPI `/js/{filename}` CORS ルート復元

CanvasChat の `parent-bridge.js` 404 / `iframe-rpc.js` CORS エラーはまだ未解決（docker restart 後に確認中）。
</current_state>

<completed_work>

### Plan 20-01 (Wave 1) — 完了
- Task 1: `mcp_server/` uv プロジェクト作成 (fastmcp 3.2.3, 4 stub tools) — commit `2dc3a21`
- Task 2: `tests/test_mcp_server.py` 4テストグリーン — commit `f78641d`
- SUMMARY: `20-01-SUMMARY.md` 作成済み

### Plan 20-02 (Wave 2) — 途中
- Task 1: `docker-compose.yml` に mcp-server サービス追加 + langchain-mcp-adapters — commit `db17370` (worktree)
- Task 2: `scripts/verify_mcp_worker.py` スモーク検証 PASS — commit `9ebf646` (worktree)
- Task 3: human-verify チェックポイント — **未完了・承認待ち**

### デグレ修正 (今セッション)
- `static/js/iframe-rpc.js`, `parent-bridge.js` 復元 — commit `ebf4570`, `b454244`
- `app/api/routes/iframe_rpc.py`, `hosted_apps.py` 復元 — commit `b454244`
- `app/jobs/handlers/iframe_rpc_handler.py`, `worker.py` 復元 — commit `b454244`
- FastAPI `/js/` CORS ルート復元 — commit `8e680fb`
- Vite `/js/` プロキシ追加 — commit `a6b7c30`, `d8c5ef0`
- `canvas.py` `$URL_PREFIX` 置換 — commit `ebf4570`
- CLAUDE.md にマージ安全ルール追加 — commit `6cd27a9`
</completed_work>

<remaining_work>

### Phase 20 で残っている作業

1. **CanvasChat iframe エラー確認** — `docker-restart.sh` 後のブラウザ確認がまだ。
   - `parent-bridge.js 404` が直ったか（Vite `/js` fallback proxy）
   - `iframe-rpc.js CORS エラー` が直ったか（FastAPI CORS ルート復元）

2. **Plan 20-02 Task 3 human-verify 承認** — UAT チェックリスト `.planning/phases/20-fastmcp-docker/20-02-UAT.md` の確認
   - チェック 1-4: `docker compose ps`, logs, smoketest, ホストポート非公開
   - チェック 6: 既存チャット機能の非退行確認

3. **worktree マージ** — `worktree-agent-af5a6286` が承認後にマージ・クリーンアップ必要

4. **古い planning ファイルの unstaged deletion** — `git status` に大量の `D .planning/phases/01~18/...` が表示されているが、これらは大量削除マージ前から存在していた worktree の状態。コミットすべきかどうかを確認する。

5. **`mcp_server/pyproject.toml`, `mcp_server/uv.lock`** に未コミットの変更あり（`git status` で `M` 表示）。内容確認後にコミットが必要。
</remaining_work>

<decisions_made>

- FastMCP transport: `transport="http"` (FastMCP側) / `transport="streamable_http"` (langchain-mcp-adapters側) — アンダースコアの違いに注意
- `mcp_server/` は独立 uv プロジェクト。root `pyproject.toml` から分離（fastmcp はルート dev deps にも追加済み）
- `langchain-mcp-adapters>=0.2.2` を root pyproject.toml に追加（worktree の変更を先行マージ）
- Vite dev server: VITE_APP_BASE=/orochi のため `/js/` プロキシは fallback ルール（プレフィックスなし）も追加
</decisions_made>

<blockers>

- **CanvasChat iframe エラー**: `parent-bridge.js 404` / `iframe-rpc.js CORS` — 直近の修正で解消見込みだが未確認
- **worktree-agent-af5a6286**: まだ存在。Plan 20-02 Task 3 承認後にマージ・削除する
- **worktree 内の残ゴミ**: `.claude/worktrees/agent-aabe3c19`, `agent-aad511f0`, `agent-aefc8d85` — 中身確認してクリーンアップが必要
</blockers>

## Required Reading (in order)

1. `.planning/phases/20-fastmcp-docker/20-02-UAT.md` — human-verify チェックリスト（残り確認項目）
2. `.planning/phases/20-fastmcp-docker/20-01-SUMMARY.md` — Wave 1 完了内容
3. `CLAUDE.md` の「Merge Safety Rules」セクション — 大量削除マージを防ぐルール
4. `scripts/test_mcp_tools.py` — 全ツール呼び出しテストスクリプト

## Infrastructure State

- `docker compose up` 済み（`./docker-restart.sh` で再起動）
- `mcp-server`: healthy
- `worker`: running（langchain-mcp-adapters インストール済み）
- `api`, `frontend`: running
- ブランチ: `gsd/phase-20-fastmcp-docker`

## Pre-Execution Critique Required

なし（実行フェーズは完了済み。残りは UAT 確認とマージのみ）

<context>
大量削除マージのデグレ対応に時間を費やしたセッション。
Phase 20 の本来の実装（mcp_server + docker統合）は完了しているが、
worktree マージの影響で Phase 18/19 の iframe RPC 実装が消え、その復元作業が発生した。

次セッションで最優先すべきは：
1. CanvasChat エラーが直ったか確認
2. 20-02 UAT 承認 → worktree マージ → Phase 20 完了
3. 不要な worktree (`agent-aabe3c19` 等) のクリーンアップ
</context>

<next_action>
ブラウザで CanvasChat を開いてエラーが解消されているか確認。
OK なら「approved」と伝えて Plan 20-02 を完了させる。
</next_action>
