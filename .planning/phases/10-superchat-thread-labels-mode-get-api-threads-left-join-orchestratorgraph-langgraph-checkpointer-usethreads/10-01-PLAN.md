---
phase: 10-superchat-thread-labels-mode-get-api-threads-left-join-orchestratorgraph-langgraph-checkpointer-usethreads
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_api_chat.py
  - tests/test_worker.py
autonomous: true
requirements: [DB-01, DB-02, API-01, API-02, API-03, ORC-01]

must_haves:
  truths:
    - "Failing or skipped tests exist for every new Phase 10 behavior"
    - "Existing tests are not broken by the additions"
  artifacts:
    - path: "tests/test_api_chat.py"
      provides: "New skipped test functions for applications/threads tables, app_id filter, LEFT JOIN, upsert app_id"
      contains: "test_applications_table_exists"
    - path: "tests/test_worker.py"
      provides: "New skipped test function for OrchestratorHandler thread_id config"
      contains: "test_orchestrator_handler_uses_thread_id"
  key_links: []
---

<objective>
Add failing/skipped test scaffolds for all Phase 10 behaviors before implementation begins.

Purpose: TDD Wave 0 -- tests define the contract that subsequent waves must satisfy.
Output: New test functions in test_api_chat.py and test_worker.py, all marked with pytest.mark.skip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-superchat-thread-labels-mode-get-api-threads-left-join-orchestratorgraph-langgraph-checkpointer-usethreads/10-CONTEXT.md
@tests/test_api_chat.py
@tests/test_worker.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 10 test scaffolds to test_api_chat.py</name>
  <files>tests/test_api_chat.py</files>
  <action>
Append the following test functions to the end of tests/test_api_chat.py. All tests must be decorated with `@pytest.mark.skip(reason="Phase 10: not yet implemented")`.

1. `test_applications_table_exists(api_client)` -- Docstring: "applications table exists with 'chat' and 'superchat' rows after migration." The test would connect to DB and verify `SELECT * FROM applications` returns 2 rows with app_id in ('chat', 'superchat'). Skip for now.

2. `test_threads_table_exists(api_client)` -- Docstring: "threads table exists (replaces thread_labels) with app_id FK." The test would verify `SELECT * FROM threads` succeeds. Skip for now.

3. `test_list_threads_app_id_filter(api_client, jwt_cookie)` -- Docstring: "GET /api/threads?app_id=chat returns only chat threads, not superchat." The test would mock psycopg.AsyncConnection.connect to return controlled rows, call GET /api/threads?app_id=chat, and verify only chat threads appear. Skip for now.

4. `test_list_threads_left_join(api_client, jwt_cookie)` -- Docstring: "Threads in threads table but NOT in checkpoints table still appear in GET /api/threads (LEFT JOIN behavior)." The test would mock the DB to return a thread row with NULL checkpoint_id, verify it appears in the response. Skip for now.

5. `test_chat_upsert_app_id(api_client, mock_arq_redis, jwt_cookie)` -- Docstring: "POST /api/chat with mode='super' upserts app_id='superchat' in threads table." The test would mock psycopg and verify the INSERT INTO threads call includes app_id='superchat'. Skip for now.

Each test should have only the decorator, docstring, and `pass` body. Import pytest at top if not already present (it is).
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && uv run pytest tests/test_api_chat.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>5 new test functions exist in test_api_chat.py, all marked skip. Existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add OrchestratorHandler thread_id test to test_worker.py</name>
  <files>tests/test_worker.py</files>
  <action>
Append a new test function to tests/test_worker.py:

`test_orchestrator_handler_uses_thread_id` -- decorated with `@pytest.mark.skip(reason="Phase 10: not yet implemented")`.

Docstring: "OrchestratorHandler passes thread_id config to graph.ainvoke for checkpointer continuity (ORC-01)."

The test would:
1. Import OrchestratorHandler
2. Mock build_orchestrator_graph, SubAgentRegistry, AsyncPostgresSaver
3. Call handler.handle(ctx, job) with job containing thread_id="t-orch-1"
4. Verify graph.ainvoke is called with config={"configurable": {"thread_id": "t-orch-1"}}
5. Verify build_orchestrator_graph is called with checkpointer= (not None)

Body is just `pass` for now (skip-marked).
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && uv run pytest tests/test_worker.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>1 new skipped test in test_worker.py. Existing worker tests still pass.</done>
</task>

</tasks>

<verification>
```bash
cd /home/parallels/workspaces/copilot-langgraph && uv run pytest tests/test_api_chat.py tests/test_worker.py -v 2>&1 | grep -E "(PASSED|FAILED|SKIPPED|ERROR)"
```
All existing tests PASSED, 6 new tests SKIPPED, 0 FAILED.
</verification>

<success_criteria>
- 6 new test functions added (5 in test_api_chat.py, 1 in test_worker.py)
- All 6 are marked with pytest.mark.skip
- All pre-existing tests continue to pass
- `uv run pytest tests/ -x` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/10-superchat-thread-labels-mode-get-api-threads-left-join-orchestratorgraph-langgraph-checkpointer-usethreads/10-01-SUMMARY.md`
</output>
