---
phase: 10-superchat-thread-labels-mode
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_api_chat.py
  - tests/test_worker.py
autonomous: true
requirements: [DB-01, API-01, API-02, API-03, ORC-01]

must_haves:
  truths:
    - "New tests exist for mode column, LEFT JOIN, mode upsert, and orchestrator checkpointer"
    - "All new tests FAIL because production code has not been changed yet"
  artifacts:
    - path: "tests/test_api_chat.py"
      provides: "Tests for mode filter, LEFT JOIN, mode upsert"
      contains: "test_list_threads_mode_filter"
    - path: "tests/test_worker.py"
      provides: "Test for orchestrator handler with thread_id"
      contains: "test_orchestrator_handler_uses_checkpointer"
  key_links: []
---

<objective>
Add failing tests (Wave 0 / RED phase) for all Phase 10 backend behaviors before any production code changes.

Purpose: Establish test scaffolding so subsequent waves can validate their work incrementally.
Output: Extended test_api_chat.py and test_worker.py with new test functions that will initially fail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-superchat-thread-labels-mode-get-api-threads-left-join-orchestratorgraph-langgraph-checkpointer-usethreads/10-RESEARCH.md

@tests/test_api_chat.py
@tests/test_worker.py
@tests/conftest.py
@app/api/routes/chat.py
@app/jobs/handlers/orchestrator_handler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mode-related tests to test_api_chat.py</name>
  <files>tests/test_api_chat.py</files>
  <action>
Add the following test functions to `tests/test_api_chat.py`. These tests describe the DESIRED behavior that does not exist yet. They should be written so they will FAIL against the current code but PASS once Waves 1-2 are complete.

**Test 1: `test_list_threads_mode_filter`**
- POST /api/chat with `mode="super"` and a JWT cookie to create a superchat thread (upsert)
- GET /api/threads?mode=superchat should return that thread
- GET /api/threads?mode=chat should NOT return that thread
- This test will fail because: (a) mode column does not exist, (b) GET /api/threads ignores ?mode param

**Test 2: `test_list_threads_no_mode_returns_all`**
- GET /api/threads without ?mode should return all threads regardless of mode (backward compat)
- This test validates Pitfall 5 from RESEARCH.md

**Test 3: `test_chat_upsert_mode`**
- POST /api/chat with `mode="super"` should write `mode='superchat'` to thread_labels
- POST /api/chat with `mode="simple"` (or no mode) should write `mode='chat'` to thread_labels
- This test will fail because the current upsert does not include mode column

**Test 4: `test_list_threads_left_join`**
- Create a thread_labels row directly (no checkpoint), then GET /api/threads should still return it
- This tests the LEFT JOIN change (currently INNER JOIN excludes threads without checkpoints)

IMPORTANT: Since the API tests use mocked DB (no real postgres), these tests need to mock psycopg.AsyncConnection at the route level. Follow the existing test pattern where `api_client` fixture skips lifespan and injects mocks into app.state.

For `test_list_threads_mode_filter` and `test_list_threads_left_join`: these tests need a real or mock DB connection. Since the current test suite mocks at the app.state level and the list_threads route opens its own psycopg connection, use `unittest.mock.patch` on `psycopg.AsyncConnection.connect` to return a mock connection that returns controlled cursor results.

Mark these tests with `@pytest.mark.skip(reason="Phase 10 Wave 0: will pass after Wave 1-2")` so the existing test suite remains green. The skip will be removed in Wave 2.
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && uv run pytest tests/test_api_chat.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>New test functions exist in test_api_chat.py. Existing tests still pass. New tests are marked skip.</done>
</task>

<task type="auto">
  <name>Task 2: Add orchestrator checkpointer test to test_worker.py</name>
  <files>tests/test_worker.py</files>
  <action>
Add a new test function `test_orchestrator_handler_uses_checkpointer` to `tests/test_worker.py`.

**Test behavior:**
- Instantiate `OrchestratorHandler`
- Call `handler.handle(ctx, job)` with a job dict containing `thread_id`
- Verify that `build_orchestrator_graph` is called with a `checkpointer` argument (not None)
- Verify that `graph.ainvoke` is called with `config={"configurable": {"thread_id": thread_id}}`

**Mock setup:**
- Patch `app.jobs.handlers.orchestrator_handler.build_orchestrator_graph` to return a mock graph
- Patch `app.jobs.handlers.orchestrator_handler.SubAgentRegistry` to return a mock registry with at least one agent
- Patch `app.jobs.handlers.orchestrator_handler.AsyncPostgresSaver` (will need to be imported in the handler)
- Mock graph.ainvoke to return `{"output": "test reply", "messages": [], "next": ""}`

Mark with `@pytest.mark.skip(reason="Phase 10 Wave 0: will pass after Wave 3")` so existing suite stays green.
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && uv run pytest tests/test_worker.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>test_orchestrator_handler_uses_checkpointer exists in test_worker.py. Existing tests still pass. New test is marked skip.</done>
</task>

</tasks>

<verification>
All existing tests pass: `uv run pytest tests/ -x`
New test functions exist but are skipped.
</verification>

<success_criteria>
- tests/test_api_chat.py contains 4 new test functions (skip-marked)
- tests/test_worker.py contains 1 new test function (skip-marked)
- Full test suite passes with no failures
</success_criteria>

<output>
After completion, create `.planning/phases/10-superchat-thread-labels-mode-get-api-threads-left-join-orchestratorgraph-langgraph-checkpointer-usethreads/10-01-SUMMARY.md`
</output>
