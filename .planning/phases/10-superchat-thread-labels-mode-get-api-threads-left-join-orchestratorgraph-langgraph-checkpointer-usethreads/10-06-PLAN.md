---
phase: 10-superchat-thread-labels-mode-get-api-threads-left-join-orchestratorgraph-langgraph-checkpointer-usethreads
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/general-assistant/AGENT.md
autonomous: true
requirements: [ORC-01]
gap_closure: true

must_haves:
  truths:
    - "SuperChatで任意のメッセージを送ると、エラー文字列ではなくAIの自然な返答が返る"
    - "RouterNodeが一般会話を general-assistant にルーティングする"
  artifacts:
    - path: "agents/general-assistant/AGENT.md"
      provides: "汎用会話エージェントの定義（name, description, model, system_prompt）"
      contains: "name: general-assistant"
  key_links:
    - from: "app/orchestrator/graph.py (RouterNode)"
      to: "agents/general-assistant/AGENT.md"
      via: "SubAgentRegistry が AGENT.md をロードし、RouterNode の agent_descriptions に含める"
      pattern: "general-assistant"
---

<objective>
SuperChat で任意のメッセージを送ると「対応できるエージェントが見つかりませんでした。」が返る問題を修正する。

Root cause: `SubAgentRegistry` に汎用会話エージェントが存在しないため、RouterNode が一般メッセージを `fallback` にルーティングし、`fallback_node()` が固定エラー文字列を返す。

Fix: `agents/general-assistant/AGENT.md` を追加する。`SubAgentRegistry` は起動時に `agents/**/AGENT.md` を glob でロードするため、ファイルを置くだけで自動登録される。コード変更は不要。

Purpose: SuperChat の会話が成立するようにする（UAT Test 2 のブロッカー解消）
Output: `agents/general-assistant/AGENT.md` — 1ファイルのみ
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- Agent loading mechanism (from app/orchestrator/agent.py):
SubAgentRegistry.__init__ runs:
  for path in Path(agent_dir).glob("**/AGENT.md"):
      agent = SubAgent.from_dir(path.parent, github_token)
      self.agents[agent.name] = agent

SubAgent.from_dir reads frontmatter from AGENT.md:
  meta["name"]        → agent.name (used as graph node name and routing key)
  meta["description"] → shown to RouterNode LLM for routing decisions
  meta.get("model", "claude-sonnet-4-6") → LLM model
  post.content        → system_prompt passed as SystemMessage

Existing agent example (agents/code-reviewer/AGENT.md):
---
name: code-reviewer
description: |
  Python/JavaScript/TypeScript コードの静的解析・リント・フォーマットチェックを行う。
  入力: コードスニペットまたはファイルパス
  出力: 指摘リストと修正提案
  対象外: テスト実行 / デプロイ / DB操作
model: claude-opus-4-6
---

あなたは厳格なコードレビュアーです。
指摘は重大度（error/warning/info）付きで箇条書きにしてください。

RouterNode routing logic (from app/orchestrator/graph.py):
- ROUTER_PROMPT instructs LLM to pick one agent name or return "fallback"
- valid set = {a.name for a in agents} | {"fallback"}
- If general-assistant is in the registry, the router can choose it for general messages
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add general-assistant agent</name>
  <files>agents/general-assistant/AGENT.md</files>
  <action>
    Create the directory `agents/general-assistant/` and write `AGENT.md` with the following exact content:

    ```
    ---
    name: general-assistant
    description: |
      汎用会話エージェント。コードレビューやSQL解析などの専門エージェントが対応しない、
      一般的な質問・雑談・要約・翻訳・アイデア出しなどあらゆるメッセージに対応する。
      他のエージェントが明らかに適切な場合はそちらを優先すること。
    model: claude-sonnet-4-6
    ---

    あなたは親切で知識豊富なアシスタントです。
    ユーザーの質問や依頼に対して、丁寧かつ簡潔に回答してください。
    専門的な内容も分かりやすく説明し、必要に応じて具体例を交えてください。
    ```

    Key constraints:
    - The `name` field MUST be `general-assistant` (used as graph node key — spaces/underscores break routing)
    - The `description` must clearly indicate this is the catch-all for non-specialized messages, so the RouterNode LLM routes general queries here instead of "fallback"
    - Use `claude-sonnet-4-6` as the model (matches the default in SubAgent.from_dir and is suitable for general conversation)
    - Do NOT create a `rules.md` file — it is optional and not required for the agent to load
  </action>
  <verify>
    ```bash
    # File exists and has correct frontmatter
    python3 -c "
    import frontmatter, sys
    post = frontmatter.load('agents/general-assistant/AGENT.md')
    assert post.metadata['name'] == 'general-assistant', 'name mismatch'
    assert 'description' in post.metadata, 'missing description'
    assert post.content.strip(), 'empty system prompt'
    print('AGENT.md valid:', post.metadata['name'])
    "
    ```
  </verify>
  <done>
    - `agents/general-assistant/AGENT.md` exists
    - frontmatter parses without error
    - `name` == `general-assistant`
    - `description` explains catch-all role
    - `content` (system prompt) is non-empty
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify registry loads the new agent</name>
  <files></files>
  <action>
    Run a smoke test to confirm `SubAgentRegistry` picks up `general-assistant` from the agents directory. Use `AGENT_DIR=agents` (the value set in Docker Compose for the worker service).

    ```bash
    python3 -c "
    import asyncio, os, sys
    sys.path.insert(0, '.')
    os.environ.setdefault('GITHUB_TOKEN', 'dummy')

    # Patch ChatCopilot to avoid SDK init during registry load
    from unittest.mock import patch, MagicMock
    with patch('app.providers.copilot.ChatCopilot.__init__', return_value=None):
        from app.orchestrator.agent import SubAgentRegistry
        registry = SubAgentRegistry.__new__(SubAgentRegistry)
        registry.agents = {}
        registry._github_token = 'dummy'
        from pathlib import Path
        import frontmatter as fm
        for path in Path('agents').glob('**/AGENT.md'):
            post = fm.load(path)
            meta = post.metadata
            print(f'  loaded: {meta[\"name\"]}')
            registry.agents[meta['name']] = meta['name']  # placeholder

    names = list(registry.agents.keys())
    print('Registry agents:', names)
    assert 'general-assistant' in names, f'general-assistant not found in {names}'
    print('OK: general-assistant is registered')
    "
    ```

    If the python-frontmatter package is not installed in the local environment, verify by checking the file directly:

    ```bash
    grep -r "name: general-assistant" agents/general-assistant/AGENT.md
    ls agents/
    ```
  </action>
  <verify>
    ```bash
    grep "name: general-assistant" agents/general-assistant/AGENT.md
    ls agents/
    ```
    Expected: `general-assistant` directory visible alongside `code-reviewer` and `sql-analyst`.
  </verify>
  <done>
    - `agents/` lists: `code-reviewer`, `general-assistant`, `sql-analyst`
    - `grep` finds `name: general-assistant` in the AGENT.md
    - No existing tests broken (`python3 -m pytest tests/ -x -q --ignore=tests/test_worker.py` passes or was already passing)
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. File structure check:
   ```
   agents/
     code-reviewer/AGENT.md
     general-assistant/AGENT.md   ← new
     sql-analyst/AGENT.md
   ```

2. Frontmatter validation:
   ```bash
   grep "name: general-assistant" agents/general-assistant/AGENT.md
   ```

3. Existing tests unaffected:
   ```bash
   python3 -m pytest tests/ -x -q 2>&1 | tail -5
   ```

The fix requires no Docker rebuild — the agent directory is mounted as a volume in Docker Compose (`./agents:/app/agents`). Restarting the worker service picks up the new file:
```bash
docker compose restart worker
```

To verify end-to-end in SuperChat: send any general message (e.g. "今日の天気は？") and confirm a natural AI response is returned instead of「対応できるエージェントが見つかりませんでした。」
</verification>

<success_criteria>
- `agents/general-assistant/AGENT.md` committed with valid frontmatter (`name: general-assistant`)
- SuperChat sends a general message → receives natural AI response (not the fallback error string)
- UAT Test 2 ("SuperChat conversation continuity") can now be retested and is expected to pass
- No existing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/10-superchat-thread-labels-mode-get-api-threads-left-join-orchestratorgraph-langgraph-checkpointer-usethreads/10-06-SUMMARY.md`
</output>
