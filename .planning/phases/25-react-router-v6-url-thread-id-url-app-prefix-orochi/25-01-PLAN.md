---
phase: 25
plan: 01
title: "React Router v6 URL ルーティング実装"
type: execute
wave: 1
wave_count: 4
status: ready
depends_on: []
autonomous: false
requirements: [URL-01, URL-02, URL-03, URL-04, URL-05]
files_modified:
  - frontend/package.json
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/components/ChatApp.tsx
  - frontend/src/components/SuperChatApp.tsx
  - frontend/src/components/GemChatApp.tsx
  - frontend/src/components/CanvasChatApp.tsx
  - frontend/src/components/DebateChatApp.tsx
  - frontend/src/components/MenuScreen.tsx
  - frontend/src/components/GemsScreen.tsx
  - frontend/src/components/CanvasScreen.tsx
  - docs/nginx.md

must_haves:
  truths:
    - "ブラウザで /orochi/chat/abc-123 にアクセスすると ChatApp がスレッド abc-123 を開いた状態で表示される"
    - "スレッドサイドバーでスレッドを選択すると URL バーが /orochi/{appType}/{threadId} に更新される"
    - "ブラウザの戻る/進むボタンで画面遷移・スレッド切り替えが機能する"
    - "URL をコピーして別タブで開くと同じスレッドが表示される(共有リンク)"
    - "メニューから各アプリを開いたときに URL が /orochi/{appType} に変わる"
    - "TypeScript ビルドがエラーなしで通る"
  artifacts:
    - path: "frontend/package.json"
      provides: "react-router 7.x 依存関係"
      contains: "react-router"
    - path: "frontend/src/main.tsx"
      provides: "BrowserRouter ラッパー + basename"
      contains: "BrowserRouter"
    - path: "frontend/src/App.tsx"
      provides: "Routes/Route によるスクリーンマッピング"
      contains: "Routes"
    - path: "frontend/src/components/ChatApp.tsx"
      provides: "useParams による threadId 同期"
      contains: "useParams"
  key_links:
    - from: "main.tsx"
      to: "App.tsx"
      via: "BrowserRouter basename"
      pattern: "BrowserRouter basename"
    - from: "App.tsx"
      to: "各 ChatApp コンポーネント"
      via: "Routes / Route path mapping"
      pattern: "Route path"
    - from: "ChatApp(などの子)"
      to: "useThreads.switchThread"
      via: "useParams + useEffect 同期"
      pattern: "useParams.*threadId"
    - from: "ThreadSidebar onSelectThread"
      to: "useNavigate"
      via: "URL 更新ハンドラ"
      pattern: "navigate.*threadId"
---

# Phase 25, Plan 01: React Router v6 URL ルーティング実装

## Goal

React Router v7(declarative = v6 API互換) を導入し、`/{APP_PREFIX}/{appType}/{threadId?}` 形式の URL 構造で SPA の画面とスレッドを管理できるようにする。スレッド共有リンクとブラウザ Back/Forward 対応の基盤を作る。

## Context

現状の `frontend/src/App.tsx` は `useState<Screen>` による条件分岐で画面切替を行っており、URL は `/orochi/` のまま一切変化しない。このため:

- スレッドの共有リンクが作れない
- ブラウザの戻る/進むボタンが効かない
- URL ブックマーク/リロードで元のスレッドに戻れない

Phase 25 は React Router を導入してこれらを解決する。`VITE_APP_BASE=/orochi` と Vite の `base` 設定が既に整備されているため、`BrowserRouter basename` に `import.meta.env.BASE_URL` の末尾スラッシュを除去した値を渡すだけで APP_PREFIX 対応が完結する。

主な技術判断 (25-RESEARCH.md より):
- `react-router` v7.x(declarative mode) を採用。v6 と API 互換のため「v6 ルーティング」の要件を満たす
- URL 構造は `/:appType/:threadId?` (basename は BrowserRouter が自動付加)
- `useThreads` の `activeThreadId` は残しつつ、URL を single source of truth として各 ChatApp で `useParams` → `switchThread` 同期
- SuperChat は `appSlug` から `AppDefinition` を解決する `SuperChatWrapper` を新設
- Canvas は `canvasGemId` を CanvasChatApp 内部で取得するよう移動(App.tsx の state を削除)

## Waves

### Wave 1: 基盤導入(依存追加 + Router セットアップ)

**Rationale:** 依存追加と Router ラッパー導入が全てのルート実装の前提。他のタスクは全て Wave 1 完了後に実行される。

#### Task 25-01-01: react-router 依存追加

**File:** `frontend/package.json`
**Action:** MODIFY
**Why:** React Router を導入するための依存追加。bun add で package.json と lockfile を更新する。

<task_detail>
Docker コンテナ内で bun add を実行する(CLAUDE.md の「Primary startup method: docker compose up」方針に従う)。

実行コマンド:
```bash
docker compose exec frontend bun add react-router@^7.14.0
```

もしコンテナが起動していない場合:
```bash
docker compose run --rm frontend bun add react-router@^7.14.0
```

期待される package.json の差分:
```json
"dependencies": {
  ...
  "react-dom": "^19.2.4",
  "react-markdown": "^10.1.0",
  "react-router": "^7.14.0",
  ...
}
```

注意事項:
- `react-router-dom` は追加しない(v7 では `react-router` に統合済み、RESEARCH.md State of the Art 参照)
- 既存の依存(react 19, vite 8 等)には影響しないはず。追加後に `bun install` が再実行されることを確認
</task_detail>

<automated>
cd frontend && grep -q '"react-router"' package.json && echo OK
</automated>

#### Task 25-01-02: BrowserRouter ラッパー導入 + 型宣言整備

**File:** `frontend/src/main.tsx`
**Action:** MODIFY
**Why:** アプリ全体を `BrowserRouter` でラップし、`basename` を Vite の `BASE_URL` から派生させる。これが以降の全ルーティングの起点となる。

<task_detail>
`frontend/src/main.tsx` を以下のように変更する(既存の CSS import とテーマ import は保持する):

```typescript
// frontend/src/main.tsx
// IMPORTANT: chatscope CSS must be imported here, before any React rendering.
// Per 07-RESEARCH.md Pattern section: import at app root, not in components.
import '@chatscope/chat-ui-kit-styles/dist/default/styles.min.css';
import './theme.css';

import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import { BrowserRouter } from 'react-router';
import { App } from './App';

// Phase 25: APP_PREFIX 対応
// Vite は VITE_APP_BASE を元に import.meta.env.BASE_URL を末尾スラッシュ付きで公開する
// (例: VITE_APP_BASE=/orochi → BASE_URL="/orochi/")。
// BrowserRouter の basename は末尾スラッシュなしを期待するため replace で除去する。
// 25-RESEARCH.md Pitfall 1 参照。
const basename = (import.meta.env.BASE_URL ?? '/').replace(/\/$/, '');

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <BrowserRouter basename={basename}>
      <App />
    </BrowserRouter>
  </StrictMode>
);
```

ポイント:
- 既存 CSS import の順序は変更しない(Pitfall 対象)
- `import { BrowserRouter } from 'react-router'` (react-router-dom ではない)
- basename は必ず末尾スラッシュ除去(二重スラッシュ URL 防止)
</task_detail>

<automated>
cd frontend && bun run build 2>&1 | tail -20
</automated>

### Wave 2: App.tsx の Routes 化(コアルーティング)

**Rationale:** Wave 1 で BrowserRouter が入った状態で、App.tsx の useState-based 画面切替を Routes/Route に置換する。このタスク後、全てのスクリーンコンポーネントは URL 経由で表示されるようになる。

#### Task 25-02-01: App.tsx を Routes ベースに置換(MenuScreen + wrapper 群込み)

**File:** `frontend/src/App.tsx`
**Action:** MODIFY
**Why:** useState ベースの画面切替を Routes に置き換え、各画面を URL にマップする。SuperChat は appSlug 解決のためのラッパー、GemChat は gemId 解決ラッパーを同ファイル内に定義する(軽量で済むため別ファイル化しない)。

<task_detail>
`frontend/src/App.tsx` を以下の構造に全面的に書き換える:

```typescript
// frontend/src/App.tsx
// Phase 25: React Router v6(v7 declarative) による URL ルーティング
// URL 構造: /{APP_PREFIX}/{appType}/{threadId?}
//   - /orochi/             → MenuScreen
//   - /orochi/chat[/:tid]  → ChatApp
//   - /orochi/superchat/:appSlug[/:tid] → SuperChatWrapper
//   - /orochi/gems         → GemsScreen
//   - /orochi/gemchat/:gemId[/:tid] → GemChatWrapper
//   - /orochi/canvas       → CanvasScreen
//   - /orochi/canvaschat[/:tid] → CanvasChatApp
//   - /orochi/debate[/:tid] → DebateChatApp
// basename("/orochi") は main.tsx の BrowserRouter で設定済み

import { useEffect, useState } from 'react';
import { Routes, Route, useNavigate, useParams, Navigate } from 'react-router';
import { AuthContext, useAuthProvider } from './hooks/useAuth';
import { AuthPanel } from './components/AuthPanel';
import { Header } from './components/Header';
import { ChatApp } from './components/ChatApp';
import { SuperChatApp } from './components/SuperChatApp';
import { MenuScreen } from './components/MenuScreen';
import { GemsScreen } from './components/GemsScreen';
import { GemChatApp } from './components/GemChatApp';
import { DebateChatApp } from './components/DebateChatApp';
import { CanvasScreen } from './components/CanvasScreen';
import { CanvasChatApp } from './components/CanvasChatApp';
import { useTheme } from './hooks/useTheme';
import { ThemeContext } from './contexts/ThemeContext';
import { getApps, listGems } from './api/client';
import type { AppDefinition, GemInfo } from './types';

// ---- SuperChat ラッパー: appSlug → AppDefinition 解決 ----
// 25-RESEARCH.md Pitfall 4 対応。useParams で appSlug を取得し、
// /api/apps からアプリ定義を解決してから SuperChatApp に渡す。
function SuperChatWrapper({
  selectedModel,
  theme,
  onToggleTheme,
  onModelChange,
}: {
  selectedModel: string;
  theme: 'light' | 'dark';
  onToggleTheme: () => void;
  onModelChange: (m: string) => void;
}) {
  const { appSlug } = useParams<{ appSlug: string }>();
  const [app, setApp] = useState<AppDefinition | null>(null);
  const [notFound, setNotFound] = useState(false);
  const navigate = useNavigate();

  useEffect(() => {
    let cancelled = false;
    (async () => {
      try {
        const apps = await getApps();
        const found = apps.find((a) => a.slug === appSlug) ?? null;
        if (!cancelled) {
          if (!found) {
            setNotFound(true);
          } else {
            setApp(found);
          }
        }
      } catch {
        if (!cancelled) setNotFound(true);
      }
    })();
    return () => { cancelled = true; };
  }, [appSlug]);

  if (notFound) return <Navigate to="/" replace />;
  if (!app) return null; // ロード中

  return (
    <>
      <Header
        selectedModel={selectedModel}
        onModelChange={onModelChange}
        theme={theme}
        onToggleTheme={onToggleTheme}
        onBackToMenu={() => navigate('/')}
        appName={app.name}
      />
      <SuperChatApp
        selectedModel={selectedModel}
        appId={app.slug}
        appName={app.name}
        appAgents={app.agents}
      />
    </>
  );
}

// ---- GemChat ラッパー: gemId → GemInfo 解決 ----
function GemChatWrapper({
  selectedModel,
  theme,
  onToggleTheme,
  onModelChange,
}: {
  selectedModel: string;
  theme: 'light' | 'dark';
  onToggleTheme: () => void;
  onModelChange: (m: string) => void;
}) {
  const { gemId } = useParams<{ gemId: string }>();
  const [gem, setGem] = useState<GemInfo | null>(null);
  const [notFound, setNotFound] = useState(false);
  const navigate = useNavigate();

  useEffect(() => {
    let cancelled = false;
    (async () => {
      try {
        const gems = await listGems();
        const found = gems.find((g) => g.id === gemId) ?? null;
        if (!cancelled) {
          if (!found) setNotFound(true);
          else setGem(found);
        }
      } catch {
        if (!cancelled) setNotFound(true);
      }
    })();
    return () => { cancelled = true; };
  }, [gemId]);

  if (notFound) return <Navigate to="/gems" replace />;
  if (!gem) return null;

  return (
    <>
      <Header
        selectedModel={selectedModel}
        onModelChange={onModelChange}
        theme={theme}
        onToggleTheme={onToggleTheme}
      />
      <GemChatApp
        gem={gem}
        selectedModel={selectedModel}
        onBack={() => navigate('/gems')}
      />
    </>
  );
}

// ---- Menu / Gems / Canvas スクリーンラッパー(URL ナビゲーション注入) ----
function MenuScreenRoute({
  selectedModel, theme, onToggleTheme, onModelChange,
}: { selectedModel: string; theme: 'light' | 'dark'; onToggleTheme: () => void; onModelChange: (m: string) => void; }) {
  const navigate = useNavigate();
  return (
    <>
      <Header selectedModel={selectedModel} onModelChange={onModelChange} theme={theme} onToggleTheme={onToggleTheme} />
      <MenuScreen
        onNavigate={(app) => {
          if (app.type === 'chat') navigate('/chat');
          else navigate(`/superchat/${app.slug}`);
        }}
        onOpenGems={() => navigate('/gems')}
        onOpenDebate={() => navigate('/debate')}
        onOpenCanvas={() => navigate('/canvas')}
      />
    </>
  );
}

function GemsScreenRoute({
  selectedModel, theme, onToggleTheme, onModelChange,
}: { selectedModel: string; theme: 'light' | 'dark'; onToggleTheme: () => void; onModelChange: (m: string) => void; }) {
  const navigate = useNavigate();
  return (
    <>
      <Header selectedModel={selectedModel} onModelChange={onModelChange} theme={theme} onToggleTheme={onToggleTheme} />
      <GemsScreen
        onSelectGem={(gem) => navigate(`/gemchat/${gem.id}`)}
        onBack={() => navigate('/')}
      />
    </>
  );
}

function CanvasScreenRoute({
  selectedModel, theme, onToggleTheme, onModelChange,
}: { selectedModel: string; theme: 'light' | 'dark'; onToggleTheme: () => void; onModelChange: (m: string) => void; }) {
  const navigate = useNavigate();
  return (
    <>
      <Header selectedModel={selectedModel} onModelChange={onModelChange} theme={theme} onToggleTheme={onToggleTheme} />
      <CanvasScreen
        onBack={() => navigate('/')}
        onStartChat={(tid) => navigate(tid ? `/canvaschat/${tid}` : '/canvaschat')}
      />
    </>
  );
}

// ---- ChatApp / CanvasChatApp / DebateChatApp 用 Header 付きルート ----
function ChatRoute({
  selectedModel, theme, onToggleTheme, onModelChange,
}: { selectedModel: string; theme: 'light' | 'dark'; onToggleTheme: () => void; onModelChange: (m: string) => void; }) {
  const navigate = useNavigate();
  return (
    <>
      <Header selectedModel={selectedModel} onModelChange={onModelChange} theme={theme} onToggleTheme={onToggleTheme} onBackToMenu={() => navigate('/')} />
      <ChatApp selectedModel={selectedModel} />
    </>
  );
}

function CanvasChatRoute({
  selectedModel, theme, onToggleTheme, onModelChange,
}: { selectedModel: string; theme: 'light' | 'dark'; onToggleTheme: () => void; onModelChange: (m: string) => void; }) {
  const navigate = useNavigate();
  return (
    <>
      <Header selectedModel={selectedModel} onModelChange={onModelChange} theme={theme} onToggleTheme={onToggleTheme} />
      <CanvasChatApp selectedModel={selectedModel} onBack={() => navigate('/canvas')} />
    </>
  );
}

function DebateRoute({
  selectedModel, theme, onToggleTheme, onModelChange,
}: { selectedModel: string; theme: 'light' | 'dark'; onToggleTheme: () => void; onModelChange: (m: string) => void; }) {
  const navigate = useNavigate();
  return (
    <>
      <Header selectedModel={selectedModel} onModelChange={onModelChange} theme={theme} onToggleTheme={onToggleTheme} onBackToMenu={() => navigate('/')} appName="討論チャット" />
      <DebateChatApp selectedModel={selectedModel} />
    </>
  );
}

// ---- ルート App コンポーネント ----
export function App() {
  const authValue = useAuthProvider();
  const [selectedModel, setSelectedModel] = useState('gpt-4.1');
  const { theme, toggleTheme } = useTheme();
  const isAuthenticated = authValue.authState === 'authenticated';

  const common = {
    selectedModel,
    theme,
    onToggleTheme: toggleTheme,
    onModelChange: setSelectedModel,
  };

  return (
    <AuthContext.Provider value={authValue}>
      <ThemeContext.Provider value={theme}>
        {isAuthenticated ? (
          <div style={{ display: 'flex', flexDirection: 'column', height: '100%' }}>
            <Routes>
              <Route index element={<MenuScreenRoute {...common} />} />

              <Route path="chat" element={<ChatRoute {...common} />} />
              <Route path="chat/:threadId" element={<ChatRoute {...common} />} />

              <Route path="superchat/:appSlug" element={<SuperChatWrapper {...common} />} />
              <Route path="superchat/:appSlug/:threadId" element={<SuperChatWrapper {...common} />} />

              <Route path="gems" element={<GemsScreenRoute {...common} />} />
              <Route path="gemchat/:gemId" element={<GemChatWrapper {...common} />} />
              <Route path="gemchat/:gemId/:threadId" element={<GemChatWrapper {...common} />} />

              <Route path="canvas" element={<CanvasScreenRoute {...common} />} />
              <Route path="canvaschat" element={<CanvasChatRoute {...common} />} />
              <Route path="canvaschat/:threadId" element={<CanvasChatRoute {...common} />} />

              <Route path="debate" element={<DebateRoute {...common} />} />
              <Route path="debate/:threadId" element={<DebateRoute {...common} />} />

              {/* 未知のパスはメニューへ */}
              <Route path="*" element={<Navigate to="/" replace />} />
            </Routes>
          </div>
        ) : (
          <AuthPanel />
        )}
      </ThemeContext.Provider>
    </AuthContext.Provider>
  );
}
```

重要ポイント:
- `useState<Screen>` とナビゲーションハンドラ一式を全削除
- `activeApp/activeGem/activeCanvasAppId/canvasGemId` の state を全て削除(各 ChatApp と Wrapper が URL から自己復元)
- `<Navigate to="/*" />` で未知パスはメニューにフォールバック
- `listGems` / `getApps` は api/client.ts に既存(existing)。存在しない場合は必要分を import で確認
- SuperChat/Gem Wrapper でロード中は `null` を返す(短時間のためスピナー不要)

もし `listGems` が api/client.ts に存在しない場合は、代わりに既存の `/api/gems` エンドポイント呼び出し関数名に合わせて import を調整する(実装時に確認)。
</task_detail>

<automated>
cd frontend && bun run build 2>&1 | tail -30
</automated>

### Wave 3: 各 ChatApp での useParams 同期

**Rationale:** App.tsx のルーティングは完成したが、各 ChatApp 内の `useThreads` は URL を知らない。ここで `useParams` と `switchThread` を接続し、URL を single source of truth にする。各ファイルは独立しているため並列実行可。

#### Task 25-03-01: ChatApp に useParams / useNavigate を導入

**File:** `frontend/src/components/ChatApp.tsx`
**Action:** MODIFY
**Why:** URL の `:threadId` を `switchThread` と同期し、サイドバーでのスレッド選択時に `navigate` で URL を更新する。

<task_detail>
`ChatApp` コンポーネントを以下のように変更する。既存の MainContainer レイアウト、canvas、useChat は保持する。

変更点:

1. import に `useParams, useNavigate` を追加:
```typescript
import { useParams, useNavigate } from 'react-router';
```

2. コンポーネント冒頭で URL パラメータ取得 + 同期 useEffect を追加:
```typescript
export function ChatApp({ selectedModel }: ChatAppProps) {
  const { threadId: urlThreadId } = useParams<{ threadId?: string }>();
  const navigate = useNavigate();

  const {
    threads,
    activeThreadId,
    messages,
    isLoadingMessages,
    switchThread,
    createNewThread,
    removeThread,
    setMessages,
    refreshThreads,
  } = useThreads('chat');

  // 25-RESEARCH.md Pitfall 5 対策: URL を single source of truth とする
  // URL の threadId が変わったら useThreads の activeThreadId を同期する
  useEffect(() => {
    if (urlThreadId && urlThreadId !== activeThreadId) {
      switchThread(urlThreadId);
    }
    // urlThreadId が undefined(/chat) の場合は activeThreadId をクリアしない
    // (新規スレッド作成時に /chat → /chat/:tid の遷移で消えないように)
  }, [urlThreadId, activeThreadId, switchThread]);
  // ...
```

3. ナビゲーションハンドラを URL 更新に接続:
```typescript
const handleNewChat = async () => {
  const tid = await createNewThread();
  // 新規スレッドは履歴を汚染しないよう replace
  navigate(`/chat/${tid}`, { replace: true });
};

const handleSelectThread = async (threadId: string) => {
  navigate(`/chat/${threadId}`);
};
```

4. `handleSend` 内で新規スレッド作成時に URL も更新:
```typescript
const handleSend = async (text: string) => {
  let threadId = activeThreadId;
  if (!threadId) {
    threadId = await createNewThread();
    navigate(`/chat/${threadId}`, { replace: true });
  }
  // ...既存の sendMessage 呼び出し
};
```

5. `useEffect` import を忘れずに追加(`import { useCallback, useEffect, useRef, useState } from 'react';`)

注意:
- `useThreads` フックへの変更は行わない。呼び出し側で同期する方針(RESEARCH Pitfall 5 推奨)
- basename("/orochi") は BrowserRouter が自動付加するので `/chat/${tid}` のまま書く(先頭スラッシュあり)
</task_detail>

<automated>
cd frontend && bun run build 2>&1 | tail -20
</automated>

#### Task 25-03-02: SuperChatApp / GemChatApp / DebateChatApp / CanvasChatApp に URL 同期を導入

**File:** `frontend/src/components/SuperChatApp.tsx`, `frontend/src/components/GemChatApp.tsx`, `frontend/src/components/DebateChatApp.tsx`, `frontend/src/components/CanvasChatApp.tsx`
**Action:** MODIFY
**Why:** 同じ URL 同期パターンを他の ChatApp 類にも適用する。CanvasChatApp は `canvasGemId` の自己取得も合わせて実装する。

<task_detail>
各ファイルに対して Task 25-03-01 と同じパターンを適用する。URL パターンのみファイルごとに異なる。

**SuperChatApp.tsx:**
- `useParams<{ appSlug: string; threadId?: string }>()`
- スレッド選択: `navigate(\`/superchat/${appSlug}/${tid}\`)`
- 新規作成: `navigate(\`/superchat/${appSlug}/${tid}\`, { replace: true })`
- `useEffect` で `urlThreadId` → `switchThread` 同期
- appId props は既存のまま(Wrapper が渡してくれる)

**GemChatApp.tsx:**
- `useParams<{ gemId: string; threadId?: string }>()`
- スレッド選択: `navigate(\`/gemchat/${gemId}/${tid}\`)`
- 新規作成: `navigate(\`/gemchat/${gemId}/${tid}\`, { replace: true })`
- `useEffect` で URL 同期

**DebateChatApp.tsx:**
- `useParams<{ threadId?: string }>()`
- スレッド選択: `navigate(\`/debate/${tid}\`)`
- 新規作成: `navigate(\`/debate/${tid}\`, { replace: true })`
- `useEffect` で URL 同期

**CanvasChatApp.tsx:** (追加変更あり)
- RESEARCH.md Open Question 1 の決定に従い、`canvasGemId` と `initialThreadId` props を廃止
- コンポーネント内部で `getCanvasGemId()` を mount 時に呼び出して取得
- `useParams<{ threadId?: string }>()` で threadId を取得
- props は `{ selectedModel, onBack }` のみに簡素化
- App.tsx 側(Task 25-02-01 の `CanvasChatRoute`)は既にこの新 props に合わせて書いている

CanvasChatApp 変更例(冒頭のみ):
```typescript
import { useEffect, useState } from 'react';
import { useParams, useNavigate } from 'react-router';
import { getCanvasGemId } from '../api/client';

interface CanvasChatAppProps {
  selectedModel: string;
  onBack: () => void;
}

export function CanvasChatApp({ selectedModel, onBack }: CanvasChatAppProps) {
  const { threadId: urlThreadId } = useParams<{ threadId?: string }>();
  const navigate = useNavigate();
  const [canvasGemId, setCanvasGemId] = useState<string | null>(null);
  const [gemLoadError, setGemLoadError] = useState(false);

  useEffect(() => {
    let cancelled = false;
    (async () => {
      try {
        const { gem_id } = await getCanvasGemId();
        if (!cancelled) setCanvasGemId(gem_id);
      } catch {
        if (!cancelled) setGemLoadError(true);
      }
    })();
    return () => { cancelled = true; };
  }, []);

  // gemId ロード前は何も表示しない(短時間)
  if (!canvasGemId) {
    if (gemLoadError) return <div>Canvas gem が取得できませんでした</div>;
    return null;
  }

  // 既存の useThreads/useChat ロジックは下に続く。
  // useThreads 呼び出しで canvasGemId を使用し、
  // urlThreadId → switchThread 同期 useEffect を追加。
  // ...既存コードの残り...
}
```

注意事項:
- 各ファイルの既存ロジック(useThreads, useChat, MainContainer, canvas ペイン等)は保持する
- `useNavigate` は react-router から import する
- `useParams` の戻り値は optional なので `threadId: urlThreadId` として扱う
- `navigate` はテンプレート文字列のバッククォートに注意(replace_all 不可のパターン。Edit ツールで手動確認)
</task_detail>

<automated>
cd frontend && bun run build 2>&1 | tail -30
</automated>

### Wave 4: 検証 + nginx ドキュメント更新 + 手動確認

**Rationale:** コード変更が全て入った段階で TypeScript ビルドを通し、nginx ドキュメントに SPA fallback 設定を追記する。最後にブラウザで全 URL パターンを手動確認する。

#### Task 25-04-01: nginx SPA fallback ドキュメント更新

**File:** `docs/nginx.md`
**Action:** MODIFY
**Why:** RESEARCH.md Pitfall 2 で特定された、production nginx が `/orochi/chat/abc-123` のような deep URL を 404 返却する問題への対策を明文化する。

<task_detail>
`docs/nginx.md` に以下のセクションを追記する(既存内容は保持):

```markdown
## SPA Fallback (Phase 25)

React Router v6(HTML5 History API) を使うため、`/orochi/chat/abc-123` のような
deep URL を直接開いたときに nginx が 404 を返さないよう `try_files` を設定する。

```nginx
location /orochi/ {
  # 既存のプレフィックス strip + proxy_pass 設定はそのまま
  # SPA fallback を追加:
  try_files $uri $uri/ /orochi/index.html;
}
```

注意:
- `/orochi/api/` など API パスには fallback が適用されないよう location 分離を維持する
- 開発環境(Vite dev server)は自動的に fallback するためこの設定は production のみ必要
```

もし `docs/nginx.md` が存在しない場合は、新規作成してこのセクションのみ記載する。
</task_detail>

<automated>
test -f docs/nginx.md && grep -q "try_files" docs/nginx.md && echo OK
</automated>

#### Task 25-04-02: TypeScript ビルド + ブラウザ手動確認

**File:** (検証のみ)
**Action:** VERIFY
**Why:** 全てのコード変更が整合していることを TypeScript ビルドで確認し、ブラウザで URL パターンを実動作確認する。

<task_detail>
1. Docker コンテナでフロントエンドをビルドして型エラーがないことを確認:
```bash
docker compose run --rm frontend bun run build
```
期待: エラーなしで完了。`dist/` が生成される。

2. 開発サーバーを起動(既に起動済みならスキップ):
```bash
docker compose up -d frontend
```

3. ブラウザ手動確認チェックリスト(`http://localhost:5173/orochi/` を起点):

   - [ ] URL-01: トップページ `http://localhost:5173/orochi/` で MenuScreen が表示される
   - [ ] URL-02: メニューから「Chat」を選択すると URL が `/orochi/chat` に変わり、ChatApp が表示される
   - [ ] URL-03: サイドバーで既存スレッドを選択すると URL が `/orochi/chat/{thread_id}` に変わる
   - [ ] URL-04: ブラウザの戻るボタンでメニューに戻る。進むボタンで再びスレッドに遷移する
   - [ ] URL-05: 現在の URL をコピーして別タブで開くと同じスレッドがロードされる
   - [ ] URL-06: `/orochi/superchat/{既存アプリslug}` に直接アクセスすると SuperChat がアプリ名付きで表示される
   - [ ] URL-07: `/orochi/gems` → Gem 選択 → `/orochi/gemchat/{gemId}` URL が反映される
   - [ ] URL-08: `/orochi/canvaschat` に直接アクセスすると canvasGemId が自動取得されて Canvas チャットが開く
   - [ ] URL-09: `/orochi/debate` に直接アクセスすると討論チャットが開く
   - [ ] URL-10: 存在しないパス `/orochi/nonexistent` にアクセスするとメニューにリダイレクトされる
   - [ ] URL-11: URL バーに二重スラッシュ `/orochi//chat` が発生しない(basename trailing slash 対策)

4. 失敗時の対応:
   - 型エラー → 該当ファイルの import / 型定義を確認
   - 画面が空 → Routes の path 設定漏れを確認
   - URL が更新されない → navigate 呼び出し漏れを確認
   - 二重スラッシュ → main.tsx の basename replace 処理を確認

このタスクは checkpoint:human-verify として扱い、ユーザーに確認を依頼する。
</task_detail>

<automated>
docker compose run --rm frontend bun run build 2>&1 | tail -10
</automated>

## Verification

全 URL-01〜URL-11 の手動確認に加え、以下の自動検証を通す:

1. `cd frontend && bun run build` が型エラーなしで通る
2. `grep -r 'react-router-dom' frontend/src` が何もヒットしない(誤った import がない)
3. `grep -c 'BrowserRouter' frontend/src/main.tsx` が 1 以上

## Success Criteria

- [ ] `react-router` 7.x が package.json に追加されている
- [ ] `main.tsx` で `BrowserRouter basename` が `import.meta.env.BASE_URL` から末尾スラッシュ除去で生成されている
- [ ] `App.tsx` が Routes/Route ベースに置き換わり、useState<Screen> による切替ロジックが完全に削除されている
- [ ] 各 ChatApp / SuperChatApp / GemChatApp / CanvasChatApp / DebateChatApp が `useParams` で threadId を取得し `useThreads` と同期している
- [ ] CanvasChatApp が `canvasGemId` を内部で取得するようになり、App.tsx の canvasGemId state が削除されている
- [ ] SuperChat/Gem 用の Wrapper が URL から AppDefinition/GemInfo を解決している
- [ ] TypeScript ビルドがエラーなしで通る
- [ ] 全 URL 手動確認項目(URL-01〜URL-11)をユーザーが確認済み
- [ ] `docs/nginx.md` に SPA fallback 設定が記載されている

<threat_model>
## Threat Model

| ID | Threat | Category | Severity | Mitigation |
|----|--------|----------|----------|------------|
| T-25-01 | 操作された URL パラメータによるオープンリダイレクト | Tampering | LOW | React Router は内部ルーティングのみ。外部ホストへの遷移は `Navigate` / `navigate` で発生しない。`<Link>` も basename 内限定 |
| T-25-02 | URL 経由の Thread ID 列挙による他ユーザースレッドへの不正アクセス | Elevation of Privilege | LOW | バックエンドの既存 JWT ガード(github_login フィルタ)が防御。URL で thread_id を指定しても所有者でなければ 403/404 が返る |
| T-25-03 | URL パラメータ不正値(非存在 thread_id, 不正 UUID 文字) | Tampering | LOW | `loadThreadMessages` の try-catch で空メッセージにフォールバック(既存 useThreads.switchThread)。threadId は encodeURIComponent で URL 生成、デコードは React Router が担当 |
| T-25-04 | 未認証状態で deep URL にアクセスしたときの URL コンテキスト喪失 | Information Disclosure (UX) | LOW | Phase スコープ外だが、現在の `AuthPanel` フォールバックにより認証前は全てのルートがブロックされる。認証後は URL がそのまま復元される(BrowserRouter が URL を保持) |
| T-25-05 | 開発/本番での basename 不整合による画面が真っ白 | DoS(自爆) | LOW | `import.meta.env.BASE_URL` を唯一の情報源とし、ハードコードしない。Vite config の base とビルド時に自動同期 |
| T-25-06 | SPA deep link で nginx が 404 返却(production のみ) | Availability | MEDIUM | `docs/nginx.md` に `try_files $uri $uri/ /orochi/index.html;` を追記。インフラ担当が反映する手順を明記(Task 25-04-01) |
</threat_model>

## Output

完了後、以下を作成:
`.planning/phases/25-react-router-v6-url-thread-id-url-app-prefix-orochi/25-01-SUMMARY.md`

summary には以下を含める:
- 追加された依存関係(react-router 7.x)
- 新しい URL 構造の一覧
- 各 ChatApp で導入した同期パターン
- nginx 側で必要な `try_files` 設定
- ユーザーが確認した URL 動作チェックリスト結果
