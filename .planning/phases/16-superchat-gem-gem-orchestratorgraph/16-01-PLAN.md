---
phase: 16-superchat-gem-gem-orchestratorgraph
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/orchestrator/gem_agent.py
  - app/jobs/handlers/orchestrator_handler.py
autonomous: true
requirements:
  - GEM-SUB-01
  - GEM-SUB-02

must_haves:
  truths:
    - "GemSubAgent が run(state) を呼び出すと、Gem の system_prompt + knowledge を結合したシステムプロンプトで LLM が応答を返す"
    - "OrchestratorHandler に gem_ids を渡すと、DB から Gem を取得して GemSubAgent として registry にマージされる"
    - "gem_ids が空または None のとき、既存の OrchestratorHandler の動作が変わらない（後方互換）"
  artifacts:
    - path: "app/orchestrator/gem_agent.py"
      provides: "GemSubAgent クラス（keywords=[], run(state) 実装）"
      exports: ["GemSubAgent"]
    - path: "app/jobs/handlers/orchestrator_handler.py"
      provides: "gem_ids 受け取り・DB 取得・GemSubAgent マージロジック"
      contains: "gem_ids"
  key_links:
    - from: "app/jobs/handlers/orchestrator_handler.py"
      to: "app/orchestrator/gem_agent.py"
      via: "GemSubAgent インスタンス生成"
      pattern: "GemSubAgent"
    - from: "app/orchestrator/gem_agent.py"
      to: "app/orchestrator/state.py:AgentState"
      via: "run(state: AgentState) -> AgentState"
      pattern: "AgentState"
---

<objective>
GemSubAgent クラスを新規実装し、OrchestratorHandler が gem_ids ジョブパラメータを受け取って Gem をルーティング対象として扱えるようにする。

Purpose: SuperChat のオーケストレーターが ./agents/ のツールエージェントと Gem（プロンプトエージェント）の両方をルーティング候補として扱えるようにする基盤を作る。
Output: app/orchestrator/gem_agent.py（GemSubAgent クラス）、app/jobs/handlers/orchestrator_handler.py（gem_ids 対応）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/orchestrator/agent.py
@app/orchestrator/state.py
@app/jobs/handlers/orchestrator_handler.py
@app/orchestrator/graph.py

<interfaces>
<!-- GemSubAgent が準拠すべき既存インターフェース -->

From app/orchestrator/state.py:
```python
class AgentState(TypedDict):
    input: str
    output: str
    messages: Annotated[list[BaseMessage], operator.add]
    next: str
    context: Annotated[RPCContext, _keep_first]
    error: str | None
```

From app/orchestrator/agent.py (SubAgent.run — GemSubAgent が同パターンを実装する):
```python
async def run(self, state: AgentState) -> AgentState:
    messages = [
        SystemMessage(content=self._system_prompt),
        HumanMessage(content=state["input"]),
    ]
    response = await self._llm.ainvoke(messages)
    return {
        "output": response.content,
        "messages": [AIMessage(content=response.content)],
    }
```

From app/orchestrator/graph.py (build_orchestrator_graph — GemSubAgent が持つべきフィールド):
```python
# registry.all() → list of agents with .name, .description, .run attributes
for agent in registry.all():
    graph.add_node(agent.name, agent.run)
routing_map = {a.name: a.name for a in registry.all()}
```

From app/jobs/handlers/orchestrator_handler.py (gem_ids 挿入箇所):
```python
# agents_filter フィルタリング完了後、graph.compile 前が挿入ポイント
if agents_filter:
    registry.agents = {
        k: v for k, v in registry.agents.items() if k in agents_filter
    }
# ← ここに gem_ids 処理を追加する（D-08）
```

gems テーブルカラム (app/api/routes/gems.py より):
gem_id, github_login, name, system_prompt, description, knowledge, type, is_public, created_at, updated_at
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GemSubAgent クラスの実装</name>
  <files>app/orchestrator/gem_agent.py</files>
  <behavior>
    - GemSubAgent(name, description, system_prompt, knowledge, llm) で初期化できる
    - keywords プロパティは常に空リスト [] を返す（Stage-1 キーワードルーターをスキップ）
    - run(state) は SystemMessage(system_prompt + "\n\n" + knowledge if knowledge else system_prompt) + HumanMessage(state["input"]) で llm.ainvoke() を呼び出す
    - run(state) の戻り値は {"output": response.content, "messages": [AIMessage(content=response.content)]}
    - knowledge が空文字列の場合、system_prompt のみをシステムプロンプトとして使用する
  </behavior>
  <action>
`app/orchestrator/gem_agent.py` を新規作成する（D-01, D-02, D-03, D-04）。

```python
from __future__ import annotations
from langchain_core.messages import AIMessage, HumanMessage, SystemMessage
from langchain_core.language_models import BaseChatModel
from app.orchestrator.state import AgentState


class GemSubAgent:
    """Gem をプロンプトエージェントとして OrchestratorGraph に統合するラッパー。

    - SubAgent は継承しない（D-01）: SubAgent は from_dir() / ChatCopilot 生成ロジックを持つが
      GemSubAgent は DB から取得した system_prompt/knowledge を使うため不要。
    - keywords=[] 固定（D-03）: Stage-1 キーワードルーターをスキップし、
      常に Stage-2 LLM ルーターで評価される。
    - run() が返す AgentState は SubAgent と同一形式（D-05: graph.py 変更不要）。
    """

    def __init__(
        self,
        name: str,
        description: str,
        system_prompt: str,
        knowledge: str,
        llm: BaseChatModel,
    ) -> None:
        self.name = name
        self.description = description
        self.keywords: list[str] = []  # D-03: always empty → skips Stage-1
        self._llm = llm
        # D-02: system_prompt + knowledge を結合。knowledge が空なら system_prompt のみ
        self._full_prompt = (
            f"{system_prompt}\n\n{knowledge}" if knowledge else system_prompt
        )

    async def run(self, state: AgentState) -> AgentState:
        """D-04: BaseChatModel.ainvoke で応答を生成する。"""
        messages = [
            SystemMessage(content=self._full_prompt),
            HumanMessage(content=state["input"]),
        ]
        response = await self._llm.ainvoke(messages)
        return {
            "output": response.content,
            "messages": [AIMessage(content=response.content)],
        }
```

`SubAgent` を継承しない理由をコメントに明記する（D-01）。`close()` メソッドは不要（llm の lifecycle は OrchestratorHandler が管理する ChatCopilot インスタンスを共有するため）。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && python -c "from app.orchestrator.gem_agent import GemSubAgent; print('GemSubAgent import OK')"</automated>
  </verify>
  <done>
    - app/orchestrator/gem_agent.py が存在し GemSubAgent をエクスポートする
    - keywords プロパティが [] を返す
    - run(state) が正しいシグネチャで実装されている
    - knowledge が空の場合と非空の場合で _full_prompt が正しく構築される
  </done>
</task>

<task type="auto">
  <name>Task 2: OrchestratorHandler への gem_ids 統合</name>
  <files>app/jobs/handlers/orchestrator_handler.py</files>
  <action>
`orchestrator_handler.py` の `handle()` メソッドに gem_ids 処理を追加する（D-06, D-07, D-08, D-09）。

**変更箇所: agents_filter フィルタリング完了後、graph.compile 前**

```python
# 既存 import に追加
import psycopg
from psycopg.rows import dict_row
from app.orchestrator.gem_agent import GemSubAgent
from app.providers.copilot import ChatCopilot
```

`handle()` メソッド内の agents_filter フィルタリング処理の直後（`if not registry.agents: raise RuntimeError(...)` の後、`github_login = job.get(...)` の前）に以下を挿入する:

```python
# D-06: gem_ids を job から読み取る
gem_ids: list[str] = job.get("gem_ids") or []

# D-07: DB から招待 Gem を一括取得（所有者または公開 Gem のみ）
if gem_ids:
    gem_llm = ChatCopilot(model="claude-haiku-4-5-20251001", github_token=github_token)
    try:
        async with await psycopg.AsyncConnection.connect(
            DB_URI, row_factory=dict_row
        ) as conn:
            async with conn.cursor() as cur:
                await cur.execute(
                    """SELECT gem_id, name, description, system_prompt, knowledge
                       FROM gems
                       WHERE gem_id = ANY(%s::uuid[])
                         AND (is_public = true OR github_login = %s)""",
                    (gem_ids, github_login if 'github_login' in dir() else job.get("github_login", "unknown")),
                )
                gem_rows = await cur.fetchall()
    except Exception as e:
        logger.warning("OrchestratorHandler: failed to fetch gems: %s", e)
        gem_rows = []

    # D-08: GemSubAgent を生成し registry.agents に直接マージする
    for row in gem_rows:
        gem_agent = GemSubAgent(
            name=row["name"],
            description=row["description"] or f"Gem: {row['name']}",
            system_prompt=row["system_prompt"] or "",
            knowledge=row["knowledge"] or "",
            llm=gem_llm,
        )
        registry.agents[gem_agent.name] = gem_agent
        logger.info("[registry] merged gem agent: %s", gem_agent.name)
```

**重要な順序の考慮:** `github_login` 変数は元のコードでは `agents_filter` 処理の後に定義されているため、`gem_ids` 処理のタイミングで参照できるよう `job.get("github_login", "unknown")` を直接使う。あるいはコードを `github_login` 定義の後に移動する。

**実際の配置:** `github_login: str = job.get("github_login", "unknown")` の行の直後（context 構築の前、AsyncPostgresSaver ブロックの前）に挿入するのが最もクリーン。

**D-09 後方互換:** `gem_ids` が空または None のとき、上記ブロックは完全にスキップされる（`if gem_ids:` ガード）。

`finally` ブロックの `await registry.close()` は GemSubAgent には `close()` がないため影響しない（`registry.close()` は `for agent in self.agents.values(): await agent.close()` を呼ぶが、SubAgent 型でないオブジェクトは close() を持たない）。

**対応策:** `SubAgentRegistry.close()` を変更するか、GemSubAgent に no-op の `close()` メソッドを追加する。`gem_agent.py` に `async def close(self) -> None: pass` を追加する。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && python -c "from app.jobs.handlers.orchestrator_handler import OrchestratorHandler; print('OrchestratorHandler import OK')"</automated>
  </verify>
  <done>
    - orchestrator_handler.py が gem_ids をジョブパラメータから読み取る
    - DB クエリが所有者または公開 Gem のみを返すフィルタ条件（D-07）を実装している
    - GemSubAgent が registry.agents dict に直接マージされる（D-08）
    - gem_ids が空の場合は既存動作が変わらない（D-09 後方互換）
    - GemSubAgent に async close() no-op が追加されている（registry.close() との互換）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job payload → handler | arq worker job dict は信頼境界。gem_ids は UUID 文字列の配列として受け取る |
| handler → PostgreSQL | DB クエリは psycopg パラメタライズドクエリで SQL インジェクションを防ぐ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Elevation | OrchestratorHandler gem_ids クエリ | mitigate | `WHERE gem_id = ANY(%s::uuid[]) AND (is_public = true OR github_login = %s)` — 所有者または公開のみ許可。他ユーザーの private Gem へのアクセスを遮断 |
| T-16-02 | Tampering | gem_ids 配列に不正 UUID | accept | psycopg が `::uuid[]` キャストで不正値を DB レベルで拒否する。アプリ側追加バリデーション不要 |
| T-16-03 | Denial of Service | 大量 gem_ids | accept | 200名規模の社内利用。現実的な上限はアプリレイヤーで管理（将来フェーズ候補） |
</threat_model>

<verification>
1. `python -c "from app.orchestrator.gem_agent import GemSubAgent"` — インポートエラーなし
2. `python -c "from app.jobs.handlers.orchestrator_handler import OrchestratorHandler"` — インポートエラーなし
3. GemSubAgent のインスタンスを生成し `keywords` が `[]` であることを確認
4. knowledge 空文字列 → `_full_prompt == system_prompt`、非空 → `_full_prompt == f"{system_prompt}\n\n{knowledge}"` を確認
5. OrchestratorHandler の handle() に gem_ids キーが存在しない job dict を渡したとき、既存動作（agents_filter のみ）が維持されることを確認
</verification>

<success_criteria>
- GemSubAgent クラスが `app/orchestrator/gem_agent.py` に実装されている
- `keywords = []`、`run(state: AgentState) -> AgentState` が正しく動作する
- OrchestratorHandler が gem_ids を受け取り、DB から Gem を取得して registry にマージする
- gem_ids=None/[] のとき後方互換が保たれる
- SubAgentRegistry.close() との互換のため GemSubAgent に no-op close() がある
</success_criteria>

<output>
完了後、`.planning/phases/16-superchat-gem-gem-orchestratorgraph/16-01-SUMMARY.md` を作成すること。
</output>
