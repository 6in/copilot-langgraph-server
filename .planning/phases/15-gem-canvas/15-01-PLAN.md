---
phase: 15-gem-canvas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/main.py
  - app/api/models.py
  - app/api/routes/gems.py
autonomous: true
requirements: [GEM-01, GEM-02, GEM-03, CANVAS-01]

must_haves:
  truths:
    - "gems テーブルと canvas_apps テーブルが PostgreSQL に存在する"
    - "threads テーブルに gem_id カラムが存在する（既存データは NULL）"
    - "認証済みユーザーが Gem を作成・一覧・取得・更新・削除できる"
    - "他ユーザーの Gem にはアクセスできない（404 返却）"
  artifacts:
    - path: "app/api/routes/gems.py"
      provides: "Gem CRUD API endpoints"
      exports: ["router"]
    - path: "app/api/models.py"
      provides: "GemCreate, GemUpdate, GemInfo, CanvasAppInfo, CanvasDeployResponse"
  key_links:
    - from: "app/api/routes/gems.py"
      to: "app/api/routes/chat.py"
      via: "import get_jwt_payload"
      pattern: "from app.api.routes.chat import get_jwt_payload"
    - from: "app/api/main.py"
      to: "app/api/routes/gems.py"
      via: "include_router"
      pattern: "app.include_router\\(gems.router\\)"
---

<objective>
Phase 15 のバックエンド基盤を構築する。PostgreSQL に gems / canvas_apps テーブルを追加し、threads テーブルに gem_id カラムを追加する。Pydantic モデルを定義し、Gem CRUD API エンドポイントを実装する。

Purpose: Gem（AI ペルソナ）と Canvas（HTML 生成・デプロイ）の永続化層と Gem 管理 API を提供する。Canvas API と Worker 拡張（Plan 02）、フロントエンド（Plan 03/04）の前提条件となる。
Output: gems.py ルート、DDL マイグレーション、Pydantic モデル
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-gem-canvas/15-CONTEXT.md
@.planning/phases/15-gem-canvas/15-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From app/api/routes/chat.py:
```python
async def get_jwt_payload(request: Request) -> dict:
    """FastAPI dependency: decode JWT session cookie and return the full payload dict."""
    # Returns dict with 'github_login', 'github_token', etc.

async def get_github_token(payload: dict = Depends(get_jwt_payload)) -> str:
    """FastAPI dependency: extract and decrypt GitHub token from JWT session cookie."""
```

From app/api/main.py (lifespan DDL pattern):
```python
async with await psycopg.AsyncConnection.connect(DB_URI) as conn:
    await conn.execute("CREATE TABLE IF NOT EXISTS ...")
    await conn.commit()
```

From app/api/main.py (router registration):
```python
app.include_router(auth.router)
app.include_router(chat.router)
app.include_router(jobs.router)
app.include_router(me.router)
app.include_router(agents.router)
app.include_router(apps.router)
app.include_router(health.router)
```

From app/api/models.py (existing pattern):
```python
from typing import Literal
from pydantic import BaseModel

class AppInfo(BaseModel):
    slug: str
    name: str
    description: str
    icon: str
    agents: list[str]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DDL マイグレーション + Pydantic モデル定義</name>
  <files>app/api/main.py, app/api/models.py</files>
  <read_first>
    - app/api/main.py (現在の lifespan DDL パターンと include_router 順序を確認)
    - app/api/models.py (既存の Pydantic モデルパターンを確認)
    - .planning/phases/15-gem-canvas/15-CONTEXT.md (DDL スキーマの正確な定義)
    - .planning/phases/15-gem-canvas/15-RESEARCH.md (Pitfall 2: FK 順序依存)
  </read_first>
  <action>
**app/api/main.py の lifespan 内、既存の `await conn.commit()` の直前に以下の DDL を追加する。** 順序が重要: gems -> threads ALTER -> canvas_apps の順。

```python
# --- Phase 15: Gem + Canvas tables ---

# gems テーブル（AI ペルソナ管理）
await conn.execute(
    """CREATE TABLE IF NOT EXISTS gems (
           gem_id        UUID PRIMARY KEY DEFAULT gen_random_uuid(),
           github_login  TEXT NOT NULL,
           name          TEXT NOT NULL,
           system_prompt TEXT NOT NULL DEFAULT '',
           type          TEXT NOT NULL DEFAULT 'default',
           created_at    TIMESTAMPTZ DEFAULT now(),
           updated_at    TIMESTAMPTZ DEFAULT now()
       )"""
)
await conn.execute(
    "CREATE INDEX IF NOT EXISTS gems_github_login_idx ON gems(github_login)"
)

# threads テーブルへの gem_id カラム追加（gems テーブル作成後に実行 — Pitfall 2）
await conn.execute(
    "ALTER TABLE threads ADD COLUMN IF NOT EXISTS gem_id UUID REFERENCES gems(gem_id) ON DELETE SET NULL"
)

# canvas_apps テーブル（生成 HTML 保存）
await conn.execute(
    """CREATE TABLE IF NOT EXISTS canvas_apps (
           app_id       UUID PRIMARY KEY DEFAULT gen_random_uuid(),
           thread_id    TEXT REFERENCES threads(thread_id) ON DELETE SET NULL,
           github_login TEXT NOT NULL,
           name         TEXT NOT NULL,
           html         TEXT NOT NULL,
           source       TEXT NOT NULL DEFAULT 'canvas',
           deployed     BOOLEAN NOT NULL DEFAULT FALSE,
           deployed_at  TIMESTAMPTZ,
           created_at   TIMESTAMPTZ DEFAULT now(),
           UNIQUE (thread_id, github_login)
       )"""
)
await conn.execute(
    "CREATE INDEX IF NOT EXISTS canvas_apps_thread_id_idx ON canvas_apps(thread_id)"
)
await conn.execute(
    "CREATE INDEX IF NOT EXISTS canvas_apps_github_login_idx ON canvas_apps(github_login)"
)
```

UNIQUE (thread_id, github_login) 制約を canvas_apps に追加する理由: Worker の upsert で `ON CONFLICT (thread_id, github_login) DO UPDATE` を使用するため（15-RESEARCH.md Open Questions #2）。

**app/api/models.py に以下の Pydantic モデルを追加:**

```python
class GemCreate(BaseModel):
    """Request body for POST /api/gems."""
    name: str
    system_prompt: str = ""
    type: Literal["default", "canvas"] = "default"

class GemUpdate(BaseModel):
    """Request body for PATCH /api/gems/{gem_id}."""
    name: str | None = None
    system_prompt: str | None = None
    type: Literal["default", "canvas"] | None = None

class GemInfo(BaseModel):
    """Response model for Gem endpoints."""
    gem_id: str
    name: str
    system_prompt: str
    type: str
    created_at: str
    updated_at: str

class CanvasAppInfo(BaseModel):
    """Response model for Canvas App endpoints."""
    app_id: str
    thread_id: str | None = None
    name: str
    html: str
    source: str
    deployed: bool
    deployed_at: str | None = None
    created_at: str

class CanvasDeployResponse(BaseModel):
    """Response model for POST /api/canvas/apps/{app_id}/deploy."""
    url: str
```

**app/api/main.py の import に gems ルートを追加し、include_router を登録する。** health.router の後、StaticFiles mount の前に配置:

```python
from app.api.routes import agents, apps, auth, canvas, chat, gems, health, jobs, me
# ...
app.include_router(gems.router)
```

canvas ルートの import と include_router は Plan 02 で追加するため、ここでは gems のみ。ただし import 行に canvas を先に含めても問題ない（モジュールが存在すれば）。gems のみ追加すること。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && python -c "from app.api.models import GemCreate, GemUpdate, GemInfo, CanvasAppInfo, CanvasDeployResponse; print('models OK')" && grep -c "CREATE TABLE IF NOT EXISTS gems" app/api/main.py && grep -c "gems_github_login_idx" app/api/main.py && grep -c "canvas_apps" app/api/main.py</automated>
  </verify>
  <acceptance_criteria>
    - app/api/main.py contains `CREATE TABLE IF NOT EXISTS gems`
    - app/api/main.py contains `ALTER TABLE threads ADD COLUMN IF NOT EXISTS gem_id UUID REFERENCES gems(gem_id)`
    - app/api/main.py contains `CREATE TABLE IF NOT EXISTS canvas_apps`
    - app/api/main.py contains `UNIQUE (thread_id, github_login)` inside canvas_apps DDL
    - app/api/main.py contains `gems_github_login_idx`
    - app/api/main.py contains `canvas_apps_thread_id_idx`
    - app/api/main.py contains `canvas_apps_github_login_idx`
    - DDL 順序: gems テーブル作成 → threads ALTER → canvas_apps テーブル作成
    - app/api/models.py contains `class GemCreate(BaseModel)`
    - app/api/models.py contains `class GemUpdate(BaseModel)`
    - app/api/models.py contains `class GemInfo(BaseModel)`
    - app/api/models.py contains `class CanvasAppInfo(BaseModel)`
    - app/api/models.py contains `class CanvasDeployResponse(BaseModel)`
    - app/api/main.py contains `app.include_router(gems.router)`
    - `python -c "from app.api.models import GemCreate, GemUpdate, GemInfo, CanvasAppInfo, CanvasDeployResponse"` exits 0
  </acceptance_criteria>
  <done>gems/canvas_apps テーブル DDL が lifespan に追加され、全 Pydantic モデルが models.py に定義されている。gems ルーターが main.py に登録されている。</done>
</task>

<task type="auto">
  <name>Task 2: Gem CRUD API エンドポイント実装</name>
  <files>app/api/routes/gems.py</files>
  <read_first>
    - app/api/routes/chat.py (get_jwt_payload パターン、psycopg 接続パターン)
    - app/api/routes/apps.py (FastAPI route パターン参照実装)
    - app/api/models.py (GemCreate, GemUpdate, GemInfo の定義 — Task 1 で追加済み)
    - .planning/phases/15-gem-canvas/15-CONTEXT.md (Gems API エンドポイント仕様)
  </read_first>
  <action>
**app/api/routes/gems.py を新規作成する。** 既存の apps.py / chat.py のパターンに従い、すべてのエンドポイントで `Depends(get_jwt_payload)` を使用する。

```python
"""Gem CRUD API routes — AI persona management (Phase 15).

Endpoints:
- POST   /api/gems              — Create a new Gem
- GET    /api/gems              — List user's Gems
- GET    /api/gems/{gem_id}     — Get a specific Gem
- PATCH  /api/gems/{gem_id}     — Update a Gem
- DELETE /api/gems/{gem_id}     — Delete a Gem
"""
import psycopg
from fastapi import APIRouter, Depends, HTTPException, Request
from psycopg.rows import dict_row

from app.api.models import GemCreate, GemInfo, GemUpdate
from app.api.routes.chat import get_jwt_payload

router = APIRouter(prefix="/api", tags=["gems"])
```

**POST /api/gems (create_gem):**
- Request body: `GemCreate(name, system_prompt, type)`
- `github_login = payload.get("github_login", "unknown")`
- INSERT INTO gems (github_login, name, system_prompt, type) VALUES (%s, %s, %s, %s) RETURNING gem_id, name, system_prompt, type, created_at, updated_at
- datetime を `.isoformat()` で文字列化して `GemInfo` で返す
- Status 201

**GET /api/gems (list_gems):**
- `response_model=list[GemInfo]`
- SELECT gem_id, name, system_prompt, type, created_at, updated_at FROM gems WHERE github_login = %s ORDER BY created_at DESC
- `row_factory=dict_row` を使い、datetime フィールドを `.isoformat()` で変換

**GET /api/gems/{gem_id} (get_gem):**
- SELECT ... FROM gems WHERE gem_id = %s::uuid AND github_login = %s
- 見つからない場合: `raise HTTPException(status_code=404, detail="Gem not found")`
- `%s::uuid` キャストで UUID 文字列を安全にバインド

**PATCH /api/gems/{gem_id} (update_gem):**
- Request body: `GemUpdate(name?, system_prompt?, type?)`
- `body.model_dump(exclude_unset=True)` で更新フィールドのみ取得
- 空なら 422 を返す
- UPDATE gems SET {field}=%s, ..., updated_at=now() WHERE gem_id = %s::uuid AND github_login = %s
- SET 句を動的に構築（`", ".join(f"{k} = %s" for k in fields)` + values リスト）
- RETURNING で更新後の行を返す
- 見つからない場合: 404

**DELETE /api/gems/{gem_id} (delete_gem):**
- DELETE FROM gems WHERE gem_id = %s::uuid AND github_login = %s
- rowcount == 0 なら 404
- Status 204 (`status_code=204`, return None)

**所有権チェック:** すべてのエンドポイントで WHERE github_login = %s を使い、他ユーザーの Gem へのアクセスを防ぐ。404 を返すことで Gem の存在自体を漏洩させない。

**datetime 変換パターン:**
```python
def _row_to_gem(row: dict) -> GemInfo:
    return GemInfo(
        gem_id=str(row["gem_id"]),
        name=row["name"],
        system_prompt=row["system_prompt"],
        type=row["type"],
        created_at=row["created_at"].isoformat() if row["created_at"] else "",
        updated_at=row["updated_at"].isoformat() if row["updated_at"] else "",
    )
```
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && python -c "from app.api.routes.gems import router; print(f'routes: {len(router.routes)}')"</automated>
  </verify>
  <acceptance_criteria>
    - app/api/routes/gems.py exists
    - app/api/routes/gems.py contains `router = APIRouter(prefix="/api", tags=["gems"])`
    - app/api/routes/gems.py contains `from app.api.routes.chat import get_jwt_payload`
    - app/api/routes/gems.py contains `async def create_gem`
    - app/api/routes/gems.py contains `async def list_gems`
    - app/api/routes/gems.py contains `async def get_gem`
    - app/api/routes/gems.py contains `async def update_gem`
    - app/api/routes/gems.py contains `async def delete_gem`
    - All routes use `Depends(get_jwt_payload)`
    - All queries include `WHERE ... github_login = %s` for ownership check
    - DELETE endpoint has `status_code=204`
    - `python -c "from app.api.routes.gems import router"` exits 0
  </acceptance_criteria>
  <done>5 つの Gem CRUD エンドポイントが実装され、JWT 認証と所有権チェックが全エンドポイントに適用されている。</done>
</task>

</tasks>

<threat_model>
  <threat id="T-15-01" severity="high">
    <description>他ユーザーの Gem へのアクセス（Elevation of Privilege）: GET/PATCH/DELETE で gem_id を推測して他ユーザーのデータにアクセスする</description>
    <mitigation>すべてのクエリで WHERE github_login = %s を使用し、JWT の github_login と照合する。gem_id のみでの検索は行わない。見つからない場合は 404 を返し、Gem の存在自体を漏洩させない。</mitigation>
  </threat>
  <threat id="T-15-02" severity="medium">
    <description>SQL インジェクション（Tampering）: Gem の name / system_prompt フィールドへの不正入力</description>
    <mitigation>psycopg のパラメータバインディング（%s プレースホルダ）を使用。文字列連結による SQL 構築は行わない。PATCH の動的 SET 句もカラム名はハードコード、値のみパラメータバインド。</mitigation>
  </threat>
  <threat id="T-15-03" severity="low">
    <description>大量 Gem 作成による DoS（Denial of Service）: 1 ユーザーが大量の Gem を作成して DB を圧迫する</description>
    <mitigation>accept — 200 名規模の社内ツールであり、リスクは低い。必要に応じて将来的に per-user Gem 数制限を追加可能。</mitigation>
  </threat>
</threat_model>

<verification>
- `python -c "from app.api.models import GemCreate, GemUpdate, GemInfo, CanvasAppInfo, CanvasDeployResponse"` が成功する
- `python -c "from app.api.routes.gems import router"` が成功する
- gems.py に 5 つのエンドポイント（POST, GET list, GET detail, PATCH, DELETE）が定義されている
- DDL が正しい順序（gems -> threads ALTER -> canvas_apps）で main.py に追加されている
</verification>

<success_criteria>
- PostgreSQL に gems / canvas_apps テーブルが作成される DDL が lifespan に追加されている
- threads テーブルに gem_id カラムが追加される ALTER が DDL に含まれている
- Gem CRUD の全 5 エンドポイントが JWT 認証 + 所有権チェック付きで動作する
- Pydantic モデル（GemCreate, GemUpdate, GemInfo, CanvasAppInfo, CanvasDeployResponse）が定義されている
</success_criteria>

<output>
After completion, create `.planning/phases/15-gem-canvas/15-01-SUMMARY.md`
</output>
