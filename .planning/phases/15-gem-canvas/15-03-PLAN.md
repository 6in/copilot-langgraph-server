---
phase: 15-gem-canvas
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - frontend/src/types.ts
  - frontend/src/api/client.ts
  - frontend/src/hooks/useGems.ts
  - frontend/src/hooks/useCanvas.ts
  - frontend/src/hooks/useThreads.ts
  - frontend/src/components/GemSelector.tsx
autonomous: true
requirements: [FE-01, FE-02]

must_haves:
  truths:
    - "GemInfo / GemCreate / CanvasAppInfo の TypeScript 型が定義されている"
    - "Gem API / Canvas API の fetch 関数が client.ts に追加されている"
    - "useGems hook で Gem の一覧取得・作成・削除ができる"
    - "GemSelector コンポーネントが実装され export されている"
    - "GemSelector で新しい Gem を作成できる（インラインフォーム）"
    - "useThreads の createThread が gem_id を受け取り POST /api/threads ボディで送信する"
  artifacts:
    - path: "frontend/src/types.ts"
      provides: "GemInfo, GemCreate, CanvasAppInfo, ChatRequest with gem_id TypeScript types"
    - path: "frontend/src/hooks/useGems.ts"
      provides: "Gem state management hook"
      exports: ["useGems"]
    - path: "frontend/src/hooks/useCanvas.ts"
      provides: "Canvas app state management hook"
      exports: ["useCanvas"]
    - path: "frontend/src/components/GemSelector.tsx"
      provides: "Gem selection chip strip UI"
      exports: ["GemSelector"]
  key_links:
    - from: "frontend/src/hooks/useGems.ts"
      to: "frontend/src/api/client.ts"
      via: "import listGems, createGem, deleteGem"
      pattern: "import.*from.*client"
    - from: "frontend/src/components/GemSelector.tsx"
      to: "frontend/src/hooks/useGems.ts"
      via: "import useGems"
      pattern: "import.*useGems"
    - from: "frontend/src/hooks/useThreads.ts"
      to: "frontend/src/api/client.ts"
      via: "createThread with gem_id"
      pattern: "gem_id"
---

<objective>
フロントエンドの基盤を構築する。TypeScript 型定義、API クライアント関数、Gem / Canvas の state management hooks、GemSelector コンポーネントを実装する。さらに useThreads の createThread に gem_id パラメータを追加し、POST /api/threads ボディで送信できるようにする。

Purpose: Gem 管理 UI の完成と Canvas 統合（Plan 04）の前提条件となる型・hooks・API 関数を提供する。
Output: types.ts 拡張、client.ts 拡張、useGems.ts、useCanvas.ts、useThreads.ts 修正、GemSelector.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-gem-canvas/15-CONTEXT.md
@.planning/phases/15-gem-canvas/15-UI-SPEC.md

<interfaces>
<!-- 既存のフロントエンドパターン -->

From frontend/src/types.ts (existing pattern):
```typescript
export interface ThreadInfo {
  thread_id: string;
  updated_at?: string | null;
  label: string;
  app_id?: string;
}

export interface ChatRequest {
  message: string;
  thread_id: string;
  model: string;
  task_type?: string;
  mode?: 'simple' | 'super';
  agents?: string[];
  app_id?: string;
}
```

From frontend/src/api/client.ts (existing pattern):
```typescript
const API_BASE = (import.meta.env.VITE_APP_BASE ?? '').replace(/\/$/, '');

async function apiFetch<T>(path: string, init?: RequestInit): Promise<T> {
  const resp = await fetch(path, { credentials: 'include', ...init });
  if (!resp.ok) throw new Error(`API error ${resp.status} on ${path}`);
  return resp.json() as Promise<T>;
}

export const createThread = () =>
  apiFetch<{ thread_id: string; label: string }>(`${API_BASE}/api/threads`, { method: 'POST' });

export const deleteThread = async (threadId: string): Promise<void> => {
  const resp = await fetch(`${API_BASE}/api/threads/...`, { method: 'DELETE', credentials: 'include' });
  if (resp.status !== 204 && !resp.ok) throw new Error(...);
};
```

From frontend/src/hooks/useThreads.ts (existing hook pattern):
```typescript
export function useThreads(appId?: 'chat' | 'superchat'): UseThreadsReturn {
  const [threads, setThreads] = useState<ThreadInfo[]>([]);
  const refreshThreads = useCallback(async () => {
    const data = await listThreads(appId);
    setThreads(data);
  }, [appId]);
  useEffect(() => { refreshThreads(); }, [refreshThreads]);

  const createNewThread = useCallback(async (): Promise<string> => {
    const { thread_id } = await createThread();
    setActiveThreadId(thread_id);
    setMessages([]);
    return thread_id;
  }, []);
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript 型定義 + API クライアント関数 + hooks + useThreads gem_id 対応</name>
  <files>frontend/src/types.ts, frontend/src/api/client.ts, frontend/src/hooks/useGems.ts, frontend/src/hooks/useCanvas.ts, frontend/src/hooks/useThreads.ts</files>
  <read_first>
    - frontend/src/types.ts (既存の型定義パターン)
    - frontend/src/api/client.ts (apiFetch パターン、API_BASE 使用法、createThread 関数)
    - frontend/src/hooks/useThreads.ts (既存 hook パターン — useState, useCallback, useEffect、createNewThread 関数)
    - .planning/phases/15-gem-canvas/15-UI-SPEC.md (Component Inventory: types.ts セクション)
    - .planning/phases/15-gem-canvas/15-CONTEXT.md (Pydantic モデル定義 — TypeScript 型の対応)
  </read_first>
  <action>
**frontend/src/types.ts に以下の型を追加する（ファイル末尾）:**

```typescript
// --- Phase 15: Gem + Canvas types ---

export interface GemInfo {
  gem_id: string;
  name: string;
  system_prompt: string;
  type: 'default' | 'canvas';
  created_at: string;
  updated_at: string;
}

export interface GemCreate {
  name: string;
  system_prompt: string;
  type: 'default' | 'canvas';
}

export interface CanvasAppInfo {
  app_id: string;
  thread_id: string | null;
  name: string;
  html: string;
  source: 'canvas' | 'upload' | 'builtin';
  deployed: boolean;
  deployed_at: string | null;
  created_at: string;
}

export interface CanvasDeployResponse {
  url: string;
}

export interface CanvasResult {
  type: 'canvas';
  app_id: string;
  html: string;
}
```

**ChatRequest に gem_id フィールドを追加する（BLOCKER 1 対応 — CONTEXT.md ロック決定）:**

```typescript
export interface ChatRequest {
  message: string;
  thread_id: string;
  model: string;
  task_type?: string;
  mode?: 'simple' | 'super';
  agents?: string[];
  app_id?: string;
  gem_id?: string | null;  // Phase 15: Gem association for thread creation
}
```

**frontend/src/api/client.ts に以下の関数を追加する（ファイル末尾）:**

```typescript
// --- Phase 15: Gems API ---

export const listGems = () =>
  apiFetch<GemInfo[]>(`${API_BASE}/api/gems`);

export const createGemApi = (data: GemCreate) =>
  apiFetch<GemInfo>(`${API_BASE}/api/gems`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(data),
  });

export const updateGemApi = (gemId: string, data: Partial<GemCreate>) =>
  apiFetch<GemInfo>(`${API_BASE}/api/gems/${encodeURIComponent(gemId)}`, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(data),
  });

export const deleteGemApi = async (gemId: string): Promise<void> => {
  const resp = await fetch(`${API_BASE}/api/gems/${encodeURIComponent(gemId)}`, {
    method: 'DELETE',
    credentials: 'include',
  });
  if (resp.status !== 204 && !resp.ok) {
    throw new Error(`Delete gem failed: ${resp.status}`);
  }
};

// --- Phase 15: Canvas API ---

export const getCanvasApp = (appId: string) =>
  apiFetch<CanvasAppInfo>(`${API_BASE}/api/canvas/apps/${encodeURIComponent(appId)}`);

export const getCanvasAppByThread = (threadId: string) =>
  apiFetch<CanvasAppInfo[]>(`${API_BASE}/api/canvas/apps?thread_id=${encodeURIComponent(threadId)}`);

export const updateCanvasApp = (appId: string, html: string, name?: string) =>
  apiFetch<CanvasAppInfo>(`${API_BASE}/api/canvas/apps/${encodeURIComponent(appId)}`, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ html, ...(name ? { name } : {}) }),
  });

export const deployCanvasApp = (appId: string) =>
  apiFetch<CanvasDeployResponse>(`${API_BASE}/api/canvas/apps/${encodeURIComponent(appId)}/deploy`, {
    method: 'POST',
  });
```

import 行に `GemInfo, GemCreate, CanvasAppInfo, CanvasDeployResponse` を追加する。

**frontend/src/api/client.ts の createThread 関数を修正して gem_id を受け取るようにする（BLOCKER 1 対応）:**

現在:
```typescript
export const createThread = () =>
  apiFetch<{ thread_id: string; label: string }>(`${API_BASE}/api/threads`, { method: 'POST' });
```

修正後:
```typescript
export const createThread = (gemId?: string | null) =>
  apiFetch<{ thread_id: string; label: string }>(`${API_BASE}/api/threads`, {
    method: 'POST',
    headers: gemId ? { 'Content-Type': 'application/json' } : undefined,
    body: gemId ? JSON.stringify({ gem_id: gemId }) : undefined,
  });
```

**frontend/src/hooks/useThreads.ts の createNewThread を修正して gem_id を受け取るようにする（BLOCKER 1 対応）:**

現在:
```typescript
const createNewThread = useCallback(async (): Promise<string> => {
  const { thread_id } = await createThread();
  setActiveThreadId(thread_id);
  setMessages([]);
  return thread_id;
}, []);
```

修正後:
```typescript
const createNewThread = useCallback(async (gemId?: string | null): Promise<string> => {
  const { thread_id } = await createThread(gemId);
  setActiveThreadId(thread_id);
  setMessages([]);
  return thread_id;
}, []);
```

UseThreadsReturn の型定義も更新する:
```typescript
createNewThread: (gemId?: string | null) => Promise<string>;
```

**frontend/src/hooks/useGems.ts を新規作成する:**

```typescript
// frontend/src/hooks/useGems.ts
// Gem CRUD state management hook.

import { useCallback, useEffect, useState } from 'react';
import { listGems, createGemApi, deleteGemApi } from '../api/client';
import type { GemInfo, GemCreate } from '../types';

interface UseGemsReturn {
  gems: GemInfo[];
  isLoading: boolean;
  error: string | null;
  createGem: (data: GemCreate) => Promise<GemInfo>;
  deleteGem: (gemId: string) => Promise<void>;
  refreshGems: () => Promise<void>;
}

export function useGems(): UseGemsReturn {
  const [gems, setGems] = useState<GemInfo[]>([]);
  const [isLoading, setIsLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  const refreshGems = useCallback(async () => {
    try {
      setIsLoading(true);
      setError(null);
      const data = await listGems();
      setGems(data);
    } catch (e) {
      setError('Could not load Gems. Please refresh.');
    } finally {
      setIsLoading(false);
    }
  }, []);

  useEffect(() => {
    refreshGems();
  }, [refreshGems]);

  const createGem = useCallback(async (data: GemCreate): Promise<GemInfo> => {
    const gem = await createGemApi(data);
    setGems((prev) => [gem, ...prev]);
    return gem;
  }, []);

  const deleteGem = useCallback(async (gemId: string): Promise<void> => {
    await deleteGemApi(gemId);
    setGems((prev) => prev.filter((g) => g.gem_id !== gemId));
  }, []);

  return { gems, isLoading, error, createGem, deleteGem, refreshGems };
}
```

**frontend/src/hooks/useCanvas.ts を新規作成する:**

```typescript
// frontend/src/hooks/useCanvas.ts
// Canvas app state management hook.

import { useCallback, useState } from 'react';
import { updateCanvasApp, deployCanvasApp } from '../api/client';
import type { CanvasAppInfo } from '../types';

interface UseCanvasReturn {
  canvasApp: CanvasAppInfo | null;
  setCanvasApp: (app: CanvasAppInfo | null) => void;
  isSaving: boolean;
  isDeploying: boolean;
  deployUrl: string | null;
  deployError: string | null;
  saveCanvas: (appId: string, html: string) => Promise<void>;
  deployCanvas: (appId: string) => Promise<string>;
  dismissCanvas: () => void;
}

export function useCanvas(): UseCanvasReturn {
  const [canvasApp, setCanvasApp] = useState<CanvasAppInfo | null>(null);
  const [isSaving, setIsSaving] = useState(false);
  const [isDeploying, setIsDeploying] = useState(false);
  const [deployUrl, setDeployUrl] = useState<string | null>(null);
  const [deployError, setDeployError] = useState<string | null>(null);

  const saveCanvas = useCallback(async (appId: string, html: string) => {
    setIsSaving(true);
    try {
      const updated = await updateCanvasApp(appId, html);
      setCanvasApp(updated);
    } finally {
      setIsSaving(false);
    }
  }, []);

  const deployCanvas = useCallback(async (appId: string): Promise<string> => {
    setIsDeploying(true);
    setDeployError(null);
    try {
      const res = await deployCanvasApp(appId);
      setDeployUrl(res.url);
      if (canvasApp) {
        setCanvasApp({ ...canvasApp, deployed: true, deployed_at: new Date().toISOString() });
      }
      return res.url;
    } catch {
      setDeployError('Deploy failed. Please try again.');
      throw new Error('Deploy failed');
    } finally {
      setIsDeploying(false);
    }
  }, [canvasApp]);

  const dismissCanvas = useCallback(() => {
    setCanvasApp(null);
    setDeployUrl(null);
    setDeployError(null);
  }, []);

  return { canvasApp, setCanvasApp, isSaving, isDeploying, deployUrl, deployError, saveCanvas, deployCanvas, dismissCanvas };
}
```
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph/frontend && grep -c "GemInfo" src/types.ts && grep -c "CanvasAppInfo" src/types.ts && grep -c "gem_id" src/types.ts && grep -c "listGems" src/api/client.ts && grep -c "updateGemApi" src/api/client.ts && grep -c "deployCanvasApp" src/api/client.ts && grep -c "gemId" src/api/client.ts && grep -c "gemId" src/hooks/useThreads.ts && test -f src/hooks/useGems.ts && test -f src/hooks/useCanvas.ts && echo "all files OK"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/types.ts contains `interface GemInfo`
    - frontend/src/types.ts contains `interface GemCreate`
    - frontend/src/types.ts contains `interface CanvasAppInfo`
    - frontend/src/types.ts contains `interface CanvasDeployResponse`
    - frontend/src/types.ts contains `interface CanvasResult`
    - frontend/src/types.ts ChatRequest contains `gem_id?: string | null`
    - frontend/src/api/client.ts contains `export const listGems`
    - frontend/src/api/client.ts contains `export const createGemApi`
    - frontend/src/api/client.ts contains `export const updateGemApi` (W-1 fix)
    - frontend/src/api/client.ts contains `export const deleteGemApi`
    - frontend/src/api/client.ts contains `export const getCanvasApp`
    - frontend/src/api/client.ts contains `export const updateCanvasApp`
    - frontend/src/api/client.ts contains `export const deployCanvasApp`
    - frontend/src/api/client.ts createThread accepts `gemId` parameter
    - frontend/src/api/client.ts createThread sends `gem_id` in body when gemId provided
    - frontend/src/hooks/useThreads.ts createNewThread accepts `gemId?: string | null` parameter
    - frontend/src/hooks/useThreads.ts createNewThread passes gemId to createThread
    - frontend/src/hooks/useGems.ts exists and contains `export function useGems`
    - frontend/src/hooks/useCanvas.ts exists and contains `export function useCanvas`
  </acceptance_criteria>
  <done>Gem / Canvas の TypeScript 型、API クライアント関数（updateGemApi 含む）、state management hooks が完成し、useThreads の createThread / createNewThread が gem_id を受け取って送信する。</done>
</task>

<task type="auto">
  <name>Task 2: GemSelector コンポーネント実装</name>
  <files>frontend/src/components/GemSelector.tsx</files>
  <read_first>
    - frontend/src/components/MessageArea.tsx (GemSelector の挿入先 — 入力バーの上に配置)
    - frontend/src/hooks/useGems.ts (Task 1 で作成 — useGems の返り値)
    - frontend/src/theme.css (既存のカラー変数 — CSS custom properties)
    - .planning/phases/15-gem-canvas/15-UI-SPEC.md (GemSelector の完全な仕様: チップ、フォーム、色、スペーシング)
  </read_first>
  <action>
**frontend/src/components/GemSelector.tsx を新規作成する。** UI-SPEC に完全準拠した実装。

**Props:**
```typescript
interface GemSelectorProps {
  selectedGemId: string | null;
  onSelectGem: (gemId: string | null) => void;
}
```

**コンポーネント構造:**
1. 水平スクロール可能なチップストリップ: `display: flex`, `overflowX: 'auto'`, `gap: '8px'`, `padding: '4px 12px'`
2. 各チップは `<button>` 要素: `borderRadius: '14px'`, `height: '28px'`, `padding: '0 12px'`, `fontSize: '0.875rem'`, `fontWeight: 600`
3. "No Gem" チップ: 常に先頭。`aria-pressed={selectedGemId === null}`
4. Gem チップ: 名前を 20 文字で truncate（ellipsis）。`aria-pressed={selectedGemId === gem.gem_id}`
5. `+` チップ: 常に末尾。フォーム表示のトグル

**選択状態のスタイル:**
- 非選択: `border: '1px solid #d1dbe3'`, `background: 'transparent'`
- 選択: `border: '2px solid #7c6ff7'`, `background: 'rgba(124,111,247,0.12)'`

**削除ボタン:**
- チップの hover 時のみ表示（`opacity: 0` → `opacity: 1`）
- `aria-label="Gem を削除"` (UI-SPEC Accessibility Contract)
- クリックで inline confirm: "Delete?" + "Yes" / "No" テキストリンク（モーダルなし）
- `<span>` で表示、keyboard accessible（Tab で Yes/No に移動可能）

**インラインフォーム:**
- `+` チップクリックで `showForm` state を toggle
- フォーム: `display: showForm ? 'block' : 'none'`（アニメーションなし）
- Name: `<input type="text" maxLength={80}>` with `<label htmlFor="gem-name">Name</label>`
- System Prompt: `<textarea rows={4} maxLength={4000}>` with `<label htmlFor="gem-prompt">System Prompt</label>`
- Type: radio — `<label><input type="radio" value="default" checked={type==='default'}> Chat</label>` / `<label><input type="radio" value="canvas"> Canvas</label>`
- "Create Gem" ボタン: `background: '#7c6ff7'`, `color: 'white'`, `borderRadius: '6px'`, `height: '36px'`
- "Cancel" ボタン: `background: 'none'`, `border: '1px solid #d1dbe3'`, `height: '36px'`
- フォームの gap: `16px`（垂直スタック）

**空の状態:** Gem が 0 個で `+` チップのみの場合、チップストリップの下に muted テキスト: "No Gems yet. Click + to create one."

**エラー状態:** `useGems().error` が非 null の場合、`role="alert"` で "Could not load Gems. Please refresh." を表示

**フォーカスリング:** すべての `<button>` に `outline: '2px solid #7c6ff7'`, `outlineOffset: '2px'` の `:focus-visible` スタイル

**アクセシビリティ:**
- チップ `<button>` に `aria-pressed`
- フォームの input/textarea に `<label>` で htmlFor 接続
- エラー表示に `role="alert"`
- 削除ボタンに `aria-label="Gem を削除"`
- すべてのインタラクティブ要素の最小高さ 44px（タッチターゲット）— チップは 28px だが、親の padding で実効タッチ領域を確保

**useGems hook を内部で使用:** props では gem リストを受け取らない。コンポーネント内で `useGems()` を呼ぶ。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph/frontend && grep -c "GemSelector" src/components/GemSelector.tsx && grep -c "aria-pressed" src/components/GemSelector.tsx && grep -c "useGems" src/components/GemSelector.tsx && grep -c '#7c6ff7' src/components/GemSelector.tsx && echo "GemSelector OK"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/GemSelector.tsx exists
    - GemSelector.tsx contains `export function GemSelector` or `export const GemSelector`
    - GemSelector.tsx contains `useGems()` call
    - GemSelector.tsx contains `aria-pressed` on chip buttons
    - GemSelector.tsx contains `aria-label` with "Gem を削除" on delete button
    - GemSelector.tsx contains accent color `#7c6ff7`
    - GemSelector.tsx contains border color `#d1dbe3`
    - GemSelector.tsx contains `maxLength={80}` on name input
    - GemSelector.tsx contains `maxLength={4000}` on system prompt textarea
    - GemSelector.tsx contains `role="alert"` for error display
    - GemSelector.tsx contains "No Gem" text
    - GemSelector.tsx contains "Create Gem" text
    - GemSelector.tsx contains radio inputs for "default" and "canvas" type
  </acceptance_criteria>
  <done>GemSelector コンポーネントが UI-SPEC に準拠して実装され、チップ選択・インラインフォーム作成・インライン削除確認が動作する。</done>
</task>

</tasks>

<threat_model>
  <threat id="T-15-07" severity="low">
    <description>フロントエンドの XSS（Spoofing）: Gem 名やシステムプロンプトに HTML/JS が含まれる場合</description>
    <mitigation>React の JSX は自動的にテキストをエスケープする（dangerouslySetInnerHTML を使用しない限り）。Gem 名はチップのテキストコンテンツとして表示されるため、XSS リスクなし。</mitigation>
  </threat>
</threat_model>

<verification>
- frontend/src/types.ts に GemInfo, GemCreate, CanvasAppInfo, CanvasDeployResponse, CanvasResult が定義されている
- frontend/src/types.ts の ChatRequest に gem_id フィールドが追加されている
- frontend/src/api/client.ts に Gem / Canvas API 関数（updateGemApi 含む）が追加されている
- frontend/src/api/client.ts の createThread が gemId パラメータを受け取る
- frontend/src/hooks/useThreads.ts の createNewThread が gemId を受け取り createThread に渡す
- frontend/src/hooks/useGems.ts が useGems hook を export している
- frontend/src/hooks/useCanvas.ts が useCanvas hook を export している
- frontend/src/components/GemSelector.tsx が UI-SPEC のアクセシビリティ要件を満たしている
</verification>

<success_criteria>
- TypeScript コンパイルエラーなし（型定義が一貫している）
- useGems hook で Gem の一覧取得・作成・削除の state management ができる
- useCanvas hook で Canvas アプリの保存・デプロイの state management ができる
- GemSelector で Gem チップの選択・作成・削除が UI-SPEC に準拠して動作する
- createThread / createNewThread が gem_id を POST /api/threads ボディで送信する
</success_criteria>

<output>
After completion, create `.planning/phases/15-gem-canvas/15-03-SUMMARY.md`
</output>
