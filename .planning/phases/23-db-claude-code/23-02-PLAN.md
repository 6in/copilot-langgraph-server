---
phase: 23
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - mcp_server/tools/claude_code.py
  - mcp_server/tools/stubs.py
  - mcp_server/server.py
  - docker-compose.yml
  - tests/test_mcp_server.py
autonomous: true
requirements: [CODE-01, CODE-02, CODE-03]
must_haves:
  truths:
    - "エージェントが claude_code ツールを呼び出すと claude CLI が実行され stdout を含む構造化レスポンスが返る"
    - "CLAUDECODE=1 / ANTHROPIC_API_KEY / DATABASE_URL 等の環境変数が claude サブプロセスに継承されない"
    - "60 秒経過すると SIGTERM、5 秒猶予後 SIGKILL でプロセスが強制終了し、ゾンビが残らない"
    - "stdout が 4000 文字を超える場合は切り捨て、フルテキストは shared volume に書き出され file_path で参照可能"
    - "claude CLI が mcp-server コンテナ内で `claude --version` 実行可能"
  artifacts:
    - path: "mcp_server/tools/claude_code.py"
      provides: "claude_code ツール本番実装 + env sanitization + timeout escalation + output truncation"
      min_lines: 80
    - path: "tests/test_mcp_server.py"
      provides: "CODE-01/02/03 ユニットテスト（subprocess mock）"
      contains: "test_claude_code"
  key_links:
    - from: "mcp_server/server.py"
      to: "mcp_server/tools/claude_code.py"
      via: "register_tools(mcp) import"
      pattern: "from tools.claude_code import register_tools"
    - from: "mcp_server/tools/claude_code.py"
      to: "asyncio.create_subprocess_exec"
      via: "env=sanitized_env, cwd=cwd"
      pattern: "create_subprocess_exec.*env="
    - from: "docker-compose.yml mcp-server"
      to: "claude-code-outputs named volume"
      via: "/shared/claude-code-outputs"
      pattern: "claude-code-outputs"
---

<objective>
Phase 23 Plan 02: claude_code MCP ツール本番実装。

`mcp_server/tools/stubs.py` の `claude_code_stub` を、Claude Code CLI をサブプロセスとして実行する本番ツールに差し替える。環境変数を許可リスト方式でサニタイズし（CODE-02）、60 秒タイムアウト + SIGTERM→SIGKILL エスカレーションで zombie プロセスを防ぐ（CODE-03）。出力は 4000 文字で切り捨て、超過分は shared volume に書き出す（D-06）。

Purpose: エージェントが Claude Code CLI に対してコード生成・編集タスクを委譲できる。安全な環境分離（許可リスト env）と確実なリソース管理（タイムアウトエスカレーション）により、CLAUDECODE=1 再帰実行や zombie プロセス問題を回避する。

Output: `mcp_server/tools/claude_code.py`（新規）、`docker-compose.yml`（claude-code-outputs named volume + Dockerfile or 既存イメージへの claude CLI 追加）、`tests/test_mcp_server.py`（テスト追加）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-db-claude-code/23-CONTEXT.md
@.planning/phases/23-db-claude-code/23-RESEARCH.md
@.planning/phases/23-db-claude-code/23-VALIDATION.md
@.planning/phases/23-db-claude-code/23-01-PLAN.md
@mcp_server/server.py
@mcp_server/tools/web_search.py
@mcp_server/tools/stubs.py
@docker-compose.yml
@tests/test_mcp_server.py

<interfaces>
<!-- Plan 01 で更新済みの server.py を前提とする -->

From mcp_server/server.py（Plan 01 完了後の状態）:
```python
@asynccontextmanager
async def lifespan(server):
    config_path = os.environ.get("DB_POOLS_CONFIG", "/mcp_server/config/db_pools.yaml")
    await init_pools(config_path)
    try:
        yield
    finally:
        await close_pools()

mcp = FastMCP("copilot-mcp-server", lifespan=lifespan)
register_stub_tools(mcp)         # ping のみ残っている
register_web_search_tools(mcp)
register_db_query_tools(mcp)     # Plan 01 で追加済み
```
→ Plan 02 では `register_claude_code_tools(mcp)` を追加し、stubs.py から `claude_code_stub` を削除する。

23-RESEARCH.md L443-505 「claude_code ツール全体像」を実装の出発点とする（VERIFIED locally）。

claude CLI 想定:
- 実行コマンド: `claude --print <prompt>`
- ホスト環境: `/home/parallels/.local/bin/claude` v2.1.104 [VERIFIED]
- mcp-server コンテナ内: **未確認 [ASSUMED A1]** — Wave 0 で確認必須

EXPECTED_TOOLS の最終形（Plan 02 完了後）:
```python
EXPECTED_TOOLS = {"ping", "web_search", "db_query", "claude_code"}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: claude_code ツール実装 + env sanitization + timeout escalation</name>
  <files>
    mcp_server/tools/claude_code.py,
    mcp_server/tools/stubs.py,
    tests/test_mcp_server.py
  </files>
  <behavior>
    - **CODE-01:** `claude_code(prompt="hello", cwd="/tmp")` を呼ぶと `asyncio.create_subprocess_exec("claude", "--print", "hello", cwd="/tmp", ...)` が起動され、戻り値は `{"output": str, "exit_code": int, "truncated": bool, "file_path": None}` 形式
    - **CODE-02:** mock した `os.environ` に `CLAUDECODE=1`, `ANTHROPIC_API_KEY=secret`, `DATABASE_URL=...` を入れた状態で claude_code を呼び、`create_subprocess_exec` に渡された `env` 引数に **`PATH`, `HOME`, `LANG`, `LC_ALL`, `TERM` のみ** が含まれることを assert
    - **CODE-03 (timeout):** subprocess を mock して `communicate()` が `asyncio.TimeoutError` を発生させると、`proc.terminate()` が呼ばれ、その後 `proc.wait()` が成功すれば SIGKILL は呼ばれない
    - **CODE-03 (kill escalation):** subprocess を mock して `communicate()` も `wait()` も TimeoutError を発生させると、`proc.kill()` が呼ばれ、最後の `await proc.wait()` で zombie 回収される。戻り値は `{"output": "", "exit_code": -1, "truncated": False, "file_path": None, "error": "Timeout after 60s"}`
    - **D-06 truncation:** stdout が 4000 文字超のとき、`output` は 4000 文字で切られ `truncated=True`、`file_path` は `/shared/claude-code-outputs/{ts}_{uuid}.txt` 形式の文字列
    - stubs.py から `claude_code_stub` 関数が削除されている（`ping` のみ残る）
  </behavior>
  <action>
    1. **`mcp_server/tools/claude_code.py` 新規作成**（D-04 〜 D-12 per CONTEXT.md）。
       23-RESEARCH.md L443-505 のサンプルコードを実装の出発点とし、以下を含める:
       - `ALLOWED_ENV_KEYS = frozenset({"PATH", "HOME", "LANG", "LC_ALL", "TERM"})` （D-08）
       - `TIMEOUT_SECS = 60` （D-10, CODE-03）
       - `SIGTERM_GRACE_SECS = 5` （D-11）
       - `MAX_INLINE_CHARS = 4000` （D-06）
       - `OUTPUT_DIR = os.environ.get("CLAUDE_CODE_OUTPUT_DIR", "/shared/claude-code-outputs")` （D-07）
       - `_save_overflow_output(output: str) -> str` ヘルパー — `os.makedirs(OUTPUT_DIR, exist_ok=True)` してから `{ts}_{uuid8}.txt` で書き出し、フルパスを返す
       - `register_tools(mcp)`:
         ```python
         @mcp.tool
         async def claude_code(prompt: str, cwd: str = "/tmp") -> dict:
             """Claude Code CLI をサブプロセスとして実行する（CODE-01〜03）。

             Args:
                 prompt: claude --print に渡すプロンプト文字列
                 cwd: claude を実行する作業ディレクトリ（デフォルト /tmp）

             Returns:
                 {"output": str, "exit_code": int, "truncated": bool, "file_path": str | None}
                 エラー時は "error" キーを追加
             """
             # D-08, D-09: 許可リスト env サニタイズ
             sanitized_env = {k: v for k, v in os.environ.items() if k in ALLOWED_ENV_KEYS}
             try:
                 proc = await asyncio.create_subprocess_exec(
                     "claude", "--print", prompt,
                     stdout=asyncio.subprocess.PIPE,
                     stderr=asyncio.subprocess.PIPE,
                     cwd=cwd,
                     env=sanitized_env,
                 )
             except FileNotFoundError:
                 return {"output": "", "exit_code": -1, "truncated": False,
                         "file_path": None, "error": "claude CLI not found in PATH"}
             except Exception as e:
                 return {"output": "", "exit_code": -1, "truncated": False,
                         "file_path": None, "error": f"claude_code spawn failed: {e}"}

             try:
                 stdout, stderr = await asyncio.wait_for(
                     proc.communicate(), timeout=TIMEOUT_SECS
                 )
                 exit_code = proc.returncode or 0
                 output = stdout.decode(errors="replace")
             except asyncio.TimeoutError:
                 # D-11: SIGTERM → grace → SIGKILL escalation
                 proc.terminate()
                 try:
                     await asyncio.wait_for(proc.wait(), timeout=SIGTERM_GRACE_SECS)
                 except asyncio.TimeoutError:
                     proc.kill()
                     await proc.wait()
                 return {"output": "", "exit_code": -1, "truncated": False,
                         "file_path": None, "error": "Timeout after 60s"}

             # D-06: 4000 文字切り捨て + shared volume 書き出し
             if len(output) <= MAX_INLINE_CHARS:
                 return {"output": output, "exit_code": exit_code,
                         "truncated": False, "file_path": None}
             file_path = _save_overflow_output(output)
             return {"output": output[:MAX_INLINE_CHARS], "exit_code": exit_code,
                     "truncated": True, "file_path": file_path}
         ```

    2. **`mcp_server/tools/stubs.py` 編集** — `claude_code_stub` 関数を削除。`ping` のみ残す。docstring を「Phase 23 完了 — db_query, claude_code は本番実装に差し替え済み」に更新。

    3. **`tests/test_mcp_server.py` 編集** — 以下のテストを追加（subprocess mock ベース、CI 環境で claude CLI 不要）:
       - `test_claude_code_env_sanitized` — `unittest.mock.patch.dict(os.environ, {"CLAUDECODE": "1", "ANTHROPIC_API_KEY": "secret", "DATABASE_URL": "x"})` した上で、`asyncio.create_subprocess_exec` を `AsyncMock` で patch して claude_code を呼ぶ。`create_subprocess_exec` に渡された `env` kwarg を取り出し、`set(env.keys()).issubset({"PATH", "HOME", "LANG", "LC_ALL", "TERM"})` と `"CLAUDECODE" not in env` を assert
       - `test_claude_code_returns_output` — `create_subprocess_exec` を mock して `communicate()` が `(b"hello world", b"")`、`returncode=0` を返すように設定。`claude_code("test")` の戻り値が `{"output": "hello world", "exit_code": 0, "truncated": False, "file_path": None}` であることを assert
       - `test_claude_code_timeout_terminate` — `communicate()` が `asyncio.TimeoutError` を raise、`wait()` は成功するように mock。`proc.terminate` が呼ばれ、`proc.kill` は呼ばれないことを assert。戻り値の `error` が `"Timeout after 60s"` を含むこと
       - `test_claude_code_timeout_kill_escalation` — `communicate()` も `wait()` も `asyncio.TimeoutError`、`proc.kill` が呼ばれた後の最後の `wait()` は成功するように mock。`proc.kill` が呼ばれることを assert
       - `test_claude_code_truncation` — `communicate()` が 5000 文字の bytes を返すように mock し、`_save_overflow_output` を patch（実 disk write 防止）。戻り値が `truncated=True`、`output` が 4000 文字、`file_path` が patch 戻り値であることを assert
       - `test_claude_code_cli_missing` — `create_subprocess_exec` が `FileNotFoundError` を raise する mock。戻り値の `error` が `"claude CLI not found in PATH"` を含むこと
       - `test_stub_tools_registered` の `EXPECTED_TOOLS` を更新: `{"ping", "web_search", "db_query", "claude_code"}` （`claude_code_stub` を削除し `claude_code` に置き換え）

    Avoid:
    - `subprocess.run`（sync）を使わない — async 必須（Pitfall 4）
    - `env=None` でプロセス起動しない — Pitfall 3、CLAUDECODE 漏洩
    - `subprocess.PIPE` に大量出力を貯めない — `communicate()` でまとめて読むのは OK だが `proc.stdout.read()` で blocking しない
    - SIGKILL を直接送らない — 必ず terminate → grace → kill のエスカレーション
    - `proc.kill()` 後に `await proc.wait()` を忘れない — zombie 回収必須
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && docker compose exec -T app uv run pytest tests/test_mcp_server.py -k "claude_code" -x -q</automated>
  </verify>
  <done>
    - `mcp_server/tools/claude_code.py` が存在し、`register_tools` をエクスポート
    - `mcp_server/tools/stubs.py` から `claude_code_stub` 削除（`ping` のみ残る）
    - 6 つの新規テスト（env_sanitized, returns_output, timeout_terminate, timeout_kill_escalation, truncation, cli_missing）が全て green
    - `EXPECTED_TOOLS = {"ping", "web_search", "db_query", "claude_code"}` で `test_stub_tools_registered` が green
    - CODE-01, CODE-02, CODE-03 の単体検証が完了
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: server.py 統合 + docker-compose volume + claude CLI 確認 + e2e smoke</name>
  <files>
    mcp_server/server.py,
    docker-compose.yml,
    tests/test_mcp_server.py
  </files>
  <behavior>
    - mcp-server コンテナで `claude --version` が成功する（v2.x 系のバージョン文字列が返る）
    - mcp-server コンテナ内で `/shared/claude-code-outputs/` ディレクトリが書き込み可能
    - tests/test_mcp_server.py の `EXPECTED_TOOLS` 更新後の `test_stub_tools_registered` が green
    - e2e smoke: `Client(mcp)` 経由で `claude_code(prompt="echo hello", cwd="/tmp")` を呼び、`exit_code` が int で `output` が str を含む構造化レスポンスが返る
  </behavior>
  <action>
    1. **`mcp_server/server.py` 編集** — claude_code register_tools を import + 呼び出し追加:
       ```python
       from tools.claude_code import register_tools as register_claude_code_tools
       ...
       register_claude_code_tools(mcp)
       ```

    2. **`docker-compose.yml` 編集** — mcp-server サービスに以下を追加（D-07, Pitfall 6）:
       ```yaml
       mcp-server:
         ...
         volumes:
           - ./mcp_server:/mcp_server
           - ./config:/mcp_server/config:ro
           - claude-code-outputs:/shared/claude-code-outputs   # ← Phase 23 Plan 02 追加
         environment:
           - TAVILY_API_KEY=${TAVILY_API_KEY}
           - DB_POOLS_CONFIG=/mcp_server/config/db_pools.yaml
           - CLAUDE_CODE_OUTPUT_DIR=/shared/claude-code-outputs   # ← 追加
       ```
       worker サービスにも同じ named volume をマウント（worker からも file_path を読めるように）:
       ```yaml
       worker:
         volumes:
           ...
           - claude-code-outputs:/shared/claude-code-outputs:ro   # ← 追加（read-only）
       ```
       `volumes:` セクションに追加:
       ```yaml
       volumes:
         redis-data:
         postgres-data:
         claude-code-outputs:   # ← Phase 23 Plan 02 追加
       ```

    3. **claude CLI を mcp-server コンテナで利用可能にする** — Assumption A1 解消（23-RESEARCH.md L633）:
       - **Step 3a:** まず `docker compose up -d mcp-server && docker compose exec mcp-server which claude` で確認
       - **Step 3b (claude が無い場合):** `mcp_server/Dockerfile` を新規作成（uv ベースイメージ + Node.js + claude CLI）。ベースは現行と同じ `ghcr.io/astral-sh/uv:python3.12-bookworm-slim`:
         ```dockerfile
         FROM ghcr.io/astral-sh/uv:python3.12-bookworm-slim

         # Install Node.js 20 (required by Claude Code CLI)
         RUN apt-get update && \
             apt-get install -y --no-install-recommends curl ca-certificates && \
             curl -fsSL https://deb.nodesource.com/setup_20.x | bash - && \
             apt-get install -y nodejs && \
             rm -rf /var/lib/apt/lists/*

         # Install Claude Code CLI
         RUN npm install -g @anthropic-ai/claude-code

         WORKDIR /mcp_server

         CMD ["sh", "-c", "uv sync && uv run python server.py"]
         ```
         そして `docker-compose.yml` の mcp-server セクションを変更:
         ```yaml
         mcp-server:
           build:
             context: ./mcp_server
             dockerfile: Dockerfile
           working_dir: /mcp_server
           ...
         ```
         （`image: ghcr.io/astral-sh/uv:...` と `command: sh -c "..."` を削除し、`build:` ブロックに置き換え）

    4. **e2e smoke テスト追加** — `tests/test_mcp_server.py` に `pytest.mark.integration` でマークしたテストを追加:
       - `test_claude_code_e2e_smoke` — `async with Client(mcp)` 経由で `claude_code(prompt="print('hello')", cwd="/tmp")` を呼ぶ。claude CLI が無い環境では `pytest.skip` するか、`error` キー存在を許容して exit_code が int であることだけ assert
       - 既存の `test_stub_tools_registered` が `EXPECTED_TOOLS = {"ping", "web_search", "db_query", "claude_code"}` で green であることを再確認

    5. **マニュアル検証コマンド**（PLAN 完了時に実行）:
       ```bash
       docker compose build mcp-server   # Dockerfile を変更した場合
       docker compose up -d mcp-server
       docker compose exec mcp-server claude --version   # → 2.x.x 表示
       docker compose exec mcp-server ls -la /shared/claude-code-outputs/   # → ディレクトリ存在
       docker compose exec -T app uv run pytest tests/test_mcp_server.py -x -q
       ```

    Avoid:
    - claude CLI を pip install しない（npm 経由のみ正規）
    - Dockerfile を全面書き換えしない — 既存パターン（uv sync）を維持して差分を最小化
    - worker volume を read-write でマウントしない — read-only で十分（worker は file_path を読むだけ）
    - named volume を忘れない — `volumes:` トップレベルセクションに `claude-code-outputs:` 宣言が必須
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && docker compose build mcp-server && docker compose up -d mcp-server && docker compose exec -T mcp-server claude --version && docker compose exec -T app uv run pytest tests/test_mcp_server.py -x -q</automated>
  </verify>
  <done>
    - `mcp_server/Dockerfile` 作成（claude CLI 含む、必要な場合のみ）
    - `docker-compose.yml` の mcp-server サービスに `claude-code-outputs` volume + `CLAUDE_CODE_OUTPUT_DIR` env、worker にも read-only マウント、トップレベル `volumes:` 宣言追加
    - `mcp_server/server.py` で `register_claude_code_tools(mcp)` 呼び出し
    - `docker compose exec mcp-server claude --version` がエラーなく実行できる
    - `tests/test_mcp_server.py` 全 green（unit + e2e smoke）
    - Phase Success Criteria #3, #4, #5 達成
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| エージェント → MCP claude_code | LLM が生成した prompt と cwd が claude CLI に渡る。LLM 出力は信頼できない |
| mcp-server プロセス環境 → claude サブプロセス環境 | 親プロセスの env が継承される危険性 |
| claude サブプロセス → ファイルシステム | claude が cwd 配下のファイルを読み書きする |
| shared volume `/shared/claude-code-outputs/` | mcp-server (RW) と worker (RO) の間で共有 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07 | Information Disclosure | env 変数の子プロセス継承（CLAUDECODE, ANTHROPIC_API_KEY, DATABASE_URL, JWT_SECRET 等） | mitigate | D-08/D-09 許可リスト方式: `sanitized_env = {k: v for k, v in os.environ.items() if k in ALLOWED_ENV_KEYS}`。`ALLOWED_ENV_KEYS = {"PATH", "HOME", "LANG", "LC_ALL", "TERM"}` のみ。`test_claude_code_env_sanitized` で env kwarg を直接 assert。|
| T-23-08 | DoS | 無限ループ / 長時間実行による zombie プロセス | mitigate | D-10/D-11 タイムアウトエスカレーション: `asyncio.wait_for(proc.communicate(), timeout=60)` → TimeoutError で `proc.terminate()` → `wait_for(proc.wait(), timeout=5)` → TimeoutError で `proc.kill()` → 最後に必ず `await proc.wait()` で zombie 回収。`test_claude_code_timeout_kill_escalation` で kill パスを確認。|
| T-23-09 | Tampering | path traversal via cwd（`cwd="/etc"` 等） | accept | 内部エージェントのみ使用、社内 200 名規模、低リスク。23-RESEARCH.md「Assumption A4」記載済み。将来必要なら allowlist 制限を追加（例: `/tmp` 配下のみ）。|
| T-23-10 | Tampering | prompt インジェクションで claude が悪意あるコードを実行 | accept | claude CLI 自体が一定の安全機構を持つ（`--print` モードは対話的承認を求めない）。社内利用、信頼境界を明示。|
| T-23-11 | Information Disclosure | shared volume 上の出力ファイルが他ジョブから読める | accept | volume は mcp-server (RW) と worker (RO) のみマウント、外部公開されない。ファイル名に uuid を含めて衝突回避。|
| T-23-12 | DoS | 巨大 stdout で OOM | mitigate | D-06: `MAX_INLINE_CHARS = 4000` で切り捨て、超過分は file 書き出し（ストリーミング読み込みではないので、claude が無限出力するケースは TIMEOUT_SECS でカット）。|
| T-23-13 | Spoofing | claude CLI バイナリのすり替え | accept | Dockerfile で `npm install -g @anthropic-ai/claude-code` から取得、ベースイメージは pinned。CI で再ビルド時に整合性チェックは未実装（社内、低リスク）。|
| T-23-14 | Information Disclosure | エラーメッセージに sanitized_env や cwd が漏れる | mitigate | `error` 文字列に `os.environ` や cwd 値を含めない。例外 `e` の文字列化のみ返す。|
</threat_model>

<verification>
1. `docker compose build mcp-server` 成功
2. `docker compose up -d mcp-server` で healthy
3. `docker compose exec mcp-server claude --version` で 2.x.x 表示
4. `docker compose exec mcp-server ls -la /shared/claude-code-outputs/` でディレクトリ存在
5. `docker compose exec -T app uv run pytest tests/test_mcp_server.py -x -q` 全 green
6. Phase Success Criteria #3, #4, #5 を全て満たす
</verification>

<success_criteria>
- Phase Success Criteria #3 達成: claude_code ツールから Claude Code CLI が実行され出力が返る
- Phase Success Criteria #4 達成: CLAUDECODE=1 等が継承されない（test_claude_code_env_sanitized で確認）
- Phase Success Criteria #5 達成: 60 秒タイムアウトで zombie が残らない（test_claude_code_timeout_kill_escalation で確認）
- Plan 01 と合わせて Phase 23 全 success criteria（5 件）達成
- tests/test_mcp_server.py 全 green
</success_criteria>

<output>
After completion, create `.planning/phases/23-db-claude-code/23-02-SUMMARY.md`
</output>
