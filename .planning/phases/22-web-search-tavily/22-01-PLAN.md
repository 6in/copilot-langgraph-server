---
phase: 22-web-search-tavily
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp_server/tools/web_search.py
  - mcp_server/tools/stubs.py
  - mcp_server/server.py
  - mcp_server/pyproject.toml
  - docker-compose.yml
  - agents/general-assistant/AGENT.md
  - tests/test_mcp_server.py
  - scripts/test_mcp_tools.py
autonomous: true
requirements: [SEARCH-01, SEARCH-02]
must_haves:
  truths:
    - "MCP サーバーに web_search ツールが登録され、Tavily API 経由で検索結果を返す"
    - "検索結果の各 content が 1000 文字以内に切り捨てられる"
    - "Tavily API エラー時に {error: ...} を返し、ジョブ失敗にならない"
    - "web_search_stub が MCP サーバーから完全に削除されている"
    - "general-assistant エージェントが web_search ツールを使用できる"
  artifacts:
    - path: "mcp_server/tools/web_search.py"
      provides: "Tavily web_search ツール実装"
      contains: "def web_search"
    - path: "mcp_server/server.py"
      provides: "web_search ツール登録"
      contains: "register_web_search_tools"
    - path: "tests/test_mcp_server.py"
      provides: "web_search テスト（正常系・サイズ制限・エラー系）"
      contains: "test_web_search"
  key_links:
    - from: "mcp_server/server.py"
      to: "mcp_server/tools/web_search.py"
      via: "import register_tools"
      pattern: "from tools.web_search import"
    - from: "agents/general-assistant/AGENT.md"
      to: "mcp_server/tools/web_search.py"
      via: "SubAgentRegistry tool name filter"
      pattern: "web_search"
    - from: "docker-compose.yml"
      to: ".env"
      via: "TAVILY_API_KEY 環境変数注入"
      pattern: "TAVILY_API_KEY"
---

<objective>
web_search_stub を Tavily API を使った本番 web_search ツールに差し替え、テストを追加する。

Purpose: エージェント（general-assistant）がリアルタイム Web 検索を使って最新情報を回答に含められるようにする。
Output: mcp_server/tools/web_search.py（本番実装）、更新された server.py・stubs.py・docker-compose.yml・AGENT.md・テスト
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-web-search-tavily/22-CONTEXT.md
@.planning/phases/22-web-search-tavily/22-RESEARCH.md

@mcp_server/server.py
@mcp_server/tools/stubs.py
@mcp_server/pyproject.toml
@docker-compose.yml
@agents/general-assistant/AGENT.md
@tests/test_mcp_server.py
@scripts/test_mcp_tools.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From mcp_server/server.py:
```python
from fastmcp import FastMCP
mcp = FastMCP("copilot-mcp-server")
# register_tools(mcp) パターンでツールを登録
```

From mcp_server/tools/stubs.py:
```python
def register_tools(mcp: "FastMCP") -> None:
    # @mcp.tool デコレータでツールを登録
    @mcp.tool
    def ping() -> dict: ...
    @mcp.tool
    def web_search_stub(query: str) -> str: ...  # 削除対象
    @mcp.tool
    def db_query_stub(sql: str) -> list[dict]: ...
    @mcp.tool
    def claude_code_stub(command: str) -> str: ...
```

From tests/test_mcp_server.py:
```python
from fastmcp import Client
from server import mcp
EXPECTED_TOOLS = {"ping", "web_search_stub", "db_query_stub", "claude_code_stub"}
# Client(mcp) で in-process テスト
# client.call_tool("tool_name", {args}) で呼び出し
# result.data でレスポンス dict を取得
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: web_search ツール実装 + stubs 差し替え + 設定更新</name>
  <files>
    mcp_server/tools/web_search.py
    mcp_server/tools/stubs.py
    mcp_server/server.py
    mcp_server/pyproject.toml
    docker-compose.yml
    agents/general-assistant/AGENT.md
    scripts/test_mcp_tools.py
    tests/test_mcp_server.py
  </files>
  <read_first>
    mcp_server/tools/stubs.py
    mcp_server/server.py
    mcp_server/pyproject.toml
    docker-compose.yml
    agents/general-assistant/AGENT.md
    scripts/test_mcp_tools.py
    tests/test_mcp_server.py
  </read_first>
  <behavior>
    - Test 1 (test_web_search_normal): web_search ツールを呼び出すと {"results": [{"url": ..., "content": ...}]} が返る
    - Test 2 (test_web_search_truncates_content): 2000 文字の content が 1000 文字に切り捨てられる
    - Test 3 (test_web_search_error_handling): 例外時に {"error": "web_search failed: ..."} が返る
    - Test 4 (test_stub_tools_registered): EXPECTED_TOOLS に web_search が含まれ web_search_stub が含まれない
  </behavior>
  <action>
    **1. mcp_server/pyproject.toml — langchain-community 依存追加:**
    `dependencies` に `"langchain-community>=0.4.1"` を追加する。

    **2. mcp_server/tools/web_search.py — 新規作成:**
    ```python
    from __future__ import annotations
    from typing import TYPE_CHECKING

    if TYPE_CHECKING:
        from fastmcp import FastMCP

    def register_tools(mcp: "FastMCP") -> None:
        """Phase 22: Tavily web_search ツールを登録する。"""

        @mcp.tool
        def web_search(query: str) -> dict:
            """Web 検索を実行してリアルタイム情報を返す（Tavily API 使用）。

            Args:
                query: 検索クエリ文字列

            Returns:
                {"results": [{"url": "...", "content": "..."}]} or {"error": "..."}
            """
            try:
                from langchain_community.tools.tavily_search import TavilySearchResults
                tavily = TavilySearchResults(max_results=3)
                results: list[dict] = tavily.invoke(query)
                # SEARCH-02: コンテキスト超過防止 — 各結果の content を 1000 文字で切り捨て
                for r in results:
                    if isinstance(r, dict) and "content" in r:
                        r["content"] = r["content"][:1000]
                return {"results": results}
            except Exception as e:
                return {"error": f"web_search failed: {e}"}
    ```

    **3. mcp_server/tools/stubs.py — web_search_stub 関数を削除:**
    `def web_search_stub(query: str) -> str:` ブロック全体（デコレータ `@mcp.tool` 含む）を削除する。ping, db_query_stub, claude_code_stub は残す。docstring の Phase 22 参照も更新する。

    **4. mcp_server/server.py — web_search モジュールの登録追加:**
    既存の `from tools.stubs import register_tools` の後に以下を追加:
    ```python
    from tools.stubs import register_tools as register_stub_tools
    from tools.web_search import register_tools as register_web_search_tools
    ```
    そして呼び出しを:
    ```python
    register_stub_tools(mcp)
    register_web_search_tools(mcp)
    ```
    に変更する。

    **5. docker-compose.yml — mcp-server に TAVILY_API_KEY 環境変数追加:**
    `mcp-server` サービスの定義に `environment:` セクションを追加:
    ```yaml
    environment:
      - TAVILY_API_KEY=${TAVILY_API_KEY}
    ```

    **6. agents/general-assistant/AGENT.md — ツール名更新:**
    `tools:` セクションの `web_search_stub` を `web_search` に変更:
    ```yaml
    tools:
      - web_search
      - ping
    ```

    **7. scripts/test_mcp_tools.py — ツール名更新:**
    TOOL_INPUTS dict の `"web_search_stub"` キーを `"web_search"` に変更。値は `{"query": "LangGraph MCP integration"}` のまま。`--tool` の choices リストも更新。コメントやドキュメント文字列内の `web_search_stub` 参照も `web_search` に更新。

    **8. tests/test_mcp_server.py — EXPECTED_TOOLS 更新 + web_search テスト 3 件追加:**
    - `EXPECTED_TOOLS` を `{"ping", "web_search", "db_query_stub", "claude_code_stub"}` に変更
    - `test_stub_schemas_have_required_params` の `"web_search_stub": "query"` を `"web_search": "query"` に変更
    - 以下の 3 テストを追加（TavilySearchResults.invoke を mock）:

    ```python
    @pytest.mark.asyncio
    async def test_web_search_normal():
        """SEARCH-01: web_search が Tavily 検索結果を返す。"""
        mock_results = [
            {"url": "https://example.com/1", "content": "Result 1"},
            {"url": "https://example.com/2", "content": "Result 2"},
        ]
        with patch(
            "tools.web_search.TavilySearchResults",
        ) as MockTavily:
            MockTavily.return_value.invoke.return_value = mock_results
            async with Client(mcp) as client:
                result = await client.call_tool("web_search", {"query": "test query"})
        payload = result.data if hasattr(result, "data") else result.structured_content
        assert "results" in payload
        assert len(payload["results"]) == 2
        assert payload["results"][0]["url"] == "https://example.com/1"

    @pytest.mark.asyncio
    async def test_web_search_truncates_content():
        """SEARCH-02: 1000 文字超の content が切り捨てられる。"""
        long_content = "x" * 2000
        mock_results = [{"url": "https://example.com", "content": long_content}]
        with patch(
            "tools.web_search.TavilySearchResults",
        ) as MockTavily:
            MockTavily.return_value.invoke.return_value = mock_results
            async with Client(mcp) as client:
                result = await client.call_tool("web_search", {"query": "test"})
        payload = result.data if hasattr(result, "data") else result.structured_content
        assert len(payload["results"][0]["content"]) == 1000

    @pytest.mark.asyncio
    async def test_web_search_error_handling():
        """web_search エラー時に {"error": ...} を返す。"""
        with patch(
            "tools.web_search.TavilySearchResults",
        ) as MockTavily:
            MockTavily.return_value.invoke.side_effect = Exception("API key invalid")
            async with Client(mcp) as client:
                result = await client.call_tool("web_search", {"query": "test"})
        payload = result.data if hasattr(result, "data") else result.structured_content
        assert "error" in payload
        assert "web_search failed" in payload["error"]
    ```

    **重要な注意:** mock のパッチ先は `"tools.web_search.TavilySearchResults"`（web_search.py 内でインポートされるパスの、実際の import 文に合わせる）。web_search.py では `from langchain_community.tools.tavily_search import TavilySearchResults` が関数内 import なので、パッチ先はモジュールパスではなくクラスを直接パッチする形にする。実際のテスト実行時に正しく動作するパッチパスを確認し、必要に応じて調整すること（`langchain_community.tools.tavily_search.TavilySearchResults` をパッチする方が確実な場合がある）。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && python -m pytest tests/test_mcp_server.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `mcp_server/tools/web_search.py` が存在し `def web_search(query: str) -> dict:` を含む
    - `mcp_server/tools/web_search.py` に `r["content"] = r["content"][:1000]` が含まれる
    - `mcp_server/tools/web_search.py` に `return {"error": f"web_search failed: {e}"}` が含まれる
    - `mcp_server/tools/web_search.py` に `TavilySearchResults(max_results=3)` が含まれる
    - `mcp_server/tools/stubs.py` に `web_search_stub` が含まれない
    - `mcp_server/tools/stubs.py` に `ping`, `db_query_stub`, `claude_code_stub` が含まれる
    - `mcp_server/server.py` に `from tools.web_search import` が含まれる
    - `mcp_server/pyproject.toml` に `langchain-community` が含まれる
    - `docker-compose.yml` の mcp-server セクションに `TAVILY_API_KEY` が含まれる
    - `agents/general-assistant/AGENT.md` に `web_search` が含まれ `web_search_stub` が含まれない
    - `tests/test_mcp_server.py` の EXPECTED_TOOLS に `web_search` が含まれ `web_search_stub` が含まれない
    - `tests/test_mcp_server.py` に `test_web_search_normal`, `test_web_search_truncates_content`, `test_web_search_error_handling` が含まれる
    - `scripts/test_mcp_tools.py` に `web_search` キーが含まれ `web_search_stub` が含まれない
    - `python -m pytest tests/test_mcp_server.py -x` が全テスト PASS
  </acceptance_criteria>
  <done>
    web_search ツールが Tavily API を使って実装され、stubs.py から web_search_stub が削除され、server.py に web_search が登録され、docker-compose.yml に TAVILY_API_KEY が設定され、AGENT.md のツール名が更新され、テスト 3 件（正常系・サイズ制限・エラー系）+ 既存テスト更新がすべて PASS する
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user query -> web_search tool | ユーザーの検索クエリが Tavily API に送信される |
| Tavily API -> mcp-server | 外部 API のレスポンスが MCP サーバー内で処理される |
| .env -> docker-compose | TAVILY_API_KEY がコンテナ環境変数として注入される |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Information Disclosure | TAVILY_API_KEY | mitigate | `.env` は `.gitignore` 済み。docker-compose 経由で環境変数注入（コンテナイメージに含めない）。pyproject.toml や ソースコードにキーをハードコードしない |
| T-22-02 | Denial of Service | web_search tool | accept | `max_results=3` で API コールあたりの結果数を制限。Tavily 側のレート制限で追加制御。200名規模の社内利用でリスク低 |
| T-22-03 | Spoofing | Tavily API response | accept | Tavily は信頼できる商用 API。レスポンスは LLM への入力として使用されるのみで、直接実行されない |
| T-22-04 | Information Disclosure | search query logging | accept | 検索クエリはツール呼び出しログに記録されるが、社内利用のため許容。PII を含むクエリは利用ポリシーで制御（スコープ外） |
</threat_model>

<verification>
1. `python -m pytest tests/test_mcp_server.py -x -v` — 全テスト PASS（web_search 3 件 + 既存 4 件）
2. `grep -c "web_search_stub" mcp_server/tools/stubs.py` — 0（完全削除）
3. `grep "web_search" mcp_server/server.py` — import 行が存在
4. `grep "TAVILY_API_KEY" docker-compose.yml` — mcp-server セクション内に存在
5. `grep "web_search" agents/general-assistant/AGENT.md` — `web_search`（stub なし）
</verification>

<success_criteria>
- web_search ツールが MCP サーバーに登録され、TavilySearchResults を使って検索結果を返す（SEARCH-01）
- 検索結果の content が 1000 文字で切り捨てられる（SEARCH-02）
- エラー時に {"error": ...} を返し、ジョブ失敗にならない
- web_search_stub が完全に削除され、web_search に置き換えられている
- テスト 7 件（既存更新 4 + 新規 3）が全て PASS
</success_criteria>

<output>
After completion, create `.planning/phases/22-web-search-tavily/22-01-SUMMARY.md`
</output>
