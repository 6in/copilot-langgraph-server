---
phase: 22-web-search-tavily
plan: 02
type: execute
wave: 1
gap_closure: true
depends_on: [22-01]
files_modified:
  - app/providers/copilot.py
  - agents/general-assistant/AGENT.md
  - tests/test_copilot_bind_tools.py
autonomous: false
requirements: [SEARCH-01, SEARCH-02]

must_haves:
  truths:
    - "「最新の Python 3.13 の新機能を教えて」のような現在情報を要する質問に対し、エージェントが web_search ツール呼び出し JSON を出力する"
    - "web_search が実行され、Tavily の取得結果が回答本文に反映される（モデルの内部知識のみで回答しない）"
    - "ツール呼び出し JSON 形式（`{\"tool\": \"web_search\", \"args\": {...}}`）が誤って自然文として漏れない"
  artifacts:
    - path: "app/providers/copilot.py"
      provides: "強化された TOOL_SYSTEM_PROMPT_TEMPLATE（ツール使用が必須なケースを明示）"
      contains: "TOOL_SYSTEM_PROMPT_TEMPLATE"
    - path: "agents/general-assistant/AGENT.md"
      provides: "ツール使用ガイドライン入りシステムプロンプト"
      contains: "web_search"
    - path: "tests/test_copilot_bind_tools.py"
      provides: "強化プロンプトの内容を固定するユニットテスト"
  key_links:
    - from: "BoundChatCopilot._agenerate"
      to: "TOOL_SYSTEM_PROMPT_TEMPLATE"
      via: "SystemMessage 注入（copilot.py:265）"
      pattern: "TOOL_SYSTEM_PROMPT_TEMPLATE\\.format"
    - from: "general-assistant SubAgent"
      to: "AGENT.md system prompt"
      via: "SubAgentRegistry が AGENT.md 本文を system プロンプトとして使用"
      pattern: "web_search"
---

<objective>
Phase 22 UAT で発覚した major ギャップ 2 件（test 3, test 4）を解消する。
両ギャップの根本原因は同一: `TOOL_SYSTEM_PROMPT_TEMPLATE` と general-assistant
の system プロンプトがツール使用に関して許容的すぎるため、Copilot モデル（GPT-4.1）が
最新情報を要する質問でも自分の知識で回答してしまう。

Purpose: web_search ツールが「最新情報を要する質問では必ず呼ばれる」状態にし、
Phase 22 の SEARCH-01 / SEARCH-02 要件を満たす。

Output:
- TOOL_SYSTEM_PROMPT_TEMPLATE をツール使用必須シナリオを明示する内容に更新
- general-assistant AGENT.md にツール使用ガイダンスを追記
- プロンプト内容を固定するユニットテスト
- 実機での再 UAT（test 3 / test 4）合格
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-web-search-tavily/22-UAT.md
@.planning/phases/22-web-search-tavily/22-01-SUMMARY.md
@app/providers/copilot.py
@agents/general-assistant/AGENT.md
@tests/test_copilot_bind_tools.py

<interfaces>
<!-- 既存コード抜粋（PE 担当者がコードベース探索なしで実装に入れるため） -->

From app/providers/copilot.py (lines 207-215, current state):
```python
TOOL_SYSTEM_PROMPT_TEMPLATE = """\
You have access to the following tools:
{tool_schemas}

To call a tool, respond ONLY with valid JSON (no markdown, no explanation):
{{"tool": "<tool_name>", "args": {{...}}}}

If no tool is needed, respond normally in plain text.\
"""
```

From app/providers/copilot.py (line 265, 使用箇所):
```python
content=TOOL_SYSTEM_PROMPT_TEMPLATE.format(tool_schemas=tool_schemas)
```

From agents/general-assistant/AGENT.md (current body, after frontmatter):
```
あなたは親切で知識豊富なアシスタントです。
ユーザーの質問や依頼に対して、丁寧かつ簡潔に回答してください。
専門的な内容も分かりやすく説明し、必要に応じて具体例を交えてください。
```

frontmatter には `tools: [web_search, ping]` が既に宣言済み。
</interfaces>

## UAT で観測された失敗ログ（要点）

- test 3: 「最新の Python 3.13 の新機能を教えて」 → web_search 未呼び出し
- test 4: 「調査して」と明示しても、モデルが
  「現在は私（GitHub Copilot CLI）として応答しているため web_search ツールは使っていない」
  と自然文で回答。ツール呼び出し JSON が出力されなかった

## 根本原因（UAT.md より）

`TOOL_SYSTEM_PROMPT_TEMPLATE` の "If no tool is needed, respond normally" が
モデルに「自分で答えられるならツールを使わなくてよい」という逃げ道を与えている。
インフラ層（MCP 接続、ToolEnabledSubAgent 登録、tool schema 注入）はすべて正常動作している。
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TOOL_SYSTEM_PROMPT_TEMPLATE をツール使用必須型に更新</name>
  <files>app/providers/copilot.py, tests/test_copilot_bind_tools.py</files>
  <behavior>
    - Test 1: TOOL_SYSTEM_PROMPT_TEMPLATE.format(tool_schemas="...") 結果に
      「最新情報」「current」「real-time」のような必須使用シグナルを示すフレーズが含まれる
    - Test 2: フォーマット結果に web_search のような名前ではなく、
      「あなたは内部知識だけで答えてはいけない場合がある」というルール文が含まれる
    - Test 3: 既存の JSON フォーマット指示（`{"tool": "<tool_name>", "args": {...}}`）は維持される
    - Test 4: 既存の `{tool_schemas}` プレースホルダがそのまま埋め込まれる（後方互換）
  </behavior>
  <action>
    `app/providers/copilot.py` の `TOOL_SYSTEM_PROMPT_TEMPLATE`（行 207-215）を以下の方針で書き換える:

    1. 「ツールが利用可能なときの判断基準」を明示する。具体的に以下を含めること:
       - 「最新情報・現在の情報・リアルタイム情報・あなたの知識カットオフ以降の情報を要する質問では、
          必ずツールを呼び出すこと」
       - 「ツールを呼べるのに自分の内部知識だけで答えてはならない」
       - 「ユーザーが『調べて』『最新の』『現在の』『今の』『最近の』と言ったときは必ずツールを呼ぶ」
    2. JSON 出力フォーマット（`{"tool": "<tool_name>", "args": {...}}`）の指示は維持する。
       「JSON 以外の前置きや説明文を一切出力しない」ことを強調する。
    3. ツール呼び出しの簡潔な例を 1 つ含める（例:
       `{"tool": "web_search", "args": {"query": "Python 3.13 new features"}}`）。
    4. 「If no tool is needed, respond normally in plain text.」のような逃げ道フレーズは削除し、
       「ツールが不要な純粋な雑談・既知の数学計算・翻訳のような場合のみ通常回答してよい」という
       狭い例外条件に置き換える。
    5. `{tool_schemas}` プレースホルダは保持。
    6. 言語: 既存テンプレートが英語なので英語で書く（モデルの追従性が高い）。

    同時に `tests/test_copilot_bind_tools.py` に新規テストクラス
    `TestToolSystemPromptTemplate` を追加し、上記 behavior の 4 ケースを assert する
    （ファイル末尾に追記、import が不足していれば追加）。

    既存テストへの影響:
    - もし `tests/test_copilot_bind_tools.py` 内で旧テンプレート文字列（"If no tool is needed,
      respond normally in plain text"）を直接 assert している箇所があれば更新する。
    - grep で `respond normally` を探して該当箇所を修正。

    NEVER:
    - tool_schemas 以外の format プレースホルダを増やしてはいけない（呼び出し側 copilot.py:265 が
      `tool_schemas=` のみを渡しているため、KeyError を引き起こす）。
    - JSON フォーマット指示を削ってはいけない（BoundChatCopilot のレスポンスパーサが依存）。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph &amp;&amp; uv run pytest tests/test_copilot_bind_tools.py -x -q</automated>
  </verify>
  <done>
    - TOOL_SYSTEM_PROMPT_TEMPLATE が更新済み（"If no tool is needed, respond normally in plain text" は削除）
    - 新規 4 テスト pass、既存テスト全 pass
    - copilot.py:265 の format 呼び出しが KeyError を起こさないこと（既存 BoundChatCopilot テストで担保）
  </done>
</task>

<task type="auto">
  <name>Task 2: general-assistant AGENT.md にツール使用ガイダンスを追記</name>
  <files>agents/general-assistant/AGENT.md</files>
  <action>
    `agents/general-assistant/AGENT.md` の frontmatter 以下の本文を、ツール使用方針を明示する内容に更新する。

    更新後の本文（frontmatter は変更しない、本文のみ置換）:
    ```
    あなたは親切で知識豊富なアシスタントです。
    ユーザーの質問や依頼に対して、丁寧かつ簡潔に回答してください。
    専門的な内容も分かりやすく説明し、必要に応じて具体例を交えてください。

    ## ツール使用ルール（重要）

    あなたには `web_search` ツールが提供されています。次のいずれかに該当する質問では、
    自分の内部知識だけで答えず、必ず `web_search` ツールを呼び出して最新情報を取得してから回答すること:

    - 「最新の」「現在の」「今の」「最近の」「2025年の」「2026年の」のような時制を含む質問
    - ライブラリ・フレームワーク・製品の最新バージョンや新機能に関する質問
    - 価格・為替・株価・統計・ニュース・出来事など、時間とともに変化する情報
    - ユーザーが「調べて」「検索して」「リサーチして」と明示的に依頼した場合
    - あなたの知識カットオフ以降に発生した事象に関する質問

    ツールを呼べるにもかかわらず内部知識のみで回答してはいけません。
    「web_search ツールは使っていない」「私の知識範囲内でまとめます」といった
    断り書きを書かずに、まずツールを呼び出してください。

    純粋な雑談・翻訳・要約・既知の概念説明・数学計算など、外部情報が不要な場合のみ
    通常通り回答してください。
    ```

    NEVER:
    - frontmatter（`---` で囲まれた YAML ブロック）は変更しない。`tools:` の宣言はそのまま維持。
    - keywords を変更しない（Phase 13 で `[]` 固定が決定済み: catch-all agent）。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph &amp;&amp; python -c "import frontmatter; p = frontmatter.load('agents/general-assistant/AGENT.md'); assert 'web_search' in p.content; assert '最新の' in p.content; assert p.metadata['name'] == 'general-assistant'; assert 'web_search' in p.metadata['tools']; print('OK')"</automated>
  </verify>
  <done>
    - AGENT.md 本文にツール使用ルールセクションが追加されている
    - frontmatter（name, keywords, description, model, tools）は無変更
    - python-frontmatter で読み込み時に metadata と content の両方が期待値を含む
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 実機 UAT 再テスト（test 3 + test 4）</name>
  <files>(なし — 実機検証のみ)</files>
  <action>
    Task 1 + Task 2 の変更を実機で検証する。Docker サービスを再起動し、Phase 22 UAT で
    failed となった test 3 / test 4 を再実行し、web_search ツールが正しく呼ばれることを確認する。
    詳細手順は <how-to-verify> 参照。
  </action>
  <what-built>
    - TOOL_SYSTEM_PROMPT_TEMPLATE を強化（ツール使用必須シナリオを明示）
    - general-assistant AGENT.md にツール使用ルールを追加
    - ユニットテスト追加・既存テスト全 pass
  </what-built>
  <how-to-verify>
    1. Docker サービスを再起動して新しいプロンプトをロード:
       ```
       docker compose restart api worker mcp-server
       ```
       （注: AGENT.md は worker 起動時の SubAgentRegistry でロードされ、
       TOOL_SYSTEM_PROMPT_TEMPLATE は app/providers/copilot.py の import で読まれるため
       両者ともコンテナ再起動が必要）

    2. ブラウザで `http://localhost:5173/orochi/` を開く

    3. SuperChat（または通常チャット）で general-assistant を選び、test 3 を再実行:
       - 入力: 「最新の Python 3.13 の新機能を教えて」
       - 期待: 回答に Tavily の検索結果（URL 引用や具体的な機能名）が含まれる
       - 期待: 回答に「私の知識では」「内部知識で」のような断り文が含まれない
       - 期待: worker ログに `web_search` ツール呼び出しの形跡がある
         （`docker compose logs worker | grep -i web_search` で確認可能）

    4. test 4 を再実行:
       - 入力: 「Tavily の最新ニュースを調査して」
       - 期待: web_search が呼ばれ、コンテキスト超過エラーなく回答が返る
       - 期待: max_results=3 の制限により回答長が常識的範囲（数千トークン以内）

    5. 念のためツール非対象の質問でリグレッション確認:
       - 入力: 「こんにちは。あなたは誰？」
       - 期待: web_search を呼ばずに通常回答（自己紹介）が返る

    再テスト失敗時:
    - worker ログで BoundChatCopilot のレスポンス内容を確認
    - JSON フォーマット出力に失敗していれば Task 1 のテンプレート文言を再調整
    - ツール呼び出しは出ているが結果が反映されない場合は MCP 側の問題（別ギャップ）
  </how-to-verify>
  <verify>
    <automated>docker compose logs worker --tail=200 2>&amp;1 | grep -iE "web_search|tool_call" || echo "manual verification only"</automated>
  </verify>
  <done>
    - test 3: 「最新の Python 3.13 の新機能を教えて」で web_search 呼び出しが発火し、Tavily 検索結果を含む回答が返る
    - test 4: 「調査して」と明示した質問でも web_search が呼ばれる
    - 「こんにちは」のような雑談ではツールが呼ばれない（過剰呼び出しなし）
    - ユーザーが approved を返答
  </done>
  <resume-signal>「approved」または失敗内容を記述してください</resume-signal>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_copilot_bind_tools.py -x -q` グリーン
- `uv run pytest tests/ -x -q --ignore=tests/integration` グリーン（リグレッションなし）
- Task 3 の実機 UAT で test 3 / test 4 が pass
- 22-UAT.md の Gaps 2 件が closed 状態に更新される（execute 時に gap closure ハンドラが処理）
</verification>

<success_criteria>
- TOOL_SYSTEM_PROMPT_TEMPLATE がツール必須シナリオを明示している
- general-assistant AGENT.md がツール使用ルールを含む
- ユニットテスト追加 + 既存テスト全 pass
- 実機で「最新の Python 3.13 の新機能を教えて」に対し web_search が呼ばれ、
  Tavily の取得情報が回答に反映される
- 「調査して」と明示した質問でも web_search が呼ばれる
- 雑談（「こんにちは」）ではツールが呼ばれない（過剰呼び出しなし）
</success_criteria>

<output>
完了後 `.planning/phases/22-web-search-tavily/22-02-SUMMARY.md` を作成し、
UAT.md の Gaps 2 件を closed としてマークすること。
</output>
