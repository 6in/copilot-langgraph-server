---
phase: 31
plan: 05
type: execute
wave: 3
depends_on:
  - 31-02
  - 31-03
files_modified:
  - app/api/routes/iframe_rpc.py
  - app/jobs/handlers/iframe_rpc_handler.py
  - tests/test_iframe_rpc_trace.py
autonomous: true
requirements:
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12
  - D-14
risk: medium
threat_ids:
  - T-31-01
  - T-31-02
tags:
  - observability
  - iframe-rpc
  - tool-call
  - correlation-id

must_haves:
  truths:
    - "iframe_rpc ルート (app/api/routes/iframe_rpc.py) が arq job dict に correlation_id / app_id=\"canvas\" / user_id を載せる"
    - "IframeRpcHandler.handle() が RPCContext を構築し request span で wrap する"
    - "_handle_query の tool.ainvoke (db_query) が tool_call span で wrap される"
    - "_handle_ai の llm.ainvoke が tool_call span (tool_name=\"ai\", privileged=false) で wrap される"
    - "既存フリーテキスト log (`iframe-rpc QUERY user=... pool=...`) は削除され span attribute (user_id / pool_name) に置き換わる"
    - "tests/test_iframe_rpc_trace.py で 3 ケース以上の unit test が green"
  artifacts:
    - path: "app/api/routes/iframe_rpc.py"
      provides: "correlation_id / app_id / user_id を job dict に追加"
      contains: "correlation_id"
    - path: "app/jobs/handlers/iframe_rpc_handler.py"
      provides: "RPCContext 構築 + request span + tool_call span (db_query/ai)"
      contains: "RPCContext"
    - path: "tests/test_iframe_rpc_trace.py"
      provides: "iframe_rpc 経路の span emit unit test"
      min_lines: 100
  key_links:
    - from: "app/api/routes/iframe_rpc.py"
      to: "app/jobs/handlers/iframe_rpc_handler.py"
      via: "enqueue_job の payload に correlation_id/app_id/user_id を追加"
      pattern: "correlation_id.*uuid4|app_id.*canvas"
    - from: "app/jobs/handlers/iframe_rpc_handler.py"
      to: "app.observability.trace.trace_span"
      via: "handle() / _handle_query / _handle_ai の 3 箇所で async with"
      pattern: "async with trace_span"
---

<objective>
Phase 31 軸 B 経路 3（iframe_rpc_handler）を observability 基盤に統合する。Plan 04 の対象外である Canvas アプリからの postMessage → MCP db_query / AI 呼び出しを span として記録し、軸 A の trace_id（correlation_id）を request 起点で正しく伝播させる。既存フリーテキストログ (`iframe-rpc QUERY user=... pool=...`) は廃止し、全て span attribute 経由で集計可能にする。

Purpose: CONTEXT.md D-08 / D-10 を iframe_rpc 経路まで完全適用。Plan 04 の 3 handler (orchestrator / langgraph / debate) と合わせて、4 handler すべてで trace_id が一貫した schema で emit される状態を作る。
Output: 3 ファイル修正 (route + handler + test)。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-agent-mcp-observability/31-CONTEXT.md
@.planning/phases/31-agent-mcp-observability/31-RESEARCH.md
@.planning/phases/31-agent-mcp-observability/31-PATTERNS.md
@.planning/phases/31-agent-mcp-observability/31-VALIDATION.md
@app/observability/trace.py
@app/observability/traced_tool.py
@app/observability/config.py
@app/orchestrator/context.py
@app/api/routes/iframe_rpc.py
@app/jobs/handlers/iframe_rpc_handler.py

<interfaces>
<!-- 現行 iframe_rpc.py: job dict 構造 (L65-76) -->
Current enqueue_job payload (iframe_rpc.py L65-76):
  "process_chat" job with:
    - job_id: str
    - thread_id: "" (empty, iframe-rpc does not use LangGraph threads)
    - prompt: ""
    - github_token: str
    - reply_to: {"type": "web", "job_id": job_id}
    - task_type: "iframe_app_api"
    - github_login: str
    - rpc_method: str
    - rpc_params: dict

Phase 31 で追加する field:
    - correlation_id: str (uuid4().hex or str(uuid4()))
    - app_id: "canvas"

<!-- 現行 iframe_rpc_handler.handle() I/F (L79-101) -->
class IframeRpcHandler(TaskHandler):
    async def handle(self, ctx: dict, job: dict) -> dict:
        # job["job_id"], job["reply_to"], job.get("rpc_method"), job.get("rpc_params")
        # 現行: method="QUERY" → _handle_query(ctx, params)
        # 現行: method="AI"    → _handle_ai(job, params)

<!-- tool_call span 共通 attributes (D-09) -->
common_attrs = {
    "user_id": job.get("github_login", "unknown"),  # params.get("user") fallback
    "app_id": "canvas",
    "thread_id": "",  # iframe-rpc は thread_id 無し
    "agent_name": "iframe_rpc",  # operation source
    "model_name": "",  # AI call のときは resolve_model 結果
}

<!-- db_query は sandbox_exposed=true なので privileged=false -->
From config/mcp_tools.yaml:
  db_query: sandbox_exposed: true  → privileged=false

From app/observability/config.py (Plan 02):
  get_args_max_chars() -> int (default 500)
  get_result_max_chars() -> int (default 1000)

From app/observability/trace.py (Plan 02):
  async with trace_span(operation_name, trace_id, attributes, parent_span_id) as span
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/test_iframe_rpc_trace.py を RED で作成</name>
  <files>tests/test_iframe_rpc_trace.py</files>
  <read_first>
    - tests/test_iframe_rpc_trace.py (新規作成 — 存在確認のみ)
    - tests/conftest.py (capture_trace_logs fixture)
    - app/jobs/handlers/iframe_rpc_handler.py (handler I/F)
    - .planning/phases/31-agent-mcp-observability/31-RESEARCH.md §3.3
    - .planning/phases/31-agent-mcp-observability/31-PATTERNS.md (iframe_rpc_handler 修正セクション)
  </read_first>
  <behavior>
    - Test 1 (test_request_span_on_handle): handle() 呼び出し後、operation_name=="request" / attributes.app_id=="canvas" / attributes.handler=="iframe_rpc" / trace_id=job["correlation_id"] の span が 1 件 emit
    - Test 2 (test_query_emits_tool_call_span): method="QUERY" で _handle_query を呼ぶと tool_call span が 1 件、attributes.tool_name=="db_query" / attributes.privileged==false / attributes.app_id=="canvas" / success=true (mock tool が正常 dict 返却時)
    - Test 3 (test_query_error_dict_sets_error_status): mock tool が `{"error": "permission denied"}` を返す場合 tool_call span の status_code=="ERROR" / success=false
    - Test 4 (test_query_exception_sets_error_status): mock tool.ainvoke が raise する場合 tool_call span status_code=="ERROR"、handler が caller 側に {"result": False, "error": ...} を返す（例外は握られる、現行 pattern 維持）
    - Test 5 (test_ai_emits_tool_call_span): method="AI" で _handle_ai を呼ぶと tool_call span が 1 件 (tool_name=="ai", model_name=<resolved model>, privileged=false)
    - Test 6 (test_unknown_method_no_tool_call_span): method="UNKNOWN" → request span だけ emit、tool_call span なし
  </behavior>
  <action>
    `tests/test_iframe_rpc_trace.py` を新規作成。既存 `tests/test_iframe_rpc_handler.py` があればそのパターンを踏襲する。存在しない場合は `AsyncMock` で ctx / job_store / notifier / tool を mock する。

    shape:
    ```python
    from __future__ import annotations
    """IframeRpcHandler tracing unit tests (Phase 31 D-08..D-12)."""
    import json
    import uuid
    import pytest
    from unittest.mock import AsyncMock, MagicMock

    from app.jobs.handlers.iframe_rpc_handler import IframeRpcHandler


    def _make_job(method="QUERY", params=None, correlation_id=None):
        return {
            "job_id": "job-1",
            "reply_to": {"type": "web", "job_id": "job-1"},
            "rpc_method": method,
            "rpc_params": params or {},
            "github_token": "ghu_test",
            "github_login": "tester",
            "correlation_id": correlation_id or str(uuid.uuid4()),
            "app_id": "canvas",
        }


    def _make_ctx(tool_mock=None, tools_list=None):
        job_store = AsyncMock()
        job_store.save_result = AsyncMock()
        ctx = {
            "job_store": job_store,
            "mcp_tools": tools_list or [],
        }
        return ctx


    @pytest.mark.asyncio
    async def test_request_span_on_handle(capture_trace_logs):
        handler = IframeRpcHandler()
        job = _make_job(method="UNKNOWN")
        await handler.handle(_make_ctx(), job)
        spans = capture_trace_logs()
        req = [s for s in spans if s["operation_name"] == "request"]
        assert len(req) == 1
        assert req[0]["trace_id"] == job["correlation_id"]
        assert req[0]["attributes"]["app_id"] == "canvas"
        assert req[0]["attributes"]["handler"] == "iframe_rpc"

    # ... 以下 Test 2-6 ...
    ```

    Test 2 (QUERY): `mock_tool.name = "db_query"; mock_tool.ainvoke = AsyncMock(return_value={"rows": [{"id": 1}]})` を用意。`tools_list=[mock_tool]` で ctx 構築。`params={"pool_name": "default", "sql": "SELECT 1"}`。
    Test 5 (AI): `app.jobs.handlers.iframe_rpc_handler.ChatCopilot` を patch して `llm.ainvoke = AsyncMock(return_value=MagicMock(content="answer"))`、`llm.close = AsyncMock()`。

    RED 確認: `uv run pytest tests/test_iframe_rpc_trace.py -x` — 現状の handler には span emit がないので全ケース失敗。
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('tests/test_iframe_rpc_trace.py').read()); print('OK')" && grep -cE 'async def test_' tests/test_iframe_rpc_trace.py | awk '{ exit ($1 < 5) }'</automated>
  </verify>
  <done>
    tests/test_iframe_rpc_trace.py が syntax valid、test case 5 件以上、RED 状態で commit。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: iframe_rpc.py (route) に correlation_id / app_id / user_id を enqueue_job payload に追加</name>
  <files>app/api/routes/iframe_rpc.py</files>
  <read_first>
    - app/api/routes/iframe_rpc.py (現行 L41-78)
    - tests/test_iframe_rpc.py (存在確認 — 既存 route test があれば patch 対象)
    - .planning/phases/31-agent-mcp-observability/31-RESEARCH.md §3.3, §11.7
    - .planning/phases/31-agent-mcp-observability/31-PATTERNS.md (iframe_rpc_handler 修正)
  </read_first>
  <action>
    **事前確認 (W7 反映):** 既存 iframe_rpc route test の有無を確認する:
    ```bash
    ls tests/test_iframe_rpc*.py 2>/dev/null || echo "no existing iframe_rpc test"
    ```
    既存 test がある場合は、enqueue_job の kwargs が増える影響で `assert_called_with` 系アサートが失敗する可能性があるため、patch 修正を明示的な sub-step として扱う。具体的には:
    - `mock_arq.enqueue_job.assert_called_with(...)` を `assert_called_once()` + 個別 kwarg 検証に書き換える、または
    - assert_called_with の期待値に `correlation_id=ANY, app_id="canvas"` (unittest.mock.ANY) を追加する

    既存 test が `tests/test_iframe_rpc.py` 等に存在しない場合はこの sub-step をスキップしてよい (新規 `tests/test_iframe_rpc_trace.py` 側の assert が Task 1 で担保される)。

    続いて `app/api/routes/iframe_rpc.py` の `iframe_rpc` 関数 (L42-78) に以下を追加:

    - 先頭 import に `import uuid` は既に含まれる
    - `job_id = str(uuid.uuid4())` (既存) の直後に:
      ```python
      correlation_id = str(uuid.uuid4())  # Phase 31 D-08: trace_id source
      ```
    - `await arq_redis.enqueue_job("process_chat", ...)` の kwargs に以下 2 つを追加:
      ```python
      correlation_id=correlation_id,
      app_id="canvas",
      ```

    変更前後の enqueue_job:
    ```python
    # BEFORE (L65-76):
    await arq_redis.enqueue_job(
        "process_chat",
        job_id=job_id,
        thread_id="",
        prompt="",
        github_token=github_token,
        reply_to={"type": "web", "job_id": job_id},
        task_type="iframe_app_api",
        github_login=github_login,
        rpc_method=body.method,
        rpc_params=body.params or {},
    )

    # AFTER:
    await arq_redis.enqueue_job(
        "process_chat",
        job_id=job_id,
        thread_id="",
        prompt="",
        github_token=github_token,
        reply_to={"type": "web", "job_id": job_id},
        task_type="iframe_app_api",
        github_login=github_login,
        rpc_method=body.method,
        rpc_params=body.params or {},
        correlation_id=correlation_id,  # Phase 31: trace_id
        app_id="canvas",                # Phase 31: D-09 共通 attribute
    )
    ```

    既存テストが壊れないよう、arq_redis モックの assert_called_with が kwargs 増加を許容するパターンかを確認する。既存 `tests/test_iframe_rpc.py` があれば patch 修正が必要。
  </action>
  <verify>
    <automated>grep -q 'correlation_id = str(uuid.uuid4())' app/api/routes/iframe_rpc.py && grep -q 'correlation_id=correlation_id' app/api/routes/iframe_rpc.py && grep -q 'app_id="canvas"' app/api/routes/iframe_rpc.py && uv run pytest tests/ -x --tb=short -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    iframe_rpc.py が correlation_id UUID4 を生成し enqueue_job payload に `correlation_id` / `app_id="canvas"` 2 フィールドを追加している。既存テスト非破壊。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: iframe_rpc_handler.py に request span + tool_call span (db_query / AI) を実装して GREEN にする</name>
  <files>app/jobs/handlers/iframe_rpc_handler.py</files>
  <read_first>
    - app/jobs/handlers/iframe_rpc_handler.py (現行 L1-179)
    - app/observability/trace.py (Plan 02)
    - app/observability/config.py (Plan 02)
    - app/orchestrator/context.py (RPCContext)
    - tests/test_iframe_rpc_trace.py (Task 1 の契約)
    - .planning/phases/31-agent-mcp-observability/31-RESEARCH.md §3.3 (実装スケッチ)
    - .planning/phases/31-agent-mcp-observability/31-PATTERNS.md (iframe_rpc_handler 修正セクション)
  </read_first>
  <action>
    Step 1: imports 追加:
    ```python
    from app.orchestrator.context import RPCContext
    from app.observability.trace import trace_span
    from app.observability.config import get_args_max_chars, get_result_max_chars
    ```

    Step 2: `IframeRpcHandler.handle()` 全体を request span で wrap:
    ```python
    async def handle(self, ctx: dict, job: dict) -> dict:
        job_id = job["job_id"]
        reply_to = job["reply_to"]
        method = job.get("rpc_method", "")
        params = job.get("rpc_params") or {}
        github_login = job.get("github_login", "unknown")
        correlation_id = job.get("correlation_id", "")  # route 側で付与 (Task 2)
        app_id = job.get("app_id", "canvas")
        job_store = ctx["job_store"]
        notifier = build_notifier(reply_to, job_store)

        # Phase 31 D-08: RPCContext 構築 (他 handler と揃える)
        context = RPCContext.from_http(user_id=github_login, app_id=app_id, thread_id="")

        async with trace_span(
            "request",
            trace_id=correlation_id or context.correlation_id,
            attributes={
                "user_id": github_login,
                "app_id": app_id,
                "thread_id": "",
                "handler": "iframe_rpc",
                "rpc_method": method,
            },
        ) as req_span:
            try:
                if method == "QUERY":
                    result = await self._handle_query(ctx, params, correlation_id or context.correlation_id, github_login)
                elif method == "AI":
                    result = await self._handle_ai(job, params, correlation_id or context.correlation_id, github_login)
                else:
                    result = {"result": False, "error": f"Unknown method: {method}"}
                    req_span.set_status("ERROR", f"unknown_method={method}")

                await job_store.save_result(job_id, json.dumps(result, default=_json_default))
                await notifier.done()
            except Exception as e:
                req_span.set_status("ERROR", str(e)[:200])
                await job_store.save_result(job_id, json.dumps({"result": False, "error": str(e)}))
                await notifier.done()

            return {"job_id": job_id, "status": "done"}
    ```

    注: `_handle_query` / `_handle_ai` のシグネチャに trace_id と user_id を追加する。

    Step 3: `_handle_query` に tool_call span を追加:
    ```python
    async def _handle_query(self, ctx: dict, params: dict, trace_id: str, user_id: str) -> dict:
        pool_name = params.get("pool_name", "")
        sql = params.get("sql", "")
        # 旧 `logger.info("iframe-rpc QUERY user=%s pool=%s", user, pool_name)` は削除

        mcp_tools = ctx.get("mcp_tools") or []
        tool = next((t for t in mcp_tools if getattr(t, "name", None) == "db_query"), None)
        if tool is None:
            return {"result": False, "error": "db_query tool unavailable (MCP DEGRADED)"}

        args_json = json.dumps({"sql": sql, "pool_name": pool_name}, ensure_ascii=False)
        args_max = get_args_max_chars()
        result_max = get_result_max_chars()

        async with trace_span(
            "tool_call",
            trace_id=trace_id,
            attributes={
                "user_id": user_id,
                "app_id": "canvas",
                "thread_id": "",
                "agent_name": "iframe_rpc",
                "model_name": "",
                "tool_name": "db_query",
                "args_bytes": len(args_json.encode("utf-8")),
                "args_prefix": args_json[:args_max],
                "privileged": False,  # db_query は sandbox_exposed: true
                "pool_name": pool_name,  # iframe_rpc 固有の運用 attribute
            },
        ) as span:
            try:
                out = await tool.ainvoke({"sql": sql, "pool_name": pool_name})
            except Exception as e:
                span.set_status("ERROR", str(e)[:200])
                span.set_attribute("success", False)
                return {"result": False, "error": f"db_query invocation failed: {e}"}

            # langchain-mcp-adapters の content list 正規化 (既存ロジック L134-144 維持)
            if isinstance(out, list):
                text = next((item["text"] for item in out if isinstance(item, dict) and item.get("type") == "text"), None)
                if text is None:
                    span.set_status("ERROR", "empty_content_list")
                    span.set_attribute("success", False)
                    return {"result": False, "error": "db_query returned empty content list"}
                try:
                    out = json.loads(text)
                except json.JSONDecodeError:
                    span.set_status("ERROR", "unparseable_content")
                    span.set_attribute("success", False)
                    return {"result": False, "error": f"db_query returned unparseable content: {text!r}"}

            out_json = json.dumps(out, ensure_ascii=False, default=str)
            span.set_attribute("result_bytes", len(out_json.encode("utf-8")))
            span.set_attribute("result_prefix", out_json[:result_max])

            if not isinstance(out, dict):
                span.set_status("ERROR", f"unexpected_type={type(out).__name__}")
                span.set_attribute("success", False)
                return {"result": False, "error": f"db_query returned unexpected type: {type(out).__name__}"}
            if "error" in out:
                span.set_status("ERROR", str(out["error"])[:200])
                span.set_attribute("success", False)
                return {"result": False, "error": out["error"]}

            span.set_attribute("success", True)
            span.set_attribute("row_count", len(out.get("rows", [])))
            return {"result": True, "rows": out.get("rows", [])}
    ```

    Step 4: `_handle_ai` に tool_call span を追加:
    ```python
    async def _handle_ai(self, job: dict, params: dict, trace_id: str, user_id: str) -> dict:
        raw_model = params.get("model")
        prompt = params.get("prompt", "")
        github_token = job.get("github_token", "")

        try:
            model = resolve_model(raw_model)
        except ValueError as e:
            return {"result": False, "error": str(e)}

        args_json = json.dumps({"model": model, "prompt": prompt}, ensure_ascii=False)
        args_max = get_args_max_chars()
        result_max = get_result_max_chars()

        async with trace_span(
            "tool_call",
            trace_id=trace_id,
            attributes={
                "user_id": user_id,
                "app_id": "canvas",
                "thread_id": "",
                "agent_name": "iframe_rpc",
                "model_name": model,
                "tool_name": "ai",
                "args_bytes": len(args_json.encode("utf-8")),
                "args_prefix": args_json[:args_max],
                "privileged": False,
            },
        ) as span:
            llm = ChatCopilot(github_token=github_token, model=model)
            try:
                result = await llm.ainvoke([HumanMessage(content=prompt)])
                content = result.content
                span.set_attribute("result_bytes", len(str(content).encode("utf-8")))
                span.set_attribute("result_prefix", str(content)[:result_max])
                span.set_attribute("success", True)
                return {"result": True, "responseText": content}
            except Exception as e:
                span.set_status("ERROR", str(e)[:200])
                span.set_attribute("success", False)
                return {"result": False, "error": str(e)}
            finally:
                await llm.close()
    ```

    Step 5: 旧フリーテキストログ `logger.info("iframe-rpc QUERY user=%s pool=%s", user, pool_name)` (L120) を削除。`user` 変数は削除、`github_login` を span attribute 経由で参照する。

    Step 6: `uv run pytest tests/test_iframe_rpc_trace.py -x` を実行し全 green 確認。失敗があれば調整。
  </action>
  <verify>
    <automated>uv run pytest tests/test_iframe_rpc_trace.py -x --tb=short && grep -q 'async with trace_span' app/jobs/handlers/iframe_rpc_handler.py && grep -q 'RPCContext' app/jobs/handlers/iframe_rpc_handler.py && ! grep -q 'iframe-rpc QUERY user=' app/jobs/handlers/iframe_rpc_handler.py</automated>
  </verify>
  <done>
    iframe_rpc_handler.py に request span + 2 経路の tool_call span 追加、旧フリーテキストログ削除、6 test case 全 green、既存 iframe_rpc 関連テスト非破壊。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iframe (Canvas) → parent-bridge.js → POST /api/iframe-rpc | JWT Cookie 経由認証、信頼境界 |
| /api/iframe-rpc → arq job dict | route 内で RPCContext / correlation_id を強制付与（route 側で trust 済） |
| arq job → IframeRpcHandler.handle() | job payload は trusted enqueue（attacker による書き換え経路なし） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Information Disclosure | SQL や AI prompt が span attribute に載る | mitigate | `get_args_max_chars()` 制御で truncate (default 500)。pool_name / row_count は運用上必要な抜粋 |
| T-31-02 | Information Disclosure | AI response content が result_prefix に含まれる | mitigate | `get_result_max_chars()` default 1000 で truncate、完全文は LangGraph checkpointer には残らない（iframe-rpc は 1 回限り）ため docker logs のみに制限 |
| T-31-04 | Tampering | correlation_id 偽装 | accept | route 側で `str(uuid.uuid4())` により強制生成。body からは受け取らない設計で route-level で trust 確定 |
</threat_model>

<verification>
- `uv run pytest tests/test_iframe_rpc_trace.py -x` 全 green (6+ cases)
- `uv run pytest tests/ -x` 全 green (既存テスト非破壊)
- `grep -q 'correlation_id = str(uuid.uuid4())' app/api/routes/iframe_rpc.py`
- `grep -q 'app_id="canvas"' app/api/routes/iframe_rpc.py`
- `grep -c 'async with trace_span' app/jobs/handlers/iframe_rpc_handler.py` で 3 以上 (request + QUERY + AI)
- `! grep -q 'iframe-rpc QUERY user=%s pool=%s' app/jobs/handlers/iframe_rpc_handler.py`
- `grep -q 'RPCContext' app/jobs/handlers/iframe_rpc_handler.py`
</verification>

<success_criteria>
- [ ] iframe_rpc route が correlation_id を UUID4 生成して job payload に追加
- [ ] iframe_rpc route が app_id="canvas" を job payload に追加
- [ ] IframeRpcHandler.handle() に request span (handler="iframe_rpc") 追加
- [ ] _handle_query (db_query) が tool_call span で wrap (privileged=false)
- [ ] _handle_ai (AI) が tool_call span で wrap (tool_name="ai", model_name=<resolved>)
- [ ] 旧フリーテキスト log `iframe-rpc QUERY user=... pool=...` が削除されている
- [ ] tests/test_iframe_rpc_trace.py 6+ cases green
- [ ] 既存テストスイート非破壊
</success_criteria>

<output>
After completion, create `.planning/phases/31-agent-mcp-observability/31-05-SUMMARY.md` with:
- iframe_rpc route の enqueue_job 追加 field (correlation_id, app_id)
- handle() の request span attributes (handler, rpc_method)
- 2 経路の tool_call span (db_query, ai) の attribute 仕様
- 旧フリーテキストログの廃止に伴う運用変更点（新 span attribute でどう代替するか）
- Plan 06 の CLI recipes に「iframe_rpc 経路で privileged=false + app_id=canvas を組み合わせたクエリ例」を載せる準備
</output>
