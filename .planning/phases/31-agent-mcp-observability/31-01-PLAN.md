---
phase: 31
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - docs/phase-31-reasoning-token-spike.md
  - scripts/spike_copilot_reasoning.py
autonomous: false
requirements:
  - D-14
  - D-15
risk: low
threat_ids: []
tags:
  - observability
  - copilot-sdk
  - spike

must_haves:
  truths:
    - "Copilot SDK 0.2.0 の ASSISTANT_USAGE / ASSISTANT_REASONING イベントが実際のチャット要求で発火するかが確認できている"
    - "モデル別の露出マトリクスが docs/phase-31-reasoning-token-spike.md に記録されている"
    - "SubAgent span に載せる attribute 名（input_tokens / output_tokens / cache_read_tokens / cache_write_tokens / reasoning_chars / reasoning_prefix のうち取れたもの）が確定している"
  artifacts:
    - path: "docs/phase-31-reasoning-token-spike.md"
      provides: "モデル別の reasoning / usage 露出判定結果 + span schema 反映判断"
      min_lines: 40
    - path: "scripts/spike_copilot_reasoning.py"
      provides: "SessionEventType 捕捉スクリプト（docker exec で実行）"
      exports: ["main"]
  key_links:
    - from: "scripts/spike_copilot_reasoning.py"
      to: "copilot.generated.session_events.SessionEventType"
      via: "SDK import"
      pattern: "from copilot.generated.session_events import SessionEventType"
    - from: "docs/phase-31-reasoning-token-spike.md"
      to: "Plan 02 SubAgent span attribute set"
      via: "span schema 反映判断セクション"
      pattern: "span schema への反映"
---

<objective>
Copilot SDK 0.2.0 の `ASSISTANT_USAGE` / `ASSISTANT_REASONING` イベントが実チャット要求で露出するかを最小スパイクで確認し、Phase 31 の SubAgent span attribute に載せる field を確定する。

Purpose: D-15 により「取れる場合だけ span attribute に追加」と規定されているため、後続 Plan（Plan 04 の SubAgent span emit）が実装方針を確定するための前提情報を得る。
Output: `docs/phase-31-reasoning-token-spike.md`（結果マトリクス + 採用 attribute 一覧）、`scripts/spike_copilot_reasoning.py`（調査用一時スクリプト）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-agent-mcp-observability/31-CONTEXT.md
@.planning/phases/31-agent-mcp-observability/31-RESEARCH.md
@.planning/phases/31-agent-mcp-observability/31-PATTERNS.md
@.planning/patterns.md
@docs/adr/INDEX.md

<interfaces>
<!-- Copilot SDK 0.2.0 の reasoning / usage 露出 field（RESEARCH §5.1 で VERIFIED） -->
<!-- 実装者は以下の field を直接参照する（import 経路は `copilot.generated.session_events`） -->

SessionEventType enum (抜粋):
  ASSISTANT_REASONING          — reasoning 完了イベント
  ASSISTANT_REASONING_DELTA    — reasoning streaming delta
  ASSISTANT_USAGE              — token usage イベント
  ASSISTANT_MESSAGE            — assistant response
  ASSISTANT_TURN_START / END   — turn boundary

Data クラス関連フィールド:
  reasoning_effort: str | None
  reasoning_id: str | None
  reasoning_text: str | None
  reasoning_opaque: str | None
  input_tokens: float | None
  output_tokens: float | None
  cache_read_tokens: float | None
  cache_write_tokens: float | None
  conversation_tokens: float | None
  system_tokens: float | None
  tool_definitions_tokens: float | None
  copilot_usage.total_nano_aiu: float
  copilot_usage.token_details: list[TokenDetail]

既存の session.on(callback) 登録パターン:
  `app/providers/copilot.py:_astream` L134-207 が `session.on(on_event)` で
  ASSISTANT_MESSAGE_DELTA / ASSISTANT_MESSAGE / SESSION_IDLE を監視している。
  同じ手法で ASSISTANT_USAGE / ASSISTANT_REASONING を捕捉可能。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copilot SDK スパイクスクリプトを作成して複数モデルで実行し docker logs を採取する</name>
  <files>scripts/spike_copilot_reasoning.py</files>
  <read_first>
    - scripts/spike_copilot_reasoning.py (新規作成対象、存在確認のみ)
    - scripts/probe_sdk_events.py (既存 SDK プローブのテンプレート参考)
    - app/providers/copilot.py (session.on パターンと既存 _astream L134-207)
    - .planning/phases/31-agent-mcp-observability/31-RESEARCH.md §5.1, §5.2
    - .planning/phases/31-agent-mcp-observability/31-PATTERNS.md (docs/phase-31-reasoning-token-spike.md セクション)
  </read_first>
  <action>
    `scripts/spike_copilot_reasoning.py` を `#!/usr/bin/env python3` shebang + `from __future__ import annotations` で新規作成。`scripts/probe_sdk_events.py` の shebang + main() return int + `if __name__ == "__main__": sys.exit(main())` パターンを踏襲する。

    スクリプト仕様:
    - `argparse` で `--model` (default: claude-haiku-4-5-20251001) `--prompt` (default: "東京の今日の天気を短く教えて") を受け取る
    - `app.auth.manager.CopilotAuthManager` で token をロード、`app.providers.copilot.ChatCopilot(github_token=token, model=<model>)` を生成
    - `ChatCopilot._astream` と同様に session.on(on_event) で以下を capture:
      - `ASSISTANT_USAGE` → `input_tokens` / `output_tokens` / `cache_read_tokens` / `cache_write_tokens` / `conversation_tokens` / `system_tokens` / `tool_definitions_tokens` を dict に記録（`getattr(data, "<name>", None)` で None 許容）
      - `ASSISTANT_REASONING` → `reasoning_text` / `reasoning_id` / `reasoning_opaque` を list に append
      - `ASSISTANT_REASONING_DELTA` → delta を別 list に append してイベント数だけ数える
    - `llm.ainvoke([HumanMessage(content=prompt)])` を呼び出して結果を待つ
    - 終了時に `logger.info(json.dumps({"event":"spike-usage","model":model,"usage":captured_usage,"reasoning_count":len(captured_reasoning),"reasoning_total_chars":sum(len(t) for t in captured_reasoning),"reasoning_delta_count":len(captured_reasoning_deltas)}, ensure_ascii=False))` で 1 行 JSON を stdout に出す
    - `finally` で `await llm.close()` を呼ぶ

    検証実行（docker compose 環境で手動確認）:
    ```bash
    docker compose up -d
    for MODEL in claude-haiku-4-5-20251001 claude-sonnet-4-6 gpt-4.1; do
      docker compose exec -T worker uv run python /app/scripts/spike_copilot_reasoning.py --model "$MODEL" --prompt "東京の今日の天気を短く教えて"
    done
    docker compose logs worker 2>&1 | grep '"event":"spike-usage"' > /tmp/phase-31-spike.jsonl
    cat /tmp/phase-31-spike.jsonl
    ```

    スクリプトは Plan 完了後に残す（/scripts ディレクトリ置きっぱなし）。将来の SDK upgrade 時に再実行して回帰確認できる。
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('scripts/spike_copilot_reasoning.py').read()); print('OK')" && grep -q 'ASSISTANT_USAGE' scripts/spike_copilot_reasoning.py && grep -q 'ASSISTANT_REASONING' scripts/spike_copilot_reasoning.py && grep -q "__name__ == " scripts/spike_copilot_reasoning.py</automated>
  </verify>
  <done>
    `scripts/spike_copilot_reasoning.py` が Python として syntax valid、ASSISTANT_USAGE / ASSISTANT_REASONING の 2 イベント種別を session.on() で捕捉し、最後に 1 行 JSON ログを stdout に吐く実装になっている。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 実モデルで spike を実行して結果マトリクスを作成し docs/phase-31-reasoning-token-spike.md に記録する</name>
  <files>(human verification — no direct file target)</files>
  <action>
    Task 1 で作成したスパイクスクリプトを複数モデルに対して実行し、どのモデルで reasoning / usage イベントが露出するか、実値をまとめたマトリクスを `docs/phase-31-reasoning-token-spike.md` に記録。後続 Plan が SubAgent span に載せる attribute 名を確定する根拠資料となる。

    実施手順:
    1. Chromium リモートデバッグ起動不要（spike は docker compose 内で完結）
    2. docker compose 起動確認:
       ```bash
       docker compose ps | grep -E 'api|worker'
       ```
       `api` と `worker` が Up であることを確認。`Up` でない場合は `docker compose up -d` で起動。
    3. Device Flow 認証済みかを確認:
       ```bash
       docker compose exec -T worker ls -la /root/.copilot_sdk/token.enc
       ```
       ファイルが存在しない場合は Web UI (http://localhost:5173/orochi/) で Device Flow 認証を先に完了する。
    4. 以下コマンドで 3 モデルに対して spike を実行:
       ```bash
       for MODEL in claude-haiku-4-5-20251001 claude-sonnet-4-6 gpt-4.1; do
         docker compose exec -T worker uv run python /app/scripts/spike_copilot_reasoning.py --model "$MODEL" --prompt "東京の今日の天気を短く教えて" 2>&1 | tee -a /tmp/phase-31-spike.log
       done
       ```
    5. 3 モデル分すべての stdout に `{"event":"spike-usage","model":...}` の JSON 行が出力されていることを確認
    6. `docs/phase-31-reasoning-token-spike.md` を新規作成して以下の節を含める:
       - `# Phase 31 Spike: Copilot SDK reasoning / thinking token 露出調査`
       - `## 目的` — D-15 の引用と調査動機
       - `## 実施手順` — 実行コマンド再現可能な形で記載
       - `## 結果` — 以下マトリクスを埋める:
         | model | ASSISTANT_USAGE 露出 | input_tokens | output_tokens | cache_read_tokens | cache_write_tokens | ASSISTANT_REASONING 露出 | reasoning_chars | reasoning_delta_count |
         |-------|---------------------|--------------|---------------|-------------------|--------------------|-----------------|-----------------|-------------|
         | claude-haiku-4-5-20251001 | ... | ... | ... | ... | ... | ... | ... | ... |
         | claude-sonnet-4-6 | ... | ... | ... | ... | ... | ... | ... | ... |
         | gpt-4.1 | ... | ... | ... | ... | ... | ... | ... | ... |
       - `## span schema への反映判断` — 以下を明記:
         - 採用する attribute name 一覧（例: `input_tokens`, `output_tokens`, `cache_read_tokens`, `cache_write_tokens`）
         - reasoning_text / reasoning_opaque の扱い（全量 NG / 200 字 prefix / 文字数のみ / 載せない のいずれか）
         - 露出しなかった field は Deferred として別記
       - `## 結論` — Phase 31 Plan 04 で実装する attribute 名の最終確定リスト
    7. ファイルの存在確認:
       ```bash
       test -f docs/phase-31-reasoning-token-spike.md && wc -l docs/phase-31-reasoning-token-spike.md
       ```
       40 行以上あることを確認
    8. 結果マトリクスの「span schema への反映判断」を人間が読み、Plan 04 で実装すべき attribute set に論理的欠陥がないかを確認

    期待: 3 モデルすべて実行成功（最低でも Haiku で usage が取れる）。reasoning が 3 モデルで null ならそれはそれで OK（Phase 31 scope から reasoning を除外する判断材料になる）。

    完了シグナル: 「承認」または問題点を記載して再実行を依頼
  </action>
  <verify>
    <automated>echo "Human-verify checkpoint — confirm manually"</automated>
  </verify>
  <done>
    ユーザーが「「承認」または問題点を記載して再実行を依頼」で承認、または問題点の修正要請が行われ次ステップへ進める状態になっている
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Copilot API → SDK → script | SDK 側の新規イベント field に信頼境界なし（自前プロセス内で閉じる） |
| stdout → docker logs | 運用者以外へ stdout 漏洩リスクなし（docker logs は IAM 前提） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Tampering | spike スクリプト | accept | spike は一時的な調査コード、任意の trace_id も emit 範囲外。stdout-only で attacker 書き込み経路なし。プロダクションでは実行しない |
| T-31-01 | Information Disclosure | spike log (`reasoning_text`) | mitigate | `docs/phase-31-reasoning-token-spike.md` に記載する reasoning 例は汎用プロンプト（天気）で生成し、個人情報を含まない |
</threat_model>

<verification>
- `scripts/spike_copilot_reasoning.py` が Python syntax valid
- 3 モデル（少なくとも 2 モデル）で spike 実行が完了し、`{"event":"spike-usage"}` ログが docker logs に出力された
- `docs/phase-31-reasoning-token-spike.md` が作成され、モデル別マトリクス + span schema 反映判断を含む
- 後続 Plan（特に Plan 04）が参照する「Phase 31 で SubAgent span に載せる attribute 名リスト」が明示的に記載されている
</verification>

<success_criteria>
- [ ] `scripts/spike_copilot_reasoning.py` 存在・syntax valid
- [ ] `docs/phase-31-reasoning-token-spike.md` 40 行以上、マトリクス表 + 結論セクションあり
- [ ] 結論セクションに Plan 04 で採用する具体的な attribute name list（例: `input_tokens`, `output_tokens`, `cache_read_tokens`, `cache_write_tokens`）が列挙されている
- [ ] 露出しなかった field は Deferred として明示
</success_criteria>

<output>
After completion, create `.planning/phases/31-agent-mcp-observability/31-01-SUMMARY.md` with:
- 実際に実行したコマンドと結果
- spike 結果マトリクス（Markdown テーブル貼付）
- Plan 04 への引き継ぎ事項（採用 attribute name list）
- 残存課題（Deferred なもの）
</output>
