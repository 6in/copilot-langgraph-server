---
phase: 31
plan: 07
type: execute
wave: 5
depends_on:
  - 31-02
  - 31-04
  - 31-05
  - 31-06
files_modified:
  - app/api/main.py
  - tests/test_api_chat.py
  - docs/adr/0045-phase-31-observability-jsonl.md
  - .planning/patterns.md
autonomous: false
requirements:
  - D-01
  - D-02
  - D-08
  - D-10
  - D-15
  - D-17
risk: medium
threat_ids:
  - T-31-06
tags:
  - observability
  - schema-cleanup
  - adr
  - patterns

must_haves:
  truths:
    - "app/api/main.py の CREATE TABLE audit_log と 2 つの CREATE INDEX audit_log_*_idx が削除されている"
    - "app/api/main.py L52 コメントから `, audit_log` 部分が消えている"
    - "grep で `audit_log` が app/ 以下のコメント以外から完全消去されている (! grep -q audit_log app/api/main.py)"
    - "docs/adr/0045-phase-31-observability-jsonl.md が新規作成されて Phase 31 の設計判断を記録している"
    - "docs/adr/INDEX.md が pre-commit hook により再生成されている (新 ADR 登録済)"
    - ".planning/patterns.md に Phase 31 関連パターン (1-2 エントリ、Data・Persistence または LangGraph・Graph カテゴリ) が手動追記されている"
    - ".planning/adr-categories.yaml に 0045 のカテゴリマッピングが追加されている"
    - "app 起動で DDL 削除後のエラー発生なし (本 Plan では SUMMARY に手動 DROP 手順を記載する task のみ、コード変更は DDL 削除のみ)"
  artifacts:
    - path: "app/api/main.py"
      provides: "audit_log DDL / INDEX 削除、コメント修正"
      min_lines: 0
    - path: "docs/adr/0045-phase-31-observability-jsonl.md"
      provides: "Phase 31 observability 設計の ADR"
      min_lines: 60
    - path: ".planning/patterns.md"
      provides: "Phase 31 関連パターン追記"
    - path: ".planning/adr-categories.yaml"
      provides: "ADR 0045 カテゴリマッピング"
  key_links:
    - from: "docs/adr/0045-phase-31-observability-jsonl.md"
      to: "docs/adr/INDEX.md"
      via: "pre-commit hook (scripts/generate_adr_index.py)"
      pattern: "0045-phase-31"
    - from: ".planning/patterns.md"
      to: "docs/adr/0045-phase-31-observability-jsonl.md"
      via: "手動追記エントリ内のリンク"
      pattern: "ADR-0045"
---

<objective>
Phase 31 D-02 に基づく `audit_log` テーブル DDL の削除と、Phase 31 の設計判断を記録する ADR の作成、`.planning/patterns.md` への手動追記を実施する。`app/api/main.py` のコード変更は DDL + INDEX の純粋削除のみで、既存運用環境への DB 破壊的影響は発生させない（手動 DROP 手順は SUMMARY.md に記載）。

Purpose: D-02 (audit_log 退役) と CLAUDE.md の ADR 追加時 patterns.md 手動追記ルール (D-15) を同時に満たす。これにより Phase 31 の設計判断が ADR カタログと patterns.md に公式に記録され、後続フェーズの GSD discuss が canonical_refs 経由で自動参照できる状態になる。
Output: `app/api/main.py` (DDL 削除)、`tests/test_api_chat.py` のコメント更新、`docs/adr/0045-*.md` 新規、`.planning/patterns.md` 追記、`.planning/adr-categories.yaml` 更新、自動生成される `docs/adr/INDEX.md`。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-agent-mcp-observability/31-CONTEXT.md
@.planning/phases/31-agent-mcp-observability/31-RESEARCH.md
@.planning/phases/31-agent-mcp-observability/31-PATTERNS.md
@.planning/phases/31-agent-mcp-observability/31-04-SUMMARY.md
@.planning/phases/31-agent-mcp-observability/31-05-SUMMARY.md
@.planning/phases/31-agent-mcp-observability/31-06-SUMMARY.md
@.planning/patterns.md
@docs/adr/INDEX.md
@docs/adr/0044-mcp-tool-catalog-single-source-of-truth.md
@CLAUDE.md

<interfaces>
<!-- 削除対象 DDL (app/api/main.py L52 コメント + L105-124) -->
現行コード:
  L52  (applications, threads, audit_log).
  L105-116 CREATE TABLE IF NOT EXISTS audit_log (
      id BIGSERIAL PRIMARY KEY,
      github_login TEXT NOT NULL,
      app_id TEXT REFERENCES applications(app_id),
      thread_id TEXT,
      action TEXT NOT NULL,
      metadata JSONB,
      created_at TIMESTAMPTZ DEFAULT now()
  )
  L118-124 CREATE INDEX IF NOT EXISTS audit_log_github_login_idx ON audit_log(github_login)
           CREATE INDEX IF NOT EXISTS audit_log_created_at_idx ON audit_log(created_at)

<!-- 既存 ADR 番号体系 (docs/adr/) -->
最新: 0044-mcp-tool-catalog-single-source-of-truth.md
新規: 0045-phase-31-observability-jsonl.md

<!-- patterns.md 7 カテゴリ (CLAUDE.md 記載) -->
Auth / LangGraph・Graph / MCP・Tools / Worker・Jobs / Frontend・UI / Infra・Deploy / Data・Persistence

Phase 31 は Data・Persistence (JSONL stdout 設計) + LangGraph・Graph (ContextVar parent_span_id 伝搬) にまたがる

<!-- adr-categories.yaml (scripts/generate_adr_index.py のソース) -->
- scripts/generate_adr_index.py が .planning/adr-categories.yaml を読んで docs/adr/INDEX.md を生成
- 新 ADR は yaml にカテゴリ登録必須 (CLAUDE.md 記載)
- pre-commit hook (scripts/install-hooks.sh) が ADR 追加時に INDEX.md を自動再生成
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: app/api/main.py から audit_log DDL + INDEX を削除しテストコメント修正</name>
  <files>app/api/main.py, tests/test_api_chat.py</files>
  <read_first>
    - app/api/main.py L40-130 (lifespan の schema migration ブロック全体)
    - tests/test_api_chat.py (L100 のコメントを確認)
    - .planning/phases/31-agent-mcp-observability/31-RESEARCH.md §8 (影響範囲 VERIFIED)
    - .planning/phases/31-agent-mcp-observability/31-PATTERNS.md (app/api/main.py セクション)
  </read_first>
  <action>
    Step 1: `app/api/main.py` を編集:

    - L52 のコメント変更:
      ```python
      # BEFORE:
      # Schema migration: replace thread_labels with normalized schema
      # (applications, threads, audit_log).
      # AFTER:
      # Schema migration: replace thread_labels with normalized schema
      # (applications, threads). Phase 31: audit_log DDL removed —
      # trace is emitted as OTEL span-like JSONL to stdout (see app/observability/trace.py).
      ```

    - L105-124 を丸ごと削除 (次の `# --- Phase 15: Gem + Canvas tables ---` コメントと接続するように):
      ```python
      # 削除する行範囲:
      # L105 # 5. Audit log (schema only — no write logic added here)
      # L106-116 await conn.execute("""CREATE TABLE IF NOT EXISTS audit_log (...)""")
      # L118-124 2 つの CREATE INDEX
      ```

      threads テーブル作成 (L94-103) と Phase 15 Gem テーブル作成 (L126〜) の間がスムーズにつながるように、L105-124 の 20 行分を完全削除。コメント「# 5. Audit log...」と「# 6. Indexes on audit_log」も一緒に削除。

    Step 2: `tests/test_api_chat.py:100` のコメント更新。`grep -n "audit_log" tests/test_api_chat.py` で該当箇所を特定し:
      ```python
      # BEFORE: # Phase 10: Tests for normalized schema (applications/threads/audit_log)
      # AFTER:  # Phase 10: Tests for normalized schema (applications/threads). audit_log removed in Phase 31.
      ```

    Step 3: grep で `audit_log` が app/ 配下から完全消去されていることを確認:
    ```bash
    grep -rn "audit_log" app/  # → 0 件であるべき
    grep -rn "audit_log" tests/  # → コメントのみ（削除後）
    ```

    コメント除去後、app/ 配下には 0 件、tests/test_api_chat.py にはコメント説明が残るが `audit_log` 文字列はコメント内だけなので機能に影響なし。

    Step 4: 非破壊確認のため既存テストを実行:
    ```bash
    uv run pytest tests/ -x --tb=short
    ```
    全 green を確認。
  </action>
  <verify>
    <automated>! grep -q "CREATE TABLE IF NOT EXISTS audit_log" app/api/main.py && ! grep -q "CREATE INDEX.*audit_log" app/api/main.py && ! grep -q "audit_log" app/api/main.py && uv run pytest tests/ -x --tb=short -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    app/api/main.py から audit_log DDL + INDEX + コメント (`, audit_log`) が完全削除され、grep -q audit_log app/api/main.py が 0 件。既存テストスイート全 green。
  </done>
</task>

<task type="auto">
  <name>Task 2: docs/adr/0045-phase-31-observability-jsonl.md ADR を新規作成</name>
  <files>docs/adr/0045-phase-31-observability-jsonl.md, .planning/adr-categories.yaml</files>
  <read_first>
    - docs/adr/INDEX.md (既存 ADR カテゴリ俯瞰)
    - docs/adr/0044-mcp-tool-catalog-single-source-of-truth.md (書式参考、直近 ADR)
    - docs/adr/0024-mcp-tool-catalog-validation.md (sandbox_exposed の根拠、Phase 31 D-12 参照元)
    - docs/adr/0041-codeact-direct-execution-over-react.md (CodeAct 経路 2、Phase 31 D-10 参照元)
    - .planning/adr-categories.yaml (既存カテゴリマッピング)
    - .planning/phases/31-agent-mcp-observability/31-CONTEXT.md (D-01..D-18)
    - .planning/phases/31-agent-mcp-observability/31-RESEARCH.md (Summary + §1, §2, §3, §8)
    - CLAUDE.md (ADR 追加時 patterns.md 手動追記義務)
  </read_first>
  <action>
    Step 1: `docs/adr/0045-phase-31-observability-jsonl.md` を新規作成。既存 ADR のフォーマットを踏襲:

    ```markdown
    # ADR-0045: Phase 31 — エージェント実行・MCP ツール利用の observability 基盤 (stdout JSONL + OTEL span-like schema)

    **Date:** 2026-04-18
    **Status:** Accepted
    **Related:** ADR-0024 (sandbox_exposed フラグ), ADR-0041 (CodeAct 直接実行), ADR-0044 (MCP tool catalog single source of truth)

    ## Context

    Phase 31 で「エージェント実行（軸 A: routing / ReAct / LLM）と MCP ツール呼び出し（軸 B: ToolEnabledSubAgent / CodeActSubAgent / iframe_rpc の 3 経路）」を統一的に記録・閲覧できる observability 基盤を構築する必要があった。運用コンテキストは 200 名規模・社内利用。既存の `docker compose logs` + 構造化 JSON ログ（`app/orchestrator/graph.py` の `event: routing` パターン）をベースに、OTEL Collector / Loki / Jaeger / Tempo 等の外部集約基盤を追加せず、stdout の OTEL span-like JSONL に寄せる方針を採用した。既存の未使用 PostgreSQL テーブル `audit_log` (Phase 10 以来読み書きゼロ) は退役させる。

    ## Decision

    以下を採用する:

    1. **Writer 抽象** (`app/observability/trace.py`): `async with trace_span(operation_name, trace_id, attributes)` context manager + `ContextVar` ベースの親 span 伝搬。`logger.info(json.dumps(asdict(span_dict), ensure_ascii=False, default=str))` で stdout に 1 行 1 span を emit。新規インフラ依存ゼロ、stdlib のみ。

    2. **Span schema** (OTEL span-like): `trace_id` / `span_id` / `parent_span_id` / `operation_name` / `start_time` / `end_time` / `duration_ms` / `status_code` / `status_message` / `attributes` の 10 フィールド。`trace_id = RPCContext.correlation_id` (Phase 11 の UUID4 を再利用)。`span_id = uuid4().hex[:16]`。

    3. **3 層 span**: `request` (handler 全体) → `routing` / `sub_agent` (並列兄弟) → `tool_call`。ReAct の各 LLM turn は別 span にせず、SubAgent span の `message_count` attribute で表現。

    4. **3 経路統合 (軸 B)**: `TracedTool` wrapper (ToolEnabledSubAgent) + `execute_python` 直接 wrap (CodeActSubAgent) + `tool.ainvoke` 直接 wrap (iframe_rpc_handler) の 3 経路を同一 `tool_call` span schema で emit。`privileged` attribute は `config/mcp_tools.yaml` の `sandbox_exposed=false` 判定 (ADR-0024 の延長)。

    5. **PII / 秘匿情報の保護**: `user_input_prefix` / `llm_output_prefix` は 200 字、`args_prefix` / `result_prefix` は env var `TRACE_ARGS_MAX_CHARS` (default 500) / `TRACE_RESULT_MAX_CHARS` (default 1000) で truncate。全ツール一律制御（ツール個別の redact 設定は `config/mcp_tools.yaml` に入れない）。

    6. **既存 PostgreSQL `audit_log` テーブル退役**: `app/api/main.py` の DDL / INDEX を削除。新規 JSONL 一本化方針を明示。既存データベースの `DROP TABLE` は手動運用として SUMMARY に記載。

    7. **閲覧手段**: `scripts/trace_query.py` CLI (stdlib のみ、argparse + stdin JSONL) + `docs/trace-query-recipes.md` (jq レシピ集)。管理 UI / admin API は Phase 31 では作らない。

    ## Consequences

    ### Positive
    - 新規 infra 依存ゼロで即運用開始できる
    - 既存 `docker compose logs --follow` + jq / scripts/trace_query.py で完結
    - 将来 OTLP exporter を追加する際は writer の `_emit` を差し替えるだけで済む（D-05）
    - `audit_log` の未使用テーブル削除で DB スキーマが純化される
    - ToolEnabledSubAgent / CodeActSubAgent / iframe_rpc_handler の 3 経路すべてが統一 schema でトレースできる
    - Copilot SDK の ASSISTANT_USAGE / ASSISTANT_REASONING イベント（spike VERIFIED、Plan 01）を SubAgent span attribute に取り込めるようになった

    ### Negative
    - docker logs rotation (quick 260418-tin で設定済、max-size: 50m × max-file: 10) に依存するため、長期保存は別途 volume / external shipping が必要 (Deferred)
    - 可視化は CLI + jq のみで、時系列グラフ等は作れない (Deferred)
    - 既存環境の `audit_log` テーブルは手動で `DROP TABLE IF EXISTS audit_log CASCADE;` を運用者が実行する必要がある（コード側では削除しない、運用影響を避けるため）
    - 本番 DB で DROP 実行漏れがあると空テーブルが残り続ける（混乱の原因になる可能性）→ Plan 07 SUMMARY.md の運用手順に明記

    ### Neutral
    - `app/orchestrator/graph.py` の旧 `event: routing` / `event: routing_fallback` JSON ログは新 OTEL schema に一括置換。互換 alias は残さない (破壊的変更、docker logs 解析スクリプトを使っている運用者は recipes.md 参照で書き換え)

    ## Alternatives Considered

    - **OTEL SDK + OTLP exporter + Tempo/Jaeger**: 200 名規模に対してオーバーヘッド過大。運用負担増。D-05 で明示的に却下。
    - **PostgreSQL audit_log 書き込み復活**: JSON 永続化は stdout で足りる + DB クエリの運用知識が必要になる。D-01 で stdout JSONL に倒した。
    - **structlog**: 既存 `logger.info(json.dumps(...))` パターンと不整合。新規依存追加の価値が薄い。
    - **@trace_span デコレータ**: LangGraph node 関数の引数は `state` のみで `trace_id` を抽出する必要があるためデコレータ不向き。context manager のほうが柔軟。

    ## Links

    - Phase 31 CONTEXT: `.planning/phases/31-agent-mcp-observability/31-CONTEXT.md`
    - Phase 31 RESEARCH: `.planning/phases/31-agent-mcp-observability/31-RESEARCH.md`
    - Phase 31 PATTERNS: `.planning/phases/31-agent-mcp-observability/31-PATTERNS.md`
    - Writer 実装: `app/observability/trace.py`, `app/observability/traced_tool.py`, `app/observability/config.py`
    - CLI: `scripts/trace_query.py`
    - Docs: `docs/trace-query-recipes.md`, `docs/phase-31-reasoning-token-spike.md`
    - 関連 ADR: ADR-0024 (sandbox_exposed), ADR-0041 (CodeAct), ADR-0044 (MCP catalog SoT)
    ```

    Step 2: `.planning/adr-categories.yaml` を編集。既存の最新 ADR (0044) の次の位置に 0045 を追記:
    ```bash
    grep -n "0044" .planning/adr-categories.yaml
    tail -10 .planning/adr-categories.yaml  # 書式を確認
    ```

    既存 yaml は `adr_categories:` 配下の 1 行 1 ADR マッピング形式 (primary/secondary キー付き)。Phase 31 は **Data・Persistence** (stdout JSONL 永続化が主) + **LangGraph・Graph** (ContextVar 親 span 伝搬が副)。既存書式に従って以下の 1 行を `"0044"` の直下に追加する:

    ```yaml
    # 正しい書式例 (adr_categories: ブロック末尾、"0044" の次行):
    "0045": { primary: "Data・Persistence", secondary: "LangGraph・Graph" }
    ```

    **必ず事前に既存 adr-categories.yaml の書式 (L16-55 の既存エントリ) を確認してから追記する**（書式を崩すと generate_adr_index.py が失敗する）。

    Step 3: INDEX.md の自動再生成をテスト:
    ```bash
    python3 scripts/generate_adr_index.py
    grep -q "0045-phase-31-observability-jsonl" docs/adr/INDEX.md
    ```
    INDEX.md に新 ADR のエントリが追加されていることを確認。pre-commit hook (scripts/install-hooks.sh) がインストール済みなら commit 時に自動再生成される。

    ADR 本文の Date や Status は 2026-04-18 + Accepted で記述し、既存 ADR のフォーマットにできるだけ揃える（書式差が出ると generate_adr_index.py が日付を抽出できない可能性）。Phase 26 の [Date 正規表現を [*:\s]+ 文字クラス方式にして **Date:** と **Date**: を両立] の決定に従う。
  </action>
  <verify>
    <automated>test -f docs/adr/0045-phase-31-observability-jsonl.md && awk 'END{exit (NR<60)}' docs/adr/0045-phase-31-observability-jsonl.md && grep -q '0045' .planning/adr-categories.yaml && grep -q '0045-phase-31-observability-jsonl' docs/adr/INDEX.md</automated>
  </verify>
  <done>
    docs/adr/0045-phase-31-observability-jsonl.md が 60 行以上で作成、.planning/adr-categories.yaml に登録、docs/adr/INDEX.md 再生成済 (pre-commit hook or 手動 generate_adr_index.py 実行後)。
  </done>
</task>

<task type="auto">
  <name>Task 3: .planning/patterns.md に Phase 31 パターンを手動追記</name>
  <files>.planning/patterns.md</files>
  <read_first>
    - .planning/patterns.md (現行 7 カテゴリ構成、21 エントリ程度)
    - CLAUDE.md (ADR 追加時 patterns.md 手動追記ルール、1 エントリ 5-10 行、ADR にないパターンは載せない)
    - docs/adr/0045-phase-31-observability-jsonl.md (Task 2 で作成)
    - .planning/phases/31-agent-mcp-observability/31-PATTERNS.md (shared patterns A-F 参考)
  </read_first>
  <action>
    `.planning/patterns.md` の既存構成に従い、以下 2 エントリを追記する (CLAUDE.md: 1 エントリ 5-10 行、ADR 唯一の真実源):

    **エントリ 1 — Data・Persistence カテゴリ** (JSONL stdout 基盤):

    ```markdown
    ### Stdout 1 行 JSONL による observability 永続化

    **パターン:** エージェント実行とツール呼び出しのトレースを、OpenTelemetry SDK や外部集約基盤を追加せずに `logger.info(json.dumps(span_dict, ensure_ascii=False, default=str))` で stdout へ 1 行 1 span emit する。docker logging driver の rotation (max-size/max-file) をそのまま永続層として使う。
    **適用条件:** 社内 200 名規模、単一 docker compose クラスタ、可視化 UI 不要、CLI + jq で運用完結。
    **鍵となる実装:** `app/observability/trace.py` の `async with trace_span(...)` context manager、`ContextVar` による親 span_id 伝搬、`RPCContext.correlation_id` を `trace_id` として再利用。
    **関連 ADR:** ADR-0045（Phase 31 observability 基盤）
    ```

    **エントリ 2 — MCP・Tools カテゴリ** (TracedTool wrapper):

    ```markdown
    ### LangChain BaseTool を透過 wrap する TracedTool で tool_call span を統一

    **パターン:** MCP / LangChain BaseTool を `TracedTool(wrapped, privileged_tool_names, common_attrs_provider)` で wrap し、`ainvoke` 実行前後に `tool_call` span を自動 emit する。`privileged` attribute は `config/mcp_tools.yaml` の `sandbox_exposed=false` 集合で判定 (ADR-0024)。args/result は env var `TRACE_ARGS_MAX_CHARS` / `TRACE_RESULT_MAX_CHARS` で truncate。
    **適用条件:** ToolEnabledSubAgent の ReAct ループ（軸 B 経路 1）で tool_call 単位のトレースを取りたいとき。CodeAct (経路 2) と iframe_rpc_handler (経路 3) は直接 `async with trace_span("tool_call")` で包む。
    **鍵となる実装:** `app/observability/traced_tool.py`、`ToolNode([TracedTool(t) for t in tools])` による差し替え。
    **関連 ADR:** ADR-0045（Phase 31）、ADR-0024（sandbox_exposed）、ADR-0041（CodeAct 経路 2）
    ```

    追記位置は既存 patterns.md のカテゴリ順（Auth → LangGraph・Graph → MCP・Tools → Worker・Jobs → Frontend・UI → Infra・Deploy → Data・Persistence）を維持。**ファイル全体を読んで適切な位置に insert する（末尾追記ではない）**。

    追記前に:
    ```bash
    wc -l .planning/patterns.md  # 追記前行数
    grep -cE '^### ' .planning/patterns.md  # 追記前エントリ数
    ```
    追記後に:
    ```bash
    grep -q 'TracedTool' .planning/patterns.md
    grep -q 'Stdout 1 行 JSONL' .planning/patterns.md
    grep -q 'ADR-0045' .planning/patterns.md
    ```

    CLAUDE.md D-08: ADR にないパターンは載せない → 本 Plan で載せる 2 エントリは ADR-0045 の「Decision 1/4」に直接対応する。
  </action>
  <verify>
    <automated>grep -q 'TracedTool' .planning/patterns.md && grep -q 'Stdout 1 行 JSONL' .planning/patterns.md && grep -q 'ADR-0045' .planning/patterns.md</automated>
  </verify>
  <done>
    .planning/patterns.md に 2 つの Phase 31 パターンエントリが追記、ADR-0045 へのリンク付き、既存カテゴリ構造を維持。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 手動 DROP TABLE 実行手順を SUMMARY.md に記載するための運用確認</name>
  <files>(human verification — no direct file target)</files>
  <action>
    app/api/main.py から audit_log DDL を削除したが、既存の docker volume には依然として audit_log テーブルが残存する。Phase 31 完了後に運用者が手動で `DROP TABLE` を実行する必要があり、その手順を 31-07-SUMMARY.md と ADR に明記する必要がある。

    実施手順:
    1. 現在の postgres コンテナの audit_log テーブル状態を確認:
       ```bash
       docker compose exec -T postgres psql -U postgres -d postgres -c "\dt audit_log"
       ```
       テーブルが存在することを確認（Phase 10 以来残存）。行数は確認不要（想定 0 件）。

    2. audit_log テーブルのデータがないことを確認:
       ```bash
       docker compose exec -T postgres psql -U postgres -d postgres -c "SELECT COUNT(*) FROM audit_log;"
       ```
       0 であることを確認。0 でない場合は Phase 10 以降で誰かが書き込みを追加した可能性があるので調査必要（grep で確認済だが念のため）。

    3. **手動 DROP 実行前にユーザー承認を得る**。承認後:
       ```bash
       docker compose exec -T postgres psql -U postgres -d postgres -c "DROP TABLE IF EXISTS audit_log CASCADE;"
       docker compose exec -T postgres psql -U postgres -d postgres -c "\dt audit_log"
       ```
       テーブルが存在しないことを確認 ("Did not find any relation named" メッセージ)。

    4. アプリ再起動テスト:
       ```bash
       docker compose restart api worker
       sleep 10
       curl -sf http://localhost:8000/health
       ```
       `{"status":"ok"}` などの 200 応答を確認。lifespan 内の schema migration が audit_log 作成行を削除されていても他の CREATE TABLE に影響しないことを確認。

    5. 31-07-SUMMARY.md に以下を書く指針になる:
       - 「app/api/main.py から DDL 削除した」
       - 「本番データベースを使っている運用者は次のコマンドを手動実行する必要がある: `docker compose exec -T postgres psql -U postgres -d postgres -c "DROP TABLE IF EXISTS audit_log CASCADE;"`」
       - 「執行時、Phase 31 開発環境では本 checkpoint で実際に DROP 実行・再起動テスト完了済 (or 未実行の場合は『開発環境では DROP 未実行。運用環境 deploy 前に必ず実行』と明記)」

    期待: DROP 実行 → アプリ再起動 → /health 200。失敗した場合は別 handler が audit_log を使っている可能性 (grep 見落とし)、ロールバック手順は別 checkpoint で。

    完了シグナル: 「承認」（手動 DROP 実行含む）または「開発環境では未実行で OK、運用環境 deploy 前に実行」
  </action>
  <verify>
    <automated>echo "Human-verify checkpoint — confirm manually"</automated>
  </verify>
  <done>
    ユーザーが「「承認」（手動 DROP 実行含む）または「開発環境では未実行で OK、運用環境 deploy 前に実行」」で承認、または問題点の修正要請が行われ次ステップへ進める状態になっている
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app/api/main.py lifespan schema migration | 起動時のみ実行、外部入力なし |
| docker volume (既存 postgres-data) | Phase 31 コード変更の影響を受けない前提（DDL 削除のみ） |
| 運用者による手動 DROP コマンド | 人間の意図的操作、trust 済 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-06 | Data Destruction (operational) | 既存 DB volume の audit_log テーブル | mitigate | コード側では DROP せず（既存 volume に破壊的影響を与えない）、手動実行手順を SUMMARY.md に記載し運用者判断に委ねる（§8.3 Approach C）。grep で書き込み 0 件確認済のため手動 DROP しても他機能への影響なし（Test verify 済）|
| T-31-04 | Tampering | ADR 番号衝突 | mitigate | docs/adr/INDEX.md を pre-commit hook で再生成、.planning/adr-categories.yaml に 0045 を登録することで番号重複を自動検知できる |
</threat_model>

<verification>
- `! grep -q "CREATE TABLE.*audit_log" app/api/main.py`
- `! grep -q "CREATE INDEX.*audit_log" app/api/main.py`
- `! grep -q "audit_log" app/api/main.py` (コメントも含めて除去)
- `test -f docs/adr/0045-phase-31-observability-jsonl.md`
- `grep -q '0045' .planning/adr-categories.yaml`
- `grep -q '0045-phase-31' docs/adr/INDEX.md`
- `grep -q 'ADR-0045' .planning/patterns.md`
- `grep -q 'TracedTool' .planning/patterns.md`
- `uv run pytest tests/ -x` 全 green（既存テスト非破壊）
- 手動 DROP 実行確認（checkpoint task 完了）
</verification>

<success_criteria>
- [ ] app/api/main.py から audit_log 関連 DDL / INDEX 完全削除
- [ ] コメント L52 から `, audit_log` 削除
- [ ] tests/test_api_chat.py L100 コメント更新
- [ ] docs/adr/0045-phase-31-observability-jsonl.md 60 行以上で新規作成
- [ ] .planning/adr-categories.yaml に 0045 登録
- [ ] docs/adr/INDEX.md 再生成済、0045 エントリ含む
- [ ] .planning/patterns.md に 2 エントリ追加（Data・Persistence + MCP・Tools）
- [ ] 既存テストスイート全 green
- [ ] 手動 DROP 実行（or 未実行判断）と結果の確認完了
</success_criteria>

<output>
After completion, create `.planning/phases/31-agent-mcp-observability/31-07-SUMMARY.md` with:
- 削除した行数と diff サマリ（app/api/main.py）
- ADR-0045 のリンクと要約
- patterns.md に追加した 2 エントリの要約（カテゴリ / タイトル / ADR リンク）
- **[重要]** 運用手順: 本番 DB で手動実行するコマンド `docker compose exec -T postgres psql -U postgres -d postgres -c "DROP TABLE IF EXISTS audit_log CASCADE;"` と「deploy 前の実行が必要」である旨
- 開発環境 DROP 実行済/未実行の記録（Task 4 checkpoint の結果）
</output>
