---
phase: 31
plan: 04
type: execute
wave: 3
depends_on:
  - 31-01
  - 31-02
  - 31-03
files_modified:
  - app/orchestrator/graph.py
  - app/orchestrator/tool_agent.py
  - app/orchestrator/agent.py
  - app/orchestrator/codeact_agent.py
  - app/orchestrator/gem_agent.py
  - app/jobs/handlers/orchestrator_handler.py
  - app/jobs/handlers/langgraph_handler.py
  - app/jobs/handlers/debate_handler.py
  - tests/test_sub_agent_trace.py
autonomous: true
requirements:
  - D-06
  - D-08
  - D-09
  - D-10
  - D-12
  - D-13
  - D-14
risk: high
threat_ids:
  - T-31-01
  - T-31-02
  - T-31-03
tags:
  - observability
  - integration
  - routing
  - sub-agent
  - tool-call

must_haves:
  truths:
    - "軸 A の 3 層 span (request → routing → sub_agent → tool_call) が同じ trace_id で emit される"
    - "app/orchestrator/graph.py の event:routing / routing_fallback ログが OTEL span 形式に完全置換されている (互換 alias なし)"
    - "ToolEnabledSubAgent の ToolNode が TracedTool で wrap された tools を使う (軸 B 経路 1)"
    - "CodeActSubAgent.run の execute_python 呼び出しが tool_call span で包まれ privileged=true attribute を持つ (軸 B 経路 2)"
    - "SubAgent / ToolEnabledSubAgent / CodeActSubAgent / GemSubAgent の run() が sub_agent span で包まれる"
    - "OrchestratorHandler / LangGraphHandler / DebateHandler の handle() が request span で包まれる (trace_id = RPCContext.correlation_id)"
    - "langgraph_handler / debate_handler でも RPCContext が構築され correlation_id が trace_id として流れる"
    - "SubAgent span に Plan 01 スパイク結果に基づく token usage attribute が載る"
  artifacts:
    - path: "app/orchestrator/graph.py"
      provides: "RouterNode で trace_span(\"routing\") emit"
      contains: "trace_span"
    - path: "app/orchestrator/tool_agent.py"
      provides: "ToolEnabledSubAgent.run() を trace_span(\"sub_agent\") で wrap + TracedTool wrap"
      contains: "TracedTool"
    - path: "app/orchestrator/codeact_agent.py"
      provides: "run() を trace_span(\"sub_agent\") + execute_python 呼び出しを trace_span(\"tool_call\", privileged=true)"
      contains: "trace_span"
    - path: "app/orchestrator/agent.py"
      provides: "SubAgent.run() を trace_span(\"sub_agent\") で wrap"
      contains: "trace_span"
    - path: "app/orchestrator/gem_agent.py"
      provides: "GemSubAgent.run() を trace_span(\"sub_agent\") で wrap (agent.py 継承経由)"
    - path: "app/jobs/handlers/orchestrator_handler.py"
      provides: "handle() を trace_span(\"request\") で wrap"
      contains: "trace_span"
    - path: "app/jobs/handlers/langgraph_handler.py"
      provides: "handle() で RPCContext 構築 + trace_span(\"request\") + LangGraph 内部 LLM 呼び出しを sub_agent span で wrap"
      contains: "RPCContext"
    - path: "app/jobs/handlers/debate_handler.py"
      provides: "handle() で RPCContext 構築 + trace_span(\"request\") wrap"
      contains: "RPCContext"
    - path: "tests/test_sub_agent_trace.py"
      provides: "integration test: routing → sub_agent → tool_call span 階層が同 trace_id で出る"
      min_lines: 120
  key_links:
    - from: "app/orchestrator/tool_agent.py"
      to: "app.observability.traced_tool.TracedTool"
      via: "ToolNode([TracedTool(t, privileged_tool_names=..., common_attrs_provider=...) for t in tools])"
      pattern: "TracedTool"
    - from: "app/orchestrator/graph.py"
      to: "app.observability.trace.trace_span"
      via: "RouterNode.__call__ 全体を wrap"
      pattern: "async with trace_span"
    - from: "app/jobs/handlers/orchestrator_handler.py"
      to: "app.observability.trace.trace_span"
      via: "handle() 全体を wrap、trace_id=context.correlation_id"
      pattern: "trace_span.*request"
---

<objective>
Phase 31 軸 A（3 層 span: request/routing/sub_agent）と軸 B 経路 1（ToolEnabledSubAgent の TracedTool 差し替え）・経路 2（CodeAct の execute_python 包囲）を実装し、既存 `event:routing` 構造化ログを OTEL span 形式へ一括置換する。同時に langgraph_handler / debate_handler に `RPCContext` を追加して 4 handler 全てで trace_id を統一する。

Purpose: アプリの中核 ReAct / routing パスを observability 対象にし、Plan 06 の CLI / jq クエリが request-level で完結したトレースを表示できる土台を作る。Plan 01 の spike 結果に基づき、取れる token usage attribute を SubAgent span に載せる。
Output: 8 ファイル修正 + 1 integration test 新規。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-agent-mcp-observability/31-CONTEXT.md
@.planning/phases/31-agent-mcp-observability/31-RESEARCH.md
@.planning/phases/31-agent-mcp-observability/31-PATTERNS.md
@.planning/phases/31-agent-mcp-observability/31-VALIDATION.md
@.planning/phases/31-agent-mcp-observability/31-01-SUMMARY.md
@docs/phase-31-reasoning-token-spike.md
@app/observability/trace.py
@app/observability/traced_tool.py
@app/observability/config.py
@app/orchestrator/context.py

<interfaces>
<!-- 既存 RPCContext (Phase 11 整備済) -->
From app/orchestrator/context.py:
```python
@dataclass(frozen=True)
class RPCContext:
    user_id: str
    app_id: str = ""
    thread_id: str = ""
    correlation_id: str = field(default_factory=lambda: str(uuid.uuid4()))

    @classmethod
    def from_http(cls, user_id, app_id, thread_id) -> "RPCContext": ...
```

<!-- 既存 handler パターン -->
From app/jobs/handlers/orchestrator_handler.py (現行 L130-136):
  `context = RPCContext.from_http(user_id=github_login, app_id=app_id, thread_id=thread_id)`
  → これを trace_id 源として使う

From app/jobs/handlers/langgraph_handler.py (変更対象):
  現行は RPCContext を構築しない → Phase 31 で追加
  job dict から: job_id / thread_id / prompt / model / github_token / reply_to / github_login (fallback "unknown")
  app_id は job.get("app_id", "chat") として追加

From app/jobs/handlers/debate_handler.py (変更対象):
  現行は RPCContext を構築しない → Phase 31 で追加
  job dict から: job_id / thread_id / prompt / github_token / reply_to / github_login / participants
  app_id は "debate" ハードコード

From app/orchestrator/graph.py RouterNode.__call__ (現行 L35-102):
  `context = state.get("context")` で RPCContext 取得、`context.correlation_id` が trace_id
  3 箇所の `logger.info(json.dumps({"event": "routing", ...}))` (L47, L61, L93) と 1 箇所の warning (L86) を置換

<!-- Plan 01 spike 結果 (docs/phase-31-reasoning-token-spike.md から読み取って採用 field を決める) -->
期待採用 attribute (spike 結果次第で調整):
  - input_tokens, output_tokens, cache_read_tokens, cache_write_tokens
  - 追加候補: conversation_tokens, system_tokens, tool_definitions_tokens
  - reasoning 系: docs/phase-31-reasoning-token-spike.md の「結論」セクション記載に従う
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: graph.py の routing ログを OTEL span に一括置換 + RouterNode test を追加する</name>
  <files>app/orchestrator/graph.py, tests/test_sub_agent_trace.py</files>
  <read_first>
    - app/orchestrator/graph.py (現行 L35-102 の 4 箇所 routing log)
    - tests/test_routing_keyword.py (既存 routing test パターン)
    - app/observability/trace.py (Plan 02 writer I/F)
    - .planning/phases/31-agent-mcp-observability/31-RESEARCH.md §9.2, §9.3 Task B
    - .planning/phases/31-agent-mcp-observability/31-PATTERNS.md (graph.py 修正セクション)
  </read_first>
  <behavior>
    - Test A (test_routing_keyword_emits_span): keyword ステージで 1 件の routing span が emit、operation_name=="routing", attributes.stage=="keyword", attributes.chosen=<agent name>, trace_id=<context.correlation_id>, status_code=="OK"
    - Test B (test_routing_llm_fallback_emits_error_span): LLM が unknown agent を返すケースで span.status_code=="ERROR", status_message が "unknown_agent=" で始まり、attributes.fallback==true, attributes.stage=="llm"
    - Test C (test_routing_single_agent_stage): registry.all() が 1 個だけの場合 attributes.stage=="single"
    - Test D (test_routing_common_attributes): attributes に user_id / app_id / agent_name / user_input_prefix が含まれる (agent_name は chosen の値)
  </behavior>
  <action>
    Step 1: `tests/test_sub_agent_trace.py` を新規作成（ファイル自体は Task 1〜4 で段階的に充実させる）。冒頭に上記 Test A-D を書き、RED 確認後 Step 2 へ進む。

    Step 2: `app/orchestrator/graph.py` の `RouterNode.__call__` を以下のように書き換える（既存 L35-102 を全置換）:

    - 先頭 import 追加:
      ```python
      from app.observability.trace import trace_span
      ```
    - `async def __call__(self, state: AgentState) -> AgentState:` の先頭で:
      ```python
      context = state.get("context")
      trace_id = context.correlation_id if context else ""
      common_attrs = {
          "user_id": context.user_id if context else "unknown",
          "app_id": context.app_id if context else "",
          "thread_id": context.thread_id if context else "",
          "user_input_prefix": state["input"][:200],  # D-13: 200 字 prefix
          "candidates": [a.name for a in agents],
      }
      async with trace_span("routing", trace_id=trace_id, attributes=common_attrs) as span:
          # --- Stage 1: keyword pre-filter ---
          agents = self._registry.all()
          user_input = state["input"].lower()
          keyword_matches = [a for a in agents if any(kw.lower() in user_input for kw in getattr(a, "keywords", []))]
          if len(keyword_matches) == 1:
              chosen = keyword_matches[0].name
              span.set_attribute("stage", "keyword")
              span.set_attribute("chosen", chosen)
              span.set_attribute("agent_name", chosen)
              return {"next": chosen}

          if len(agents) == 1:
              chosen = agents[0].name
              span.set_attribute("stage", "single")
              span.set_attribute("chosen", chosen)
              span.set_attribute("agent_name", chosen)
              return {"next": chosen}

          # --- Stage 2: LLM routing ---
          descriptions = "\n\n".join(
              f"name: {a.name}\ndescription: {a.description}" for a in agents
          )
          messages = [
              SystemMessage(content=ROUTER_PROMPT.format(agent_descriptions=descriptions)),
              HumanMessage(content=state["input"]),
          ]
          response = await self._llm.ainvoke(messages)
          chosen = response.content.strip()

          valid = {a.name for a in agents} | {"fallback"}
          if chosen not in valid:
              unknown = chosen
              chosen = "fallback"
              span.set_attribute("stage", "llm")
              span.set_attribute("chosen", chosen)
              span.set_attribute("agent_name", chosen)
              span.set_attribute("fallback", True)
              span.set_status("ERROR", f"unknown_agent={unknown}"[:200])
              return {"next": chosen}

          span.set_attribute("stage", "llm")
          span.set_attribute("chosen", chosen)
          span.set_attribute("agent_name", chosen)
          return {"next": chosen}
      ```

    互換 alias は残さない (`event: routing` / `routing_fallback` の旧 JSON フォーマットはファイル中から完全消去する)。agents 取得 (`agents = self._registry.all()`) は span context 内に移す必要はなく、span attributes 構築の前に行うのが自然（必要なら common_attrs.candidates で 2 回参照しないよう、agents を span 外で計算する）。

    Step 3: Test A-D が green になるまで graph.py を調整。旧 routing log テスト (`tests/test_routing_keyword.py`) は新 schema に合わせて調整する必要がある場合があるため、必ず `uv run pytest tests/test_routing_keyword.py -x` を走らせて修正。既存 test が `event: routing` 文字列を grep している場合は新 schema の `operation_name: routing` に書き換える。

    Step 4: `grep -q '"event": "routing"' app/orchestrator/graph.py` が 0 件を確認。`grep -q 'routing_fallback' app/orchestrator/graph.py` も 0 件。
  </action>
  <verify>
    <automated>uv run pytest tests/test_routing_keyword.py tests/test_sub_agent_trace.py -x --tb=short && ! grep -q '"event": "routing"' app/orchestrator/graph.py && ! grep -q 'routing_fallback' app/orchestrator/graph.py && grep -q 'async with trace_span' app/orchestrator/graph.py</automated>
  </verify>
  <done>
    graph.py の routing ログが OTEL span に全置換、旧 `event:routing` alias 0 件、Test A-D green、既存 routing テストが新 schema で green。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SubAgent / ToolEnabledSubAgent / CodeActSubAgent / GemSubAgent の run() を sub_agent span で wrap + TracedTool 統合</name>
  <files>app/orchestrator/agent.py, app/orchestrator/tool_agent.py, app/orchestrator/codeact_agent.py, app/orchestrator/gem_agent.py, tests/test_sub_agent_trace.py</files>
  <read_first>
    - app/orchestrator/agent.py L128-159 (SubAgent.run の現行)
    - app/orchestrator/tool_agent.py L229-344 (ToolEnabledSubAgent.__init__ + run)
    - app/orchestrator/codeact_agent.py L152-260 (run() と execute_python 呼び出し)
    - app/orchestrator/gem_agent.py 全体 (SubAgent 継承のみ)
    - app/observability/traced_tool.py (Plan 03 の TracedTool)
    - .planning/phases/31-agent-mcp-observability/31-RESEARCH.md §3.1, §3.2, §4
    - .planning/phases/31-agent-mcp-observability/31-PATTERNS.md (tool_agent.py / codeact_agent.py 修正セクション)
    - docs/phase-31-reasoning-token-spike.md (Plan 01 の採用 attribute リスト)
  </read_first>
  <action>
    Step 1 (agent.py): `SubAgent.run()` の先頭で sub_agent span を開始する:
    - import 追加: `from app.observability.trace import trace_span`
    - `run()` 全体を `async with trace_span("sub_agent", trace_id=..., attributes={...})` で囲む
    - 属性 (D-09 共通 4):
      ```python
      context = state.get("context")
      trace_id = context.correlation_id if context else ""
      attrs = {
          "user_id": context.user_id if context else "unknown",
          "app_id": context.app_id if context else "",
          "thread_id": context.thread_id if context else "",
          "agent_name": self.name,
          "model_name": getattr(self._llm, "model", "unknown"),
      }
      ```
    - span 内で message accumulate 後に `span.set_attribute("message_count", len(ctx_msgs or []) + 1)` を追加
    - GemSubAgent は SubAgent を継承しているだけなので自動的に span で包まれる。gem_agent.py は変更不要（SubAgent.run が wrap されるため）だが、GemSubAgent の agent_type attribute を Gem と区別したい場合は `span.set_attribute("agent_type", "gem")` を追加する検討あり。**Phase 31 スコープでは agent_type は必須でないため追加しない**

    Step 2 (tool_agent.py): ToolEnabledSubAgent を拡張:

    **W2 固定方針 (1 案のみ、2 択判断は除去):**

    `app/observability/trace.py` の `trace_span` context manager が起動時に `attributes` から共通 4 key (`user_id` / `app_id` / `agent_name` / `model_name`) を抽出して `_current_common_attrs.set(subset)` し、退出時に reset する仕組み (Plan 31-02 Task 2 で実装済、W2 反映)。TracedTool は `_current_common_attrs.get()` を読むだけで共通 attributes を取得できる。

    したがって ToolEnabledSubAgent 側の実装は以下の通りシンプル:

    - import 追加:
      ```python
      from app.observability.trace import trace_span
      from app.observability.traced_tool import TracedTool
      ```
    - `__init__` の `self._tool_node = ToolNode(tools)` を以下に置き換え:
      ```python
      # privileged_tool_names は __init__ 引数で受け取る (SubAgentRegistry が注入)
      self._privileged_names: frozenset[str] = privileged_tool_names
      wrapped_tools = [
          TracedTool(
              wrapped=t,
              privileged_tool_names=self._privileged_names,
              # common_attrs_provider は渡さない — TracedTool 側で _current_common_attrs.get() を参照する。
              # Plan 31-03 で TracedTool を「common_attrs_provider より ContextVar 優先」に書き換える (W2 反映)
          )
          for t in tools
      ]
      self._tool_node = ToolNode(wrapped_tools)
      ```
    - `__init__` シグネチャに `privileged_tool_names: frozenset[str] = frozenset()` を追加し、SubAgentRegistry からの注入を受ける。

    - `run()` を `async with trace_span("sub_agent", trace_id=..., attributes={...}) as span:` で wrap:
      ```python
      async def run(self, state):
          context = state.get("context")
          trace_id = context.correlation_id if context else ""
          common = {
              "user_id": context.user_id if context else "unknown",
              "app_id": context.app_id if context else "",
              "thread_id": context.thread_id if context else "",
              "agent_name": self.name,
              "model_name": getattr(self._llm, "model", "unknown"),
          }
          from app.observability.trace import _current_common_attrs
          token_attrs = _current_common_attrs.set(common)
          try:
              async with trace_span("sub_agent", trace_id=trace_id, attributes=common) as span:
                  # ... 既存の run ロジック ...
                  # span.set_attribute("message_count", len(all_messages))
                  # (Plan 01 spike 結果) span.set_attribute("input_tokens", ...) 等
                  return {"output": output, "messages": all_messages, "agent_name": self.name}
          finally:
              _current_common_attrs.reset(token_attrs)
      ```
    - GraphRecursionError 捕捉箇所で `span.set_status("ERROR", "recursion_limit_reached"); span.set_attribute("recursion_limit_reached", True)` を呼ぶ
    - SubAgentRegistry が privileged_tool_names を持っているので、agent.py L216-248 付近で `ToolEnabledSubAgent` を構築する箇所で `agent._privileged_names = self._privileged` を setter 経由で注入。もしくは __init__ に `privileged_tool_names: frozenset[str] = frozenset()` を追加して SubAgentRegistry から渡す。**推奨: __init__ 引数追加**

    Step 3 (codeact_agent.py): `CodeActSubAgent.run()` を以下で変更:
    - import 追加: `from app.observability.trace import trace_span, _current_common_attrs`
    - `run()` 全体を `async with trace_span("sub_agent", trace_id=..., attributes=common):` で包む（Step 2 と同じ common dict、agent_name=self.name）
    - `execute_python.ainvoke(...)` の呼び出し (L200) を以下で wrap:
      ```python
      args_json = json.dumps({"code": code}, ensure_ascii=False)
      from app.observability.config import get_args_max_chars, get_result_max_chars
      async with trace_span(
          "tool_call",
          trace_id=trace_id,
          attributes={
              **common,
              "tool_name": "execute_python",
              "args_bytes": len(args_json.encode("utf-8")),
              "args_prefix": args_json[:get_args_max_chars()],
              "privileged": True,  # execute_python は常に privileged (sandbox 内全権)
          },
      ) as tool_span:
          try:
              result = await execute_python.ainvoke({"code": code})
              raw_result = _normalize_tool_result(result)
          except Exception as e:
              raw_result = json.dumps({"stdout": "", "stderr": str(e), "exit_code": -1})
              tool_span.set_status("ERROR", str(e)[:200])
          result_max = get_result_max_chars()
          tool_span.set_attribute("result_bytes", len(raw_result.encode("utf-8")))
          tool_span.set_attribute("result_prefix", raw_result[:result_max])
          stdout, stderr, exit_code = _parse_execute_result(raw_result)
          tool_span.set_attribute("success", exit_code == 0)
          if exit_code != 0:
              tool_span.set_status("ERROR", f"exit_code={exit_code}")
      ```

    Step 4 (token usage attribute — W3 反映、spike 結果による 2 分岐を明文化):

    **事前判定スクリプト (acceptance_criteria — executor が実行して分岐を決める):**
    ```bash
    # Plan 01 の spike 結論セクションを grep して分岐
    if grep -q "ASSISTANT_USAGE.*露出確認" docs/phase-31-reasoning-token-spike.md; then
      BRANCH=a
    else
      BRANCH=b
    fi
    echo "token usage branch: $BRANCH"
    ```

    **分岐 (a) — spike 結論が「ASSISTANT_USAGE event が取れる」:**
    - `app/providers/copilot.py` の `_agenerate` / `_astream` を改修。`session.on(callback)` で `ASSISTANT_USAGE` event を capture し、`ChatCopilot` instance の `last_usage: dict | None = None` 属性に `{"input_tokens", "output_tokens", "cache_read_tokens", "cache_write_tokens"}` を格納する。
    - SubAgent.run() 内で sub_agent span に attribute を追加:
      ```python
      usage = getattr(self._llm, "last_usage", None) or {}
      for key in ("input_tokens", "output_tokens", "cache_read_tokens", "cache_write_tokens"):
          if usage.get(key) is not None:
              span.set_attribute(key, usage[key])
      span.set_attribute("message_count", len(all_messages))
      ```
    - reasoning 対応: spike 結論で `ASSISTANT_REASONING` 露出も確認済なら、`reasoning_chars` (int) + `reasoning_prefix` (200 字) を同様の経路で capture → attribute 追加。

    **分岐 (b) — spike 結論が「露出せず」:**
    - token usage attribute は設置しない。`message_count` (int) のみを attribute として記録する:
      ```python
      span.set_attribute("message_count", len(all_messages))
      ```
    - `docs/phase-31-reasoning-token-spike.md` の Deferred セクションに「D-14 の token usage 記録部分は Phase 31 では未実装。将来 SDK で露出確認後に再評価」と追記する。

    executor は BRANCH 判定結果を 31-04-SUMMARY.md に記録すること (どの分岐を採用したか + 理由)。

    Step 5: tests/test_sub_agent_trace.py に以下 integration-style test を追加（ChatCopilot mock 使用）:
    - Test E (test_sub_agent_span_hierarchy): mock registry + mock RouterNode + mock SubAgent.run で routing span → sub_agent span → tool_call span (× N) が同一 trace_id で emit される。parent_span_id が正しく親子関係を張る
    - Test F (test_tool_enabled_sub_agent_tracedtool): ToolEnabledSubAgent の `self._tool_node` の tools が全て TracedTool インスタンスである (`all(isinstance(t, TracedTool) for t in self._tool_node.tools_by_name.values())` で検証)
    - Test G (test_codeact_execute_python_span): CodeActSubAgent.run を呼ぶと tool_call span (tool_name=execute_python, privileged=true) が 1 件 emit される
    - Test H (test_recursion_limit_span_error): GraphRecursionError 発生時 sub_agent span の status_code=="ERROR" + recursion_limit_reached attribute
  </action>
  <verify>
    <automated>uv run pytest tests/test_sub_agent_trace.py tests/test_routing_keyword.py -x --tb=short && grep -q 'TracedTool' app/orchestrator/tool_agent.py && grep -q 'trace_span' app/orchestrator/codeact_agent.py && grep -q 'trace_span' app/orchestrator/agent.py</automated>
  </verify>
  <done>
    SubAgent / ToolEnabledSubAgent / CodeActSubAgent の run() が sub_agent span で包まれ、ToolEnabledSubAgent の ToolNode が TracedTool で wrap された tools を使い、CodeAct の execute_python 呼び出しが tool_call span で包まれる。integration test E-H 全 green。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 4 handler (orchestrator / langgraph / debate / iframe_rpc 以外の 3 つ) に request span + langgraph_handler / debate_handler に RPCContext を追加</name>
  <files>app/jobs/handlers/orchestrator_handler.py, app/jobs/handlers/langgraph_handler.py, app/jobs/handlers/debate_handler.py, tests/test_sub_agent_trace.py</files>
  <read_first>
    - app/jobs/handlers/orchestrator_handler.py L25-209 (既存 RPCContext 使用箇所 L130-136)
    - app/jobs/handlers/langgraph_handler.py (RPCContext 未使用)
    - app/jobs/handlers/debate_handler.py (RPCContext 未使用)
    - app/orchestrator/context.py (RPCContext.from_http I/F)
    - app/observability/trace.py (Plan 02 writer)
    - .planning/phases/31-agent-mcp-observability/31-RESEARCH.md §4.4 (request span 推奨), §11.7
    - .planning/phases/31-agent-mcp-observability/31-PATTERNS.md (graph.py 以外の integration セクション)
  </read_first>
  <action>
    Step 1 (orchestrator_handler.py): 既存 RPCContext 構築 (L130-136) はそのまま。`handle()` の `try:` の**前**に request span を開始:
    ```python
    async def handle(self, ctx, job):
        # ... 既存の job / param 取得 ...
        github_login: str = job.get("github_login", "unknown")
        context = RPCContext.from_http(user_id=github_login, app_id=app_id, thread_id=thread_id)

        from app.observability.trace import trace_span
        async with trace_span(
            "request",
            trace_id=context.correlation_id,
            attributes={"user_id": context.user_id, "app_id": context.app_id, "thread_id": context.thread_id},
        ) as req_span:
            req_span.set_attribute("handler", "orchestrator")
            # ... 既存の try / except / finally の全体を span 内に含める ...
    ```
    既存 `context = RPCContext.from_http(...)` は span 構築の前に移動。initial state に `"context": context` を入れる挙動は変えない。

    Step 2 (langgraph_handler.py): `handle()` に RPCContext を追加し request span で wrap:
    - import 追加:
      ```python
      from app.orchestrator.context import RPCContext
      from app.observability.trace import trace_span
      ```
    - `handle()` の先頭 job 取得後:
      ```python
      github_login: str = job.get("github_login", "unknown")
      app_id: str = job.get("app_id", "chat")
      context = RPCContext.from_http(user_id=github_login, app_id=app_id, thread_id=thread_id)
      async with trace_span(
          "request",
          trace_id=context.correlation_id,
          attributes={"user_id": github_login, "app_id": app_id, "thread_id": thread_id, "handler": "langgraph"},
      ) as req_span:
          # ... 既存の build_graph / checkpointer / ainvoke ...
      ```
    - LangGraph の graph.ainvoke はそのまま実行（SubAgent span は graph 内では出ない — LangGraph chatbot_node は単一 LLM 呼び出し）。Plan 04 の scope では langgraph_handler 経路では **request span のみ** を emit する。LLM 1 回呼び出しの LLM span を追加するかは **判断マターで、最低限 request span のみ** で done とする
    - ただし代替として `llm.ainvoke` 呼び出し箇所（build_graph の chatbot_node 内）を sub_agent span で包む手もあるが、本 task では `langgraph_handler` での request span だけに留め、「単一エージェント」を意味する `attributes.agent_type="chatbot"` だけ設定

    Step 3 (debate_handler.py): 同様に request span を追加:
    - import: RPCContext + trace_span
    - `handle()` 先頭で:
      ```python
      github_login: str = job.get("github_login", "unknown")
      context = RPCContext.from_http(user_id=github_login, app_id="debate", thread_id=thread_id)
      async with trace_span(
          "request",
          trace_id=context.correlation_id,
          attributes={"user_id": github_login, "app_id": "debate", "thread_id": thread_id, "handler": "debate"},
      ):
          # ... 既存 debate_graph ainvoke ...
      ```
    - debate_graph 内の各 SubAgent の run() は Task 2 で sub_agent span が既に仕込まれているので、ContextVar 経由で request span の span_id が parent として自動継承される
    - DebateHandler が initial_state に context を入れる改修: debate_graph の build_debate_graph が初期 state に context を入れているか確認し、入れていなければ `initial_state["context"] = context` を追加する。**もし build_debate_graph の state 仕様が context を受け付けないなら、本タスクでは RPCContext 構築と trace_span wrap のみで完了**（SubAgent 内で `state.get("context")` が None を返す既存 defensive pattern でフォールバック）

    Step 4: tests/test_sub_agent_trace.py に以下追加:
    - Test I (test_request_span_in_orchestrator_handler): 既存 orchestrator_handler の handle をモック graph で実行 → request span が 1 件 emit、operation_name=="request", attributes.handler=="orchestrator", trace_id=<correlation_id>
    - Test J (test_request_span_in_langgraph_handler): langgraph_handler の handle をモック graph で実行 → request span が 1 件 emit、attributes.handler=="langgraph", attributes.app_id=="chat"
    - Test K (test_request_span_in_debate_handler): debate_handler の handle をモック実行 → request span が 1 件 emit、attributes.handler=="debate", attributes.app_id=="debate"

    handler test は重量級のためテスト用の小さな compiled graph mock を使う。既存 handler の test（あれば）を参考に AsyncPostgresSaver や SubAgentRegistry を mock する。もし handler テストが重すぎる場合は **Test I-K のうち最低 1 つ（orchestrator_handler）だけ書いて残り 2 つは Plan 08 の integration validation に委ねる** 判断も OK。
  </action>
  <verify>
    <automated>uv run pytest tests/test_sub_agent_trace.py -x --tb=short && grep -q 'trace_span' app/jobs/handlers/orchestrator_handler.py && grep -q 'trace_span' app/jobs/handlers/langgraph_handler.py && grep -q 'trace_span' app/jobs/handlers/debate_handler.py && grep -q 'RPCContext' app/jobs/handlers/langgraph_handler.py && grep -q 'RPCContext' app/jobs/handlers/debate_handler.py</automated>
  </verify>
  <done>
    3 handler に request span 追加、langgraph_handler / debate_handler に RPCContext 構築を追加、test Test I (最低限) が green。既存 handler テストが non-regression。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arq worker job dict → RPCContext | job payload の github_login / app_id は chat.py が JWT から抽出、trust 済 |
| RPCContext.correlation_id → trace_id | 内部生成 UUID4、外部書き換え不能 |
| SubAgent state → sub_agent span attributes | state.context は Phase 11 で構築済、trust 済 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Information Disclosure | user_input_prefix が routing span に載る | mitigate | 200 字で truncate (state["input"][:200])、D-13 準拠。tool args/result は Plan 03 の TracedTool で別途 truncate |
| T-31-02 | Information Disclosure | SubAgent span に載るメッセージ内容 | mitigate | message_count は数のみ、実テキストは span に載せない（LangGraph checkpointer に残る設計、D-13） |
| T-31-03 | Privilege Escalation | TracedTool の privileged 判定が抜ける | mitigate | SubAgentRegistry で privileged_tool_names frozenset を ToolEnabledSubAgent / CodeActSubAgent に明示的に注入。integration test (Test F) で TracedTool wrap 有無を検証。CodeAct の execute_python は hardcode `privileged=true` |
</threat_model>

<verification>
- `uv run pytest tests/test_sub_agent_trace.py tests/test_routing_keyword.py -x` 全 green
- `uv run pytest tests/ -x` 全 green (既存テスト非破壊)
- `grep -cE 'trace_span' app/orchestrator/graph.py app/orchestrator/tool_agent.py app/orchestrator/codeact_agent.py app/orchestrator/agent.py app/jobs/handlers/orchestrator_handler.py app/jobs/handlers/langgraph_handler.py app/jobs/handlers/debate_handler.py` で各ファイルに少なくとも 1 件
- `! grep -q '"event": "routing"' app/orchestrator/graph.py`
- `! grep -q 'routing_fallback' app/orchestrator/graph.py`
- `grep -q 'TracedTool' app/orchestrator/tool_agent.py`
- `grep -q 'privileged.*True' app/orchestrator/codeact_agent.py`
</verification>

<success_criteria>
- [ ] graph.py の routing log が OTEL span に完全置換 (互換 alias 0 件)
- [ ] SubAgent / ToolEnabledSubAgent / CodeActSubAgent の run() に sub_agent span 追加
- [ ] ToolEnabledSubAgent の ToolNode が TracedTool wrap 済
- [ ] CodeActSubAgent.run の execute_python に tool_call span 追加 (privileged=true)
- [ ] 3 handler (orchestrator/langgraph/debate) に request span 追加
- [ ] langgraph_handler / debate_handler に RPCContext 構築追加
- [ ] Plan 01 spike 結果に基づく token usage attribute が SubAgent span に載る (取れる場合)
- [ ] tests/test_sub_agent_trace.py 4+ cases (Test A-D 必須、E-H 推奨、I-K は最低 1 個) green
- [ ] 既存テストスイート非破壊
</success_criteria>

<output>
After completion, create `.planning/phases/31-agent-mcp-observability/31-04-SUMMARY.md` with:
- 各ファイルの修正差分サマリ（行数）
- Plan 01 spike の結論に基づく採用 attribute リスト（実際に実装した分）
- 既存 routing test の schema 移行内容
- Plan 05 への引き継ぎ（iframe_rpc_handler の request span + db_query tool_call span 実装が残っている旨）
- Plan 08 integration validation で実環境検証する項目（request / routing / sub_agent / tool_call の 4 層 span tree）
</output>
