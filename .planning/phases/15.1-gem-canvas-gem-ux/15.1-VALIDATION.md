---
phase: 15.1
slug: gem-canvas-gem-ux
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-05
---

# Phase 15.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (frontend) |
| **Config file** | frontend/vite.config.ts or vitest.config.ts |
| **Quick run command** | `cd frontend && bun run build` |
| **Full suite command** | `cd frontend && bun run build && bun run typecheck` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd frontend && bun run build`
- **After every plan wave:** Run `cd frontend && bun run build && bun run typecheck`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 15.1-01-01 | 01 | 1 | Gem UX | — | N/A | build | `cd frontend && bun run build` | ✅ | ⬜ pending |
| 15.1-01-02 | 01 | 1 | Gem UX | — | N/A | build | `cd frontend && bun run build` | ✅ | ⬜ pending |
| 15.1-01-03 | 01 | 2 | Gem UX | — | N/A | build | `cd frontend && bun run build` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- Existing infrastructure covers all phase requirements.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Gem カードが MenuScreen に表示される | Gem UX | UI 確認が必要 | MenuScreen を開き Gems カードが表示されることを確認 |
| GemChatApp でスレッド作成・会話ができる | Gem UX | E2E UI 確認 | Gem カードクリック → スレッド作成 → メッセージ送信 → 返答確認 |
| Gem 名・システムプロンプト編集が反映される | Gem UX | UI 確認が必要 | 設定編集 → 保存 → 再度開いて値が保持されていることを確認 |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
