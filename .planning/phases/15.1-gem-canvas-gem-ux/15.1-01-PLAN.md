# Phase 15.1 Plan 1: バックエンド — description / knowledge フィールド追加

**Goal:** `gems` テーブルに `description` と `knowledge` の2カラムを追加し、API モデル・routes・LangGraph ハンドラーをすべて更新する。

**Depends on:** Phase 15（完了済み）

**Requirements:** Todo 6（description フィールド）、Todo 7（knowledge フィールド）

---

## 現状確認

### 実装済み（変更不要）
- `App.tsx` — 4画面ナビゲーション完了（D-04〜D-07）
- `GemsScreen.tsx` — 新規作成済み（D-08〜D-12）
- `GemChatApp.tsx` — 新規作成済み（D-13〜D-17）
- `MenuScreen.tsx` — Gems カード追加済み（D-01〜D-03）
- `MessageArea.tsx` — GemSelector 撤去済み（D-18〜D-21）
- `useGems.ts` — updateGem 実装済み（D-11）
- `useThreads.ts` — string 型対応済み

### このプランの対象（未実装）
- `app/api/main.py` — gems テーブルへの `description` / `knowledge` カラム追加（ALTER TABLE）
- `app/api/models.py` — `GemCreate`・`GemUpdate`・`GemInfo` に `description`・`knowledge` 追加
- `app/api/routes/gems.py` — INSERT / UPDATE の SQL・`_row_to_gem` 変換を更新
- `app/graph/builder.py` — knowledge が空でない場合に system_prompt + knowledge を結合
- `app/jobs/handlers/langgraph_handler.py` — `_get_gem_info` で `knowledge` を取得し、knowledge 結合ロジックを適用

---

## Tasks

### Task 1: DB スキーマ — gems テーブルに description / knowledge カラムを追加

**File:** `app/api/main.py`

**Action:** `lifespan` 関数内の `# --- Phase 15: Gem + Canvas tables ---` セクションにある gems テーブル作成ブロックの **後ろ**（`gems_github_login_idx` インデックス作成の直後、`threads` への `gem_id` カラム追加の前）に、以下の2つの `ALTER TABLE` 文を追加する。

```python
# Phase 15.1: Gem description / knowledge カラム追加（idempotent）
await conn.execute(
    "ALTER TABLE gems ADD COLUMN IF NOT EXISTS description TEXT NOT NULL DEFAULT ''"
)
await conn.execute(
    "ALTER TABLE gems ADD COLUMN IF NOT EXISTS knowledge TEXT NOT NULL DEFAULT ''"
)
```

**配置場所の目印（現在のコード L139 付近）:**

```python
await conn.execute(
    "CREATE INDEX IF NOT EXISTS gems_github_login_idx ON gems(github_login)"
)

# ↑ この直後に2行追加 ↑

# threads テーブルへの gem_id カラム追加（gems テーブル作成後に実行 — Pitfall 2）
await conn.execute(
    "ALTER TABLE threads ADD COLUMN IF NOT EXISTS gem_id UUID REFERENCES gems(gem_id) ON DELETE SET NULL"
)
```

`ADD COLUMN IF NOT EXISTS` を使うことで、起動のたびに実行されても冪等になる（Alembic 未使用のためこのパターンを採用）。

**Verify:** `docker compose restart backend` 後、`docker compose logs backend | grep -E "error|Error"` が0件。`psql` 接続で `\d gems` を実行し `description` と `knowledge` カラムが存在することを確認。

---

### Task 2: API モデル — GemCreate / GemUpdate / GemInfo に description / knowledge を追加

**File:** `app/api/models.py`

**Action:** 以下の3モデルを更新する。

```python
class GemCreate(BaseModel):
    """Request body for POST /api/gems."""

    name: str
    system_prompt: str = ""
    description: str = ""    # Todo 6: Gem の説明文
    knowledge: str = ""      # Todo 7: ナレッジ（system_prompt に結合される）
    type: Literal["default", "canvas"] = "default"


class GemUpdate(BaseModel):
    """Request body for PATCH /api/gems/{gem_id}."""

    name: str | None = None
    system_prompt: str | None = None
    description: str | None = None   # Todo 6
    knowledge: str | None = None     # Todo 7
    type: Literal["default", "canvas"] | None = None


class GemInfo(BaseModel):
    """Response model for Gem endpoints."""

    gem_id: str
    name: str
    system_prompt: str
    description: str          # Todo 6: Gem カードに表示する説明文
    knowledge: str            # Todo 7: ナレッジ（フロントエンドの編集フォームに表示）
    type: str
    created_at: str
    updated_at: str
```

**Verify:** `cd /home/parallels/workspaces/copilot-langgraph && python -c "from app.api.models import GemCreate, GemUpdate, GemInfo; print('OK')"` が `OK` を出力。

---

### Task 3: API routes — INSERT / RETURNING / SET 句を更新

**File:** `app/api/routes/gems.py`

**Action:** 以下の4箇所を変更する。

#### 3-a: `_row_to_gem` の変換関数を更新

```python
def _row_to_gem(row: dict) -> GemInfo:
    """Convert a psycopg dict_row result to a GemInfo response model."""
    return GemInfo(
        gem_id=str(row["gem_id"]),
        name=row["name"],
        system_prompt=row["system_prompt"],
        description=row.get("description", ""),
        knowledge=row.get("knowledge", ""),
        type=row["type"],
        created_at=row["created_at"].isoformat() if row["created_at"] else "",
        updated_at=row["updated_at"].isoformat() if row["updated_at"] else "",
    )
```

`.get("description", "")` でフォールバックを付けることで、既存データにカラムが NULL の行があっても壊れない。

#### 3-b: `create_gem` の INSERT 文を更新

```python
await cur.execute(
    """INSERT INTO gems (github_login, name, system_prompt, description, knowledge, type)
       VALUES (%s, %s, %s, %s, %s, %s)
       RETURNING gem_id, name, system_prompt, description, knowledge, type, created_at, updated_at""",
    (github_login, body.name, body.system_prompt, body.description, body.knowledge, body.type),
)
```

#### 3-c: `list_gems` の SELECT 文を更新

```python
await cur.execute(
    """SELECT gem_id, name, system_prompt, description, knowledge, type, created_at, updated_at
       FROM gems
       WHERE github_login = %s
       ORDER BY created_at DESC""",
    (github_login,),
)
```

#### 3-d: `get_gem` の SELECT 文を更新

```python
await cur.execute(
    """SELECT gem_id, name, system_prompt, description, knowledge, type, created_at, updated_at
       FROM gems
       WHERE gem_id = %s::uuid AND github_login = %s""",
    (gem_id, github_login),
)
```

#### 3-e: `update_gem` の `allowed_columns` に description / knowledge を追加

```python
allowed_columns = {"name", "system_prompt", "description", "knowledge", "type"}
```

RETURNING 句も同様に `description, knowledge` を追加：

```python
f"""UPDATE gems
    SET {set_clause}, updated_at = now()
    WHERE gem_id = %s::uuid AND github_login = %s
    RETURNING gem_id, name, system_prompt, description, knowledge, type, created_at, updated_at"""
```

**Verify:** `curl -s -X GET http://localhost:8000/api/gems -H "Cookie: ..."` のレスポンス JSON に `"description"` と `"knowledge"` キーが含まれる。

---

### Task 4: LangGraph ハンドラー — knowledge をシステムプロンプトに結合

**File:** `app/jobs/handlers/langgraph_handler.py`

**Action:**

#### 4-a: `_get_gem_info` を更新して `knowledge` を返す

```python
async def _get_gem_info(thread_id: str, db_uri: str) -> tuple[str | None, str | None, str | None, str | None, str | None]:
    """thread_id から gem_id, gem_type, gem_name, system_prompt, knowledge を取得する。"""
    try:
        async with await psycopg.AsyncConnection.connect(db_uri) as conn:
            async with conn.cursor() as cur:
                await cur.execute(
                    """SELECT g.gem_id, g.type, g.name, g.system_prompt, g.knowledge
                       FROM threads t
                       LEFT JOIN gems g ON t.gem_id = g.gem_id
                       WHERE t.thread_id = %s""",
                    (thread_id,),
                )
                row = await cur.fetchone()
                if row and row[0] is not None:
                    return str(row[0]), row[1], row[2], row[3], row[4] or ""
    except Exception:
        pass
    return None, None, None, None, None
```

#### 4-b: `handle` メソッドで `knowledge` を受け取り、system_prompt に結合

呼び出し箇所を以下に変更：

```python
gem_id, gem_type, gem_name, system_prompt, knowledge = await _get_gem_info(thread_id, DB_URI)

# knowledge が空でなければ system_prompt に結合（Todo 7）
if system_prompt and knowledge:
    system_prompt = system_prompt + "\n\n## 知識\n" + knowledge
elif knowledge and not system_prompt:
    system_prompt = "## 知識\n" + knowledge
```

`messages_input` への `SystemMessage` 注入ロジックはそのまま（`if system_prompt:` 条件で追加）。

**Verify:** Gem を作成し knowledge フィールドに文字列を入れてチャット送信後、`docker compose logs worker` に `system_prompt + knowledge` の組み合わせがエラーなく処理されることを確認。

---

## Verification

- [ ] `docker compose up -d` 後、`docker compose logs backend 2>&1 | grep -i error` が0件
- [ ] `\d gems` で `description TEXT`・`knowledge TEXT` カラムが存在する
- [ ] `python -c "from app.api.models import GemInfo; g = GemInfo(gem_id='x', name='t', system_prompt='', description='d', knowledge='k', type='default', created_at='', updated_at=''); print(g.description, g.knowledge)"` が `d k` を出力
- [ ] GET /api/gems のレスポンスに `description`・`knowledge` フィールドが含まれる
- [ ] POST /api/gems で `description`・`knowledge` を含むリクエストが 201 を返す
- [ ] Gem の knowledge フィールドにテキストを設定してチャット送信 → worker ログにエラーなし
