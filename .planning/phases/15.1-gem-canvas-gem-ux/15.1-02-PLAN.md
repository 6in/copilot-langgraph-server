# Phase 15.1 Plan 2: フロントエンド — description / knowledge フィールド対応

**Goal:** フロントエンドの型定義・GemsScreen を更新し、description と knowledge の入力・表示・送信を実装する。

**Depends on:** Plan 1（バックエンド API が description / knowledge を返す状態であること）

**Requirements:** Todo 6（description フロントエンド）、Todo 7（knowledge フロントエンド）

---

## 現状確認

### 実装済み（変更不要）
- `frontend/src/hooks/useGems.ts` — `updateGem(gemId, data: Partial<GemCreate>)` 実装済み。`GemCreate` に `description`/`knowledge` が追加されれば自動的に送信対象になる。
- `frontend/src/api/client.ts` — `updateGemApi`・`createGemApi` は Pydantic 型を JSON で送るだけのため型更新で対応完了。
- `GemChatApp.tsx`、`App.tsx`、`MenuScreen.tsx` — 変更不要。

### このプランの対象（変更必要）
- `frontend/src/types.ts` — `GemInfo`・`GemCreate` に `description`・`knowledge` を追加
- `frontend/src/components/GemsScreen.tsx` — カード表示・作成フォーム・編集フォームに対応

---

## Tasks

### Task 1: types.ts — GemInfo / GemCreate に description / knowledge を追加

**File:** `frontend/src/types.ts`

**Action:** Phase 15 で定義した `GemInfo` と `GemCreate` インターフェースを以下のように更新する。

```typescript
// --- Phase 15: Gem + Canvas types ---

export interface GemInfo {
  gem_id: string;
  name: string;
  system_prompt: string;
  description: string;   // Todo 6: Gem カードに表示する説明文
  knowledge: string;     // Todo 7: ナレッジ（system_prompt に結合される）
  type: 'default' | 'canvas';
  created_at: string;
  updated_at: string;
}

export interface GemCreate {
  name: string;
  system_prompt: string;
  description: string;   // Todo 6
  knowledge: string;     // Todo 7
  type: 'default' | 'canvas';
}
```

**Verify:** `cd /home/parallels/workspaces/copilot-langgraph/frontend && bun run build 2>&1 | grep -E "error|Error"` が0件。型エラーが GemsScreen の `createGem` 呼び出し箇所で出る場合は Task 2 で修正する。

---

### Task 2: GemsScreen.tsx — description / knowledge のフォーム対応と表示切り替え

**File:** `frontend/src/components/GemsScreen.tsx`

**Action:** 以下の5箇所を変更する。

#### 2-a: インライン編集フォームに description / knowledge state を追加

既存の `editName`・`editPrompt` state の宣言ブロックに追加：

```typescript
const [editDescription, setEditDescription] = useState('');
const [editKnowledge, setEditKnowledge] = useState('');
```

#### 2-b: `handleEdit` で description / knowledge を初期化

```typescript
const handleEdit = (gem: GemInfo) => {
  setEditingGemId(gem.gem_id);
  setEditName(gem.name);
  setEditPrompt(gem.system_prompt);
  setEditDescription(gem.description);   // 追加
  setEditKnowledge(gem.knowledge);       // 追加
  setConfirmDeleteId(null);
  setFormError(null);
};
```

#### 2-c: `handleCancelEdit` で description / knowledge をリセット

```typescript
const handleCancelEdit = () => {
  setEditingGemId(null);
  setEditName('');
  setEditPrompt('');
  setEditDescription('');   // 追加
  setEditKnowledge('');     // 追加
};
```

#### 2-d: `handleSave` で description / knowledge を送信

```typescript
await updateGem(gemId, {
  name: editName,
  system_prompt: editPrompt,
  description: editDescription,   // 追加
  knowledge: editKnowledge,       // 追加
});
```

#### 2-e: 新規作成フォームの state に description / knowledge を追加

既存 `createName`・`createPrompt`・`createType` の宣言ブロックに追加：

```typescript
const [createDescription, setCreateDescription] = useState('');
const [createKnowledge, setCreateKnowledge] = useState('');
```

`handleShowCreateForm` でリセット：

```typescript
setCreateDescription('');
setCreateKnowledge('');
```

`handleCancelCreate` でリセット：

```typescript
setCreateDescription('');
setCreateKnowledge('');
```

`handleCreate` の `createGem` 呼び出しを更新：

```typescript
await createGem({
  name: createName,
  system_prompt: createPrompt,
  description: createDescription,   // 追加
  knowledge: createKnowledge,       // 追加
  type: createType,
});
// handleCreate 後の reset にも追加:
setCreateDescription('');
setCreateKnowledge('');
```

#### 2-f: カード通常表示 — system_prompt → description に切り替え（Todo 6）

通常表示ブロック（現在 `gem.system_prompt.slice(0, 50)` を表示している箇所）を以下に変更：

```tsx
<div
  style={{
    fontSize: '0.875rem',
    color: subtitleColor,
    marginBottom: '16px',
    lineHeight: 1.5,
  }}
>
  {gem.description
    ? gem.description.slice(0, 80) + (gem.description.length > 80 ? '…' : '')
    : <span style={{ fontStyle: 'italic', opacity: 0.6 }}>説明なし</span>
  }
</div>
```

#### 2-g: インライン編集フォームに Description / Knowledge テキストエリアを追加

System Prompt テキストエリアの**後**に、以下を追加する：

```tsx
<div style={{ marginBottom: '12px' }}>
  <label
    style={{
      display: 'block',
      fontSize: '0.875rem',
      fontWeight: 600,
      color: textColor,
      marginBottom: '4px',
    }}
  >
    Description
    <span style={{ fontWeight: 400, color: subtitleColor, marginLeft: '4px' }}>
      （カード表示に使用）
    </span>
  </label>
  <input
    type="text"
    value={editDescription}
    onChange={(e) => setEditDescription(e.target.value)}
    maxLength={200}
    placeholder="例: コードレビューを行う AI ペルソナ"
    style={inputStyle}
  />
</div>
<div style={{ marginBottom: '16px' }}>
  <label
    style={{
      display: 'block',
      fontSize: '0.875rem',
      fontWeight: 600,
      color: textColor,
      marginBottom: '4px',
    }}
  >
    Knowledge
    <span style={{ fontWeight: 400, color: subtitleColor, marginLeft: '4px' }}>
      （システムプロンプトに追加される知識）
    </span>
  </label>
  <textarea
    value={editKnowledge}
    onChange={(e) => setEditKnowledge(e.target.value)}
    maxLength={8000}
    rows={3}
    placeholder="ドメイン固有の知識・ルール・参照情報を記述してください"
    style={{ ...inputStyle, resize: 'vertical' }}
  />
</div>
```

このブロックは既存の System Prompt フィールドと Save ボタンの間に挿入する。

#### 2-h: 新規作成フォームにも Description / Knowledge フィールドを追加

新規作成フォームの System Prompt フィールドの**後**、Type セレクトの**前**に同様のフィールドを追加する：

```tsx
<div style={{ marginBottom: '12px' }}>
  <label
    style={{
      display: 'block',
      fontSize: '0.875rem',
      fontWeight: 600,
      color: textColor,
      marginBottom: '4px',
    }}
  >
    Description
    <span style={{ fontWeight: 400, color: subtitleColor, marginLeft: '4px' }}>
      （カード表示に使用）
    </span>
  </label>
  <input
    type="text"
    value={createDescription}
    onChange={(e) => setCreateDescription(e.target.value)}
    maxLength={200}
    placeholder="例: コードレビューを行う AI ペルソナ"
    style={inputStyle}
  />
</div>
<div style={{ marginBottom: '12px' }}>
  <label
    style={{
      display: 'block',
      fontSize: '0.875rem',
      fontWeight: 600,
      color: textColor,
      marginBottom: '4px',
    }}
  >
    Knowledge
    <span style={{ fontWeight: 400, color: subtitleColor, marginLeft: '4px' }}>
      （システムプロンプトに追加される知識）
    </span>
  </label>
  <textarea
    value={createKnowledge}
    onChange={(e) => setCreateKnowledge(e.target.value)}
    maxLength={8000}
    rows={3}
    placeholder="ドメイン固有の知識・ルール・参照情報を記述してください"
    style={{ ...inputStyle, resize: 'vertical' }}
  />
</div>
```

**Verify:**

```bash
cd /home/parallels/workspaces/copilot-langgraph/frontend && bun run build 2>&1 | tail -10
```

TypeScript エラー0件でビルドが成功すること。

---

## Verification

- [ ] `cd /home/parallels/workspaces/copilot-langgraph/frontend && bun run build` が成功（エラー0件）
- [ ] `GemInfo` に `description: string` と `knowledge: string` が存在する
- [ ] `GemCreate` に `description: string` と `knowledge: string` が存在する
- [ ] GemsScreen のカード表示が `gem.description` を表示する（説明なしの場合はイタリック体の「説明なし」）
- [ ] GemsScreen の作成フォームに Description フィールドと Knowledge テキストエリアが表示される
- [ ] GemsScreen の編集フォームに Description フィールドと Knowledge テキストエリアが表示される
- [ ] Gem を新規作成・編集して保存後、description / knowledge が正しく保存・反映される
