---
phase: 11-rpccontext-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/orchestrator/context.py
  - tests/test_rpc_context.py
autonomous: true
requirements:
  - CONTEXT-02
  - CONTEXT-03

must_haves:
  truths:
    - "RPCContext is a frozen dataclass — field assignment raises FrozenInstanceError"
    - "RPCContext.from_http() constructs context with user_id, app_id, thread_id and auto-generated correlation_id"
    - "RPCContext.from_slack() constructs context from Slack event dict"
    - "_keep_first(existing, new) returns existing; _keep_first(None, new) returns new"
  artifacts:
    - path: "app/orchestrator/context.py"
      provides: "RPCContext dataclass + _keep_first reducer"
      exports: ["RPCContext", "_keep_first"]
    - path: "tests/test_rpc_context.py"
      provides: "Unit tests for RPCContext and _keep_first"
      min_lines: 50
  key_links: []
---

<objective>
Create the RPCContext frozen dataclass and _keep_first reducer function in a new module, plus the Wave 0 test file covering all RPCContext unit behaviors.

Purpose: RPCContext is the foundation type used by all subsequent plans. Tests must exist before implementation (Nyquist).
Output: app/orchestrator/context.py with RPCContext + _keep_first, tests/test_rpc_context.py with unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/pre/agent_architecture_additions.md (section 17 — RPCContext spec)
@.planning/phases/11-rpccontext-integration/11-RESEARCH.md
@.planning/phases/11-rpccontext-integration/11-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave 0 test stubs + RPCContext implementation</name>
  <files>tests/test_rpc_context.py, app/orchestrator/context.py</files>
  <behavior>
    - test_rpccontext_frozen: RPCContext(user_id="u1") is frozen — assigning ctx.user_id = "x" raises dataclasses.FrozenInstanceError
    - test_rpccontext_defaults: RPCContext(user_id="u1") has app_id="", thread_id="", and a non-empty UUID correlation_id
    - test_rpccontext_correlation_id_unique: Two RPCContext instances have different correlation_ids
    - test_from_http_factory: RPCContext.from_http(user_id="alice", app_id="superchat", thread_id="t1") produces correct fields with auto-generated correlation_id
    - test_from_slack_factory: RPCContext.from_slack({"user": "U123", "thread_ts": "ts1", "ts": "ts2"}) produces user_id="U123", thread_id="ts1"; without thread_ts falls back to ts
    - test_keep_first_preserves_existing: _keep_first(ctx_a, ctx_b) returns ctx_a
    - test_keep_first_none_first_arg: _keep_first(None, ctx_b) returns ctx_b (handles unset checkpoint case)
  </behavior>
  <action>
    1. Create tests/test_rpc_context.py with all 7 test cases above. Tests import from app.orchestrator.context.
    2. Run tests — they MUST fail (RED) because context.py does not exist yet.
    3. Create app/orchestrator/context.py following the design doc (section 17-6 of agent_architecture_additions.md):
       - `_keep_first(a, b)`: returns `a if a is not None else b` (handles None first-arg per Pitfall 3 in RESEARCH.md)
       - `@dataclass(frozen=True) class RPCContext`: fields user_id (str), app_id (str, default ""), thread_id (str, default ""), correlation_id (str, default_factory=lambda: str(uuid.uuid4()))
       - `@classmethod from_http(cls, user_id: str, app_id: str, thread_id: str) -> RPCContext`: takes explicit kwargs (NOT raw HTTP headers — the worker never has raw request; per RESEARCH.md Open Question 1)
       - `@classmethod from_slack(cls, event: dict) -> RPCContext`: user_id=event["user"], thread_id=event.get("thread_ts", event["ts"])
    4. Run tests — all MUST pass (GREEN).

    Note: The design doc section 17-6 defines extra fields (user_roles, message_id, session_id). Per RESEARCH.md recommendation, implement the minimal set needed for Phase 11 (user_id, app_id, thread_id, correlation_id). Additional fields can be added in future phases.
  </action>
  <verify>
    <automated>docker compose exec backend pytest tests/test_rpc_context.py -x -q</automated>
  </verify>
  <done>
    - app/orchestrator/context.py exports RPCContext and _keep_first
    - All 7 test cases pass
    - RPCContext is frozen (FrozenInstanceError on mutation)
    - from_http and from_slack produce correct field values
    - _keep_first handles both normal and None-first-arg cases
  </done>
</task>

</tasks>

<verification>
docker compose exec backend pytest tests/test_rpc_context.py -v
</verification>

<success_criteria>
- RPCContext dataclass is frozen and has 4 fields (user_id, app_id, thread_id, correlation_id)
- _keep_first reducer handles None gracefully
- from_http and from_slack factory methods produce correct instances
- All unit tests green
</success_criteria>

<output>
After completion, create `.planning/phases/11-rpccontext-integration/11-01-SUMMARY.md`
</output>
