---
phase: quick
plan: 260401-f4x
type: execute
wave: 1
depends_on: []
files_modified: [.gitignore]
autonomous: true
---

<objective>
Review and update .gitignore to properly exclude data files (SQLite DB), IDE settings, Claude worktrees, and other common Python/project artifacts.

Purpose: Prevent accidental commits of database files, IDE config, and tool-generated directories.
Output: Updated .gitignore with comprehensive exclusions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.gitignore
Current .gitignore has basic Python exclusions and token/secret files, but is missing:
- `data/` directory (contains chat.db SQLite files — runtime data, not source)
- `.vscode/` (IDE settings, per-developer)
- `.claude/` (Claude Code worktrees — tool-generated)
- `.pytest_cache/` (test cache)
- `.env` / `.env.*` (environment variable files)
- `*.db` / `*.db-shm` / `*.db-wal` (SQLite files anywhere in tree)
- `.mypy_cache/`, `.ruff_cache/` (linter/type checker caches)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .gitignore with comprehensive exclusions</name>
  <files>.gitignore</files>
  <action>
Rewrite .gitignore with organized sections. Keep all existing entries and add missing ones:

1. **Python** (existing): __pycache__/, *.pyc, *.pyo, *.pyd, .venv/, *.egg-info/, dist/, build/, .eggs/
2. **Secrets** (existing): .enc_key, token.enc, ~/.copilot_sdk/
3. **Environment variables** (new): .env, .env.*, !.env.example
4. **Data / SQLite** (new): data/, *.db, *.db-shm, *.db-wal
5. **IDE / Editor** (new): .vscode/, .idea/, *.swp, *.swo, *~
6. **Tool caches** (new): .pytest_cache/, .mypy_cache/, .ruff_cache/, .coverage, htmlcov/
7. **Claude Code** (new): .claude/
8. **OS** (new): .DS_Store, Thumbs.db

Use clear section comment headers (# Section Name) for readability.
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && git check-ignore data/chat.db .vscode/settings.json .claude/worktrees .pytest_cache .env && echo "ALL PATTERNS MATCH"</automated>
  </verify>
  <done>.gitignore covers data files, IDE settings, tool caches, Claude worktrees, env files, and SQLite databases. git check-ignore confirms all key paths are ignored.</done>
</task>

</tasks>

<verification>
- `git check-ignore` confirms data/, .vscode/, .claude/, .pytest_cache/, .env are all ignored
- Existing exclusions (secrets, Python bytecode) still present
- No source files accidentally excluded
</verification>

<success_criteria>
- .gitignore updated with organized, commented sections
- All runtime/generated directories (data/, .claude/, .pytest_cache/, .vscode/) properly excluded
- SQLite files excluded regardless of location
- Environment variable files excluded
</success_criteria>

<output>
After completion, create `.planning/quick/260401-f4x-gitignore/260401-f4x-SUMMARY.md`
</output>
