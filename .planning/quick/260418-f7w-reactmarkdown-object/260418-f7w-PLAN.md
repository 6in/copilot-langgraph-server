---
phase: quick-260418-f7w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/routes/chat.py
  - frontend/src/components/MarkdownMessage.tsx
  - frontend/src/components/MessageArea.tsx
autonomous: false
requirements: [QUICK-260418-f7w]
must_haves:
  truths:
    - "SuperChat で Phase 28 CodeAct 導入後に作成されたスレッドの履歴をクリックしても白画面にならない"
    - "ToolMessage は履歴に含まれない（ユーザーに見せる必要のない内部メッセージ）"
    - "構造化コンテンツ（list[dict]）を持つ AIMessage はテキスト部分を抽出して表示される"
    - "過去の DB に既に保存されている壊れたチェックポイントも、フロントエンドのガードで白画面にならずにレンダリングされる"
    - "Copy all ボタンも content が非 string でクラッシュしない"
  artifacts:
    - path: "app/api/routes/chat.py"
      provides: "_messages_to_response が ToolMessage を除外し、非 string content を string に正規化"
      contains: "isinstance(msg, ToolMessage)"
    - path: "frontend/src/components/MarkdownMessage.tsx"
      provides: "content prop が string でない場合に防御的に string 化するガード"
      contains: "typeof content"
  key_links:
    - from: "app/api/routes/chat.py::_messages_to_response"
      to: "frontend ReactMarkdown children prop"
      via: "GET /api/threads/{thread_id}/messages response"
      pattern: "entry\\[.content.\\]"
---

<objective>
SuperChat でチャット履歴をクリックすると白画面になる不具合を、バックエンドのコンテンツ正規化とフロントエンドの防御的ガードの二段構え（defense-in-depth）で修正する。

Purpose: Phase 28 CodeAct 導入以降、チェックポイントに `ToolMessage` や structured content（list[dict]）を持つ AIMessage が蓄積されており、`_messages_to_response` が `msg.content` をそのまま返すため ReactMarkdown の `children must be string` アサーションが発火して UI 全体がクラッシュする。
- バックエンド正規化: 新規リクエストは string 形式でフロントに届くようにする（根本治療）
- フロントエンドガード: 既に DB に保存されている過去のチェックポイントからもクラッシュしないように守る（後方互換）

Output:
- `app/api/routes/chat.py` — `_messages_to_response` を ToolMessage 除外 + content 正規化に更新。debate ブランチのインラインコピーも同じロジックを使うよう共通化
- `frontend/src/components/MarkdownMessage.tsx` — content が string でない場合に防御的に string 化
- `frontend/src/components/MessageArea.tsx::CopyAllButton` — 同様のガード
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/todos/completed/2026-04-18-chat-history-white-screen-markdown-object.md
@app/api/routes/chat.py
@frontend/src/components/MarkdownMessage.tsx
@frontend/src/components/MessageArea.tsx

<interfaces>
<!-- LangChain message content types (langchain_core.messages.BaseMessage) -->

- `HumanMessage.content: str` — 常に string
- `SystemMessage.content: str` — 常に string（_messages_to_response で既に除外済み）
- `AIMessage.content: str | list[dict]` — Copilot SDK からの応答は通常 string だが、CodeAct / tool_use を含む応答は `[{"type": "text", "text": "..."}, {"type": "tool_use", ...}]` のような構造化 list になる
- `ToolMessage.content: str | list[dict]` — ツール実行結果。ユーザーに直接見せるべきではない内部メッセージ
- インポート済み: `from langchain_core.messages import HumanMessage, SystemMessage` — `ToolMessage` は追加 import が必要

<!-- Frontend props -->

- `MarkdownMessage` props: `{ content: string }`（型定義上は string だが、API レスポンスから untyped で流れ込むと任意型になりうる）
- `ChatMessage.content: string`（types.ts で string 定義）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: バックエンドで ToolMessage を除外し、非 string content を正規化</name>
  <files>app/api/routes/chat.py</files>
  <action>
`app/api/routes/chat.py` の `_messages_to_response` ヘルパー（L402-413）を以下のように書き換える:

1. 先頭の import 行（L22）に `ToolMessage` を追加:
   ```python
   from langchain_core.messages import HumanMessage, SystemMessage, ToolMessage
   ```

2. モジュールトップレベルに、型不定の content を string に正規化する純粋関数を追加（`_messages_to_response` の上、router 定義の後あたり）:
   ```python
   def _normalize_content(content) -> str:
       """Normalize BaseMessage.content to a string for UI rendering.

       Copilot SDK usually returns str, but structured content (list[dict])
       appears when tool_use blocks or CodeAct results are included in the
       AIMessage. ReactMarkdown requires a string children prop — passing
       an object crashes the entire chat UI (assertion error).

       - str                  -> return as-is
       - list[dict] (LC v2)   -> concatenate "text" blocks, skip non-text blocks
       - anything else        -> json.dumps fallback (never return non-str)
       """
       if isinstance(content, str):
           return content
       if isinstance(content, list):
           parts: list[str] = []
           for block in content:
               if isinstance(block, dict):
                   # LangChain structured content: {"type": "text", "text": "..."}
                   if block.get("type") == "text" and isinstance(block.get("text"), str):
                       parts.append(block["text"])
                   # tool_use / tool_result / image_url などは履歴 UI には出さない
               elif isinstance(block, str):
                   parts.append(block)
           if parts:
               return "\n".join(parts)
           # 構造化ブロックがあるが text が無い場合は JSON ダンプで可視化
           try:
               return json.dumps(content, ensure_ascii=False)
           except Exception:
               return str(content)
       # dict / その他型 — JSON ダンプ
       try:
           return json.dumps(content, ensure_ascii=False)
       except Exception:
           return str(content)
   ```

3. `_messages_to_response` 本体（L402-413）を次のように更新:
   - `ToolMessage` を SystemMessage と同様に `continue` でスキップ
   - `entry["content"]` を `_normalize_content(msg.content)` に通す

   ```python
   def _messages_to_response(raw_messages: list) -> list[dict]:
       messages = []
       for msg in raw_messages:
           # System prompts and internal tool results are not shown in chat history UI
           if isinstance(msg, (SystemMessage, ToolMessage)):
               continue
           role = "user" if isinstance(msg, HumanMessage) else "ai"
           entry: dict = {"role": role, "content": _normalize_content(msg.content)}
           sender = getattr(msg, "name", None)
           if sender:
               entry["senderName"] = sender
           messages.append(entry)
       return messages
   ```

4. Debate ブランチのインラインループ（L494-502）も同じロジックを再利用するよう `_messages_to_response(debate_msgs)` に置き換える。現状 debate は SystemMessage も通す設計ではない（HumanMessage 以外は全部 ai 扱い）ため、共通化しても挙動差分は「SystemMessage と ToolMessage がフィルタされる」のみで、これは望ましい方向の変更。

   具体的には L494-502 の
   ```python
   debate_msgs = debate_state.values.get("messages", [])
   messages = []
   for msg in debate_msgs:
       role = "user" if isinstance(msg, HumanMessage) else "ai"
       entry: dict = {"role": role, "content": msg.content}
       sender = getattr(msg, "name", None)
       if sender:
           entry["senderName"] = sender
       messages.append(entry)
   return {"messages": messages, "thread_id": thread_id}
   ```
   を
   ```python
   debate_msgs = debate_state.values.get("messages", [])
   messages = _messages_to_response(debate_msgs)
   return {"messages": messages, "thread_id": thread_id}
   ```
   に差し替える。

注意:
- `json` は既に L12 で import 済みなので追加不要
- `ToolMessage.content` が string の場合でも除外する設計。ツール実行の詳細はログやストリーミングで既に見えており、履歴 UI に出す必要はない（todo のオプション 3「ToolMessage は履歴表示から除外」に対応）
- orchestrator ブランチが先に処理される（L416）ので、SystemMessage/ToolMessage フィルタは全 3 パス（orchestrator/chat/debate）で一貫して効く
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph &amp;&amp; docker compose exec -T api python -c "
from app.api.routes import chat as chat_mod
from langchain_core.messages import HumanMessage, AIMessage, SystemMessage, ToolMessage

# 1. _normalize_content
assert chat_mod._normalize_content('hello') == 'hello'
assert chat_mod._normalize_content([{'type': 'text', 'text': 'abc'}, {'type': 'tool_use', 'id': 'x'}]) == 'abc'
assert chat_mod._normalize_content([{'type': 'text', 'text': 'a'}, {'type': 'text', 'text': 'b'}]) == 'a\nb'
out = chat_mod._normalize_content([{'type': 'tool_use', 'id': 'x'}])
assert isinstance(out, str) and 'tool_use' in out
assert chat_mod._normalize_content({'k': 'v'}) == '{\"k\": \"v\"}'

# 2. _messages_to_response: ToolMessage を除外、structured content を string 化
msgs = [
    SystemMessage(content='sys prompt'),
    HumanMessage(content='hi'),
    AIMessage(content=[{'type': 'text', 'text': 'hello'}, {'type': 'tool_use', 'id': 'x', 'name': 'search'}]),
    ToolMessage(content='tool output', tool_call_id='x'),
    AIMessage(content='final reply'),
]
result = chat_mod._messages_to_response(msgs)
assert len(result) == 3, result
assert result[0] == {'role': 'user', 'content': 'hi'}
assert result[1]['role'] == 'ai' and result[1]['content'] == 'hello'
assert result[2]['role'] == 'ai' and result[2]['content'] == 'final reply'
assert all(isinstance(m['content'], str) for m in result)
print('OK: backend normalization works')
"</automated>
  </verify>
  <done>
- `_normalize_content` が新規追加され、str / list[dict]（text block 抽出）/ その他型（json.dumps）を安全に string に変換する
- `_messages_to_response` が SystemMessage と ToolMessage をスキップし、残りの content を `_normalize_content` で正規化する
- debate ブランチのインラインコピーが `_messages_to_response` 呼び出しに統合されている
- verify コマンドの assertion 全パス
  </done>
</task>

<task type="auto">
  <name>Task 2: フロントエンドで非 string content を防御的に string 化</name>
  <files>frontend/src/components/MarkdownMessage.tsx, frontend/src/components/MessageArea.tsx</files>
  <action>
過去の壊れたチェックポイント（既に DB に保存されている ToolMessage / structured content）が履歴 API から流れてきた場合でも白画面にならないよう、フロント側で最後の砦となるガードを入れる。

**`frontend/src/components/MarkdownMessage.tsx`:**

L292-402 の `MarkdownMessage` コンポーネント本体の直前（L291 の `});` の後、L292 の `export const MarkdownMessage = memo(...)` の中）で、render の前に content を string 化する処理を追加:

```tsx
export const MarkdownMessage = memo(function MarkdownMessage({ content }: MarkdownMessageProps) {
  const theme = useCurrentTheme();
  const monacoTheme = theme === 'dark' ? 'vs-dark' : 'vs';

  // Defense-in-depth: ReactMarkdown requires a string children prop. Old threads in DB
  // (pre-260418 fix) may have structured content (list[dict]) persisted in checkpoints;
  // stringify defensively so the entire chat UI never blanks out.
  const safeContent = typeof content === 'string'
    ? content
    : (() => {
        try {
          return JSON.stringify(content, null, 2);
        } catch {
          return String(content);
        }
      })();

  const components = useMemo(() => ({
    // ... (既存の components 定義そのまま)
```

そして最後の ReactMarkdown 呼び出し（L397-399）の children を `safeContent` に差し替え:
```tsx
<ReactMarkdown remarkPlugins={[remarkGfm]} components={components}>
  {safeContent}
</ReactMarkdown>
```

`MarkdownMessageProps` の型定義（L45-47）は `content: string` のままでよい（型システム上は string が期待される、実行時ガードは防御用）。`// eslint-disable-next-line @typescript-eslint/no-explicit-any` などは追加不要。

**`frontend/src/components/MessageArea.tsx`:**

L67-101 の `CopyAllButton` 内、L74 の
```tsx
const content = m.content.replace(/\|/g, '\\|').replace(/\n/g, '<br>');
```
を以下のように修正（非 string 入力でも落ちないように）:
```tsx
const rawContent = typeof m.content === 'string'
  ? m.content
  : (() => { try { return JSON.stringify(m.content); } catch { return String(m.content); } })();
const content = rawContent.replace(/\|/g, '\\|').replace(/\n/g, '<br>');
```

同様に L250 の `<CopyButton text={msg.content} />` と L298 の `<CopyButton text={msg.content} />` は props 型が `text: string` のためそのまま渡しても TypeScript は通るが、実行時 `navigator.clipboard.writeText(value)` は非 string を暗黙変換するので問題は起きない（今回は CopyAll のみ修正すれば十分）。

注意:
- `types.ts` の `ChatMessage.content: string` 型定義は変更しない（API 正規化で string 化される前提が正）
- `useChat` などのフックで content を組み立てる箇所は修正対象外（バックエンドが string で返すことを保証した上で、フロントは防御層のみ）
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph/frontend &amp;&amp; docker compose run --rm --no-deps frontend bun run build 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
- `MarkdownMessage` が content が string でない場合に `JSON.stringify` で可視化して ReactMarkdown に渡す（クラッシュしない）
- `CopyAllButton` も `m.content` が非 string でも `.replace()` で throw しない
- `bun run build` が TypeScript エラーなく通る
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
バックエンド `_messages_to_response` の ToolMessage 除外 + 非 string content 正規化、およびフロントエンド `MarkdownMessage` / `CopyAllButton` の防御的 string ガード
  </what-built>
  <how-to-verify>
1. 開発環境を再起動: `docker compose restart api worker frontend`（api と worker は chat.py 変更反映のため両方必要）
2. ブラウザで `http://localhost:5173/orochi/` を開き、ログイン
3. メニューから SuperChat を選択
4. **サイドバーから既存の SuperChat スレッド（できれば Phase 28 CodeAct 導入後に作成されたもの — 例えば「Python コードを実行して…」のようなプロンプトのスレッド）をクリック**
   - 期待: 白画面にならず、メッセージが正常に表示される
   - ToolMessage の内部出力は表示されない（AIMessage のテキスト部分のみ見える）
5. 新規スレッドで「1 から 10 までの和を Python で計算して」のようなプロンプトを送信（CodeAct ツールを呼ぶ）
6. 応答完了後、別のスレッドに切り替えて戻る
   - 期待: 履歴がクラッシュせず、AIMessage の最終テキストが表示される
7. 「Copy all」ボタンをクリック
   - 期待: 何もエラーにならず、クリップボードに Markdown テーブルが入る
8. ブラウザコンソールに `Uncaught Assertion: Unexpected value [object Object]` のエラーが出ていないことを確認

もし再現用の既存スレッドが手元にない場合:
- 代替検証: DevTools Network タブで `GET /api/threads/{id}/messages` のレスポンス JSON を確認し、すべての `messages[].content` が string 型であることを確認
  </how-to-verify>
  <resume-signal>修正が動作していれば「approved」、問題があれば症状を記述</resume-signal>
</task>

</tasks>

<verification>
- バックエンド: `docker compose exec api python -c "..."` の正規化単体テスト（Task 1 verify）が全パス
- フロントエンド: `bun run build` が通る
- 統合: Task 3 の手動 UAT で、従来再現していた白画面が発生しないことを確認
</verification>

<success_criteria>
- SuperChat で Phase 28 CodeAct 導入後のスレッド履歴をクリックしても白画面にならない（primary bug fixed）
- `GET /api/threads/{id}/messages` レスポンスの `messages[].content` が常に string 型
- ToolMessage は履歴 UI に表示されない
- 過去の DB に保存された壊れたチェックポイント（structured content 含む）があっても、フロントのガードにより UI がクラッシュしない
- `bun run build` と Python 単体テストが通る
</success_criteria>

<output>
After completion, create `.planning/quick/260418-f7w-reactmarkdown-object/260418-f7w-SUMMARY.md`
</output>
