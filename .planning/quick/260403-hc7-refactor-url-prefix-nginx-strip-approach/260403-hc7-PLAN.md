---
phase: quick
plan: 260403-hc7
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/api/client.ts
  - frontend/vite.config.ts
  - app/api/main.py
  - docs/nginx.md
autonomous: true
must_haves:
  truths:
    - "Frontend API calls use relative paths (./api/...) so browser resolves them correctly under any prefix"
    - "Vite base config respects VITE_APP_BASE env var for asset URL prefix at build time"
    - "Vite dev proxy handles prefixed paths and strips prefix before forwarding to FastAPI"
    - "FastAPI root_path is set from APP_PREFIX env var for OpenAPI docs awareness"
    - "VITE_BASE_URL is fully removed — no references remain in client.ts"
    - "docs/nginx.md provides working nginx reverse-proxy config example"
  artifacts:
    - path: "frontend/src/api/client.ts"
      provides: "Relative API paths, no VITE_BASE_URL"
      contains: "./api/"
    - path: "frontend/vite.config.ts"
      provides: "Vite base config + proxy rewrite"
      contains: "VITE_APP_BASE"
    - path: "app/api/main.py"
      provides: "FastAPI root_path from APP_PREFIX"
      contains: "root_path"
    - path: "docs/nginx.md"
      provides: "Nginx reverse-proxy documentation"
  key_links:
    - from: "frontend/src/api/client.ts"
      to: "browser URL resolution"
      via: "relative paths (./api/...)"
      pattern: "\\./api/"
    - from: "frontend/vite.config.ts"
      to: "dev proxy"
      via: "VITE_APP_BASE prefix in proxy key + rewrite"
      pattern: "VITE_APP_BASE"
---

<objective>
Refactor URL prefix handling to the nginx-strip approach: remove VITE_BASE_URL from the frontend,
switch to relative API paths, configure Vite base for asset prefixing, add FastAPI root_path,
and document the nginx reverse-proxy pattern.

Purpose: Enable clean sub-path deployment (e.g. /orochi/) where nginx strips the prefix before
forwarding to FastAPI — no route changes needed, browser resolves relative paths correctly.

Output: Updated client.ts, vite.config.ts, main.py, and new docs/nginx.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/api/client.ts
@frontend/vite.config.ts
@app/api/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor frontend to relative paths and configure Vite base</name>
  <files>frontend/src/api/client.ts, frontend/vite.config.ts</files>
  <action>
**frontend/src/api/client.ts:**
- Delete the line `const BASE_URL = import.meta.env.VITE_BASE_URL ?? '';`
- Replace the top-of-file comment block (lines 1-5) with:
  ```
  // frontend/src/api/client.ts
  // Typed fetch wrappers for all backend endpoints.
  // All API paths are RELATIVE (./api/...) so the browser resolves them against
  // the current page origin + Vite's `base` path. This means:
  //   - Dev: Vite proxy intercepts and forwards to FastAPI
  //   - Prod behind nginx: nginx strips the prefix, FastAPI sees /api/...
  //   - No hardcoded prefix needed in JS code
  ```
- In `apiFetch()`, change `fetch(\`${BASE_URL}${path}\`, ...)` to `fetch(path, ...)`
- Change ALL `/api/...` string literals to `./api/...` (add leading dot):
  - `/api/auth/status` -> `./api/auth/status`
  - `/api/auth/start` -> `./api/auth/start`
  - `/api/auth/poll?flow_id=...` -> `./api/auth/poll?flow_id=...`
  - `/api/auth/logout` -> `./api/auth/logout`
  - `/api/me` -> `./api/me`
  - `/api/chat` -> `./api/chat`
  - `/api/job/${...}` -> `./api/job/${...}`
  - `/api/chat/${...}/stream` -> `./api/chat/${...}/stream`
  - `/api/threads` -> `./api/threads`
  - `/api/threads/${...}` -> `./api/threads/${...}` (DELETE, PATCH, messages)
- In `streamJob()`: change `new EventSource(\`${BASE_URL}/api/chat/...\`)` to `new EventSource(\`./api/chat/...\`)`
- In `deleteThread()`: change `fetch(\`${BASE_URL}/api/threads/...\`)` to `fetch(\`./api/threads/...\`)`

**frontend/vite.config.ts:**
- Add `base: process.env.VITE_APP_BASE ?? '/'` to the defineConfig object (top-level, alongside plugins and server)
- Change proxy key from `'/api'` to a template: `` [`${process.env.VITE_APP_BASE ?? ''}/api`] ``
  Note: JS computed property syntax required here.
- Add `rewrite` to the proxy config:
  ```ts
  rewrite: (path) =>
    path.replace(
      new RegExp('^' + (process.env.VITE_APP_BASE ?? '')),
      '',
    ),
  ```
- Add comment above proxy block: `// VITE_APP_BASE=/orochi in .env to dev with prefix; rewrite strips it before forwarding`
- Keep existing `target` and `changeOrigin` settings
- Update the existing NO rewrite comment to reflect the new rewrite purpose
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && grep -c "VITE_BASE_URL" frontend/src/api/client.ts | grep "^0$" && grep -c "\./api/" frontend/src/api/client.ts | grep -v "^0$" && grep "VITE_APP_BASE" frontend/vite.config.ts && grep "root_path" app/api/main.py; cd frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
- VITE_BASE_URL completely removed from client.ts (zero occurrences)
- All API paths in client.ts start with ./api/ (relative)
- apiFetch uses path directly without prefix concatenation
- Vite config has base property reading VITE_APP_BASE
- Vite proxy key includes VITE_APP_BASE prefix with rewrite to strip it
- TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add FastAPI root_path and create nginx docs</name>
  <files>app/api/main.py, docs/nginx.md</files>
  <action>
**app/api/main.py:**
- Add near top (after the DB_URI line, around line 30):
  ```python
  # root_path tells FastAPI its public URL prefix (for OpenAPI docs /docs, /redoc).
  # nginx strips the prefix before forwarding, so all routes stay at /api/...
  APP_PREFIX = os.getenv("APP_PREFIX", "")
  ```
- Change line 74 from:
  `app = FastAPI(title="Copilot Chat", lifespan=lifespan)`
  to:
  `app = FastAPI(title="Copilot Chat", lifespan=lifespan, root_path=APP_PREFIX)`

**docs/nginx.md (new file):**
Create a concise reference document with:

1. **Overview** — nginx strips the path prefix (e.g. /orochi) before forwarding to FastAPI. FastAPI routes remain unchanged at /api/... The `root_path` setting only affects OpenAPI docs URLs.

2. **Nginx config example** — location block for /orochi/ with:
   - `proxy_pass http://backend:8000/;` (trailing slash = strip prefix)
   - `proxy_set_header Host $host;`
   - `proxy_set_header X-Real-IP $remote_addr;`
   - `proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;`
   - `proxy_set_header X-Forwarded-Proto $scheme;`
   - `proxy_set_header X-Forwarded-Prefix /orochi;`

3. **Environment variables** table:
   | Variable | Where | Example | Purpose |
   - APP_PREFIX: FastAPI, /orochi, OpenAPI docs prefix
   - VITE_APP_BASE: Vite build, /orochi, Asset URL prefix in built HTML

4. **Dev vs Prod** section:
   - Dev: `VITE_APP_BASE=/orochi bun run dev` — Vite serves at :5173, proxy rewrites prefix
   - Prod: `APP_PREFIX=/orochi uvicorn app.api.main:app` — FastAPI serves frontend/dist at /react, static at /

5. **Docker Compose snippet** — nginx service with volume-mounted config, depends_on backend
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && python -c "import ast; ast.parse(open('app/api/main.py').read()); print('syntax ok')" && grep "root_path=APP_PREFIX" app/api/main.py && test -f docs/nginx.md && grep "proxy_pass" docs/nginx.md</automated>
  </verify>
  <done>
- app/api/main.py has APP_PREFIX env var and root_path=APP_PREFIX in FastAPI constructor
- Python syntax valid
- docs/nginx.md exists with nginx config example, env var table, dev/prod notes, and Docker Compose snippet
  </done>
</task>

</tasks>

<verification>
- `grep -r "VITE_BASE_URL" frontend/src/` returns zero matches (fully removed)
- `grep -c "\./api/" frontend/src/api/client.ts` returns non-zero (relative paths present)
- `grep "VITE_APP_BASE" frontend/vite.config.ts` shows base config
- `grep "root_path" app/api/main.py` shows FastAPI root_path
- `cd frontend && npx tsc --noEmit` passes (no type errors)
- `python -c "import ast; ast.parse(open('app/api/main.py').read())"` passes (valid Python)
- `docs/nginx.md` exists and contains proxy_pass example
</verification>

<success_criteria>
1. Zero occurrences of VITE_BASE_URL in the entire frontend/src/ directory
2. All API calls in client.ts use relative paths (./api/...)
3. Vite config supports VITE_APP_BASE for both base path and dev proxy
4. FastAPI root_path set from APP_PREFIX env var
5. docs/nginx.md provides complete reverse-proxy setup reference
6. No TypeScript or Python syntax errors introduced
</success_criteria>

<output>
After completion, create `.planning/quick/260403-hc7-refactor-url-prefix-nginx-strip-approach/260403-hc7-SUMMARY.md`
</output>
