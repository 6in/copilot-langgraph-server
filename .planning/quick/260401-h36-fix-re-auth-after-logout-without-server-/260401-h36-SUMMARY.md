---
phase: quick
plan: 260401-h36
subsystem: auth
tags: [bug-fix, logout, re-auth, frontend, ux]
dependency_graph:
  requires: []
  provides: [logout-resets-client, dismissible-logout-overlay]
  affects: [app/api/routes/auth.py, static/app.js]
tech_stack:
  added: []
  patterns: [llm.close() resets ChatCopilot._client for re-auth without restart]
key_files:
  created: []
  modified:
    - app/api/routes/auth.py
    - static/app.js
    - .planning/STATE.md
    - .planning/todos/done/2026-04-01-waiting-for-authentication.md
decisions:
  - "logout() must call llm.close() so _ensure_client() re-initializes with new token on next chat"
  - "Overlay is dismissible via inline onclick on button — no event listener needed for single-use element"
metrics:
  duration: 5min
  completed: 2026-04-01
  tasks: 2
  files: 4
---

# Quick Task 260401-h36: Fix Re-Auth After Logout Without Server Restart — Summary

**One-liner:** Logout now calls `llm.close()` to reset `ChatCopilot._client`, enabling in-browser re-auth without server restart; overlay and confirm dialog updated to remove misleading restart instructions.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Reset ChatCopilot client on logout + update frontend UX | 1b4bf70 | app/api/routes/auth.py, static/app.js |
| 2 | Move todo from pending to done, update STATE.md | 2f16ed0 | .planning/todos/done/*, .planning/STATE.md |

## What Was Done

### Task 1 — Backend + Frontend Fix

**Backend (`app/api/routes/auth.py`):**
- Added `await request.app.state.llm.close()` in `logout()` after `device_flows.clear()`
- `ChatCopilot.close()` sets `_client = None`, so `_ensure_client()` re-initializes with the newly obtained token on the next chat request
- Updated docstring to reflect that re-auth works in-browser without restart
- Changed response messages: removed "Restart the server to re-authenticate." from both branches

**Frontend (`static/app.js`):**
- Simplified logout confirm dialog: removed "You will need to restart the server to re-authenticate."
- Replaced logout overlay content: removed restart instruction, updated copy to direct users to "Login with GitHub" header link
- Added `<button class="logout-dismiss-btn">` with inline `onclick` to remove the overlay from the DOM

### Task 2 — Housekeeping

- Moved `.planning/todos/pending/2026-04-01-waiting-for-authentication.md` to `.planning/todos/done/`
- Updated STATE.md: cleared Pending Todos section, added 260401-h36 to Quick Tasks Completed table

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None.

## Self-Check: PASSED

- `app/api/routes/auth.py` — contains `await request.app.state.llm.close()`, no "Restart" text
- `static/app.js` — contains `logout-dismiss-btn`, no "restart" text (case-insensitive)
- `.planning/todos/done/2026-04-01-waiting-for-authentication.md` — exists
- `.planning/todos/pending/2026-04-01-waiting-for-authentication.md` — does not exist
- Commits 1b4bf70 and 2f16ed0 — both present in git log
