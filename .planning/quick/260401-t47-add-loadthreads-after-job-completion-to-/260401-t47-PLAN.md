---
phase: quick
plan: 260401-t47
type: execute
wave: 1
depends_on: []
files_modified: [static/app.js]
autonomous: true
requirements: [QUICK-T47]

must_haves:
  truths:
    - "Thread sidebar refreshes after AI response completes via immediate-done path"
    - "Thread sidebar refreshes after AI response completes via SSE done event"
    - "Thread sidebar refreshes after AI response completes via polling fallback"
  artifacts:
    - path: "static/app.js"
      provides: "loadThreads() calls at all 3 job completion points"
      contains: "await loadThreads()"
  key_links:
    - from: "sendMessage() completion paths"
      to: "loadThreads()"
      via: "await call before/after appendMessage"
      pattern: "loadThreads"
---

<objective>
Add `await loadThreads()` at all 3 job completion points in `sendMessage()` so the sidebar
thread list refreshes after the first message creates a new thread checkpoint.

Purpose: When a user sends the first message (auto-creating a thread), the thread only appears
in GET /api/threads after the first checkpoint is written (job completion). Currently the sidebar
never refreshes at that point, so the new thread is invisible until manual page reload.

Output: Updated static/app.js with loadThreads() calls at all completion points.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@static/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add loadThreads() at all 3 job completion points in sendMessage()</name>
  <files>static/app.js</files>
  <action>
Add `await loadThreads()` at the following 3 locations in static/app.js. Place it AFTER
hideTyping() and BEFORE the input re-enable block in each case:

1. **Immediate done check** (around line 376-384): Inside the `if (immediate.status === 'done')`
   block, add `await loadThreads();` after `appendMessage('ai', immediate.result || '');` and
   before the input re-enable lines.

2. **SSE onmessage done** (around line 392-407): Inside the `if (status === 'done')` block in
   `es.onmessage`, add `await loadThreads();` after the appendMessage/error block (after line 401)
   and before the input re-enable lines.

3. **Polling done** (around line 437-445): Inside the `if (job.status === 'done')` block in the
   `startPolling` interval callback, add `await loadThreads();` after `appendMessage('ai', job.result || '');`
   and before the input re-enable lines. Note: the interval callback is already `async () => {}` so
   `await` works here.

Do NOT modify the existing `loadThreads()` call at line 371 (thread_id assignment case) — that
handles a different scenario.
  </action>
  <verify>
    <automated>grep -n "await loadThreads()" static/app.js | wc -l</automated>
    Expect output: 4 (1 existing at ~line 371 + 3 new ones). Also verify with:
    grep -n "await loadThreads()" static/app.js
    to confirm placement in the 3 completion blocks plus the existing one.
  </verify>
  <done>
    All 3 job completion paths in sendMessage() call loadThreads() so the sidebar refreshes
    when a new thread's first checkpoint is written. The 4 total calls are: (1) thread_id
    assignment, (2) immediate done, (3) SSE done, (4) polling done.
  </done>
</task>

</tasks>

<verification>
1. `grep -c "await loadThreads()" static/app.js` returns 4
2. Manual test: open app, start with no active thread, send a message. After AI responds,
   the new thread should appear in the sidebar without page reload.
</verification>

<success_criteria>
- static/app.js contains `await loadThreads()` at all 3 completion points
- No other functionality is broken (existing loadThreads call at thread_id assignment preserved)
- Sidebar refreshes after job completion in all 3 code paths
</success_criteria>

<output>
After completion, create `.planning/quick/260401-t47-add-loadthreads-after-job-completion-to-/260401-t47-SUMMARY.md`
</output>
