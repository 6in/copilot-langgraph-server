---
phase: quick-260406-f9k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/jobs/handlers/langgraph_handler.py
autonomous: true
requirements:
  - チャットのコンテキストにてユーザー情報も入れるようにする
must_haves:
  truths:
    - "LangGraph グラフ実行時の config[\"configurable\"] に github_login が含まれる"
    - "github_login の値は job payload から取得され、未設定の場合は 'unknown' がフォールバックとして使われる"
  artifacts:
    - path: "app/jobs/handlers/langgraph_handler.py"
      provides: "github_login を configurable に追加した LangGraphHandler"
      contains: "github_login.*configurable"
  key_links:
    - from: "job payload"
      to: "config[\"configurable\"]"
      via: "job.get(\"github_login\", \"unknown\")"
      pattern: "github_login.*configurable"
---

<objective>
LangGraphHandler の config["configurable"] に github_login を追加し、LangGraph グラフ実行時にユーザー情報が渡るようにする。

Purpose: RPCContext / LangGraph の checkpointer やノード内でユーザー識別情報を参照できるようにするための基盤整備。
Output: config["configurable"] に thread_id と github_login の両方が含まれた langgraph_handler.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: config["configurable"] に github_login を追加する</name>
  <files>app/jobs/handlers/langgraph_handler.py</files>
  <action>
    `LangGraphHandler.handle` メソッド内の以下の行を変更する。

    現状 (62行目):
    ```python
    config = {"configurable": {"thread_id": thread_id}}
    ```

    変更後:
    ```python
    github_login = job.get("github_login", "unknown")
    config = {"configurable": {"thread_id": thread_id, "github_login": github_login}}
    ```

    あわせて、85行目の既存の `github_login = job.get("github_login", "unknown")` は削除する
    (canvas_apps upsert のブロック内で再代入しているが、上で定義した変数を使えばよいため重複する)。

    変更後のコード全体の flow:
    1. `github_login` を job から取得 (62行目直後あたりで)
    2. `config` に `github_login` を含めてセット
    3. canvas_apps upsert ブロック内の重複した `github_login = ...` 行を削除
  </action>
  <verify>
    以下の grep が空でないこと (configurable に github_login が存在する):
    `grep -n "github_login.*configurable\|configurable.*github_login" app/jobs/handlers/langgraph_handler.py`

    85行目付近に重複した `github_login = job.get(...)` が残っていないこと:
    `grep -c "github_login = job.get" app/jobs/handlers/langgraph_handler.py` が 1 を返すこと

    Python 構文エラーがないこと:
    `python -c "import ast; ast.parse(open('app/jobs/handlers/langgraph_handler.py').read()); print('OK')"`
  </verify>
  <done>
    - config["configurable"] が {"thread_id": thread_id, "github_login": github_login} を含む
    - github_login = job.get("github_login", "unknown") の行が 1 か所のみ存在する (重複なし)
    - Python 構文チェックが OK を返す
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job payload → configurable | arq ワーカーが受け取った job dict の github_login をグラフ設定に渡す |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-f9k-01 | Tampering | job["github_login"] | accept | arq ジョブは JWT 検証済みの chat.py ルートでエンキューされ、github_login は JWT から抽出済み (Quick 260403-oo9 の決定による)。ワーカー内での再検証は不要。 |
</threat_model>

<verification>
`python -c "import ast; ast.parse(open('app/jobs/handlers/langgraph_handler.py').read()); print('OK')"` が OK を返すこと。
</verification>

<success_criteria>
- config["configurable"] に thread_id と github_login が含まれている
- github_login の重複定義が解消されている
- Python 構文エラーなし
</success_criteria>

<output>
完了後、`.planning/quick/260406-f9k-langgraph-handler-py-config-configurable/260406-f9k-SUMMARY.md` を作成する。
</output>
