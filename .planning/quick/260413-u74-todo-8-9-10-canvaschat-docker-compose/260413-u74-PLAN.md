---
phase: quick-260413-u74
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/CanvasChatApp.tsx
  - frontend/Dockerfile
  - docker-compose.prod.yml
  - docker/nginx/nginx.conf
  - app/graph/builder.py
  - app/jobs/handlers/langgraph_handler.py
autonomous: false
requirements:
  - TODO-8
  - TODO-9
  - TODO-10
must_haves:
  truths:
    - "CanvasChat でアプリをデプロイするとチャットエリアに『デプロイ完了』通知メッセージが追加される"
    - "デプロイ通知メッセージにはデプロイ URL が含まれ、クリックで新規タブに開ける"
    - "docker compose -f docker-compose.prod.yml up で本番モード（nginx + ビルド済みフロント + API + worker + mcp）が起動する"
    - "本番モードで http://localhost/orochi/ にアクセスするとチャット UI が表示され、/orochi/api/* が FastAPI に到達する"
    - "ユーザーが『私の名前は？』と聞いたとき、AI が認証済み github_login を答える"
  artifacts:
    - path: frontend/src/components/CanvasChatApp.tsx
      provides: "デプロイ成功時に setMessages でデプロイ通知を追加する handleDeploy ハンドラ"
    - path: frontend/Dockerfile
      provides: "multi-stage build: bun build → nginx:alpine で静的配信する production stage"
    - path: docker-compose.prod.yml
      provides: "api/worker/postgres/redis/mcp-server/frontend(prod)/nginx サービス定義"
    - path: docker/nginx/nginx.conf
      provides: "/orochi プレフィックスを strip して /api を api:8000 に、それ以外を frontend:80 にプロキシ"
    - path: app/graph/builder.py
      provides: "SYSTEM_PROMPT に github_login を動的注入する chatbot_node"
  key_links:
    - from: "CanvasChatApp.tsx deployCanvas 完了"
      to: "setMessages (MessageArea)"
      via: "handleDeploy ラッパー"
      pattern: "setMessages.*デプロイ"
    - from: "docker-compose.prod.yml nginx service"
      to: "frontend:80 + api:8000"
      via: "nginx.conf proxy_pass"
      pattern: "proxy_pass"
    - from: "langgraph_handler.py config"
      to: "chatbot_node via configurable.github_login"
      via: "build_graph chatbot_node が config から github_login を読み SystemMessage 注入"
      pattern: "configurable.*github_login"
---

<objective>
Todo #8 (CanvasChat デプロイ時チャットポスト), Todo #9 (本番モード Docker Compose), Todo #10 (チャット履歴にユーザー名) を一括で解消する。

Purpose: 社内 200 名展開前に、本番モード検証経路を用意し、Canvas デプロイ履歴と認証コンテキストが LLM に渡るようにする。
Output: CanvasChatApp 修正、frontend Dockerfile multi-stage 化、docker-compose.prod.yml + nginx.conf 新規作成、builder.py に github_login 注入ロジック追加。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@frontend/src/components/CanvasChatApp.tsx
@frontend/src/hooks/useCanvas.ts
@frontend/Dockerfile
@docker-compose.yml
@app/graph/builder.py
@app/jobs/handlers/langgraph_handler.py
@app/api/routes/chat.py

<interfaces>
From frontend/src/hooks/useCanvas.ts:
```typescript
deployCanvas: (appId: string) => Promise<string>;  // returns deploy URL
```

From frontend/src/components/CanvasPane.tsx の props:
```typescript
onDeploy: (appId: string) => Promise<string>;  // CanvasChatApp が渡す (現状は deployCanvas 直渡し)
```

From frontend/src/types.ts (既存):
```typescript
interface ChatMessage { role: 'user' | 'ai'; content: string; senderName?: string }
```

From app/graph/builder.py:
```python
async def chatbot_node(state: MessagesState, config: RunnableConfig) -> dict:
    gem_system_prompt = (config or {}).get("configurable", {}).get("system_prompt", "")
    # ← ここに github_login を追加する
```

From app/jobs/handlers/langgraph_handler.py:
```python
config = {"configurable": {
    "thread_id": thread_id,
    "github_login": github_login,  # 既に渡されている
    "system_prompt": system_prompt or "",
}}
```
</interfaces>

**重要な既存状態:**
- `langgraph_handler.py` は既に `github_login` を `config.configurable` に渡している（line 76）
- しかし `builder.py` の chatbot_node は `github_login` を読んでいない → これが Todo #10 の根本原因
- `docker-compose.yml` は dev only（Vite dev server）。本番モード用が存在しない
- `CanvasChatApp.tsx` は `deployCanvas` を直接 `CanvasPane` に渡している（handleDeploy ラッパーなし）
</context>

<tasks>

<task type="auto">
  <name>Task 1: CanvasChat デプロイ完了時にチャットメッセージをポスト (Todo #8)</name>
  <files>frontend/src/components/CanvasChatApp.tsx</files>
  <action>
`CanvasChatApp.tsx` に `handleDeploy` ラッパーを追加し、`CanvasPane` の `onDeploy` に渡す（現状は `deployCanvas` を直接渡している）。

実装:
1. `useThreads` の返り値から `setMessages` を既に取得済み — これを利用する
2. `handleDeploy` 関数を定義:
   ```typescript
   const handleDeploy = useCallback(async (appId: string): Promise<string> => {
     const url = await deployCanvas(appId);
     // デプロイ通知メッセージをチャットエリアに追加
     // - URL はクリッカブル（Markdown リンク）
     // - 現在の HTML スニペット（先頭200文字）を含める
     const htmlSnippet = currentHtml ? currentHtml.slice(0, 200) + (currentHtml.length > 200 ? '...' : '') : '';
     const deployMessage = `🚀 **デプロイ完了**\n\nURL: [${url}](${url})\n\n\`\`\`html\n${htmlSnippet}\n\`\`\``;
     setMessages((prev) => [...prev, { role: 'ai', content: deployMessage, senderName: 'System' }]);
     return url;
   }, [deployCanvas, currentHtml, setMessages]);
   ```
3. `<CanvasPane ... onDeploy={handleDeploy} />` に差し替え（line 289）
4. `useCallback` を既存の import から取得（既に import 済み）

注意:
- LangGraph checkpointer に反映するには `/api/chat` 経由で送信する必要があるが、今回はフロント UI の即時表示のみ対応する（次回スレッド再オープン時に履歴として残るかは `GET /api/threads/{id}/messages` の挙動次第 — これはフロント state のみの追加なので DB には残らない）
- DB 永続化は将来対応 — 本タスクでは **UI 上の即時表示** のみ実装し、コメントで「DB 永続化は TODO」と明記する
- デプロイ失敗時（`deployCanvas` throw）はメッセージを追加しない（try/catch で url が取れた場合のみ push）
  </action>
  <verify>
    <automated>cd frontend && bun run build 2>&1 | tail -20</automated>
  </verify>
  <done>
- `handleDeploy` ラッパーが追加されている
- `CanvasPane` の `onDeploy` prop に `handleDeploy` が渡されている
- デプロイ成功時に `setMessages` が呼ばれ `🚀 デプロイ完了` を含むメッセージが追加される
- TypeScript ビルドエラーなし
  </done>
</task>

<task type="auto">
  <name>Task 2: 本番モード Docker Compose 整備 (Todo #9)</name>
  <files>frontend/Dockerfile, docker-compose.prod.yml, docker/nginx/nginx.conf</files>
  <action>
**1. `frontend/Dockerfile` を multi-stage build に書き換える:**

```dockerfile
# Stage 1: dev (既存の node:22-alpine を維持して docker-compose.yml は動作継続)
FROM node:22-alpine AS dev
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]

# Stage 2: builder (Bun でビルド)
FROM oven/bun:1-alpine AS builder
WORKDIR /app
COPY package*.json bun.lockb* ./
RUN bun install --frozen-lockfile || bun install
COPY . .
ARG VITE_APP_BASE=/orochi
ENV VITE_APP_BASE=$VITE_APP_BASE
RUN bun run build

# Stage 3: prod (nginx で静的ファイル配信)
FROM nginx:1.27-alpine AS prod
COPY --from=builder /app/dist /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

既存の `docker-compose.yml`（dev）は `target` 指定なしで `dev` ステージを使うよう `target: dev` を `frontend` service の `build` セクションに追記する（既存挙動維持）。

**2. `docker/nginx/nginx.conf` を新規作成:**

```nginx
events { worker_connections 1024; }

http {
  include /etc/nginx/mime.types;
  default_type application/octet-stream;
  sendfile on;
  keepalive_timeout 65;

  upstream api_backend { server api:8000; }
  upstream frontend_backend { server frontend:80; }

  server {
    listen 80;
    server_name _;

    # /orochi プレフィックスを strip
    # docs/nginx.md の方式に合わせる — trailing slash で location prefix を削除
    location /orochi/api/ {
      proxy_pass http://api_backend/api/;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      # SSE 用 buffering 無効化
      proxy_buffering off;
      proxy_cache off;
      proxy_read_timeout 600s;
    }

    location /orochi/ {
      proxy_pass http://frontend_backend/;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
    }

    location / {
      return 301 /orochi/;
    }
  }
}
```

**3. `docker-compose.prod.yml` を新規作成:**

```yaml
# 本番モード compose — nginx + ビルド済みフロント + API + worker + MCP
# 起動: docker compose -f docker-compose.prod.yml up --build
services:
  postgres:
    image: pgvector/pgvector:pg17
    environment:
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_DB=postgres
    volumes:
      - ./docker/initdb:/docker-entrypoint-initdb.d
      - postgres-data-prod:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    volumes:
      - redis-data-prod:/data

  mcp-server:
    build:
      context: ./mcp_server
      dockerfile: Dockerfile
    working_dir: /mcp_server
    volumes:
      - ./mcp_server:/mcp_server
      - ./config:/mcp_server/config:ro
      - claude-code-outputs-prod:/shared/claude-code-outputs
    environment:
      - TAVILY_API_KEY=${TAVILY_API_KEY}
      - DB_POOLS_CONFIG=/mcp_server/config/db_pools.yaml
      - CLAUDE_CODE_OUTPUT_DIR=/shared/claude-code-outputs
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "python3 -c \"import urllib.request; urllib.request.urlopen('http://localhost:8001/health')\""]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  api:
    image: ghcr.io/astral-sh/uv:python3.12-bookworm-slim
    working_dir: /app
    command: uv run uvicorn app.api.main:app --host 0.0.0.0 --port 8000
    environment:
      - REDIS_URL=redis://redis:6379
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/postgres?sslmode=disable
      - AGENT_DIR=/app/agents
      - MENU_DIR=/app/menus
      - APP_DIR=/app/apps
      - APP_PREFIX=/orochi
    volumes:
      - .:/app
      - ~/.copilot_sdk:/root/.copilot_sdk
      - ./config:/app/config:ro
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started

  worker:
    image: ghcr.io/astral-sh/uv:python3.12-bookworm-slim
    working_dir: /app
    command: uv run arq app.jobs.worker.WorkerSettings
    environment:
      - REDIS_URL=redis://redis:6379
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/postgres?sslmode=disable
      - AGENT_DIR=/app/agents
      - MENU_DIR=/app/menus
      - APP_DIR=/app/apps
      - MCP_SERVER_URL=http://mcp-server:8001
    volumes:
      - .:/app
      - ~/.copilot_sdk:/root/.copilot_sdk
      - ./config:/app/config:ro
      - claude-code-outputs-prod:/shared/claude-code-outputs:ro
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
      mcp-server:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: prod
      args:
        VITE_APP_BASE: /orochi
    depends_on:
      - api

  nginx:
    image: nginx:1.27-alpine
    volumes:
      - ./docker/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
    depends_on:
      - frontend
      - api

volumes:
  redis-data-prod:
  postgres-data-prod:
  claude-code-outputs-prod:
```

**4. `docker-compose.yml` (既存 dev) の `frontend` service の `build` セクションに `target: dev` を追記:**

```yaml
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: dev        # ← 追記
```

注意:
- `APP_PREFIX=/orochi` は FastAPI 側で root_path として解釈される（docs/nginx.md 参照）
- nginx が `/orochi/api/` → `/api/` に strip するので FastAPI のルートは `/api/...` のまま
- dev compose との衝突を避けるため、volume 名にサフィックス `-prod` を付ける
- HTTPS 対応は今回対象外（社内利用、リバプロは別途）
  </action>
  <verify>
    <automated>docker compose -f docker-compose.prod.yml config > /dev/null && cd frontend && docker build --target prod -t frontend-prod-check . 2>&1 | tail -5</automated>
  </verify>
  <done>
- `frontend/Dockerfile` が multi-stage（dev / builder / prod）になっている
- 既存 `docker-compose.yml` の frontend service に `target: dev` が追記されている
- `docker-compose.prod.yml` と `docker/nginx/nginx.conf` が作成されている
- `docker compose -f docker-compose.prod.yml config` がエラーなく通る
- `docker build --target prod ./frontend` が成功する
  </done>
</task>

<task type="auto">
  <name>Task 3: チャット履歴に github_login を注入 (Todo #10)</name>
  <files>app/graph/builder.py</files>
  <action>
`build_graph` と `build_canvas_graph` の `chatbot_node` が `config.configurable.github_login` を読み取り、SystemMessage として注入するように修正する。

実装:
1. `SYSTEM_PROMPT` 定数はそのまま維持
2. `chatbot_node` 内で:
   ```python
   async def chatbot_node(state: MessagesState, config: RunnableConfig) -> dict:
       configurable = (config or {}).get("configurable", {})
       gem_system_prompt = configurable.get("system_prompt", "")
       github_login = configurable.get("github_login", "")

       system_messages = [SYSTEM_PROMPT]
       if github_login and github_login != "unknown":
           system_messages.append(SystemMessage(content=(
               f"The authenticated user's GitHub login is: {github_login}. "
               f"When the user asks about their name, identity, or who they are, "
               f"answer with this GitHub username."
           )))
       if gem_system_prompt:
           system_messages.append(SystemMessage(content=gem_system_prompt))

       response = await llm.ainvoke(system_messages + state["messages"])
       return {"messages": [response]}
   ```
3. `build_canvas_graph` の `chatbot_node` も同様に修正（`_trim_html_history(state["messages"])` を維持）

注意:
- `langgraph_handler.py` は既に `github_login` を config に渡している → 修正不要
- `github_login == "unknown"` の場合は注入しない（匿名扱い）
- SystemMessage は state に追加しないので履歴には残らないが、毎回 config 経由で注入されるので継続する
- `debate_graph.py` や `orchestrator/graph.py` は Todo 10 のスコープ外（ChatApp での動作確認が先）
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/copilot-langgraph && python -c "from app.graph.builder import build_graph, build_canvas_graph; print('import OK')"</automated>
  </verify>
  <done>
- `build_graph` の `chatbot_node` が `github_login` を config から読んで SystemMessage 注入
- `build_canvas_graph` の `chatbot_node` も同様
- `github_login == "unknown"` で注入スキップ
- Python import エラーなし
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 3 Todo の手動検証</name>
  <what-built>
- Todo #8: CanvasChat デプロイ完了時にチャットエリアへ通知メッセージが追加される
- Todo #9: `docker compose -f docker-compose.prod.yml up --build` で本番モードが起動する
- Todo #10: ユーザーが自分の名前を聞くと AI が github_login を答える
  </what-built>
  <how-to-verify>
**Todo #10 (開発モードで検証):**
1. `docker compose up -d` で dev モードを起動
2. http://localhost:5173/orochi/ でログイン
3. 新規チャットで「私のGitHubユーザー名は？」と質問
4. AI がログイン中の GitHub ユーザー名を返すことを確認

**Todo #8 (開発モードで検証):**
1. Canvas チャットでシンプルな HTML アプリを生成（例:「カウンターアプリ作って」）
2. CanvasPane で Deploy ボタンをクリック
3. デプロイ成功後、チャットエリアに `🚀 デプロイ完了` メッセージが追加され URL がクリッカブルであることを確認
4. URL をクリックして別タブで動作することを確認

**Todo #9 (本番モード検証):**
1. `docker compose down` で dev を停止
2. `docker compose -f docker-compose.prod.yml up --build` を実行
3. 全サービスが healthy になるまで待つ
4. http://localhost/orochi/ にブラウザでアクセス
5. ログイン画面が表示されること（静的ファイルが nginx から配信）
6. Device Flow ログインが動作すること（`/orochi/api/auth/*` が FastAPI に到達）
7. チャット送信 → SSE でレスポンスが返ること（`/orochi/api/chat/*/stream` が動作）
8. 確認できたら `docker compose -f docker-compose.prod.yml down` で停止
  </how-to-verify>
  <resume-signal>「approved」または発見した問題を記述</resume-signal>
</task>

</tasks>

<verification>
- フロントエンドビルド: `cd frontend && bun run build`
- Python import: `python -c "from app.graph.builder import build_graph, build_canvas_graph"`
- Compose 構文: `docker compose -f docker-compose.prod.yml config`
- Dockerfile prod stage: `docker build --target prod ./frontend`
- 手動検証: Task 4 の human-verify チェックポイント
</verification>

<success_criteria>
- [ ] Canvas デプロイ完了時にチャットエリアへ通知メッセージがポストされる (Todo #8)
- [ ] 本番モード Docker Compose が起動し http://localhost/orochi/ で動作する (Todo #9)
- [ ] AI がログイン中ユーザーの github_login を認識して返答する (Todo #10)
- [ ] 既存 dev compose (docker compose up) は影響を受けず動作継続
- [ ] 全タスクの automated verify コマンドが成功
</success_criteria>

<output>
After completion, create `.planning/quick/260413-u74-todo-8-9-10-canvaschat-docker-compose/260413-u74-SUMMARY.md`
</output>
