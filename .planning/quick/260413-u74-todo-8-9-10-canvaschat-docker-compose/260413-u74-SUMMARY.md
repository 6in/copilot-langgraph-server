# Quick Task 260413-u74: Todo #8・#9・#10 一括対応

**完了日:** 2026-04-13
**コミット:** 8dabbeb, 3b54cb2, badb59e

## 実施内容

### Task 1 — Todo #8: CanvasChat デプロイ完了通知 (commit: 8dabbeb)

`CanvasChatApp.tsx` に `handleDeploy` ラッパーを追加。
- `deployCanvas` の完了後、`🚀 **デプロイ完了**` メッセージと URL・HTML スニペットをチャット履歴に挿入
- `setMessages` で即時 UI 反映
- `currentHtml` を参照して最新 HTML の先頭 200 字をスニペットとして埋め込む

### Task 2 — Todo #9: 本番モード Docker Compose 整備 (commit: 3b54cb2)

- `frontend/Dockerfile` を multi-stage build に変更（`dev` stage + `prod` stage）
- `docker-compose.prod.yml` を新規作成（nginx + ビルド済みフロント + API + worker + MCP + postgres + redis）
- `docker/nginx/nginx.conf` を新規作成（`/orochi` プレフィックスストリップ、`/api` プロキシ）
- `docker-compose.yml` に `target: dev` を追記して後方互換維持

起動コマンド: `docker compose -f docker-compose.prod.yml up --build`

### Task 3 — Todo #10: チャット履歴ユーザー名注入 (commit: badb59e)

`app/graph/builder.py` の `build_graph` / `build_canvas_graph` 両 `chatbot_node` で `configurable.github_login` を読み取り SystemMessage として注入。
- `langgraph_handler.py` は既に `github_login` を configurable に渡していた（フロント・API 変更不要）
- `"The authenticated user's GitHub login is: {github_login}"` をシステムプロンプトに追加

## 変更ファイル

| ファイル | 変更内容 |
|---------|---------|
| `frontend/src/components/CanvasChatApp.tsx` | handleDeploy ラッパー追加 |
| `frontend/Dockerfile` | multi-stage build 化 |
| `docker-compose.prod.yml` | 新規作成（本番用） |
| `docker/nginx/nginx.conf` | 新規作成 |
| `docker-compose.yml` | `target: dev` 追記 |
| `app/graph/builder.py` | github_login SystemMessage 注入 |
