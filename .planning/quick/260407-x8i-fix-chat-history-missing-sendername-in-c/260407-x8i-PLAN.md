---
id: 260407-x8i
type: quick
autonomous: true
files_modified:
  - app/api/routes/chat.py
---

<objective>
Fix GET /threads/{thread_id}/messages regular path to include senderName in each message entry.

The debate fallback path (line ~412-418) already reads `msg.name` into `senderName`, but the regular LangGraph state path (line ~352-354) omits it. The frontend already handles `senderName` — the fix is purely in the backend.
</objective>

<context>
@app/api/routes/chat.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add senderName to regular path message entries</name>
  <files>app/api/routes/chat.py</files>
  <action>
At line 352-354 in the regular state path, change the message dict construction to include `senderName` when `msg.name` is set. Mirror the debate fallback pattern exactly.

Current code (line 352-354):
```python
for msg in state.values["messages"]:
    role = "user" if isinstance(msg, HumanMessage) else "ai"
    messages.append({"role": role, "content": msg.content})
```

Replace with:
```python
for msg in state.values["messages"]:
    role = "user" if isinstance(msg, HumanMessage) else "ai"
    entry: dict = {"role": role, "content": msg.content}
    sender = getattr(msg, "name", None)
    if sender:
        entry["senderName"] = sender
    messages.append(entry)
```
  </action>
  <verify>grep -n "senderName" /home/parallels/workspaces/copilot-langgraph/app/api/routes/chat.py</verify>
  <done>Both the regular path (around line 354) and the debate fallback path include senderName assignment from msg.name.</done>
</task>

</tasks>

<success_criteria>
- `grep -n "senderName" app/api/routes/chat.py` shows at least two occurrences (regular path + debate path)
- No syntax errors: `python -c "import ast; ast.parse(open('app/api/routes/chat.py').read())"` exits 0
</success_criteria>

<output>
No SUMMARY file needed for this single-line fix.
</output>
