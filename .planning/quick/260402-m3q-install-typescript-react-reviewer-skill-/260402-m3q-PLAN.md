---
phase: quick
plan: 260402-m3q
type: execute
wave: 1
depends_on: []
files_modified: [CLAUDE.md]
autonomous: true
requirements: []
must_haves:
  truths:
    - "typescript-react-reviewer skill is installed globally"
    - "CLAUDE.md Architecture section accurately describes the actual project structure"
    - "CLAUDE.md Frontend tech stack reflects React 19 + TypeScript + Vite + Bun, not Vanilla JS"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated architecture and tech stack documentation"
      contains: "React 19"
  key_links: []
---

<objective>
Install the typescript-react-reviewer skill globally and update CLAUDE.md to accurately reflect the current project architecture, folder structure, and frontend tech stack (React 19 + TypeScript + Vite replacing the outdated Vanilla JS references).

Purpose: The project has evolved through 7 phases and CLAUDE.md is stale -- it still says "Vanilla JS" and "Architecture not yet mapped." Accurate CLAUDE.md ensures Claude sessions get correct context.
Output: Updated CLAUDE.md, installed skill
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install typescript-react-reviewer skill globally</name>
  <files>(no project files modified)</files>
  <action>
Run the following command to install the skill globally:

```bash
npx skills add dotneet/claude-code-marketplace@typescript-react-reviewer -g -y
```

Verify it installed by checking the global skills directory exists.
  </action>
  <verify>
    <automated>ls ~/.claude/skills/typescript-react-reviewer/SKILL.md 2>/dev/null || ls ~/.skills/typescript-react-reviewer/SKILL.md 2>/dev/null || echo "Check npx skills list output"</automated>
  </verify>
  <done>Skill is installed globally and accessible to Claude Code</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md with accurate architecture and tech stack</name>
  <files>CLAUDE.md</files>
  <action>
Update CLAUDE.md with the following changes:

1. **Frontend tech stack table**: Replace the "Vanilla JS + HTML/CSS" row with the actual current stack:
   - React 19.2 + ReactDOM 19.2 (SPA)
   - TypeScript 5.9
   - Vite 8.0 (dev server + bundler)
   - @chatscope/chat-ui-kit-react 2.1 (chat UI components)
   - react-markdown 10.1 + remark-gfm + rehype-highlight (markdown rendering)
   - Bun (package manager, used via docker)
   Keep the Jinja2 row but note it is used for the legacy Vanilla JS UI at `/`.

2. **Alternatives Considered table**: Update the Frontend row -- React is now the chosen frontend, not an alternative. Note that Vanilla JS remains as a legacy UI served at `/`.

3. **Architecture section** (between `GSD:architecture-start` and `GSD:architecture-end`): Replace "Architecture not yet mapped" with a clear description covering:

   **Backend (Python / FastAPI)**:
   ```
   app/
     api/
       main.py          — FastAPI app factory, lifespan, CORS, static mounts
       models.py         — Pydantic request/response models
       routes/
         auth.py         — Device Flow OAuth + JWT login/logout
         chat.py         — POST /api/chat (enqueue), GET /api/chat/history
         jobs.py         — GET /api/job/{id}, GET /api/job/{id}/stream (SSE)
         me.py           — GET /api/me (GitHub user info)
     auth/
       jwt_utils.py      — JWT HS256 encode/decode, JTI blocklist
       manager.py        — CopilotAuthManager (Device Flow + token encryption)
     graph/
       builder.py        — LangGraph StateGraph (MessagesState) compilation
     jobs/
       job_store.py      — In-memory job result store with asyncio.Queue SSE
       notifier.py       — SSE notification bridge (worker -> client)
       worker.py         — arq worker: process_chat task
     providers/
       copilot.py        — ChatCopilot (BaseChatModel wrapper for Copilot SDK)
   ```

   **Frontend (React 19 + TypeScript + Vite)**:
   ```
   frontend/
     src/
       App.tsx            — Root component, AuthContext.Provider
       main.tsx           — ReactDOM entry point
       types.ts           — Shared TypeScript types (Thread, Message, etc.)
       api/
         client.ts        — apiFetch wrapper (JWT cookie auth)
       components/
         AuthPanel.tsx    — Device Flow login UI
         ChatApp.tsx      — Main chat layout (sidebar + messages)
         Header.tsx       — App header with user info
         MarkdownMessage.tsx — Markdown rendering with syntax highlighting
         MessageArea.tsx  — Message list + input (chatscope)
         ThreadSidebar.tsx — Thread list with CRUD
       hooks/
         useAuth.ts       — Auth state management
         useChat.ts       — Chat messaging + SSE job polling
         useThreads.ts    — Thread CRUD operations
   ```

   **Infrastructure**:
   - Docker Compose: FastAPI backend + PostgreSQL (langgraph-checkpoint-postgres) + Redis (arq worker queue) + React frontend (Bun/Vite)
   - Legacy Vanilla JS UI served at `/` via FastAPI StaticFiles (static/ directory)
   - React UI served at `/app` (Vite dev server proxies /api to backend)

   **Key Patterns**:
   - Async-first: all routes are `async def`, arq worker for background jobs
   - SSE for job completion notification (not WebSocket)
   - JWT HS256 in httpOnly cookie for auth
   - ChatCopilot wraps Copilot SDK behind BaseChatModel interface
  </action>
  <verify>
    <automated>grep -q "React 19" CLAUDE.md && grep -q "app/api/main.py" CLAUDE.md && grep -q "frontend/src/App.tsx" CLAUDE.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>CLAUDE.md Architecture section contains accurate folder structure for both backend and frontend. Frontend tech stack table reflects React 19 + TypeScript + Vite instead of Vanilla JS.</done>
</task>

</tasks>

<verification>
- `grep "Architecture not yet mapped" CLAUDE.md` returns nothing (old placeholder removed)
- `grep "React 19" CLAUDE.md` returns matches (new stack documented)
- typescript-react-reviewer skill is accessible globally
</verification>

<success_criteria>
1. typescript-react-reviewer skill installed globally
2. CLAUDE.md Architecture section documents the actual backend (app/) and frontend (frontend/src/) structure
3. CLAUDE.md Frontend tech stack table reflects React 19 + TypeScript + Vite, not Vanilla JS
</success_criteria>

<output>
After completion, create `.planning/quick/260402-m3q-install-typescript-react-reviewer-skill-/260402-m3q-SUMMARY.md`
</output>
